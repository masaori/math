# MEMORY.md — cellular-automata-statistical-mechanics

> 着手前に [README.md](README.md)（このプロジェクトのゴール設定）を読むこと。
> 可算コア／$\mathbb{R}$ 脱出の一次情報は
> [`docs/discussion/対数順序群上の統計力学/`](../docs/discussion/対数順序群上の統計力学/) と
> [`docs/discussion/可算性の効用/`](../docs/discussion/可算性の効用/)。

## 研究姿勢の正本化（2026-08-14）

[`docs/マニフェスト.md`](docs/マニフェスト.md) を新設した。既存の量子論・場の量子論・相対論を
CA へ移植するのではなく、有限規則から内在的な数学構造を先に抽出し、既存理論は後から明示的な
写像で比較する。実数・複素数を含む非可算構造は説明能力の高い理想化された近似でありうる、という
研究上の前提と、数学的な近似主張には対象・比較写像・誤差または収束概念が要る、という証明上の
規律を分離した。CA 自体も正解として先取りせず、非対応・反例を成果として保存する。

## 区間記法は母集合を添字で明示する（2026-08-15）

自然数区間を実数区間と同じ無添字の $[a,b]$ で書くと、定義後に断りを置いても読者を
ミスリードする。構造化論文では $[a,b]_{\mathbb{N}}$ のように母集合を必ず添字で示す。
既存の自然数区間表記を本文・検算概要・Lean 注釈・台帳で統一し、構造化本文の無添字区間を
`tools/validate-content.ts` が拒むようにした。

## 30 分自動ループの開始（2026-08-14）

`docs/tasks/auto-loop-runbook.md` と `auto-loop-state.md` を正本に、毎時 12 分・42 分に独立した
エージェントを起動する。専用 worktree `~/git/masaori/math-cellular-automata-loop` を使い、
Claude と Codex を交互に動かす。各 tick は前回成果をレビューした後、構造化記述・SageMath・
Lean 具体版・Lean 必要十分版と導出のうち一層だけを進め、`origin/main` への包含まで確認する。

最初の探索列は、物理的意味を入れず、局所真理値表の本質的依存台から始める。冗長近傍への不変性、
有限時間展開上の直接依存、到達可能性の反対称性、有限伝播境界を順に扱い、その後で初めて既存の
因果構造と比較する。

## 自動ループ tick: 構造化証明基盤の初期化と本質的依存台の記述（2026-08-14）

`structured-latex/` を初期化した（システムはリポジトリ直下を利用、`schema.ts`・tools は
`exact-solution-of-2d-ising-model-lambda` の流儀を流用。住処 `habitat`/`realEscape` を型で強制。
Lean 未着手のため lean 連携検査は未設置）。最初の構造化記述
`structured-latex/content/essential-dependency.ts` に、局所真理値表の本質的依存台の定義と
「本質的依存 ⟺ 一点反転検査」の同値、有限決定可能性（高々 $|S|\cdot 2^{|S|}$ 回の比較）を
証明つきで書いた。`pnpm run check`・`pnpm run build:pdf` 通過。**SageMath・Lean は未着手**
（状況は `docs/tasks/auto-loop-state.md` が正本）。用語上の判断: 近傍 $N(v)$ は順序のない
集合なので「座標」でなく「近傍の元」と呼ぶ（番号付けがある舞台に限り「座標」を許す）。
独立再検証で、有限集合・写像を habitat "N" とした分類を訂正した。元の個数を扱うブロックだけを
"N"、有限集合・写像・述語を扱う残りを "finite" とする。schema には今後の可算集合用に
"countable" も追加した。負の型テストの一時生成物も ignore し、検証後に worktree を汚さない。

## 自動ループ tick: 本質的依存台の SageMath 検算（2026-08-14）

前 tick の構造化証明をレビューし、2 元集合の入れ替え写像、本質的依存の存在量化から一点反転検査への
同値、依存台を得る走査の比較回数上界に修正を要する誤りは見つからなかった。
`sagemath/check/essential-dependency-support/` に論理段ごとの検算を追加し、$|S|=0,1,2,3$ の
全 278 真理値表を厳密全数検査した。$|S|=3$ は初等 CA と同じ入力数を持つ 256 規則を全て含む。
本質的依存と一点反転検査、走査結果と定義どおりの依存台は全件で一致し、比較回数は
$|S|2^{|S|}$ 以下だった。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

構造化記述の両 claim から検算ディレクトリへの対応を付け、`sagemath/tools/verify-check-linkage.ts` を
追加した。構造化記述以前の Clifford 塔の探索検算は「未昇格」と明示し、構造化ラベル付き検算と
混同しない。**現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。**
次 tick は前層をレビューした後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。

## 自動ループ tick: 本質的依存台の Lean 具体版（2026-08-15）

前 tick の SageMath 検算と構造化記述を突き合わせ、誤りは見つからなかった。`lean/` を初期化し
（lib 名 `CellularAutomata`、toolchain・mathlib は既存プロジェクトと同じ v4.32.1、依存は
`exact-solution-of-2d-ising-model-lambda` の `.lake` を clone copy で持ち込む運用。README に明記）、
`lean/CellularAutomata/EssentialDependency.lean` に人手証明と 1 対 1 の具体版を書いた。
状態集合は演算を入れない 2 元帰納型（`Bool`・`Fin 2` の既成演算を流用しない）、ν は値の場合分け、
「本質的依存 ⟺ 一点反転検査」は人手証明の両方向をそのまま写した。有限決定可能性は
決定可能性インスタンス＋`mem_supp_iff` と、走査する組 (w,x) の総数 $|S|\cdot2^{|S|}$
（`card_scan_pairs`）として形式化した。**比較回数のコストモデル自体は形式化していない**
（ファイル冒頭に範囲を明記）。`lake build`・`check-no-sorry.sh`（4 定理とも sorryAx 非依存）・
構造化テキスト check・build:pdf・SageMath 3 本・対応検査を全て通した。
**現在地は Lean 具体版まで。次 tick は Lean 必要十分版と導出**（同値だけなら `Fintype S` が
不要なことが具体版の `omit` から既に見えている。これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 本質的依存台の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・証明順序が一致しており、修正を要する
誤りがないことを確認した。`lean/CellularAutomata/NecSuf/EssentialDependency.lean` に必要十分版を
追加した。一点反転との同値に要るのは、添字型の等号判定と、状態型上の写像 `nu` が
「各 `a` と異なる `b` は `nu a` に一意に定まる」ことだけである。添字型・状態型の有限性は同値には
不要で、有限決定可能性に両型の有限性と状態の等号判定、走査組数の式に両型の有限性を追加すれば足りる。
一般の走査組数は $|S|\,|A|^{|S|}$ である。

具体版の決定可能性インスタンスを必要十分版の特殊化として定義し直し、同値と走査組数についても
2 元状態型への特殊化で得られることを別定理で明示した。構造化テキスト check・PDF、SageMath 3 本、
検算対応、Lean build、sorry 検査を全て通した。**本質的依存台の有限決定は四層が揃い `done`。**
次 tick は前層をレビューしてから、「冗長近傍からの独立性」の構造化記述だけを進める。

## 自動ループ tick: 冗長近傍からの独立性の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と具体版からの導出をレビューし、修正を要する誤りは見つからなかった。
台帳先頭の未完了対象「冗長近傍からの独立性」の構造化記述を
`structured-latex/content/redundant-neighbor.ts` に書いた。同一視を使わず、制限写像
$\rho^T_S:A^T\to A^S$ と基準値延長写像 $\iota^T_S:A^S\to A^T$（$\rho\circ\iota$ が恒等）の
2 本の写像だけで $A^S$ と $A^T$ を行き来する。冗長拡大は合成 $(T,\,f\circ\rho^T_S)$ として定義した。
claim は 3 つ: 足した元 $w\in T\setminus S$ には本質的に依存しない（一点反転が制限で消えることによる）、
$w\in S$ への本質的依存は拡大の前後で同値（延長・制限で証人を移す）、よって
$\operatorname{supp}(f\circ\rho^T_S)=\operatorname{supp}(f)$（$T$ の部分集合としての等号）。
全ブロック habitat "finite"、$\mathbb{R}/\mathbb{C}$ なし。構造化テキスト check・PDF（5 ページ・
未解決参照 0）、既存 SageMath 3 本、対応検査、Lean build・sorry 検査を全て通した。
**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 冗長近傍からの独立性の SageMath 検算（2026-08-15）

前 tick の構造化記述をレビューした。3 つの claim の数学的内容に反例はなかったが、制限・延長・
冗長拡大の計算で複数等号を一行へまとめた箇所が「一ステップ一定理」に違反していたため、各等号と
根拠を分離した。`sagemath/check/redundant-neighbor-independence/` に、追加元の一点反転が制限で消える
こと、$\rho^T_S\circ\iota^T_S$ が恒等写像であること、追加元への非依存、元の添字への依存の移送、
本質的依存台の一致を別々に検査する 5 本を追加した。$|T|\le3$ の全包含 $S\subseteq T$、全局所
真理値表、必要な全入力で厳密に検査し全て PASS。一般の有限集合に対する証明ではなく、人手証明の
有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

構造化テキスト check・PDF、既存を含む SageMath 8 本、検算対応、Lean build、sorry 検査を全て
通した。**この章の現在地は SageMath まで。次 tick はレビュー後に、人手証明と同じ対象・仮定・
順序の Lean 具体版だけを作る。**

## 自動ループ tick: 冗長近傍からの独立性の Lean 具体版（2026-08-15）

前 tick の SageMath 5 本と構造化記述の対応をレビューし、修正を要する誤りは見つからなかった。
`lean/CellularAutomata/RedundantNeighbor.lean` に人手証明と 1 対 1 の具体版を書いた。$T$ は
有限型、$S$ は `Finset T`、$A^S$ は部分型 `↥S` 上の写像で表し、$A^S$ と $A^T$ の行き来は
制限写像 ρ と基準値延長写像 ι の 2 本だけを通した（同一視をしないという人手証明の規律を
そのまま形式化に写した）。ρ∘ι の恒等、追加元への非依存、依存の移送、依存台の等号
（supp f を包含写像で `Finset T` へ写した像との等号）の 4 定理を形式化し、sorry 検査の
対象へ追加した。構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、sorry 検査を
全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を
作る**（同値・移送には $T$ の有限性が不要で、依存台の等号にだけ要る見込み。具体版の
`variable [Fintype T]` の位置から既に見えている。これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 冗長近傍からの独立性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、制限写像・基準値延長写像の 2 本だけを通ること、
3 つの claim と証明順序が一致することを確認し、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/RedundantNeighbor.lean` に必要十分版を追加した。制限・冗長拡大・
依存の移送には添字型の等号判定と延長用の基準値だけが要り、添字型・状態型の有限性は不要である。
追加元への非依存は具体版と同じ一点反転検査で証明し、状態側に要る性質を「各元と異なる元が
入れ替え写像の値に一意に定まること」へ限定した。依存台の等号より前の点ごとの必要十分条件にも
有限性は要らず、有限性が要るのは具体版で依存述語を `Finset` として集める段階だけである。

具体版の制限・延長の恒等性、追加元への非依存、依存の移送、依存台の等号が必要十分版の特殊化から
得られることを別定理で明示した。sorry 検査の一時ファイル作成も macOS の `mktemp` で確実に動く
一時ディレクトリ方式へ修正した。構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、
sorry 検査を全て通した。**「冗長近傍からの独立性」は四層が揃い `done`。次 tick はレビュー後に
「時間展開上の直接依存」の構造化記述だけを進める。**

## 自動ループ tick: 時間展開上の直接依存の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と導出をレビューし、仮定の限定・証明手順の一致・特殊化の明示を確認、
修正を要する誤りは見つからなかった。台帳先頭の未完了対象「時間展開上の直接依存」の構造化記述を
`structured-latex/content/time-expansion-dependency.ts` に書いた。有限舞台 $(V,N)$・有限舞台上の
2 値 CA・大域写像 $F$（各セルの値写像が冗長拡大 $f_v\circ\rho^V_{N(v)}$ に等しいことを定義内で
明示）・時間区間 $[0,\tau]_{\mathbb{N}}$・イベント集合 $E_\tau=[0,\tau]_{\mathbb{N}}\times V$・
一段依存関係 $D_\tau$（$t=s+1$ かつ $u\in\operatorname{supp}(f_v)$）を定義した。claim は 4 つ:
大域写像の一点反転による値の変化 ⟺ $u\in\operatorname{supp}(f_v)$（前二章の一点反転同値と
依存台不変性の合成。依存条件に $N(v)$ でなく $\operatorname{supp}(f_v)$ を採る根拠）、
$|E_\tau|=(\tau+1)\cdot|V|$、$D_\tau$ の有限性と所属の有限決定可能性、一段依存での時刻の厳密増加
（次対象「推移閉包の反対称性」の入力）。時間は $\mathbb{N}$ の大小比較と後者だけを使い、
$\mathbb{R}/\mathbb{C}$ 脱出なし。構造化テキスト check・PDF（7 ページ・未解決参照 0）、
SageMath 8 本、対応検査、Lean build、sorry 検査を全て通した。
**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 時間展開上の直接依存の SageMath 検算（2026-08-15）

前 tick の構造化記述を、前二章の定義・主張および用語の正本と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/time-expansion-dependency/` に、大域写像の一点反転による特徴づけ、
時間区間の個数、イベント集合の個数、一段依存関係の所属条件、依存台走査の比較回数上界、
一段依存での時刻増加を分けた 6 本の検算を追加した。$|V|\leq3$ の全ての近傍と局所真理値表を含む
有限範囲で厳密に検査し、全て PASS。一般の有限集合・任意の $\tau\in\mathbb{N}$ に対する証明ではなく、
人手証明の有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 時間展開上の直接依存の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する誤りは見つからなかった
（一段依存の検算が全セル同一規則の範囲に限られる点は、所属条件が組 $(N(v),f_v)$ ごとに
分解されるため、検証範囲の記載どおりで主張の反例探索に影響しない）。
`lean/CellularAutomata/TimeExpansionDependency.lean` に人手証明と 1 対 1 の具体版を書いた。
有限舞台は有限型 $V$ と `N : V → Finset V`、局所規則の族は前二章と同じ部分型上の写像で表し、
$A^V$ と $A^{N(v)}$ の行き来は前二章の制限写像・冗長拡大だけを通した。形式化した定理は、
大域写像の各セルの値写像が冗長拡大に等しいこと、大域一点反転の特徴づけ（前二章の一点反転同値・
依存台定義・依存台不変性を人手証明と同じ順で適用）、時間区間の所属と個数、イベント集合の個数
（直積の積の法則）、一段依存関係の所属の言い換えと $\operatorname{supp}(f_v)\subseteq N(v)$ の含意、
$E_\tau\times E_\tau$ の部分集合性、依存台走査の組数 $|N(v)|\cdot2^{|N(v)|}$、時刻の厳密増加の
10 定理。比較回数のコストモデル自体は前二章と同じく形式化していない（ファイル冒頭に明記）。
構造化テキスト check・PDF、SageMath 14 本、対応検査、Lean build、sorry 検査（新 10 定理を追加）を
全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を
作る**（同値・所属の言い換え・時刻増加には $V$ の有限性が不要な見込み。具体版の `omit` の位置から
イベント集合の個数と依存台の像の段階だけに有限性が要ると既に見えている。これが必要十分版の
最初の検査項目になる）。

## 自動ループ tick: 時間展開上の直接依存の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・証明順序に修正を要する不一致は
見つからなかった。`lean/CellularAutomata/NecSuf/TimeExpansionDependency.lean` に必要十分版を追加した。
大域写像と一点反転の特徴づけには舞台・状態型の有限性は要らず、舞台の等号判定、状態側の
「別値の一意性」、制限と延長の移送に使う基準値一つだけが要る。イベント個数には選んだ有限時間集合と
有限舞台だけが要る。一段依存の時刻増加は任意の時間型上の `next`、`lt`、`lt s (next s)` だけから導ける。
具体版の大域写像、一点反転の特徴づけ、イベント個数、時刻増加がそれぞれ特殊化で得られる導出定理も追加した。
全層の検証を通し、**「時間展開上の直接依存」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「依存の推移閉包の反対称性」の構造化記述だけを進める。**

## 自動ループ tick: 必要十分版の余分な仮定の削除と、推移閉包の反対称性の構造化記述（2026-08-15）

前 tick の「時間展開上の直接依存」の Lean 必要十分版をレビューし、イベント集合の定義と個数定理が
証明で使わない `DecidableEq Time`・`DecidableEq V` を仮定していたのを見つけて削除した
（必要十分版の要件「仮定は実際に使う性質だけ」への修正。要るのは選んだ時間集合の有限性と
舞台の有限性だけである）。修正後に Lean build・sorry 検査を通した。

台帳先頭の未完了対象「依存の推移閉包の反対称性」の構造化記述を
`structured-latex/content/transitive-closure-antisymmetry.ts` に書いた。依存経路
（長さ $n\geq1$ の写像 $p:[0,n]_{\mathbb{N}}\to E_\tau$ で隣接対が $D_\tau$ に入るもの）、到達可能関係
$C_\tau$（依存経路の存在）、反射的到達可能関係 $\preceq_\tau$、部分順序の定義と、claim 5 つ:
経路に沿った時刻の厳密増加（帰納法）、$D_\tau\subseteq C_\tau$、$C_\tau$ の推移性（経路の連結）、
$C_\tau$ が $D_\tau$ を含む最小の推移的関係であること（「推移閉包」の名の根拠）、相互到達の不存在
（反対称性。特例として非反射性）、$\preceq_\tau$ が $E_\tau$ 上の部分順序であること。
使う $\mathbb{N}$ の構造は大小比較と後者だけで、$\mathbb{R}/\mathbb{C}$ 脱出はない。
構造化テキスト check・PDF（9 ページ・未解決参照 0）、SageMath 14 本、対応検査、Lean build、
sorry 検査を全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の
SageMath 検算を作る。**

## 自動ループ tick: 依存順序から得る部分構造の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存・到達可能関係・反射的到達可能関係の定義と突き合わせ、数学的な
不一致は見つからなかった。記録上は claim が 7 つあるのに 6 つと数えていたため訂正した。
`sagemath/check/dependency-order-substructures/` に、下方集合と上方集合の順序凸性、順序凸集合の
共通部分、反鎖の順序凸性、時刻切片の反鎖性、下方集合へ外から入る一段依存の不存在、下方集合の
境界の外向き表示を分離した 6 本の検算を追加した。

$|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係と各イベント集合の全ての部分集合で
7 claim が全て PASS。検査対象は CA から得る関係より広いが、一般の有限舞台に対する証明ではない。
浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**この章の現在地は SageMath まで。次 tick は
検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 依存の推移閉包の反対称性の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存関係の定義と時刻増加の主張に突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/transitive-closure-antisymmetry/` に、経路に沿う時刻増加、
一段依存の包含、経路連結と推移性、推移閉包の最小性、相互到達・自己到達の不存在、反射閉包の
部分順序性を分けた 6 本の検算を追加した。検査対象は CA から得る関係より広い隣接時刻間の任意の
有限関係とし、$|V|\leq2,\ 0\leq\tau\leq3$ と $|V|=3,\tau=1$ の全 4,900 関係で全て PASS。
最小性は $|E_\tau|\leq3$ の全候補関係も列挙した。一般の有限集合に対する証明ではなく、人手証明の
有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 依存の推移閉包の反対称性の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算対象を隣接時刻間の任意の有限関係へ広げている点は、人手証明が一段依存から使う性質が
時刻の厳密増加だけなので、主張の反例探索を狭めない）。
`lean/CellularAutomata/TransitiveClosureAntisymmetry.lean` に人手証明と 1 対 1 の具体版を書いた。
依存経路は 3 条件（長さ 1 以上・値が $E_\tau$ に属する・隣接対が $D_\tau$ に入る）の構造、
到達可能関係 $C_\tau$ は依存経路の存在で定義し、$E_\tau\times E_\tau$ の部分集合ゆえ有限という
定義中の注記も定理として形式化した。経路に沿う時刻増加（経路長の帰納法）、$D_\tau\subseteq C_\tau$、
経路連結による推移性（継ぎ目の well-defined 性は $p(m)=b=q(0)$ の等式）、最小性（経路長の帰納法。
仮定 $R\subseteq E_\tau\times E_\tau$ は人手証明の主張どおり置き、証明で使わないことを明記）、
相互到達の不存在、非反射性、反射的到達可能関係の部分順序性（4 場合分け）の 8 定理を sorry 検査へ
追加した。構造化テキスト check・PDF、SageMath 20 本、対応検査、Lean build、sorry 検査を全て通した。
**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**
（経路・到達可能性・反対称性には舞台・状態型の有限性が不要で、要るのは「一段依存で時刻が
厳密に増える」性質だけの見込み。有限性が要るのは $C_\tau$ の有限性の注記だけ。これが必要十分版の
最初の検査項目になる）。

## 自動ループ tick: 依存の推移閉包の反対称性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、経路長の帰納法、経路連結、時刻増加の両向き適用、
反射閉包の 4 場合分けが同じ順で形式化されており、修正を要する不一致がないことを確認した。
`lean/CellularAutomata/NecSuf/TransitiveClosureAntisymmetry.lean` に必要十分版を追加した。
経路・到達可能性・推移閉包にはイベント集合と一段関係だけが要る。反対称性には任意の時刻型への
写像、推移的かつ非反射的な時刻関係、一段関係の各辺で時刻が増えることだけが要り、舞台・状態・
近傍・局所規則・自然数のその他の構造は不要である。イベント集合の有限性は到達可能関係を有限集合と
して述べる注記だけに分離した。

具体版の経路・到達可能性・有限性・一段包含・推移性・最小性・相互到達と自己到達の不存在・
部分順序性が必要十分版の特殊化として得られる導出定理を追加した。全層の検証を通し、
**「依存の推移閉包の反対称性」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「有限伝播境界」の構造化記述だけを進める。**

## 自動ループ tick: 有限伝播境界の構造化記述（2026-08-15）

前 tick の「依存の推移閉包の反対称性」の Lean 必要十分版と導出をレビューし、仮定の限定
（時刻写像・推移的かつ非反射的な時刻関係・一段ごとの時刻増加のみ。有限性は到達可能関係の
有限性の注記だけ）と 8 定理の導出の明示を確認、修正を要する誤りは見つからなかった。
台帳先頭の未完了対象「有限伝播境界」の構造化記述を
`structured-latex/content/finite-propagation-boundary.ts` に書いた。claim は 4 つ:
依存経路に沿って時刻がちょうど 1 ずつ増える（$t=s+n$。前章の $s<t$ の等式への精密化）、
伝播球（$B_1(v)=\operatorname{supp}(f_v)$、$B_{n+1}(v)=\bigcup_{u\in\operatorname{supp}(f_v)}B_n(u)$）の
有限性と個数の再帰的上界、依存経路の始点セルが伝播球に属すること、依存元集合
$P_\tau(t,v)$ が伝播球の有限合併 $\bigcup_{n\in[1,t]_{\mathbb{N}}}(\{t-n\}\times B_n(v))$ に含まれること
（個数上界 $\sum_{n\in[1,t]_{\mathbb{N}}}|B_n(v)|$ は $|V|$ にも $\tau$ にも依存しない。$P_\tau(0,v)=\emptyset$）。
時間・個数は habitat "N"、残りは "finite"、$\mathbb{R}/\mathbb{C}$ 脱出なし。構造化テキスト
check・PDF（11 ページ・未解決参照 0）、SageMath 20 本、対応検査、Lean build、sorry 検査を
全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 有限伝播境界の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存・依存経路・到達可能関係の定義と突き合わせ、伝播球の再帰の向き、
経路長と時刻差、依存元集合の包含と個数上界に修正を要する不一致は見つからなかった。
`sagemath/check/finite-propagation-boundary/` に、経路の正確な時刻差、伝播球の再帰式と個数上界、
経路始点セルの伝播球所属、依存元集合の包含・個数上界・時刻 0 での空性を分けた 4 本を追加した。
$|V|\leq3$ の全 531 個の本質的依存台の族と $0\leq\tau\leq4$ の 2,655 個の組で全て PASS。
一般の有限舞台に対する証明ではなく、人手証明の有限範囲での裏取りである。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 有限伝播境界の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算の一段依存・伝播球・境界合併の定義は構造化記述と一致し、個数上界は包含と境界集合の個数を
経由した連鎖で検査されている）。`lean/CellularAutomata/FinitePropagationBoundary.lean` に
人手証明と 1 対 1 の具体版を書いた。supp(f_v) を V の部分集合へ写した像を `suppV` と置き
（前章の一段依存の所属条件と同じ表し方）、経路の正確な時刻差 $t=s+n$（経路長の帰納法。基底は
一段依存の条件 $t=s+1$、帰納段は ℕ の加法の結合則）、伝播球の再帰（人手証明が定義しない
$n=0$ は番人値の空集合と明記。有限性は `Finset` の型が表す）、個数の等式と合併の和の上界、
経路始点セルの伝播球所属、依存元集合（E_τ の部分集合ゆえ有限の注記つき）、有限伝播境界の
包含・個数上界（部分集合 → 合併の和 → 一元集合との直積の順で人手証明と同じ）・時刻 0 での
空性の 12 定理を形式化し、sorry 検査対象へ追加した。伝播球の定義と個数に V の有限性が不要な
ことは `omit [Fintype V]` で明示した。構造化テキスト check・PDF、SageMath 24 本、対応検査、
Lean build、sorry 検査を全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と導出を作る**（伝播球・経路の時刻差には V の有限性と自然数の特殊構造の一部が
不要な見込み。`omit` の位置が最初の検査項目になる）。

## 自動ループ tick: 有限伝播境界の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、経路長の帰納法、伝播球の再帰、依存元集合の
境界包含と個数上界が同じ順で形式化されており、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/FinitePropagationBoundary.lean` に必要十分版を追加した。
経路の時刻差には自然数時刻と一段ごとの後者だけ、伝播球にはセル型の等号判定とセルごとの有限な
直接依存元集合だけが要り、セル型全体の有限性は不要である。依存元集合の有限性は舞台全体の有限性
でなく、選んだイベント集合の有限性だけから従う。

具体版の主要定義・定理が必要十分版の特殊化として得られる導出定理を追加した。全層の検証を通し、
**「有限伝播境界」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「依存順序から得る部分構造」の構造化記述だけを進める。**

## 自動ループ tick: 必要十分版の未使用仮定の削除と、依存順序から得る部分構造の構造化記述（2026-08-15）

前 tick の「有限伝播境界」の Lean 必要十分版をレビューし、依存元個数上界が証明で使わない仮定
「イベント集合の有限性」を持っていたのを見つけて削除した（個数上界は境界が有限集合である
ことだけで通る。イベント集合の有限性が要るのは依存元集合の有限性の注記だけ。具体版の
導出定理の適用も合わせて修正）。他に修正を要する不一致はなかった。

台帳先頭の未完了対象「依存順序から得る部分構造」の構造化記述を
`structured-latex/content/dependency-order-substructures.ts` に書いた。イベント集合 $E_\tau$ と
反射的到達可能関係 $\preceq_\tau$・一段依存 $D_\tau$ だけから、順序凸部分集合・下方集合・
上方集合・非比較関係 $\perp_\tau$・反鎖・時刻切片・一段境界 $\partial K$ を定義した。
claim は 7 つ: 下方集合と上方集合はそれぞれ順序凸、順序凸集合の共通部分は順序凸、
反鎖は順序凸（前章の反対称性が効く）、時刻切片は反鎖（時刻の厳密増加と $\mathbb{N}$ の
非反射性）、下方集合には外から入る一段依存が存在しない、よって下方集合の一段境界は外向きの
一段依存だけで定まる。物理的意味は持ち込まず、既存因果構造との比較は別対象に残した。
時刻切片の claim だけ habitat "N"、残りは "finite"、$\mathbb{R}/\mathbb{C}$ 脱出なし。
構造化テキスト check・PDF（13 ページ・未解決参照 0）、SageMath 24 本、対応検査、Lean build、
sorry 検査を全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の
SageMath 検算を作る。**

## 自動ループ tick: 依存順序から得る部分構造の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算の補助定義が構造化記述の定義と一致し、検査対象を CA から得る関係より広い「隣接時刻間の
任意の関係」としている点は overview に明記済みで、主張の反例探索を狭めない）。
`lean/CellularAutomata/DependencyOrderSubstructures.lean` に人手証明と 1 対 1 の具体版を書いた。
順序凸部分集合・下方集合・上方集合・反鎖は有限集合上の述語、非比較関係と一段境界は集合として
定義し、反射的到達可能関係・到達可能関係・一段依存は前二章の定義だけを通した。7 つの claim
（下方・上方集合の順序凸性、順序凸集合の共通部分、反鎖の順序凸性、時刻切片の反鎖性、下方集合へ
外から入る一段依存の不存在、下方集合の一段境界の外向き表示）と定義中の 2 つの注記（非比較関係の
入れ替え対称性、一段境界の有限性）を形式化し、sorry 検査対象へ追加した。人手証明の主張が置く
部分集合条件（J ⊆ E_τ 等）は証明で使わない場合も主張どおり仮定に置き、未使用を引数名の下線で
明示した。構造化テキスト check・PDF、SageMath 30 本、対応検査、Lean build、sorry 検査を全て
通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**
（順序凸・反鎖の claim に舞台の有限性・部分集合条件が不要な見込みが引数の下線から既に見えている。
これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 依存順序から得る部分構造の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、7 つの claim と定義中の 2 注記について、対象・
仮定・場合分け・証明順序に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/DependencyOrderSubstructures.lean` に必要十分版を追加した。
下方集合・上方集合・順序凸性には集合と関係だけ、反鎖の順序凸性には関係の反対称性と推移性だけ、
時刻切片の反鎖性には時刻写像・非反射的な時刻関係・相異なる二点間での時刻増加だけが要る。
一段境界と下方集合の境界には、一段関係が順序関係に含まれることだけが要る。舞台・状態・近傍・
局所規則・自然数・有限性は、有限境界の注記で対象集合の有限性を仮定する箇所以外では不要である。

具体版の 7 claim と 2 注記を必要十分版の特殊化として導出する定理を追加し、すべてを sorry 検査へ
登録した。構造化テキスト check・PDF、SageMath 30 本、対応検査、Lean build、sorry 検査を通した。
**「依存順序から得る部分構造」は四層が揃い `done`。** 次 tick は必要十分版と導出をレビューし、
台帳先頭の未完了対象「既存因果構造との比較」の構造化記述に着手する。

## 自動ループ tick: 既存因果構造との比較の構造化記述（2026-08-15）

前 tick の「依存順序から得る部分構造」の Lean 必要十分版と導出をレビューし、仮定の限定と
全 claim・注記の導出定理を確認、修正を要する不一致は見つからなかった。台帳先頭の未完了対象
「既存因果構造との比較」の構造化記述を `structured-latex/content/causal-structure-comparison.ts`
に書いた。外部の公理系を目標仕様にせず、組 $(E_\tau,\preceq_\tau,D_\tau)$ だけから述べられる
順序側の性質に固定した。claim は 4 つ: 区間 $I_\tau(a,b)$ の有限性（$\leq(\tau+1)\cdot|V|$）、
$D_\tau\subseteq\lessdot_\tau$（被覆関係。時刻差 1 が中間イベントを排除）、
$\lessdot_\tau\subseteq D_\tau$（長さ 2 以上の経路の中間点で被覆に矛盾）、両包含による
$D_\tau=\lessdot_\tau$——一段依存関係は $(E_\tau,C_\tau)$ だけから復元できる（保存される構造）。
加えて反例 claim: 順序を保つ全単射は時刻写像を保つとは限らない（定値規則・$\tau=1$ で
$\preceq_\tau$ が等号に退化）。時刻写像は順序だけから定まらない（失われる情報）。
「causal set」等の文献名との同定は一次文献の定義を確認した後の層で行うと remark に明記し、
二次情報による同定は記していない。構造化テキスト check・PDF（15 ページ・未解決参照 0）、
SageMath 30 本、対応検査、Lean build、sorry 検査を全て通した。**この章の現在地は構造化記述まで。
次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 既存因果構造との比較の SageMath 検算（2026-08-15）

前 tick の構造化記述と、その入力である「依存順序から得る部分構造」の Lean 必要十分版・導出を
レビューし、修正を要する不一致は見つからなかった。
`sagemath/check/causal-structure-comparison/` に、区間の有限性、一段依存関係と被覆関係の両包含、
両関係の集合としての一致、順序を保つ全単射が時刻写像を保たない反例を分けた 5 本を追加した。
一般側は $|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係について、9,521 区間、
1,061 一段辺、一段辺でない到達可能対 449 組を厳密に検査し、全て PASS。反例側は一元舞台の
定値局所規則と $\tau=1$ を構造化記述どおり構成し、2 イベント上の全 4 順序対で順序保存を確認し、
時刻が変わることを確認した。一般の場合の証明ではなく、有限範囲の裏取りである。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。**この章の現在地は SageMath まで。次 tick は検算と構造化記述の
対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 既存因果構造との比較の SageMath レビュー（2026-08-15、レビューのみ）

前 tick の SageMath 5 本を再実行して構造化記述と突き合わせ、修正を要する不一致は
見つからなかった（反例の構成、一般側の検査対象の広さとも記述どおり）。この tick は開始が
13:12 で 13:39 に強制終了されるため、Lean 具体版を sorry なしで完了できないと判断し新規層に
着手しなかった。**この章の現在地は SageMath まで。次 tick は Lean 具体版を作る。** 再利用先は
`TransitiveClosureAntisymmetry.lean` の `Reachable`・`ReflReachable`・
`path_time_strictly_increases`・`reachable_transitive`・`reachable_irreflexive`。
区間 $I_\tau(a,b)$ と被覆関係 $\lessdot_\tau$ は新規定義になる。

## 自動ループ tick: 既存因果構造との比較の Lean 具体版（2026-08-15）

前 tick の SageMath 5 本を再実行して全て PASS し、構造化記述との対応にも修正を要する不一致は
見つからなかった。`lean/CellularAutomata/CausalStructureComparison.lean` に人手証明と 1 対 1 の
具体版を書いた。区間の有限性と個数上界はイベント集合への包含から示した。被覆関係は到達可能な
中間イベントを持たない到達可能対として定義し、一段依存が被覆される向きは時刻差 1 と経路時刻増加、
被覆が一段依存になる向きは長さ 2 以上の経路の中間点を構成する人手証明の順で形式化した。
両包含による一段依存関係と被覆関係の等号も示した。

順序から時刻を復元できない反例は、一元舞台・自己近傍・定値 0 規則・$\tau=1$ を具体的に置き、
一段依存と到達可能関係が空で反射的到達可能関係が等号に退化すること、二つのイベントを交換する
全単射がその関係を保存するが時刻を変えることとして形式化した。外部の causal set 等の公理系は
持ち込まず、有限集合・有限関係・自然数時刻だけを使い、$\mathbb{R}/\mathbb{C}$ 脱出はない。
`lake build` と sorry 検査を通した。**この章の現在地は Lean 具体版まで。次 tick は構造化記述との
対応をレビュー後、Lean 必要十分版と具体版からの導出を作る。**

## 自動ループ tick: 既存因果構造との比較の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/CausalStructureComparison.lean` を追加した。区間には集合と関係だけ、
有限性・個数上界には集合の有限性だけが要る。一段関係が被覆関係に含まれることには D⊆C・C に沿った
時刻増加・D の対の隣接性（間に時刻がない）だけが要り、自然数時刻は不要。被覆が一段関係に含まれる
ことには経路による到達可能性だけが要り、時刻は不要。時刻非保存の反例には一段関係が空であることと
時刻の異なる二点だけが要り、舞台・近傍・局所規則は不要。具体版の区間・被覆は必要十分版の特殊化に
定義として等しく（`rfl`）、6 定理を必要十分版から導出した。`lake build` と sorry 検査（13 定理追加）
を通した。**これで対象表の全 7 対象が done になった。次 tick はこの必要十分版と導出をレビューし、
runbook「最初の探索列」の末尾（カタログ化後の既存因果構造との一次文献比較）を新対象として
起こすかを判断する。**

## 自動ループ tick: 因果集合の一次文献との照合（2026-08-15）

前 tick の「既存因果構造との比較」の Lean 必要十分版と具体版からの導出を、具体版・構造化記述と
突き合わせ、修正を要する不一致は見つからなかった。Bombelli–Lee–Meyer–Sorkin (1987) の本文で、
因果集合が局所有限な半順序集合として定義されること、連続時空への近似には順序保存埋め込み・
一様密度・尺度分離が要求されることを確認した。

`docs/survey/CA依存順序と因果集合の一次文献比較.md` を追加した。有限な
$(E_\tau,\preceq_\tau)$ は因果集合の数学的定義を満たし、$D_\tau$ は被覆関係として復元できる。
ただし比較先の多様体、埋め込み、密度、尺度、誤差または収束概念は未定義なので、連続時空近似や
物理的因果との同一視は主張しない。時刻写像が順序から復元できない既証明の反例は、因果集合の定義が
外部ラベルの復元を要求しないため矛盾ではない。次 tick はこの比較をレビューし、調査成果として
確定するか、数学的主張だけを構造化記述へ追加するかを判断する。

## 自動ループ tick: 因果集合の一次文献との照合の構造化記述（2026-08-15）

前 tick の一次文献比較（survey）を既証明の部分順序性・区間有限性・被覆関係の等号・時刻非保存の
反例と突き合わせ、修正を要する不一致は見つからなかった（一次文献の本文はこの tick で再取得して
おらず、定義の引用は前 tick の確認記録に依る）。比較のうち数学的主張になる部分だけを
`structured-latex/content/causal-set-primary-literature.ts` へ移した。局所有限な部分順序集合を
順序の言葉だけで定義し（有限集合上では区間有限性が自動で従うことを定義内に明記）、claim
「$(E_\tau,\preceq_\tau)$ は局所有限な部分順序集合で、その区間は前章の $I_\tau(a,b)$ と等しい」を
既証明の claim の合成として証明した。一次文献の出典と、主張しないこと（連続時空近似・物理的因果・
光円錐・時刻写像の復元）は remark に限定した。全ブロック habitat "finite"、$\mathbb{R}/\mathbb{C}$
脱出なし。構造化テキスト check・PDF（16 ページ・未解決参照 0）、対応検査、Lean build、sorry 検査を
通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 因果集合の一次文献との照合の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一次文献比較の調査記録および既証明の部分順序性・区間有限性と突き合わせ、
修正を要する不一致は見つからなかった。`sagemath/check/causal-set-primary-literature/` に、
反射的到達可能関係の部分順序性、一次文献の区間の有限性、一次文献の区間と前章の区間の集合としての
一致を分けた 3 本の検算を追加した。$|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係で
部分順序性を検査し、9,521 区間の有限性と二つの定義による区間の一致を全て PASS。一般の有限舞台に
対する証明ではなく、有限範囲の裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。
**この章の現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ
対象・仮定・順序の Lean 具体版を作る。**

## 論文アーティファクトと通知の一本化（2026-08-15）

`scripts/publish-artifact.sh` が構造化証明から自己完結 HTML を生成し、
`hexagonal-computation/artifacts` の Firebase Hosting にある
`math/cellular-automata-statistical-mechanics` 配下へ公開する。
公開 URL が HTTP 200 を返した場合だけ、直近の台帳記録と URL を Slack へ一度通知する。
Slack の完了通知には公開アーティファクト URL を必ず含め、URL のない通知は送らない。
通知本文は最新の研究コミット件名から作る一文だけとし、目的・経緯・検証の列挙を含めない。
同じプロジェクト版は外部マーカーで再通知せず、別プロジェクトだけが進んだ場合も再公開しない。
tick 内のエージェントは Slack を送らず、正常終了かつ clean のときに外側が公開・通知を担う。
公開物は一時閲覧用であり、リポジトリ内から恒久リンクしない。
シェル変数の直後に日本語を置くログ文字列は、変数名の境界を `${name}` で明示する。

## 完了済み（2026-08-02, プロジェクト新設）

- プロジェクト新設。ゴール設定を [README.md](README.md) に記述。
- **既存研究の網羅調査**（[docs/survey/](docs/survey/)）:
  CA と統計力学／場の量子論を結ぶ **互いにほとんど交流のない独立した経路**として整理。
  対応辞書（20 行）と、可算性から見た地形の表を作成。文献表は既読／未読を区別して記載。
- **アイディアの種**（[docs/ideas/](docs/ideas/)）: （一覧は docs/ideas/ を参照）。
  上位 3 つ（種「Φ_N の台と代数的複雑度」・種「線形CAとMahler測度」・種「Λ値不変量の三つの顔」）は個別ファイルで深掘り。

## 方針の組み直し（2026-08-02, docs/context 確立後）

`docs/context/` の三レイヤーが固まったので、プロジェクトの立て方を組み直した。

- **問いを反転した。** 「CA と物理の対応パターンを探す」ではなく
  **「連続物理が $\mathbb{R}$ の対象に帰している内容のうち、どこまでを CA の可算構造が担えるか」**。
  既存文献は全て「CA が連続物理を再現する」向きで書かれているが、**矢印を逆に読む**。
- **なぜ CA かを明確にした。** CA は**定義に $\mathbb{R}$ を一切含まない**唯一の物理模型クラス
  （Boltzmann 重みが $\{0,1\}$、理論全体が有限の記号から生成される）。
- **中心文書を新設した**: [`docs/survey/連続物理の可算な担い手.md`](docs/survey/連続物理の可算な担い手.md)。
  概念ごとに「CA 側の担い手／決定可能か／$\mathbb{R}$ が足すもの」を台帳にし、
  各経路を矢印反転で読み直した。**担い手が見つかっていないもの**も正直に列挙した。
- **成果物を定義し直した**: 可算コアの台帳／$\Lambda$ 値熱力学の構成／決定可能な述語の表／
  失われるものの同定。

台帳から読めた要点:
1. 分配関数からフェルミオンまでは $\mathbb{R}$ をほとんど必要としていない。
   $\mathbb{R}$ が足すのは「連続に動かせる・微分できる」という**計算の便宜**であって内容ではない。
2. $\mathbb{R}$ が本当に効くのは**極限を含む二項目**（自由エネルギー密度・臨界指数）だけ。
3. **箱玉系が最強の実例** — $q\to0$ で $\mathbb{C}$ の装置を全部落としても可積分構造が丸ごと残る。
   既存研究そのものが「$\mathbb{C}$ は内容を運んでいなかった」ことの証明になっている。
4. **GNVW 指数が雛形** — $\mathbb{R}$ 値の中心電荷に、決定可能な $\Lambda$ 値の格子祖先が実在する。

## 探索順序の組み直し（2026-08-02, 二相化）

**物理側の代数構造を起点にすると確証バイアスで探索が狭くなる**という指摘を受け、
探索を二相に分けた。第一相は CA 側から出発して代数構造をカタログ化する（物理を見ない）。
第二相でそれを物理と突き合わせる。枠組みは
[`docs/survey/CAから出発する代数構造の探索.md`](docs/survey/CAから出発する代数構造の探索.md)。

**バイアスは実際に効いていた。** 最初の調査は経路をすべて物理側の終着点で定義していたため、
純粋に数学側の構造を丸ごと落としていた。CA 起点で探すと即座に 4 つ出た。

- **次元群・Bowen–Franks 群** — SFT の共役・流れ同値の不変量。
  次元群は $\varinjlim(\mathbb{Z}^n\xrightarrow{A}\mathbb{Z}^n)$ で**順序アーベル群**、
  $\Lambda$ と同じ形の対象。Franks により既約 SFT の完全不変量系が知られている。
- **オートマトン群（自己相似群）** — 2 元アルファベットのオートマトンが生成する無限群。
  Grigorchuk 群、Nekrashevych の反復モノドロミー群。**物理に対応物がない。**
- **Perron 数（Lind 1984）** — 混合的 SFT のエントロピー全体＝Perron 数の対数全体。
  **「どの実数がエントロピーになりうるか」への完全な代数的回答**が既に存在する。
- **Artin–Mazur ゼータ関数** — $Z_N$ の母関数。有理性／超越性の二分。

代数構造が現れる機構を 6 型（係数環を載せる／極限を取る／自己相似で畳む／不変量を取る／
母関数化／退化させる）に分類し、これを軸に総当たりする方針にした。
歯止めは 4 条件（不変性・決定可能性・識別力・非自明性）。

## 舞台を有限から集める方針（2026-08-02）

**有限の代数構造を集め、その極限として無限を作る**順で舞台のパターン出しをする方針に決めた。
→ [`docs/舞台のカタログ_有限構造とその極限.md`](docs/舞台のカタログ_有限構造とその極限.md)

整理して出た要点:

- **極限には三つの向きがあり、可算性の帰結が違う。**
  順極限（直和・有限台、**可算**）／逆極限・完備化（直積、**非可算**）／局所的極限。
- **中心原理**: 同じ有限の族から順極限と逆極限の両方が作れる。
  **どちらを選ぶかが可算／非可算の分岐そのもの。**
  $\Lambda=\bigoplus_p\mathbb{Z}\ell_p$ を直和で定義しているのは、順極限側を選ぶという宣言である。
  $\mathbb{R}$ は $\mathbb{Q}$ の完備化＝逆極限側なので、
  **「$\mathbb{R}$ 脱出」と「逆極限・完備化を取る」は同じ機構の別の顔**である。
- **CA にとって正しい極限は局所的極限。** 局所規則は半径 $r$ しか見ないので
  $C_L\to\mathbb{Z}$、$(\mathbb{Z}/L)^d\to\mathbb{Z}^d$ が自然。
  群の逆極限 $\varprojlim\mathbb{Z}/L=\hat{\mathbb{Z}}$（非可算）ではない。
  2 次元 Ising を周期境界で解いて無限格子へ飛ばす操作はこの局所的極限である。
- **非可算化の点を精密化した。** 「舞台を無限にすると非可算」は粗い。正しくは
  局所的極限では舞台は可算のままで、**その上の全配位 $A^{G_\infty}=\varprojlim_F A^F$ を
  取ったときに初めて非可算**になる。有限台配位・周期配位に制限すれば可算のまま。
- **従順性は極限で初めて現れる**（有限群はすべて従順）。
  「有限では見えず極限で出る性質」という枠を作り、位相的エントロピー・Garden of Eden・
  Curtis–Hedlund–Lyndon の逆向きもそこに入れた。
- **オートマトン群は三つの極限のどれでもない第四の機構**（有限オートマトンが生成する無限群）。

## 局所的極限の定式化が決着（2026-08-02）

→ [`docs/局所的極限の定式化.md`](docs/局所的極限の定式化.md)

- **群**: 標識群の空間（Cayley / Chabauty–Grigorchuk 位相）での収束を主定義とする。
  「長さ $\le r$ の関係式集合が最終的に一致する」＝「Cayley グラフの半径 $r$ の球が同型」。
  **$\mathbb{R}$ を使わない。** $\forall r\,\exists n_0\,\forall n$ の $\Pi^0_2$ 文で、
  各段の判定は有限集合の一致。
- **$\mathbb{Z}/L\to\mathbb{Z}$ に一行の証明が付いた**（語の指数和 $|\sigma(w)|\le r<L$ より
  $L\mid\sigma(w)\iff\sigma(w)=0$）。自前で書いたのでこれは確定。
- **どの無限群が有限群の局所的極限か ＝ LEF 群。**
  有限表示に限れば剰余有限（Vershik–Gordon）。$\mathbb{Z}$・$\mathbb{Z}^d$・自由群・曲面群は該当。
- **CA の理論と噛み合う**: 従順 → Garden of Eden、sofic → surjunctive（Gromov–Weiss）。
  LEF ⊂ sofic なので、**有限から極限で得られる舞台のクラスと、CA の近似定理が効くクラスが重なる。**
- **グラフ（非推移的）には Benjamini–Schramm 収束**を使う。極限は unimodular random rooted graph。
  ランダム $d$-正則グラフ → 無限 $d$-正則木（非従順）。
  ただし確率測度の弱収束なので**極限で $\mathbb{R}$ が入る**（有限段階は $\mathbb{Q}$）。脱出点として記録。
- **pointed Gromov–Hausdorff は採らない。** 局所有限グラフでは局所同型と同値なのに
  $\mathbb{R}$ 値距離と $\varepsilon$ を持ち込むため。
  **同値な定式化が複数あるなら $\mathbb{R}$ を導入しない方を選ぶ**、という方針からの選択。
- **副産物**: Benjamini–Schramm 収束の下で自由エネルギーが連続であることが既に示されている
  （Abért–Csikvári–Frenkel–Kun、monomer–dimer）。
  **局所的極限は統計力学の熱力学極限を厳密に扱う既存の枠組みそのもの**であり、
  「$C_L\to\mathbb{Z}$ が熱力学極限の正体」という見方が数学側の裏付けを持つ。

## 有限→無限の機構は群に固有ではない（2026-08-04, 純粋数学レイヤー）

→ [`docs/有限代数系から無限代数系が現れる機構.md`](docs/有限代数系から無限代数系が現れる機構.md)

「有限を拡大すると別の代数系が現れるパターンは群だけか、有限体・環・多元環にはないのか」
という問いを、物理を一切見ずに純粋数学の側だけで調べた。

- **機構は五つある**（従来の三つ＋二つ）。
  順極限 / 逆極限・完備化 / 局所的極限 に加えて、
  **Fraïssé 極限**（有限対象のクラスの融合性から一意な可算対象を作る。**可算側に留まる**）と
  **超積・擬有限**（超フィルタ。選択公理を要するので**採らない**）。
- **群に固有ではない。むしろ完全な分類定理は群以外で得られている。**
  - 多元環: **Elliott** — ultramatricial 代数は次元群 $(K_0,K_0^+,[1])$ で完全分類。
    **純代数的で、$C^*$ 完備化を経ずに可算側で成立している。**
  - 多元環: **Bezushchak–Oliynyk** — **可算次元**の単位的局所行列代数は Steinitz 数で一意。
    非可算次元では一意でない。**可算性の仮定が分類の効き目を決めている。**
  - リー環: **Baranov** — finitary 単純リー環は $\mathfrak{sl}(\infty),\mathfrak{o}(\infty),\mathfrak{sp}(\infty)$ で尽きる。
  - 体: $\varinjlim\mathbb{F}_{p^n}=\overline{\mathbb{F}_p}$（**可算な代数閉体**）／
    Galois 側は $\hat{\mathbb{Z}}$（非可算）。**同じ図式の順極限側が可算、逆極限側が非可算。**
- **$\Lambda$ の位置づけが一段はっきりした（重要）。**
  $\overline{\mathbb{F}_p}$ の部分体は**超自然数（Steinitz 数）** $\prod_q q^{a_q}$, $a_q\in\mathbb{N}\cup\{\infty\}$ と 1 対 1。
  その**有限部分のモノイド $\mathbb{N}_{>0}$ の群化が $\mathbb{Q}_{>0}\cong\Lambda$** である。
  同じ Steinitz 数が局所行列代数と profinite 群の位数も分類している。
  **$\Lambda$ はこのリポジトリの発明ではなく、複数分野に独立に現れる標準対象の順極限側の顔。**
- **$\mathbb{R}$ の位置づけも順序論の標準的事実として書ける。**
  $\mathbb{Q}$ ＝有限線形順序の Fraïssé 極限、$\mathbb{R}$ ＝その Dedekind–MacNeille 完備化。
- **圏論で統一される**: $\mathrm{Ind}$（有向余極限を自由添加）と $\mathrm{Pro}$（有向極限）。
  $\mathrm{Pro}(\textbf{FinSet})\simeq\textbf{Stone}\simeq\textbf{BoolAlg}^{\mathrm{op}}$。
  **「順極限は可算・逆極限は非可算」は代数系の種類に依存しない圏論的な一般命題。**
- **Stone 双対が「非可算な空間 ↔ 可算な代数」の橋になる。**
  $\{0,1\}^{\mathbb{N}}$（非可算）↔ 可算な原子なしブール代数。
  全配位空間を扱う必要が出たときの逃げ道として記録した。
- **LEF は群専用ではない。** Gordon–Vershik は最初から群と有限次元代数の両方に定義しており、
  半群・環へも拡張されている（$F[G]$ が LEF $\iff$ $G$ が LEF）。
  **既に採用した局所的極限の枠組みを、状態集合側・規則環側へそのまま持ち上げてよい。**
- **半群では逆極限側が「記法」として使われている**（Reiterman: 擬多様体＝profinite 等式で定義）。
  有限の世界を語るために逆極限側の元を言語として持ち出す用法。

### 具体例を三つ入れた（同日追記）

抽象論だけでは噛み合わないので、手で追える例を文書の先頭に置いた。

- **行列環の塔 → Clifford 代数**。$\mathrm{Cl}_{2n}(\mathbb{C})\cong M_{2^n}(\mathbb{C})$ で、
  生成元を 2 個増やす操作＝$M_2$ をもう一つテンソルする操作。
  $M_2\subset M_4\subset M_8\subset\cdots$ の**順極限が無限 Clifford 代数（可算次元）**、
  **$C^*$ 完備化が UHF $M_{2^\infty}$＝CAR 代数（非可算）**。同じ塔のどこで止めるかで濃度が決まる。
  Steinitz 数 $2^\infty$、次元群 $K_0=\mathbb{Z}[1/2]$。
  $M_3$ の塔なら $\mathbb{Z}[1/3]$、全部混ぜれば $\mathbb{Q}$。
  **完全不変量が $\mathbb{Q}$ の部分群として出る**＝$\Lambda$ と同種の対象。
  → 検算 [`sagemath/check/clifford_matrix_tower/`](sagemath/check/clifford_matrix_tower/)（$n=1,2,3$ で確認、$\mathbb{R}$ 脱出なし）
- **繋ぎ方を変えると順序に代数的数が出る**。包含の重複度を $\binom{1\ 1}{1\ 0}$ にすると
  $K_0\cong\mathbb{Z}^2$ のままだが正錐が $a+b\varphi>0$（$\varphi$＝黄金比）で決まる。
  **$\mathbb{R}$ に埋まって見えるだけで、必要なのは $\overline{\mathbb{Q}}$ まで**（符号判定は決定可能）。
  SFT の次元群一般に同じことが起きる（正錐＝Perron 固有ベクトル、成分は代数的数）。
  **Lind の Perron 数定理はこの事実の別の顔。**
- **有限体の塔**。$\mathbb{F}_2\subset\mathbb{F}_4\subset\mathbb{F}_{16}\subset\cdots$、
  $\varinjlim\mathbb{F}_{2^{n!}}=\overline{\mathbb{F}_2}$（**可算な代数閉体**）、Galois 側は $\hat{\mathbb{Z}}$（非可算）。

**無限 Clifford 代数と $\overline{\mathbb{F}_2}$ という無関係な代数系が、同じ Steinitz 数の枠で分類される。**
これが「機構は代数系の種類に依存しない」ことの最も具体的な現れ。

### 軸を取り違えていた（同日、指摘を受けて修正）

上の Clifford の例は**行列サイズを大きくする軸**の話だが、
質問は**係数体を大きくする軸**（$\mathbb{F}_q$ 係数）だった。**二つは独立な軸である。**

| 軸 | 何を大きくするか | CA では | 支配する理論 |
|---|---|---|---|
| **大きさ** | 行列サイズ・格子サイズ・群の位数 | **舞台 $G$** | 五つの極限機構（順極限〜超積） |
| **係数** | $\mathbb{F}_2\to\overline{\mathbb{F}_2}$、$\mathbb{Q}\to\mathbb{C}$ | **状態集合 $A$ と重み** | **Brauer 群・Galois コホモロジー** |

**これまでのカタログはすべて大きさの軸だった。** 係数の軸を調べた結果:

- **係数体を大きくしても新しい代数系は出ない。むしろ潰れる。**
  $M_n(k)\otimes_k K=M_n(K)$。差が出るのは行列環でない代数だけで、それを測るのが Brauer 群。
- $\mathrm{Br}(\mathbb{F}_q)=0$（Wedderburn の小定理）、$\mathrm{Br}(\overline{k})=0$。
  **有限体上でも代数閉体上でも、中心単純代数はすべて行列環に潰れる。**
- **Clifford 代数はまさにこの話。** $\mathbb{C}$ 上は常に $M_{2^n}$、$\mathbb{R}$ 上は $\mathbb{H}$ 等が出て 8 周期、
  **$\mathbb{F}_q$ 上は潰れる。**
  よって「有限体係数の行列環から Clifford 代数が出る」は**逆向きにしか成り立たない。**
- **$\mathbb{R}$ がこの軸で足しているものは $\mathrm{Br}(\mathbb{R})=\mathbb{Z}/2$、つまり 1 ビット。**
  $\mathbb{R}$ の非可算性は Brauer 群に何も寄与しない。
  $\mathrm{Br}(\mathbb{Q}_p)=\mathbb{Q}/\mathbb{Z}$、$\mathrm{Br}(\mathbb{Q})$ も可算。
  **係数の軸には $\mathbb{R}$ 脱出が存在しない。脱出はすべて大きさの軸で起きる。**
- **2 値 CA では係数の出発点が $\mathbb{F}_2$ に固定されており $\mathrm{Br}(\mathbb{F}_2)=0$ なので、
  この軸には最初から何もない。** 意味があるのは $\mathbb{F}_2\to\overline{\mathbb{F}_2}$ で
  **規則多項式が分解する**こと（$\Lambda$／Mahler 測度の入口）だけ。
  → 種「線形CAとMahler測度」がこの軸の唯一の入口である。

## 事例集を二本立てた（2026-08-08）

機構の分類（五つの機構）は揃っていたが、**具体的な塔の事例が 3 つしかなかった**ので事例を集めた。
その過程で、**二つの軸を別文書に分ける**必要があることが分かったので二本立てにした。

### 舞台を大きくする軸 → [`docs/有限の舞台を無限へ飛ばす対応の事例集.md`](docs/有限の舞台を無限へ飛ばす対応の事例集.md)

周期境界の有限トーラスから整数格子へ飛ばす対応（2 次元 Ising の熱力学極限がこれ）。
**この対応が主張していることを四つの層に分けた**のが最大の成果。

1. **舞台**（$(\mathbb{Z}/L)^d\to\mathbb{Z}^d$）— $\mathbb{R}$ 不要。証明済み。
2. **局所規則**（$L>2r$ なら有限側と無限側で同一の有限データ）— **極限操作すら要らない。**
   「同じものとみなせる」が最も強い意味で成り立つのはこの層。
3. **示量量の密度**（自由エネルギー密度）— 収束の根拠は表面／体積 $\to0$（従順性・van Hove／Følner）。
   境界条件に依らないのも同じ理由。**$\mathbb{R}$ 脱出はここだけ。**
4. **状態（Gibbs 測度）** — 何の一意性かを確定した。**無限体積 Gibbs 測度（DLR 方程式の解）の一意性**
   であり、非一意性とは「極限が存在しない」ことではなく
   **「境界条件ごとの列はどれも収束するが行き先が違う」**ことである。

**層 4 について、定義を置かずに結論を書いて二度訂正した。指摘を受けて書き方を変えた（2026-08-09）。**

反省点: 定義を確定しないまま「層 4 の破れ＝相転移」等の結論めいた文を文書へ書き、
二度訂正する結果になった。**定義が定まっていない段階で結論を文書へ書かないこと。**

対応として [`docs/Gibbs測度と相転移の定義.md`](docs/Gibbs測度と相転移の定義.md) を新設し、
**定義と定理だけ**を（配位空間・$\sigma$ 加法族・相互作用・有限体積 Gibbs 分布・仕様・DLR 方程式・
純粋相・相転移の二つの定義、および $\mathbb{R}$ 脱出の一覧まで）書いた。
事例集の層 4 の節からは結論を削り、この文書への参照に置き換えた。
**この文書の冒頭に書き方の規律を明記した**: このリポジトリは物理を論じる場ではないので、
物理学者になら通じる程度の抽象度の文章には価値がない。記号の帰属・演算の定義・
極限の位相・$\mathbb{R}$ を使った箇所が書かれていない主張は主張として成立しない。

**一次文献の調査を行い、さらに 4 つの誤りが見つかった（2026-08-09）。**
→ [`docs/相転移まわりの一次文献ノート.md`](docs/相転移まわりの一次文献ノート.md)

Duminil-Copin の講義録（arXiv:1707.00520）の**本文**から定理文を取って照合した
（原論文の本文はまだ一つも読んでいない）。確認の水準を A／B／C の 3 段階で記録する規約にした。

- **「相転移の定義は 2 つ」は不足だった。** 講義録が明記しているとおり、連続転移の定義は
  相関長の発散・秩序変数の連続性・臨界点での Gibbs 状態の一意性・感受率の発散・
  スケール不変性など**少なくとも 5 つあり、数学的には同値でない**（同値にならない模型の例がある）。
  同値性は**平面ランダムクラスター模型**についての定理（Duminil-Copin–Sidoravicius–Tassion）である。
- **「任意次元で指数減衰」は誤り。** Duminil-Copin–Goswami–Raoufi が示したのは
  $d\ge3$・純粋相・$\beta>\beta_c$。高温側の出典は未特定のまま。
- **Lebowitz–Martin-Löf の主張は「磁化の連続性」であって「自由エネルギーの微分可能性」ではない。**
  後者への言い換えは凸性を使う別の一歩である。
- **臨界点での Gibbs 測度の一意性は単一の定理ではなく 3 つの結果の合成**であり、
  繋ぎ目（スピン反転対称性による磁化の片側極限）を確認していない。

収穫: 臨界点で指数減衰しないことは、$d\ge3$ で**定量的な上下界**として得られている
（自由境界条件で $c\|x\|^{-(d-1)}\le\langle\sigma_0\sigma_x\rangle\le C\|x\|^{-(d-2)}$）。
また、**測度の非一意性が起きる点の集合が高々可算**であることは、ランダムクラスター模型では
直接の定理として存在する（凸性から導いた自分の議論より強い形）。

以下は、その定義文書の上で成り立つ関係の要約である。

- 誤り 1「層 4 の破れ＝相転移」。正しくは**破れ（測度の非一意性）は相共存と同値**であり、
  Lebowitz–Martin-Löf により**自由エネルギーの磁場についての微分可能性と同値**。
  橋渡しは具体的な等式で、**微分の左右差＝二つの純粋相の磁化の差（自発磁化の 2 倍）**。
  相そのものは $\mathcal{G}$ の**端点**であり、端点でない測度は純粋相の混合にすぎない。
- 誤り 2「臨界点は四層のどこにも現れない」。**現れないのは測度の個数の側だけ**で、
  **測度の性質の側には現れる**。臨界点でも測度は一意だが、
  **Ising の切断相関は臨界温度を除くすべての温度で指数減衰する**（Duminil-Copin ら）ので、
  **臨界点＝一意な測度の相関が指数減衰しない唯一の温度**として特徴づけられる。
- **この立場にとっては良い知らせ。** 「指数減衰するかしないか」は**二択であって数値ではない**。
  臨界**指数**（実数値）と臨界**点の存在**（二択）は別物であり、後者だけなら可算側で書ける見込みがある。
  種を「臨界点は相関の減衰の二択として現れる」へ改名した。
- 副産物: 自由エネルギーは $h$ について凸なので**一次転移の集合は高々可算**。
- 副産物: 無限体積 Gibbs 測度は**円筒事象（可算個）の上の値で決まる**。
  添字は可算で、非可算なのは値の側である。

事例は 13 件（らせん境界 $\mathrm{Cay}(\mathbb{Z}/L^2,\{1,L\})\to\mathbb{Z}^2$ は自分で一行証明を付けた）。
重要な二つの否定例:
**$T_d$ の半径 $n$ の球は $T_d$ に収束しない**（canopy tree になる。従順性が効く理由の反例）、
**$K_n$・ハイパーキューブは次数発散で枠外**（平均場模型はこの枠組みの外にある）。

模型ごと運ぶ層は既に定理がある: Dembo–Montanari（局所的に木状なグラフ上の Ising で
**Boltzmann 分布そのものが極限へ局所収束し自由エネルギーが Bethe 値へ**）、
Abért ら（monomer–dimer）。

**複雑ネットワークを新規に取り込んだ。** 成長規則（有限データ）を無限回反復して大域指標を計算する模型群。
Erdős–Rényi → Poisson 分岐木、優先的選択 → Pólya point tree など。
**指標が有理数・整数で出るものが多い**（巨大成分の閾値 $c=1$、次数指数 $3$、閾値 $1/(d-1)$）。
そして**グラフの極限理論が疎（Benjamini–Schramm＝局所的極限側）と
密（グラフォン＝カット距離での完備化側）に割れている**ことが分かった。
これは中心原理（順極限側か逆極限側かが可算／非可算の分岐）のグラフにおける顔である。
**格子模型が疎グラフであることが、局所的極限を使える理由そのもの。**

### 代数系そのものを大きくする軸 → [`docs/有限構造の塔の事例集.md`](docs/有限構造の塔の事例集.md)

$M_2\subset M_4\subset\cdots$ 型の塔の事例を 13 件。横断的に見えた一般則が三つ:

- **実数に見える量の正体は Perron 数で、$\overline{\mathbb{Q}}$ で足りている。**
  SFT の次元群の正錐（黄金比）、融合圏の Frobenius–Perron 次元、Jones 指数 $4\cos^2(\pi/n)$ が
  すべて非負整数行列の Perron 根である。決定可能。
- **$\mathbb{R}$ が入るのは対象ではなく、その上の状態を全部集めたとき。**
  行列環の塔は状態空間が一点（トレース一意）なので $\mathbb{R}$ が入らないが、
  対称群の塔 $\mathbb{C}[S_n]$ は状態空間が Thoma 単体（実数パラメータ）になる。
  **自由エネルギー・エントロピーが実数値なのは状態側の量だからだと読める。**
- **綺麗に分類されるのは結合的・半単純な塔だけ**（Cayley–Dickson の塔は分類が付かない）。

最良の新事例は**有限射影幾何の塔 → von Neumann の連続幾何**。
次元関数が整数 → 二進有理数（順極限）→ $[0,1]$（完備化）と動き、
**連続体そのものが完備化の産物として現れる。** 行列環の塔（$K_0=\mathbb{Z}[1/2]$）の束論側の双子。

### 併せて直したこと

- 「複素 Clifford 代数 $\cong$ 行列環」の**向き**の注記を追加した。逆は成り立たない
  （$M_3$ は次元 9 なのでどの Clifford 代数とも同型でない）。
  Clifford 代数は行列環の塔のうち「2 の冪」という一点にすぎない。

**すべて二次情報（検索・要旨）に基づく。原論文で確認したものは一つもない。**

## 2 次元以上の CA と物理法則の対応を調べた（2026-08-10）

→ [`docs/survey/2次元以上のCAと物理法則の対応.md`](docs/survey/2次元以上のCAと物理法則の対応.md)

「ライフゲームに熱力学極限はあるか」という問いから出発した調査。

- **ライフゲームには熱力学極限の理論がなく、構造的な理由が 4 つある。**
  決定論的で詳細釣り合いを持たない（＝零温度）／加法的保存量がない／Turing 完全で
  漸近的性質が決定不能／得られている量（漸近密度 $\approx0.0287$）は経験則で定理ではない。
  自己組織化臨界の論争（Bak–Chen–Creutz 対 Bennett–Bourzutschky）は**決着していない**。
  数値実験の解釈の争いであり、可算／非可算の分別に効かないので**追わない**と判断した。
- **既存の経路別調査に欠落があった。** 既存調査は CA と**代数構造**の対応を軸にしていたため、
  CA と**連続方程式**の対応を軸にする経路が丸ごと漏れていた。別立てで補った。
- 新しく拾った経路: 確率的 CA の時空履歴が $(d+1)$ 次元の Gibbs 状態に対応する
  （Lebowitz–Maes–Speer 1990。**CA の統計力学の中核。要精読に追加**）／
  Toom 規則（2 次元で相転移が厳密に示される非平衡模型。eroder という有限条件で判定）／
  格子気体 CA → Navier–Stokes ／ 量子 CA → Weyl 方程式。
- **最大の収穫は格子気体の等方性条件。** 正方格子の HPP が失敗し三角格子の FHP が成功する理由は
  純粋に代数的で、「2 次元で $60^\circ$ 回転不変な 4 階テンソルは必ず等方的だが、
  $90^\circ$ 回転不変なものには非等方なものがある」に尽きる。
  **「連続極限で正しい物理が出るか」が格子の対称群についての有限の判定に帰着している。**
  → 種「連続極限の正しさは格子の対称群で決まる」として登録。検算は有限群の指標計算で済む。
- **Q2R（Vichniac）を見つけた。** 2 次元の可逆・保存的 CA で Ising のエネルギーを厳密に保存する。
  種「$\Lambda$ 値熱力学の構成」の実装対象として、下記「次にやること」の初等 CA より筋がよい。

**すべて水準 B 以下（要旨のみ、または二次情報）。原論文の本文は一つも読んでいない。**

## 次にやること（優先順）

1. **舞台のカタログを埋める（最優先）** — 定式化は決着したので、有限構造の族ごとに
   「局所的極限／順極限／逆極限」「どの収束を使うか」「$\mathbb{R}$ が入るか」
   「その性質が有限側に存在するか極限で初めて出るか」を表にする。
   LEF・sofic の原論文（Vershik–Gordon、Gromov、Weiss）の精読が要る。

2. **第一相の総当たり** — 初等 CA 256 個について、CA 側の不変量を全部厳密計算し
   規則ごとの表にする: 加法的保存量の $\mathbb{Z}$-格子、$Z_N$ とゼータ関数、
   次元群と Bowen–Franks 群、線形性・可逆性。
   **同時に識別力（どの不変量がどれだけ規則を分けるか）を測る。**
   → `sagemath/check/rule_invariants/`

3. **種「Λ値熱力学の構成」の検算** — 可逆な初等 CA を一つ選び、
   加法的保存量を線型代数で全部求め、$\Omega(U)\in\mathbb{N}$ を数え上げ、
   $S(U),\beta(U)\in\Lambda$ を素因数分解で書き、第〇法則が整数比較で確認できるか見る。
   **$\mathbb{R}$ 脱出ゼロで熱力学が完結すること**の実証。→ `sagemath/check/lambda_thermodynamics/`

4. **種「Φ_N の台と代数的複雑度」の検算** — `sagemath/check/phi_support/` を作る。
   初等 CA 256 個 × $L=3..14$ × $N=1..24$ で $Z_N=\#\{x:F^N(x)=x\}$ を計算し、
   $\Phi_N=\log Z_N\in\Lambda$ の台を表にする。
   確認: 加法的規則で台 $\subseteq\{2\}$（命題「線形 CA では台が 1 点に潰れる」）、可逆規則で $M=0$（命題「可逆 CA では傾きが消える」）、
   台が有界な非加法的規則の有無（仮説 種「Φ_N の台と代数的複雑度」の反例探索）。
   → [docs/ideas/種_Φ_Nの台と代数的複雑度.md](docs/ideas/種_Φ_Nの台と代数的複雑度.md) 「最初の検算（`sagemath/check/phi_support/` に置く）」節
5. **一次文献の精読（重複潰し）** — [docs/survey/文献表.md](docs/survey/文献表.md) の
   「要精読」3 本を先に：
   - D'Amico–Manzini–Margara（線形 CA のエントロピー公式の**正確な形**。種「線形CAとMahler測度」の鍵）
   - GNVW（指数の定義と値域、エントロピー輸送との関係。種「Λ値不変量の三つの顔」の最大の重複リスク）
   - Rujàn 1987（「転送行列と確率的CA」の経路で既に何が対応づけられているか）
   - **Lind 1984（Perron 数）と Lind–Marcus の教科書（次元群）— 第一相の中核。追加。**
6. **種「二つの壁は独立」を `docs/discussion/対数順序群上の統計力学/README.md` に反映** — 「計算不能性の壁」の欄を脱出台帳に追加する。
   これは検算不要（既知結果の整理）で、リポジトリの中心テーゼの精密化になる。**早く反映すべき。**
7. **種「線形CAとMahler測度」の検算** — `sagemath/check/ledrappier_padic/`。
   Ledrappier three-dot（$1+x+y$ over $\mathbb{F}_2$）で
   アルキメデス Mahler 測度・$p$ 進 $\mu_2$・CA 側エントロピーの三者を突き合わせる。
   既存コード `integrable-lattice/sagemath/check/cycle6_T1_padic_mahler/` を流用。
8. **種「可解性は有限検査で決まる」の総当たり** — 初等 CA 256 個に対する線形性・可逆性・加法的保存量の有限検査。
   YB 写像性はブロック CA への型合わせが先。

## 未解決・確認事項

- **調査は書誌・要旨レベルに留まる。** 「空白である」は「見つからなかった」の意味で、
  存在しないことの証明ではない。種を命題へ昇格させる前に一次文献で重複を潰すこと。
- 種「Λ値不変量の三つの顔」の「2 値 QCA の指数 $\in2^{\mathbb{Z}}$」は要一次文献確認。
- 種「線形CAとMahler測度」の「線形 CA のエントロピーの係数が非負整数」は要一次文献確認。
- 種「線形CAとMahler測度」の $\mathbb{F}_p\to\mathbb{Z}$ の持ち上げの非一意性が、$p$ 進側の量にどう効くか未定式化。
- $Z_N=0$（不動点なし）の規則では $\Phi_N=\log0$ が未定義。定義域の扱いを決めること。
- **Fraïssé 極限をこのプロジェクトの道具として使えるかは未検討。**
  有限のものだけから一意な可算対象を作れる点は順極限より強いが、
  得られる対象（Rado グラフ等）が等質すぎて構造を区別しない恐れがある。
- **Elliott の分類定理の純代数版と $C^*$ 版の境目**を一次文献で確認すること。
- 「有限 Steinitz 数のモノイドの群化 $\cong\Lambda$」を命題として書けるが、
  それが有限体の塔と局所行列代数の分類の両方と整合することまで書かないと意味がない。

## 判断の記録（なぜこの構成にしたか）

- **探索フェーズでは Markdown で書く**（構造化テキストへは命題が固まってから移す）。
  理由: 種は反証されて消える前提のもので、ラベル・参照解決の対象にすると回転が落ちる。
- **経路という切り方**を採った理由: 文献が実際にこの単位で分かれており（互いに引用が少ない）、
  経路どうしが繋がっていないことが空白の所在を最も明確に示すため。
- **種の優先順位の基準を変えた**（2026-08-02）。以前は検算コストの安い順だったが、
  **理論物理レイヤーの中心命題をどれだけ直接に実行するかを第一基準**に置き直した。
  その結果 種「Λ値熱力学の構成」が最優先に上がった（$\mathbb{R}$ 脱出ゼロで熱力学が
  完結することの実証であり、しかも検算が安い）。
- **中心文書を「全体像の地図」から「連続物理の可算な担い手（台帳）」へ移した**。
  地図は既存研究の分かれ方を示すもので、我々の主張ではない。主張は台帳の側にある。
- **さらに中心を「CA から出発する代数構造の探索」へ移した**（二相化）。
  台帳も**物理の概念リストを起点にしている**点で同じバイアスを持つ。
  第一相（CA 起点のカタログ）を先に置き、台帳は第二相の突き合わせ表として使う。

## 自動ループ tick: 因果集合の一次文献との照合の Lean 具体版（2026-08-15）

前 tick の SageMath 3 本を再実行し（全て PASS）、構造化記述との対応をレビューして修正を要する
不一致は見つからなかった。`lean/CellularAutomata/CausalSetPrimaryLiterature.lean` に人手証明と
1 対 1 の具体版を書いた。一次文献の区間 $A_R(x,y)$ と局所有限な部分順序集合（前章の部分順序の
述語と全区間の有限性の連言）を順序の言葉だけで定義し、有限集合上では区間が常に有限であるという
定義中の注記、一次文献の区間と前章の順序区間の集合としての一致、$(E_\tau,\preceq_\tau)$ が局所有限な
部分順序集合であることを、既証明の部分順序性と区間有限性だけから人手証明と同じ順序で証明した。
連続時空への近似・物理的因果・時刻写像の復元は人手証明どおり主張せず形式化もしていない。
入口 import と sorry 検査の対象へ追加し、Lean build・sorry 検査、構造化テキスト check・PDF、
SageMath、対応検査を通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と具体版の導出を作る**（注記の補題にセル型の有限性・等号判定が不要なことが
`omit` から見えている。区間・局所有限性は集合と関係だけで定義できる見込み）。

## 自動ループ tick: 因果集合の一次文献との照合の Lean 必要十分版（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、定義・主張・証明順序に修正を要する不一致は
見つからなかった。`lean/CellularAutomata/NecSuf/CausalSetPrimaryLiterature.lean` に、任意の集合と
関係だけで一次文献の区間と局所有限な部分順序集合を定義した。区間は台集合の部分集合なので、区間の
有限性に要るのは台集合の有限性だけであり、部分順序性は使わない。有限集合上の部分順序が局所有限に
なる定理を具体版と同じ順序で証明し、具体版の区間・局所有限性述語・区間有限性・イベント依存順序の
局所有限性を必要十分版の特殊化として導出した。セル、状態、近傍、局所規則、自然数時刻、物理的意味、
多様体、実数・複素数は必要十分版に現れない。**「因果集合の一次文献との照合」は四層と導出が揃い、
台帳上 done になった。**

## 自動ループ tick: 第二の探索列の開始と「軌道の最終周期性」の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。第一の探索列
（本質的依存台から因果集合の一次文献との照合まで）が全て done になったので、第二の探索列
「大域写像の反復から内在的に生じる構造」を台帳に開いた（survey「時間（F の反復）を動かす」に
対応。物理的名称を入れず、有限集合上の写像の反復であることだけを使う）。最初の対象「軌道の最終周期性」の
構造化記述 `structured-latex/content/global-map-iteration.ts` に、反復 $F^n$・軌道の定義、鳩の巣による
衝突（$0\le i<j\le 2^{|V|}$）、衝突の反復不変性、最終周期性、衝突組の有限決定可能性、主張しないこと
（無限舞台・極限集合・母関数、最小前周期・周期の一意性は次層以降）を書いた。$\mathbb{R}/\mathbb{C}$ 脱出なし。
構造化テキスト check・PDF（18 ページ）・対応検査、Lean build・sorry 検査を通した。**現在地は構造化記述
まで。次 tick はレビュー後に SageMath 検算を作る。** 後続の対象として「最小前周期と最小周期」
「周期点の個数」「大域写像の可逆性の有限決定」を todo で台帳に置いた。

## 自動ループ tick: 軌道の最終周期性の SageMath 検算（2026-08-15）

前 tick の構造化記述をレビューし、鳩の巣による衝突上界、衝突後の帰納法、$p=j-i$ による
最終周期性、候補組数の計算に修正を要する不一致は見つからなかった。
`sagemath/check/global-map-eventual-periodicity/` に、軌道衝突、衝突の反復不変性、最終周期性、
衝突組の走査回数を分けた 4 本の検算を追加した。セル数 0 の唯一の配位と、セル数 1〜3 の
巡回舞台上の全 256 初等 CA 規則・全配位からなる 3,585 軌道で全て PASS。48,489 個の衝突と
403,370 個のシフト後等式を検査した。これは任意の非一様 CA の全数列挙でも一般の場合の証明でもない。
最終周期性の全ての指数に対する根拠は人手証明の帰納法である。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応を
レビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 軌道の最終周期性の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本を再実行し、構造化記述との対応をレビューして修正を要する不一致は
見つからなかった。`lean/CellularAutomata/GlobalMapIteration.lean` に、反復・軌道の定義、
配位集合の個数、鳩の巣による衝突、衝突の反復不変性、最終周期性、候補組の有限集合とその個数
（2 倍が $M(M+1)$）、候補組の中の衝突の存在、配位等号の座標別分解、候補組上の存在文の決定可能性を
人手証明と同じ順で形式化した。等号検査回数のコストモデルは形式化していない。`lake build`・sorry 検査
（新 8 定理）・構造化テキスト check・PDF・SageMath・対応検査を全て通した。**現在地は Lean 具体版まで。
次 tick は Lean 必要十分版（有限型上の自己写像だけで述べる見込み）と具体版の導出を作る。**

## 自動ループ tick: 軌道の最終周期性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、反復の再帰、鳩の巣による衝突、衝突後の帰納法、
$p:=j-i$ による最終周期性、有限候補組の走査が同じ対象・仮定・順序で形式化されていることを確認した。
修正を要する不一致は見つからなかった。

`lean/CellularAutomata/NecSuf/GlobalMapIteration.lean` に必要十分版を追加した。反復・軌道・衝突の
反復不変性には型 $X$ と自己写像 $F:X\to X$ だけ、衝突と最終周期性には $X$ の有限性だけ、
衝突候補の有限決定可能性には $X$ の等号判定だけが要る。二値状態、セル、近傍、局所規則は現れない。
具体版の反復・軌道・衝突・衝突移送・最終周期性・候補組数・候補中の衝突存在を必要十分版から導く定理も追加した。
`lake build` と sorry 検査を通し、新たに登録した定理はすべて `sorryAx` に依存しない。
**「軌道の最終周期性」は四層と導出が揃い `done`。次 tick は必要十分版と導出のレビュー後、
「最小前周期と最小周期」の構造化記述を作る。**

## 自動ループ tick: 最小前周期と最小周期の構造化記述（2026-08-15）

前 tick の「軌道の最終周期性」の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。

`structured-latex/content/minimal-preperiod-period.ts` に「最小前周期と最小周期」の構造化記述を書いた。
前周期・周期の組の集合 $P(y)$ の所属が 1 回の配位の等号 $F^{i+p}y=F^{i}y$（と $p\ge1$）と同値であること、
$\mu(y)=\min I(y)$・$\pi(y)=\min Q(y)$ が $\mathbb{N}$ の整列性と反対称性で一意に定まること、
周期の倍数が周期であること、任意の周期 $p$ が $F^{\mu(y)+p}y=F^{\mu(y)}y$ を満たすこと、
$\mu(y)+\pi(y)\le 2^{|V|}$、有限範囲の走査で $\mu,\pi$ が決まることを、全て前章の衝突移送と
最終周期性から導いた。ℝ/ℂ なし。構造化テキスト check・PDF（19 ページ）・対応検査・Lean build・
sorry 検査を全て通した。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算
（`sagemath/check/` に P(y) 所属の同値、μ・π の走査値と定義値の一致、上界を分けて置く）を作る。**

## 自動ループ tick: 最小前周期と最小周期の SageMath 検算（2026-08-15）

前 tick の構造化記述を、前章の反復・衝突移送・最終周期性と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/minimal-preperiod-period/` に、周期組の所属と 1 回の衝突の同値、
任意の周期の最小前周期への移送、有限走査値と最初の再訪から得る値の一致、最小前周期と最小周期の
和の上界を分けた 4 本の検算を追加した。

配位数 1・2・4 の有限集合上の全自己写像・全初期値と、セル数 0〜3 の巡回舞台上の全 256 初等 CA
規則・全配位、合計 4,618 軌道で全て PASS。一般の有限舞台に対する証明ではなく、有限範囲の
厳密な裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。
次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 最小前周期と最小周期の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/MinimalPreperiodPeriod.lean` に、周期組の述語と衝突との同値、最小前周期・
最小周期（`Nat.find` による最小元。最小性と所属を別定理に）、周期の倍数、周期の最小前周期への
移送、$\mu(y)+\pi(y)\le 2^{|V|}$ を人手証明と同じ順序で形式化した。`lake build`、sorry 検査
（12 定理登録）、構造化テキスト check、対応検査を通した。

**有限範囲の走査による有限決定の claim はまだ形式化していない**（file 冒頭に明記）。
**現在地は Lean 具体版まで（有限決定を除く）。次 tick はレビュー後、有限決定の走査定理を具体版へ
足してから Lean 必要十分版と導出へ進む。**

## 自動ループ tick: 最小前周期と最小周期の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、修正を要する不一致は見つからなかった。具体版へ
有限候補組、最小組の候補範囲への所属、任意の成立候補に対する最小性、有限候補上の存在文の
決定可能性を追加した。必要十分版 `lean/CellularAutomata/NecSuf/MinimalPreperiodPeriod.lean` は、
型 $X$ 上の自己写像だけで周期組、二つの最小元、周期の倍数、最小前周期への移送、$|X|$ による上界、
有限走査を同じ順序で証明する。有限性が要るのは存在・最小元・上界、等号判定が要るのは有限走査の
判定だけである。二値状態、舞台、近傍、局所規則、$\mathbb{R}/\mathbb{C}$ は使わない。
具体版の周期組、周期の倍数、有限走査範囲が特殊化であることを導出した。
**「最小前周期と最小周期」は `done`。次 tick は必要十分版と導出をレビュー後、
「周期点の個数」の構造化記述を作る。**

## 自動ループ tick: 周期点の個数の構造化記述（2026-08-15）

前 tick の「最小前周期と最小周期」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。`structured-latex/content/periodic-point-count.ts` に、各 $n\ge1$ の反復の不動点集合
$\mathrm{Fix}_n(F)$ とその個数 $Z_n(F)\in\mathbb{N}$、周期点集合 $\mathrm{Per}(F)$ を定義し、
$y\in\mathrm{Per}(F)\iff\mu(y)=0$、$y\in\mathrm{Fix}_n(F)\iff(\mu(y)=0$ かつ $\pi(y)\mid n)$
（除法の原理と最小周期の最小性）、$Z_n(F)$ の最小周期ごとの個数への分解、$\mathrm{Fix}_n$ の
所属が配位の等号 1 回で決まり $Z_n$ が $2^{|V|}$ 回の等号検査で決まることを証明つきで書いた。
「分配関数」「トレース」の名前は使わず個数としてだけ定義した。$\mathbb{R}/\mathbb{C}$ なし。
**現在地は構造化記述まで。SageMath・Lean は未着手。** 次 tick は本章をレビューし SageMath 検算を作る。

## 自動ループ tick: 周期点の個数の SageMath 検算（2026-08-15）

前 tick の構造化記述を、反復・最小前周期・最小周期の既証明と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/periodic-point-count/` に、周期点と最小前周期 0 の同値、
反復の不動点と最小周期の整除条件、最小周期ごとの個数分解、全配位走査による有限決定を分けた
4 本の検算を追加した。配位数 1・2・4 の全自己写像と、セル数 1〜3 の巡回舞台上の全 256 初等 CA
規則で全て PASS。整除条件は 4,618 軌道・51,236 指数、個数分解は 1,029 写像・9,234 指数、
有限決定は 51,234 回の等号検査で一致した。一般の場合の証明ではなく、有限範囲の厳密な裏取りである。
浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。次 tick は検算と
構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 周期点の個数の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/PeriodicPointCount.lean` に、$\mathrm{Fix}_n(F)$（全配位の filter）と
$Z_n(F)$（その個数）、周期点、周期点 ⟺ $\mu(y)=0$、$y\in\mathrm{Fix}_n\iff(\mu(y)=0$ かつ
$\pi(y)\mid n)$（除法の原理と最小周期の最小性）、$Z_n$ の最小周期ごとの個数分解（fiber 分け）、
所属の決定可能性と全配位走査を人手証明と同じ順序で形式化した。`lake build`、sorry 検査
（9 定理登録）、構造化テキスト check、SageMath 4 本、対応検査を通した。
**現在地は Lean 具体版まで。次 tick は具体版をレビュー後、Lean 必要十分版（型 $X$ 上の自己写像と
有限性だけ）と具体版の導出を作る。**

## 自動ループ tick: 周期点の個数の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、四つの主張と証明順序に修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/PeriodicPointCount.lean` に、有限型 $X$ 上の
自己写像 $F:X\to X$ だけを用いる必要十分版を追加した。反復の不動点集合と個数、周期点、最小前周期・
最小周期による特徴づけ、最小周期ごとの個数分解には、二値状態・セル・近傍・局所規則は不要である。
不動点条件を実行可能に判定する箇所にだけ $X$ の等号判定を要求した。

具体版の不動点集合・個数・周期点・整除条件・最小周期類・個数分解・全要素走査が必要十分版の特殊化で
あることを別定理で導出した。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry
検査を全て通した。**「周期点の個数」は `done`。次 tick は必要十分版と導出をレビュー後、
「大域写像の可逆性の有限決定」の構造化記述を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の構造化記述（2026-08-15）

前 tick の「周期点の個数」の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。
`structured-latex/content/reversibility-finite-decidability.ts` に、像・単射・全射の定義、
「単射 ⟺ 全射」（有限集合の元の個数の数え上げだけ）、「単射 ⟺ 全ての配位で $\mu(y)=0$
（$\mathrm{Per}(F)=A^{V}$）」、有限決定（全対の等号走査または全配位の最小前周期走査）、主張しないこと
（無限舞台・逆写像の局所性・真理値表からの直接判定）を書いた。「可逆」「時間反転」等の名前は
主張に入れていない。構造化テキスト check・PDF（23 ページ）、対応検査、Lean build、sorry 検査を通した。
**現在地は構造化記述まで。次 tick は本章をレビュー後、SageMath 検算
（`sagemath/check/reversibility-finite-decidability/`）を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の SageMath 検算（2026-08-16）

前 tick の構造化記述を、像・単射・全射の定義、前章の最小前周期・最小周期・周期組の衝突形と
突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/reversibility-finite-decidability/` に、単射 ⟺ 全射（証明が使う像の個数の数え上げの
各段を含む）、単射 ⟺ 全配位で $\mu(y)=0$（両方向の中間段を含む）、全対走査による有限決定、
最小前周期走査による有限決定を分けた 4 本の検算を追加した。配位数 $1,2,4$ の全自己写像と、
セル数 $1$ から $4$ の巡回舞台上の全 256 初等 CA 規則（合計 1,285 写像。うち単射 263）で全て PASS。
一般の有限舞台に対する証明ではなく、有限範囲の裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$
脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と
同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の Lean 具体版（2026-08-16）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/ReversibilityFiniteDecidability.lean` に人手証明と 1 対 1 の具体版を書いた。
像を全配位の `Finset` の像、単射・全射を人手証明どおりの全称文と集合の等号で定義し、
単射 ⟺ 全射（前段の個数比較、単射側の個数一致、非単射側の $B=A^V\setminus\{y_1\}$ への像の縮小、
$\mathbb{N}$ の非反射性による矛盾）、単射 ⟺ 全配位で $\mu(y)=0$（⇒ は $m=\mu(y)-1$ と反復の帰納的
定義・単射性・最小性、⇐ は $y=F(F^ky)$ による全射から前定理）、$\mathrm{Per}(F)=A^V$ への言い換え、
全対走査（対数 $2^{2|V|}$、`Decidable` インスタンス）と全配位の最小前周期走査への言い換えを、人手証明と
同じ順序で 13 定理として形式化した。数え上げの既製事実は人手証明が名指しした初等的な 5 つに限り、
ファイル冒頭に列挙した。比較回数のコストモデルは形式化していない。構造化テキスト check・PDF、SageMath
4 本、対応検査、Lean build、sorry 検査を全て通した。**現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と導出を作る**（単射 ⟺ 全射には有限型上の自己写像だけが要る見込み）。

## 自動ループ tick: 大域写像の可逆性の有限決定の Lean 必要十分版と導出（2026-08-16）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/ReversibilityFiniteDecidability.lean` に必要十分版を追加した。単射の定義には
型 $X$ 上の自己写像だけが要り、有限性も等号判定も不要。像・全射・「単射 ⟺ 全射」・「単射 ⟺ 全元で
$\mu=0$」には $X$ の有限性だけが要る（像の `Finset` 化は古典的な等号判定で済ませ、実行可能な等号判定は
`Decidable` インスタンスにだけ要求）。二値状態・舞台・近傍・局所規則は現れず、$|X|^2$・$|X|$ が具体版の
$2^{2|V|}$・$2^{|V|}$ に特殊化される。具体版の像・単射・全射・二つの同値・二つの走査・対数が必要十分版の
特殊化であることを別定理 9 本で導出し、sorry 検査の対象へ 22 定理を追加した。構造化テキスト check・PDF、
SageMath 4 本、対応検査、Lean build、sorry 検査を全て通した。**「大域写像の可逆性の有限決定」は `done`。
台帳の未完了対象は無く、次 tick はレビュー後に次の対象（候補: 単射な大域写像の逆写像の局所性）を台帳へ
立てて構造化記述を作る。**

## 自動ループ tick: 逆写像の局所性の有限検査（反例あり）（2026-08-16）

前 tick の「大域写像の可逆性の有限決定」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。台帳に次の対象「逆写像の局所性」を立て、構造化記述の前に反例の有無を有限検査で
確かめた（`sagemath/check/inverse-map-locality-exploration/`、未昇格）。巡回舞台 $L\le7$ の全初等 CA 規則の
うち単射なものについて、逆写像の各セルの本質的依存台を一点反転検査で厳密に数えた。$L=5$ の規則 45 は
順写像の依存台の個数 3 に対し逆写像の依存台が全セルで $V$ 全体（個数 5）、規則 150 は $L=4,5,7$ で
逆写像の依存台の個数が $3,3,5$ と増える。**「逆写像は順写像と同じ近傍の局所規則から定まる」は偽。**
Sage の前処理は `^` を冪に変えるので、`.sage` ファイル内で XOR に `^` を使わないこと（実際に踏んだ）。
**次 tick はこの探索をレビュー後、逆写像とその依存台の定義・依存台の有限決定・具体的反例を claim とする
構造化記述を作る。舞台の族にわたる非有界性は観察に留め、主張しない。**

## 自動ループ tick: 逆写像の局所性の構造化記述（反例を claim として記述）（2026-08-16）

前 tick の探索を再実行してレビューし、修正を要する不一致は見つからなかった。`content/inverse-map-locality.ts` に、
単射な大域写像の逆写像 $F^{-1}$、各セルの値写像 $(F^{-1})_v$ とその本質的依存台（既存の局所真理値表の定義を
$S=V$ で適用）、依存台の有限決定（既存の走査定理の適用）、および反例 claim（$V=\{0,1,2,3,4\}$、$\ell,r$ を表で
与えた近傍、規則 45 の真理値表 $g$。$F$ の単射性は 32 配位の像の表、$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$
は 10 組の証人配位の表で証明）を書いた。$\mathbb{Z}/5\mathbb{Z}$ の同一視を避けるため $\ell,r$ を表で定義した。
$\operatorname{supp}((F^{-1})_v)=V$、規則 150 の観察、舞台の族にわたる非有界性は remark で主張しないと明記した。
構造化テキスト check・build:pdf（25 ページ）、対応検査を通した。**現在地は構造化記述まで。次 tick はレビュー後に
SageMath 検算（像の表・単射性・10 組の証人・依存台走査を分離）を作る。**

## 自動ループ tick: 逆写像の局所性の SageMath 検算（2026-08-16）

前 tick の構造化記述をレビューし、証明 (1) の像の表 32 行と証明 (2) の証人表 10 行を定義から再計算して一致を確認した。
修正を要する不一致は見つからなかった。`sagemath/check/inverse-map-locality/` に、像の表の再計算、単射性（右列の相異と
定義どおりの全対走査）、10 組の証人（$u\notin N(v)$、$\varphi_u z$ の形、$F^{-1}$ の値、$v$ での不一致、$V\setminus N(v)$
を尽くすこと）、逆写像の依存台の一点反転走査（回数 $\le160$、$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$、
順写像の依存台 $\subseteq N(v)$）の 4 本を追加し全て PASS。$\ell,r$ は表で与え剰余類の演算を使わない。反例 claim は
一つの有限対象なので全 32 配位の検査が主張の範囲を尽くす。構造化テキスト check・PDF、SageMath 4 本、対応検査（13 本 +
未昇格 2 件）、Lean build、sorry 検査を全て通した。**現在地は SageMath まで。次 tick はレビュー後に Lean 具体版
（逆写像の定義、$(F^{-1})_v$ と依存台、5 元舞台・規則 45 の反例）を人手証明と同じ順序で作る。反例の単射性は
`decide` に丸投げせず、人手証明どおり 32 配位の像の相異として形式化する方針を検討する。**

## 自動ループ tick: 逆写像の局所性の Lean 具体版（2026-08-16）

前 tick の SageMath 4 本を再実行し（全て PASS）、構造化記述との対応をレビューした。修正を要する不一致は見つからなかった。
`lean/CellularAutomata/InverseMapLocality.lean` に人手証明と 1 対 1 の具体版を書いた。逆写像は「$Fy=z$ を満たす $y$ の
存在（前章の単射 ⟺ 全射）と一意性（単射性）」から `Fintype.choose` で定義し、$F(F^{-1}z)=z$・$F^{-1}(Fy)=y$ を人手証明の
根拠どおりに示した。$(F^{-1})_v$ の依存台は前章の `supp` を $S=V$ で適用し、有限決定は所属の決定可能性と走査組数
$|V|\cdot2^{|V|}$ で形式化した。反例は 5 元帰納型 `Cell`（整数・剰余類を使わない）、$\ell,r$ の表、規則 45 の表 `g` から
$f_v$ を定義し、(1) 単射性と (2) 10 組の証人の存在を `decide`（有限型上の場合尽くし。`native_decide` は不使用）で検査した。
これは人手証明の表を写すのではなく定義から計算した値を照合するもので、人手証明の「有限個の 5 文字列の等号検査」と同じ内容である。
$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$、$\operatorname{supp}((F^{-1})_v)\not\subseteq N(v)$、
$\operatorname{supp}(f_v\circ\rho^V_{N(v)})\subseteq N(v)$（追加元への非依存による）、両依存台の非包含まで 12 定理を形式化し
sorry 検査の対象へ追加した。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry 検査を全て通した。
**現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**（逆写像の存在一意と両恒等には有限型上の
単射自己写像だけが要る見込み。反例は具体的対象なので必要十分版の対象外とし、一般部分の特殊化として具体版を導出する）。

## 自動ループ tick: 逆写像の局所性の Lean 必要十分版と導出（2026-08-16）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/InverseMapLocality.lean` を追加した。逆写像の存在一意には有限型上の単射自己写像だけが
要り（存在は前章の必要十分版「単射 ⟺ 全射」、一意性は単射性）、逆写像を選ぶ構成にだけ等号判定を要求した。
$(F^{-1})_v$ は写像型 $S\to A$ であることだけを使い、依存の一点反転判定・有限決定・走査組数 $|S|\,|A|^{|S|}$ は
前々章の必要十分版の適用である。反例は具体的対象なので必要十分版の対象外とし、具体版側に一般部分の導出定理 7 本を
追加した。sorry 検査へ 12 定理を追加し、構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry 検査を
全て通した。**「逆写像の局所性」は四層が揃い done。** 次 tick はレビュー後に次の対象を台帳へ立てる（候補: 逆写像を
「別の近傍 $N'$ をもつ 2 値 CA」として書くための最小近傍の定義と有限決定）。

## 自動ループ tick: 依存台による局所規則の表現の構造化記述（2026-08-16）

前 tick の「逆写像の局所性」の Lean 必要十分版と導出 7 本をレビューし、Lean build と sorry 検査を再実行した。
修正を要する不一致は見つからなかった。次の対象「依存台による局所規則の表現」を台帳へ立て、
`content/local-rule-representation.ts` に構造化記述を書いた。写像 $g:A^V\to A$ が $S\subseteq V$ 上の局所規則で
表せる（$g=h\circ\rho^V_S$ なる $h:A^S\to A$ がある）ことを定義し、表せるなら $\operatorname{supp}(g)\subseteq S$
（前章の依存台不変性）、$\operatorname{supp}(g)\subseteq S$ なら $g=(g\circ\iota^V_S)\circ\rho^V_S$（$V\setminus S$ 上の
非零座標数についての有限帰納法。一点反転と非依存で 1 座標ずつ基準値へ戻す）、$\operatorname{supp}(g)$ が表せる添字集合の
最小元、単射な大域写像の逆写像が近傍 $N'$ の 2 値 CA として書ける $\iff$ 全 $v$ で $\operatorname{supp}((F^{-1})_v)\subseteq N'(v)$、
最小近傍 $N^\ast(v)=\operatorname{supp}((F^{-1})_v)$ の有限決定を claim として証明した。$N^\ast$ を順写像の近傍で抑える
ことは主張しない。構造化テキスト check・PDF（28 ページ）、対応検査、Lean build、sorry 検査を通した。
**現在地は構造化記述まで。次 tick はレビュー後に SageMath 検算（表現可能性 $\iff$ 依存台の包含の小規模全数検査と、
逆写像の最小近傍の反例・可逆例での分離）を作る。**

## 自動ループ tick: 依存台による局所規則の表現の SageMath 検算（2026-08-16 09:12）

前 tick の構造化記述（表現可能性 $\iff$ $\operatorname{supp}(g)\subseteq S$、帰納法、最小性、逆写像の最小近傍）を
レビューし、修正を要する不一致は見つからなかった。`sagemath/check/local-rule-representation/` に検算 4 本を追加し、
$|V|\le3$ の全 276 写像・全 $S$ と、$L\le5$ 巡回舞台の単射な初等 CA 規則 124 組で全て PASS（有限範囲、一般証明ではない）。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の SageMath 検算（2026-08-16 23:42）

前 tick の構造化記述を衝突開始位置・最小正周期・周期伝播の既証明と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid-tail-cycle-decomposition/` に、最小衝突開始位置より前の
非衝突と安定後尾との非交差、安定後尾の一周期分への還元と相異性、過渡部と巡回部の非交和および元数
$\mu_F+\lambda_F$、有限真理値表からの有限決定を分離した検算 4 本を追加した。セル数 0 と
$1\le|V|\le3$ の巡回舞台上の全 256 初等 CA（計 769 写像）で全て PASS。有限範囲の検算であり一般証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick はレビュー後、
Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の構造化記述（2026-08-16 23:12）

前 tick の「反復モノイドの最小正周期」の Lean 必要十分版と導出を全層と突き合わせ、必要十分版が
Lean の検査入口へ直接登録されていない不整合を見つけて import を追加した。次の対象
「反復モノイドの過渡部と巡回部への分解」を立て、構造化記述を
`structured-latex/content/iterate-monoid-tail-cycle-decomposition.ts` に書いた。最小衝突開始位置
$\mu_F$ より前の反復写像は互いに異なり安定後の反復写像と交わらないこと、安定後の後尾集合は最小正周期
$\lambda_F$ の一周期分で尽くされ各元が互いに異なること、反復モノイドは両部分の非交和で元数が
$\mu_F+\lambda_F$ になること、これらが有限真理値表から有限決定できることを証明した。既存半群論の
分類名、物理的名称、$\mathbb R/\mathbb C$ は使わない。**現在地は構造化記述まで。次 tick はレビュー後、
本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの最小正周期の Lean 必要十分版と導出（2026-08-16 22:42）

前 tick の Lean 具体版を構造化記述・SageMath 3 本と突き合わせ、正周期の述語、最小性、周期の伝播
（加法則）、除法と帰納法による整除、各候補の有限判定に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidMinimalPeriod.lean` に必要十分版を、具体版と同じ手順で書いた。
要るのは型 $X$、自己写像 $F$、衝突開始位置の存在（前章の必要十分版の入力）だけで、$X$ の有限性は
この存在を与えるためにだけ使う。周期の伝播には加法則だけ、整除には伝播・自然数の除法・帰納法・最小性
だけ、各候補の有限判定には $X\to X$ の等号判定だけが要り、二値状態・セル・近傍・局所規則は現れない。
具体版の全主要主張（述語の一致、非空性、最小正周期の一致＝両方向の最小性、伝播、整除）を必要十分版の
特殊化として導出した。Lean build・sorry 検査（新 11 定理を追加）、構造化テキスト check、対応検査（20 件）
を通した。本文・SageMath は変更していないため PDF と SageMath は前 tick のものと同一で再実行していない。
対象は `done`。**次 tick は本 tick の必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの最小正周期の Lean 具体版（2026-08-16 22:12）

前 tick の SageMath 3 本を再実行して構造化記述と突き合わせ、周期の伝播、除法と帰納法による
余り 0 の導出、上限なし走査の停止根拠に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidMinimalPeriod.lean` に、正周期の述語と非空性、自然数の整列性による
最小正周期、最小性、周期の伝播、自然数の除法と帰納法から最小正周期が全正周期を割り切ること、
各候補の有限判定を人手証明と同じ対象・仮定・順序で形式化した。比較回数のコストモデルは形式化していない。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・
build:pdf（36 ページ・未解決参照 0）、SageMath 3 本、対応検査（20 件）、Lean build、sorry 検査を
全て通した。
**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 依存台による局所規則の表現の Lean 具体版（2026-08-16 09:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/LocalRuleRepresentation.lean` に、部分集合 $S$ 上の局所規則による表現可能性、
表現可能性 $\iff\operatorname{supp}(g)\subseteq S$、依存台自身による表現と最小性を形式化した。
包含から表現を得る向きは、人手証明と同じく有限集合上で依存しない座標を一つずつ基準値へ置換する帰納法で示した。
単射な大域写像については、逆写像が近傍 $N'$ の局所規則族の大域写像になること
$\iff$ 全 $v$ で $\operatorname{supp}((F^{-1})_v)\subseteq N'(v)$、局所規則
$(F^{-1})_v\circ\iota^V_{N'(v)}$、最小近傍 $N^\ast(v)=\operatorname{supp}((F^{-1})_v)$、
走査組数 $|V|\,2^{|V|}$ をこの順で形式化した。有限型・二元状態・自然数だけで閉じ、$\mathbb{R}/\mathbb{C}$ 脱出はない。
**現在地は Lean 具体版まで。次 tick はレビュー後、Lean 必要十分版と具体版からの導出を作る。**

## 自動ループ tick: 依存台による局所規則の表現の Lean 必要十分版と導出（2026-08-16 10:12）

前 tick の Lean 具体版を人手証明・SageMath と突き合わせ、主張と順序は一致。人手証明の $|D(y)|$ についての帰納法が
Lean では変更を許す座標の `Finset` についての帰納法（各段は同じ一座標の置換）になっている点だけ台帳に記録した。
`lean/CellularAutomata/NecSuf/LocalRuleRepresentation.lean` を追加した。表現可能性と `Finset` 帰納法には添字型の
等号判定だけが要り、表現可能 ⟹ 外に依存元なし、は具体版と同じ手順（前章の一点反転検査）を保つため状態側の ν と
別値の一意性を要求、外に依存元なし ⟹ 基準値延長で表せる、には添字型の有限性と基準値だけが要る。逆写像の同値は前章の
逆写像の構成のため $V,A$ の有限性・等号判定を仮定し、二値性は不要。依存台を `Finset` として集めるのは具体版だけ。
具体版側に導出定理 6 本を追加し、全検証を通した。**「依存台による局所規則の表現」は done。次 tick は次の対象を立てる。**

## 自動ループ tick: 大域写像の反復が生成する有限可換モノイドの構造化記述（2026-08-16 10:42）

前 tick の「依存台による局所規則の表現」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。第一相のカタログにある未着手操作「反復の生成するモノイド」を次の対象として台帳へ立て、
`structured-latex/content/iterate-monoid.ts` に構造化記述を書いた。反復写像の集合 $P_F$、
$F^m\circ F^n=F^{m+n}$、有限可換モノイド性、写像空間 $(A^V)^{A^V}$ の有限性から得る反復写像の衝突、
衝突後の有限代表集合、有限真理値表による元と合成表の決定を証明した。有限集合と自然数だけで閉じ、
無限反復の極限・位相・実数・複素数は使わない。構造化テキスト check・build:pdf（29 ページ）、
既存 SageMath 対応検査、Lean build・sorry 検査を全て通した。**現在地は構造化記述まで。次 tick はレビュー後、
反復写像の衝突・有限代表・合成表の SageMath 検算を分けて作る。**

## 自動ループ tick: 反復モノイドの SageMath 検算（2026-08-16）

前 tick の「大域写像の反復が生成する有限可換モノイド」の構造化記述をレビューし、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid/` に、加法則 $F^m\circ F^n=F^{m+n}$ の基底段・帰納段、
衝突 $F^i=F^j$ と反復不変性・除法による還元、モノイド公理と $|P_F|\leq M^M$、状態等号への分解による
有限決定と合成表の一致を別々に検査する 4 本を追加し、セル数 $0$ と $1\leq|V|\leq3$ の全 256 初等 CA 規則
（769 写像）で全て PASS。上界 $K=M^M$ までは走査せず、$M+\operatorname{lcm}(1,\dots,M)$ 以内の最初の衝突指数が
$K$ 以下であることを検査した。有限範囲の裏取りであり一般証明ではない。浮動小数点と $\mathbb{R}/\mathbb{C}$
脱出はない。**本章の現在地は SageMath まで。次 tick はレビュー後に Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの Lean 具体版（2026-08-16 11:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoid.lean` に人手証明と 1 対 1 の具体版を書いた。反復写像の集合 $P_F$、
$F^m\circ F^n=F^{m+n}$、恒等元・閉性・結合律・可換律、$|P_F|\leq M^M$、写像空間への鳩の巣原理による
衝突、衝突後の反復不変性と自然数の除法による有限代表への還元、有限代表集合と $P_F$ の一致、写像等号の
全配位・全セルの二値状態等号への分解、指数の加法による合成を人手証明と同じ順序で形式化した。
比較回数のコストモデル自体は形式化していない。**現在地は Lean 具体版まで。次 tick はレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの Lean 必要十分版と導出（2026-08-16 12:12）

前 tick の Lean 具体版を人手証明・SageMath と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoid.lean` を追加した。反復写像の集合・加法則・有限可換モノイドの
各公理には型 X と自己写像 F だけが要り、有限性も等号判定も不要。個数上界と鳩の巣による衝突には X の
有限性だけ（等号判定は Lean の構成上の都合なので仮定に含めず古典論理で写像空間の Fintype を得る）、
代表集合の `Finset` 化と有限候補上の衝突判定にだけ等号判定が要ることを分離した。具体版側に導出定理
10 本を追加し、具体版の等号判定を使わない定理から未使用仮定を `omit` で外した。構造化テキスト check・
build:pdf（29 ページ）、SageMath 4 本、対応検査、Lean build、sorry 検査（29 定理追加）を全て通した。
**「大域写像の反復が生成する有限可換モノイド」は四層が揃い done。次 tick はレビュー後、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの冪等元の構造化記述（2026-08-16 12:42）

前 tick の反復モノイドの Lean 必要十分版と導出を、構造化記述・SageMath・具体版と突き合わせ、修正を
要する不一致は見つからなかった。次の対象「反復モノイドの冪等元」を台帳へ立て、
`structured-latex/content/iterate-monoid-idempotents.ts` に、冪等元と冪等指数、衝突後の周期、正の冪等指数の
存在、冪等元全体の有限決定を記述した。非単位冪等元の存在は恒等 CA が否定し、冪等元の一意性は一元舞台の
定値規則が否定することも claim として保存した。有限集合と自然数だけで閉じ、極限と
$\mathbb{R}/\mathbb{C}$ は使わない。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの冪等元の SageMath 検算（2026-08-16 13:12）

前 tick の構造化記述を、前章の反復モノイドの定義・加法則・衝突・有限決定と突き合わせ、衝突からの
以後の周期の式変形、正冪等指数の構成（$i=0$ で $e=p$、$i\ge1$ で $e=ip\ge i$ とし以後の周期を
$i$ 回適用）、有限決定、2 つの反例のいずれにも修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-idempotents/` に、衝突からの以後の周期（式変形を一行ずつ）、
正冪等指数の存在（場合分けごと）、冪等元全体の 2 値状態等号走査による有限決定
（検査回数 $|P_F|\cdot2^{|V|}\cdot|V|$ 以下）、恒等 CA の反例、定値規則の反例を分けた 5 本を追加した。
一般側はセル数 0 と $1\leq|V|\leq3$ の巡回舞台上の全 256 初等 CA 規則の計 769 写像で全て PASS。
一般の有限舞台に対する証明ではないが、反例 2 件は構造化記述が構成する有限対象そのものなので
全数検査が主張の範囲を尽くす。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。構造化テキスト
check・PDF（31 ページ・未解決参照 0）、対応検査（16 本 + 未昇格 2 件）、Lean build、sorry 検査を
全て通した。**この章の現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、
人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 反復モノイドの冪等元の Lean 具体版（2026-08-16 13:42）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidIdempotents.lean` に、冪等元・冪等指数、衝突後の周期、周期の有限回反復、
正の冪等指数の存在、有限代表を対角合成で絞る有限決定を人手証明と同じ順序で形式化した。一元舞台の
恒等局所規則と定値局所規則から大域写像を構成し、非単位冪等元の存在が強制されない反例と、相異なる
二つの冪等元をもつ反例も形式化した。有限集合と自然数だけで閉じ、$\mathbb{R}/\mathbb{C}$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元の Lean 必要十分版と導出（2026-08-16 14:12）

前 tick の Lean 具体版を人手証明・SageMath 5 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidIdempotents.lean` を追加した。冪等元・衝突後の周期・周期の反復には
型と自己写像だけが要り、正の冪等指数の存在に要るのは衝突が一つ存在することだけ（有限性は衝突を得るためにだけ
使う）、有限決定にだけ等号判定が要る。反例は恒等写像（構造不要）と、相異なる二点への定値写像だけで成り立つ。
具体版側に導出定理 9 本を追加し、sorry 検査へ 18 定理を追加した。構造化テキスト check・PDF（31 ページ）、
SageMath 5 本、対応検査、Lean build、sorry 検査を全て通した。**「反復モノイドの冪等元」は四層が揃い done。
次 tick はレビュー後、次の対象（候補: 反復モノイドの巡回部分、最小前周期・最小周期のモノイドへの反映、
第一相カタログの未着手操作）を立てる。**

## 自動ループ tick: 反復モノイドの主イデアル列の構造化記述（2026-08-16 14:42）

前 tick の「反復モノイドの冪等元」の必要十分版と導出を四層で突き合わせ、SageMath 5 本、Lean build、
sorry 検査を再実行した。修正を要する不一致は見つからなかった。第一相カタログの Green 関係へ進む前段として
「反復モノイドの主イデアル列」を台帳へ立て、`structured-latex/content/iterate-monoid-principal-ideal-tail.ts` に
構造化記述を書いた。後尾集合 $I_n(F)=\{F^{n+k}:k\in\mathbb N\}$ が $F^n$ の生成する主イデアルであること、
反復写像の合成を吸収すること、包含について減少すること、衝突 $F^i=F^j$ の後は $I_n(F)=I_i(F)$ に
安定すること、列と最初の安定位置の有限決定を証明した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、主イデアルの等号・
包含・衝突後の安定・最初の安定位置を分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の SageMath 検算（2026-08-16 15:12）

前 tick の「反復モノイドの主イデアル列」の構造化記述をレビューした。主イデアルの両包含、吸収の加法則と
$\mathbb N$ の結合律・交換律、$I_{n+1}\subseteq I_n$、衝突後の安定で以後の周期を $d$ 回適用して
$q=k+dp-d\ge0$ と置く箇所、有限決定の論証のいずれにも修正を要する誤りは見つからなかった。
`sagemath/check/iterate-monoid-principal-ideal-tail/` に主イデアルの等号と吸収、包含の減少、衝突後の安定、
走査による有限決定を分離した 4 本を追加した。$|V|\le3$ の 769 写像を全数で厳密検査し全て PASS。
有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・
build:pdf（33 ページ、未解決参照 0）、対応検査（17 本 + 未昇格 2 件）、Lean build、sorry 検査を全て通した。
**現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版（後尾集合、主イデアル等号、吸収、減少、衝突後の安定、有限決定）を作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の Lean 具体版（2026-08-16 15:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidPrincipalIdealTail.lean` に、後尾集合、主イデアル表示、合成の吸収、包含の減少、
衝突後の安定を人手証明と同じ順序で形式化した。有限決定は、衝突指数未満の有限代表から後尾集合を作る走査が
定義と一致すること、衝突の始点までの有限候補から最初の安定位置を選べることとして形式化した。比較回数のコストモデル自体は
形式化していない。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版と人手証明の対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の Lean 必要十分版と導出（2026-08-16 16:42）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、後尾集合、主イデアル表示、合成の吸収、
包含の減少、衝突後の安定、有限走査の対象・仮定・証明順序に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidPrincipalIdealTail.lean` を追加した。後尾集合、主イデアル表示、
合成の吸収、包含の減少には型と自己写像だけが要り、衝突後の安定には衝突が一つ与えられていることだけが
要る。有限性は衝突を得る箇所にだけ使い、有限代表から後尾集合と最初の安定位置を走査する構成にだけ写像の
等号判定を要求した。具体版側に必要十分版との一致と特殊化による導出定理を追加した。有限集合・自然数・
写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**対象は四層と導出が揃い done。次 tick は本 tick の
必要十分版と導出を全層でレビュー後、第一相カタログの未着手操作から次の対象を立てる。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の構造化記述（2026-08-16 17:12）

前 tick の「反復モノイドの主イデアル列」の四層と導出を突き合わせ、SageMath 4 本、Lean build、sorry 検査を
再実行した。後尾集合、主イデアル表示、吸収、減少、衝突後の安定、有限走査に修正を要する不一致は見つからなかった。
第一相カタログの「反復の生成するモノイド」に残る主イデアルの比較を、既存半群論の名称を先取りせず直接調べる
「反復モノイドの主イデアル同値と有限鎖」として台帳へ立てた。

`structured-latex/content/iterate-monoid-tail-equivalence.ts` に、反復写像 $G\in P_F$ が生成する主イデアル
$J_F(G)$、その等号による主イデアル同値を定義した。集合の等号の反射律・対称律・推移律から同値関係であること、
$G=F^m,H=F^n$ の指数を $\mathbb N$ の全順序で比較し、後尾集合の包含へ移すことで任意の二つの生成主イデアルが
包含で比較できること、生成主イデアル全体 $\mathcal J_F$ が有限集合 $P_F$ の像として有限で、合成表の有限走査から
全ての元・包含関係・同値類を有限決定できることを証明した。既存半群論の関係との名称上の同定・既知性・分類定理との
接続は一次文献確認の別層へ残した。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキスト check・build:pdf、既存 SageMath 対応検査、Lean build、sorry 検査を全て通した。
**現在地は構造化記述まで。次 tick はレビュー後、主イデアル同値、包含による全比較、有限鎖の決定を分離した
SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の SageMath 検算（2026-08-16 17:42）

前 tick の構造化記述を反復モノイドと後尾集合の既証明に突き合わせ、主イデアル表示、集合の等号による
同値関係、自然数指数の全順序による包含比較、有限合成表からの列挙に修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-principal-ideal-chain/` に、主イデアル同値、包含による全比較、有限鎖と同値類の
有限決定を分離した 3 本を追加した。$|V|\le3$ の 769 写像について、同値律 22,906 件、生成元の順序対
6,972 組、相異なる主イデアルと同値類各 1,419 個を厳密に検査し全て PASS。一般の有限舞台に対する証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応を
レビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の Lean 具体版（2026-08-16 18:12）

前 tick の SageMath 3 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidPrincipalIdealChain.lean` に、生成主イデアル、主イデアル同値と
その同値律、自然数指数の全順序による生成主イデアルの比較可能性、衝突指数未満の有限代表と合成表から
生成主イデアル全体を有限列挙する構成を、人手証明と同じ順序で形式化した。有限集合・自然数・写像合成
だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick はレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の Lean 必要十分版と導出（2026-08-16 18:42）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidPrincipalIdealChain.lean` で、生成主イデアル・同値律・包含比較に要るのが
型 $X$ と自己写像 $F:X\to X$ だけであることを示した。$X$ の有限性は衝突の存在、等号判定は写像の有限代表・像・
有限部分集合の等号判定にだけ要る。二値状態、セル、近傍、局所規則は必要十分版に現れない。
具体版側に定義の一致と主要主張の特殊化による導出を追加し、Lean build と sorry 検査を通した。
**「反復モノイドの主イデアル同値と有限鎖」は四層と導出が揃い `done`。次 tick は本 tick を全層でレビュー後、
第一相カタログの未着手操作から次の対象を立てる。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の構造化記述（2026-08-16 19:12）

前 tick の「反復モノイドの主イデアル同値と有限鎖」の必要十分版と導出を全層で突き合わせ、修正を要する
不一致は見つからなかった。次の対象を、既存半群論の名称を先取りせず反復列と後尾集合だけから調べる
「反復モノイドの衝突開始位置と主イデアル列の安定位置」として台帳へ立てた。
`structured-latex/content/iterate-monoid-stabilization-index.ts` に、正周期をもつ写像衝突の最小開始指数
$\mu_F$、隣接する後尾主イデアルの等号と同じ位置で始まる衝突の同値、$\mu_F$ より前の厳密減少と
以後の安定、最初の安定位置と $\mu_F$ の一致・有限決定を記述した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の SageMath 検算（2026-08-16 19:42）

前 tick の構造化記述を前章の後尾集合の定義・性質と突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stabilization-index/` に、衝突開始位置の非空性と最小値 $\mu_F$、
隣接後尾集合の等号と衝突の同値の両方向、$\mu_F$ より前の真部分集合による減少と以後の一定、
走査で得る最初の安定位置と $\mu_F$ の一致を分離した検算 4 本を置き、769 写像で全て PASS。
有限範囲の検算であり一般証明ではない。$\mathbb R/\mathbb C$ 脱出はない。
**現在地は SageMath まで。次 tick はレビュー後、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の Lean 具体版（2026-08-16 20:12）

前 tick の SageMath 4 本を再実行して構造化記述と突き合わせ、769 写像で全て PASS し、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/IterateMonoidStabilizationIndex.lean` に、衝突開始述語、
自然数の整列性による最小衝突開始位置、隣接後尾集合の等号との同値、最小位置より前の真の減少と以後の安定、
有限代表を使う最初の安定位置との一致を、人手証明と同じ順序で形式化した。真部分集合は包含と集合の不等号を
分けて記録し、比較回数のコストモデル自体は形式化していない。有限集合と自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、
sorry 検査を全て通した。**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の Lean 必要十分版と導出（2026-08-16 20:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStabilizationIndex.lean` を追加した。衝突開始述語と隣接後尾集合の
等号との同値には型と自己写像だけが要り、最小衝突開始位置・真の減少・以後の安定には「衝突開始位置の存在」
だけが要る（有限性はその存在を与えるためにだけ使う）。最初の安定位置との一致には走査用の等号判定と衝突証人
だけが要り、衝突証人が存在を与えるので有限性は不要。具体版の主要主張は必要十分版の特殊化として導出し、
最小値の一致は両方向の最小性、存在証明の違いは証明無関係で吸収した。Lean build・sorry 検査、
構造化テキスト check、SageMath 4 本、対応検査を通した（本文未変更のため PDF は再生成せず）。
対象は `done`。**次 tick は本必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの最小正周期の構造化記述（2026-08-16 21:12）

前 tick の「反復モノイドの衝突開始位置と主イデアル列の安定位置」の Lean 必要十分版と導出を、
具体版・構造化記述・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
次の対象「反復モノイドの最小正周期」を台帳へ立て、
`structured-latex/content/iterate-monoid-minimal-period.ts` に構造化記述を書いた。
最小衝突開始位置 $\mu_F$ で戻る正周期の集合 $\Pi_F$ と最小元 $\lambda_F$ を定義し、
正周期が $\mu_F$ 以後へ伝わること、自然数の除法の余りが正なら最小性に矛盾することから
$\lambda_F$ が全正周期を割り切ること、大域真理値表の等号走査による有限決定を証明した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの最小正周期の SageMath 検算（2026-08-16 21:42）

前 tick の構造化記述（周期の伝播、除法と帰納法、走査の停止根拠）をレビューし、修正を要する誤りは
見つからなかった。`sagemath/check/iterate-monoid-minimal-period/` に、周期の伝播の各段、
除法の余りが 0 になること（帰納法の各段と最小性への矛盾を分離）、上限なし走査による有限決定の
3 本を追加した。セル数 0 と $1\le|V|\le3$ の巡回舞台上の全 256 初等 CA（計 769 写像）で全て PASS。
有限集合の写像の等号と非負整数の除法だけを使い、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check と対応検査（20 件）を通した。本文・Lean は未変更のため PDF・Lean build は再実行せず。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の Lean 必要十分版と導出（2026-08-17 00:42）

前 tick の Lean 具体版を構造化記述（5 claim）と SageMath 4 本と突き合わせ、非衝突・非交差・
一周期分への還元・一周期内の相異性・非交和と元数公式・有限決定の対象と順序が一致し、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidTailCycleDecomposition.lean` を
追加した。非衝突・非交差・還元・相異性・尽くすことに要るのは型 X、自己写像 F、衝突開始位置の存在
だけで、X の有限性はその存在を与える側にだけ現れる。X → X の等号判定は過渡部・巡回部を `Finset` に
書いて元数公式を述べる段階（`Finset.image` と `card_image_iff`）にだけ要り、等号判定を使わない
定理には `omit` を付けた。有限決定は hex を型クラスでは渡せないため instance ではなく def とした。
具体版の過渡部・巡回部・非衝突・還元・相異性・非交和と元数公式・尽くすことを必要十分版の
特殊化として導出し、Lean の入口と sorry 検査の登録へ追加した。lake build・sorry 検査・
構造化テキスト check・対応検査を通した。**対象は done。次 tick は本 tick の必要十分版と導出を
レビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の Lean 具体版（2026-08-17 00:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidTailCycleDecomposition.lean` に、過渡部と巡回部の有限集合、
最小衝突開始位置より前の非衝突、過渡部と安定後尾の非交差、安定後尾の最小正周期一周期分への還元、
一周期内の相異性、二部分による反復モノイドの非交和、元数公式
$|P_F|=\mu_F+\lambda_F$、有限決定を人手証明と同じ順序で形式化した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と具体版からの
導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の構造化記述（2026-08-17 01:12）

前 tick の「反復モノイドの過渡部と巡回部への分解」の Lean 必要十分版と導出を、具体版・構造化記述・
SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。次の対象として、反復モノイド全体では
冪等元が一意とは限らない一方、巡回部では一意になることを立てた。安定後にある最小の
$\lambda_F$ の倍数 $e_F$ と $E_F:=F^{e_F}$ を定義し、周期の伝播から $E_F$ が巡回部に属して冪等であること、
巡回部の冪等元 $F^n$ は $F^{2n}=F^n$ と一周期内の相異性から $\lambda_F\mid n$ を満たすため
$E_F$ に一致すること、$e_F$ と $E_F$ の有限決定を構造化記述した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、剰余の一致・冪等性・
一意性・有限決定を分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の SageMath 検算（2026-08-17 01:42）

前 tick の構造化記述をレビューし、一意性の証明の最終段で $e_F\le n$（$e_F$ の最小性）と
$n=e_F+t\lambda_F$ の形が暗黙だったため、最小性の参照と周期の伝播の適用範囲を明示する小修正を
入れた（結論は変わらない）。`sagemath/check/iterate-monoid-cycle-idempotent/` に SageMath 4 本を作り、
証人 $\mu_F\lambda_F\in D_F$ と有限範囲で列挙した最小元 $e_F$、$E_F=F^{e_F}$ の巡回部への所属と伝播の
逐次適用による冪等性、巡回部の全元を走査して冪等な元について人手証明の各段（$F^{2n}=F^n$、剰余 $s$、
$r=s$、$\lambda_F\mid n$、$e_F\le n$、$F^{e_F}=F^n$）、有限真理値表からの走査を分離して全て PASS
（769 写像、巡回部の元 1,412 個、各写像で巡回部の冪等元はちょうど一つ。参考として過渡部の冪等元は
恒等写像に限ることも 540 写像で観測）。有限範囲の検算であり一般証明ではない。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の Lean 具体版（2026-08-17 02:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidCycleIdempotent.lean` に、安定後周期倍数指数とその最小元、周期の
逐次適用、安定後指数の一周期への剰余還元、候補の巡回部所属と冪等性、巡回部の冪等元の一意性を
人手証明と同じ順序で形式化した。一意性では自然数の合同から最小正周期が指数を割ることを導き、
最小の安定後周期倍数指数との差だけ周期を逐次適用した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の Lean 必要十分版と導出（2026-08-17 02:42）

前 tick の Lean 具体版を構造化記述の 3 claim と SageMath 4 本と突き合わせ、対象・仮定・証明順序に
修正を要する不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidCycleIdempotent.lean`
を追加した。安定後周期倍数の存在と最小元、周期の逐次適用、剰余還元、$E_F$ の冪等性、および
「指数 $\mu_F+r$（$r<\lambda_F$）にある冪等な反復写像は $E_F$ に等しい」ことには型 $X$・自己写像 $F$・
衝突開始位置の存在だけが要り、$X$ の有限性は現れない。$X\to X$ の等号判定は巡回部を有限集合として
書き「$E_F\in C_F$」「$g\in C_F$ かつ冪等 $\Rightarrow g=E_F$」と述べる段階にだけ要る（使わない定理は
`omit`）。具体版の $D_F$・$e_F$・$E_F$・所属と冪等性・一意性を必要十分版の特殊化として導出し、
入口と sorry 検査へ登録した。lake build、sorry 検査、構造化テキスト check、対応検査、SageMath 4 本を
通した。**対象は `done`。次 tick は本 tick の必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の構造化記述（2026-08-17 03:12）

前 tick の「反復モノイドの巡回部にある唯一の冪等元」の Lean 必要十分版と導出を全層で突き合わせ、
SageMath 4 本、Lean build、sorry 検査を再実行した。対象・仮定・証明順序に修正を要する不一致は
見つからなかった。次の対象を、既存の群を目標仕様として置かず、巡回部の合成から内在的に調べる
「反復モノイドの巡回部がなす有限巡回群」として台帳へ立てた。

`structured-latex/content/iterate-monoid-cyclic-group.ts` に、合成の閉性、唯一の冪等元 $E_F$ の左右単位元性、
各 $F^n$ の逆元 $F^{e_F+n(\lambda_F-1)}$、$K_F:=F^{e_F+1}$ の群の冪による巡回部全体の生成、
位数 $\lambda_F$、演算表・逆元・生成元の有限決定を記述した。証明は反復回数の加法則、安定後の周期伝播、
一周期内の相異性だけを使う。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、閉性・単位元・逆元・巡回生成と位数・有限決定を
分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の SageMath 検算（2026-08-17 03:42）

前 tick の構造化記述を前章の定義・claim と突き合わせ、逆元の指数と群冪の帰納段の還元に要る $\ge\mu_F$ が
成り立つこと、相異性の還元が前章の一周期内の相異性で閉じることを確認し、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-cyclic-group/` に SageMath 4 本を作り、合成の閉性・可換律・結合律と $E_F$ の
左右単位元性、逆元 $F^{e_F+n(\lambda_F-1)}$、$K_F=F^{e_F+1}$ の群冪による巡回生成と相異性と位数 $\lambda_F$、
有限真理値表からの合成表・逆元列走査・生成元の決定を人手証明の各段に分けて全て PASS（769 写像、
巡回部の元 1,412 個）。有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の Lean 具体版（2026-08-17 04:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidCyclicGroup.lean` に、巡回部の合成閉性、$E_F$ の左右単位元性、
$F^{e_F+n(\lambda_F-1)}$ による左右逆元、$K_F=F^{e_F+1}$ の群冪 $F^{e_F+r}$、最初の
$\lambda_F$ 個の冪の相異性と巡回部全体の生成、位数 $\lambda_F$ を人手証明と同じ順序で形式化した。
合成表と逆元候補表も有限集合として定義した。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の Lean 必要十分版と導出（2026-08-17 04:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、claim「有限可換群」の可換律だけが
定理として欠けていたので `cyclePart_comp_comm` を具体版・必要十分版へ補った。他に修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidCyclicGroup.lean` に必要十分版を
追加した。逆元の左右恒等性・群冪の反復写像表示・一周期内の相異性には型・自己写像・衝突開始位置の
存在だけが要り、写像の等号判定は巡回部を有限集合として所属・生成・位数を述べる段階にだけ要る。
舞台・状態・近傍・局所規則の有限性はこの章の定理に現れない。具体版の主要主張を必要十分版の特殊化として
導出した（8 本）。Lean build・sorry 検査（21 定理を追加登録）・構造化テキスト check・対応検査・
SageMath 4 本・PDF を通した。**「反復モノイドの巡回部がなす有限巡回群」は四層が揃い done。
次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の構造化記述（2026-08-17 05:12）

前 tick の巡回部の有限巡回群の Lean 必要十分版と具体版からの導出を全層で突き合わせ、補われた
可換律を含め、修正を要する不一致は見つからなかった。次の対象を、巡回部の唯一の冪等元が配位集合に
内在的に定める「安定像」として立てた。`structured-latex/content/iterate-monoid-stable-image.ts` に、
$Q_F=E_F(A^V)$、$E_F$ が $Q_F$ 上で恒等になること、$n\ge\mu_F$ の全ての $F^n$ の像が $Q_F$ に
一致すること、$F$ の $Q_F$ への制限が全単射で $F^{e_F+\lambda_F-1}$ が逆写像になること、安定像と
制限写像の表の有限決定を記述した。証明は既証明の冪等性、巡回部の群法則、周期伝播だけを使う。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。
次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の SageMath 検算（2026-08-17 05:42）

前 tick の構造化記述を前章の定義・claim と突き合わせ、$R_F=F^{e_F+\lambda_F-1}$ が $\mathbb N$ 上で
定義できること、$2e_F+\lambda_F-1\ge\mu_F$ による安定後の像の一致への還元、巡回部の群法則から取る
逆元の使い方を確認し、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stable-image/` に SageMath 4 本を作り、安定像上での $E_F$ の恒等性、
$\mu_F\le n$ の反復写像の像と $Q_F$ の一致（両包含を点ごとに）、$F$・$R_F$ が $Q_F$ を保ち制限が
互いに逆写像になること、有限真理値表からの $Q_F$ と制限表の走査を人手証明の各段に分けて全て PASS
（769 写像、安定像の点 2,201 個）。像の一致で $n\ge\mu_F+\lambda_F$ の逆元は周期の伝播で一周期内へ
還元してから前章の指数を用いた（人手証明は存在だけを使う）。有限範囲の検算であり一般証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick はレビュー後、
Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の Lean 具体版（2026-08-17 06:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableImage.lean` に、安定像 $Q_F=E_F(A^V)$、$E_F$ の
安定像上での恒等性、$n\ge\mu_F$ の反復写像の像と安定像の一致、
$R_F=F^{e_F+\lambda_F-1}$ と $F$ による安定像の保存、両制限の相互逆性、$F|_{Q_F}$ の
全単射性を人手証明と同じ順序で形式化した。安定像の部分型は有限型として構成した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の Lean 必要十分版と導出（2026-08-17 06:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、修正を要する不一致は
見つからなかった（台帳で対象表の後ろに置かれていた 03:42・04:12 tick の記録を時系列へ移した）。
`lean/CellularAutomata/NecSuf/IterateMonoidStableImage.lean` に必要十分版を追加した。安定像の定義、
$E_F$ の恒等性、$R_F=F^{e_F+\lambda_F-1}$ の左右恒等性、$F$・$R_F$ による安定像の保存、制限の
相互逆性と全単射性には型・自己写像・衝突開始位置の存在だけが要り、写像の等号判定は人手証明どおり
$F^n\in C_F$ を経由する像の一致にだけ、$X$ の有限性と等号判定は走査表にだけ要る。具体版の主要主張を
特殊化として導出する定理 8 本を具体版へ追加した。Lean build、sorry 検査、構造化テキスト check、
PDF、対応検査、SageMath 4 本を通した。**「反復モノイドの冪等元が定める安定像」は四層が揃い `done`。
次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の構造化記述（2026-08-17 07:12）

前 tick の「反復モノイドの冪等元が定める安定像」の Lean 必要十分版と具体版からの導出を全層と
突き合わせ、修正を要する不一致は見つからなかった。次の対象を、冪等反復写像の値ごとの逆像が
配位集合に定める内在的構造を調べる「反復モノイドの安定像による配位集合の分割」として台帳へ立てた。
`structured-latex/content/iterate-monoid-stable-partition.ts` に、安定ファイバー
$B_F(q)=\{y\in A^V\mid E_F(y)=q\}$、各配位がただ一つのファイバーに属すること、異なるファイバーの
非交差、$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$、全ファイバーと個数の有限決定を構造化記述した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の SageMath 検算（2026-08-17 07:42）

前 tick の構造化記述を前章の安定像の定義・恒等性・有限決定と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid-stable-partition/` に SageMath 5 本を作り、安定像の
各元が自身のファイバーに属すること、各配位がただ一つの安定ファイバーに属すること（存在と一意性）、
異なるファイバーの非交差、指示値 $\delta_F(q,y)$ による個数分解
$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$ の各等号、有限真理値表からのファイバーと個数の走査を
人手証明の各段に分けて全て PASS（769 写像、配位 3,585 個、相異なる組 7,048 件）。有限範囲の検算であり
一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。検算スクリプトのカウンタ名が
補助関数 `configurations` を隠して落ちたので改名した（結果への影響なし）。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の Lean 具体版（2026-08-17 08:12）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStablePartition.lean` に、安定ファイバー、$E_F(y)$ による代表元、
代表元の存在一意性、異なるファイバーの非交差、全配位を代表元ごとに filter する有限走査表、
$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$、全ファイバーと正の個数の有限列挙を人手証明と同じ順序で
形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の Lean 必要十分版と導出（2026-08-17 08:42）

前 tick の Lean 具体版を構造化記述・SageMath 5 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStablePartition.lean` に必要十分版を追加した。ファイバーの
定義、代表元 $E(y)$ の存在一意性、異なるファイバーの非交差には型 $X$ と自己写像 $E$ だけが要り、
冪等性・反復写像であること・衝突開始位置と周期の存在は使わない。安定像の各元が自身のファイバーに
属すること（したがって個数の正値性）にだけ点ごとの冪等性 $E(E(y))=E(y)$ が要る。有限性と等号判定は
有限走査表と個数分解にだけ要り、総和は $|X|$ で止め、$2^{|V|}$ へ進む段は具体版の導出で `card_config`
により継いだ。具体版の主要主張を特殊化として導く導出定理 9 本を追加し、sorry 検査へ計 19 定理を
登録した。Lean build、sorry 検査、構造化テキスト check、PDF（44 ページ）、対応検査、SageMath 5 本を通した。
**「反復モノイドの安定像による配位集合の分割」は四層が揃い `done`。次 tick は本 tick の必要十分版と
導出をレビューし、次の対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバー間の一段発展の構造化記述（2026-08-17 09:12）

前 tick の「反復モノイドの安定像による配位集合の分割」の Lean 必要十分版と具体版からの導出を
構造化記述・SageMath 5 本と突き合わせ、SageMath・Lean build・sorry 検査を再実行した。一般の
自己写像だけで成り立つ部分、冪等性を要する部分、有限性と等号判定を要する部分の分離、および
具体版への特殊化に修正を要する不一致は見つからなかった。

次の対象「安定ファイバー間の一段発展」を立て、
`structured-latex/content/iterate-monoid-stable-fiber-dynamics.ts` に、安定像上の添字写像
$\sigma_F(q)=F(q)$、$E_F\circ F=F\circ E_F$、完全逆像
$F^{-1}(B_F(\sigma_F(q)))=B_F(q)$、ファイバーの像が次のファイバー全体とは限らない一元舞台の
定値規則による反例、有限決定を構造化記述した。有限集合・写像合成・等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の SageMath 検算（2026-08-17 10:12）

前 tick の構造化記述（添字写像 $\sigma_F$、$E_F\circ F=F\circ E_F$、完全逆像
$F^{-1}(B_F(\sigma_F(q)))=B_F(q)$、一元舞台の定値規則による反例、有限決定）をレビューし、修正を要する
誤りは見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-dynamics/` に添字写像の全単射性、
可換性、完全逆像の両包含、反例の各段と全数範囲での包含（真の包含 724 件）、有限走査を分離した 5 本を
追加し、769 写像で全て PASS。有限範囲の裏取りであり一般証明ではない。$\mathbb R/\mathbb C$ 脱出なし。
構造化テキスト check・PDF、検算対応 26 件、Lean build・sorry 検査を通した。**現在地は SageMath まで。
次 tick はレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の Lean 具体版（2026-08-17 11:12）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableFiberDynamics.lean` に、安定像上の添字写像と全単射性、
$E_F\circ F=F\circ E_F$、ファイバーの完全逆像の両方向、像の包含、一元舞台の定値規則で像が
真部分集合になる反例、添字写像・像・完全逆像の有限走査表を人手証明と同じ順序で形式化した。
反例では $\mu_F=\lambda_F=e_F=1$ を順に導き、$E_F=F$ とした。有限集合・自然数・写像合成・
等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick は
全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の Lean 必要十分版と導出（2026-08-17 12:12）

前 tick の Lean 具体版を構造化記述・SageMath 5 本と突き合わせ（SageMath は再実行）、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidStableFiberDynamics.lean` に
必要十分版を追加した。一般層（型 $X$ と二つの自己写像 $F,E$）では、添字写像 $\sigma(q)=F(q)$ に
「$F$ が $\operatorname{range}E$ を保つこと」だけ、順方向と像の包含 $F(B(q))\subseteq B(\sigma q)$ に
点ごとの可換性 $E\circ F=F\circ E$ だけ、逆方向（完全逆像）に可換性と $F$ の $\operatorname{range}E$ 上の
単射性だけが要る。$E$ の冪等性、$E$ が反復写像であること、衝突開始位置の存在、$\sigma$ の全射性は
この章のどの主張にも要らない。反復層 $E:=E_F$ では三性質が加法則・可換性・$R_F\circ F=E_F$ から従い、
型・自己写像・衝突開始位置の存在だけで $\sigma_F$ と完全逆像が定まる（前々章の `stableStep` が経由した
$X\to X$ の等号判定は不要。等号判定のもとで一致することは別定理）。反例は相異なる二点への定値写像だけで
具体版と同じ順に $\mu_F=\lambda_F=e_F=1$、$E_F=F$ を導いた。有限性・等号判定は走査表にだけ要る。
具体版の主要主張と反例を必要十分版の特殊化として導出する定理 8 本を追加し、sorry 検査へ計 30 定理を
登録した。Lean build、sorry 検査、構造化テキスト check、PDF（45 ページ）、検算対応 26 件、
SageMath 5 本を通した。台帳末尾に重複していた 10:12 tick の節を除いた。
**「安定ファイバー間の一段発展」は四層が揃い `done`。次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバー間の分岐個数の構造化記述（2026-08-17 13:12）

前 tick の「安定ファイバー間の一段発展」の Lean 必要十分版と具体版からの導出を全層で突き合わせ、
SageMath 5 本、Lean build、sorry 検査を再実行した。像の保存・可換性・像上の単射性の分離と、反復層・
具体版への特殊化に修正を要する不一致は見つからなかった。次の対象を、一段発展の完全逆像から内在的な
有限計数を取り出す「安定ファイバー間の分岐個数」として立てた。

`structured-latex/content/iterate-monoid-stable-fiber-predecessor-count.ts` に、一段前像集合
$\operatorname{Pre}_F(z)$ と一段前像数 $d_F(z)$、異なる配位の前像集合の非交差、完全逆像の有限非交和への
分解、保存式 $|B_F(q)|=\sum_{z\in B_F(\sigma_F(q))}d_F(z)$、局所真理値表からの有限決定を記述した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバー間の分岐個数の SageMath 検算（2026-08-17 14:12）

前 tick の構造化記述（一段前像集合 $\operatorname{Pre}_F(z)$、一段前像数 $d_F(z)$、非交差、完全逆像の
前像集合への分解、保存式 $|B_F(q)|=\sum_{z\in B_F(\sigma_F(q))}d_F(z)$、有限決定）をレビューし、修正を要する
誤りは見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-branching/` に非交差、完全逆像の所属同値の
各段、保存式の 4 等号、全組走査と有限加算を分離した 4 本を追加し、769 写像で全て PASS。有限範囲の裏取りであり
一般証明ではない。$\mathbb R/\mathbb C$ 脱出なし。構造化テキスト check・PDF（47 ページ）、検算対応 27 件、
Lean build・sorry 検査を通した。**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバー間の分岐個数の Lean 具体版（2026-08-17 15:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableFiberBranching.lean` に、一段前像集合と有限走査表、
異なる値の前像の非交差、完全逆像の有限合併への分解、保存式の 4 等号、全前像と個数の有限列挙を
人手証明と同じ順で形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（47 ページ）、検算対応 27 件、SageMath 4 本、Lean build・sorry 検査を通した。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**
