# MEMORY.md — cellular-automata-statistical-mechanics

> 着手前に [README.md](README.md)（このプロジェクトのゴール設定）を読むこと。
> 可算コア／$\mathbb{R}$ 脱出の一次情報は
> [`docs/discussion/対数順序群上の統計力学/`](../docs/discussion/対数順序群上の統計力学/) と
> [`docs/discussion/可算性の効用/`](../docs/discussion/可算性の効用/)。

## 自動ループ tick: 合併作用の Lean 具体版（2026-08-28 13:12）

前 tick の SageMath 検算 7 本を構造化記述と突き合わせ、合併の存在量化、合成の向き、恒等作用、
一元部分集合からの復元、単射性、冪等性、有限決定に修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentSubsetUnionAction.lean` を追加した。
有限部分集合を `Finset V`、合併写像を `S.biUnion N` として、人手証明と同じ順序で主要主張を
形式化した。部分集合全体の元数、全表への所属、冪等性の決定可能性も記録した。
有限型・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 合併作用の SageMath 検算（2026-08-28 12:12）

前 tick の「近傍割り当てが部分集合に定める合併作用」の構造化記述を、合成近傍・自己近傍割り当ての
定義へ戻ってレビューし、修正を要する誤りは見つからなかった。

`sagemath/check/neighborhood-assignment-subset-union-action/` に検算 7 本を追加した。合併写像の
定義、合成、単位元、一元部分集合からの復元、単射性、冪等性の同値、有限決定を別々に検査する。
舞台元数 $0\le|V|\le3$ の全 531 割り当てとその 262,405 対を走り全て PASS。合併写像が冪等な
割り当ては 137 個で、前章の合成冪等な割り当ての個数と一致した。$\mathbb R/\mathbb C$ 脱出はない。

本文は合成の向きの必要性を主張していないが、$U_N\circ U_M$ では一致しない二元舞台の具体例を
検算側で証人として記録した。人手証明は変えていない。

構造化テキストの検査と PDF（105 ページ、未解決参照 0）、本章 SageMath 7 本、検算対応検査 55 件、
Lean 全体ビルド、sorry 検査を通した。対象は `SageMath まで`。次 tick は本検算をレビュー後、
Lean 具体版だけを作る。

## 自動ループ tick: 近傍割り当てが部分集合に定める合併作用の構造化記述（2026-08-28 11:12）

前 tick の「合成冪等な近傍割り当ての特徴づけ」の Lean 必要十分版と導出を全層・検査入口へ
突き合わせ、修正を要する不一致は見つからなかった。

新対象「近傍割り当てが部分集合に定める合併作用」を起票した。近傍割り当て $N$ から有限舞台の
部分集合 $S$ を $\bigcup_{v\in S}N(v)$ へ送る写像 $U_N$ を構造化記述に定義し、合成近傍が
$U_M\circ U_N$、自己近傍割り当てが恒等写像として作用することを証明した。一元部分集合への値から
元の近傍割り当てを復元できるので対応は単射であり、合成冪等性と写像の冪等性は同値である。
有限真理値表から作用表と冪等性を有限決定できる。$\mathbb R/\mathbb C$ 脱出はない。

対象は `構造化記述まで`。次 tick は本章をレビュー後、SageMath 検算だけを作る。

## 自動ループ tick: 合成冪等な近傍割り当ての特徴づけの Lean 具体版（2026-08-28 09:12）

前 tick の SageMath 検算 5 本を構造化記述と合成近傍の定義へ戻って突き合わせ、
合成冪等性と推移性・二段分解可能性の同値、自己近傍を含む場合、二つの反例、
有限決定に修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentCompositionIdempotents.lean` を追加し、
合成冪等性から二条件を個別に取り出す順方向、二条件から各近傍の両包含を経て
写像の等号を得る逆方向を、人手証明と同じ順序で形式化した。自己近傍の場合は
二段分解の証人に対象元自身を取る。推移的だけの二元反例、二段分解可能だけの
三元反例、有限全称量化・有限存在量化による三述語の決定可能性も形式化した。
必要十分版と具体版の導出は未着手である。$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（103 ページ、未解決参照 0）、本章 SageMath 5 本、
検算対応検査 54 件、Lean 全体ビルド、sorry 検査を通した。対象は `Lean 具体版まで`。
次 tick は本具体版を全層とレビュー後、Lean 必要十分版と具体版の導出だけを作る。

## 自動ループ tick: 合成冪等な近傍割り当ての特徴づけの SageMath 検算（2026-08-28 08:12）

前 tick の構造化記述を合成近傍・近傍割り当ての定義へ戻ってレビューし、特徴づけの両方向、
自己近傍を含む場合の証人、二つの反例、有限決定の各段に修正を要する誤りは見つからなかった。

`sagemath/check/composition-idempotent-neighborhood-assignment/` に、特徴づけの前半・後半、
自己近傍を含む場合、二つの反例、有限決定を別々に検査する 5 本を追加した。舞台元数 $0\le|V|\le3$ の
全 531 割り当てを走り、合成冪等な割り当て 137 個が推移的かつ二段分解可能な割り当てと一致し、
自己近傍を含む 70 個で冪等性と推移性の判定が一致した。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

走査で本文との差が一つ見つかった。本文は二段分解可能だが推移的でない反例を三元舞台で挙げているが、
二元舞台に既に反例がある（$N(a)=\{a,b\}$、$N(b)=\{a\}$）。本文は存在だけを主張しており最小性を
主張していないため人手証明は変えず、検算側でこの二元反例も個別に検査して `overview.md` へ記録した。

構造化テキストの検査と PDF（103 ページ、未解決参照 0）、本章 SageMath 5 本、検算対応検査 54 件、
Lean 全体ビルド、sorry 検査を通した。対象は `SageMath まで`。
次 tick は本章をレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 合成冪等な近傍割り当ての特徴づけ（2026-08-28 07:12）

前 tick の Lean 必要十分版・具体版への導出・検査入口・台帳を突き合わせ、有限性を除いた全射性の
論証に修正を要する不一致は見つからなかった。対象表が全て完了していたため、新対象
「合成冪等な近傍割り当ての特徴づけ」を起票した。

構造化記述では、合成近傍についての冪等性を、近傍関係の推移性と、各近傍辺が二段の近傍辺へ
分解できることの組として特徴づけた。自己近傍を含む場合は二段分解可能性が自動的に従うため、
冪等性と推移性が同値になる。一般には二条件が互いを含意しないことを、二元舞台と三元舞台の
有限反例で示した。有限集合・有限部分集合・写像だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（103 ページ、未解決参照 0、組めない文字 0、版面外へ出た行 0）、
検算対応検査（53 件）、前 tick の SageMath 5 本、Lean 全体ビルド、sorry 検査を通した。
対象は `構造化記述まで`。次 tick は本章をレビュー後、推移性、二段分解可能性、同値、二つの反例、
有限決定を分離した SageMath 検算だけを作る。

## 自動ループ tick: 役割交換による全射性の Lean 必要十分版と導出（2026-08-28 06:12）

前 tick の Lean 具体版の全射性を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は
見つからなかった。Lean 必要十分版の全射性を、有限性から出す論法から、役割交換で得る
右逆写像 $\tau$ による論法へ差し替えた。これにより可逆な近傍割り当てが置換に対応することの
特徴づけと、有限表現の可逆性との同値から、舞台の有限性の仮定 `Finite V` を削除できた。
舞台の有限性が実際に要るのは、可逆元を置換の像として並べる有限表と個数公式の段だけである。
具体版が選んだ一元値の写像が必要十分版のものと一致すること（どちらも同じ一元集合の唯一の元）と、
具体版の全射性がその特殊化であることを導出定理として置いた。新規 4 定理を sorry 検査対象へ
登録した。$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（101 ページ、未解決参照 0）、本章 SageMath 5 本、検算対応検査（53 件）、
Lean 全体ビルド、sorry 検査を通した。対象は四層が揃い `done`。
次 tick は対象表が全て `done` なので、新しい対象を起票して構造化記述から始める。

## 自動ループ tick: 役割交換による全射性の Lean 具体版（2026-08-28 05:12）

前 tick の SageMath 検算を人手証明と突き合わせ、修正を要する不一致は見つからなかった。
Lean 具体版に、役割交換で得る $M(u)=\{\tau(u)\}$ と
$N(\tau(u))=\{u\}=\{\sigma(\tau(u))\}$ から、右逆像 $\tau(u)$ を明示して $\sigma$ の
全射性を導く定理を追加した。置換の構成はこの定理を使い、`Finite.injective_iff_surjective` への
依存を除いた。新定理は sorry 検査対象へ登録した。有限舞台の具体版なのでファイル全体の
`Fintype V` は保つが、全射性の証明では有限性を使わない。$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF、本章 SageMath 5 本、検算対応検査、Lean 全体ビルド、sorry 検査を通した。
対象は `Lean 具体版まで`。次 tick は Lean 必要十分版から `Finite V` を除き、具体版の導出を作り直す。

## 自動ループ tick: 役割交換による全射性の SageMath 検算（2026-08-28 04:12）

前 tick が差し替えた全射性の人手証明を定義へ戻って検査し、誤りは見つからなかった。
SageMath の検算を、有限性から全射性を出す段から、役割交換で得る右逆写像 $\tau$ による段へ
作り直した。新規 `check_role_swap_surjectivity.sage` は役割交換した一元性の論証の各段、
$\{\sigma(\tau(u))\}=N(\tau(u))=\{u\}$ の二つの等号、witness $\tau(u)$ による全射性を分けて検査する。
既存 `check_permutation_from_invertible.sage` の全射性の段も同じ経路へ置き換えた。
$0\le|V|\le3$ の全 531 割り当て、逆元を持つ 10 組で PASS。$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF、本章 SageMath 5 本、検算対応検査、Lean 全体ビルド、sorry 検査を通した。
対象は `SageMath まで`。次 tick は Lean 具体版の全射性を
`Finite.injective_iff_surjective` から役割交換の右逆写像へ書き換える（現在は人手証明と食い違う）。

## 自動ループ tick: 可逆な近傍割り当ての全射性から有限性を除去（2026-08-28 03:12）

前 tick の必要十分版と導出を全層・検査入口と突き合わせた。左右逆元を持つ $N,M$ について、
一元性の論証を役割交換して適用すると、各 $u$ に対して $M(u)=\{\tau(u)\}$ かつ
$N(\tau(u))=\{u\}$ を得る。一方 $N(v)=\{\sigma(v)\}$ なので
$\sigma(\tau(u))=u$ となり、$\sigma$ の全射性に舞台の有限性は不要である。

人手証明をこの経路へ差し替えた。有限性は可逆元の個数と有限決定にだけ残る。
$\mathbb R/\mathbb C$ 脱出はない。構造化テキストの検査と PDF、既存 SageMath 検算 4 本、
検算対応検査、Lean 全体ビルド、sorry 検査を全て通した。対象は `構造化記述まで`。
次 tick は SageMath の置換構成検算を、
有限性から全射性を出す段から、役割交換で得る右逆写像による段へ作り直す。

## 自動ループ tick: 有限近傍割り当てモノイドの可逆元の Lean 必要十分版と導出（2026-08-28 02:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算 4 本・検査入口・sorry 検査対象と突き合わせ、
修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/NeighborhoodAssignmentMonoidUnits.lean` を追加した。
近傍の値を `Set` で表せば、一元性・置換との対応・逆向き・逆元の一意性・置換対応の単射性は
型に等号判定を要求せずに成り立つ。具体版の等号判定は `Finset` 表現のための要求である。
舞台の有限性は「単射な自己写像は全単射である」の一箇所でしか使わないので `Fintype V` を
`Finite V` へ弱めた。`Fintype V` が要るのは可逆元の有限表と個数の段だけである。
有限表現との橋渡し（集合として読む写像の単射性、合成・自己近傍・置換近傍の保存、可逆性の同値）と、
具体版の特徴づけ・逆元の一意性・置換対応の単射性・個数公式の導出を別々の定理に置いた。
新規 17 定理を sorry 検査対象へ登録した。$\mathbb R/\mathbb C$ 脱出はない。

**次の対象**: 一元性の補題を二つの割り当ての役割を入れ替えて適用すれば、有限性を使わずに
全射性が出る見込みがある。人手証明の手順の変更にあたるため本 tick では行わず起票した。

対象は四層が揃い `done`。

## 自動ループ tick: 有限近傍割り当てモノイドの可逆元の Lean 具体版（2026-08-28 01:12）

前 tick の構造化記述と SageMath 検算を合成近傍・自己近傍・単位律・結合律の正本へ戻って
突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentMonoidUnits.lean` に、左右逆元から
各近傍を一元集合にする前半、その唯一元から置換を構成する後半、逆置換が左右の
逆元を与える逆向き、単位律と結合律による逆元の一意性を人手証明と同じ順序で形式化した。
置換から作る可逆元の有限表、その所属と可逆性の同値、置換から近傍割り当てへの対応の
単射性、個数公式 `(Fintype.card V).factorial` も証明した。検査入口と sorry 検査対象へ接続済み。
有限集合・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 有限近傍割り当てモノイドの可逆元の SageMath 検算（2026-08-28 00:12）

前 tick の構造化記述を、合成近傍・自己近傍割り当ての定義、単位律・結合律の claim と突き合わせて
レビューした。可逆性から各近傍が一元集合になること、置換の構成と全単射性、逆向きの式変形に数学的な
誤りは無かったが、個数の claim が逆元の一意性を主張しないまま「各逆元を決定できる」と書いていた。
単位律と結合律から逆元の一意性を出す claim を本文へ追加し、個数の claim がそれを引くようにした。

`sagemath/check/neighborhood-assignment-monoid-units/` に検算 4 本を追加した。本文の前半（一元集合性を
出す五段）、後半（置換の構成・単射性・全単射性・$N=P_\sigma$・一意性）、逆向きの式変形の各行、
個数と有限決定を別々のファイルへ分け、最終式だけの一致で済ませていない。舞台元数 0..3 の近傍割り当て
531 個を全走査し、可逆元は順に 1, 1, 2, 6 個で $n!$ と一致、逆元は全ての可逆元で一意だった。
$|V|=4$ は順序対が 42 億組になるため走査していない。有限範囲の全数検査であって一般証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

対象は `SageMath まで`。次 tick は本検算を全層と突き合わせた後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。

検証は構造化テキストの検査と PDF（101 ページ、未解決参照 0、版面外へ出た行 0）、本章の SageMath 4 本、
検算と証明の対応検査（53 件）、Lean 全体ビルド、sorry 検査を全て通した。

## 自動ループ tick: 有限近傍割り当てモノイドの可逆元の構造化記述（2026-08-27 23:12）

前 tick の「有限近傍割り当てモノイドの中心」の Lean 必要十分版と具体版からの導出を、構造化記述、
SageMath 検算、Lean 具体版、検査入口、sorry 検査対象と突き合わせた。中心の特徴づけ、有限表現との
橋渡し、具体版への特殊化に修正を要する不一致は見つからなかった。

対象表が全て `done` だったため、新対象「有限近傍割り当てモノイドの可逆元」を起票した。構造化記述
`structured-latex/content/neighborhood-assignment-monoid-units.ts` に、合成近傍についての可逆元と、
有限舞台の置換が定める一元近傍割り当てを定義した。左右の逆元等式から各近傍が空でない一元集合で
あることを順に導き、その唯一の元が定める自己写像が単射、したがって有限舞台上で全単射になることを
証明した。逆向きは逆置換が逆元を与える。可逆元の個数は舞台元数を $n$ として $n!$ であり、可逆元・
逆元・可逆性は有限決定できる。有限集合・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `構造化記述まで`。次 tick は本章をレビュー後、一元性、置換との対応、逆元、個数と有限決定を
分離した SageMath 検算だけを作る。

検証は構造化テキストの検査と PDF（101 ページ、未解決参照 0）、前 tick の SageMath 検算 4 本、
検算と証明の対応検査（52 件）、Lean 全体ビルド、sorry 検査を全て通した。

## 自動ループ tick: 有限近傍割り当てモノイドの中心の Lean 必要十分版と導出（2026-08-27 22:12）

前 tick の Lean 具体版を、構造化記述・SageMath 検算 4 本・検査入口・sorry 検査対象と突き合わせた。
中心の定義、証人、第一段から最終段までの順序、有限決定に修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/NeighborhoodAssignmentMonoidCenter.lean` を追加した。
中心の特徴づけは、値を `Set` で表せば型にインスタンスを一つも要求せずに成り立つ。具体版の
`Fintype V` と `DecidableEq V` は、近傍を `Finset` で表すことと中心を有限列挙することのための
要求であって、特徴づけそのものは有限性を要求しない。証人も `Set` では等号判定なしに書け、
$p \neq q$ で値が空になることは連言の第一成分としてそのまま出る。始域と終域が同じ型であることは
落とせない（中心が二つの合成の比較だから）。空でない仮定は第三段でだけ要る。空舞台も含まれる。
有限表現版を同じ順序で証明し、両表現の中心所属が同値であることで等号判定が表現のためだけに要ることを
示した。導出として、具体版の述語が有限表現版の述語そのものであること、証人の一致、各段と表への所属が
特殊化として得られることを別々の定理に置いた。新規 20 定理を sorry 検査対象へ登録した。

構造化テキストの検査、PDF（100 ページ、未解決参照 0）、本章の SageMath 4 本、検算対応検査 52 件、
Lean 全体ビルド、sorry 検査を全て通した。有限集合・有限部分集合・自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。本対象は四層が揃い `done`。次 tick は本必要十分版と導出を
レビューし、次の対象を起票する。

## 自動ループ tick: 有限近傍割り当てモノイドの中心の Lean 具体版（2026-08-27 21:12）

前 tick の SageMath 検算 4 本を再実行し、構造化記述の証明段と一致して全て PASS することを確認した。
Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentMonoidCenter.lean` に、中心所属、向き付き辺の
証人、空近傍と自己近傍の中心所属、中心元の辺が自己ループになること、空でない中心元が全自己ループを
持つこと、中心が空近傍と自己近傍だけからなる特徴づけ、中心の有限列挙表を、人手証明と同じ順序で
形式化した。検査入口へ接続し、新規 6 定理を sorry 検査対象へ登録した。有限集合・有限部分集合・
自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF、本章の SageMath 4 本、検算対応検査、Lean 全体ビルド、sorry 検査を
全て通した。対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出
だけを作る。

## 自動ループ tick: 有限近傍割り当てモノイドの中心の SageMath 検算（2026-08-27 20:12）

前 tick の構造化記述を、合成近傍・空近傍・自己近傍の定義の正本と突き合わせてレビューした。中心の
定義、証人 $E_{a,b}$、証明の四段、空舞台で右辺が一元集合になることに、修正を要する不一致は
見つからなかった。

`sagemath/check/neighborhood-assignment-monoid-center/` に検算 4 本を追加した。$O_V$ と $I_V$ が
中心に属すること、$q\in N(p)$ から $p=q$ を出す段、自己ループから $I_V\subseteq N$ を出す段、
特徴づけと有限決定を、それぞれ本文の証明の段ごとに別ファイルへ分けた。舞台元数 0..3 の近傍割り当て
531 個を全て走り、中心の元が各舞台で $O_V$ と $I_V$ だけであることを確認して全て PASS。$|V|=4$ は
順序対が 4,294,967,296 組になるため走査していない。有限範囲の全数検査であって一般証明ではない。
有限集合・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（100 ページ、未解決参照 0）、本章の SageMath 4 本、検算対応検査 52 件、
Lean 全体ビルド、sorry 検査を通した。対象は `SageMath まで`。次 tick は本検算を全層と突き合わせた
後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。

## 自動ループ tick: 有限近傍割り当てモノイドの中心の構造化記述（2026-08-27 19:12）

前 tick の「自己転置な近傍割り当て全体の合成閉性の特徴づけ」の Lean 必要十分版と導出を、
構造化記述、SageMath 検算、Lean 具体版、検査入口、sorry 検査対象と突き合わせた。閉性と
`Subsingleton V` の同値、有限表現との橋渡し、具体版への特殊化に修正を要する不一致は見つからなかった。

対象表が全て `done` だったため、新対象「有限近傍割り当てモノイドの中心」を起票した。構造化記述
`structured-latex/content/neighborhood-assignment-monoid-center.ts` に中心と一つの向き付き辺だけを持つ
近傍割り当てを定義し、中心が空近傍割り当てと自己近傍割り当てだけからなることを証明した。中心元が
空でなければ、一つの辺と可換することから全ての自己ループを持つこと、任意の辺が自己ループであることを
順に導く。中心所属は二つの有限写像との等号へ帰着するので有限決定できる。有限集合・有限部分集合・
自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `構造化記述まで`。次 tick は本章の証明をレビュー後、中心の二つの包含と有限決定を分離した
SageMath 検算だけを作る。

## 自動ループ tick: 自己転置な近傍割り当て全体の合成閉性の特徴づけの Lean 必要十分版と導出（2026-08-27 18:12）

前 tick の Lean 具体版を、構造化記述の正本
`structured-latex/content/self-transpose-composition-total-closure-characterization.ts`、
SageMath 検算 4 本、検査入口、sorry 検査対象と突き合わせた。閉性の述語、順方向の二つの証人と
非可換性、逆方向の合成所属条件の交換、同値の向きと順序に修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/SelfTransposeCompositionTotalClosureCharacterization.lean`
を追加した。閉性と `Subsingleton V` の同値は、値を `Set` で表せば型にインスタンスを一つも要求せずに
成り立つ（`setAllSelfTransposeCompositionClosed_iff_subsingleton`）。具体版の条件
`Fintype.card V ≤ 1` は有限舞台での `Subsingleton V` の言い換えにすぎず、主張そのものが有限性を
要求してはいない。順方向に要るのは前章の必要十分版の非閉性（相異なる二元だけを使う
`not_closed_of_exists_ne`）であり、逆方向に要るのは合成の証人と出力元を入力セルへ移す二回の
`Subsingleton.elim` と連言の交換だけである（`setComp_comm_of_subsingleton`）。自己転置性は
逆方向では前章の同値へ渡す最後の一段でしか使わない。空舞台も `Subsingleton V` に含まれ、
証明はそのまま通る。有限性と等号判定が要るのは、転置を `Finset.univ.filter`、合成を
`Finset.biUnion` で書く有限表現の段だけであることを、同じ順序の証明
（`hetAllSelfTransposeCompositionClosed_iff_subsingleton`）と、両表現の閉性が同値であること
（`hetAll_iff_setAll`）で示した。導出として、具体版の述語が有限表現版の述語そのものであること
（`allSelfTransposeCompositionClosed_eq_het`、定義の一致）、具体版の同値が必要十分版の同値と
`Fintype.card V ≤ 1 ↔ Subsingleton V` の合成で得られること、具体版の証人の非可換性が有限表現版の
特殊化であることを別々の定理として置いた。新規 9 定理を sorry 検査対象へ登録した。

構造化テキストの検査、PDF（98 ページ、未解決参照 0、版面外へ出た行 0）、SageMath 4 本、
検算対応検査 51 件、Lean 全体ビルド、sorry 検査を通した。有限集合・有限部分集合・自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。本対象は四層が揃い `done`。
次 tick は本必要十分版と導出を全層とレビューし、次の対象を起票する。

## 自動ループ tick: 自己転置な近傍割り当て全体の合成閉性の特徴づけの Lean 具体版（2026-08-27 17:12）

前 tick の SageMath 検算 4 本を構造化記述と突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/SelfTransposeCompositionTotalClosureCharacterization.lean` を追加した。
全ての自己転置な近傍割り当てが合成で閉じるという述語を有限舞台上で定義し、それが舞台元数一以下と
同値であることを証明した。順方向は相異なる二元から自己ループと対称辺の証人を作って非可換性を示し、
逆方向は全ての舞台元が等しいことから二つの合成の所属条件を交換した。人手証明と同じ対象・仮定・順序で
ある。検査入口へ接続し、新規 4 定理を sorry 検査対象へ登録した。有限集合・有限部分集合・自然数だけで
閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は `Lean 具体版まで`。次 tick は全層との対応をレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 自己転置な近傍割り当て全体の合成閉性の特徴づけの SageMath 検算（2026-08-27 16:12）

前 tick の構造化記述を、合成近傍・転置・自己転置性の定義の正本と前章の同値（合成が自己転置 ⟺ 可換）へ
突き合わせてレビューし、修正を要する不一致は見つからなかった。

`sagemath/check/self-transpose-composition-total-closure-characterization/` に検算 4 本を追加した。
二つの証人の自己転置性、二つの合成の値の相違による非閉性、舞台元数が一以下のときの六段の同値連鎖、
特徴づけの有限決定を、本文の証明の段ごとに分けて別ファイルにした。証人の検査は舞台元数 2..5 の
相異なる順序対 40 組、閉性の全走査判定は舞台元数 0..3（可換な順序対は 1、4、42、1,204）で全て PASS。
舞台元数 4 以上の全走査は行っておらず一般証明ではない。$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（98 ページ、未解決参照 0）、SageMath 4 本、検算対応検査 51 件、
Lean 全体ビルド、sorry 検査を通した。対象は `SageMath まで`。次 tick はレビュー後、Lean 具体版だけを作る。

## 自動ループ tick: 自己転置な近傍割り当ての合成閉性の Lean 必要十分版と導出（2026-08-27 14:12）

前 tick の Lean 具体版を構造化記述と SageMath 検算 4 本へ突き合わせた。同値の三段、二つの証人と
その自己転置性、二つの合成の値、非自己転置性、有限決定に修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/SelfTransposeCompositionClosure.lean` を追加した。
合成が自己転置であることと可換性の同値は、値を `Set` で表せば型にインスタンスを一つも要求せずに
成り立つ。始域と終域が同じ型であることは落とせない（自己転置性も二つの合成の比較も同じ型を要求する）。
反例に要るのは舞台が相異なる二元を持つことだけで、舞台元数が二であることではない。証人の自己転置性は
$a \neq b$ すら使わず、$a \neq b$ は二つの合成の値を分離する段だけで要る。有限性と等号判定は
`Finset` 表現と有限表の段でだけ要る。具体版の全主張が特殊化として得られる導出定理を具体版の末尾へ
付けた。逆向き（相異なる二元が無ければ合成で閉じること）は人手証明が述べていないので主張していない。

検査入口へ接続し、新規 23 定理を sorry 検査対象へ登録した。$\mathbb R/\mathbb C$ 脱出はない。
検証は構造化テキストの検査、PDF（97 ページ、未解決参照 0）、SageMath 4 本、検算対応検査 50 件、
Lean 全体ビルド、sorry 検査を通した。対象は四層が揃ったので `done`。次 tick は本層をレビュー後、
新対象を起票する。

## 自動ループ tick: 自己転置な近傍割り当ての合成閉性の Lean 具体版（2026-08-27 13:12）

前 tick の SageMath 検算 4 本を再実行し、合成の自己転置性と可換性の同値、二つの証人の
自己転置性、二元舞台の非閉性反例、有限決定の各段が構造化記述と一致して全て PASS することを
確認した。修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/SelfTransposeCompositionClosure.lean` を追加した。転置が合成順序を
反転する既証明へ二つの自己転置性を順に代入して同値を示し、二元舞台の自己ループと対称辺の証人、
それぞれの自己転置性、二つの合成の値と非等号、合成が自己転置でないことを、人手証明と同じ順序で
形式化した。自己転置な順序対と、その合成が自己転置になるかを有限表で判定する定理も追加した。
検査入口へ接続し、新規 8 定理を sorry 検査対象へ登録した。有限集合・有限部分集合・有限表の等号だけで
閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 自己転置な近傍割り当ての合成閉性の SageMath 検算（2026-08-27 12:12）

前 tick の構造化記述を、合成近傍・転置・自己転置性の定義の正本と突き合わせてレビューし、
修正を要する誤りは見つからなかった。

`sagemath/check/self-transpose-composition-closure/` に検算 4 本を追加した。合成の自己転置性と
可換性の同値、二元舞台の二つの証人が自己転置であること、二つの合成の値が異なることによる明示反例、
有限決定の手続きを、それぞれ本文の証明の段ごとに分けて別ファイルにした。走査は舞台元数 0..3 の
全数検査（自己転置な割り当ての順序対 4,165 組）で全て PASS。|V| >= 4 は走査しておらず一般証明では
ない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

走査で、合成が自己転置になる順序対が |V| = 0,1,2,3 でそれぞれ 1、4、42、1,204（全体は 1、4、64、
4,096）であり、走査範囲で合成閉性が成り立つのは |V| <= 1 のときだけだと観測した。本文はこれを
主張していない（観測にとどめている）。

検証は本章の SageMath 4 本、検算対応検査 50 件、構造化テキストの検査、PDF（97 ページ、
未解決参照 0、版面外へ出た行 0）、Lean 全体ビルドと sorry 検査を通した。対象は `SageMath まで`。
次 tick は本検算をレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 自己転置な近傍割り当ての個数の Lean 必要十分版と導出（2026-08-27 10:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、自己転置性と所属対称性の同値、
非順序対符号と復元の相互逆性、全単射、非順序対の個数、`2^(n(n+1)/2)`、有限表の各段に修正を要する
不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/SelfTransposeNeighborhoodAssignmentCount.lean` を
追加した。検査結果は三段に分かれる。第一に、自己転置性の特徴づけ、`Sym2 V` への符号化、復元、
両合成が恒等であること、全単射は、値を `Set` で表せば型にインスタンスを一つも要求せずに成り立つ。
使うのは所属の向きの反転、`s(v,w) = s(w,v)`、外延性だけである。第二に、始域と終域が同じ型で
あることはこの章では落とせない（自己転置性 `Nᵀ = N` が同じ型に住むことを要求するため。前章の
転置そのものは異なる型の間で書けた）。第三に、非順序対の個数と自己転置な割り当ての個数には
`Fintype V` だけが要り、`DecidableEq V` は要らない。等号判定が要るのは `Finset` 表現と有限表を
作る段だけである。具体版の主要主張 11 本を必要十分版の特殊化として導出し、新規 26 定理を
sorry 検査対象へ登録した。有限集合・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$
脱出はない。

検証は Lean 全体ビルドと sorry 検査、本章の SageMath 検算 5 本の再実行（全て PASS）、検算対応
検査 49 件、構造化テキストの検査、PDF（96 ページ、未解決参照 0、版面外へ出た行 0）を通した。
対象は `done`。次 tick は本必要十分版と導出をレビュー後、新しい対象を起票する。

## 自動ループ tick: 自己転置な近傍割り当ての個数の Lean 具体版（2026-08-27 09:12）

前 tick の SageMath 検算 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/SelfTransposeNeighborhoodAssignmentCount.lean` を追加した。
本文の非空な一元・二元部分集合 `{v,w}` を `Sym2 V` で符号化し、自己転置性と所属対称性の同値、
非順序対符号と復元の相互逆性および全単射、非順序対の個数 `n + choose n 2 = n(n+1)/2`、
自己転置な近傍割り当ての個数 `2^(n(n+1)/2)`、有限表による決定を人手証明と同じ順序で形式化した。
検査入口へ接続し、新規 10 定理を sorry 検査対象へ登録した。有限集合・有限部分集合・自然数だけで
閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 自己転置な近傍割り当ての個数の SageMath 検算（2026-08-27 08:12）

前 tick の構造化記述「自己転置な近傍割り当ての個数」を、近傍割り当て・転置の定義の正本と
突き合わせてレビューした。自己転置性と近傍所属の対称性の同値、非順序対符号 $\varepsilon_V$ の
所属条件が表示順序に依存しないこと、復元 $\rho_V$ との相互逆性、$n+\binom n2=n(n+1)/2$ の
計算、$2^{n(n+1)/2}$ への到達に、修正を要する誤りは見つからなかった。

`sagemath/check/self-transpose-neighborhood-assignment-count/` に検算 5 本を追加した。
自己転置性と所属の対称性の同値（順方向・逆方向を段ごとに分離）、非順序対の個数（非交和・
一元部分集合の全単射・二項係数・通分・分配律・偶数性を分離）、符号と復元が互いに逆であること
（表示順序への非依存、復元の自己転置性、両向きの合成が恒等）、個数 $2^{n(n+1)/2}$、
有限決定（列挙の個数 $2^{n^2}$、転置表を作る所属判定 $n^2$ 回、自己転置性の値比較 $n$ 回）を
それぞれ別ファイルにした。走査は舞台元数 $0..4$ の全数検査（近傍割り当て 66,067 個、うち
自己転置 1,099 個。個数の等式だけは $0..7$）で全て PASS。$|V|\ge5$ は走査しておらず、
これは有限範囲の全数検査であって一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（96 ページ、未解決参照 0、版面外へ出た行 0）、検算対応検査 49 件、
Lean 全体ビルドと sorry 検査も通した。対象は `SageMath まで`。

次 tick は本検算をレビュー後、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 自己転置な近傍割り当ての個数の起票と構造化記述（2026-08-27 07:12）

前 tick の「近傍割り当ての転置対合」の Lean 必要十分版と導出を全層・検査入口と突き合わせ、
転置の所属同値、対合性、合成順序の反転、点ごとの和・積と自己近傍の保存、有限表と全単射に
修正を要する不一致は見つからなかった。

対象表が全て `done` だったため、SageMath 走査で得た「自己転置な近傍割り当ての個数」を起票した。
構造化記述 `structured-latex/content/self-transpose-neighborhood-assignment-count.ts` で、自己転置性と
近傍所属の対称性の同値、セルの非空な一元・二元部分集合による符号化と復元、その二写像が互いに
逆であること、非順序対の個数 $n(n+1)/2$、自己転置な近傍割り当ての個数
$2^{n(n+1)/2}$ と有限決定可能性を証明した。有限集合・有限部分集合・自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキストの検査と PDF（96 ページ、未解決参照 0、
版面外へ出た行 0）、前対象の SageMath 検算 5 本、検算対応検査 48 件、Lean 全体ビルドと
sorry 検査を通した。対象は `構造化記述まで`。

次 tick は本記述をレビュー後、各証明段を分けた SageMath 検算だけを作る。

## 自動ループ tick: 近傍割り当ての転置対合の Lean 必要十分版と導出（2026-08-27 06:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、転置の定義、所属の向きの反転、
対合性、合成の証人を反転する段、点ごとの和・積と自己近傍割り当ての保存、有限表と全単射の
いずれにも修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/NeighborhoodAssignmentTransposeInvolution.lean` を
追加した。分離できた構造は次のとおりである。**転置そのものには型のインスタンスが一つも要らない。**
値を `Set` で表せば、所属の向きの反転、対合性、合成順序の反転、点ごとの和・積の保存、
自己近傍割り当ての保存がすべて有限性も等号判定も無しに成り立つ。**始域と終域が同じ型である
必要も無い。** 転置は `V → Finset W` を `W → Finset V` へ移す型の入れ替えであり、
合成順序の反転は互いに異なる三つの型の上で成立する。有限性と等号判定が要るのは部分集合を
`Finset` として表す段だけで、`Set` 版との対応は橋渡し定理で示した。自己近傍割り当ての保存に
だけ始域と終域の一致が要る（型が閉じないと主張自体が書けない）。全単射性は対合性だけから従い、
有限性を追加で使わない。

具体版の転置・所属同値・対合性・合成順序の反転・和と積と自己近傍の保存・全単射・有限表を、
必要十分版の同じ舞台への特殊化として導出する定理 10 本を置いた。新規 25 定理を sorry 検査対象へ
登録した。有限集合・有限写像表だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

検証は Lean 全体ビルドと sorry 検査、構造化テキストの検査、PDF（94 ページ、未解決参照 0、
版面外へ出た行 0）、本章の SageMath 5 本、検算対応検査 48 件を通した。四層が揃ったので
対象は `done`。次 tick は本必要十分版と導出をレビューし、次の対象を起票する。

## 自動ループ tick: 近傍割り当ての転置対合の Lean 具体版（2026-08-27 05:12）

前 tick の SageMath 検算 5 本を構造化記述と突き合わせ、転置所属、対合性、合成順序の反転、
点ごとの和・積と自己近傍の保存、有限表の各段に修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentTransposeInvolution.lean` に、有限舞台上の
転置、所属同値、対合性、合成順序の反転、点ごとの和・積と自己近傍割り当ての保存、転置の有限表と
全単射を、人手証明と同じ順序で形式化した。検査入口へ接続し、具体版の 8 定理を sorry 検査対象へ
登録した。有限集合・有限写像表だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
対象は `Lean 具体版まで`。

次 tick は本具体版を全層と突き合わせた後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 近傍割り当ての転置対合の SageMath 検算（2026-08-27 04:12）

前 tick の構造化記述を既出の合成近傍・点ごとの和・積・自己近傍割り当ての定義と突き合わせ、
修正を要する誤りは見つからなかった。

`sagemath/check/neighborhood-assignment-transpose-involution/` に検算 5 本を追加した。
所属の向きの反転、対合性、合成順序の反転、点ごとの和・積と単位元の保存、転置表の有限決定を
別ファイルに分け、本文の式変形を段ごとに検査した。走査は $|V|\leq3$ の全数（近傍割り当て 531 個、
組 262,405 組）で全て PASS。$|V|\geq4$ は走査していない。

走査で本文にない事実が二つ出た。順序を反転しない等式 $(N\star M)^{\mathsf T}=N^{\mathsf T}\star M^{\mathsf T}$
は一般には成り立たず反例の最小舞台元数は 2 であること、自己転置な近傍割り当ての個数が
$2^{|V|(|V|+1)/2}$ であること。どちらも本文は主張していないので記述との差ではなく、
overview.md へ記録した。

検証は本章の SageMath 5 本、検算対応検査 48 件、構造化テキストの検査、PDF（94 ページ、
未解決参照 0）、Lean 全体ビルドと sorry 検査を通した。対象は実態どおり `SageMath まで`。

次 tick は本検算をレビュー後、本章の Lean 具体版だけを作る。

## 自動ループ tick: 近傍割り当ての転置対合の起票と構造化記述（2026-08-27 03:12）

前 tick の非分配反例の最小舞台の Lean 必要十分版と導出を全層・検査入口と突き合わせ、
有限性と subsingleton 性の分離、相異なる二元からの左右反例、具体版への特殊化に修正を要する
不一致は見つからなかった。

対象表が全て `done` だったため、新対象「近傍割り当ての転置対合」を起票した。構造化記述
`structured-latex/content/neighborhood-assignment-transpose-involution.ts` で、近傍所属の向きを逆にする
転置を有限舞台上で定義し、対合性、合成順序の反転、点ごとの和・積と自己近傍の保存、転置表と
各法則の有限決定を証明した。有限集合・有限部分集合・所属判定だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキストの検査と PDF（94 ページ、未解決参照 0）、
前対象の SageMath 検算 4 本、検算対応検査 47 件、Lean 全体ビルドと sorry 検査を通した。
対象は実態どおり `構造化記述まで`。

次 tick は本記述をレビュー後、対合・合成順序反転・和と積および単位元の保存・有限決定を
分けた SageMath 検算だけを作る。

## 自動ループ tick: 非分配反例の最小舞台の Lean 必要十分版と導出（2026-08-27 02:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算 4 本と突き合わせ、修正を要する不一致は
見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/NeighborhoodAssignmentIntersectionMinimalCounterexample.lean`
を追加した。合成と点ごとの積の一致に実際に要るのは、舞台が相異なる二元を持たないことだけであり、
**具体版が仮定していた舞台の有限性は要らない**（具体版は $|V|\leq1$ から subsingleton を取り出す
段でしか有限性を使っていない）。等号判定は有限集合の共通部分と合併を書くためだけに要る。
始域と終域が同じ型であることは削れない。左右の反例に要るのは相異なる二点だけで、有限性も
元数の指定も要らず、**$a\neq b$ は削れない**（削ると舞台は subsingleton になり分配律が成り立つ）。

この二つを合わせて「左（右）分配律が全ての近傍割り当てで成り立つ $\iff$ 舞台が相異なる二元を
持たない」という同値を定理として置いた。**具体版の「最小舞台元数は二」はこの同値の有限舞台への
特殊化である。** 具体版側の導出節 14 本で、演算の一致、一元以下での一致と左右の分配律、
二元舞台の証人が $a:=0,\ b:=1$ への特殊化であること、最小舞台定理を示した。

入口 import と sorry 検査へ必要十分版 12 定理・導出 14 定理を登録した。検証は Lean 全体ビルドと
sorry 検査、本章の SageMath 4 本、検算対応検査 47 件、構造化テキストの検査、PDF（92 ページ、
未解決参照 0）を通した。四層が揃ったので対象は `done`。

次 tick は本必要十分版と導出をレビュー後、対象表が全て `done` なので新対象を起票して
構造化記述だけを作る。

## 自動ループ tick: 非分配反例の最小舞台の起票と構造化記述（2026-08-26 23:12）

前 tick の点ごとの積の Lean 必要十分版・導出を全層と検査入口まで突き合わせ、修正を要する
不一致がないことを確認した。対象表が全て `done` だったため、SageMath 走査が発見した
「非分配反例の最小舞台は二元」を新対象に起票した。

構造化記述 `structured-latex/content/neighborhood-assignment-intersection-minimal-counterexample.ts` で、
舞台が一元以下なら合成と点ごとの積が一致することから左右の分配律を導き、二元舞台の
左右それぞれの明示反例を計算した。よって最小舞台元数はどちらも二である。有限集合・有限写像・
自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は `構造化記述まで`。

次 tick は本記述をレビュー後、一元以下での反例の非存在と、二元舞台の左右の反例を
分けた SageMath 検算だけを作る。

## 自動ループ tick: 点ごとの積と合成の非分配性の Lean 必要十分版と導出（2026-08-26 22:12）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/NeighborhoodAssignmentIntersectionNondistributivity.lean`
を追加した。積の可換・結合・冪等律、二つの分配律、和の最小上界性と積の最大下界性のいずれにも
舞台の有限性は要らず、要るのは値の集合を取る先の型の等号判定だけである。始域と終域は異なる型でよい。
**単位律だけ終域の有限性を落とせなかった**が、それは全近傍の値 `Finset.univ` を有限部分集合として
書くための要求であって積の性質ではない。部分集合を `Set` で表せば全近傍は `Set.univ` として
インスタンス無しに書け、単位律・分配律・上下界はすべて成り立つ（橋渡し定理で対応を示した）。

左右の非分配性は、三元舞台の反例が実際に使う条件だけを取り出した。左は $a$、相異なる $b\neq c$、
$z$ があって $N(a)=\{b\}$、$M(a)=\{c\}$、$z\in L(b)$、$z\in L(c)$ で足り、三つの型は互いに
異なってよく有限でなくてよい。**$b\neq c$ は削れない**。右は $b,c\in L(a)$、$z\in N(b)$、$z\in M(c)$、
$L(a)$ のどの元でも $z$ が両側には入らないこと、で足り **$b\neq c$ の仮定は要らない**。
有限性が要るのは積の全演算表と分配性の有限表を作る段だけである。

具体版の全主張が必要十分版の特殊化として得られる導出定理を `Derivation` へ追加し、入口 import と
sorry 検査へ新規 49 定理を登録した。検証は SageMath 5 本、検算対応検査 46 件、構造化テキストの検査と
PDF（90 ページ、未解決参照 0）、Lean 全体ビルドと sorry 検査を通した。四層が揃ったので対象は `done`。

次 tick は本 tick の必要十分版と導出をレビューし、次の対象を起票する。SageMath 走査で出た
「非分配性の最小反例は $|V|=2$ に現れる」を主張として立てるかも、その起票で決める。

## 自動ループ tick: 点ごとの積と合成の非分配性の Lean 具体版（2026-08-26 21:12）

前 tick の SageMath 検算 5 本を再実行し、構造化記述の点ごとの積の四法則、有限分配束の二つの
分配律と最小上界・最大下界、三元舞台上の左右の非分配反例、有限決定可能性と突き合わせた。
修正を要する不一致は見つからなかった。検算で見つかった二元舞台の最小反例は、本文が最小性を
主張していないため、本 tick の具体版では本文どおり三元舞台の反例を形式化した。

Lean 具体版 `lean/CellularAutomata/NeighborhoodAssignmentIntersectionNondistributivity.lean` を追加した。
点ごとの積と全近傍、積の可換・結合・冪等・単位律、和と積の二つの分配律、包含順序に関する和の
最小上界性と積の最大下界性を、人手証明と同じ対象・順序で形式化した。左右の非分配性は、本文の
三元舞台にある二つの反例について、左辺が空近傍、右辺が一元近傍になる計算を分けて示した。
積の全演算表と、左右の分配性を満たす三つ組の有限表も構成した。入口 import と sorry 検査へ
新規定理を登録した。有限型・有限部分集合・有限写像だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は実態どおり `Lean 具体版まで`。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と
具体版からの導出だけを作る。

## 自動ループ tick: 点ごとの積と合成の非分配性の SageMath 検算（2026-08-26 20:12）

前 tick の構造化記述を既出の点ごとの和・包含順序・合成近傍の定義と突き合わせ、点ごとの積の四法則、
分配束の二つの分配律と最小上界・最大下界、三元舞台上の左右二つの明示反例の式変形、有限決定可能性の
いずれにも修正を要する誤りが無いことを確認した。

SageMath 検算 `sagemath/check/neighborhood-assignment-intersection-nondistributivity/` を 5 本追加した。
積の可換・結合・冪等・全近傍の単位律、分配束の二つの分配律と最小上界・最大下界、左側の非分配反例、
右側の非分配反例、積の全演算表と分配性の有限決定可能性を別ファイルに分け、本文の式変形を段ごとに
検査した。全て PASS。

走査で本文にない事実が出た: **非分配性の反例が最初に現れる舞台は $|V|=2$ である**
（$|V|=0,1$ では反例なし、$|V|=2$ では 4,096 三つ組のうち左右それぞれ 420 個が反例）。
本文が挙げた三元反例は最小ではない。本文は最小性を主張していないので記述の誤りではないが、
overview.md へ記録した。最小性を主張として立てるかは次の対象の判断に回す。

有限集合・有限写像表・自然数の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。検証は本章の
SageMath 5 本、検算対応検査（46 件）、構造化テキストの検査と PDF（90 ページ、未解決参照 0）、
Lean 全体ビルドと sorry 検査を通した。対象は `SageMath まで`。
次 tick は本検算をレビュー後、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 近傍割り当ての点ごとの積と合成の非分配性（2026-08-26 19:12）

前 tick の「近傍割り当ての点ごとの和と合成の分配性」を、構造化記述・SageMath 検算 5 本・
Lean 具体版・Lean 必要十分版・具体版からの導出・検査入口と突き合わせた。SageMath 検算は全て PASS、
Lean 全体ビルドと sorry 検査も成功し、修正を要する不一致は見つからなかった。

新対象「近傍割り当ての点ごとの積と合成の非分配性」を起票し、構造化記述
`structured-latex/content/neighborhood-assignment-intersection-nondistributivity.ts` を追加した。
点ごとの共通部分と全近傍割り当てを定義し、点ごとの和・積が包含順序の最小上界・最大下界を与える
有限分配束であることを定義から証明した。一方、合成近傍は点ごとの積を左右いずれからも保存しない。
異なる中間セルから同じ終点へ合流すると共通部分の前後が交換できないことを、三元舞台上の二つの
明示反例で示した。反例と分配性の成否は有限真理値表から有限決定できる。

有限集合・有限写像・所属判定だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は実態どおり
`構造化記述まで`。次 tick は本章をレビュー後、点ごとの積の法則・有限分配束・左右の非分配反例・
有限決定可能性を分けた SageMath 検算だけを作る。

## 自動ループ tick: 点ごとの和と合成の分配性の Lean 必要十分版と導出（2026-08-26 18:12）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、対象・仮定・証明順序が一致することを
確認した。修正を要する不一致は見つからなかった。

`lean/CellularAutomata/NecSuf/NeighborhoodAssignmentUnionDistributivity.lean` に必要十分版を、
具体版ファイル末尾に導出を追加した。和の四法則・包含順序の特徴づけ・左右分配・空近傍の両側吸収は、
舞台の有限性を一切使わず、始域と終域が異なる型のままで成り立つ。部分集合を `Set` で表せば
インスタンスは一つも要らず、`Finset` 版の等号判定は有限表現のためだけであることを橋渡し定理で示した。
第二の分配律では中間の型の等号判定も要らない。有限性が要るのは和の全演算表の段だけで、冪等半環に
なるのは和と合成の型がともに閉じるときだけである。具体版の各主張を特殊化として導出し、新規 40 定理を
sorry 検査へ登録した。$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキスト検査、PDF（88 ページ、未解決参照 0）、SageMath 5 本の再実行（全て PASS）、
検算対応検査（45 件）、Lean 全体ビルドと sorry 検査を通した。対象「近傍割り当ての点ごとの和と
合成の分配性」は四層が揃ったので `done`。次 tick は新しい対象を起票して構造化記述だけを作る。

## 自動ループ tick: 点ごとの和と合成の分配性の Lean 具体版（2026-08-26 17:12）

前 tick の SageMath 検算 5 本を再実行して構造化記述の各段と突き合わせ、全て PASS した。
修正を要する不一致は見つからなかった。

`lean/CellularAutomata/NeighborhoodAssignmentUnionDistributivity.lean` に Lean 具体版を追加した。
有限舞台上の空近傍割り当てと点ごとの和を定義し、和の可換・結合・冪等・単位律、包含順序の
`N \preccurlyeq M \leftrightarrow N \sqcup M=M`、合成近傍の左右分配、空近傍の両側吸収を
人手証明と同じ対象・仮定・順序で形式化した。点ごとの和の全演算表と、その表への任意の演算結果の
所属も形式化した。入口 import と sorry 検査へ新定理を登録した。有限型・有限部分集合・写像だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出を作る。

## 自動ループ tick: 点ごとの和と合成の分配性の SageMath 検算（2026-08-26 16:12）

前 tick の構造化記述を、引用先の定義（合成近傍、点ごとの包含順序、合成モノイドと元数の有限決定）から
再計算して突き合わせ、修正を要する不一致は見つからなかった。

`sagemath/check/neighborhood-assignment-union-distributivity/` に検算 5 本を追加した。点ごとの和の
可換・結合・冪等・単位、包含順序 $N\preccurlyeq M\iff N\sqcup M=M$ の両方向、合成近傍の左右分配、
空近傍割り当ての両側吸収、冪等半環の公理が同じ舞台で同時に成り立つことと両演算表の有限決定を、
それぞれ別のファイルへ分けた。本文の証明の各段（点ごとの定義への展開、集合演算の法則、
所属の含意列、証人の場合分け、写像の外延性）を段ごとに検査し、最終式だけの一致で済ませていない。
検証範囲は和の冪等・単位と吸収が $|V|\le3$ の全 531 割り当て、包含の特徴づけが $|V|\le3$ の全
262,405 組、可換・結合・分配・半環が $|V|\le2$ の全 4,105 三つ組である。全て PASS。
有限範囲の全数検査であり、任意の有限舞台に対する一般証明ではない。浮動小数点と
$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（88 ページ、未解決参照 0）、新規 5 本を含む検算、検算対応検査
（45 件）、前章の SageMath 4 本の再実行、Lean 全体ビルドと sorry 検査を通した。対象は
`SageMath まで`。次 tick は本章の Lean 具体版を、人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 近傍割り当ての点ごとの和と合成の分配性（2026-08-26 15:12）

前 tick の「近傍割り当ての包含順序と合成の単調性」を全層と検査入口まで突き合わせ、SageMath 4 本を
再実行して全て PASS、Lean 全体ビルドも成功した。修正を要する不一致は見つからなかった。

新対象「近傍割り当ての点ごとの和と合成の分配性」を起票し、構造化記述
`structured-latex/content/neighborhood-assignment-union-distributivity.ts` を追加した。空近傍割り当てと
点ごとの合併から、和の可換・結合・冪等律、包含順序の $N\preccurlyeq M\iff N\sqcup M=M$ という
特徴づけ、合成近傍の左右分配、空近傍の両側吸収性を定義から証明した。これらと既証明の合成モノイドを
合わせて有限な冪等半環になることを記録した。有限集合・有限写像・所属判定だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（88 ページ、未解決参照 0）、検算対応検査、前対象の SageMath 4 本、
Lean 全体ビルドを通した。対象は `構造化記述まで`。次 tick は本章をレビュー後、点ごとの和の法則・
包含順序の特徴づけ・左右分配・空近傍の吸収性を分けた SageMath 検算だけを作る。

## 自動ループ tick: 近傍割り当ての包含順序の Lean 必要十分版と導出（2026-08-26 14:12）

前 tick の Lean 具体版を構造化記述と本章の SageMath 検算 4 本（再実行して全て PASS）へ突き合わせ、
修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/OrderedNeighborhoodAssignmentMonoid.lean` を追加した。
点ごとの包含の反射律・推移律は値が部分集合であることしか使わず、反対称律に要るのは集合と写像の
外延性だけである。合成の単調性は始域と終域が異なる型のままでも成り立ち、`Finset` 版が要求する
等号判定は合併の側の要求にすぎない。部分集合を `Set` で表した版を別に証明し、`Finset` 版との一致を
橋渡し定理で示した。有限性が要るのは走査表と走査組数の段だけで、そこでも始域の等号判定は要らない。
順序モノイドになるのは、積を取るために始域と終域を同じ型へ閉じたときだけである。既製の順序との
一致は、自前の証明を置いたうえで一本の橋渡し定理に留めた。具体版の末尾に `Derivation` 名前空間を
置き、具体版の各主張を必要十分版の特殊化として導出した。$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（86 ページ、未解決参照 0）、本章の SageMath 4 本、検算対応検査、
Lean 全体ビルド、sorry 検査（新規 22 定理を登録）を全て通した。四層が揃ったので対象
「近傍割り当ての包含順序と合成の単調性」は `done`。次 tick は全層をレビューし、次対象を起票する。

## 自動ループ tick: 必要十分版の検査入口修正と近傍割り当ての包含順序（2026-08-26 11:12）

前 tick の「合成写像の本質的依存台」の必要十分版と導出を全層でレビューし、数学的な不一致は
見つからなかった。一方、必要十分版が Lean の検査入口から直接 import されておらず、前 tick の
sorry 検査成功という記録が実態と一致しないことを検出した。入口へ直接 import を追加し、全体ビルドと
sorry 検査の対象へ接続した。

新対象「近傍割り当ての包含順序と合成の単調性」を起票した。点ごとの包含が部分順序をなし、合成近傍が
両引数について単調であること、包含判定が有限決定できること、既存の有限モノイド構造と合わせて
有限順序モノイドになることを構造化記述した。有限集合・写像・所属判定だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（86 ページ、未解決参照 0）、検算対応検査、Lean 全体ビルドと
sorry 検査を通した。対象は `構造化記述まで`。次 tick は本章の SageMath 検算だけを作る。

## 自動ループ tick: 有限近傍割り当ての合成モノイドの Lean 具体版（2026-08-26 05:12）

前 tick の SageMath 検算 4 本を再実行し、両側単位律、結合律、元数と合成表、三元舞台の非可換反例が
構造化記述の各段と一致することを確認した。修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/FiniteNeighborhoodAssignmentMonoid.lean` を追加した。有限型 `V` 上の
近傍割り当てを `V → Finset V` とし、自己近傍割り当て、合成近傍の両側単位律、二重の存在量化を
展開する結合律、合成近傍と自己近傍を持つモノイド構造、元数 `2^(|V|*|V|)`、元の全列挙表と合成表を
人手証明と同じ順序で形式化した。三元舞台の二つの近傍割り当てについて、`a` での二つの合成値が
それぞれ `{c}` と空集合になることを分けて示し、合成が一般には可換でないことを導いた。
有限型・有限部分集合・有限写像・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（82 ページ、未解決参照 0）、本章の SageMath 4 本、
検算と証明の対応検査、Lean 全体ビルド、sorry 検査を全て通した。
対象は `Lean 具体版まで`。次 tick は本具体版を全層と突き合わせ、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 有限近傍割り当ての合成モノイドの SageMath 検算（2026-08-26 04:12）

前 tick の構造化記述を、引用先の合成近傍の定義へ戻って段ごとに再計算してレビューした。両側単位律・
結合律・元数・非可換反例のいずれにも数学的な不一致は無かった。記号 $\mathcal P$ が冪集合と
数値プロファイルの二つの意味で使われていることを検出し、`done` の章を跨ぐ改名になるため
台帳の「保留中の判断」へ記録した。

`sagemath/check/finite-neighborhood-assignment-monoid/` に検算 4 本を追加し、全て PASS した。
両側単位律（$|V|\leq3$ の全 530 割り当て）、結合律（$|V|\leq2$ の全 4,104 三つ組）、閉性・単位元・
元数 $2^{|V|^2}$・合成表の有限構成、3 セル舞台の非可換反例を分けて検査した。結合律を $|V|=3$ へ
広げていないことは overview に明記した。有限集合・有限写像表・自然数の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（82 ページ、未解決参照 0）、本章の SageMath 4 本、検算と証明の
対応検査、Lean の全体ビルドと sorry 検査を通した。対象は `SageMath まで`。
次 tick は本章の Lean 具体版を作る。

## 自動ループ tick: 有限近傍割り当ての合成モノイドの構造化記述（2026-08-26 03:12）

前 tick の「合成近傍による大域写像の合成表現」の構造化記述・SageMath 4 本・Lean 具体版・
Lean 必要十分版・導出・検査登録を突き合わせ、成果 commit が `origin/main` に含まれることも確認した。
二段制限を実際に引く修正、必要十分版が落とした仮定、具体版への六つの導出に修正を要する不一致は
見つからなかった。

survey の「規則空間を動かす／規則の合成」を、局所規則より一段下の近傍割り当て自体について進め、
新対象「有限近傍割り当ての合成モノイド」を起票した。有限舞台上の近傍割り当て全体
`N(V)` について、直前に定義した合成近傍が結合的で、自己近傍割り当てが両側単位元になること、
元数が `2^{|V|^2}` で合成表を有限決定できることを構造化記述した。さらに 3 セル舞台上の明示例で
合成が一般には可換でないことを示した。有限集合・写像・自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

対象は `構造化記述まで`。次 tick は本章をレビュー後、単位律・結合律・元数と有限決定・
非可換反例を分けた SageMath 検算だけを作る。

## 自動ループ tick: 合成近傍による大域写像の合成表現の Lean 必要十分版と導出（2026-08-26 02:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせた。数学的な段は一致していたが、
前 tick の成果一式が未コミットのまま作業ツリーに残り、`origin/main` へ一切入っていなかった。
本 tick で最新を取り込み直し、前 tick 分と合わせて反映した。あわせて、具体版の等号証明が人手証明の
要である二段制限の補題を実際には引いていなかったので、局所規則への引数の等号へ落として
各セルで補題を使う形へ直した。

Lean 必要十分版を追加した。この章の全主張に要るのは舞台の添字型の等号判定だけであり、
それも有限部分集合の族を一つに合併するためだけに要る。状態が二元であること・有限であること・
等号判定を持つこと・演算を持つことと、舞台の有限性はいずれも不要である。有限性が要るのは、
具体版で大域写像全体を有限集合として集める所属の段だけで、必要十分版では所属を
「合成近傍上の局所規則族が存在する」という存在文で述べている。具体版の六つの主張を
必要十分版の特殊化として導出した。

検証は構造化テキストの検査、PDF（80 ページ、未解決参照 0）、本章の SageMath 4 本、検算と証明の
対応検査、Lean の全体ビルドと sorry 検査を通した。対象は `done`。次 tick は新しい対象を起票する。

## 自動ループ tick: 合成近傍による大域写像の合成表現の Lean 具体版（2026-08-26 01:12）

前 tick の SageMath 検算 4 本を再実行し、合成近傍、二段制限、合成局所規則族、大域写像の合成の一致と
所属が構造化記述と一致することを確認した。修正を要する不一致は見つからなかった。

Lean 具体版 `lean/CellularAutomata/ComposedNeighborhoodClosure.lean` を追加した。人手証明と同じ有限舞台、
二元状態、二つの有限近傍割り当てと局所規則族について、合成近傍を有限合併として定義し、部分近傍の包含、
二段制限の一致、合成局所規則族の明示構成、大域写像の合成との一致、合成近傍上の大域写像全体への所属を
同じ順序で形式化した。有限型・有限部分集合・写像だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `Lean 具体版まで`。次 tick は本具体版を全層と突き合わせ、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 合成近傍による大域写像の合成表現の SageMath 検算（2026-08-26 00:12）

前 tick の構造化記述を引用先の定義から再計算してレビューし、修正を要する不一致は見つからなかった。
`sagemath/check/composed-neighborhood-closure/` に検算 4 本を追加した。合成近傍の合併としての定義と
有限性・部分近傍の包含、証明中の二段制限の段、合成局所規則族が合成近傍上の有限真理値表であることと
セルごとの一致、大域写像の合成が合成局所規則族の大域写像に等しく合成近傍上の大域写像全体に属することを
それぞれ分けて検査した。前二者は $|V|\leq3$ の全ての近傍割り当ての組、後二者は $|V|\leq2$ の全ての
（近傍割り当て, 局所規則族）の組（456,976 通り）で全数検査し、全て PASS。有限範囲の全数検査であって
一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査、PDF（80 ページ、未解決参照 0）、本章の SageMath 4 本、検算と証明の対応検査、
Lean のビルドと sorry 検査を通した。対象は `SageMath まで`。次 tick は本章の Lean 具体版を作る。

## 自動ループ tick: 合成近傍による大域写像の合成表現の構造化記述（2026-08-25 23:12）

前 tick の「固定近傍による可逆大域写像族の合成非閉性」の Lean 必要十分版と導出を、構造化記述・
Lean 具体版と段ごとに突き合わせた。数学的な各段は一致していたが、必要十分版ファイルが Lean の入口
`lean/CellularAutomata.lean` から import されておらず、前 tick の「sorry 検査成功」という記録が
実態と一致していないことを検出した。入口へ import を追加し、必要十分版と導出を実際の全体ビルド・
sorry 検査の対象へ接続した。

survey の「規則空間を動かす／規則の合成」を次の対象として具体化し、二つの近傍割り当て
$N,M$ に対して $(N\star M)(v)=\bigcup_{u\in N(v)}M(u)$ を定義した。$N$ 上の局所規則族が定める
$F$ と $M$ 上の局所規則族が定める $G$ について、各 $u\in N(v)$ で $M(u)$ へ制限して後段の
局所規則を評価し、その値の族を前段の局所規則へ渡す合成局所規則族を構成した。制限写像の二段制限を
各座標で展開し、$F\circ G$ が合成近傍上の大域写像に一致することを構造化記述した。

有限集合・写像・有限真理値表だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は
`構造化記述まで`。次 tick は本記述をレビューし、合成近傍の構成・二段制限・合成大域写像の一致を
分けた SageMath 検算だけを作る。

## 自動ループ tick: 固定近傍による可逆大域写像族の合成非閉性の Lean 必要十分版と導出（2026-08-25 22:12）

前 tick の Lean 具体版を構造化記述・SageMath 検算と段ごとに突き合わせた。局所規則族による座標送り
写像の実現、三回巡回から作る両側逆写像と単射性、二回合成の $a$ 座標写像が $x(c)$ に等しいこと、
定値零配位と一点反転から $c\in\operatorname{supp}(g_a)$ を得る段、依存台包含による表現不能の順序は
いずれも一致し、修正を要する不一致は見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/FixedNeighborhoodCompositionNonclosure.lean` を追加した。
具体版の証明が実際に使う構造を一つずつ検査した結果は次のとおりである。舞台側に要るのはセルの型の
等号判定だけで、セル数が 3 であること、舞台が有限であること、巡回写像が置換であることはいずれも
要らない。単射性に要るのは巡回写像の右逆写像だけであり、三回で戻ることも両側逆であることも要らない
（具体版はこれを満たす特別な場合である）。状態側に要るのは入れ替え写像と「各元と異なる元がその値に
一意に定まる」ことだけで、状態が二元であること・有限であること・等号判定を持つことは要らない。
本質的依存の証人を作るために基準値を一つだけ要求する。表現不能に要るのは、二回合成が読むセルが
その近傍の外にあることだけである。大域写像全体を有限集合として集める段は具体版にだけ残し、
必要十分版では所属を局所規則族の存在として述べたので有限性を使わない。使わない等号判定の仮定は
`omit` で明示的に外し、linter の未使用警告が出ないことを確認した。

具体版の各定理が必要十分版の特殊化として得られることを `Derivation` 名前空間で導出した
（セルの型・状態型・入れ替え写像・基準値・巡回写像・右逆写像・着目セルを特殊化する）。最終主張は
必要十分版の合成非閉性へ有限性を足して大域写像全体を有限集合として集めたものになっている。

検証は、構造化テキストの検査、PDF（79 ページ、未解決参照 0）、SageMath 3 本、検算と証明の対応検査、
Lean 全体ビルド、sorry 検査（必要十分版 9 定理と導出 10 定理を新規登録）を通した。有限性を使わない
形へ弱めたので $\mathbb R/\mathbb C$ 脱出はない。四層が揃ったので対象は `done`。
対象表に未完了対象が無くなったため、次 tick は本 tick の必要十分版と導出をレビューし、
survey に沿って次の対象を起票して構造化記述を始める。

## 自動ループ tick: 固定近傍による可逆大域写像族の合成非閉性の Lean 具体版（2026-08-25 21:12）

前 tick の SageMath 検算 3 本を再実行し、座標送り写像の可逆性、二回合成の $a$ 座標での近傍外依存、
固定近傍で表せる全 64 大域写像からの非所属が構造化記述と一致することを確認した。

Lean 具体版 `lean/CellularAutomata/FixedNeighborhoodCompositionNonclosure.lean` を追加した。人手証明と
同じ 3 セル型、巡回写像、一元近傍、二元状態を用い、局所規則族による座標送り写像の実現、三回巡回から
作る両側逆写像、単射性、二回合成の $a$ 座標写像 $g_a(x)=x(c)$、定値零配位と一点反転による
$c\in\operatorname{supp}(g_a)$、$c\notin N(a)$、依存台包含定理による表現不能、固定近傍で表せる
可逆大域写像族の合成非閉性を同じ順序で形式化した。必要十分版と導出は未着手である。

検証は、構造化テキストの検査、PDF（79 ページ、未解決参照 0）、SageMath 3 本、検算と証明の対応検査、
Lean 全体ビルド、sorry 検査（新規 14 定理を登録）を通した。有限型・有限部分集合・写像だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。対象は `Lean 具体版まで`。次 tick は本具体版を全層と突き合わせ、
Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 固定近傍による可逆大域写像族の合成非閉性の SageMath 検算（2026-08-25 20:12）

前 tick の構造化記述を定義から再計算して突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/fixed-neighborhood-composition-nonclosure/` に検算 3 本を追加した。座標送り写像の
可逆性と固定近傍での表現可能性、二回合成の $a$ 座標写像が近傍の外の座標へ本質的に依存すること、
固定近傍で表せる大域写像 64 通りの全数列挙と $F\circ F$ の非所属（可逆な元だけに絞っても同じ）を
分けて検査し、全て PASS。反例は一つの有限対象についての主張なので、この全数検査が主張の範囲を
尽くす。有限集合・写像・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

検証は、構造化テキストの検査と PDF（79 ページ、未解決参照 0）、本章の SageMath 3 本、
検算と証明の対応検査、`lake build`、sorry 検査を通した。対象は `SageMath まで`。
次 tick は本 tick の検算をレビューし、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 自動ループ tick: 固定近傍による可逆大域写像族の合成非閉性の構造化記述（2026-08-25 19:12）

前 tick の自己近傍舞台の可逆大域写像群について、Lean 必要十分版と導出を全層でレビューし、修正を
要する不一致は見つからなかった。新対象として、3 セルを巡回して一つ先の座標を読む可逆大域写像を
構成し、その二回合成が元の近傍外の座標へ本質的に依存するため、同じ固定近傍では表せないことを
構造化記述した。自己近傍で成立した群構造は一般の固定近傍では成立しない。有限集合・写像・有限真理値表
だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は `構造化記述まで`。次 tick は構造化記述をレビューし、
可逆性・二回合成の外部依存・局所表現不能を分けた SageMath 検算を作る。

## 自動ループ tick: 自己近傍舞台の可逆大域写像群の Lean 具体版（2026-08-25 17:12）

前 tick の構造化記述と SageMath 5 本を再照合し、修正を要する不一致は見つからなかった。
Lean 具体版 `SelfNeighborhoodReversibleMapGroup.lean` に、一般の有限自己近傍舞台での点ごとの表示、
各セルの二元全単射の分類、反転集合による可逆大域写像の一意な分類と元数、合成と対称差、
有限可換群の各段、および一般セル数での巡回型を人手証明と同じ対象・順序で形式化した。
有限集合・写像・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出を作る。

## 自動ループ tick: 局所性による巡回型の制限の Lean 必要十分版と導出（2026-08-25 14:12）

前 tick の具体版をレビューし、主張「実現される巡回型は配位数の分割であり有限決定できる」だけが
形式化されていなかったので先に補った（分割への包含と、可逆な大域写像全体が有限型であることによる
像の有限性）。

Lean 必要十分版 `NecSuf/LocalityRestrictsCycleType.lean` を追加した。具体版が実際に使うのは、
(1) 近傍が一点の舞台の大域写像は成分ごとの写像であり、単射性の同値には添字型の等号判定しか要らない、
(2) 元数 2 の有限型上の単射写像は対合である、(3) 有限台上の対合の巡回型の元は 2 以下である、の三つ
だけであり、それぞれ独立した段に分けた。台が配位型であること、元数が 2 の冪であることは要らない。

**具体版の過剰な仮定を一つ検出した。** 単射性の必要条件の証人配位は注目する成分の外で定数に取れば
足り、具体版が使っていた状態集合の基準値（基準値延長写像）は要らない。必要十分版では落とした。

導出は具体版末尾の名前空間に置き、単射性の同値・対合性・八つの配位を一周する巡回型の非実現・
真部分集合性が必要十分版の特殊化であることを示した。$\mathbb R/\mathbb C$ 脱出はない。
対象「局所性による巡回型の制限」は四層が揃い `done`。次 tick は本 tick をレビューし次の対象を起票する。

## 自動ループ tick: 局所性による巡回型の制限の Lean 具体版の完了（2026-08-25 13:12）

前 tick の具体版前半を構造化記述・SageMath と照合し、修正を要する不一致はなかった。
Lean 具体版へ、可逆な自己近傍大域写像が恒等写像または固定点なしの対合になること、巡回型が
8 個の 1 または 4 個の 2 に限られること、両者の明示的実現、分割 `{{8}}` の非実現と真部分集合性を
追加した。有限型・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出を作る。

## 自動ループ tick: 局所性による巡回型の制限の SageMath 検算（2026-08-25 11:12）

前 tick の構造化記述をレビューし、局所規則族の復元、自己近傍舞台での単射性の点ごとの特徴づけ、
二回反復、巡回型の二場合、真部分集合性に修正を要する不一致はなかった。

`sagemath/check/locality-restricts-cycle-type/` に検算 4 本を追加した。1〜6 セルの自己近傍舞台の
全 5,460 局所規則族で大域写像への単射性と個数公式を、3 セルの全 64 族で単射性の同値・二回反復・
実現巡回型二種・分割 `{{8}}` の非実現を検査した。1〜5 セルの全 1,364 族でも実現巡回型を列挙した。
全て PASS。有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
対象は `SageMath まで`。次 tick は本章の Lean 具体版を作る。

## 自動ループ tick: 巡回型の分割実現を明示構成へ修正（2026-08-25 09:12）

前 tick の必要十分版と導出をレビューし、共役不変性・完全性・商の全単射に修正を要する
不一致はなかった。ただし、分割の実現が Mathlib の巡回型存在定理へ結論まで委ねていた点は、
人手証明と 1 対 1 になっていないため修正した。

有限型の元を一列に並べ、分割の値ごとに互いに交わらない連続部分列へ切り分け、各部分列の
巡回置換を合成する構成を Lean に明示した。具体版はその配位型への特殊化である。
有限集合・自然数・有限列・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
対象「可逆な大域写像の巡回型」は `done` のまま。次 tick はこの修正をレビュー後、
次の CA 内在的対象を起票する。

## 自動ループの Claude 固定モデルを Opus 5 へ変更（2026-08-23）

Claude 回は 2026-08-22 19:12 から 2026-08-23 19:12 まで、専用アカウントの
`claude-fable-5` が利用上限に達したため連続して exit 1 になっていた。同じ専用アカウントを使う
別ループで `claude-opus-5` の正常終了を確認し、このループも固定モデルを `claude-opus-5`、
effort `medium` へ変更した。実行時に別モデルへ落とすフォールバックは行わない。

## 自動ループ tick: 可逆な大域写像の巡回型の Lean 必要十分版と導出（2026-08-25 08:12）

前 tick の分割の実現と商の全単射をレビューした。修正を要する誤りはないが、分割の実現の一段だけ
Lean 具体版が Mathlib の `Equiv.Perm.exists_with_cycleType_iff` を呼んでおり、配位を一列に並べて
分割の値ごとに切り分ける人手証明の構成と 1 対 1 になっていない。次 tick で明示的な巡回列の構成へ
置き換えるかを判断する。

Lean 必要十分版を追加し、具体版が実際に使う仮定は台の有限性と等号判定だけであることを示した。
状態が 2 値であること、台が配位型であること、舞台がグラフであること、元数が 2 の冪であることは
すべて落とせた。具体版へ、巡回型・共役・総和・共役不変性・完全性・分割の実現・商の全単射が
必要十分版の特殊化として得られる導出節を追加した。$\mathbb R/\mathbb C$ 脱出はない。
対象「可逆な大域写像の巡回型」は四層と導出が揃い `done`。SageMath はこの tick では変更も再実行も
していない。

## 自動ループ tick: 可逆な大域写像の巡回型の Lean 具体版の完了（2026-08-25 07:12）

前 tick の周期軌道分割段は、最小周期・反復列・自然数の除法による人手証明と対応し、修正を要する
不一致はなかった。Lean 具体版に、正の自然数分割の定義、1 と 2 以上の部分への分解、各分割の巡回置換による実現、
可逆写像の共役関係の商から巡回型への代表非依存な写像、その単射性・全射性・全単射を追加した。
有限集合・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は `Lean 具体版まで`。
次 tick は本 tick を全層でレビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 可逆な大域写像の巡回型の Lean 具体版の前半（2026-08-25 05:12）

前 tick の SageMath 7 本を構造化記述と突き合わせて再実行し、単射な自己写像 40,347 個、
単射に限らない自己写像 261 個を含む全検査が再び PASS した。修正を要する不一致はなかった。

`lean/CellularAutomata/ReversibleGlobalMapCycleType.lean` に、有限舞台の配位型上の単射な自己写像を
有限置換へ移す Lean 具体版を追加した。Mathlib の巡回型は固定点を除くため、非自明周期の巡回型へ
固定点数ぶんの 1 を加え、人手証明の全周期軌道の元数を集める定義と一致させた。全配位の周期性、
巡回型の正値性と総和 $2^{|V|}$、共役不変性、巡回型一致と共役全単射存在の同値まで形式化した。
有限集合・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期軌道の分割と最小周期との対応、任意の自然数分割の実現、共役類と分割の全単射は Lean で
未形式化である。対象は実態どおり `SageMath まで` のまま。次 tick は本 tick の Lean 前半を
レビュー後、残る主張を同じ具体版へ追加する。

## 自動ループ tick: 可逆な大域写像の巡回型の SageMath 検算（2026-08-25 04:12）

前 tick で完了した本章の構造化記述を全段レビューし、既出の最小周期・反復の加法則・共役による
反復移送・全大域写像の局所規則表現と突き合わせた。修正を要する不一致は見つからなかった。

`sagemath/check/reversible-global-map-cycle-type/` に検算 7 本を追加した。単射な自己写像の個数と
全配位の周期性、反復による写像の像が周期軌道であることと単射性からの元数と最小周期の一致、
周期軌道の合併と非交差、巡回型の和が $2^{|V|}$ であること、共役による巡回型の保存、巡回型が
等しい対での共役全単射の構成と共役条件、異なる対での共役全単射の非存在、各分割の巡回置換に
よる実現と共役類との対応を、それぞれ別の検査に分けた。元数 1・2・4・8 の単射な自己写像
40,347 個を全数検査し、元数と最小周期の一致は全自己写像 261 個で検査して全て PASS。
任意の有限舞台に対する一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

構造化テキスト check、PDF（72 ページ、302 ブロック、未解決参照 0）、`validate-content`、
検算対応検査（37 件）、Lean 全体ビルドと sorry 検査を通した。本章の Lean は未着手で、現在地は
`SageMath まで`。次 tick は本 tick の検算をレビュー後、本章の Lean 具体版だけを作る。

## 自動ループ tick: 可逆な大域写像の巡回型の構造化記述の完了（2026-08-25 03:12）

前 tick の構造化記述の前半を、既出の最小周期・反復の加法則・共役による反復移送・全近傍での
大域写像の実現と突き合わせ、修正を要する不一致は見つからなかった。残りの構造化記述として、
可逆な大域写像の周期軌道が配位集合を分割すること、巡回型の要素の和が $2^{|V|}$ であること、
共役全単射による巡回型の保存、巡回型が等しい二写像の対応周期軌道を同じ反復位置で結ぶ共役全単射の
構成を追加した。さらに $2^{|V|}$ の正の自然数への各分割を配位集合上の巡回置換として実現し、
可逆な大域写像の共役類とそれらの分割が全単射であること、共役類数が有限決定できることを示した。
有限集合・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキスト check、PDF（72 ページ、302 ブロック、未解決参照 0）、検算対応検査（36 件）、
Lean 全体ビルドと sorry 検査を通した。本対象の SageMath 検算と Lean は未着手で、現在地は
`構造化記述まで`。次 tick は本章全体をレビュー後、周期軌道分割・巡回型の和・共役完全性・
自然数分割との全単射を段ごとに分けた SageMath 検算だけを作る。

## 自動ループ tick: 共役類と写像符号の像の Lean 必要十分版（2026-08-25 01:12）

前 tick の Lean 具体版を構造化記述・SageMath・既出の写像符号の完全不変量と照合し、修正を要する
不一致は見つからなかった。Lean 必要十分版を追加し、商集合と符号の集合論的な像の全単射には、
型上の同値関係、符号写像、同値関係と符号等号の両方向だけが要ることを示した。型や符号型の有限性・
等号判定、自己写像、共役、二値状態、セル、近傍、局所規則、符号の内部構造はこの段では不要である。
有限性と等号判定は具体版の有限列挙・個数計算にだけ残る。具体版の共役類と符号像の全単射を
必要十分版の特殊化として導出し、値が具体版の代表符号写像と一致することも示した。
$\mathbb R/\mathbb C$ 脱出はない。四層と導出が揃ったため対象は `done`。

## 自動ループ tick: 共役類と写像符号の像の Lean 具体版（2026-08-25 00:12）

前 tick の SageMath 検算 4 本を構造化記述と再照合し再実行して全て PASS。修正を要する誤りは
見つからなかった。「共役類の集合と写像符号の像の全単射」の Lean 具体版
`lean/CellularAutomata/ConjugacyClassCodeImageBijection.lean` を、人手証明と同じ対象・仮定・
順序で書いた。全セルを近傍とする局所真理値表の族を基準値延長から作り、配位集合上の任意の
自己写像がその大域写像に一致することを既出の局所規則表現から示し、写像全体の個数
$(2^{|V|})^{2^{|V|}}$ を形式化した。共役関係の反射律・対称律・推移律を恒等写像・逆写像・合成で
証明して同値関係とし、その商を共役類の集合とした。共役と写像符号の一致の同値は既に形式化した
完全不変量の定理をここでだけ使い、そこから共役の決定可能性・共役類の集合の有限性・商から符号への
写像の代表非依存性・単射性・全射性・全単射・個数一致を人手証明と同じ順で形式化した。
住処は有限型・自然数・有限多重集合だけで $\mathbb R/\mathbb C$ は現れない。sorry 検査の登録簿へ
本章の 14 定理を追加し、Lean 全体ビルドと sorry 検査、構造化テキスト check、PDF（69 ページ）、
`validate-content`、検算対応検査（36 件）を通した。対象は `Lean 具体版まで`。
次 tick は本具体版をレビューし、Lean 必要十分版と具体版の導出を作る。

## 自動ループ tick: 共役類と写像符号の像の SageMath 検算（2026-08-24 23:12）

前 tick の構造化記述を既証明の再帰的前像木符号の完全不変量・有限決定と照合し、修正を要する
不一致は見つからなかった。セル数 0・1・2（配位集合の元数 1・2・4）の全自己写像 261 個を対象に、
全写像表の個数と全近傍による実現、共役関係の同値律と共役類分割、商から写像符号の像への対応の
代表非依存性・単射性・全射性、共役類数と相異なる符号数の一致を 4 本の SageMath 検算に分け、
全て PASS した。共役類は元数ごとに 1、3、19 個で合計 23 個、共役な順序対は 4,633 対だった。
有限範囲の全数検査であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
対象は `SageMath まで`。次 tick は本検算をレビュー後、Lean 具体版だけを作る。

## 自動ループ tick: 共役類の集合と写像符号の像の全単射の構造化記述（2026-08-24 22:12）

前 tick の `done` 判定（周期成分に付随する再帰的前像木符号の完全性）を四層と導出の対応で
再確認し、修正を要する不一致は見つからなかった。台帳先頭に未完了対象が無くなったため、
CA 内在的な次の対象「共役類の集合と写像符号の像の全単射」を起票し、構造化記述
`content/conjugacy-class-code-image-bijection.ts` を書いた。

固定した有限集合 $V$ 上の大域写像全体 $\mathcal M(V)$（$|\mathcal M(V)|=(2^{|V|})^{2^{|V|}}$、
各元が近傍 $N(v)=V$ の 2 値セルオートマトンの大域写像であることは既出の局所規則表現の claim を
各セルへ適用して示した）、同一舞台上の共役関係 $\approx_V$ とその同値関係性（反射律・対称律・
推移律をいずれも一行一等号で書いた）、共役類の集合 $\mathcal C(V)$、写像符号の像
$\mathcal K(\mathcal M(V))$、$\overline{\mathcal K}([F]_V):=\mathcal K(F)$ が代表に依存せず
全単射であること、$|\mathcal C(V)|=|\mathcal K(\mathcal M(V))|\in\mathbb N$ の有限決定を含む。
物理側の名称は持ち込まず、有限集合・自然数・写像の合成と全単射だけで閉じており、
$\mathbb R/\mathbb C$ 脱出はない。

構造化テキスト check と PDF（69 ページ、未解決参照 0）、`validate-content`（289 ブロック、
939 参照すべて解決）、検算対応検査 35 件、Lean 全体ビルドと sorry 検査を通した。
本対象の SageMath 検算と Lean は未着手で、status は `構造化記述まで`。
次 tick は本章の SageMath 検算（$|V|\le2$ の全数列挙による共役類の個数と符号の像の一致）を作る。

## 自動ループ tick: 再帰的前像木符号の必要十分版と導出の全段レビュー（2026-08-24 21:12）

前 tick までに分離した「周期成分に付随する再帰的前像木符号の完全性」の具体版と必要十分版を
全段で突き合わせた。子符号、周期成分符号、軌道出現、軌道木、共役移送、打ち切り節点全単射と
親子辺保存、各従属和接着、一段発展との可換性、完全不変量、有限決定、`Option` の値と共役存在の
同値まで、必要十分版の特殊化としての導出に欠落はなかった。CA 固有の周期性・最小前周期・
有限配位数上界・周期軌道表の分割は、一般定理へ渡す仮定を証明する段として適切に具体版へ残っている。
四層と導出が全て対応したので対象を `done` とした。有限集合・自然数・有限な符号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check と PDF、検算対応 35 件、対象 SageMath
5 本、Lean 全体ビルド、sorry 検査は全て成功した。次 tick は本 tick の完了判定をレビューし、
台帳先頭に未完了対象が無いため、CA 内在的な次の探索対象を既存の survey と完了済み構造から起票する。

## 自動ループ tick: 共役全単射の存在判定を必要十分版から導出（2026-08-24 19:12）

前 tick の全配位可換性の必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。
本 tick は、具体版の共役全単射の存在判定を、写像符号型の等号判定、二つの自己写像、
二つの写像符号、符号一致と共役存在の同値だけを使う必要十分版 `hasConjugacyDecidable` の
特殊化へ置き換え、定義上の一致を別定理で明示した。配位型の有限性・等号判定、二値状態、
セル、近傍、局所規則、前像木、周期成分は使わず、$\mathbb R/\mathbb C$ 脱出はない。
全検証（Lean build、sorry 検査、構造化テキスト check と PDF、検算対応検査、対象の
SageMath 検算 5 本）は成功した。対象は `Lean 具体版まで` のまま。
次 tick は本 tick の導出をレビューし、有限比較で得る `Option` の値の存在と共役存在の同値を
必要十分版から直接導く段を進める。

## 自動ループ tick: 全配位の一段発展可換性を従属和接着の必要十分版から導出（2026-08-24 18:12）

前 tick の全配位接着の特殊化をレビューし、修正を要する不一致は見つからなかった。
本 tick は、成分ごとの一段発展可換性を全配位へ移す段を Lean 必要十分版へ分離した。
必要十分版が要求するのは、添字の全単射、添字ごとの部分表全単射、両側の全体表と
その従属和を結ぶ全単射、各部分表の上に与えた自己写像、それらが全体表の自己写像と
両立すること、部分表全単射が部分表の自己写像と可換することだけである。周期成分、
周期性、最小前周期、前像木、二値状態、セル、近傍、局所規則は使わず、有限性も
等号判定も使わない。具体版の全配位可換性は、周期成分分割と一段発展を渡した
特殊化として導出し直した（部分表上の自己写像との両立は定義上の等号）。
全検証（Lean build、sorry 検査、構造化テキスト check と PDF 67 ページ、検算対応 35 件）は
成功し、$\mathbb R/\mathbb C$ 脱出はない。SageMath 層はこの tick で変更していないため
再実行していない。対象は実態どおり `Lean 具体版まで` のまま。
次 tick は本 tick の可換性の導出をレビューし、符号の有限比較から共役全単射を選ぶ段の
周辺で必要十分版へ未分離の段を一つ進める。

## 自動ループ tick: 周期成分ごとの全単射の全配位への従属和接着（2026-08-24 17:12）

前 tick の周期位置ごとの表全単射の従属和接着と具体版からの導出を
レビューし、修正を要する不一致は見つからなかった。

前 tick で分離した必要十分版の一般的な従属和接着が、外側の周期成分分割にも
そのまま適用できることを確認した。具体版の全配位全単射を、周期成分出現の全単射、
成分ごとの全単射、両側の全配位と従属和の全単射だけを与えた特殊化へ置き換え、
定義上の一致を別定理で明示した。周期成分、自己写像、周期性、最小前周期、
二値状態、セル、近傍、局所規則は接着の一般定理に使わない。

構造化テキスト check、PDF、検算対応、Lean 全体ビルド、sorry 検査を通した。
SageMath 層は変更していないため再実行していない。

次にやること: 本 tick の全配位接着の特殊化をレビューし、成分ごとの一段発展可換性を
全配位へ移す段を必要十分版と具体版からの導出へ分離する。

## 自動ループ tick: 周期位置ごとの表全単射の従属和接着の必要十分版と導出（2026-08-24 16:12）

前 tick の打ち切り節点型と対象表の接着（必要十分版）と導出をレビューし、修正を要する
不一致は見つからなかった。

Lean 必要十分版に、添字ごとの部分表全単射を従属和による分割に沿って全体表へ接着する
構成を分離した。使うのは添字の全単射、添字ごとの部分表全単射、各全体表をその従属和と
結ぶ全単射だけで、添字が周期位置であること、部分表が前像木節点表であること、自己写像、
周期性、最小前周期、二値状態、セル、近傍、局所規則は使わない。接着の全単射性と、
添字ごとの主張を全体表へ移すための計算則も形式化した。

具体版の周期成分表全単射を、周期位置の全単射と周期位置ごとの前像木節点表全単射の族を
与えた接着へ置き換え、具体版がその特殊化であることを別の定理で明示した。

構造化テキスト check、PDF（67 ページ）、検算対応 35 件、Lean 全体ビルド、sorry 検査を通した。
SageMath 層は変更していないため再実行していない。

次にやること: 本 tick の従属和接着の必要十分版と導出をレビューし、周期成分全単射を
全配位へ接着する段（周期成分ごとの全単射の非交和による貼り合わせ）を必要十分版へ分離する。

## 自動ループ tick: 打ち切り節点型の節点表への接着の必要十分版と導出（2026-08-24 14:12）

前 tick の親子辺保存の必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。

Lean 必要十分版に、打ち切り節点型と任意の対象表を結ぶ全単射を両側に与え、
再帰的節点全単射を表間へ接着する構成を分離した。表の元の意味、自己写像、
周期性、最小前周期、二値状態、セル、近傍、局所規則は使わない。具体版では、
必要十分版の節点型から周期点へ流入する有限配位表への全単射を定義し、前像木節点表間の
全単射を必要十分版の表接着の特殊化へ置き換えた。展開後が従来の具体的合成と
定義上一致することも定理で明示した。$\mathbb R/\mathbb C$ 脱出はない。

周期位置ごとの表全単射を従属和上で周期成分全体へ接着する必要十分版は未形式化で、
対象は `Lean 具体版まで` のまま。次 tick は本 tick をレビューし、従属和上の接着を
必要十分版へ分離する。

## 自動ループ tick: 節点全単射の親子辺保存の必要十分版と導出（2026-08-24 13:12）

前 tick の、両節点型の再帰的全単射を挟んで具体版の節点全単射を必要十分版から導出する構成を
レビューし、修正を要する不一致は見つからなかった。

節点全単射が親子辺を保存する段を必要十分版へ分離した。有限子表一つだけから打ち切り節点型の
親子辺を帰納的に定義し、対応から構成した全単射が辺の深さの帰納法で親子辺を保つこと、
各節点が根そのものか一意な親への辺を持つことを示した。型全体の有限性、自己写像、周期性、
最小前周期、二値状態、セル、近傍、局所規則は使わない。具体版の対応する 2 定理は、
具体版と必要十分版の節点型を結ぶ全単射で親子辺を両向きに移す定理を経由し、必要十分版の
特殊化として導出する形へ置き換えた。Lean build、sorry 検査、構造化テキストの check と
build:pdf、SageMath 検算 5 本を通した。

全配位への接着の必要十分版は未形式化なので、対象は `Lean 具体版まで` のまま。
次 tick は本 tick をレビューし、周期成分の節点表全体への接着を必要十分版へ分離する。

## 自動ループ tick: 打ち切り節点全単射の必要十分版から具体版への導出（2026-08-24 12:12）

前 tick の必要十分版を具体版とレビューし、両者の打ち切り節点型と節点全単射が、子表を
非周期一段前像へ特殊化すると同じ再帰構成になることを確認した。前 tick で失敗した節点型全体の
`abbrev` 化は避け、具体版と必要十分版の節点型の間に再帰的な全単射を定義した。この全単射を
両端に挟んで具体版の節点全単射を必要十分版から導出し、非周期一段前像表への特殊化を別定理で明示した。
既存の根保存・子分岐・親子辺保存以降を含む Lean build が通ることを確認した。

周期成分内部の親子辺保存と全配位への接着の必要十分版は未形式化なので、対象は
`Lean 具体版まで` のまま。次 tick は本 tick の導出をレビューし、節点全単射の親子辺保存を
必要十分版へ分離する。

## 自動ループ tick: 再帰的前像木対応の構成段の必要十分版と導出（2026-08-24 10:12）

前 tick の、等しい後続深さ符号から子出現対応を得る必要十分版と具体版からの導出を
レビューした。後続段の符号化の単射性から子符号多重集合の等号へ戻し、既存の
多重集合出現対応と合成して符号を保つ全単射を得ていること、具体版が非周期一段前像表への
特殊化になっていることを確認し、修正を要する不一致は見つからなかった。

Lean 必要十分版へ、二つの有限子表だけから深さごとの再帰的前像木対応を定義し、
等しい打ち切り再帰符号からその対応を深さ帰納法で構成する定理を追加した。
後続段では前 tick の子出現全単射を選び、対応した各子へ一つ浅い帰納仮定を適用する。
型全体の有限性、自己写像、周期性、最小前周期、二値状態、セル、近傍、局所規則を使わない。
具体版の再帰的前像木対応をこの一般定義の特殊化へ置き換え、具体版の構成定理を
必要十分版から直接導出する形にした。有限集合・有限多重集合・自然数の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

周期成分内部の節点全単射・辺保存と全配位への接着の必要十分版は未形式化なので、対象は
`Lean 具体版まで` のまま。次 tick は本 tick をレビューし、再帰的前像木対応から
打ち切り節点型の全単射を作る段を必要十分版へ分離する。

## 自動ループ tick: 等しい符号から非周期一段前像の出現対応を得る段の必要十分版と導出（2026-08-24 09:12）

前 tick の基点語選択の必要十分版と導出を全層でレビューし、修正を要する不一致は
見つからなかった。

Lean 必要十分版へ二つの定理を追加した。一つは、打ち切り深さ `depth + 1` の再帰符号が
等しい二点について、子表の一つ浅い符号を重複込みで集めた多重集合が等しいという定理である。
使うのは二つの子表と、再帰符号の後続段が「子符号の多重集合を整列して有限列にし、
それを符号化する」ことだけである。もう一つは、それを既存の出現対応定理と合成して、
子表の出現どうしの符号を保つ全単射を得る定理である。どちらも型全体の有限性、自己写像、
周期性、最小前周期、二値状態、セル、近傍、局所規則を使わない。落とせなかった仮定は
両側の元の等号判定で、子表を有限集合として持ちその台多重集合を写すために要る。
具体版の子符号多重集合の等号と子の出現全単射を、子表に非周期一段前像を入れた
特殊化として導出する形へ置き換えた。$\mathbb R/\mathbb C$ 脱出はない。

検証は `lake build`、`scripts/check-no-sorry.sh`（新規 2 定理を登録）、構造化テキストの
`check` と `build:pdf`（67 ページ、未解決参照 0）、検算対応 35 件、対象の SageMath 検算
5 本（全て PASS）を通した。

前像木の再帰的対応そのものの構成段の必要十分版は未形式化なので、対象は
`Lean 具体版まで` のまま。次 tick は本 tick をレビューし、対応した子ごとに一つ浅い対応を
要求する再帰的前像木対応の構成段を必要十分版へ分離する。

## 自動ループ tick: 対応周期軌道から等しい基点語を選ぶ段の必要十分版と導出（2026-08-24 08:12）

前 tick の符号一致から周期軌道の出現対応を得る必要十分版と導出を全層でレビューし、
修正を要する不一致は見つからなかった。

Lean 必要十分版へ、二つの有限表に付けた値の像集合が等しいとき、左表で指定した元と
同じ値を持つ元を右表から選べる定理を追加した。要るのは二つの有限表、二つの付値、
指定した元の左表への所属、像集合の等号だけであり、型全体の有限性、自己写像、周期性、
値が基点語であることは使わない。付値型の等号判定は有限表の像を作るためにだけ要り、
両側の元の等号判定は定理から落とした。具体版の、等しい成分符号から等しい基点語を持つ
周期点の組を選ぶ定理を、この一般定理の特殊化として導出する形へ置き換えた。
$\mathbb R/\mathbb C$ 脱出はない。

周期成分内部の子対応と接着の必要十分版は未形式化なので、対象は `Lean 具体版まで` のまま。
次 tick は本 tick をレビューし、等しい点符号から非周期一段前像の出現対応を得る段を
必要十分版へ分離する。

## 自動ループ tick: 符号一致から周期軌道対応を得る段の必要十分版と導出（2026-08-24 07:12）

前 tick の写像符号の共役保存の必要十分版と導出を全層でレビューし、修正を要する不一致は
見つからなかった。

Lean 必要十分版へ、多重集合の出現型の元が元の多重集合に属すること、二つの多重集合を符号で
写した結果が等しければ出現どうしの符号を保つ全単射が取れること、および有限表の集約符号の
等号に対して同じことを述べた定理を追加した。要るのは二つの有限表、その上の二つの符号、
集約符号の等号だけで、型全体の有限性、自己写像、周期軌道、符号の内部構造は使わない。
落とせなかったのは三つの等号判定（両側の元の等号判定は出現型の構成に、符号の等号判定は
写した多重集合の出現型どうしの移送に要る）。具体版の一般の出現対応定理・出現の所属を
この特殊化へ置き換え、写像符号の一致から周期軌道の出現全単射を得る定理を、写像符号が
集約符号の特殊化であるという等式を経由して導出する形にした。
$\mathbb R/\mathbb C$ 脱出はない。

周期成分内部の接着と前像木の再帰構成の必要十分版は未形式化なので、対象は
`Lean 具体版まで` のまま。次 tick は本 tick をレビューし、対応する周期軌道から
等しい基点語を持つ周期点の組を選ぶ段を必要十分版へ分離する。

## 自動ループ tick: 写像符号の共役保存の必要十分版と導出（2026-08-24 06:12）

前 tick の周期成分符号の必要十分版と導出を全層でレビューし、修正を要する不一致は
見つからなかった。

Lean 必要十分版へ、有限表の各元に付く値を重複度つき有限多重集合へ集約する定義と、
有限表を単射で移し各元の値を保存すれば集約値も保存される定理を追加した。必要なのは
二つの有限表、表を移す単射、各元の値の保存だけであり、型全体の有限性、自己写像、
周期軌道、成分符号の内部構造は使わない。具体版の写像符号を、周期軌道表と各軌道の
成分符号をこの一般集約へ入れた特殊化として明示し、写像符号の共役保存を必要十分版から
導出する形へ置き換えた。有限集合・有限多重集合の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

共役不変性の必要十分版は写像符号まで接続した。符号一致からの再帰構成の必要十分版は
未形式化なので、対象は `Lean 具体版まで` のまま。次 tick は本 tick をレビューし、
符号一致から周期軌道の重複度付き対応を得る段を必要十分版へ分離する。

## 自動ループ tick: 完成符号の必要十分版と前 tick のビルド不成立の修正（2026-08-24 03:12）

前 tick の必要十分版ファイルが `origin/main` の状態で単体ビルドできないことを確認した
（import 不足で `Encodable.encode` と `Finset.image` が未知になる）。前 tick の台帳の
「ビルドを通した」という記載は実態と一致していない。新規作業より先に import を補って修正した。

そのうえで完成符号（各点の残り深さまで打ち切った符号）の段を必要十分版へ接続した。
打ち切り安定性に要るのは「子では深さ指標がちょうど一つ増える」ことと「深さ指標が上界を越えない」
ことだけであり、保存に要るのは子表を移す全単射・深さ指標の保存・上界の一致だけである。
型全体の有限性、自己写像、周期点、二値状態、セル、近傍、局所規則は使わない。具体版の
再帰的前像木符号をこの特殊化として明示し、打ち切り安定性と共役による符号保存を導出へ置き換えた。
落とせなかった仮定は上界の一致で、具体版では配位集合の全単射から得る $|V|=|W|$ が与える。

基点語・周期成分符号・写像符号への接続は未形式化なので、対象は `Lean 具体版まで` のまま。
次 tick は本 tick をレビューし、基点語の保存を必要十分版へ接続する。

## 自動ループ tick: 再帰的前像木符号の共役不変性の必要十分版の中核（2026-08-24 02:12）

前 tick の完全不変量・有限決定の必要十分版と具体版からの導出を全層でレビューし、修正を要する
不一致は見つからなかった。

Lean 必要十分版へ、各点に付く有限子表から作る打ち切り再帰符号と、子表を全単射に移す写像が
全ての深さで符号を保存する定理を追加した。具体版の打ち切り符号をこの一般定義の特殊化へ置き換え、
既存の共役による打ち切り符号保存を必要十分版から直接導出した。必要なのは二つの型の等号判定、
有限子表、全単射、同一の再帰だけであり、型全体の有限性、自己写像、周期点、最小前周期、二値状態、
セル、近傍、局所規則は使わない。有限集合・自然数・有限多重集合の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

完成符号以降の共役不変性の必要十分版は未形式化なので、対象は `Lean 具体版まで` のままである。
次 tick は本 tick をレビューし、最小前周期と有限型の元数の保存を使う完成符号の保存を必要十分版へ接続する。

## 自動ループ tick: 周期成分全単射の全配位での一段発展可換性（2026-08-23 21:12）

前 tick の全配位への接着をレビューした。周期成分出現と成分内配位の従属和を経由した接着は
全単射として正しく、一段発展との可換性を主張していないという記載も実態と一致していた。
一方で、成分内の構造保存全単射について既に証明されていた可換性は、周期位置と打ち切り前像木の
節点で添字づけた形（周期辺の保存と親子辺の保存）しかなく、成分表の配位そのものについて
「一段発展と可換する」と述べた形が無いことを確認した。これが接着へ進むために不足していた段である。

Lean 具体版で、等しい基点語から作った周期成分全単射が、成分表の全ての配位で一段発展と可換することを
証明した。各配位を周期根と打ち切り前像木節点の対に一意に分解し、節点が根であるか親を持つかで場合分けして、
根の場合は周期辺の保存（次の周期位置への移動、周期の最後では最初の周期根へ戻る）を、親を持つ場合は
非周期親子辺の保存を適用した。有限集合・自然数・写像・従属和の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

固定した周期成分対応を経由する全配位全単射の可換性はまだ形式化していない（周期軌道の等号で移送した
全単射を扱う必要があるため）。対象表は実態どおり `SageMath まで` のままとする。
次 tick は本 tick の可換性をレビューし、この成分ごとの可換性を全配位全単射の可換性へ接着する。

## 自動ループ tick: 構造保存周期成分対応の全配位への接着（2026-08-23 19:42）

前 tick の全周期成分にわたる同時選択をレビューし、等しい基点語を持つ周期根と構造保存周期成分
全単射が、一つの重複度付き周期成分対応の上で同時に保持されていることを確認した。修正を要する不一致はなかった。

Lean 具体版で、その周期成分対応を固定し、各成分に再帰的前像木から作った構造保存全単射を配置した。
全配位の一意な周期成分分割を使い、周期成分出現型と成分内配位の従属和上でこれらを接着して、全配位集合の
全単射を得た。有限集合・自然数・従属和・写像・有限列・有限多重集合の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

接着した全単射の一段発展との可換性と有限決定可能性は未形式化なので、対象は `SageMath まで` のまま。
次 tick は今回の接着をレビューし、全配位全単射が一段発展と可換であることだけを証明する。

## 自動ループ tick: 全周期成分の構造保存対応の同時選択（2026-08-23 18:12）

前 tick の周期成分保存と周期根対応を全層と突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版で、写像符号が与える重複度付き周期成分対応の全成分について、基点、等しい基点語、
再帰的前像木から構成した周期成分全単射、および周期辺・非周期親子辺保存を同時に選べることを
証明した。有限集合・自然数・写像・有限列・有限多重集合の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

全配位の従属和接着と共役条件は未形式化なので、対象は `SageMath まで` のまま。
次 tick は本 tick の同時選択をレビューし、選んだ成分全単射を一意な周期成分分割に沿って接着する。

## 自動ループ tick: 周期成分保存と周期根対応の補完（2026-08-23 16:12）

前 tick の周期成分内の全辺保存をレビューし、修正を要する不一致は見つからなかった。
全配位への接着に不足していた二点を Lean 具体版へ補った。一段発展は最終周期根の周期軌道を
変えないため各周期成分表を保存する。また、再帰的前像木対応から作った周期成分全単射は、
同じ有限周期添字の周期根を対応先の周期根へ送る。有限集合・自然数・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

全周期成分への接着と全配位上の共役条件は未形式化なので、対象表は `SageMath まで` のまま。
次 tick は、構造保存された周期成分対応を重複度付き周期成分対応に沿って全配位へ接着し、
全配位上の共役条件を閉じる。

## 自動ループ tick: 周期成分全単射の共役条件（2026-08-23 14:12）

前 tick の周期成分全単射による全非周期親子辺保存を、再帰的親子辺、周期位置の接着、
周期辺保存と突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版に、打ち切り前像木の各節点が根または親節点を持つこと、同じ有限添字で対応させた
周期辺の保存と、周期成分全単射による全非周期親子辺の保存を統合した定理を追加した。これにより、
対応する一つの周期成分について、周期辺と非周期辺の一段発展保存が一つの主張に揃った。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期辺の有限添字対応と周期成分全単射の根上の値の同一視、および全周期成分の対応を一つの
全配位全単射へ接着する段への移送は未形式化なので、対象表は
`SageMath まで` のまま。次 tick は、周期成分ごとの構造保存全単射を写像符号の重複度付き
周期成分対応に沿って接着し、全配位上の共役条件を閉じる。

## 自動ループ tick: 周期成分全単射への全非周期辺保存の接着（2026-08-23 12:12）

前 tick の全非周期親子辺保存を、周期位置ごとの有限配位表全単射と、周期成分表の従属和分割へ
突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版に、等しい基点語から構成した周期成分表全単射が、任意の周期位置へ流入する前像木の
全非周期親子辺で一段発展と可換することを証明した。有限集合・自然数・従属和・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

周期辺保存との統合と、全周期成分を接着した全配位全単射への移送は未形式化なので、対象表は
`SageMath まで` のまま。次 tick は、全非周期辺保存と既証明の周期辺保存を統合し、
周期成分全単射の共役条件を閉じる。

## 自動ループ tick: 子部分木内部の全親子辺の保存（2026-08-23 10:12）

前 tick の周期根直下の非周期辺保存を、打ち切り前像木の再帰的節点対応と構造保存された有限配位表全単射へ
突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版に、打ち切り前像木の非周期親子辺を根直下と子部分木内部から再帰的に生成する関係として定義した。
この関係が実配位の一段発展を表すこと、再帰的節点全単射が全深さで関係を保存すること、構造保存された
有限配位表全単射が全非周期親子辺で一段発展と可換することを深さ帰納法で証明した。有限集合・自然数・
写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期位置ごとの従属和対応への接着と全周期成分への接着は未形式化なので、対象表は `SageMath まで` のまま。
次 tick は、有限配位表上の全非周期辺保存を周期位置ごとの従属和対応へ接着する。

## 自動ループ tick: 周期根に隣接する非周期辺の保存（2026-08-23 08:12）

前 tick の構造保存された有限配位表全単射を、再帰的節点対応の根・子の計算則と突き合わせ、
抽象節点から実配位への写像および十分深い全単射に修正を要する不一致は見つからなかった。

Lean 具体版に、任意の打ち切り深さで根を表す節点と、再帰的節点対応が根を根へ送る計算則を追加した。
周期根の非周期一段前像を表す子節点を構造保存全単射へ入れると、対応先で選ばれた非周期子そのものへ
移ること、および両側の非周期子がそれぞれの周期根へ一段発展することを証明した。有限集合・自然数・
写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

固定したのは周期根に隣接する非周期辺だけであり、子部分木の内部辺すべての保存と周期成分表への接着は
未形式化である。対象表は `SageMath まで` のまま。次 tick は、子部分木内の親子辺保存を深さ帰納法で
構造保存全単射へ移す。

## 自動ループ tick: 構造保存配位写像による有限配位表全単射（2026-08-23 06:12）

前 tick の抽象節点から実配位への再帰写像を、最小前周期増分、基点への反復、周期点へ流入する
有限配位表の定義と突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版で、節点が表す配位の最小前周期と節点深さの関係を証明し、そこから実配位写像の
単射性を再帰的に示した。周期点を根とする全節点の像が有限配位表に属することを示し、十分な深さでの
既証明の個数一致から全射性を導いた。これにより、節点型と有限配位表の全単射を個数一致から任意に
選ぶものから、節点が表す実配位そのものを返す構造保存写像へ置き換えた。有限集合・自然数・写像の
等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

Lean 具体版はまだ未完了で、対象表は `SageMath まで` のまま。次 tick は、この全単射が再帰的対応の
親子辺を有限配位表上で保存することだけを証明する。

## 自動ループ tick: 前像木節点の構造保存写像（2026-08-23 04:12）

前 tick の周期成分表全単射を、再帰的前像木対応と一段発展の保存条件へ突き合わせた。
節点型から有限配位表への変換が個数一致から選ぶ任意の全単射であり、親子辺を保存する根拠を
持たないため、その全単射をそのまま使って共役条件を導くことはできないと確認した。

Lean 具体版に、打ち切り前像木の各抽象節点が表す実際の配位と基点からの深さを再帰的に定義し、
その配位を深さだけ反復すると基点へ戻ることを、各子が親の非周期一段前像であることから証明した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造を保存する有限配位表全単射と一段発展との可換性は未形式化であり、対象表は
`SageMath まで` のまま。次 tick は、今回の配位写像が十分な深さで有限配位表への全単射になることを
証明し、個数一致から選ぶ任意の全単射を置き換える。

## 自動ループ tick: 再帰的前像木対応の周期成分表への接着（2026-08-23 02:12）

前 tick の周期根による従属和分割を、打ち切り前像木節点型、十分な深さでの
有限配位表全単射、周期位置ごとの再帰的対応と突き合わせ、修正を要する不一致はなかった。

Lean 具体版で、等しい基点語を持つ二つの周期点に対応する周期成分表間の全単射を構成した。
両舞台の前周期上界の最大値を共通深さとし、各周期位置の有限配位表全単射を従属和上で
接着し、前 tick の周期根分割を通して周期成分表へ戻した。有限集合・自然数・従属和・
写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

一段発展との可換性は未形式化であり、対象表は `SageMath まで` のまま。次 tick は、
この周期成分表全単射が非周期辺と周期辺の一段発展を保存することを示す。

## 自動ループ tick: 周期根による周期成分表の従属和分割（2026-08-23 00:18）

前 tick の打ち切り前像木節点型と有限配位表の全単射を、構造化記述、相対深さ層、
一意な周期根の定義と突き合わせ、修正を要する不一致はなかった。

Lean 具体版で、一つの周期軌道へ流入する周期成分表を、最小周期より前の周期位置と、
その位置の周期点へ流入する前像木節点表との従属和へ分解する全単射を構成した。
単射性は最小周期より前の反復値の相異性と周期根の一意性から、全射性は各配位の
周期根が周期軌道のいずれかの位置にあることから示した。有限集合・自然数・写像の
等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期位置ごとの再帰的前像木全単射をこの従属和上で接着する段と、一段発展の保存は
未形式化である。対象表は `SageMath まで` のまま。次 tick は、今回の分割の各成分へ
前像木対応から得た有限配位表全単射を配置し、周期成分表間の全単射を構成する。

## 自動ループ tick: 打ち切り前像木節点型と有限配位表の全単射（2026-08-22 22:12）

前 tick の再帰的節点全単射を構造化記述・相対深さ層・有限配位表の個数一致と突き合わせ、
修正を要する不一致はなかった。Lean 具体版で打ち切り節点型の有限性と元数公式を証明し、
十分な深さでは一つの周期点へ流入する有限配位表との全単射を構成した。その両側の全単射で
再帰的節点全単射を挟み、対応する周期点の有限配位表間の全単射へ移した。

周期上の全位置に付く有限配位表を周期成分表へ接着することと、一段発展の保存は未形式化である。
対象表は `SageMath まで` のまま。次 tick は、一意な周期根による分割に沿って位置ごとの全単射を
周期成分表間の全単射へ接着する。

## 自動ループ tick: 再帰的前像木対応からの有限節点全単射（2026-08-22 20:12）

前 tick の周期成分ごとの全配位全単射は、成分表の個数一致から選んだものであり、
共役性をまだ主張していないことを構造化記述と突き合わせた。修正を要する不一致はなかった。

Lean 具体版に、打ち切り前像木の節点を根と子部分木の従属和として再帰的に表す型を追加し、
`HasTreeMatching` が持つ子出現の全単射を深さ再帰で合成して、対応する二つの節点型の
全単射を構成した。根の保存と、子出現対応の後に部分木対応へ再帰する計算則も証明した。
有限集合・自然数・従属和・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

十分な深さの節点型と周期成分表の全単射、全周期成分への接着、時間発展との可換性は
未形式化である。対象表は `SageMath まで` のままとする。次 tick は節点型を周期成分表へ
重複なく写す全単射を構成し、今回の再帰的節点全単射を成分表間の全単射へ移す。

## 自動ループ tick: 周期成分ごとの対応の全配位全単射への接着（2026-08-22 14:12）

前 tick の成分添字回復を、周期成分表の一意な分割と成分表ごとの有限全単射に
突き合わせ、修正を要する不一致がないことを確認した。

Lean 具体版に、全配位集合を周期成分出現ごとの成分表へ分解する従属和との全単射を追加し、
周期成分対応と各成分表の有限全単射を従属和上で合成した。これにより既存の全配位接着写像が
単射かつ全射であることを証明した。有限集合・有限多重集合・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

成分内全単射は個数一致から選んだもので、再帰的前像木対応への置き換えと時間発展との可換性は
未形式化である。対象表は `SageMath まで` のままとする。次 tick は、再帰的前像木対応から成分内写像を
構成し、全配位全単射性を保ったまま共役条件を証明する。

## 自動ループ tick: 成分所属からの一意な成分添字の回復（2026-08-22 12:12）

前 tick の接着写像と、周期成分表の一意な分割・周期成分出現の
全単射を突き合わせ、修正を要する不一致がないことを確認した。

Lean 具体版に、成分表への所属から、一意性によって選んだ
成分添字を回復する定理を追加した。これは全配位上の接着写像の
単射性・全射性で必要な成分対応の回復段である。有限集合と写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

重複度付き出現型の依存型等式を含む全単射性と、再帰的前像木対応への
置き換え、時間発展との可換性は未形式化である。対象表は
`SageMath まで` のままとする。次 tick は、今回の成分添字回復を使い、
全配位上での単射性と全射性を完成する。

## 自動ループ tick: 対応周期成分表の全配位写像への接着（2026-08-22 10:12）

前 tick の周期成分表による一意な全配位分割を、写像符号が与える周期成分の
重複度付き対応および成分表の個数一致と突き合わせ、修正を要する不一致は
見つからなかった。

Lean 具体版に、周期成分対応の固定、対応する成分表間の有限全単射、各配位の
一意な成分添字、その添字を重複度付き周期軌道表へ移す写像を定義した。これらを
合成して全配位上の一つの写像を定義し、その値が必ず対応先の周期成分表に属する
ことを証明した。有限集合・自然数・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

この写像の成分内全単射は個数一致から選んだものであり、再帰的前像木対応を
まだ使っていない。したがって全配位での全単射性と時間発展との可換性は
未証明で、Lean 具体版の層は未完了、対象表は `SageMath まで` のままとする。
次 tick は、本 tick の接着写像が成分対応を保存することをレビュー後、
成分内写像を再帰的前像木対応から構成した写像へ置き換え、全単射性を証明する。

## 自動ループ tick: 全周期成分接着のための一意な成分分割（2026-08-22 08:12）

前 tick の周期成分内の有限対応と周期辺の可換性を、構造化記述の
完全性証明と SageMath の再帰構成へ突き合わせた。Lean build と
SageMath の完全性構成（共役対 3,073 対、符号一致対 4,633 対）を
再実行し、修正を要する不一致は見つからなかった。

Lean 具体版に、最終周期根の軌道による周期成分表への所属条件、
相異なる周期成分表の非交差、全配位の被覆、各配位が属する周期成分
添字の存在一意性を追加した。これにより、前 tick までに得た成分ごとの
対応を全配位へ接着するとき、各配位へ適用する成分対応が一意に定まる。
有限集合、自然数、写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

成分ごとの前像木対応から具体的な全配位写像を定義し、その全単射性と
時間発展との可換性を示す段は未形式化である。Lean 具体版は未完了なので
対象表は `SageMath まで` のままとする。次 tick は、この一意な成分分割を
使って成分ごとの有限対応を全配位上の写像へ接着する。

## 自動ループ tick: 周期成分内の有限対応と周期辺の可換性（2026-08-22 06:12）

前 tick の局所的な子対応を、構造化記述の再帰構成と SageMath の
完全性検算へ突き合わせた。非周期子の対応が各親への一段発展を
保存する局所形として正しく、修正を要する不一致は見つからなかった。

Lean 具体版に、等しい基点語が与える共通の有限周期添字対応が、
周期の途中の辺と最後の点から基点へ戻る辺を両方とも保存することを
証明した。さらに、写像符号が与える周期軌道の重複度付き対応ごとに、
各周期位置の全深さ前像木対応と周期辺の可換性を同時に選べることを
証明した。有限集合、有限多重集合、自然数、写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

相異なる周期成分の有限対応を全配位上の一つの全単射へ接着する段は
未形式化である。Lean 具体版は未完了なので対象表は `SageMath まで`
のままとする。次 tick は、成分ごとの対応を全成分で接着し、
時間発展と可換な全配位全単射を構成する。

## 自動ループ tick: 再帰的子対応の局所可換性（2026-08-22 04:12）

前 tick の全配位全単射は配位数の一致だけから選ぶため、共役性の証明には
使えない。前 tick は共役性を主張しておらず、修正を要する不一致はなかった。

Lean 具体版に、正の深さの前像木対応から子の出現の全単射を選び、
対応する各子が両側の一段発展でそれぞれの親へ送られることを証明した。
これは非周期辺上の共役条件の局所形である。有限集合・有限多重集合・写像の
等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期辺対応と前像木対応を全成分で接着する段は未形式化である。Lean 具体版は
未完了なので、対象表は `SageMath まで` のままとする。次 tick は局所対応を
周期成分ごとの有限対応へ接着する。

## 自動ループ tick: 写像符号からの全配位全単射（2026-08-22 02:12）

前 tick の全配位数・セル数一致を、周期成分ごとの有限表の個数和と
写像符号が与える周期成分対応へ突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版に、写像符号の等号から得た全配位数一致を使って二つの有限配位集合の
全単射を固定する定義と、その対応写像が全単射である定理を追加した。有限集合と
自然数の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。この段の全単射は
個数一致から選んだものであり、時間発展との可換性はまだ主張していない。

共役条件と有限決定は未形式化である。Lean 具体版は未完了なので対象表は
`SageMath まで` のままとする。次 tick は、周期軌道と全深さ前像木の対応から
時間発展と可換する全配位全単射を構成し、共役条件だけを形式化する。

## 自動ループ tick: 写像符号からの全配位数とセル数の一致（2026-08-22 00:12）

前 tick の十分な深さでの前像木節点数と前像木節点表の個数一致を、
周期軌道の重複のない添字付け、周期成分への流入表、全配位の周期成分分割と
突き合わせ、修正を要する不一致は見つからなかった。

Lean 具体版に、最小周期より前の反復値の相異性、上界より深い相対層の空性、
十分な共通深さでの節点数と流入表個数の一致を追加した。周期成分ごとの流入表が
全配位を一意に分類する個数和へ、写像符号の等号が与える周期成分対応と成分ごとの
節点数一致を接続し、全配位数 $2^{|W|}=2^{|V|}$ とセル数 $|W|=|V|$ の一致を証明した。
有限集合、自然数、有限和だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので
対象表は `SageMath まで` のままとする。次 tick は、セル数一致を既存の周期軌道・
全深さ前像木対応へ代入し、全配位上の対応写像の定義と全単射性だけを形式化する。

## 自動ループ tick: 十分な深さでの前像木節点表との個数一致（2026-08-21 22:12）

前 tick の再帰的節点数と相対深さ層別和の等式を、最小前周期の上界、
周期根への到達写像、前像木節点表の定義と突き合わせた。層の深さと
最小前周期が一致し、その深さだけ反復した値が周期根であることに修正を要する
不一致は見つからなかった。

Lean 具体版に、周期点へ流入する有限表を各節点の最小前周期でファイバー分解し、
深さ $2^{|V|}-1$ 以下の相対層がその表を重複なく尽くす個数等式を追加した。
前 tick の層別和と合成し、その深さの再帰的節点数が前像木節点表の個数に
一致することを証明した。有限集合、自然数、有限和だけで閉じ、$\mathbb R/\mathbb C$
脱出はない。

対応周期成分の有限和から全配位数の一致とセル数一致を導く段、
全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので
対象表は `SageMath まで` のままとする。次 tick は、対応周期成分ごとの節点数一致と
全周期点の前像木節点表の個数和を接続し、全配位数とセル数の一致だけを形式化する。

## 自動ループ tick: 再帰的前像木節点数の相対深さ層別和（2026-08-21 20:12）

前 tick の相対深さ層と一段再帰分解を、打ち切り前像木節点数の再帰定義と
突き合わせた。深さ零で根だけを数え、後続段で非周期一段前像の各部分木を
足す定義と、相対層の一段分解に修正を要する不一致は見つからなかった。

Lean 具体版に、有限集合の基礎多重集合の出現型上の和と有限集合上の和を
結ぶ補助定理を追加し、一段再帰分解を深さ帰納法で足し上げた。これにより、
打ち切り前像木の再帰的節点数が、根からの相対深さが打ち切り深さ以下である全層の
個数和に等しいことを証明した。有限集合、自然数、有限和だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

十分な深さで相対層の和が前像木節点表の個数に一致すること、配位数・セル数の一致、
全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので対象表は
`SageMath まで` のままとする。次 tick は、十分な打ち切り深さで相対層が前像木節点表を
重複なく尽くすことを示し、再帰的節点数とその表の個数の一致だけを形式化する。

## 自動ループ tick: 前像木の相対深さ層と一段再帰分解（2026-08-21 18:12）

前 tick の前像木節点表による全配位の有限分割を、最小前周期・反復・周期点の定義と
突き合わせた。到達先の周期性、相異なる周期根の表の非交差、全配位の被覆、個数和に
修正を要する不一致は見つからなかった。

Lean 具体版に、反復回数が最小前周期以下なら反復後の最小前周期がその回数だけ減る定理、
根との最小前周期差と反復到達を同時に記録する相対深さ層、その零層が根だけであること、
一つ深い層が非周期一段前像を根とする層へ重複なく分かれる個数等式を追加した。
分類写像は深さ k+1 の節点を k 回反復して得る一段前像であり、各ファイバーが対応する
相対深さ k の層に一致することを証明した。有限集合・自然数・有限和だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

相対深さ層の個数和と再帰的節点数の一致、および十分な深さでその層の個数和が
前像木節点表の個数に一致することは未形式化である。Lean 具体版は未完了なので対象表は
`SageMath まで` のままとする。次 tick は、今回の一段再帰分解を深さ帰納法で足し上げ、
再帰的節点数が深さ以下の相対層の個数和に等しいことだけを形式化する。

## 自動ループ tick: 前像木節点表による全配位の有限分割（2026-08-21 16:12）

前 tick の周期成分ごとの前像木節点数の有限和を、構造化記述の完全性証明と SageMath の
再帰構成検算へ突き合わせ、各加数と有限和に修正を要する不一致は見つからなかった。

Lean 具体版に、各配位を最小前周期だけ反復して到達する周期点と、その周期点へ流入する配位の
有限表を定義した。到達先の周期性、相異なる周期点に属する表の非交差、全周期点の表による
全配位の被覆を証明し、一意な有限ファイバー分解から全表の個数和が $2^{|V|}$ に等しいことを
導いた。有限集合・自然数・写像の等号・有限和だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

再帰的節点数と有限表の個数の一致、それを対応周期成分の有限和へ接続して得る配位数・セル数の
一致、全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので対象表は
`SageMath まで` のままとする。次 tick は、十分な打ち切り深さにおける再帰的節点数と前像木
節点表の個数の一致だけを形式化する。

## 自動ループ tick: 対応周期成分の前像木節点数の有限和（2026-08-21 14:12）

前 tick の共通深さの前像木節点数一致を、構造化記述の完全性証明と SageMath の再帰構成検算へ
突き合わせた。対応する周期上の各位置で前像木対応があり、最小周期も一致することに修正を要する
不一致は見つからなかった。SageMath 5 本を再実行し、全て PASS した。

Lean 具体版に、一つの周期成分について周期上の各位置に付く前像木節点数を足す有限和を定義し、
等しい基点語を持つ対応周期成分では各位置の節点数一致を有限和へ持ち上げられることを証明した。
さらに写像符号の等号から、周期成分の重複度を保つ対応と、対応成分ごとの有限和の一致を同時に
選べることを形式化した。有限集合・有限多重集合・自然数の有限和だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

周期点ごとの前像木が全配位を重複なく尽くすこと、その全成分にわたる有限和が全配位数に等しい
こと、セル数一致、全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので
対象表は `SageMath まで` のままとする。次 tick は、前像木の被覆と非交差を有限表として形式化し、
全成分の節点数和が全配位数に等しいことを証明する。

## 自動ループ tick: 共通深さの前像木節点数の一致（2026-08-21 12:12）

前 tick の再帰符号の深さ安定化と共通深さの前像木対応を、構造化記述の再帰停止条件・
SageMath の再帰構成検算・Lean の最小前周期上界と突き合わせ、修正を要する不一致は
見つからなかった。

Lean 具体版に、打ち切り前像木の節点数を根と非周期子の節点数の和として再帰定義し、
前像木の出現全単射から節点数一致を深さ帰納法で導いた。さらに写像符号が等しい二写像について、
周期軌道の重複度付き対応、等しい基点語、および周期上の全位置に付く共通深さの前像木節点数一致を
同時に選べることを形式化した。有限集合・有限多重集合・自然数の和だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

各周期点に付く前像木節点数の総和が全配位数に等しいこと、その等式からのセル数一致、
全配位全単射、共役条件、有限決定は未形式化である。Lean 具体版は未完了なので対象表は
`SageMath まで` のままとする。次 tick は、周期点ごとの木が全配位を重複なく尽くすことを
有限和として証明し、全配位数一致とセル数一致を導く段だけを進める。

## 自動ループ tick: 再帰符号の深さ安定性と共通深さの前像木対応（2026-08-21 10:12）

前 tick の空多重集合表現の修正を、構造化記述の空多重集合階層と SageMath の入れ子タプル表現へ
突き合わせ、同じ葉が打ち切り深さによらず同じ符号になることを確認した。修正を要する不一致は
見つからなかった。

セル数一致を先に仮定して共通の打ち切り深さを作ると、写像符号からセル数一致を導く段が循環する。
そこで Lean 具体版に、残り深さを越えて再帰符号を延長しても値が変わらないことを最小前周期の
一段増分から帰納的に証明し、両舞台の上界の最大値を共通深さとして、セル数一致を仮定せずに
対応周期点の前像木対応を構成する定理を追加した。有限集合・有限多重集合・自然数の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

写像符号の等号からセル数一致を導く段、全配位全単射、共役条件、有限決定は未形式化である。
Lean 具体版は未完了なので対象表は `SageMath まで` のままとする。次 tick は、今回得た共通深さの
前像木対応から全配位の個数一致を導き、セル数一致へ戻す段だけを進める。

## 自動ループ tick: 再帰符号の空多重集合表現の修正（2026-08-21 08:12）

前 tick の接着定理の前提である Lean 上の再帰符号を、構造化記述と SageMath の
入れ子有限多重集合符号に突き合わせた。深さ零で空多重集合を `1` と置く一方、
後続深さでは空リストの実際の符号 `Encodable.encode [] = 0` を使っており、同じ空多重集合が
深さによって異なる自然数になる不一致を確認した。深さ零も空リストの符号に統一し、
非周期一段前像が空な点では深さ零と任意の正の深さの符号が一致することを Lean で追加証明した。

このレビュー修正を優先したため、写像符号の等号からセル数一致を導く段は未形式化である。
Lean 具体版は未完了なので、対象表は `SageMath まで` のままとする。次 tick は修正後の
再帰符号をレビュー後、セル数一致の導出だけを進める。

## 自動ループ tick: 再帰的前像木符号の周期軌道と前像木の接着（2026-08-21 06:12）

前 tick の周期軌道対応・等しい基点語の選択・周期上の各位置の再帰符号一致を、構造化記述の
完全性証明と SageMath の再帰構成検算に突き合わせ、修正を要する不一致は見つからなかった。
Lean 具体版に、等しい基点語を持つ対応周期点について、両舞台のセル数一致を明示的な仮定として
共通の打ち切り深さを作り、周期上の各位置へ全深さの非周期前像木対応を接着する定理を追加した。
さらに写像符号の等号から、周期軌道の重複度付き対応・等しい基点語・各周期位置の前像木対応を
同時に選ぶ定理を追加した。有限集合・有限多重集合・有限列・自然数の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

写像符号の等号だけからセル数一致を導く段、接着した対応から全配位写像を定義して全単射性と
共役条件を示す段、有限決定は未形式化である。Lean 具体版は未完了なので、対象表は
`SageMath まで` のままとする。次 tick は本 tick の接着定理をレビュー後、セル数一致の導出だけを進める。

## 自動ループ tick: 再帰的前像木符号の周期軌道対応段（2026-08-21 04:12）

前 tick の全深さ前像木対応を構造化記述の完全性証明・SageMath の再帰構成検算と突き合わせ、
修正を要する不一致は見つからなかった。Lean 具体版に、写像符号の多重集合等号から周期軌道の
出現を重複度つきで対応させる定理、対応する成分符号から等しい基点語を持つ周期点を選ぶ定理、
基点語の等号から最小周期と周期上の各位置の再帰符号が一致する定理を追加した。有限集合・
有限多重集合・有限列・自然数の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

周期上の対応と全深さ前像木対応を一つの全配位全単射へ接着する段、全単射性・共役条件・
有限決定は未形式化である。Lean 具体版は未完了なので、対象表は `SageMath まで` のままとする。
次 tick は本 tick 分をレビュー後、周期上の対応と全深さ前像木対応の接着だけを進める。

## 自動ループ tick: 再帰的前像木符号の Lean 具体版の全深さ対応段（2026-08-21 02:12）

前 tick の子符号対応段を構造化記述・SageMath とレビューし、修正を要する不一致は
見つからなかった。Lean 具体版へ、後続深さの符号等号から重複度を保つ子の出現全単射と
一つ浅い符号保存を得る定理、深さ全体の前像木対応を表す再帰的述語、符号等号からその対応を
深さ帰納法で構成する定理を追加した。有限集合・有限多重集合・自然数・等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。

周期軌道の重複度つき対応・基点語の選択・周期辺との接着・完全性・有限決定は未形式化なので、
対象の現在地は実態どおり `SageMath まで`。次 tick は全深さ対応をレビュー後、周期軌道と
基点語の対応を形式化して前像木対応と接着する。

## 自動ループ tick: 再帰的前像木符号の Lean 具体版の子符号対応段（2026-08-21 00:12）

前 tick の写像符号保存段を構造化記述・SageMath とレビューし、修正を要する不一致は
見つからなかった。Lean 具体版へ、写した多重集合の等号から各値を保つ出現全単射を取る
一般定理と、後続深さの符号等号から非周期一段前像の一つ前の深さの符号多重集合等号を
取り出す定理を追加した。これは完全性証明の「等しい子符号多重集合を重複度つきで対応させる」
段に対応する。有限多重集合・自然数・等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

全前像木への深さ帰納、周期軌道との接着、完全性・有限決定は未形式化なので、対象の現在地は
実態どおり `SageMath まで`。次 tick は本 tick をレビュー後、子の出現対応を全前像木へ
持ち上げ、周期軌道と接着して共役全単射を得る段だけを進める。

## 自動ループ tick: 再帰的前像木符号の Lean 具体版の写像符号保存段（2026-08-20 06:12）

前 tick の共役移送段を構造化記述・SageMath 5 本の記載と突き合わせ、修正を要する不一致は
見つからなかった。`lean/CellularAutomata/RecursivePreimageTreeCode.lean` に写像符号の保存
$\mathcal K(F)=\mathcal K(G)$ を追加し、共役不変性の claim の結論を Lean で閉じた。反復の
加法則、周期点が最小周期とその倍数回の反復で戻ること、周期軌道の所属の存在量化への言い換え、
軌道の元が周期点であること、周期軌道と成分符号が基点の取り替えで不変であること（人手証明の
基点非依存性に対応）、周期軌道の有限表全体の全単射移送、写像符号の多重集合の保存を形式化し、
sorry 検査へ 12 定理を登録した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$
脱出はない。構造化テキスト check・PDF（67 ページ・未解決参照 0）、検算対応 35 件、SageMath
5 本再実行、Lean build・sorry 検査を通した。符号一致からの再帰構成・完全性・有限決定は
未形式化なので、対象表の状態は実態どおり `SageMath まで` のままとする。次 tick は本 tick 分を
レビュー後、符号一致からの再帰構成と完全性の形式化だけを進める。

## 自動ループ tick: 再帰的前像木符号の Lean 具体版の共役移送段（2026-08-20 04:26）

前 tick の Lean 具体版の前半を構造化記述・SageMath 5 本の記載と突き合わせ、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/RecursivePreimageTreeCode.lean` に共役不変性の
証明の前段を追加した。周期性の組の点ごとの両側移送、各配位の最小前周期・最小周期の保存、
共役全単射の存在から従うセル数の一致、打ち切り深さごとの符号の保存（深さの帰納法）、
再帰的前像木符号・基点語・成分符号の保存、周期軌道の有限表の像等号を形式化し、sorry 検査へ
9 定理を登録した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（67 ページ・未解決参照 0）、検算対応 35 件、Lean build・sorry 検査を
通した。写像符号の保存・符号一致からの再帰構成・完全性・有限決定は未形式化なので、対象表の
状態は実態どおり `SageMath まで` のままとする。次 tick は本 tick 分をレビュー後、写像符号の
保存で共役不変性の claim を閉じ、完全性・有限決定へ進む。

## 自動ループ tick: 再帰的前像木符号の Lean 具体版の前半（2026-08-20 03:20）

前 tick の SageMath 5 本を構造化記述と突き合わせ、定義段・共役不変性・再帰構成・完全性・
有限決定の各検査と有限範囲の記載に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/RecursivePreimageTreeCode.lean` に Lean 具体版の前半を追加した。
非周期一段前像、その所属同値、最小前周期の一段増分、上界、有限深さの子符号多重集合を
整列して自然数へ単射符号化する再帰符号、周期軌道・基点語・成分符号・写像符号の有限表を
人手証明と同じ順序で定義した。共役による周期点の移送、非周期一段前像の点ごとの移送と
有限表の像等号まで証明した。有限集合・自然数・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。共役による再帰符号・成分符号・写像符号の保存、符号一致からの
全単射の再帰構成、完全性、有限決定は未形式化なので、対象表の状態は実態どおり `SageMath まで`
のままとする。次 tick は本 tick 分をレビュー後、Lean 具体版の後半だけを作る。

## 自動ループ tick: 再帰的前像木符号の SageMath 検算（2026-08-20 02:12）

前 tick の完全性・共役不変性・有限決定の構造化記述を定義から再検討し、修正を要する不一致は
見つからなかった。`sagemath/check/recursive-preimage-tree-code/` に検算 5 本を追加した。
定義段（最小前周期の増分・上界・再帰符号の well-defined 性と多重集合階層への所属。769 写像・
3,585 配位）、共役不変性（周期点・非周期一段前像・点ごとの符号・基点語・成分符号・写像符号の
保存。共役対 3,073 対）、完全性の再帰構成（証明の構成をそのまま実装し全単射性と共役条件を検査。
共役対 3,073 対と元数 1・2・4 の全自己写像の符号一致対 4,633 対）、完全不変量の同値（同サイズ
全順序対 65,553 対で符号等号と全単射全数走査の判定が一致。元数違い 2,568 対では両者とも不成立）、
有限決定（セル数 0・1・2 の全初等 CA 規則対 263,169 対で局所真理値表からの符号計算・等号・構成が
独立な全単射走査と一致。符号一致 32,257 対）を分離し全て PASS。浮動小数点と
$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF（67 ページ・未解決参照 0）、
検算対応 35 件、Lean build・sorry 検査を全て通した。
**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 再帰的前像木符号の完全性の構造化記述（2026-08-20 01:12）

前 tick の再帰符号・周期語・成分符号・写像符号の定義を、既存の最小前周期・共役による反復移送と
突き合わせ、修正を要する不一致は見つからなかった。構造化記述の残りとして、共役全単射による
再帰符号と写像符号の保存、等しい子符号の有限多重集合からの前像木全単射の葉側からの再帰構成、
等しい周期語による周期辺の接着、写像符号一致と共役全単射存在の同値、局所真理値表からの
有限決定を追加した。有限集合・自然数・有限列・入れ子有限多重集合だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF（67 ページ・未解決参照 0）、
検算対応 34 件、Lean build・sorry 検査を全て通した。
**現在地は構造化記述まで。次 tick は全記述をレビュー後、共役不変性・再帰構成・完全性・
有限決定を段ごとに分けた SageMath 検算を作る。**

## 自動ループ tick: 再帰的前像木符号の定義の構造化記述（2026-08-20 00:42）

前 tick の起票「周期成分に付随する再帰的前像木符号の完全性」を既存定義（周期点集合・最小前周期・
最小周期・一段発展の最小前周期一段減少・和の上界）と突き合わせ、修正を要する不一致はなかった。
`structured-latex/content/recursive-preimage-tree-code.ts` に構造化記述の前半（定義）を書いた。
入れ子有限多重集合の階層、非周期一段前像（周期点から出る辺を除いた前像関係）、非周期一段前像の
最小前周期が一つ大きいこと、上界 $\mu(y)\le2^{|V|}-1$、$\mu$ の大きい側からの帰納法で
well-defined になる再帰符号 $c_F(y)$、周期軌道・基点語・成分符号（巡回同値類を基点語全体の
集合で代表）・写像符号 $\mathcal K(F)$ を定義した。有限集合・自然数・入れ子有限多重集合だけで
閉じ、$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF（66 ページ・未解決参照 0）、
検算対応 34 件、Lean build、sorry 検査を全て通した。
**共役不変性・完全性（符号一致 ⟺ 共役全単射の存在）・有限決定の claim は未記述で、
構造化記述の層は未完了。次 tick は本 tick 分をレビュー後、これらの claim だけを追記する。**

## 自動ループ tick: 数値プロファイル非完全性の必要十分版レビューと次対象の起票（2026-08-19 22:12）

前 tick の Lean 必要十分版と具体版からの導出を構造化記述・SageMath・Lean 具体版と突き合わせた。
不動点と有限反復到達、子孫有限表、子孫個数不等式、共役存在の有限決定に必要な仮定が段ごとに
分離され、具体版の主要段が特殊化として導出されている。SageMath 5 本も再実行して全て PASS し、
修正を要する不一致はなかった。
次対象「周期成分に付随する再帰的前像木符号の完全性」を `todo` で起票した。周期辺を除いた前像木の
再帰的多重集合符号、周期上の巡回同値類、成分符号の多重集合が有限自己写像の共役を決定するか、
有限真理値表から符号と共役全単射を有限決定・復元できるかを直接問う。
**次 tick はレビュー後、この対象の構造化記述だけを作る。**

## 自動ループ tick: 数値プロファイル非完全性の Lean 必要十分版と導出（2026-08-19 21:12）

前 tick の Lean 具体版を構造化記述・SageMath と突き合わせ、反例二表・反復の各行・安定ファイバー・
深さ別個数列・分岐個数・子孫個数を定義から再計算して一致を確認した。SageMath 5 本も再実行して
全て PASS し、修正を要する不一致はなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidConjugacyNumericalProfile.lean` に必要十分版を追加した。
不動点と有界反復での到達の移送には共役条件だけが要り、単射性・全射性・有限性・等号判定は
要らない。子孫集合の有限表とその像の包含に両型の有限性・等号判定、子孫個数の不等式にはさらに
$h$ の単射性だけ、共役全単射の存在の有限決定にだけ全構造が要る。具体版の反復・根の不動性・
子孫有限表・個数不等式・有限決定が必要十分版の特殊化として得られる導出を具体版ファイルへ追加し、
全検証を通した。
**「安定ファイバー根付き木族の数値プロファイルは共役を決定するか」は四層が揃い `done`。
次 tick は本 tick の必要十分版と導出を全層でレビューし、次の CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 数値プロファイル非完全性の SageMath 検算（2026-08-19 19:12）

前 tick の構造化記述を反復表・深さ・分岐個数・子孫集合の定義から再計算してレビューした。
数学的な反例はなかったが、単元安定ファイバーの深さ別個数列を証明中で $(1)$ と書いていた
箇所が定義（添字は $k=0$ から $m_F=3$ まで）と食い違っていたため $(1,0,0,0)$ へ修正した。
結論のプロファイル等号は変わらない。

`sagemath/check/iterate-monoid-conjugacy-numerical-profile/` に検算 5 本を追加した。反例二表の
2 値 CA 実現と反復の各行・$\mu=3$・$\lambda=1$・安定ファイバー、共役対の全数族 3,073 対での
$\mathcal P(F)=\mathcal P(G)$ の定義からの再計算、反例の深さ別個数列・分岐個数多重集合と
プロファイル一致、子孫集合個数 $\{\!\{4,2\}\!\}\ne\{\!\{3,3\}\!\}$ と全 $8!$ 個の全単射での
共役不成立、元数 1・2・4 の全自己写像対 65,553 対での全単射走査の有限決定を分離し全て PASS。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF（64 ページ）、
検算対応 34 件、Lean build、sorry 検査を全て通した。
**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 数値プロファイルの非完全性の反例と構造化記述（2026-08-19 18:12）

前 tick の共役不変性の必要十分版後半と導出を全層で再レビューし、修正を要する不一致はなかった。
SageMath 6 本も再実行して全て PASS した。

有限根付き木の走査から、深さ別個数列 $(1,2,3,1)$ と分岐個数多重集合
$\{\!\{0,0,0,1,1,2,2\}\!\}$ が一致する非同型な 7 頂点の二木を得た。共通の単元ファイバーを
加えた 8 配位上の二つの大域写像は、$\mu=3$、$\lambda=1$、安定ファイバー数 2 と全ファイバーの
数値プロファイルが一致するが、根直下の部分木の頂点数多重集合が
$\{\!\{4,2\}\!\}$ と $\{\!\{3,3\}\!\}$ で異なるため共役ではない。
`structured-latex/content/iterate-monoid-conjugacy-numerical-profile.ts` に、数値プロファイルの定義、
共役不変性、この反例、全単射の有限全数検査による共役存在の有限決定を構造化記述として収録した。
有限集合と自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick は本章をレビュー後、反例表・プロファイル一致・非共役性・
全単射走査を分離した SageMath 検算だけを作る。**

## 自動ループ tick: 共役不変性の必要十分版レビューと次対象の起票（2026-08-19 17:12）

前 tick の Lean 必要十分版の後半と導出を全層でレビューした。根付き辺の移送に要るのが
ファイバー所属の両側同値・根の移送・共役写像の単射性だけ、深さの移送にはさらに両側の根到達と
反復の移送だけ、有限辺表と分岐個数にだけ両型の有限性・等号判定を加えるという仮定分離が、
構造化記述の後半 4 主張の証明が実際に使う性質と一致し、具体版の 4 導出定理が特殊化に
なっていることを確認した。修正を要する不一致はなかった。SageMath 6 本（3,073 共役対）を
再実行して全て PASS、構造化テキスト check・PDF、検算対応 33 件、Lean build、sorry 検査を通した。

次対象「安定ファイバー根付き木族の数値プロファイルは共役を決定するか」を `todo` で起票した。
共役で保存されることを証明済みの数値不変量（最小衝突開始位置・最小正周期・安定ファイバーの個数、
各ファイバーの深さ別個数と分岐個数の分布）の全一致から共役全単射の存在が従うかを問い、
従わなければ反例を claim として記録する。共役全単射の存在自体の有限決定も同じ対象で問う。
**次 tick はこの対象の成否確定（反例の有限走査）と構造化記述だけを進める。**

## 自動ループ tick: 共役不変性の Lean 必要十分版の完了と導出（2026-08-19 16:12）

前 tick の必要十分版の前半は、構造化記述・SageMath・Lean 具体版と整合し、修正を要する
不一致はなかった。後半は自己写像・ファイバー・根だけへ抽象化し、根付き辺の保存・反映、
最小根到達回数、有限辺表、分岐個数の移送を形式化した。有限性と等号判定は有限表と分岐個数にだけ要る。
具体版の根付き辺・一周期写像の全反復・深さ・分岐個数の導出を追加し、全検証を通した。
**この対象は四層が揃い `done`。次 tick は本 tick の必要十分版と導出を全層でレビューし、
修正がなければ次の CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 共役不変性の Lean 必要十分版の前半と導出（2026-08-19 14:12）

前 tick の Lean 具体版をレビューした。構造化記述の 16 主張と具体版 16 定理の対象・仮定・順序の
対応を確認し、SageMath 検算 6 本を再実行して全て PASS。修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidConjugacyInvariance.lean` に必要十分版の前半を書いた。
反復の移送は h が単なる写像で足り、反復写像の等号の保存は h の全射性だけ・反映は単射性だけを
使う。三つの最小指数の保存に要るのは両側同値と衝突開始位置の存在だけで、存在は同値で片側から
もう片側へ移り、有限性はこの章の定理に現れない。安定像・安定ファイバーの移送は巡回冪等元に
限らず共役な任意の写像対で成り立ち、E の冪等性は要らない。具体版の反復移送〜一周期写像移送の
10 定理の導出を具体版ファイルへ追加し、sorry 検査へ 25 定理を登録した。全検証を通した。
**根付き辺・深さ・分岐個数の移送と有限表の必要十分版・導出（後半）は未収録で、この層は未完了。
次 tick は本 tick 分をレビュー後、必要十分版の後半と残りの導出だけを作る。**

## 自動ループ tick: 共役不変性の Lean 具体版（2026-08-19 13:12）

前 tick の SageMath 検算 6 本を再実行し、6,146 個の写像表で全て PASS。構造化記述との対応に
修正を要する不一致は見つからなかった。`lean/CellularAutomata/IterateMonoidConjugacyInvariance.lean` に、
二つの有限舞台上の 2 値 CA と共役全単射について、反復、反復写像の等号、最小衝突開始位置・
最小正周期・最小安定周期倍数指数、巡回冪等元、安定像・安定ファイバー、一周期写像とその全反復、
根付き辺・深さ・分岐個数の保存を人手証明と同じ対象・仮定・順序で形式化した。根付き木族の有限表も
定義し、入口 import と sorry 検査へ 16 定理を登録した。有限集合・自然数・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 共役不変性の SageMath 検算（2026-08-19 12:12）

前 tick の構造化記述の後半をレビューした。数学的な反例はなかったが、記述の規律上の参照欠落を
3 か所修正した。深さ保存の証明が使う同定 $R_F^n=F^{n\lambda_F}$ を新しい claim（帰納法）として
追加して証明を深さの定義の集合で書き直し、$G$ 側の深さの根が $h(q)$ であることの導出と、
$h(q)\in Q_G$ による安定ファイバー・根付き辺集合の well-defined 性の参照を補った。

`sagemath/check/iterate-monoid-conjugacy-invariance/` に検算 6 本を追加した。各大域写像に決定的な
全単射（恒等・反転・互換・巡回・合成）を当てて共役 $G=h\circ F\circ h^{-1}$ を作り（769 写像から
3,073 対）、反復の移送と反復等号の両側同値、最小衝突開始位置・正周期集合・最小正周期・指数集合・
最小指数の一致と冪等元の移送、安定像・安定ファイバーの全単射移送、一周期写像とその全反復の移送、
根付き辺・深さ・分岐個数の保存、最初の再出現走査による有限決定を分離して全て PASS。有限集合・
自然数・写像の等号だけで閉じ、浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。構造化テキスト
check・PDF（63 ページ・未解決参照 0）、検算対応 33 件、Lean build、sorry 検査を全て通した。
**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版
だけを作る。**

## 自動ループ tick: 共役不変性の構造化記述の完了（2026-08-19 11:12）

前 tick の構造化記述の前半を既存定義と突き合わせ、反復等号の両側同値、最小衝突開始位置・
最小正周期・巡回冪等元の移送に修正を要する不一致は見つからなかった。

`structured-latex/content/iterate-monoid-conjugacy-invariance.ts` に残りを追記した。共役全単射が安定像と
各安定ファイバーを全単射に移し、一周期写像とその全反復を移送することから、根付き辺の保存と反映、
根への深さ、分岐個数の保存を証明した。最小衝突開始位置・最小正周期・巡回冪等元・安定ファイバー
根付き木族の有限決定も既証明の有限走査から導いた。有限集合・自然数・写像の合成と等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**構造化記述の層は完了。次 tick は本 tick 分をレビュー後、
SageMath 検算だけを作る。**

## 自動ループ tick: 共役不変性の構造化記述の前半（2026-08-18 19:12）

前 tick の起票「有限大域写像の共役による安定ファイバー根付き木族の不変性」を既存定義
（最小衝突開始位置・最小正周期・巡回冪等元・安定ファイバー・根付き木）と突き合わせ、
各保存主張が既存定義だけで意味を持つことを確認した。修正を要する不一致は見つからなかった。

`structured-latex/content/iterate-monoid-conjugacy-invariance.ts` に構造化記述の前半を書いた。
二つの有限舞台上の 2 値 CA の大域写像 $F,G$ を結ぶ共役全単射 $h$（$h\circ F=G\circ h$）を定義し、
反復の移送 $h\circ F^n=G^n\circ h$（帰納法）、反復写像の等号の両側同値（全射性と単射性を
別方向に使う）、衝突開始位置集合の一致による $\mu_F=\mu_G$、正周期集合の一致による
$\lambda_F=\lambda_G$、指数集合の一致による $e_F=e_G$ と冪等元の移送 $h\circ E_F=E_G\circ h$ を
証明した。有限集合・自然数・写像の合成と等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（60 ページ・未解決参照 0）、検算対応 32 件、Lean build、sorry 検査を
全て通した。tick の強制終了時刻の制約から、安定像・ファイバー対応と根付き木の辺・深さ・
分岐個数の保存・有限決定は未記述で、**構造化記述の層は未完了。次 tick は本 tick 分をレビュー後、
同じ章へ残りの構造化記述だけを追記する。SageMath はその後。**

## 自動ループ tick: 深さ対応の必要十分版レビューと次対象の起票（2026-08-18 18:12）

前 tick の「根付き木の深さと最小前周期層の対応」の Lean 必要十分版と具体版からの導出を、
構造化記述・SageMath 6 本・Lean 具体版と突き合わせた。切り上げ値、切り上げ位置での根到達、
前方位置での非到達、深さとの一致について、必要十分版の仮定が証明で実際に使う性質だけに
分離され、具体版の主要主張と有限走査表が必要十分版の特殊化として導出されていることを確認した。
修正を要する不一致は見つからなかった。SageMath 6 本（769 写像・3,585 配位）、検算対応、
構造化テキスト check・PDF、Lean build・sorry 検査を全て通した。

次の CA 内在的対象として「有限大域写像の共役による安定ファイバー根付き木族の不変性」を
`todo` で起票した。有限集合上の二つの自己写像を結ぶ全単射が反復と可換するとき、最小衝突開始位置・
最小正周期・巡回冪等元を保存し、安定像と安定ファイバーを対応させ、一周期写像の根付き木の辺・深さ・
分岐個数を保存するかを確定する。有限真理値表からこの根付き木族を有限決定できることも同じ対象で問う。
**次 tick はこの対象の構造化記述だけを進める。**

## 自動ループ tick: 根付き木の深さと最小前周期層の対応の Lean 必要十分版と導出（2026-08-18 17:15）

前 tick の Lean 具体版を構造化記述・SageMath 6 本（再実行し 769 写像・3,585 配位で全て PASS）と
突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidRootDepthPreperiodCorrespondence.lean` に必要十分版を
追加した。一つの型・自己写像・一つの点・根の値に対し、切り上げ値の定義には切り上げ集合の
非空性だけ、切り上げ位置での根到達には位置 μ での λ 周期の衝突・位置 mλ での根到達・μ ≤ mλ
だけ（λ ≥ 1 も μ の最小性も不要）、前方の非到達にはさらに λ ≥ 1 と μ の最小性だけ、深さとの
一致には上の二主張だけが要る。反復モノイド・ファイバー族・型の有限性は本体には不要で、有限性と
等号判定は全点の対応表の有限走査にだけ要る。具体版の切り上げ値・根到達・前方非到達・深さの
一致・対応表が必要十分版の特殊化として得られる導出定理を具体版ファイルへ追加し、sorry 検査へ
20 定理を登録した。構造化テキスト check・PDF（57 ページ）、SageMath 6 本、検算対応 32 件、
Lean build、sorry 検査を全て通した。**「根付き木の深さと最小前周期層の対応」は四層が揃い
`done`。次 tick は必要十分版と導出を全層でレビューし、次の CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 根付き木の深さと最小前周期層の対応の Lean 具体版（2026-08-18 16:12）

前 tick の SageMath 6 本を再実行し、全写像の周期の各配位への移送、切り上げ値の well-defined 性、
切り上げ位置での根到達、それより前の非到達、深さとの一致、有限走査に修正を要する不一致がなく、
769 写像・3,585 配位で全て PASS することを確認した。
`lean/CellularAutomata/IterateMonoidRootDepthPreperiodCorrespondence.lean` に、全写像の最小正周期を
各配位の最小前周期位置へ移す定理、切り上げ値 $c_F(y)$ とその最小性、切り上げ位置での根到達、
それより前の非到達、$d_F(y)=c_F(y)$、全配位の有限走査表を人手証明と同じ対象・仮定・順序で
形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（57 ページ）、検算対応 32 件、SageMath 6 本、Lean build・sorry 検査を
全て通した。**現在地は Lean 具体版まで。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 根付き木の深さと最小前周期層の対応の SageMath 検算（2026-08-18 15:12）

前 tick の構造化記述を既存定義（最小前周期・最小正周期・根到達指数・根付き木の深さ）と
突き合わせた。数学的な不一致は見つからなかったが、切り上げ値の定義が $\mu_F\le e_F$ を参照なしで
使っていたため、最小の安定後周期倍数指数と安定後の周期倍数指数の両定義への参照を補った。

`sagemath/check/iterate-monoid-root-depth-preperiod-correspondence/` に検算 6 本を追加した。
全写像の周期の各配位への移送、切り上げ値 $c_F(y)$ の well-defined 性と最小性、切り上げ位置での
根到達、切り上げ位置より前の非到達、深さとの一致 $d_F(y)=c_F(y)$、自然数比較走査の停止と定義値
との一致を分離した。769 写像・全 3,585 配位で全て PASS（前方位置 1,664 組、走査比較 5,249 回）。
有限集合・自然数・写像の等号だけで閉じ、浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（57 ページ・未解決参照 0）、検算対応 32 件、Lean build、sorry 検査を
全て通した。**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 根付き木の深さと最小前周期層の対応の構造化記述（2026-08-18 14:12）

前 tick の「安定ファイバー上の一周期写像が定める根付き木」の補完された導出を、必要十分版・
構造化記述・SageMath 7 本と突き合わせた。深さの零特徴づけ・有向閉路不存在・辺数について、
有限走査表の一致を経て必要十分版の特殊化となることを確認し、修正を要する不一致は見つからなかった。

次対象の成否を先に確定するため、セル数 0 と 1 以上 3 以下の巡回舞台上の全初等 CA 769 写像・
全 3,585 配位を有限真理値表から厳密に走査した。全配位で
$d_F(y)=\min\{d\in\mathbb N\mid\mu(y)\le d\lambda_F\}$ が成立し、反例はなかった。この走査は
構造化記述前の成否確認であり、対象ラベルと overview を備えた SageMath 検算層ではない。

`structured-latex/content/iterate-monoid-root-depth-preperiod-correspondence.ts` に、全写像の周期
$\lambda_F$ が各配位の最小前周期位置でも周期になること、最小前周期を $\lambda_F$ 単位で切り上げた
$c_F(y)$、$c_F(y)$ 回で根へ到達すること、それより前には最小前周期より前の衝突が生じるため到達
できないこと、したがって $d_F(y)=c_F(y)$ であること、有限決定可能性を分けて記述した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick は本章をレビュー後、周期の各配位への移送、切り上げ位置での
根到達、切り上げ位置より前の非到達、有限走査を分離した SageMath 検算を作る。**

## 自動ループ tick: 根付き木の導出定理の補完と次対象の起票（2026-08-18 13:12）

前 tick の Lean 必要十分版と導出を全層でレビューした。仮定の限定（ファイバー保存・根の不動性・
有限反復での根到達の三性質。有限性と等号判定は有限列挙だけ）と証明手順の一致は確認したが、
必要十分版に一般定理がある深さの零特徴づけ・有向閉路の不存在・辺数の 3 主張に、具体版が特殊化と
して得られることを明示する導出定理が欠けていた。修正として、ファイバー表・辺表が必要十分版の
有限走査表に一致する 2 補題と、零特徴づけ・閉路不存在（階数を深さ $d_F$ に取る）・辺数の
3 導出定理を追加し、sorry 検査へ登録した。構造化テキスト check・PDF（55 ページ・未解決参照 0）、
SageMath 7 本（769 写像で全て PASS）、検算対応 31 件、Lean build、sorry 検査を全て通した。

次対象「根付き木の深さと最小前周期層の対応」を起票した。木の深さ $d_F(y)$ が最小前周期
$\mu_F(y)$ と最小正周期 $\lambda_F$ だけから $\min\{d\in\mathbb{N}\mid\mu_F(y)\leq d\cdot\lambda_F\}$
として決まるかを先に確定し、成り立つ場合に限り四層で記述、成り立たない場合は反例を記録する。
**次 tick はこの対象の成否確定と構造化記述だけを進める。**

## 自動ループ tick: 安定ファイバー上の一周期写像が定める根付き木の Lean 必要十分版と導出（2026-08-18 12:12）

前 tick の Lean 具体版を全層と突き合わせ、修正を要する不一致は見つからなかった。
必要十分版では、自己写像 $R$、ファイバー族、根に対する「ファイバー保存・根の不動性・有限反復での
根到達」の三性質だけから、不動点一意性と最小根到達深さの零特徴づけ・一段減少を導いた。閉路排除は
自然数値の階数の厳密減少だけへ分離し、有限性と等号判定は辺・分岐の有限表にだけ要求した。
具体版では一周期写像の $d$ 回反復と $F^{d\lambda_F}$ の一致を示し、根到達、不動点一意性、深さの一致、
一段減少を必要十分版の特殊化として導出した。二値性、セル、近傍、局所規則、周期モノイドは一般層に
要らず、$\mathbb R/\mathbb C$ 脱出はない。全検証を通し、対象は `done`。
**次 tick は必要十分版と導出を全層でレビューし、次の CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバー上の一周期写像が定める根付き木の Lean 具体版（2026-08-18 11:12）

前 tick の SageMath 7 本を再実行して 769 写像で全て PASS、構造化記述との対応に修正を要する不一致は
見つからなかった。`lean/CellularAutomata/IterateMonoidStableFiberRootedTree.lean` に人手証明と
1 対 1 の具体版を書いた。一周期写像 $R_F=F^{\lambda_F}$ と根到達指数 $m_F=e_F/\lambda_F$ を定義し、
周期倍数の伝播（$d$ の帰納法）、逐次合成、$E_F\circ R_F=E_F$ の等式連鎖によるファイバー保存、
$F^{m_F\lambda_F}=E_F$ による根への到達、根の不動性、不動点の一意性（$F^{d\lambda_F}(y)=y$ の
帰納法）、根付き辺集合の有限走査表と辺数 $|B_F(q)|-1$、深さ（`Nat.find`）の零特徴づけと一段減少、
深さの厳密減少による有向閉路の不存在、深さ走査の $m_F$ 停止と分岐数の有限走査を同じ順序で
形式化し、19 定理を sorry 検査へ登録した。比較回数のコストモデルは既出章と同じく形式化していない。
構造化テキスト check・PDF（55 ページ・未解決参照 0）、SageMath 7 本、検算対応 31 件、Lean build、
sorry 検査を全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と
具体版からの導出を作る**（周期伝播・逐次合成・ファイバー保存・根到達・不動点一意性は集合上の
写像と自然数だけで述べられる見込み。有限性と等号判定が要るのは辺数・分岐数の有限走査だけかが
最初の検査項目になる）。

## 自動ループ tick: 安定ファイバー上の一周期写像が定める根付き木の SageMath 検算（2026-08-18 10:12）

前 tick の構造化記述を、最小正周期・冪等反復写像・安定ファイバーの既証明と突き合わせ、修正を要する
不一致は見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-rooted-tree/` に検算 7 本を追加し、
周期倍数の伝播と一周期写像の逐次合成、安定ファイバー保存、根到達、不動点の一意性、非根から辺への
全単射と辺数、深さ零・一段減少・有向閉路不存在、一周期写像表・深さ・分岐数の有限走査を分離した。
セル数 0 と $1\le|V|\le3$ の全初等 CA 規則から得る 769 写像で全て PASS。初回の周期伝播検査は
反復表の列挙上限不足で `IndexError` になったため、必要最大指数まで厳密に列挙するよう修正し、主張を
弱めず再実行した。有限集合・自然数・写像の等号だけで閉じ、浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
**この章の現在地は SageMath まで。次 tick は検算と構造化記述をレビュー後、人手証明と同じ対象・仮定・
順序の Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバー上の一周期写像が定める根付き木の構造化記述（2026-08-18 09:12）

前 tick（レビューと起票のみ）の起票内容を既存定義と突き合わせ、修正を要する不一致は見つからなかった。
`structured-latex/content/iterate-monoid-stable-fiber-rooted-tree.ts` に、一周期写像
$R_F:=F^{\lambda_F}$ と根到達指数 $m_F:=e_F/\lambda_F$ を定義し、周期の倍数伝播
$F^{n+d\lambda_F}=F^n$（$n\ge\mu_F$）、$R_F$ による安定ファイバーの保存、$F^{m_F\lambda_F}=E_F$
による全元の根への到達、根が唯一の不動点であること、根付き辺集合
$T_F(q)=\{(y,R_F(y))\mid y\in B_F(q)\setminus\{q\}\}$ の辺数 $|B_F(q)|-1$、深さ
$d_F(y)=\min\{d\mid F^{d\lambda_F}(y)=q\}$ の零特徴づけと一段減少、深さの厳密減少による
有向閉路の不存在、辺・深さ・分岐数の有限決定可能性を証明した。有限集合・自然数・写像の等号だけで
閉じ、$\mathbb{R}/\mathbb{C}$ 脱出はない。構造化テキスト check・PDF（55 ページ・未解決参照 0）、
検算対応、Lean build、sorry 検査を全て通した。**この章の現在地は構造化記述まで。次 tick は
レビュー後にこの章の SageMath 検算を作る**（$R_F$ 表とファイバー保存、根への到達と不動点の一意性、
辺数と深さの一段減少、分岐数を分けて検査する）。

## 自動ループ tick: 層別分岐個数の必要十分版レビューと次対象の起票（2026-08-18 08:18）

前 tick の「安定ファイバーの層別分岐個数」の Lean 必要十分版と具体版からの導出を全層でレビューした。
一段前像表の非交差、正の層と零層の個数保存、有限列挙の仮定が実際の証明で使う構造だけに分離され、
具体版の 5 主張が必要十分版の特殊化として個別に導出されていることを確認した。修正を要する不一致はなく、
SageMath 4 本は 769 写像で全て PASS、検算対応、構造化テキスト check、Lean build、sorry 検査も通した。

次対象は「安定ファイバー上の一周期写像が定める根付き木」。一周期写像
$R_F:=F^{\lambda_F}$ が各安定ファイバー $B_F(q)$ を保ち、$q$ を唯一の不動点とし、全元を有限反復で $q$ へ送るかを
先に確定する。成り立つ場合に限り、根の自己ループを除いた辺 $y\to R_F(y)$ が有限根付き木を定めることと、
辺集合・深さ・分岐の有限決定を調べる。線形性や既存の分類定理は仮定しない。**次 tick はこの対象の構造化記述だけを進める。**

## 自動ループ tick: 安定ファイバーの層別分岐個数の Lean 必要十分版と導出（2026-08-18 07:12）

前 tick の Lean 具体版を構造化記述・SageMath 4 本（769 写像で再実行し全て PASS）と突き合わせ、
修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStableFiberLayerBranching.lean` に必要十分版を追加した。
一段前像表の非交差には写像と有限走査だけ、正の層と零層の分岐個数には層別完全逆像の章と同じ
三性質（ファイバー添字の点ごとの移送同値・零層保存・正の深さの一段減少）と有限走査だけが要り、
反復・冪等性・ファイバー添字型の有限性は要らない。零層と一層の非交差は深さの値 0 ≠ 1 だけから
従い仮定を要しない。有限列挙にだけ添字型の有限性と等号判定を追加で要する。具体版の 5 主張を
必要十分版の特殊化として導出した。構造化テキスト check・PDF、SageMath 4 本、検算対応、
Lean build、sorry 検査を全て通した。**「安定ファイバーの層別分岐個数」は四層が揃い `done`。
次 tick は必要十分版と導出をレビューし、次の CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバーの層別完全逆像の Lean 具体版レビュー（2026-08-18 01:12、レビューのみ）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、零層保存・正の層の完全逆像・
零層での二層の合流・有限走査表が人手証明と同じ対象・仮定・順序で形式化されており、修正を要する
不一致は見つからなかった。SageMath 4 本を再実行し 769 写像で全て PASS、検算対応・構造化テキスト
check・`lake build`・sorry 検査も通した。開始 01:19・
強制終了 01:46 の制約から、Lean 必要十分版を sorry なしで完了できないと判断し新規層に着手しなかった。
**現在地は Lean 具体版まで。次 tick は Lean 必要十分版と具体版からの導出を作る**（両完全逆像の
等号は移送同値・零層保存・一段減少の 3 性質だけから従う見込み。有限性は零層保存・一段減少の
導出だけ、等号判定は有限走査だけが検査項目）。

## 自動ループ tick: Fable 5 のアカウント経路を固定（2026-08-17）

launchd は対話シェルのアカウント割り当てを継承せず、既定資格情報が指す
`coding-agent-0002` の Fable 5 利用上限で Claude 回が終了コード 1 になっていた。
`coding-agent-0001` は短い probe には終了コード 0 を返したが、実際の tick prompt では同じ上限で
終了コード 1 になった。実 prompt と同じ長さの入力が通った `coding-agent-0003` も、実 tick では
アカウント全体の利用クレジット不足により終了コード 1 になった。そこで、Fable 5・effort medium・
実 tick 相当以上の入力長で終了コード 0 を確認した `coding-agent-0004` の設定ディレクトリと長期トークンを、
Claude プロセスだけへ明示的に渡した。モデルと effort は変更していない。
Claude 上限時に別モデルまたは Codex へ差し替える処理は持たず、Claude と Codex の通常の交互実行だけを残す。

## 自動ループ tick: 安定ファイバーの層別完全逆像の SageMath 検算（2026-08-17 23:50）

前 tick の構造化記述をレビューし、零層保存（周期組 $(0,\pi(y))$ の移送）、正の層の完全逆像
（完全逆像と一段減少の合成）、零層の合流（場合分け）に修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stable-fiber-layer-preimage/` に、証明の各段を分けた検算 4 本を
追加した。完全逆像はファイバーと層の分割性（既証明。走査内でも検査）を根拠に、配位ごとに一度だけ
$F$ を適用して振り分ける一括走査で求め、有限決定の検算では定義どおりの全配位所属検査と突き合わせた。
769 写像（セル数 0 と $1\le|V|\le3$ の全初等 CA 規則）で全て PASS。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。次 tick はレビュー後、人手証明と
同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 安定ファイバーの層別完全逆像の構造化記述（2026-08-17 22:12）

前 tick の「安定ファイバーの最小前周期層」の Lean 必要十分版と導出を全層でレビューした。キャッシュを
使わない Lean build で有限走査表の所属同値に未解決ゴールを検出し、定義と既証明の所属同値だけを
明示的に簡約する証明へ修正した。次対象「安定ファイバーの層別完全逆像」を立て、
`structured-latex/content/iterate-monoid-stable-fiber-layer-preimage.ts` に、零層が零層へ写ること、
正の深さ $k$ の層の完全逆像が一つ上の深さ $k+1$ の層に一致すること、零層の完全逆像では零層と
一層だけが合流すること、有限決定可能性を記述した。有限集合・自然数・写像の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 研究姿勢の正本化（2026-08-14）

[`docs/マニフェスト.md`](docs/マニフェスト.md) を新設した。既存の量子論・場の量子論・相対論を
CA へ移植するのではなく、有限規則から内在的な数学構造を先に抽出し、既存理論は後から明示的な
写像で比較する。実数・複素数を含む非可算構造は説明能力の高い理想化された近似でありうる、という
研究上の前提と、数学的な近似主張には対象・比較写像・誤差または収束概念が要る、という証明上の
規律を分離した。CA 自体も正解として先取りせず、非対応・反例を成果として保存する。

## 区間記法は母集合を添字で明示する（2026-08-15）

自然数区間を実数区間と同じ無添字の $[a,b]$ で書くと、定義後に断りを置いても読者を
ミスリードする。構造化論文では $[a,b]_{\mathbb{N}}$ のように母集合を必ず添字で示す。
既存の自然数区間表記を本文・検算概要・Lean 注釈・台帳で統一し、構造化本文の無添字区間を
`tools/validate-content.ts` が拒むようにした。

## 30 分自動ループの開始（2026-08-14）

`docs/tasks/auto-loop-runbook.md` と `auto-loop-state.md` を正本に、毎時 12 分・42 分に独立した
エージェントを起動する。専用 worktree `~/git/masaori/math-cellular-automata-loop` を使い、
Claude と Codex を交互に動かす。各 tick は前回成果をレビューした後、構造化記述・SageMath・
Lean 具体版・Lean 必要十分版と導出のうち一層だけを進め、`origin/main` への包含まで確認する。

最初の探索列は、物理的意味を入れず、局所真理値表の本質的依存台から始める。冗長近傍への不変性、
有限時間展開上の直接依存、到達可能性の反対称性、有限伝播境界を順に扱い、その後で初めて既存の
因果構造と比較する。

## 自動ループ tick: 構造化証明基盤の初期化と本質的依存台の記述（2026-08-14）

`structured-latex/` を初期化した（システムはリポジトリ直下を利用、`schema.ts`・tools は
`exact-solution-of-2d-ising-model-lambda` の流儀を流用。住処 `habitat`/`realEscape` を型で強制。
Lean 未着手のため lean 連携検査は未設置）。最初の構造化記述
`structured-latex/content/essential-dependency.ts` に、局所真理値表の本質的依存台の定義と
「本質的依存 ⟺ 一点反転検査」の同値、有限決定可能性（高々 $|S|\cdot 2^{|S|}$ 回の比較）を
証明つきで書いた。`pnpm run check`・`pnpm run build:pdf` 通過。**SageMath・Lean は未着手**
（状況は `docs/tasks/auto-loop-state.md` が正本）。用語上の判断: 近傍 $N(v)$ は順序のない
集合なので「座標」でなく「近傍の元」と呼ぶ（番号付けがある舞台に限り「座標」を許す）。
独立再検証で、有限集合・写像を habitat "N" とした分類を訂正した。元の個数を扱うブロックだけを
"N"、有限集合・写像・述語を扱う残りを "finite" とする。schema には今後の可算集合用に
"countable" も追加した。負の型テストの一時生成物も ignore し、検証後に worktree を汚さない。

## 自動ループ tick: 本質的依存台の SageMath 検算（2026-08-14）

前 tick の構造化証明をレビューし、2 元集合の入れ替え写像、本質的依存の存在量化から一点反転検査への
同値、依存台を得る走査の比較回数上界に修正を要する誤りは見つからなかった。
`sagemath/check/essential-dependency-support/` に論理段ごとの検算を追加し、$|S|=0,1,2,3$ の
全 278 真理値表を厳密全数検査した。$|S|=3$ は初等 CA と同じ入力数を持つ 256 規則を全て含む。
本質的依存と一点反転検査、走査結果と定義どおりの依存台は全件で一致し、比較回数は
$|S|2^{|S|}$ 以下だった。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

構造化記述の両 claim から検算ディレクトリへの対応を付け、`sagemath/tools/verify-check-linkage.ts` を
追加した。構造化記述以前の Clifford 塔の探索検算は「未昇格」と明示し、構造化ラベル付き検算と
混同しない。**現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。**
次 tick は前層をレビューした後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。

## 自動ループ tick: 本質的依存台の Lean 具体版（2026-08-15）

前 tick の SageMath 検算と構造化記述を突き合わせ、誤りは見つからなかった。`lean/` を初期化し
（lib 名 `CellularAutomata`、toolchain・mathlib は既存プロジェクトと同じ v4.32.1、依存は
`exact-solution-of-2d-ising-model-lambda` の `.lake` を clone copy で持ち込む運用。README に明記）、
`lean/CellularAutomata/EssentialDependency.lean` に人手証明と 1 対 1 の具体版を書いた。
状態集合は演算を入れない 2 元帰納型（`Bool`・`Fin 2` の既成演算を流用しない）、ν は値の場合分け、
「本質的依存 ⟺ 一点反転検査」は人手証明の両方向をそのまま写した。有限決定可能性は
決定可能性インスタンス＋`mem_supp_iff` と、走査する組 (w,x) の総数 $|S|\cdot2^{|S|}$
（`card_scan_pairs`）として形式化した。**比較回数のコストモデル自体は形式化していない**
（ファイル冒頭に範囲を明記）。`lake build`・`check-no-sorry.sh`（4 定理とも sorryAx 非依存）・
構造化テキスト check・build:pdf・SageMath 3 本・対応検査を全て通した。
**現在地は Lean 具体版まで。次 tick は Lean 必要十分版と導出**（同値だけなら `Fintype S` が
不要なことが具体版の `omit` から既に見えている。これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 本質的依存台の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・証明順序が一致しており、修正を要する
誤りがないことを確認した。`lean/CellularAutomata/NecSuf/EssentialDependency.lean` に必要十分版を
追加した。一点反転との同値に要るのは、添字型の等号判定と、状態型上の写像 `nu` が
「各 `a` と異なる `b` は `nu a` に一意に定まる」ことだけである。添字型・状態型の有限性は同値には
不要で、有限決定可能性に両型の有限性と状態の等号判定、走査組数の式に両型の有限性を追加すれば足りる。
一般の走査組数は $|S|\,|A|^{|S|}$ である。

具体版の決定可能性インスタンスを必要十分版の特殊化として定義し直し、同値と走査組数についても
2 元状態型への特殊化で得られることを別定理で明示した。構造化テキスト check・PDF、SageMath 3 本、
検算対応、Lean build、sorry 検査を全て通した。**本質的依存台の有限決定は四層が揃い `done`。**
次 tick は前層をレビューしてから、「冗長近傍からの独立性」の構造化記述だけを進める。

## 自動ループ tick: 冗長近傍からの独立性の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と具体版からの導出をレビューし、修正を要する誤りは見つからなかった。
台帳先頭の未完了対象「冗長近傍からの独立性」の構造化記述を
`structured-latex/content/redundant-neighbor.ts` に書いた。同一視を使わず、制限写像
$\rho^T_S:A^T\to A^S$ と基準値延長写像 $\iota^T_S:A^S\to A^T$（$\rho\circ\iota$ が恒等）の
2 本の写像だけで $A^S$ と $A^T$ を行き来する。冗長拡大は合成 $(T,\,f\circ\rho^T_S)$ として定義した。
claim は 3 つ: 足した元 $w\in T\setminus S$ には本質的に依存しない（一点反転が制限で消えることによる）、
$w\in S$ への本質的依存は拡大の前後で同値（延長・制限で証人を移す）、よって
$\operatorname{supp}(f\circ\rho^T_S)=\operatorname{supp}(f)$（$T$ の部分集合としての等号）。
全ブロック habitat "finite"、$\mathbb{R}/\mathbb{C}$ なし。構造化テキスト check・PDF（5 ページ・
未解決参照 0）、既存 SageMath 3 本、対応検査、Lean build・sorry 検査を全て通した。
**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 冗長近傍からの独立性の SageMath 検算（2026-08-15）

前 tick の構造化記述をレビューした。3 つの claim の数学的内容に反例はなかったが、制限・延長・
冗長拡大の計算で複数等号を一行へまとめた箇所が「一ステップ一定理」に違反していたため、各等号と
根拠を分離した。`sagemath/check/redundant-neighbor-independence/` に、追加元の一点反転が制限で消える
こと、$\rho^T_S\circ\iota^T_S$ が恒等写像であること、追加元への非依存、元の添字への依存の移送、
本質的依存台の一致を別々に検査する 5 本を追加した。$|T|\le3$ の全包含 $S\subseteq T$、全局所
真理値表、必要な全入力で厳密に検査し全て PASS。一般の有限集合に対する証明ではなく、人手証明の
有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

構造化テキスト check・PDF、既存を含む SageMath 8 本、検算対応、Lean build、sorry 検査を全て
通した。**この章の現在地は SageMath まで。次 tick はレビュー後に、人手証明と同じ対象・仮定・
順序の Lean 具体版だけを作る。**

## 自動ループ tick: 冗長近傍からの独立性の Lean 具体版（2026-08-15）

前 tick の SageMath 5 本と構造化記述の対応をレビューし、修正を要する誤りは見つからなかった。
`lean/CellularAutomata/RedundantNeighbor.lean` に人手証明と 1 対 1 の具体版を書いた。$T$ は
有限型、$S$ は `Finset T`、$A^S$ は部分型 `↥S` 上の写像で表し、$A^S$ と $A^T$ の行き来は
制限写像 ρ と基準値延長写像 ι の 2 本だけを通した（同一視をしないという人手証明の規律を
そのまま形式化に写した）。ρ∘ι の恒等、追加元への非依存、依存の移送、依存台の等号
（supp f を包含写像で `Finset T` へ写した像との等号）の 4 定理を形式化し、sorry 検査の
対象へ追加した。構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、sorry 検査を
全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を
作る**（同値・移送には $T$ の有限性が不要で、依存台の等号にだけ要る見込み。具体版の
`variable [Fintype T]` の位置から既に見えている。これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 冗長近傍からの独立性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、制限写像・基準値延長写像の 2 本だけを通ること、
3 つの claim と証明順序が一致することを確認し、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/RedundantNeighbor.lean` に必要十分版を追加した。制限・冗長拡大・
依存の移送には添字型の等号判定と延長用の基準値だけが要り、添字型・状態型の有限性は不要である。
追加元への非依存は具体版と同じ一点反転検査で証明し、状態側に要る性質を「各元と異なる元が
入れ替え写像の値に一意に定まること」へ限定した。依存台の等号より前の点ごとの必要十分条件にも
有限性は要らず、有限性が要るのは具体版で依存述語を `Finset` として集める段階だけである。

具体版の制限・延長の恒等性、追加元への非依存、依存の移送、依存台の等号が必要十分版の特殊化から
得られることを別定理で明示した。sorry 検査の一時ファイル作成も macOS の `mktemp` で確実に動く
一時ディレクトリ方式へ修正した。構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、
sorry 検査を全て通した。**「冗長近傍からの独立性」は四層が揃い `done`。次 tick はレビュー後に
「時間展開上の直接依存」の構造化記述だけを進める。**

## 自動ループ tick: 時間展開上の直接依存の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と導出をレビューし、仮定の限定・証明手順の一致・特殊化の明示を確認、
修正を要する誤りは見つからなかった。台帳先頭の未完了対象「時間展開上の直接依存」の構造化記述を
`structured-latex/content/time-expansion-dependency.ts` に書いた。有限舞台 $(V,N)$・有限舞台上の
2 値 CA・大域写像 $F$（各セルの値写像が冗長拡大 $f_v\circ\rho^V_{N(v)}$ に等しいことを定義内で
明示）・時間区間 $[0,\tau]_{\mathbb{N}}$・イベント集合 $E_\tau=[0,\tau]_{\mathbb{N}}\times V$・
一段依存関係 $D_\tau$（$t=s+1$ かつ $u\in\operatorname{supp}(f_v)$）を定義した。claim は 4 つ:
大域写像の一点反転による値の変化 ⟺ $u\in\operatorname{supp}(f_v)$（前二章の一点反転同値と
依存台不変性の合成。依存条件に $N(v)$ でなく $\operatorname{supp}(f_v)$ を採る根拠）、
$|E_\tau|=(\tau+1)\cdot|V|$、$D_\tau$ の有限性と所属の有限決定可能性、一段依存での時刻の厳密増加
（次対象「推移閉包の反対称性」の入力）。時間は $\mathbb{N}$ の大小比較と後者だけを使い、
$\mathbb{R}/\mathbb{C}$ 脱出なし。構造化テキスト check・PDF（7 ページ・未解決参照 0）、
SageMath 8 本、対応検査、Lean build、sorry 検査を全て通した。
**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 時間展開上の直接依存の SageMath 検算（2026-08-15）

前 tick の構造化記述を、前二章の定義・主張および用語の正本と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/time-expansion-dependency/` に、大域写像の一点反転による特徴づけ、
時間区間の個数、イベント集合の個数、一段依存関係の所属条件、依存台走査の比較回数上界、
一段依存での時刻増加を分けた 6 本の検算を追加した。$|V|\leq3$ の全ての近傍と局所真理値表を含む
有限範囲で厳密に検査し、全て PASS。一般の有限集合・任意の $\tau\in\mathbb{N}$ に対する証明ではなく、
人手証明の有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 時間展開上の直接依存の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する誤りは見つからなかった
（一段依存の検算が全セル同一規則の範囲に限られる点は、所属条件が組 $(N(v),f_v)$ ごとに
分解されるため、検証範囲の記載どおりで主張の反例探索に影響しない）。
`lean/CellularAutomata/TimeExpansionDependency.lean` に人手証明と 1 対 1 の具体版を書いた。
有限舞台は有限型 $V$ と `N : V → Finset V`、局所規則の族は前二章と同じ部分型上の写像で表し、
$A^V$ と $A^{N(v)}$ の行き来は前二章の制限写像・冗長拡大だけを通した。形式化した定理は、
大域写像の各セルの値写像が冗長拡大に等しいこと、大域一点反転の特徴づけ（前二章の一点反転同値・
依存台定義・依存台不変性を人手証明と同じ順で適用）、時間区間の所属と個数、イベント集合の個数
（直積の積の法則）、一段依存関係の所属の言い換えと $\operatorname{supp}(f_v)\subseteq N(v)$ の含意、
$E_\tau\times E_\tau$ の部分集合性、依存台走査の組数 $|N(v)|\cdot2^{|N(v)|}$、時刻の厳密増加の
10 定理。比較回数のコストモデル自体は前二章と同じく形式化していない（ファイル冒頭に明記）。
構造化テキスト check・PDF、SageMath 14 本、対応検査、Lean build、sorry 検査（新 10 定理を追加）を
全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を
作る**（同値・所属の言い換え・時刻増加には $V$ の有限性が不要な見込み。具体版の `omit` の位置から
イベント集合の個数と依存台の像の段階だけに有限性が要ると既に見えている。これが必要十分版の
最初の検査項目になる）。

## 自動ループ tick: 時間展開上の直接依存の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・証明順序に修正を要する不一致は
見つからなかった。`lean/CellularAutomata/NecSuf/TimeExpansionDependency.lean` に必要十分版を追加した。
大域写像と一点反転の特徴づけには舞台・状態型の有限性は要らず、舞台の等号判定、状態側の
「別値の一意性」、制限と延長の移送に使う基準値一つだけが要る。イベント個数には選んだ有限時間集合と
有限舞台だけが要る。一段依存の時刻増加は任意の時間型上の `next`、`lt`、`lt s (next s)` だけから導ける。
具体版の大域写像、一点反転の特徴づけ、イベント個数、時刻増加がそれぞれ特殊化で得られる導出定理も追加した。
全層の検証を通し、**「時間展開上の直接依存」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「依存の推移閉包の反対称性」の構造化記述だけを進める。**

## 自動ループ tick: 必要十分版の余分な仮定の削除と、推移閉包の反対称性の構造化記述（2026-08-15）

前 tick の「時間展開上の直接依存」の Lean 必要十分版をレビューし、イベント集合の定義と個数定理が
証明で使わない `DecidableEq Time`・`DecidableEq V` を仮定していたのを見つけて削除した
（必要十分版の要件「仮定は実際に使う性質だけ」への修正。要るのは選んだ時間集合の有限性と
舞台の有限性だけである）。修正後に Lean build・sorry 検査を通した。

台帳先頭の未完了対象「依存の推移閉包の反対称性」の構造化記述を
`structured-latex/content/transitive-closure-antisymmetry.ts` に書いた。依存経路
（長さ $n\geq1$ の写像 $p:[0,n]_{\mathbb{N}}\to E_\tau$ で隣接対が $D_\tau$ に入るもの）、到達可能関係
$C_\tau$（依存経路の存在）、反射的到達可能関係 $\preceq_\tau$、部分順序の定義と、claim 5 つ:
経路に沿った時刻の厳密増加（帰納法）、$D_\tau\subseteq C_\tau$、$C_\tau$ の推移性（経路の連結）、
$C_\tau$ が $D_\tau$ を含む最小の推移的関係であること（「推移閉包」の名の根拠）、相互到達の不存在
（反対称性。特例として非反射性）、$\preceq_\tau$ が $E_\tau$ 上の部分順序であること。
使う $\mathbb{N}$ の構造は大小比較と後者だけで、$\mathbb{R}/\mathbb{C}$ 脱出はない。
構造化テキスト check・PDF（9 ページ・未解決参照 0）、SageMath 14 本、対応検査、Lean build、
sorry 検査を全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の
SageMath 検算を作る。**

## 自動ループ tick: 依存順序から得る部分構造の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存・到達可能関係・反射的到達可能関係の定義と突き合わせ、数学的な
不一致は見つからなかった。記録上は claim が 7 つあるのに 6 つと数えていたため訂正した。
`sagemath/check/dependency-order-substructures/` に、下方集合と上方集合の順序凸性、順序凸集合の
共通部分、反鎖の順序凸性、時刻切片の反鎖性、下方集合へ外から入る一段依存の不存在、下方集合の
境界の外向き表示を分離した 6 本の検算を追加した。

$|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係と各イベント集合の全ての部分集合で
7 claim が全て PASS。検査対象は CA から得る関係より広いが、一般の有限舞台に対する証明ではない。
浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**この章の現在地は SageMath まで。次 tick は
検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 依存の推移閉包の反対称性の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存関係の定義と時刻増加の主張に突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/transitive-closure-antisymmetry/` に、経路に沿う時刻増加、
一段依存の包含、経路連結と推移性、推移閉包の最小性、相互到達・自己到達の不存在、反射閉包の
部分順序性を分けた 6 本の検算を追加した。検査対象は CA から得る関係より広い隣接時刻間の任意の
有限関係とし、$|V|\leq2,\ 0\leq\tau\leq3$ と $|V|=3,\tau=1$ の全 4,900 関係で全て PASS。
最小性は $|E_\tau|\leq3$ の全候補関係も列挙した。一般の有限集合に対する証明ではなく、人手証明の
有限範囲での裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 依存の推移閉包の反対称性の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算対象を隣接時刻間の任意の有限関係へ広げている点は、人手証明が一段依存から使う性質が
時刻の厳密増加だけなので、主張の反例探索を狭めない）。
`lean/CellularAutomata/TransitiveClosureAntisymmetry.lean` に人手証明と 1 対 1 の具体版を書いた。
依存経路は 3 条件（長さ 1 以上・値が $E_\tau$ に属する・隣接対が $D_\tau$ に入る）の構造、
到達可能関係 $C_\tau$ は依存経路の存在で定義し、$E_\tau\times E_\tau$ の部分集合ゆえ有限という
定義中の注記も定理として形式化した。経路に沿う時刻増加（経路長の帰納法）、$D_\tau\subseteq C_\tau$、
経路連結による推移性（継ぎ目の well-defined 性は $p(m)=b=q(0)$ の等式）、最小性（経路長の帰納法。
仮定 $R\subseteq E_\tau\times E_\tau$ は人手証明の主張どおり置き、証明で使わないことを明記）、
相互到達の不存在、非反射性、反射的到達可能関係の部分順序性（4 場合分け）の 8 定理を sorry 検査へ
追加した。構造化テキスト check・PDF、SageMath 20 本、対応検査、Lean build、sorry 検査を全て通した。
**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**
（経路・到達可能性・反対称性には舞台・状態型の有限性が不要で、要るのは「一段依存で時刻が
厳密に増える」性質だけの見込み。有限性が要るのは $C_\tau$ の有限性の注記だけ。これが必要十分版の
最初の検査項目になる）。

## 自動ループ tick: 依存の推移閉包の反対称性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、経路長の帰納法、経路連結、時刻増加の両向き適用、
反射閉包の 4 場合分けが同じ順で形式化されており、修正を要する不一致がないことを確認した。
`lean/CellularAutomata/NecSuf/TransitiveClosureAntisymmetry.lean` に必要十分版を追加した。
経路・到達可能性・推移閉包にはイベント集合と一段関係だけが要る。反対称性には任意の時刻型への
写像、推移的かつ非反射的な時刻関係、一段関係の各辺で時刻が増えることだけが要り、舞台・状態・
近傍・局所規則・自然数のその他の構造は不要である。イベント集合の有限性は到達可能関係を有限集合と
して述べる注記だけに分離した。

具体版の経路・到達可能性・有限性・一段包含・推移性・最小性・相互到達と自己到達の不存在・
部分順序性が必要十分版の特殊化として得られる導出定理を追加した。全層の検証を通し、
**「依存の推移閉包の反対称性」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「有限伝播境界」の構造化記述だけを進める。**

## 自動ループ tick: 有限伝播境界の構造化記述（2026-08-15）

前 tick の「依存の推移閉包の反対称性」の Lean 必要十分版と導出をレビューし、仮定の限定
（時刻写像・推移的かつ非反射的な時刻関係・一段ごとの時刻増加のみ。有限性は到達可能関係の
有限性の注記だけ）と 8 定理の導出の明示を確認、修正を要する誤りは見つからなかった。
台帳先頭の未完了対象「有限伝播境界」の構造化記述を
`structured-latex/content/finite-propagation-boundary.ts` に書いた。claim は 4 つ:
依存経路に沿って時刻がちょうど 1 ずつ増える（$t=s+n$。前章の $s<t$ の等式への精密化）、
伝播球（$B_1(v)=\operatorname{supp}(f_v)$、$B_{n+1}(v)=\bigcup_{u\in\operatorname{supp}(f_v)}B_n(u)$）の
有限性と個数の再帰的上界、依存経路の始点セルが伝播球に属すること、依存元集合
$P_\tau(t,v)$ が伝播球の有限合併 $\bigcup_{n\in[1,t]_{\mathbb{N}}}(\{t-n\}\times B_n(v))$ に含まれること
（個数上界 $\sum_{n\in[1,t]_{\mathbb{N}}}|B_n(v)|$ は $|V|$ にも $\tau$ にも依存しない。$P_\tau(0,v)=\emptyset$）。
時間・個数は habitat "N"、残りは "finite"、$\mathbb{R}/\mathbb{C}$ 脱出なし。構造化テキスト
check・PDF（11 ページ・未解決参照 0）、SageMath 20 本、対応検査、Lean build、sorry 検査を
全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 有限伝播境界の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一段依存・依存経路・到達可能関係の定義と突き合わせ、伝播球の再帰の向き、
経路長と時刻差、依存元集合の包含と個数上界に修正を要する不一致は見つからなかった。
`sagemath/check/finite-propagation-boundary/` に、経路の正確な時刻差、伝播球の再帰式と個数上界、
経路始点セルの伝播球所属、依存元集合の包含・個数上界・時刻 0 での空性を分けた 4 本を追加した。
$|V|\leq3$ の全 531 個の本質的依存台の族と $0\leq\tau\leq4$ の 2,655 個の組で全て PASS。
一般の有限舞台に対する証明ではなく、人手証明の有限範囲での裏取りである。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。

**この章の現在地は SageMath まで。Lean 具体版・Lean 必要十分版と導出は未着手。次 tick は
SageMath 検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 有限伝播境界の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算の一段依存・伝播球・境界合併の定義は構造化記述と一致し、個数上界は包含と境界集合の個数を
経由した連鎖で検査されている）。`lean/CellularAutomata/FinitePropagationBoundary.lean` に
人手証明と 1 対 1 の具体版を書いた。supp(f_v) を V の部分集合へ写した像を `suppV` と置き
（前章の一段依存の所属条件と同じ表し方）、経路の正確な時刻差 $t=s+n$（経路長の帰納法。基底は
一段依存の条件 $t=s+1$、帰納段は ℕ の加法の結合則）、伝播球の再帰（人手証明が定義しない
$n=0$ は番人値の空集合と明記。有限性は `Finset` の型が表す）、個数の等式と合併の和の上界、
経路始点セルの伝播球所属、依存元集合（E_τ の部分集合ゆえ有限の注記つき）、有限伝播境界の
包含・個数上界（部分集合 → 合併の和 → 一元集合との直積の順で人手証明と同じ）・時刻 0 での
空性の 12 定理を形式化し、sorry 検査対象へ追加した。伝播球の定義と個数に V の有限性が不要な
ことは `omit [Fintype V]` で明示した。構造化テキスト check・PDF、SageMath 24 本、対応検査、
Lean build、sorry 検査を全て通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と導出を作る**（伝播球・経路の時刻差には V の有限性と自然数の特殊構造の一部が
不要な見込み。`omit` の位置が最初の検査項目になる）。

## 自動ループ tick: 有限伝播境界の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、経路長の帰納法、伝播球の再帰、依存元集合の
境界包含と個数上界が同じ順で形式化されており、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/FinitePropagationBoundary.lean` に必要十分版を追加した。
経路の時刻差には自然数時刻と一段ごとの後者だけ、伝播球にはセル型の等号判定とセルごとの有限な
直接依存元集合だけが要り、セル型全体の有限性は不要である。依存元集合の有限性は舞台全体の有限性
でなく、選んだイベント集合の有限性だけから従う。

具体版の主要定義・定理が必要十分版の特殊化として得られる導出定理を追加した。全層の検証を通し、
**「有限伝播境界」は四層が揃い `done`。次 tick は必要十分版と導出をレビュー後、
「依存順序から得る部分構造」の構造化記述だけを進める。**

## 自動ループ tick: 必要十分版の未使用仮定の削除と、依存順序から得る部分構造の構造化記述（2026-08-15）

前 tick の「有限伝播境界」の Lean 必要十分版をレビューし、依存元個数上界が証明で使わない仮定
「イベント集合の有限性」を持っていたのを見つけて削除した（個数上界は境界が有限集合である
ことだけで通る。イベント集合の有限性が要るのは依存元集合の有限性の注記だけ。具体版の
導出定理の適用も合わせて修正）。他に修正を要する不一致はなかった。

台帳先頭の未完了対象「依存順序から得る部分構造」の構造化記述を
`structured-latex/content/dependency-order-substructures.ts` に書いた。イベント集合 $E_\tau$ と
反射的到達可能関係 $\preceq_\tau$・一段依存 $D_\tau$ だけから、順序凸部分集合・下方集合・
上方集合・非比較関係 $\perp_\tau$・反鎖・時刻切片・一段境界 $\partial K$ を定義した。
claim は 7 つ: 下方集合と上方集合はそれぞれ順序凸、順序凸集合の共通部分は順序凸、
反鎖は順序凸（前章の反対称性が効く）、時刻切片は反鎖（時刻の厳密増加と $\mathbb{N}$ の
非反射性）、下方集合には外から入る一段依存が存在しない、よって下方集合の一段境界は外向きの
一段依存だけで定まる。物理的意味は持ち込まず、既存因果構造との比較は別対象に残した。
時刻切片の claim だけ habitat "N"、残りは "finite"、$\mathbb{R}/\mathbb{C}$ 脱出なし。
構造化テキスト check・PDF（13 ページ・未解決参照 0）、SageMath 24 本、対応検査、Lean build、
sorry 検査を全て通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の
SageMath 検算を作る。**

## 自動ループ tick: 依存順序から得る部分構造の Lean 具体版（2026-08-15）

前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった
（検算の補助定義が構造化記述の定義と一致し、検査対象を CA から得る関係より広い「隣接時刻間の
任意の関係」としている点は overview に明記済みで、主張の反例探索を狭めない）。
`lean/CellularAutomata/DependencyOrderSubstructures.lean` に人手証明と 1 対 1 の具体版を書いた。
順序凸部分集合・下方集合・上方集合・反鎖は有限集合上の述語、非比較関係と一段境界は集合として
定義し、反射的到達可能関係・到達可能関係・一段依存は前二章の定義だけを通した。7 つの claim
（下方・上方集合の順序凸性、順序凸集合の共通部分、反鎖の順序凸性、時刻切片の反鎖性、下方集合へ
外から入る一段依存の不存在、下方集合の一段境界の外向き表示）と定義中の 2 つの注記（非比較関係の
入れ替え対称性、一段境界の有限性）を形式化し、sorry 検査対象へ追加した。人手証明の主張が置く
部分集合条件（J ⊆ E_τ 等）は証明で使わない場合も主張どおり仮定に置き、未使用を引数名の下線で
明示した。構造化テキスト check・PDF、SageMath 30 本、対応検査、Lean build、sorry 検査を全て
通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**
（順序凸・反鎖の claim に舞台の有限性・部分集合条件が不要な見込みが引数の下線から既に見えている。
これが必要十分版の最初の検査項目になる）。

## 自動ループ tick: 依存順序から得る部分構造の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、7 つの claim と定義中の 2 注記について、対象・
仮定・場合分け・証明順序に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/DependencyOrderSubstructures.lean` に必要十分版を追加した。
下方集合・上方集合・順序凸性には集合と関係だけ、反鎖の順序凸性には関係の反対称性と推移性だけ、
時刻切片の反鎖性には時刻写像・非反射的な時刻関係・相異なる二点間での時刻増加だけが要る。
一段境界と下方集合の境界には、一段関係が順序関係に含まれることだけが要る。舞台・状態・近傍・
局所規則・自然数・有限性は、有限境界の注記で対象集合の有限性を仮定する箇所以外では不要である。

具体版の 7 claim と 2 注記を必要十分版の特殊化として導出する定理を追加し、すべてを sorry 検査へ
登録した。構造化テキスト check・PDF、SageMath 30 本、対応検査、Lean build、sorry 検査を通した。
**「依存順序から得る部分構造」は四層が揃い `done`。** 次 tick は必要十分版と導出をレビューし、
台帳先頭の未完了対象「既存因果構造との比較」の構造化記述に着手する。

## 自動ループ tick: 既存因果構造との比較の構造化記述（2026-08-15）

前 tick の「依存順序から得る部分構造」の Lean 必要十分版と導出をレビューし、仮定の限定と
全 claim・注記の導出定理を確認、修正を要する不一致は見つからなかった。台帳先頭の未完了対象
「既存因果構造との比較」の構造化記述を `structured-latex/content/causal-structure-comparison.ts`
に書いた。外部の公理系を目標仕様にせず、組 $(E_\tau,\preceq_\tau,D_\tau)$ だけから述べられる
順序側の性質に固定した。claim は 4 つ: 区間 $I_\tau(a,b)$ の有限性（$\leq(\tau+1)\cdot|V|$）、
$D_\tau\subseteq\lessdot_\tau$（被覆関係。時刻差 1 が中間イベントを排除）、
$\lessdot_\tau\subseteq D_\tau$（長さ 2 以上の経路の中間点で被覆に矛盾）、両包含による
$D_\tau=\lessdot_\tau$——一段依存関係は $(E_\tau,C_\tau)$ だけから復元できる（保存される構造）。
加えて反例 claim: 順序を保つ全単射は時刻写像を保つとは限らない（定値規則・$\tau=1$ で
$\preceq_\tau$ が等号に退化）。時刻写像は順序だけから定まらない（失われる情報）。
「causal set」等の文献名との同定は一次文献の定義を確認した後の層で行うと remark に明記し、
二次情報による同定は記していない。構造化テキスト check・PDF（15 ページ・未解決参照 0）、
SageMath 30 本、対応検査、Lean build、sorry 検査を全て通した。**この章の現在地は構造化記述まで。
次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 既存因果構造との比較の SageMath 検算（2026-08-15）

前 tick の構造化記述と、その入力である「依存順序から得る部分構造」の Lean 必要十分版・導出を
レビューし、修正を要する不一致は見つからなかった。
`sagemath/check/causal-structure-comparison/` に、区間の有限性、一段依存関係と被覆関係の両包含、
両関係の集合としての一致、順序を保つ全単射が時刻写像を保たない反例を分けた 5 本を追加した。
一般側は $|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係について、9,521 区間、
1,061 一段辺、一段辺でない到達可能対 449 組を厳密に検査し、全て PASS。反例側は一元舞台の
定値局所規則と $\tau=1$ を構造化記述どおり構成し、2 イベント上の全 4 順序対で順序保存を確認し、
時刻が変わることを確認した。一般の場合の証明ではなく、有限範囲の裏取りである。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。**この章の現在地は SageMath まで。次 tick は検算と構造化記述の
対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 既存因果構造との比較の SageMath レビュー（2026-08-15、レビューのみ）

前 tick の SageMath 5 本を再実行して構造化記述と突き合わせ、修正を要する不一致は
見つからなかった（反例の構成、一般側の検査対象の広さとも記述どおり）。この tick は開始が
13:12 で 13:39 に強制終了されるため、Lean 具体版を sorry なしで完了できないと判断し新規層に
着手しなかった。**この章の現在地は SageMath まで。次 tick は Lean 具体版を作る。** 再利用先は
`TransitiveClosureAntisymmetry.lean` の `Reachable`・`ReflReachable`・
`path_time_strictly_increases`・`reachable_transitive`・`reachable_irreflexive`。
区間 $I_\tau(a,b)$ と被覆関係 $\lessdot_\tau$ は新規定義になる。

## 自動ループ tick: 既存因果構造との比較の Lean 具体版（2026-08-15）

前 tick の SageMath 5 本を再実行して全て PASS し、構造化記述との対応にも修正を要する不一致は
見つからなかった。`lean/CellularAutomata/CausalStructureComparison.lean` に人手証明と 1 対 1 の
具体版を書いた。区間の有限性と個数上界はイベント集合への包含から示した。被覆関係は到達可能な
中間イベントを持たない到達可能対として定義し、一段依存が被覆される向きは時刻差 1 と経路時刻増加、
被覆が一段依存になる向きは長さ 2 以上の経路の中間点を構成する人手証明の順で形式化した。
両包含による一段依存関係と被覆関係の等号も示した。

順序から時刻を復元できない反例は、一元舞台・自己近傍・定値 0 規則・$\tau=1$ を具体的に置き、
一段依存と到達可能関係が空で反射的到達可能関係が等号に退化すること、二つのイベントを交換する
全単射がその関係を保存するが時刻を変えることとして形式化した。外部の causal set 等の公理系は
持ち込まず、有限集合・有限関係・自然数時刻だけを使い、$\mathbb{R}/\mathbb{C}$ 脱出はない。
`lake build` と sorry 検査を通した。**この章の現在地は Lean 具体版まで。次 tick は構造化記述との
対応をレビュー後、Lean 必要十分版と具体版からの導出を作る。**

## 自動ループ tick: 既存因果構造との比較の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/CausalStructureComparison.lean` を追加した。区間には集合と関係だけ、
有限性・個数上界には集合の有限性だけが要る。一段関係が被覆関係に含まれることには D⊆C・C に沿った
時刻増加・D の対の隣接性（間に時刻がない）だけが要り、自然数時刻は不要。被覆が一段関係に含まれる
ことには経路による到達可能性だけが要り、時刻は不要。時刻非保存の反例には一段関係が空であることと
時刻の異なる二点だけが要り、舞台・近傍・局所規則は不要。具体版の区間・被覆は必要十分版の特殊化に
定義として等しく（`rfl`）、6 定理を必要十分版から導出した。`lake build` と sorry 検査（13 定理追加）
を通した。**これで対象表の全 7 対象が done になった。次 tick はこの必要十分版と導出をレビューし、
runbook「最初の探索列」の末尾（カタログ化後の既存因果構造との一次文献比較）を新対象として
起こすかを判断する。**

## 自動ループ tick: 因果集合の一次文献との照合（2026-08-15）

前 tick の「既存因果構造との比較」の Lean 必要十分版と具体版からの導出を、具体版・構造化記述と
突き合わせ、修正を要する不一致は見つからなかった。Bombelli–Lee–Meyer–Sorkin (1987) の本文で、
因果集合が局所有限な半順序集合として定義されること、連続時空への近似には順序保存埋め込み・
一様密度・尺度分離が要求されることを確認した。

`docs/survey/CA依存順序と因果集合の一次文献比較.md` を追加した。有限な
$(E_\tau,\preceq_\tau)$ は因果集合の数学的定義を満たし、$D_\tau$ は被覆関係として復元できる。
ただし比較先の多様体、埋め込み、密度、尺度、誤差または収束概念は未定義なので、連続時空近似や
物理的因果との同一視は主張しない。時刻写像が順序から復元できない既証明の反例は、因果集合の定義が
外部ラベルの復元を要求しないため矛盾ではない。次 tick はこの比較をレビューし、調査成果として
確定するか、数学的主張だけを構造化記述へ追加するかを判断する。

## 自動ループ tick: 因果集合の一次文献との照合の構造化記述（2026-08-15）

前 tick の一次文献比較（survey）を既証明の部分順序性・区間有限性・被覆関係の等号・時刻非保存の
反例と突き合わせ、修正を要する不一致は見つからなかった（一次文献の本文はこの tick で再取得して
おらず、定義の引用は前 tick の確認記録に依る）。比較のうち数学的主張になる部分だけを
`structured-latex/content/causal-set-primary-literature.ts` へ移した。局所有限な部分順序集合を
順序の言葉だけで定義し（有限集合上では区間有限性が自動で従うことを定義内に明記）、claim
「$(E_\tau,\preceq_\tau)$ は局所有限な部分順序集合で、その区間は前章の $I_\tau(a,b)$ と等しい」を
既証明の claim の合成として証明した。一次文献の出典と、主張しないこと（連続時空近似・物理的因果・
光円錐・時刻写像の復元）は remark に限定した。全ブロック habitat "finite"、$\mathbb{R}/\mathbb{C}$
脱出なし。構造化テキスト check・PDF（16 ページ・未解決参照 0）、対応検査、Lean build、sorry 検査を
通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る。**

## 自動ループ tick: 因果集合の一次文献との照合の SageMath 検算（2026-08-15）

前 tick の構造化記述を、一次文献比較の調査記録および既証明の部分順序性・区間有限性と突き合わせ、
修正を要する不一致は見つからなかった。`sagemath/check/causal-set-primary-literature/` に、
反射的到達可能関係の部分順序性、一次文献の区間の有限性、一次文献の区間と前章の区間の集合としての
一致を分けた 3 本の検算を追加した。$|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係で
部分順序性を検査し、9,521 区間の有限性と二つの定義による区間の一致を全て PASS。一般の有限舞台に
対する証明ではなく、有限範囲の裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。
**この章の現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ
対象・仮定・順序の Lean 具体版を作る。**

## 論文アーティファクトと通知の一本化（2026-08-15）

`scripts/publish-artifact.sh` が構造化証明から自己完結 HTML を生成し、
`hexagonal-computation/artifacts` の Firebase Hosting にある
`math/cellular-automata-statistical-mechanics` 配下へ公開する。
公開 URL が HTTP 200 を返した場合だけ、直近の台帳記録と URL を Slack へ一度通知する。
Slack の完了通知には公開アーティファクト URL を必ず含め、URL のない通知は送らない。
通知本文は最新の研究コミット件名から作る一文だけとし、目的・経緯・検証の列挙を含めない。
同じプロジェクト版は外部マーカーで再通知せず、別プロジェクトだけが進んだ場合も再公開しない。
tick 内のエージェントは Slack を送らず、正常終了かつ clean のときに外側が公開・通知を担う。
公開物は一時閲覧用であり、リポジトリ内から恒久リンクしない。
シェル変数の直後に日本語を置くログ文字列は、変数名の境界を `${name}` で明示する。

## 完了済み（2026-08-02, プロジェクト新設）

- プロジェクト新設。ゴール設定を [README.md](README.md) に記述。
- **既存研究の網羅調査**（[docs/survey/](docs/survey/)）:
  CA と統計力学／場の量子論を結ぶ **互いにほとんど交流のない独立した経路**として整理。
  対応辞書（20 行）と、可算性から見た地形の表を作成。文献表は既読／未読を区別して記載。
- **アイディアの種**（[docs/ideas/](docs/ideas/)）: （一覧は docs/ideas/ を参照）。
  上位 3 つ（種「Φ_N の台と代数的複雑度」・種「線形CAとMahler測度」・種「Λ値不変量の三つの顔」）は個別ファイルで深掘り。

## 方針の組み直し（2026-08-02, docs/context 確立後）

`docs/context/` の三レイヤーが固まったので、プロジェクトの立て方を組み直した。

- **問いを反転した。** 「CA と物理の対応パターンを探す」ではなく
  **「連続物理が $\mathbb{R}$ の対象に帰している内容のうち、どこまでを CA の可算構造が担えるか」**。
  既存文献は全て「CA が連続物理を再現する」向きで書かれているが、**矢印を逆に読む**。
- **なぜ CA かを明確にした。** CA は**定義に $\mathbb{R}$ を一切含まない**唯一の物理模型クラス
  （Boltzmann 重みが $\{0,1\}$、理論全体が有限の記号から生成される）。
- **中心文書を新設した**: [`docs/survey/連続物理の可算な担い手.md`](docs/survey/連続物理の可算な担い手.md)。
  概念ごとに「CA 側の担い手／決定可能か／$\mathbb{R}$ が足すもの」を台帳にし、
  各経路を矢印反転で読み直した。**担い手が見つかっていないもの**も正直に列挙した。
- **成果物を定義し直した**: 可算コアの台帳／$\Lambda$ 値熱力学の構成／決定可能な述語の表／
  失われるものの同定。

台帳から読めた要点:
1. 分配関数からフェルミオンまでは $\mathbb{R}$ をほとんど必要としていない。
   $\mathbb{R}$ が足すのは「連続に動かせる・微分できる」という**計算の便宜**であって内容ではない。
2. $\mathbb{R}$ が本当に効くのは**極限を含む二項目**（自由エネルギー密度・臨界指数）だけ。
3. **箱玉系が最強の実例** — $q\to0$ で $\mathbb{C}$ の装置を全部落としても可積分構造が丸ごと残る。
   既存研究そのものが「$\mathbb{C}$ は内容を運んでいなかった」ことの証明になっている。
4. **GNVW 指数が雛形** — $\mathbb{R}$ 値の中心電荷に、決定可能な $\Lambda$ 値の格子祖先が実在する。

## 探索順序の組み直し（2026-08-02, 二相化）

**物理側の代数構造を起点にすると確証バイアスで探索が狭くなる**という指摘を受け、
探索を二相に分けた。第一相は CA 側から出発して代数構造をカタログ化する（物理を見ない）。
第二相でそれを物理と突き合わせる。枠組みは
[`docs/survey/CAから出発する代数構造の探索.md`](docs/survey/CAから出発する代数構造の探索.md)。

**バイアスは実際に効いていた。** 最初の調査は経路をすべて物理側の終着点で定義していたため、
純粋に数学側の構造を丸ごと落としていた。CA 起点で探すと即座に 4 つ出た。

- **次元群・Bowen–Franks 群** — SFT の共役・流れ同値の不変量。
  次元群は $\varinjlim(\mathbb{Z}^n\xrightarrow{A}\mathbb{Z}^n)$ で**順序アーベル群**、
  $\Lambda$ と同じ形の対象。Franks により既約 SFT の完全不変量系が知られている。
- **オートマトン群（自己相似群）** — 2 元アルファベットのオートマトンが生成する無限群。
  Grigorchuk 群、Nekrashevych の反復モノドロミー群。**物理に対応物がない。**
- **Perron 数（Lind 1984）** — 混合的 SFT のエントロピー全体＝Perron 数の対数全体。
  **「どの実数がエントロピーになりうるか」への完全な代数的回答**が既に存在する。
- **Artin–Mazur ゼータ関数** — $Z_N$ の母関数。有理性／超越性の二分。

代数構造が現れる機構を 6 型（係数環を載せる／極限を取る／自己相似で畳む／不変量を取る／
母関数化／退化させる）に分類し、これを軸に総当たりする方針にした。
歯止めは 4 条件（不変性・決定可能性・識別力・非自明性）。

## 舞台を有限から集める方針（2026-08-02）

**有限の代数構造を集め、その極限として無限を作る**順で舞台のパターン出しをする方針に決めた。
→ [`docs/舞台のカタログ_有限構造とその極限.md`](docs/舞台のカタログ_有限構造とその極限.md)

整理して出た要点:

- **極限には三つの向きがあり、可算性の帰結が違う。**
  順極限（直和・有限台、**可算**）／逆極限・完備化（直積、**非可算**）／局所的極限。
- **中心原理**: 同じ有限の族から順極限と逆極限の両方が作れる。
  **どちらを選ぶかが可算／非可算の分岐そのもの。**
  $\Lambda=\bigoplus_p\mathbb{Z}\ell_p$ を直和で定義しているのは、順極限側を選ぶという宣言である。
  $\mathbb{R}$ は $\mathbb{Q}$ の完備化＝逆極限側なので、
  **「$\mathbb{R}$ 脱出」と「逆極限・完備化を取る」は同じ機構の別の顔**である。
- **CA にとって正しい極限は局所的極限。** 局所規則は半径 $r$ しか見ないので
  $C_L\to\mathbb{Z}$、$(\mathbb{Z}/L)^d\to\mathbb{Z}^d$ が自然。
  群の逆極限 $\varprojlim\mathbb{Z}/L=\hat{\mathbb{Z}}$（非可算）ではない。
  2 次元 Ising を周期境界で解いて無限格子へ飛ばす操作はこの局所的極限である。
- **非可算化の点を精密化した。** 「舞台を無限にすると非可算」は粗い。正しくは
  局所的極限では舞台は可算のままで、**その上の全配位 $A^{G_\infty}=\varprojlim_F A^F$ を
  取ったときに初めて非可算**になる。有限台配位・周期配位に制限すれば可算のまま。
- **従順性は極限で初めて現れる**（有限群はすべて従順）。
  「有限では見えず極限で出る性質」という枠を作り、位相的エントロピー・Garden of Eden・
  Curtis–Hedlund–Lyndon の逆向きもそこに入れた。
- **オートマトン群は三つの極限のどれでもない第四の機構**（有限オートマトンが生成する無限群）。

## 局所的極限の定式化が決着（2026-08-02）

→ [`docs/局所的極限の定式化.md`](docs/局所的極限の定式化.md)

- **群**: 標識群の空間（Cayley / Chabauty–Grigorchuk 位相）での収束を主定義とする。
  「長さ $\le r$ の関係式集合が最終的に一致する」＝「Cayley グラフの半径 $r$ の球が同型」。
  **$\mathbb{R}$ を使わない。** $\forall r\,\exists n_0\,\forall n$ の $\Pi^0_2$ 文で、
  各段の判定は有限集合の一致。
- **$\mathbb{Z}/L\to\mathbb{Z}$ に一行の証明が付いた**（語の指数和 $|\sigma(w)|\le r<L$ より
  $L\mid\sigma(w)\iff\sigma(w)=0$）。自前で書いたのでこれは確定。
- **どの無限群が有限群の局所的極限か ＝ LEF 群。**
  有限表示に限れば剰余有限（Vershik–Gordon）。$\mathbb{Z}$・$\mathbb{Z}^d$・自由群・曲面群は該当。
- **CA の理論と噛み合う**: 従順 → Garden of Eden、sofic → surjunctive（Gromov–Weiss）。
  LEF ⊂ sofic なので、**有限から極限で得られる舞台のクラスと、CA の近似定理が効くクラスが重なる。**
- **グラフ（非推移的）には Benjamini–Schramm 収束**を使う。極限は unimodular random rooted graph。
  ランダム $d$-正則グラフ → 無限 $d$-正則木（非従順）。
  ただし確率測度の弱収束なので**極限で $\mathbb{R}$ が入る**（有限段階は $\mathbb{Q}$）。脱出点として記録。
- **pointed Gromov–Hausdorff は採らない。** 局所有限グラフでは局所同型と同値なのに
  $\mathbb{R}$ 値距離と $\varepsilon$ を持ち込むため。
  **同値な定式化が複数あるなら $\mathbb{R}$ を導入しない方を選ぶ**、という方針からの選択。
- **副産物**: Benjamini–Schramm 収束の下で自由エネルギーが連続であることが既に示されている
  （Abért–Csikvári–Frenkel–Kun、monomer–dimer）。
  **局所的極限は統計力学の熱力学極限を厳密に扱う既存の枠組みそのもの**であり、
  「$C_L\to\mathbb{Z}$ が熱力学極限の正体」という見方が数学側の裏付けを持つ。

## 有限→無限の機構は群に固有ではない（2026-08-04, 純粋数学レイヤー）

→ [`docs/有限代数系から無限代数系が現れる機構.md`](docs/有限代数系から無限代数系が現れる機構.md)

「有限を拡大すると別の代数系が現れるパターンは群だけか、有限体・環・多元環にはないのか」
という問いを、物理を一切見ずに純粋数学の側だけで調べた。

- **機構は五つある**（従来の三つ＋二つ）。
  順極限 / 逆極限・完備化 / 局所的極限 に加えて、
  **Fraïssé 極限**（有限対象のクラスの融合性から一意な可算対象を作る。**可算側に留まる**）と
  **超積・擬有限**（超フィルタ。選択公理を要するので**採らない**）。
- **群に固有ではない。むしろ完全な分類定理は群以外で得られている。**
  - 多元環: **Elliott** — ultramatricial 代数は次元群 $(K_0,K_0^+,[1])$ で完全分類。
    **純代数的で、$C^*$ 完備化を経ずに可算側で成立している。**
  - 多元環: **Bezushchak–Oliynyk** — **可算次元**の単位的局所行列代数は Steinitz 数で一意。
    非可算次元では一意でない。**可算性の仮定が分類の効き目を決めている。**
  - リー環: **Baranov** — finitary 単純リー環は $\mathfrak{sl}(\infty),\mathfrak{o}(\infty),\mathfrak{sp}(\infty)$ で尽きる。
  - 体: $\varinjlim\mathbb{F}_{p^n}=\overline{\mathbb{F}_p}$（**可算な代数閉体**）／
    Galois 側は $\hat{\mathbb{Z}}$（非可算）。**同じ図式の順極限側が可算、逆極限側が非可算。**
- **$\Lambda$ の位置づけが一段はっきりした（重要）。**
  $\overline{\mathbb{F}_p}$ の部分体は**超自然数（Steinitz 数）** $\prod_q q^{a_q}$, $a_q\in\mathbb{N}\cup\{\infty\}$ と 1 対 1。
  その**有限部分のモノイド $\mathbb{N}_{>0}$ の群化が $\mathbb{Q}_{>0}\cong\Lambda$** である。
  同じ Steinitz 数が局所行列代数と profinite 群の位数も分類している。
  **$\Lambda$ はこのリポジトリの発明ではなく、複数分野に独立に現れる標準対象の順極限側の顔。**
- **$\mathbb{R}$ の位置づけも順序論の標準的事実として書ける。**
  $\mathbb{Q}$ ＝有限線形順序の Fraïssé 極限、$\mathbb{R}$ ＝その Dedekind–MacNeille 完備化。
- **圏論で統一される**: $\mathrm{Ind}$（有向余極限を自由添加）と $\mathrm{Pro}$（有向極限）。
  $\mathrm{Pro}(\textbf{FinSet})\simeq\textbf{Stone}\simeq\textbf{BoolAlg}^{\mathrm{op}}$。
  **「順極限は可算・逆極限は非可算」は代数系の種類に依存しない圏論的な一般命題。**
- **Stone 双対が「非可算な空間 ↔ 可算な代数」の橋になる。**
  $\{0,1\}^{\mathbb{N}}$（非可算）↔ 可算な原子なしブール代数。
  全配位空間を扱う必要が出たときの逃げ道として記録した。
- **LEF は群専用ではない。** Gordon–Vershik は最初から群と有限次元代数の両方に定義しており、
  半群・環へも拡張されている（$F[G]$ が LEF $\iff$ $G$ が LEF）。
  **既に採用した局所的極限の枠組みを、状態集合側・規則環側へそのまま持ち上げてよい。**
- **半群では逆極限側が「記法」として使われている**（Reiterman: 擬多様体＝profinite 等式で定義）。
  有限の世界を語るために逆極限側の元を言語として持ち出す用法。

### 具体例を三つ入れた（同日追記）

抽象論だけでは噛み合わないので、手で追える例を文書の先頭に置いた。

- **行列環の塔 → Clifford 代数**。$\mathrm{Cl}_{2n}(\mathbb{C})\cong M_{2^n}(\mathbb{C})$ で、
  生成元を 2 個増やす操作＝$M_2$ をもう一つテンソルする操作。
  $M_2\subset M_4\subset M_8\subset\cdots$ の**順極限が無限 Clifford 代数（可算次元）**、
  **$C^*$ 完備化が UHF $M_{2^\infty}$＝CAR 代数（非可算）**。同じ塔のどこで止めるかで濃度が決まる。
  Steinitz 数 $2^\infty$、次元群 $K_0=\mathbb{Z}[1/2]$。
  $M_3$ の塔なら $\mathbb{Z}[1/3]$、全部混ぜれば $\mathbb{Q}$。
  **完全不変量が $\mathbb{Q}$ の部分群として出る**＝$\Lambda$ と同種の対象。
  → 検算 [`sagemath/check/clifford_matrix_tower/`](sagemath/check/clifford_matrix_tower/)（$n=1,2,3$ で確認、$\mathbb{R}$ 脱出なし）
- **繋ぎ方を変えると順序に代数的数が出る**。包含の重複度を $\binom{1\ 1}{1\ 0}$ にすると
  $K_0\cong\mathbb{Z}^2$ のままだが正錐が $a+b\varphi>0$（$\varphi$＝黄金比）で決まる。
  **$\mathbb{R}$ に埋まって見えるだけで、必要なのは $\overline{\mathbb{Q}}$ まで**（符号判定は決定可能）。
  SFT の次元群一般に同じことが起きる（正錐＝Perron 固有ベクトル、成分は代数的数）。
  **Lind の Perron 数定理はこの事実の別の顔。**
- **有限体の塔**。$\mathbb{F}_2\subset\mathbb{F}_4\subset\mathbb{F}_{16}\subset\cdots$、
  $\varinjlim\mathbb{F}_{2^{n!}}=\overline{\mathbb{F}_2}$（**可算な代数閉体**）、Galois 側は $\hat{\mathbb{Z}}$（非可算）。

**無限 Clifford 代数と $\overline{\mathbb{F}_2}$ という無関係な代数系が、同じ Steinitz 数の枠で分類される。**
これが「機構は代数系の種類に依存しない」ことの最も具体的な現れ。

### 軸を取り違えていた（同日、指摘を受けて修正）

上の Clifford の例は**行列サイズを大きくする軸**の話だが、
質問は**係数体を大きくする軸**（$\mathbb{F}_q$ 係数）だった。**二つは独立な軸である。**

| 軸 | 何を大きくするか | CA では | 支配する理論 |
|---|---|---|---|
| **大きさ** | 行列サイズ・格子サイズ・群の位数 | **舞台 $G$** | 五つの極限機構（順極限〜超積） |
| **係数** | $\mathbb{F}_2\to\overline{\mathbb{F}_2}$、$\mathbb{Q}\to\mathbb{C}$ | **状態集合 $A$ と重み** | **Brauer 群・Galois コホモロジー** |

**これまでのカタログはすべて大きさの軸だった。** 係数の軸を調べた結果:

- **係数体を大きくしても新しい代数系は出ない。むしろ潰れる。**
  $M_n(k)\otimes_k K=M_n(K)$。差が出るのは行列環でない代数だけで、それを測るのが Brauer 群。
- $\mathrm{Br}(\mathbb{F}_q)=0$（Wedderburn の小定理）、$\mathrm{Br}(\overline{k})=0$。
  **有限体上でも代数閉体上でも、中心単純代数はすべて行列環に潰れる。**
- **Clifford 代数はまさにこの話。** $\mathbb{C}$ 上は常に $M_{2^n}$、$\mathbb{R}$ 上は $\mathbb{H}$ 等が出て 8 周期、
  **$\mathbb{F}_q$ 上は潰れる。**
  よって「有限体係数の行列環から Clifford 代数が出る」は**逆向きにしか成り立たない。**
- **$\mathbb{R}$ がこの軸で足しているものは $\mathrm{Br}(\mathbb{R})=\mathbb{Z}/2$、つまり 1 ビット。**
  $\mathbb{R}$ の非可算性は Brauer 群に何も寄与しない。
  $\mathrm{Br}(\mathbb{Q}_p)=\mathbb{Q}/\mathbb{Z}$、$\mathrm{Br}(\mathbb{Q})$ も可算。
  **係数の軸には $\mathbb{R}$ 脱出が存在しない。脱出はすべて大きさの軸で起きる。**
- **2 値 CA では係数の出発点が $\mathbb{F}_2$ に固定されており $\mathrm{Br}(\mathbb{F}_2)=0$ なので、
  この軸には最初から何もない。** 意味があるのは $\mathbb{F}_2\to\overline{\mathbb{F}_2}$ で
  **規則多項式が分解する**こと（$\Lambda$／Mahler 測度の入口）だけ。
  → 種「線形CAとMahler測度」がこの軸の唯一の入口である。

## 事例集を二本立てた（2026-08-08）

機構の分類（五つの機構）は揃っていたが、**具体的な塔の事例が 3 つしかなかった**ので事例を集めた。
その過程で、**二つの軸を別文書に分ける**必要があることが分かったので二本立てにした。

### 舞台を大きくする軸 → [`docs/有限の舞台を無限へ飛ばす対応の事例集.md`](docs/有限の舞台を無限へ飛ばす対応の事例集.md)

周期境界の有限トーラスから整数格子へ飛ばす対応（2 次元 Ising の熱力学極限がこれ）。
**この対応が主張していることを四つの層に分けた**のが最大の成果。

1. **舞台**（$(\mathbb{Z}/L)^d\to\mathbb{Z}^d$）— $\mathbb{R}$ 不要。証明済み。
2. **局所規則**（$L>2r$ なら有限側と無限側で同一の有限データ）— **極限操作すら要らない。**
   「同じものとみなせる」が最も強い意味で成り立つのはこの層。
3. **示量量の密度**（自由エネルギー密度）— 収束の根拠は表面／体積 $\to0$（従順性・van Hove／Følner）。
   境界条件に依らないのも同じ理由。**$\mathbb{R}$ 脱出はここだけ。**
4. **状態（Gibbs 測度）** — 何の一意性かを確定した。**無限体積 Gibbs 測度（DLR 方程式の解）の一意性**
   であり、非一意性とは「極限が存在しない」ことではなく
   **「境界条件ごとの列はどれも収束するが行き先が違う」**ことである。

**層 4 について、定義を置かずに結論を書いて二度訂正した。指摘を受けて書き方を変えた（2026-08-09）。**

反省点: 定義を確定しないまま「層 4 の破れ＝相転移」等の結論めいた文を文書へ書き、
二度訂正する結果になった。**定義が定まっていない段階で結論を文書へ書かないこと。**

対応として [`docs/Gibbs測度と相転移の定義.md`](docs/Gibbs測度と相転移の定義.md) を新設し、
**定義と定理だけ**を（配位空間・$\sigma$ 加法族・相互作用・有限体積 Gibbs 分布・仕様・DLR 方程式・
純粋相・相転移の二つの定義、および $\mathbb{R}$ 脱出の一覧まで）書いた。
事例集の層 4 の節からは結論を削り、この文書への参照に置き換えた。
**この文書の冒頭に書き方の規律を明記した**: このリポジトリは物理を論じる場ではないので、
物理学者になら通じる程度の抽象度の文章には価値がない。記号の帰属・演算の定義・
極限の位相・$\mathbb{R}$ を使った箇所が書かれていない主張は主張として成立しない。

**一次文献の調査を行い、さらに 4 つの誤りが見つかった（2026-08-09）。**
→ [`docs/相転移まわりの一次文献ノート.md`](docs/相転移まわりの一次文献ノート.md)

Duminil-Copin の講義録（arXiv:1707.00520）の**本文**から定理文を取って照合した
（原論文の本文はまだ一つも読んでいない）。確認の水準を A／B／C の 3 段階で記録する規約にした。

- **「相転移の定義は 2 つ」は不足だった。** 講義録が明記しているとおり、連続転移の定義は
  相関長の発散・秩序変数の連続性・臨界点での Gibbs 状態の一意性・感受率の発散・
  スケール不変性など**少なくとも 5 つあり、数学的には同値でない**（同値にならない模型の例がある）。
  同値性は**平面ランダムクラスター模型**についての定理（Duminil-Copin–Sidoravicius–Tassion）である。
- **「任意次元で指数減衰」は誤り。** Duminil-Copin–Goswami–Raoufi が示したのは
  $d\ge3$・純粋相・$\beta>\beta_c$。高温側の出典は未特定のまま。
- **Lebowitz–Martin-Löf の主張は「磁化の連続性」であって「自由エネルギーの微分可能性」ではない。**
  後者への言い換えは凸性を使う別の一歩である。
- **臨界点での Gibbs 測度の一意性は単一の定理ではなく 3 つの結果の合成**であり、
  繋ぎ目（スピン反転対称性による磁化の片側極限）を確認していない。

収穫: 臨界点で指数減衰しないことは、$d\ge3$ で**定量的な上下界**として得られている
（自由境界条件で $c\|x\|^{-(d-1)}\le\langle\sigma_0\sigma_x\rangle\le C\|x\|^{-(d-2)}$）。
また、**測度の非一意性が起きる点の集合が高々可算**であることは、ランダムクラスター模型では
直接の定理として存在する（凸性から導いた自分の議論より強い形）。

以下は、その定義文書の上で成り立つ関係の要約である。

- 誤り 1「層 4 の破れ＝相転移」。正しくは**破れ（測度の非一意性）は相共存と同値**であり、
  Lebowitz–Martin-Löf により**自由エネルギーの磁場についての微分可能性と同値**。
  橋渡しは具体的な等式で、**微分の左右差＝二つの純粋相の磁化の差（自発磁化の 2 倍）**。
  相そのものは $\mathcal{G}$ の**端点**であり、端点でない測度は純粋相の混合にすぎない。
- 誤り 2「臨界点は四層のどこにも現れない」。**現れないのは測度の個数の側だけ**で、
  **測度の性質の側には現れる**。臨界点でも測度は一意だが、
  **Ising の切断相関は臨界温度を除くすべての温度で指数減衰する**（Duminil-Copin ら）ので、
  **臨界点＝一意な測度の相関が指数減衰しない唯一の温度**として特徴づけられる。
- **この立場にとっては良い知らせ。** 「指数減衰するかしないか」は**二択であって数値ではない**。
  臨界**指数**（実数値）と臨界**点の存在**（二択）は別物であり、後者だけなら可算側で書ける見込みがある。
  種を「臨界点は相関の減衰の二択として現れる」へ改名した。
- 副産物: 自由エネルギーは $h$ について凸なので**一次転移の集合は高々可算**。
- 副産物: 無限体積 Gibbs 測度は**円筒事象（可算個）の上の値で決まる**。
  添字は可算で、非可算なのは値の側である。

事例は 13 件（らせん境界 $\mathrm{Cay}(\mathbb{Z}/L^2,\{1,L\})\to\mathbb{Z}^2$ は自分で一行証明を付けた）。
重要な二つの否定例:
**$T_d$ の半径 $n$ の球は $T_d$ に収束しない**（canopy tree になる。従順性が効く理由の反例）、
**$K_n$・ハイパーキューブは次数発散で枠外**（平均場模型はこの枠組みの外にある）。

模型ごと運ぶ層は既に定理がある: Dembo–Montanari（局所的に木状なグラフ上の Ising で
**Boltzmann 分布そのものが極限へ局所収束し自由エネルギーが Bethe 値へ**）、
Abért ら（monomer–dimer）。

**複雑ネットワークを新規に取り込んだ。** 成長規則（有限データ）を無限回反復して大域指標を計算する模型群。
Erdős–Rényi → Poisson 分岐木、優先的選択 → Pólya point tree など。
**指標が有理数・整数で出るものが多い**（巨大成分の閾値 $c=1$、次数指数 $3$、閾値 $1/(d-1)$）。
そして**グラフの極限理論が疎（Benjamini–Schramm＝局所的極限側）と
密（グラフォン＝カット距離での完備化側）に割れている**ことが分かった。
これは中心原理（順極限側か逆極限側かが可算／非可算の分岐）のグラフにおける顔である。
**格子模型が疎グラフであることが、局所的極限を使える理由そのもの。**

### 代数系そのものを大きくする軸 → [`docs/有限構造の塔の事例集.md`](docs/有限構造の塔の事例集.md)

$M_2\subset M_4\subset\cdots$ 型の塔の事例を 13 件。横断的に見えた一般則が三つ:

- **実数に見える量の正体は Perron 数で、$\overline{\mathbb{Q}}$ で足りている。**
  SFT の次元群の正錐（黄金比）、融合圏の Frobenius–Perron 次元、Jones 指数 $4\cos^2(\pi/n)$ が
  すべて非負整数行列の Perron 根である。決定可能。
- **$\mathbb{R}$ が入るのは対象ではなく、その上の状態を全部集めたとき。**
  行列環の塔は状態空間が一点（トレース一意）なので $\mathbb{R}$ が入らないが、
  対称群の塔 $\mathbb{C}[S_n]$ は状態空間が Thoma 単体（実数パラメータ）になる。
  **自由エネルギー・エントロピーが実数値なのは状態側の量だからだと読める。**
- **綺麗に分類されるのは結合的・半単純な塔だけ**（Cayley–Dickson の塔は分類が付かない）。

最良の新事例は**有限射影幾何の塔 → von Neumann の連続幾何**。
次元関数が整数 → 二進有理数（順極限）→ $[0,1]$（完備化）と動き、
**連続体そのものが完備化の産物として現れる。** 行列環の塔（$K_0=\mathbb{Z}[1/2]$）の束論側の双子。

### 併せて直したこと

- 「複素 Clifford 代数 $\cong$ 行列環」の**向き**の注記を追加した。逆は成り立たない
  （$M_3$ は次元 9 なのでどの Clifford 代数とも同型でない）。
  Clifford 代数は行列環の塔のうち「2 の冪」という一点にすぎない。

**すべて二次情報（検索・要旨）に基づく。原論文で確認したものは一つもない。**

## 2 次元以上の CA と物理法則の対応を調べた（2026-08-10）

→ [`docs/survey/2次元以上のCAと物理法則の対応.md`](docs/survey/2次元以上のCAと物理法則の対応.md)

「ライフゲームに熱力学極限はあるか」という問いから出発した調査。

- **ライフゲームには熱力学極限の理論がなく、構造的な理由が 4 つある。**
  決定論的で詳細釣り合いを持たない（＝零温度）／加法的保存量がない／Turing 完全で
  漸近的性質が決定不能／得られている量（漸近密度 $\approx0.0287$）は経験則で定理ではない。
  自己組織化臨界の論争（Bak–Chen–Creutz 対 Bennett–Bourzutschky）は**決着していない**。
  数値実験の解釈の争いであり、可算／非可算の分別に効かないので**追わない**と判断した。
- **既存の経路別調査に欠落があった。** 既存調査は CA と**代数構造**の対応を軸にしていたため、
  CA と**連続方程式**の対応を軸にする経路が丸ごと漏れていた。別立てで補った。
- 新しく拾った経路: 確率的 CA の時空履歴が $(d+1)$ 次元の Gibbs 状態に対応する
  （Lebowitz–Maes–Speer 1990。**CA の統計力学の中核。要精読に追加**）／
  Toom 規則（2 次元で相転移が厳密に示される非平衡模型。eroder という有限条件で判定）／
  格子気体 CA → Navier–Stokes ／ 量子 CA → Weyl 方程式。
- **最大の収穫は格子気体の等方性条件。** 正方格子の HPP が失敗し三角格子の FHP が成功する理由は
  純粋に代数的で、「2 次元で $60^\circ$ 回転不変な 4 階テンソルは必ず等方的だが、
  $90^\circ$ 回転不変なものには非等方なものがある」に尽きる。
  **「連続極限で正しい物理が出るか」が格子の対称群についての有限の判定に帰着している。**
  → 種「連続極限の正しさは格子の対称群で決まる」として登録。検算は有限群の指標計算で済む。
- **Q2R（Vichniac）を見つけた。** 2 次元の可逆・保存的 CA で Ising のエネルギーを厳密に保存する。
  種「$\Lambda$ 値熱力学の構成」の実装対象として、下記「次にやること」の初等 CA より筋がよい。

**すべて水準 B 以下（要旨のみ、または二次情報）。原論文の本文は一つも読んでいない。**

## 次にやること（優先順）

1. **舞台のカタログを埋める（最優先）** — 定式化は決着したので、有限構造の族ごとに
   「局所的極限／順極限／逆極限」「どの収束を使うか」「$\mathbb{R}$ が入るか」
   「その性質が有限側に存在するか極限で初めて出るか」を表にする。
   LEF・sofic の原論文（Vershik–Gordon、Gromov、Weiss）の精読が要る。

2. **第一相の総当たり** — 初等 CA 256 個について、CA 側の不変量を全部厳密計算し
   規則ごとの表にする: 加法的保存量の $\mathbb{Z}$-格子、$Z_N$ とゼータ関数、
   次元群と Bowen–Franks 群、線形性・可逆性。
   **同時に識別力（どの不変量がどれだけ規則を分けるか）を測る。**
   → `sagemath/check/rule_invariants/`

3. **種「Λ値熱力学の構成」の検算** — 可逆な初等 CA を一つ選び、
   加法的保存量を線型代数で全部求め、$\Omega(U)\in\mathbb{N}$ を数え上げ、
   $S(U),\beta(U)\in\Lambda$ を素因数分解で書き、第〇法則が整数比較で確認できるか見る。
   **$\mathbb{R}$ 脱出ゼロで熱力学が完結すること**の実証。→ `sagemath/check/lambda_thermodynamics/`

4. **種「Φ_N の台と代数的複雑度」の検算** — `sagemath/check/phi_support/` を作る。
   初等 CA 256 個 × $L=3..14$ × $N=1..24$ で $Z_N=\#\{x:F^N(x)=x\}$ を計算し、
   $\Phi_N=\log Z_N\in\Lambda$ の台を表にする。
   確認: 加法的規則で台 $\subseteq\{2\}$（命題「線形 CA では台が 1 点に潰れる」）、可逆規則で $M=0$（命題「可逆 CA では傾きが消える」）、
   台が有界な非加法的規則の有無（仮説 種「Φ_N の台と代数的複雑度」の反例探索）。
   → [docs/ideas/種_Φ_Nの台と代数的複雑度.md](docs/ideas/種_Φ_Nの台と代数的複雑度.md) 「最初の検算（`sagemath/check/phi_support/` に置く）」節
5. **一次文献の精読（重複潰し）** — [docs/survey/文献表.md](docs/survey/文献表.md) の
   「要精読」3 本を先に：
   - D'Amico–Manzini–Margara（線形 CA のエントロピー公式の**正確な形**。種「線形CAとMahler測度」の鍵）
   - GNVW（指数の定義と値域、エントロピー輸送との関係。種「Λ値不変量の三つの顔」の最大の重複リスク）
   - Rujàn 1987（「転送行列と確率的CA」の経路で既に何が対応づけられているか）
   - **Lind 1984（Perron 数）と Lind–Marcus の教科書（次元群）— 第一相の中核。追加。**
6. **種「二つの壁は独立」を `docs/discussion/対数順序群上の統計力学/README.md` に反映** — 「計算不能性の壁」の欄を脱出台帳に追加する。
   これは検算不要（既知結果の整理）で、リポジトリの中心テーゼの精密化になる。**早く反映すべき。**
7. **種「線形CAとMahler測度」の検算** — `sagemath/check/ledrappier_padic/`。
   Ledrappier three-dot（$1+x+y$ over $\mathbb{F}_2$）で
   アルキメデス Mahler 測度・$p$ 進 $\mu_2$・CA 側エントロピーの三者を突き合わせる。
   既存コード `integrable-lattice/sagemath/check/cycle6_T1_padic_mahler/` を流用。
8. **種「可解性は有限検査で決まる」の総当たり** — 初等 CA 256 個に対する線形性・可逆性・加法的保存量の有限検査。
   YB 写像性はブロック CA への型合わせが先。

## 未解決・確認事項

- **調査は書誌・要旨レベルに留まる。** 「空白である」は「見つからなかった」の意味で、
  存在しないことの証明ではない。種を命題へ昇格させる前に一次文献で重複を潰すこと。
- 種「Λ値不変量の三つの顔」の「2 値 QCA の指数 $\in2^{\mathbb{Z}}$」は要一次文献確認。
- 種「線形CAとMahler測度」の「線形 CA のエントロピーの係数が非負整数」は要一次文献確認。
- 種「線形CAとMahler測度」の $\mathbb{F}_p\to\mathbb{Z}$ の持ち上げの非一意性が、$p$ 進側の量にどう効くか未定式化。
- $Z_N=0$（不動点なし）の規則では $\Phi_N=\log0$ が未定義。定義域の扱いを決めること。
- **Fraïssé 極限をこのプロジェクトの道具として使えるかは未検討。**
  有限のものだけから一意な可算対象を作れる点は順極限より強いが、
  得られる対象（Rado グラフ等）が等質すぎて構造を区別しない恐れがある。
- **Elliott の分類定理の純代数版と $C^*$ 版の境目**を一次文献で確認すること。
- 「有限 Steinitz 数のモノイドの群化 $\cong\Lambda$」を命題として書けるが、
  それが有限体の塔と局所行列代数の分類の両方と整合することまで書かないと意味がない。

## 判断の記録（なぜこの構成にしたか）

- **探索フェーズでは Markdown で書く**（構造化テキストへは命題が固まってから移す）。
  理由: 種は反証されて消える前提のもので、ラベル・参照解決の対象にすると回転が落ちる。
- **経路という切り方**を採った理由: 文献が実際にこの単位で分かれており（互いに引用が少ない）、
  経路どうしが繋がっていないことが空白の所在を最も明確に示すため。
- **種の優先順位の基準を変えた**（2026-08-02）。以前は検算コストの安い順だったが、
  **理論物理レイヤーの中心命題をどれだけ直接に実行するかを第一基準**に置き直した。
  その結果 種「Λ値熱力学の構成」が最優先に上がった（$\mathbb{R}$ 脱出ゼロで熱力学が
  完結することの実証であり、しかも検算が安い）。
- **中心文書を「全体像の地図」から「連続物理の可算な担い手（台帳）」へ移した**。
  地図は既存研究の分かれ方を示すもので、我々の主張ではない。主張は台帳の側にある。
- **さらに中心を「CA から出発する代数構造の探索」へ移した**（二相化）。
  台帳も**物理の概念リストを起点にしている**点で同じバイアスを持つ。
  第一相（CA 起点のカタログ）を先に置き、台帳は第二相の突き合わせ表として使う。

## 自動ループ tick: 因果集合の一次文献との照合の Lean 具体版（2026-08-15）

前 tick の SageMath 3 本を再実行し（全て PASS）、構造化記述との対応をレビューして修正を要する
不一致は見つからなかった。`lean/CellularAutomata/CausalSetPrimaryLiterature.lean` に人手証明と
1 対 1 の具体版を書いた。一次文献の区間 $A_R(x,y)$ と局所有限な部分順序集合（前章の部分順序の
述語と全区間の有限性の連言）を順序の言葉だけで定義し、有限集合上では区間が常に有限であるという
定義中の注記、一次文献の区間と前章の順序区間の集合としての一致、$(E_\tau,\preceq_\tau)$ が局所有限な
部分順序集合であることを、既証明の部分順序性と区間有限性だけから人手証明と同じ順序で証明した。
連続時空への近似・物理的因果・時刻写像の復元は人手証明どおり主張せず形式化もしていない。
入口 import と sorry 検査の対象へ追加し、Lean build・sorry 検査、構造化テキスト check・PDF、
SageMath、対応検査を通した。**この章の現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と具体版の導出を作る**（注記の補題にセル型の有限性・等号判定が不要なことが
`omit` から見えている。区間・局所有限性は集合と関係だけで定義できる見込み）。

## 自動ループ tick: 因果集合の一次文献との照合の Lean 必要十分版（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、定義・主張・証明順序に修正を要する不一致は
見つからなかった。`lean/CellularAutomata/NecSuf/CausalSetPrimaryLiterature.lean` に、任意の集合と
関係だけで一次文献の区間と局所有限な部分順序集合を定義した。区間は台集合の部分集合なので、区間の
有限性に要るのは台集合の有限性だけであり、部分順序性は使わない。有限集合上の部分順序が局所有限に
なる定理を具体版と同じ順序で証明し、具体版の区間・局所有限性述語・区間有限性・イベント依存順序の
局所有限性を必要十分版の特殊化として導出した。セル、状態、近傍、局所規則、自然数時刻、物理的意味、
多様体、実数・複素数は必要十分版に現れない。**「因果集合の一次文献との照合」は四層と導出が揃い、
台帳上 done になった。**

## 自動ループ tick: 第二の探索列の開始と「軌道の最終周期性」の構造化記述（2026-08-15）

前 tick の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。第一の探索列
（本質的依存台から因果集合の一次文献との照合まで）が全て done になったので、第二の探索列
「大域写像の反復から内在的に生じる構造」を台帳に開いた（survey「時間（F の反復）を動かす」に
対応。物理的名称を入れず、有限集合上の写像の反復であることだけを使う）。最初の対象「軌道の最終周期性」の
構造化記述 `structured-latex/content/global-map-iteration.ts` に、反復 $F^n$・軌道の定義、鳩の巣による
衝突（$0\le i<j\le 2^{|V|}$）、衝突の反復不変性、最終周期性、衝突組の有限決定可能性、主張しないこと
（無限舞台・極限集合・母関数、最小前周期・周期の一意性は次層以降）を書いた。$\mathbb{R}/\mathbb{C}$ 脱出なし。
構造化テキスト check・PDF（18 ページ）・対応検査、Lean build・sorry 検査を通した。**現在地は構造化記述
まで。次 tick はレビュー後に SageMath 検算を作る。** 後続の対象として「最小前周期と最小周期」
「周期点の個数」「大域写像の可逆性の有限決定」を todo で台帳に置いた。

## 自動ループ tick: 軌道の最終周期性の SageMath 検算（2026-08-15）

前 tick の構造化記述をレビューし、鳩の巣による衝突上界、衝突後の帰納法、$p=j-i$ による
最終周期性、候補組数の計算に修正を要する不一致は見つからなかった。
`sagemath/check/global-map-eventual-periodicity/` に、軌道衝突、衝突の反復不変性、最終周期性、
衝突組の走査回数を分けた 4 本の検算を追加した。セル数 0 の唯一の配位と、セル数 1〜3 の
巡回舞台上の全 256 初等 CA 規則・全配位からなる 3,585 軌道で全て PASS。48,489 個の衝突と
403,370 個のシフト後等式を検査した。これは任意の非一様 CA の全数列挙でも一般の場合の証明でもない。
最終周期性の全ての指数に対する根拠は人手証明の帰納法である。浮動小数点と
$\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応を
レビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 軌道の最終周期性の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本を再実行し、構造化記述との対応をレビューして修正を要する不一致は
見つからなかった。`lean/CellularAutomata/GlobalMapIteration.lean` に、反復・軌道の定義、
配位集合の個数、鳩の巣による衝突、衝突の反復不変性、最終周期性、候補組の有限集合とその個数
（2 倍が $M(M+1)$）、候補組の中の衝突の存在、配位等号の座標別分解、候補組上の存在文の決定可能性を
人手証明と同じ順で形式化した。等号検査回数のコストモデルは形式化していない。`lake build`・sorry 検査
（新 8 定理）・構造化テキスト check・PDF・SageMath・対応検査を全て通した。**現在地は Lean 具体版まで。
次 tick は Lean 必要十分版（有限型上の自己写像だけで述べる見込み）と具体版の導出を作る。**

## 自動ループ tick: 軌道の最終周期性の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述と突き合わせ、反復の再帰、鳩の巣による衝突、衝突後の帰納法、
$p:=j-i$ による最終周期性、有限候補組の走査が同じ対象・仮定・順序で形式化されていることを確認した。
修正を要する不一致は見つからなかった。

`lean/CellularAutomata/NecSuf/GlobalMapIteration.lean` に必要十分版を追加した。反復・軌道・衝突の
反復不変性には型 $X$ と自己写像 $F:X\to X$ だけ、衝突と最終周期性には $X$ の有限性だけ、
衝突候補の有限決定可能性には $X$ の等号判定だけが要る。二値状態、セル、近傍、局所規則は現れない。
具体版の反復・軌道・衝突・衝突移送・最終周期性・候補組数・候補中の衝突存在を必要十分版から導く定理も追加した。
`lake build` と sorry 検査を通し、新たに登録した定理はすべて `sorryAx` に依存しない。
**「軌道の最終周期性」は四層と導出が揃い `done`。次 tick は必要十分版と導出のレビュー後、
「最小前周期と最小周期」の構造化記述を作る。**

## 自動ループ tick: 最小前周期と最小周期の構造化記述（2026-08-15）

前 tick の「軌道の最終周期性」の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。

`structured-latex/content/minimal-preperiod-period.ts` に「最小前周期と最小周期」の構造化記述を書いた。
前周期・周期の組の集合 $P(y)$ の所属が 1 回の配位の等号 $F^{i+p}y=F^{i}y$（と $p\ge1$）と同値であること、
$\mu(y)=\min I(y)$・$\pi(y)=\min Q(y)$ が $\mathbb{N}$ の整列性と反対称性で一意に定まること、
周期の倍数が周期であること、任意の周期 $p$ が $F^{\mu(y)+p}y=F^{\mu(y)}y$ を満たすこと、
$\mu(y)+\pi(y)\le 2^{|V|}$、有限範囲の走査で $\mu,\pi$ が決まることを、全て前章の衝突移送と
最終周期性から導いた。ℝ/ℂ なし。構造化テキスト check・PDF（19 ページ）・対応検査・Lean build・
sorry 検査を全て通した。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算
（`sagemath/check/` に P(y) 所属の同値、μ・π の走査値と定義値の一致、上界を分けて置く）を作る。**

## 自動ループ tick: 最小前周期と最小周期の SageMath 検算（2026-08-15）

前 tick の構造化記述を、前章の反復・衝突移送・最終周期性と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/minimal-preperiod-period/` に、周期組の所属と 1 回の衝突の同値、
任意の周期の最小前周期への移送、有限走査値と最初の再訪から得る値の一致、最小前周期と最小周期の
和の上界を分けた 4 本の検算を追加した。

配位数 1・2・4 の有限集合上の全自己写像・全初期値と、セル数 0〜3 の巡回舞台上の全 256 初等 CA
規則・全配位、合計 4,618 軌道で全て PASS。一般の有限舞台に対する証明ではなく、有限範囲の
厳密な裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。
次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 最小前周期と最小周期の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/MinimalPreperiodPeriod.lean` に、周期組の述語と衝突との同値、最小前周期・
最小周期（`Nat.find` による最小元。最小性と所属を別定理に）、周期の倍数、周期の最小前周期への
移送、$\mu(y)+\pi(y)\le 2^{|V|}$ を人手証明と同じ順序で形式化した。`lake build`、sorry 検査
（12 定理登録）、構造化テキスト check、対応検査を通した。

**有限範囲の走査による有限決定の claim はまだ形式化していない**（file 冒頭に明記）。
**現在地は Lean 具体版まで（有限決定を除く）。次 tick はレビュー後、有限決定の走査定理を具体版へ
足してから Lean 必要十分版と導出へ進む。**

## 自動ループ tick: 最小前周期と最小周期の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を人手証明と突き合わせ、修正を要する不一致は見つからなかった。具体版へ
有限候補組、最小組の候補範囲への所属、任意の成立候補に対する最小性、有限候補上の存在文の
決定可能性を追加した。必要十分版 `lean/CellularAutomata/NecSuf/MinimalPreperiodPeriod.lean` は、
型 $X$ 上の自己写像だけで周期組、二つの最小元、周期の倍数、最小前周期への移送、$|X|$ による上界、
有限走査を同じ順序で証明する。有限性が要るのは存在・最小元・上界、等号判定が要るのは有限走査の
判定だけである。二値状態、舞台、近傍、局所規則、$\mathbb{R}/\mathbb{C}$ は使わない。
具体版の周期組、周期の倍数、有限走査範囲が特殊化であることを導出した。
**「最小前周期と最小周期」は `done`。次 tick は必要十分版と導出をレビュー後、
「周期点の個数」の構造化記述を作る。**

## 自動ループ tick: 周期点の個数の構造化記述（2026-08-15）

前 tick の「最小前周期と最小周期」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。`structured-latex/content/periodic-point-count.ts` に、各 $n\ge1$ の反復の不動点集合
$\mathrm{Fix}_n(F)$ とその個数 $Z_n(F)\in\mathbb{N}$、周期点集合 $\mathrm{Per}(F)$ を定義し、
$y\in\mathrm{Per}(F)\iff\mu(y)=0$、$y\in\mathrm{Fix}_n(F)\iff(\mu(y)=0$ かつ $\pi(y)\mid n)$
（除法の原理と最小周期の最小性）、$Z_n(F)$ の最小周期ごとの個数への分解、$\mathrm{Fix}_n$ の
所属が配位の等号 1 回で決まり $Z_n$ が $2^{|V|}$ 回の等号検査で決まることを証明つきで書いた。
「分配関数」「トレース」の名前は使わず個数としてだけ定義した。$\mathbb{R}/\mathbb{C}$ なし。
**現在地は構造化記述まで。SageMath・Lean は未着手。** 次 tick は本章をレビューし SageMath 検算を作る。

## 自動ループ tick: 周期点の個数の SageMath 検算（2026-08-15）

前 tick の構造化記述を、反復・最小前周期・最小周期の既証明と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/periodic-point-count/` に、周期点と最小前周期 0 の同値、
反復の不動点と最小周期の整除条件、最小周期ごとの個数分解、全配位走査による有限決定を分けた
4 本の検算を追加した。配位数 1・2・4 の全自己写像と、セル数 1〜3 の巡回舞台上の全 256 初等 CA
規則で全て PASS。整除条件は 4,618 軌道・51,236 指数、個数分解は 1,029 写像・9,234 指数、
有限決定は 51,234 回の等号検査で一致した。一般の場合の証明ではなく、有限範囲の厳密な裏取りである。
浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**現在地は SageMath まで。次 tick は検算と
構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 周期点の個数の Lean 具体版（2026-08-15）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/PeriodicPointCount.lean` に、$\mathrm{Fix}_n(F)$（全配位の filter）と
$Z_n(F)$（その個数）、周期点、周期点 ⟺ $\mu(y)=0$、$y\in\mathrm{Fix}_n\iff(\mu(y)=0$ かつ
$\pi(y)\mid n)$（除法の原理と最小周期の最小性）、$Z_n$ の最小周期ごとの個数分解（fiber 分け）、
所属の決定可能性と全配位走査を人手証明と同じ順序で形式化した。`lake build`、sorry 検査
（9 定理登録）、構造化テキスト check、SageMath 4 本、対応検査を通した。
**現在地は Lean 具体版まで。次 tick は具体版をレビュー後、Lean 必要十分版（型 $X$ 上の自己写像と
有限性だけ）と具体版の導出を作る。**

## 自動ループ tick: 周期点の個数の Lean 必要十分版と導出（2026-08-15）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、四つの主張と証明順序に修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/PeriodicPointCount.lean` に、有限型 $X$ 上の
自己写像 $F:X\to X$ だけを用いる必要十分版を追加した。反復の不動点集合と個数、周期点、最小前周期・
最小周期による特徴づけ、最小周期ごとの個数分解には、二値状態・セル・近傍・局所規則は不要である。
不動点条件を実行可能に判定する箇所にだけ $X$ の等号判定を要求した。

具体版の不動点集合・個数・周期点・整除条件・最小周期類・個数分解・全要素走査が必要十分版の特殊化で
あることを別定理で導出した。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry
検査を全て通した。**「周期点の個数」は `done`。次 tick は必要十分版と導出をレビュー後、
「大域写像の可逆性の有限決定」の構造化記述を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の構造化記述（2026-08-15）

前 tick の「周期点の個数」の Lean 必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。
`structured-latex/content/reversibility-finite-decidability.ts` に、像・単射・全射の定義、
「単射 ⟺ 全射」（有限集合の元の個数の数え上げだけ）、「単射 ⟺ 全ての配位で $\mu(y)=0$
（$\mathrm{Per}(F)=A^{V}$）」、有限決定（全対の等号走査または全配位の最小前周期走査）、主張しないこと
（無限舞台・逆写像の局所性・真理値表からの直接判定）を書いた。「可逆」「時間反転」等の名前は
主張に入れていない。構造化テキスト check・PDF（23 ページ）、対応検査、Lean build、sorry 検査を通した。
**現在地は構造化記述まで。次 tick は本章をレビュー後、SageMath 検算
（`sagemath/check/reversibility-finite-decidability/`）を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の SageMath 検算（2026-08-16）

前 tick の構造化記述を、像・単射・全射の定義、前章の最小前周期・最小周期・周期組の衝突形と
突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/reversibility-finite-decidability/` に、単射 ⟺ 全射（証明が使う像の個数の数え上げの
各段を含む）、単射 ⟺ 全配位で $\mu(y)=0$（両方向の中間段を含む）、全対走査による有限決定、
最小前周期走査による有限決定を分けた 4 本の検算を追加した。配位数 $1,2,4$ の全自己写像と、
セル数 $1$ から $4$ の巡回舞台上の全 256 初等 CA 規則（合計 1,285 写像。うち単射 263）で全て PASS。
一般の有限舞台に対する証明ではなく、有限範囲の裏取りである。浮動小数点と $\mathbb{R}/\mathbb{C}$
脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と
同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 大域写像の可逆性の有限決定の Lean 具体版（2026-08-16）

前 tick の SageMath 4 本と構造化記述の対応をレビューし、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/ReversibilityFiniteDecidability.lean` に人手証明と 1 対 1 の具体版を書いた。
像を全配位の `Finset` の像、単射・全射を人手証明どおりの全称文と集合の等号で定義し、
単射 ⟺ 全射（前段の個数比較、単射側の個数一致、非単射側の $B=A^V\setminus\{y_1\}$ への像の縮小、
$\mathbb{N}$ の非反射性による矛盾）、単射 ⟺ 全配位で $\mu(y)=0$（⇒ は $m=\mu(y)-1$ と反復の帰納的
定義・単射性・最小性、⇐ は $y=F(F^ky)$ による全射から前定理）、$\mathrm{Per}(F)=A^V$ への言い換え、
全対走査（対数 $2^{2|V|}$、`Decidable` インスタンス）と全配位の最小前周期走査への言い換えを、人手証明と
同じ順序で 13 定理として形式化した。数え上げの既製事実は人手証明が名指しした初等的な 5 つに限り、
ファイル冒頭に列挙した。比較回数のコストモデルは形式化していない。構造化テキスト check・PDF、SageMath
4 本、対応検査、Lean build、sorry 検査を全て通した。**現在地は Lean 具体版まで。次 tick はレビュー後に
Lean 必要十分版と導出を作る**（単射 ⟺ 全射には有限型上の自己写像だけが要る見込み）。

## 自動ループ tick: 大域写像の可逆性の有限決定の Lean 必要十分版と導出（2026-08-16）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/ReversibilityFiniteDecidability.lean` に必要十分版を追加した。単射の定義には
型 $X$ 上の自己写像だけが要り、有限性も等号判定も不要。像・全射・「単射 ⟺ 全射」・「単射 ⟺ 全元で
$\mu=0$」には $X$ の有限性だけが要る（像の `Finset` 化は古典的な等号判定で済ませ、実行可能な等号判定は
`Decidable` インスタンスにだけ要求）。二値状態・舞台・近傍・局所規則は現れず、$|X|^2$・$|X|$ が具体版の
$2^{2|V|}$・$2^{|V|}$ に特殊化される。具体版の像・単射・全射・二つの同値・二つの走査・対数が必要十分版の
特殊化であることを別定理 9 本で導出し、sorry 検査の対象へ 22 定理を追加した。構造化テキスト check・PDF、
SageMath 4 本、対応検査、Lean build、sorry 検査を全て通した。**「大域写像の可逆性の有限決定」は `done`。
台帳の未完了対象は無く、次 tick はレビュー後に次の対象（候補: 単射な大域写像の逆写像の局所性）を台帳へ
立てて構造化記述を作る。**

## 自動ループ tick: 逆写像の局所性の有限検査（反例あり）（2026-08-16）

前 tick の「大域写像の可逆性の有限決定」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。台帳に次の対象「逆写像の局所性」を立て、構造化記述の前に反例の有無を有限検査で
確かめた（`sagemath/check/inverse-map-locality-exploration/`、未昇格）。巡回舞台 $L\le7$ の全初等 CA 規則の
うち単射なものについて、逆写像の各セルの本質的依存台を一点反転検査で厳密に数えた。$L=5$ の規則 45 は
順写像の依存台の個数 3 に対し逆写像の依存台が全セルで $V$ 全体（個数 5）、規則 150 は $L=4,5,7$ で
逆写像の依存台の個数が $3,3,5$ と増える。**「逆写像は順写像と同じ近傍の局所規則から定まる」は偽。**
Sage の前処理は `^` を冪に変えるので、`.sage` ファイル内で XOR に `^` を使わないこと（実際に踏んだ）。
**次 tick はこの探索をレビュー後、逆写像とその依存台の定義・依存台の有限決定・具体的反例を claim とする
構造化記述を作る。舞台の族にわたる非有界性は観察に留め、主張しない。**

## 自動ループ tick: 逆写像の局所性の構造化記述（反例を claim として記述）（2026-08-16）

前 tick の探索を再実行してレビューし、修正を要する不一致は見つからなかった。`content/inverse-map-locality.ts` に、
単射な大域写像の逆写像 $F^{-1}$、各セルの値写像 $(F^{-1})_v$ とその本質的依存台（既存の局所真理値表の定義を
$S=V$ で適用）、依存台の有限決定（既存の走査定理の適用）、および反例 claim（$V=\{0,1,2,3,4\}$、$\ell,r$ を表で
与えた近傍、規則 45 の真理値表 $g$。$F$ の単射性は 32 配位の像の表、$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$
は 10 組の証人配位の表で証明）を書いた。$\mathbb{Z}/5\mathbb{Z}$ の同一視を避けるため $\ell,r$ を表で定義した。
$\operatorname{supp}((F^{-1})_v)=V$、規則 150 の観察、舞台の族にわたる非有界性は remark で主張しないと明記した。
構造化テキスト check・build:pdf（25 ページ）、対応検査を通した。**現在地は構造化記述まで。次 tick はレビュー後に
SageMath 検算（像の表・単射性・10 組の証人・依存台走査を分離）を作る。**

## 自動ループ tick: 逆写像の局所性の SageMath 検算（2026-08-16）

前 tick の構造化記述をレビューし、証明 (1) の像の表 32 行と証明 (2) の証人表 10 行を定義から再計算して一致を確認した。
修正を要する不一致は見つからなかった。`sagemath/check/inverse-map-locality/` に、像の表の再計算、単射性（右列の相異と
定義どおりの全対走査）、10 組の証人（$u\notin N(v)$、$\varphi_u z$ の形、$F^{-1}$ の値、$v$ での不一致、$V\setminus N(v)$
を尽くすこと）、逆写像の依存台の一点反転走査（回数 $\le160$、$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$、
順写像の依存台 $\subseteq N(v)$）の 4 本を追加し全て PASS。$\ell,r$ は表で与え剰余類の演算を使わない。反例 claim は
一つの有限対象なので全 32 配位の検査が主張の範囲を尽くす。構造化テキスト check・PDF、SageMath 4 本、対応検査（13 本 +
未昇格 2 件）、Lean build、sorry 検査を全て通した。**現在地は SageMath まで。次 tick はレビュー後に Lean 具体版
（逆写像の定義、$(F^{-1})_v$ と依存台、5 元舞台・規則 45 の反例）を人手証明と同じ順序で作る。反例の単射性は
`decide` に丸投げせず、人手証明どおり 32 配位の像の相異として形式化する方針を検討する。**

## 自動ループ tick: 逆写像の局所性の Lean 具体版（2026-08-16）

前 tick の SageMath 4 本を再実行し（全て PASS）、構造化記述との対応をレビューした。修正を要する不一致は見つからなかった。
`lean/CellularAutomata/InverseMapLocality.lean` に人手証明と 1 対 1 の具体版を書いた。逆写像は「$Fy=z$ を満たす $y$ の
存在（前章の単射 ⟺ 全射）と一意性（単射性）」から `Fintype.choose` で定義し、$F(F^{-1}z)=z$・$F^{-1}(Fy)=y$ を人手証明の
根拠どおりに示した。$(F^{-1})_v$ の依存台は前章の `supp` を $S=V$ で適用し、有限決定は所属の決定可能性と走査組数
$|V|\cdot2^{|V|}$ で形式化した。反例は 5 元帰納型 `Cell`（整数・剰余類を使わない）、$\ell,r$ の表、規則 45 の表 `g` から
$f_v$ を定義し、(1) 単射性と (2) 10 組の証人の存在を `decide`（有限型上の場合尽くし。`native_decide` は不使用）で検査した。
これは人手証明の表を写すのではなく定義から計算した値を照合するもので、人手証明の「有限個の 5 文字列の等号検査」と同じ内容である。
$V\setminus N(v)\subseteq\operatorname{supp}((F^{-1})_v)$、$\operatorname{supp}((F^{-1})_v)\not\subseteq N(v)$、
$\operatorname{supp}(f_v\circ\rho^V_{N(v)})\subseteq N(v)$（追加元への非依存による）、両依存台の非包含まで 12 定理を形式化し
sorry 検査の対象へ追加した。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry 検査を全て通した。
**現在地は Lean 具体版まで。次 tick はレビュー後に Lean 必要十分版と導出を作る**（逆写像の存在一意と両恒等には有限型上の
単射自己写像だけが要る見込み。反例は具体的対象なので必要十分版の対象外とし、一般部分の特殊化として具体版を導出する）。

## 自動ループ tick: 逆写像の局所性の Lean 必要十分版と導出（2026-08-16）

前 tick の Lean 具体版を人手証明・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/InverseMapLocality.lean` を追加した。逆写像の存在一意には有限型上の単射自己写像だけが
要り（存在は前章の必要十分版「単射 ⟺ 全射」、一意性は単射性）、逆写像を選ぶ構成にだけ等号判定を要求した。
$(F^{-1})_v$ は写像型 $S\to A$ であることだけを使い、依存の一点反転判定・有限決定・走査組数 $|S|\,|A|^{|S|}$ は
前々章の必要十分版の適用である。反例は具体的対象なので必要十分版の対象外とし、具体版側に一般部分の導出定理 7 本を
追加した。sorry 検査へ 12 定理を追加し、構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、sorry 検査を
全て通した。**「逆写像の局所性」は四層が揃い done。** 次 tick はレビュー後に次の対象を台帳へ立てる（候補: 逆写像を
「別の近傍 $N'$ をもつ 2 値 CA」として書くための最小近傍の定義と有限決定）。

## 自動ループ tick: 依存台による局所規則の表現の構造化記述（2026-08-16）

前 tick の「逆写像の局所性」の Lean 必要十分版と導出 7 本をレビューし、Lean build と sorry 検査を再実行した。
修正を要する不一致は見つからなかった。次の対象「依存台による局所規則の表現」を台帳へ立て、
`content/local-rule-representation.ts` に構造化記述を書いた。写像 $g:A^V\to A$ が $S\subseteq V$ 上の局所規則で
表せる（$g=h\circ\rho^V_S$ なる $h:A^S\to A$ がある）ことを定義し、表せるなら $\operatorname{supp}(g)\subseteq S$
（前章の依存台不変性）、$\operatorname{supp}(g)\subseteq S$ なら $g=(g\circ\iota^V_S)\circ\rho^V_S$（$V\setminus S$ 上の
非零座標数についての有限帰納法。一点反転と非依存で 1 座標ずつ基準値へ戻す）、$\operatorname{supp}(g)$ が表せる添字集合の
最小元、単射な大域写像の逆写像が近傍 $N'$ の 2 値 CA として書ける $\iff$ 全 $v$ で $\operatorname{supp}((F^{-1})_v)\subseteq N'(v)$、
最小近傍 $N^\ast(v)=\operatorname{supp}((F^{-1})_v)$ の有限決定を claim として証明した。$N^\ast$ を順写像の近傍で抑える
ことは主張しない。構造化テキスト check・PDF（28 ページ）、対応検査、Lean build、sorry 検査を通した。
**現在地は構造化記述まで。次 tick はレビュー後に SageMath 検算（表現可能性 $\iff$ 依存台の包含の小規模全数検査と、
逆写像の最小近傍の反例・可逆例での分離）を作る。**

## 自動ループ tick: 依存台による局所規則の表現の SageMath 検算（2026-08-16 09:12）

前 tick の構造化記述（表現可能性 $\iff$ $\operatorname{supp}(g)\subseteq S$、帰納法、最小性、逆写像の最小近傍）を
レビューし、修正を要する不一致は見つからなかった。`sagemath/check/local-rule-representation/` に検算 4 本を追加し、
$|V|\le3$ の全 276 写像・全 $S$ と、$L\le5$ 巡回舞台の単射な初等 CA 規則 124 組で全て PASS（有限範囲、一般証明ではない）。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の SageMath 検算（2026-08-16 23:42）

前 tick の構造化記述を衝突開始位置・最小正周期・周期伝播の既証明と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid-tail-cycle-decomposition/` に、最小衝突開始位置より前の
非衝突と安定後尾との非交差、安定後尾の一周期分への還元と相異性、過渡部と巡回部の非交和および元数
$\mu_F+\lambda_F$、有限真理値表からの有限決定を分離した検算 4 本を追加した。セル数 0 と
$1\le|V|\le3$ の巡回舞台上の全 256 初等 CA（計 769 写像）で全て PASS。有限範囲の検算であり一般証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick はレビュー後、
Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の構造化記述（2026-08-16 23:12）

前 tick の「反復モノイドの最小正周期」の Lean 必要十分版と導出を全層と突き合わせ、必要十分版が
Lean の検査入口へ直接登録されていない不整合を見つけて import を追加した。次の対象
「反復モノイドの過渡部と巡回部への分解」を立て、構造化記述を
`structured-latex/content/iterate-monoid-tail-cycle-decomposition.ts` に書いた。最小衝突開始位置
$\mu_F$ より前の反復写像は互いに異なり安定後の反復写像と交わらないこと、安定後の後尾集合は最小正周期
$\lambda_F$ の一周期分で尽くされ各元が互いに異なること、反復モノイドは両部分の非交和で元数が
$\mu_F+\lambda_F$ になること、これらが有限真理値表から有限決定できることを証明した。既存半群論の
分類名、物理的名称、$\mathbb R/\mathbb C$ は使わない。**現在地は構造化記述まで。次 tick はレビュー後、
本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの最小正周期の Lean 必要十分版と導出（2026-08-16 22:42）

前 tick の Lean 具体版を構造化記述・SageMath 3 本と突き合わせ、正周期の述語、最小性、周期の伝播
（加法則）、除法と帰納法による整除、各候補の有限判定に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidMinimalPeriod.lean` に必要十分版を、具体版と同じ手順で書いた。
要るのは型 $X$、自己写像 $F$、衝突開始位置の存在（前章の必要十分版の入力）だけで、$X$ の有限性は
この存在を与えるためにだけ使う。周期の伝播には加法則だけ、整除には伝播・自然数の除法・帰納法・最小性
だけ、各候補の有限判定には $X\to X$ の等号判定だけが要り、二値状態・セル・近傍・局所規則は現れない。
具体版の全主要主張（述語の一致、非空性、最小正周期の一致＝両方向の最小性、伝播、整除）を必要十分版の
特殊化として導出した。Lean build・sorry 検査（新 11 定理を追加）、構造化テキスト check、対応検査（20 件）
を通した。本文・SageMath は変更していないため PDF と SageMath は前 tick のものと同一で再実行していない。
対象は `done`。**次 tick は本 tick の必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの最小正周期の Lean 具体版（2026-08-16 22:12）

前 tick の SageMath 3 本を再実行して構造化記述と突き合わせ、周期の伝播、除法と帰納法による
余り 0 の導出、上限なし走査の停止根拠に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidMinimalPeriod.lean` に、正周期の述語と非空性、自然数の整列性による
最小正周期、最小性、周期の伝播、自然数の除法と帰納法から最小正周期が全正周期を割り切ること、
各候補の有限判定を人手証明と同じ対象・仮定・順序で形式化した。比較回数のコストモデルは形式化していない。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・
build:pdf（36 ページ・未解決参照 0）、SageMath 3 本、対応検査（20 件）、Lean build、sorry 検査を
全て通した。
**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 依存台による局所規則の表現の Lean 具体版（2026-08-16 09:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/LocalRuleRepresentation.lean` に、部分集合 $S$ 上の局所規則による表現可能性、
表現可能性 $\iff\operatorname{supp}(g)\subseteq S$、依存台自身による表現と最小性を形式化した。
包含から表現を得る向きは、人手証明と同じく有限集合上で依存しない座標を一つずつ基準値へ置換する帰納法で示した。
単射な大域写像については、逆写像が近傍 $N'$ の局所規則族の大域写像になること
$\iff$ 全 $v$ で $\operatorname{supp}((F^{-1})_v)\subseteq N'(v)$、局所規則
$(F^{-1})_v\circ\iota^V_{N'(v)}$、最小近傍 $N^\ast(v)=\operatorname{supp}((F^{-1})_v)$、
走査組数 $|V|\,2^{|V|}$ をこの順で形式化した。有限型・二元状態・自然数だけで閉じ、$\mathbb{R}/\mathbb{C}$ 脱出はない。
**現在地は Lean 具体版まで。次 tick はレビュー後、Lean 必要十分版と具体版からの導出を作る。**

## 自動ループ tick: 依存台による局所規則の表現の Lean 必要十分版と導出（2026-08-16 10:12）

前 tick の Lean 具体版を人手証明・SageMath と突き合わせ、主張と順序は一致。人手証明の $|D(y)|$ についての帰納法が
Lean では変更を許す座標の `Finset` についての帰納法（各段は同じ一座標の置換）になっている点だけ台帳に記録した。
`lean/CellularAutomata/NecSuf/LocalRuleRepresentation.lean` を追加した。表現可能性と `Finset` 帰納法には添字型の
等号判定だけが要り、表現可能 ⟹ 外に依存元なし、は具体版と同じ手順（前章の一点反転検査）を保つため状態側の ν と
別値の一意性を要求、外に依存元なし ⟹ 基準値延長で表せる、には添字型の有限性と基準値だけが要る。逆写像の同値は前章の
逆写像の構成のため $V,A$ の有限性・等号判定を仮定し、二値性は不要。依存台を `Finset` として集めるのは具体版だけ。
具体版側に導出定理 6 本を追加し、全検証を通した。**「依存台による局所規則の表現」は done。次 tick は次の対象を立てる。**

## 自動ループ tick: 大域写像の反復が生成する有限可換モノイドの構造化記述（2026-08-16 10:42）

前 tick の「依存台による局所規則の表現」の Lean 必要十分版と導出をレビューし、修正を要する不一致は
見つからなかった。第一相のカタログにある未着手操作「反復の生成するモノイド」を次の対象として台帳へ立て、
`structured-latex/content/iterate-monoid.ts` に構造化記述を書いた。反復写像の集合 $P_F$、
$F^m\circ F^n=F^{m+n}$、有限可換モノイド性、写像空間 $(A^V)^{A^V}$ の有限性から得る反復写像の衝突、
衝突後の有限代表集合、有限真理値表による元と合成表の決定を証明した。有限集合と自然数だけで閉じ、
無限反復の極限・位相・実数・複素数は使わない。構造化テキスト check・build:pdf（29 ページ）、
既存 SageMath 対応検査、Lean build・sorry 検査を全て通した。**現在地は構造化記述まで。次 tick はレビュー後、
反復写像の衝突・有限代表・合成表の SageMath 検算を分けて作る。**

## 自動ループ tick: 反復モノイドの SageMath 検算（2026-08-16）

前 tick の「大域写像の反復が生成する有限可換モノイド」の構造化記述をレビューし、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid/` に、加法則 $F^m\circ F^n=F^{m+n}$ の基底段・帰納段、
衝突 $F^i=F^j$ と反復不変性・除法による還元、モノイド公理と $|P_F|\leq M^M$、状態等号への分解による
有限決定と合成表の一致を別々に検査する 4 本を追加し、セル数 $0$ と $1\leq|V|\leq3$ の全 256 初等 CA 規則
（769 写像）で全て PASS。上界 $K=M^M$ までは走査せず、$M+\operatorname{lcm}(1,\dots,M)$ 以内の最初の衝突指数が
$K$ 以下であることを検査した。有限範囲の裏取りであり一般証明ではない。浮動小数点と $\mathbb{R}/\mathbb{C}$
脱出はない。**本章の現在地は SageMath まで。次 tick はレビュー後に Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの Lean 具体版（2026-08-16 11:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoid.lean` に人手証明と 1 対 1 の具体版を書いた。反復写像の集合 $P_F$、
$F^m\circ F^n=F^{m+n}$、恒等元・閉性・結合律・可換律、$|P_F|\leq M^M$、写像空間への鳩の巣原理による
衝突、衝突後の反復不変性と自然数の除法による有限代表への還元、有限代表集合と $P_F$ の一致、写像等号の
全配位・全セルの二値状態等号への分解、指数の加法による合成を人手証明と同じ順序で形式化した。
比較回数のコストモデル自体は形式化していない。**現在地は Lean 具体版まで。次 tick はレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの Lean 必要十分版と導出（2026-08-16 12:12）

前 tick の Lean 具体版を人手証明・SageMath と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoid.lean` を追加した。反復写像の集合・加法則・有限可換モノイドの
各公理には型 X と自己写像 F だけが要り、有限性も等号判定も不要。個数上界と鳩の巣による衝突には X の
有限性だけ（等号判定は Lean の構成上の都合なので仮定に含めず古典論理で写像空間の Fintype を得る）、
代表集合の `Finset` 化と有限候補上の衝突判定にだけ等号判定が要ることを分離した。具体版側に導出定理
10 本を追加し、具体版の等号判定を使わない定理から未使用仮定を `omit` で外した。構造化テキスト check・
build:pdf（29 ページ）、SageMath 4 本、対応検査、Lean build、sorry 検査（29 定理追加）を全て通した。
**「大域写像の反復が生成する有限可換モノイド」は四層が揃い done。次 tick はレビュー後、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの冪等元の構造化記述（2026-08-16 12:42）

前 tick の反復モノイドの Lean 必要十分版と導出を、構造化記述・SageMath・具体版と突き合わせ、修正を
要する不一致は見つからなかった。次の対象「反復モノイドの冪等元」を台帳へ立て、
`structured-latex/content/iterate-monoid-idempotents.ts` に、冪等元と冪等指数、衝突後の周期、正の冪等指数の
存在、冪等元全体の有限決定を記述した。非単位冪等元の存在は恒等 CA が否定し、冪等元の一意性は一元舞台の
定値規則が否定することも claim として保存した。有限集合と自然数だけで閉じ、極限と
$\mathbb{R}/\mathbb{C}$ は使わない。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの冪等元の SageMath 検算（2026-08-16 13:12）

前 tick の構造化記述を、前章の反復モノイドの定義・加法則・衝突・有限決定と突き合わせ、衝突からの
以後の周期の式変形、正冪等指数の構成（$i=0$ で $e=p$、$i\ge1$ で $e=ip\ge i$ とし以後の周期を
$i$ 回適用）、有限決定、2 つの反例のいずれにも修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-idempotents/` に、衝突からの以後の周期（式変形を一行ずつ）、
正冪等指数の存在（場合分けごと）、冪等元全体の 2 値状態等号走査による有限決定
（検査回数 $|P_F|\cdot2^{|V|}\cdot|V|$ 以下）、恒等 CA の反例、定値規則の反例を分けた 5 本を追加した。
一般側はセル数 0 と $1\leq|V|\leq3$ の巡回舞台上の全 256 初等 CA 規則の計 769 写像で全て PASS。
一般の有限舞台に対する証明ではないが、反例 2 件は構造化記述が構成する有限対象そのものなので
全数検査が主張の範囲を尽くす。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。構造化テキスト
check・PDF（31 ページ・未解決参照 0）、対応検査（16 本 + 未昇格 2 件）、Lean build、sorry 検査を
全て通した。**この章の現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、
人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 反復モノイドの冪等元の Lean 具体版（2026-08-16 13:42）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidIdempotents.lean` に、冪等元・冪等指数、衝突後の周期、周期の有限回反復、
正の冪等指数の存在、有限代表を対角合成で絞る有限決定を人手証明と同じ順序で形式化した。一元舞台の
恒等局所規則と定値局所規則から大域写像を構成し、非単位冪等元の存在が強制されない反例と、相異なる
二つの冪等元をもつ反例も形式化した。有限集合と自然数だけで閉じ、$\mathbb{R}/\mathbb{C}$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元の Lean 必要十分版と導出（2026-08-16 14:12）

前 tick の Lean 具体版を人手証明・SageMath 5 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidIdempotents.lean` を追加した。冪等元・衝突後の周期・周期の反復には
型と自己写像だけが要り、正の冪等指数の存在に要るのは衝突が一つ存在することだけ（有限性は衝突を得るためにだけ
使う）、有限決定にだけ等号判定が要る。反例は恒等写像（構造不要）と、相異なる二点への定値写像だけで成り立つ。
具体版側に導出定理 9 本を追加し、sorry 検査へ 18 定理を追加した。構造化テキスト check・PDF（31 ページ）、
SageMath 5 本、対応検査、Lean build、sorry 検査を全て通した。**「反復モノイドの冪等元」は四層が揃い done。
次 tick はレビュー後、次の対象（候補: 反復モノイドの巡回部分、最小前周期・最小周期のモノイドへの反映、
第一相カタログの未着手操作）を立てる。**

## 自動ループ tick: 反復モノイドの主イデアル列の構造化記述（2026-08-16 14:42）

前 tick の「反復モノイドの冪等元」の必要十分版と導出を四層で突き合わせ、SageMath 5 本、Lean build、
sorry 検査を再実行した。修正を要する不一致は見つからなかった。第一相カタログの Green 関係へ進む前段として
「反復モノイドの主イデアル列」を台帳へ立て、`structured-latex/content/iterate-monoid-principal-ideal-tail.ts` に
構造化記述を書いた。後尾集合 $I_n(F)=\{F^{n+k}:k\in\mathbb N\}$ が $F^n$ の生成する主イデアルであること、
反復写像の合成を吸収すること、包含について減少すること、衝突 $F^i=F^j$ の後は $I_n(F)=I_i(F)$ に
安定すること、列と最初の安定位置の有限決定を証明した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、主イデアルの等号・
包含・衝突後の安定・最初の安定位置を分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の SageMath 検算（2026-08-16 15:12）

前 tick の「反復モノイドの主イデアル列」の構造化記述をレビューした。主イデアルの両包含、吸収の加法則と
$\mathbb N$ の結合律・交換律、$I_{n+1}\subseteq I_n$、衝突後の安定で以後の周期を $d$ 回適用して
$q=k+dp-d\ge0$ と置く箇所、有限決定の論証のいずれにも修正を要する誤りは見つからなかった。
`sagemath/check/iterate-monoid-principal-ideal-tail/` に主イデアルの等号と吸収、包含の減少、衝突後の安定、
走査による有限決定を分離した 4 本を追加した。$|V|\le3$ の 769 写像を全数で厳密検査し全て PASS。
有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・
build:pdf（33 ページ、未解決参照 0）、対応検査（17 本 + 未昇格 2 件）、Lean build、sorry 検査を全て通した。
**現在地は SageMath まで。次 tick は検算と構造化記述の対応をレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版（後尾集合、主イデアル等号、吸収、減少、衝突後の安定、有限決定）を作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の Lean 具体版（2026-08-16 15:42）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidPrincipalIdealTail.lean` に、後尾集合、主イデアル表示、合成の吸収、包含の減少、
衝突後の安定を人手証明と同じ順序で形式化した。有限決定は、衝突指数未満の有限代表から後尾集合を作る走査が
定義と一致すること、衝突の始点までの有限候補から最初の安定位置を選べることとして形式化した。比較回数のコストモデル自体は
形式化していない。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版と人手証明の対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル列の Lean 必要十分版と導出（2026-08-16 16:42）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、後尾集合、主イデアル表示、合成の吸収、
包含の減少、衝突後の安定、有限走査の対象・仮定・証明順序に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidPrincipalIdealTail.lean` を追加した。後尾集合、主イデアル表示、
合成の吸収、包含の減少には型と自己写像だけが要り、衝突後の安定には衝突が一つ与えられていることだけが
要る。有限性は衝突を得る箇所にだけ使い、有限代表から後尾集合と最初の安定位置を走査する構成にだけ写像の
等号判定を要求した。具体版側に必要十分版との一致と特殊化による導出定理を追加した。有限集合・自然数・
写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**対象は四層と導出が揃い done。次 tick は本 tick の
必要十分版と導出を全層でレビュー後、第一相カタログの未着手操作から次の対象を立てる。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の構造化記述（2026-08-16 17:12）

前 tick の「反復モノイドの主イデアル列」の四層と導出を突き合わせ、SageMath 4 本、Lean build、sorry 検査を
再実行した。後尾集合、主イデアル表示、吸収、減少、衝突後の安定、有限走査に修正を要する不一致は見つからなかった。
第一相カタログの「反復の生成するモノイド」に残る主イデアルの比較を、既存半群論の名称を先取りせず直接調べる
「反復モノイドの主イデアル同値と有限鎖」として台帳へ立てた。

`structured-latex/content/iterate-monoid-tail-equivalence.ts` に、反復写像 $G\in P_F$ が生成する主イデアル
$J_F(G)$、その等号による主イデアル同値を定義した。集合の等号の反射律・対称律・推移律から同値関係であること、
$G=F^m,H=F^n$ の指数を $\mathbb N$ の全順序で比較し、後尾集合の包含へ移すことで任意の二つの生成主イデアルが
包含で比較できること、生成主イデアル全体 $\mathcal J_F$ が有限集合 $P_F$ の像として有限で、合成表の有限走査から
全ての元・包含関係・同値類を有限決定できることを証明した。既存半群論の関係との名称上の同定・既知性・分類定理との
接続は一次文献確認の別層へ残した。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキスト check・build:pdf、既存 SageMath 対応検査、Lean build、sorry 検査を全て通した。
**現在地は構造化記述まで。次 tick はレビュー後、主イデアル同値、包含による全比較、有限鎖の決定を分離した
SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の SageMath 検算（2026-08-16 17:42）

前 tick の構造化記述を反復モノイドと後尾集合の既証明に突き合わせ、主イデアル表示、集合の等号による
同値関係、自然数指数の全順序による包含比較、有限合成表からの列挙に修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-principal-ideal-chain/` に、主イデアル同値、包含による全比較、有限鎖と同値類の
有限決定を分離した 3 本を追加した。$|V|\le3$ の 769 写像について、同値律 22,906 件、生成元の順序対
6,972 組、相異なる主イデアルと同値類各 1,419 個を厳密に検査し全て PASS。一般の有限舞台に対する証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick は検算と構造化記述の対応を
レビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の Lean 具体版（2026-08-16 18:12）

前 tick の SageMath 3 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidPrincipalIdealChain.lean` に、生成主イデアル、主イデアル同値と
その同値律、自然数指数の全順序による生成主イデアルの比較可能性、衝突指数未満の有限代表と合成表から
生成主イデアル全体を有限列挙する構成を、人手証明と同じ順序で形式化した。有限集合・自然数・写像合成
だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick はレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの主イデアル同値と有限鎖の Lean 必要十分版と導出（2026-08-16 18:42）

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidPrincipalIdealChain.lean` で、生成主イデアル・同値律・包含比較に要るのが
型 $X$ と自己写像 $F:X\to X$ だけであることを示した。$X$ の有限性は衝突の存在、等号判定は写像の有限代表・像・
有限部分集合の等号判定にだけ要る。二値状態、セル、近傍、局所規則は必要十分版に現れない。
具体版側に定義の一致と主要主張の特殊化による導出を追加し、Lean build と sorry 検査を通した。
**「反復モノイドの主イデアル同値と有限鎖」は四層と導出が揃い `done`。次 tick は本 tick を全層でレビュー後、
第一相カタログの未着手操作から次の対象を立てる。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の構造化記述（2026-08-16 19:12）

前 tick の「反復モノイドの主イデアル同値と有限鎖」の必要十分版と導出を全層で突き合わせ、修正を要する
不一致は見つからなかった。次の対象を、既存半群論の名称を先取りせず反復列と後尾集合だけから調べる
「反復モノイドの衝突開始位置と主イデアル列の安定位置」として台帳へ立てた。
`structured-latex/content/iterate-monoid-stabilization-index.ts` に、正周期をもつ写像衝突の最小開始指数
$\mu_F$、隣接する後尾主イデアルの等号と同じ位置で始まる衝突の同値、$\mu_F$ より前の厳密減少と
以後の安定、最初の安定位置と $\mu_F$ の一致・有限決定を記述した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、本章の SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の SageMath 検算（2026-08-16 19:42）

前 tick の構造化記述を前章の後尾集合の定義・性質と突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stabilization-index/` に、衝突開始位置の非空性と最小値 $\mu_F$、
隣接後尾集合の等号と衝突の同値の両方向、$\mu_F$ より前の真部分集合による減少と以後の一定、
走査で得る最初の安定位置と $\mu_F$ の一致を分離した検算 4 本を置き、769 写像で全て PASS。
有限範囲の検算であり一般証明ではない。$\mathbb R/\mathbb C$ 脱出はない。
**現在地は SageMath まで。次 tick はレビュー後、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の Lean 具体版（2026-08-16 20:12）

前 tick の SageMath 4 本を再実行して構造化記述と突き合わせ、769 写像で全て PASS し、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/IterateMonoidStabilizationIndex.lean` に、衝突開始述語、
自然数の整列性による最小衝突開始位置、隣接後尾集合の等号との同値、最小位置より前の真の減少と以後の安定、
有限代表を使う最初の安定位置との一致を、人手証明と同じ順序で形式化した。真部分集合は包含と集合の不等号を
分けて記録し、比較回数のコストモデル自体は形式化していない。有限集合と自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF、SageMath 4 本、対応検査、Lean build、
sorry 検査を全て通した。**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの衝突開始位置と主イデアル列の安定位置の Lean 必要十分版と導出（2026-08-16 20:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStabilizationIndex.lean` を追加した。衝突開始述語と隣接後尾集合の
等号との同値には型と自己写像だけが要り、最小衝突開始位置・真の減少・以後の安定には「衝突開始位置の存在」
だけが要る（有限性はその存在を与えるためにだけ使う）。最初の安定位置との一致には走査用の等号判定と衝突証人
だけが要り、衝突証人が存在を与えるので有限性は不要。具体版の主要主張は必要十分版の特殊化として導出し、
最小値の一致は両方向の最小性、存在証明の違いは証明無関係で吸収した。Lean build・sorry 検査、
構造化テキスト check、SageMath 4 本、対応検査を通した（本文未変更のため PDF は再生成せず）。
対象は `done`。**次 tick は本必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの最小正周期の構造化記述（2026-08-16 21:12）

前 tick の「反復モノイドの衝突開始位置と主イデアル列の安定位置」の Lean 必要十分版と導出を、
具体版・構造化記述・SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。
次の対象「反復モノイドの最小正周期」を台帳へ立て、
`structured-latex/content/iterate-monoid-minimal-period.ts` に構造化記述を書いた。
最小衝突開始位置 $\mu_F$ で戻る正周期の集合 $\Pi_F$ と最小元 $\lambda_F$ を定義し、
正周期が $\mu_F$ 以後へ伝わること、自然数の除法の余りが正なら最小性に矛盾することから
$\lambda_F$ が全正周期を割り切ること、大域真理値表の等号走査による有限決定を証明した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの最小正周期の SageMath 検算（2026-08-16 21:42）

前 tick の構造化記述（周期の伝播、除法と帰納法、走査の停止根拠）をレビューし、修正を要する誤りは
見つからなかった。`sagemath/check/iterate-monoid-minimal-period/` に、周期の伝播の各段、
除法の余りが 0 になること（帰納法の各段と最小性への矛盾を分離）、上限なし走査による有限決定の
3 本を追加した。セル数 0 と $1\le|V|\le3$ の巡回舞台上の全 256 初等 CA（計 769 写像）で全て PASS。
有限集合の写像の等号と非負整数の除法だけを使い、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check と対応検査（20 件）を通した。本文・Lean は未変更のため PDF・Lean build は再実行せず。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版を人手証明と同じ対象・仮定・順序で作る。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の Lean 必要十分版と導出（2026-08-17 00:42）

前 tick の Lean 具体版を構造化記述（5 claim）と SageMath 4 本と突き合わせ、非衝突・非交差・
一周期分への還元・一周期内の相異性・非交和と元数公式・有限決定の対象と順序が一致し、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidTailCycleDecomposition.lean` を
追加した。非衝突・非交差・還元・相異性・尽くすことに要るのは型 X、自己写像 F、衝突開始位置の存在
だけで、X の有限性はその存在を与える側にだけ現れる。X → X の等号判定は過渡部・巡回部を `Finset` に
書いて元数公式を述べる段階（`Finset.image` と `card_image_iff`）にだけ要り、等号判定を使わない
定理には `omit` を付けた。有限決定は hex を型クラスでは渡せないため instance ではなく def とした。
具体版の過渡部・巡回部・非衝突・還元・相異性・非交和と元数公式・尽くすことを必要十分版の
特殊化として導出し、Lean の入口と sorry 検査の登録へ追加した。lake build・sorry 検査・
構造化テキスト check・対応検査を通した。**対象は done。次 tick は本 tick の必要十分版と導出を
レビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの過渡部と巡回部への分解の Lean 具体版（2026-08-17 00:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidTailCycleDecomposition.lean` に、過渡部と巡回部の有限集合、
最小衝突開始位置より前の非衝突、過渡部と安定後尾の非交差、安定後尾の最小正周期一周期分への還元、
一周期内の相異性、二部分による反復モノイドの非交和、元数公式
$|P_F|=\mu_F+\lambda_F$、有限決定を人手証明と同じ順序で形式化した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と具体版からの
導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の構造化記述（2026-08-17 01:12）

前 tick の「反復モノイドの過渡部と巡回部への分解」の Lean 必要十分版と導出を、具体版・構造化記述・
SageMath 検算と突き合わせ、修正を要する不一致は見つからなかった。次の対象として、反復モノイド全体では
冪等元が一意とは限らない一方、巡回部では一意になることを立てた。安定後にある最小の
$\lambda_F$ の倍数 $e_F$ と $E_F:=F^{e_F}$ を定義し、周期の伝播から $E_F$ が巡回部に属して冪等であること、
巡回部の冪等元 $F^n$ は $F^{2n}=F^n$ と一周期内の相異性から $\lambda_F\mid n$ を満たすため
$E_F$ に一致すること、$e_F$ と $E_F$ の有限決定を構造化記述した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、剰余の一致・冪等性・
一意性・有限決定を分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の SageMath 検算（2026-08-17 01:42）

前 tick の構造化記述をレビューし、一意性の証明の最終段で $e_F\le n$（$e_F$ の最小性）と
$n=e_F+t\lambda_F$ の形が暗黙だったため、最小性の参照と周期の伝播の適用範囲を明示する小修正を
入れた（結論は変わらない）。`sagemath/check/iterate-monoid-cycle-idempotent/` に SageMath 4 本を作り、
証人 $\mu_F\lambda_F\in D_F$ と有限範囲で列挙した最小元 $e_F$、$E_F=F^{e_F}$ の巡回部への所属と伝播の
逐次適用による冪等性、巡回部の全元を走査して冪等な元について人手証明の各段（$F^{2n}=F^n$、剰余 $s$、
$r=s$、$\lambda_F\mid n$、$e_F\le n$、$F^{e_F}=F^n$）、有限真理値表からの走査を分離して全て PASS
（769 写像、巡回部の元 1,412 個、各写像で巡回部の冪等元はちょうど一つ。参考として過渡部の冪等元は
恒等写像に限ることも 540 写像で観測）。有限範囲の検算であり一般証明ではない。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の Lean 具体版（2026-08-17 02:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidCycleIdempotent.lean` に、安定後周期倍数指数とその最小元、周期の
逐次適用、安定後指数の一周期への剰余還元、候補の巡回部所属と冪等性、巡回部の冪等元の一意性を
人手証明と同じ順序で形式化した。一意性では自然数の合同から最小正周期が指数を割ることを導き、
最小の安定後周期倍数指数との差だけ周期を逐次適用した。有限集合・自然数・写像合成だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、
Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部にある唯一の冪等元の Lean 必要十分版と導出（2026-08-17 02:42）

前 tick の Lean 具体版を構造化記述の 3 claim と SageMath 4 本と突き合わせ、対象・仮定・証明順序に
修正を要する不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidCycleIdempotent.lean`
を追加した。安定後周期倍数の存在と最小元、周期の逐次適用、剰余還元、$E_F$ の冪等性、および
「指数 $\mu_F+r$（$r<\lambda_F$）にある冪等な反復写像は $E_F$ に等しい」ことには型 $X$・自己写像 $F$・
衝突開始位置の存在だけが要り、$X$ の有限性は現れない。$X\to X$ の等号判定は巡回部を有限集合として
書き「$E_F\in C_F$」「$g\in C_F$ かつ冪等 $\Rightarrow g=E_F$」と述べる段階にだけ要る（使わない定理は
`omit`）。具体版の $D_F$・$e_F$・$E_F$・所属と冪等性・一意性を必要十分版の特殊化として導出し、
入口と sorry 検査へ登録した。lake build、sorry 検査、構造化テキスト check、対応検査、SageMath 4 本を
通した。**対象は `done`。次 tick は本 tick の必要十分版と導出をレビューし、次の対象を立てる。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の構造化記述（2026-08-17 03:12）

前 tick の「反復モノイドの巡回部にある唯一の冪等元」の Lean 必要十分版と導出を全層で突き合わせ、
SageMath 4 本、Lean build、sorry 検査を再実行した。対象・仮定・証明順序に修正を要する不一致は
見つからなかった。次の対象を、既存の群を目標仕様として置かず、巡回部の合成から内在的に調べる
「反復モノイドの巡回部がなす有限巡回群」として台帳へ立てた。

`structured-latex/content/iterate-monoid-cyclic-group.ts` に、合成の閉性、唯一の冪等元 $E_F$ の左右単位元性、
各 $F^n$ の逆元 $F^{e_F+n(\lambda_F-1)}$、$K_F:=F^{e_F+1}$ の群の冪による巡回部全体の生成、
位数 $\lambda_F$、演算表・逆元・生成元の有限決定を記述した。証明は反復回数の加法則、安定後の周期伝播、
一周期内の相異性だけを使う。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、閉性・単位元・逆元・巡回生成と位数・有限決定を
分離した SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の SageMath 検算（2026-08-17 03:42）

前 tick の構造化記述を前章の定義・claim と突き合わせ、逆元の指数と群冪の帰納段の還元に要る $\ge\mu_F$ が
成り立つこと、相異性の還元が前章の一周期内の相異性で閉じることを確認し、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-cyclic-group/` に SageMath 4 本を作り、合成の閉性・可換律・結合律と $E_F$ の
左右単位元性、逆元 $F^{e_F+n(\lambda_F-1)}$、$K_F=F^{e_F+1}$ の群冪による巡回生成と相異性と位数 $\lambda_F$、
有限真理値表からの合成表・逆元列走査・生成元の決定を人手証明の各段に分けて全て PASS（769 写像、
巡回部の元 1,412 個）。有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の Lean 具体版（2026-08-17 04:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidCyclicGroup.lean` に、巡回部の合成閉性、$E_F$ の左右単位元性、
$F^{e_F+n(\lambda_F-1)}$ による左右逆元、$K_F=F^{e_F+1}$ の群冪 $F^{e_F+r}$、最初の
$\lambda_F$ 個の冪の相異性と巡回部全体の生成、位数 $\lambda_F$ を人手証明と同じ順序で形式化した。
合成表と逆元候補表も有限集合として定義した。有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの巡回部がなす有限巡回群の Lean 必要十分版と導出（2026-08-17 04:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、claim「有限可換群」の可換律だけが
定理として欠けていたので `cyclePart_comp_comm` を具体版・必要十分版へ補った。他に修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidCyclicGroup.lean` に必要十分版を
追加した。逆元の左右恒等性・群冪の反復写像表示・一周期内の相異性には型・自己写像・衝突開始位置の
存在だけが要り、写像の等号判定は巡回部を有限集合として所属・生成・位数を述べる段階にだけ要る。
舞台・状態・近傍・局所規則の有限性はこの章の定理に現れない。具体版の主要主張を必要十分版の特殊化として
導出した（8 本）。Lean build・sorry 検査（21 定理を追加登録）・構造化テキスト check・対応検査・
SageMath 4 本・PDF を通した。**「反復モノイドの巡回部がなす有限巡回群」は四層が揃い done。
次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の構造化記述（2026-08-17 05:12）

前 tick の巡回部の有限巡回群の Lean 必要十分版と具体版からの導出を全層で突き合わせ、補われた
可換律を含め、修正を要する不一致は見つからなかった。次の対象を、巡回部の唯一の冪等元が配位集合に
内在的に定める「安定像」として立てた。`structured-latex/content/iterate-monoid-stable-image.ts` に、
$Q_F=E_F(A^V)$、$E_F$ が $Q_F$ 上で恒等になること、$n\ge\mu_F$ の全ての $F^n$ の像が $Q_F$ に
一致すること、$F$ の $Q_F$ への制限が全単射で $F^{e_F+\lambda_F-1}$ が逆写像になること、安定像と
制限写像の表の有限決定を記述した。証明は既証明の冪等性、巡回部の群法則、周期伝播だけを使う。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。
次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の SageMath 検算（2026-08-17 05:42）

前 tick の構造化記述を前章の定義・claim と突き合わせ、$R_F=F^{e_F+\lambda_F-1}$ が $\mathbb N$ 上で
定義できること、$2e_F+\lambda_F-1\ge\mu_F$ による安定後の像の一致への還元、巡回部の群法則から取る
逆元の使い方を確認し、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stable-image/` に SageMath 4 本を作り、安定像上での $E_F$ の恒等性、
$\mu_F\le n$ の反復写像の像と $Q_F$ の一致（両包含を点ごとに）、$F$・$R_F$ が $Q_F$ を保ち制限が
互いに逆写像になること、有限真理値表からの $Q_F$ と制限表の走査を人手証明の各段に分けて全て PASS
（769 写像、安定像の点 2,201 個）。像の一致で $n\ge\mu_F+\lambda_F$ の逆元は周期の伝播で一周期内へ
還元してから前章の指数を用いた（人手証明は存在だけを使う）。有限範囲の検算であり一般証明ではない。
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。**現在地は SageMath まで。次 tick はレビュー後、
Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の Lean 具体版（2026-08-17 06:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableImage.lean` に、安定像 $Q_F=E_F(A^V)$、$E_F$ の
安定像上での恒等性、$n\ge\mu_F$ の反復写像の像と安定像の一致、
$R_F=F^{e_F+\lambda_F-1}$ と $F$ による安定像の保存、両制限の相互逆性、$F|_{Q_F}$ の
全単射性を人手証明と同じ順序で形式化した。安定像の部分型は有限型として構成した。
有限集合・自然数・写像合成だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層とレビュー後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの冪等元が定める安定像の Lean 必要十分版と導出（2026-08-17 06:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、修正を要する不一致は
見つからなかった（台帳で対象表の後ろに置かれていた 03:42・04:12 tick の記録を時系列へ移した）。
`lean/CellularAutomata/NecSuf/IterateMonoidStableImage.lean` に必要十分版を追加した。安定像の定義、
$E_F$ の恒等性、$R_F=F^{e_F+\lambda_F-1}$ の左右恒等性、$F$・$R_F$ による安定像の保存、制限の
相互逆性と全単射性には型・自己写像・衝突開始位置の存在だけが要り、写像の等号判定は人手証明どおり
$F^n\in C_F$ を経由する像の一致にだけ、$X$ の有限性と等号判定は走査表にだけ要る。具体版の主要主張を
特殊化として導出する定理 8 本を具体版へ追加した。Lean build、sorry 検査、構造化テキスト check、
PDF、対応検査、SageMath 4 本を通した。**「反復モノイドの冪等元が定める安定像」は四層が揃い `done`。
次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の構造化記述（2026-08-17 07:12）

前 tick の「反復モノイドの冪等元が定める安定像」の Lean 必要十分版と具体版からの導出を全層と
突き合わせ、修正を要する不一致は見つからなかった。次の対象を、冪等反復写像の値ごとの逆像が
配位集合に定める内在的構造を調べる「反復モノイドの安定像による配位集合の分割」として台帳へ立てた。
`structured-latex/content/iterate-monoid-stable-partition.ts` に、安定ファイバー
$B_F(q)=\{y\in A^V\mid E_F(y)=q\}$、各配位がただ一つのファイバーに属すること、異なるファイバーの
非交差、$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$、全ファイバーと個数の有限決定を構造化記述した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の SageMath 検算（2026-08-17 07:42）

前 tick の構造化記述を前章の安定像の定義・恒等性・有限決定と突き合わせ、修正を要する不一致は
見つからなかった。`sagemath/check/iterate-monoid-stable-partition/` に SageMath 5 本を作り、安定像の
各元が自身のファイバーに属すること、各配位がただ一つの安定ファイバーに属すること（存在と一意性）、
異なるファイバーの非交差、指示値 $\delta_F(q,y)$ による個数分解
$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$ の各等号、有限真理値表からのファイバーと個数の走査を
人手証明の各段に分けて全て PASS（769 写像、配位 3,585 個、相異なる組 7,048 件）。有限範囲の検算であり
一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。検算スクリプトのカウンタ名が
補助関数 `configurations` を隠して落ちたので改名した（結果への影響なし）。
**現在地は SageMath まで。次 tick はレビュー後、Lean 具体版だけを作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の Lean 具体版（2026-08-17 08:12）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStablePartition.lean` に、安定ファイバー、$E_F(y)$ による代表元、
代表元の存在一意性、異なるファイバーの非交差、全配位を代表元ごとに filter する有限走査表、
$\sum_{q\in Q_F}|B_F(q)|=|A^V|=2^{|V|}$、全ファイバーと正の個数の有限列挙を人手証明と同じ順序で
形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は具体版を全層と突き合わせた後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 反復モノイドの安定像による配位集合の分割の Lean 必要十分版と導出（2026-08-17 08:42）

前 tick の Lean 具体版を構造化記述・SageMath 5 本と突き合わせ、修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStablePartition.lean` に必要十分版を追加した。ファイバーの
定義、代表元 $E(y)$ の存在一意性、異なるファイバーの非交差には型 $X$ と自己写像 $E$ だけが要り、
冪等性・反復写像であること・衝突開始位置と周期の存在は使わない。安定像の各元が自身のファイバーに
属すること（したがって個数の正値性）にだけ点ごとの冪等性 $E(E(y))=E(y)$ が要る。有限性と等号判定は
有限走査表と個数分解にだけ要り、総和は $|X|$ で止め、$2^{|V|}$ へ進む段は具体版の導出で `card_config`
により継いだ。具体版の主要主張を特殊化として導く導出定理 9 本を追加し、sorry 検査へ計 19 定理を
登録した。Lean build、sorry 検査、構造化テキスト check、PDF（44 ページ）、対応検査、SageMath 5 本を通した。
**「反復モノイドの安定像による配位集合の分割」は四層が揃い `done`。次 tick は本 tick の必要十分版と
導出をレビューし、次の対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバー間の一段発展の構造化記述（2026-08-17 09:12）

前 tick の「反復モノイドの安定像による配位集合の分割」の Lean 必要十分版と具体版からの導出を
構造化記述・SageMath 5 本と突き合わせ、SageMath・Lean build・sorry 検査を再実行した。一般の
自己写像だけで成り立つ部分、冪等性を要する部分、有限性と等号判定を要する部分の分離、および
具体版への特殊化に修正を要する不一致は見つからなかった。

次の対象「安定ファイバー間の一段発展」を立て、
`structured-latex/content/iterate-monoid-stable-fiber-dynamics.ts` に、安定像上の添字写像
$\sigma_F(q)=F(q)$、$E_F\circ F=F\circ E_F$、完全逆像
$F^{-1}(B_F(\sigma_F(q)))=B_F(q)$、ファイバーの像が次のファイバー全体とは限らない一元舞台の
定値規則による反例、有限決定を構造化記述した。有限集合・写像合成・等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の SageMath 検算（2026-08-17 10:12）

前 tick の構造化記述（添字写像 $\sigma_F$、$E_F\circ F=F\circ E_F$、完全逆像
$F^{-1}(B_F(\sigma_F(q)))=B_F(q)$、一元舞台の定値規則による反例、有限決定）をレビューし、修正を要する
誤りは見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-dynamics/` に添字写像の全単射性、
可換性、完全逆像の両包含、反例の各段と全数範囲での包含（真の包含 724 件）、有限走査を分離した 5 本を
追加し、769 写像で全て PASS。有限範囲の裏取りであり一般証明ではない。$\mathbb R/\mathbb C$ 脱出なし。
構造化テキスト check・PDF、検算対応 26 件、Lean build・sorry 検査を通した。**現在地は SageMath まで。
次 tick はレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の Lean 具体版（2026-08-17 11:12）

前 tick の SageMath 5 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableFiberDynamics.lean` に、安定像上の添字写像と全単射性、
$E_F\circ F=F\circ E_F$、ファイバーの完全逆像の両方向、像の包含、一元舞台の定値規則で像が
真部分集合になる反例、添字写像・像・完全逆像の有限走査表を人手証明と同じ順序で形式化した。
反例では $\mu_F=\lambda_F=e_F=1$ を順に導き、$E_F=F$ とした。有限集合・自然数・写像合成・
等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は Lean 具体版まで。次 tick は
全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 安定ファイバー間の一段発展の Lean 必要十分版と導出（2026-08-17 12:12）

前 tick の Lean 具体版を構造化記述・SageMath 5 本と突き合わせ（SageMath は再実行）、修正を要する
不一致は見つからなかった。`lean/CellularAutomata/NecSuf/IterateMonoidStableFiberDynamics.lean` に
必要十分版を追加した。一般層（型 $X$ と二つの自己写像 $F,E$）では、添字写像 $\sigma(q)=F(q)$ に
「$F$ が $\operatorname{range}E$ を保つこと」だけ、順方向と像の包含 $F(B(q))\subseteq B(\sigma q)$ に
点ごとの可換性 $E\circ F=F\circ E$ だけ、逆方向（完全逆像）に可換性と $F$ の $\operatorname{range}E$ 上の
単射性だけが要る。$E$ の冪等性、$E$ が反復写像であること、衝突開始位置の存在、$\sigma$ の全射性は
この章のどの主張にも要らない。反復層 $E:=E_F$ では三性質が加法則・可換性・$R_F\circ F=E_F$ から従い、
型・自己写像・衝突開始位置の存在だけで $\sigma_F$ と完全逆像が定まる（前々章の `stableStep` が経由した
$X\to X$ の等号判定は不要。等号判定のもとで一致することは別定理）。反例は相異なる二点への定値写像だけで
具体版と同じ順に $\mu_F=\lambda_F=e_F=1$、$E_F=F$ を導いた。有限性・等号判定は走査表にだけ要る。
具体版の主要主張と反例を必要十分版の特殊化として導出する定理 8 本を追加し、sorry 検査へ計 30 定理を
登録した。Lean build、sorry 検査、構造化テキスト check、PDF（45 ページ）、検算対応 26 件、
SageMath 5 本を通した。台帳末尾に重複していた 10:12 tick の節を除いた。
**「安定ファイバー間の一段発展」は四層が揃い `done`。次 tick はレビュー後、次の対象を台帳へ立てる。**

## 自動ループ tick: 安定ファイバー間の分岐個数の構造化記述（2026-08-17 13:12）

前 tick の「安定ファイバー間の一段発展」の Lean 必要十分版と具体版からの導出を全層で突き合わせ、
SageMath 5 本、Lean build、sorry 検査を再実行した。像の保存・可換性・像上の単射性の分離と、反復層・
具体版への特殊化に修正を要する不一致は見つからなかった。次の対象を、一段発展の完全逆像から内在的な
有限計数を取り出す「安定ファイバー間の分岐個数」として立てた。

`structured-latex/content/iterate-monoid-stable-fiber-predecessor-count.ts` に、一段前像集合
$\operatorname{Pre}_F(z)$ と一段前像数 $d_F(z)$、異なる配位の前像集合の非交差、完全逆像の有限非交和への
分解、保存式 $|B_F(q)|=\sum_{z\in B_F(\sigma_F(q))}d_F(z)$、局所真理値表からの有限決定を記述した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバー間の分岐個数の SageMath 検算（2026-08-17 14:12）

前 tick の構造化記述（一段前像集合 $\operatorname{Pre}_F(z)$、一段前像数 $d_F(z)$、非交差、完全逆像の
前像集合への分解、保存式 $|B_F(q)|=\sum_{z\in B_F(\sigma_F(q))}d_F(z)$、有限決定）をレビューし、修正を要する
誤りは見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-branching/` に非交差、完全逆像の所属同値の
各段、保存式の 4 等号、全組走査と有限加算を分離した 4 本を追加し、769 写像で全て PASS。有限範囲の裏取りであり
一般証明ではない。$\mathbb R/\mathbb C$ 脱出なし。構造化テキスト check・PDF（47 ページ）、検算対応 27 件、
Lean build・sorry 検査を通した。**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバー間の分岐個数の Lean 具体版（2026-08-17 15:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableFiberBranching.lean` に、一段前像集合と有限走査表、
異なる値の前像の非交差、完全逆像の有限合併への分解、保存式の 4 等号、全前像と個数の有限列挙を
人手証明と同じ順で形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
構造化テキスト check・PDF（47 ページ）、検算対応 27 件、SageMath 4 本、Lean build・sorry 検査を通した。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 安定ファイバー間の分岐個数の Lean 必要十分版と導出（2026-08-17 16:12）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、修正を要する不一致は見つからなかった。
必要十分版 `lean/CellularAutomata/NecSuf/IterateMonoidStableFiberBranching.lean` により、
一段前像集合の非交差と完全逆像の分解には写像 $F:X\to Y$ だけが要り、個数保存
$|A|=\sum_{z\in T}d_F(z)$ には「$y\in A\iff F(y)\in T$」を満たす有限集合 $A,T$ と $X$ の有限性・
$Y$ の等号判定だけが要ることを分離した。安定像・冪等性・反復・衝突開始位置は、前章の完全逆像
$F^{-1}(B(\sigma q))=B(q)$ を $A:=B(q)$, $T:=B(\sigma q)$ として代入する段階にだけ要る。
具体版の非交差・分解・個数保存を必要十分版の特殊化として導出し、全検証を通した。
**対象「安定ファイバー間の分岐個数」は四層が揃い `done`。次 tick は本 tick の必要十分版と導出を
レビューした後、次対象を立てる。**

## 自動ループ tick: 安定ファイバーの最小前周期層の構造化記述（2026-08-17 17:12）

前 tick の「安定ファイバー間の分岐個数」の Lean 必要十分版と具体版からの導出を全層で突き合わせ、
SageMath 4 本、Lean build・sorry 検査、構造化テキスト check・対応検査を再実行した。写像だけで成り立つ
前像分解、有限集合の個数保存、安定ファイバーへの特殊化に修正を要する不一致は見つからなかった。

次の対象を、既証明の最小前周期と安定ファイバーを接続する「安定ファイバーの最小前周期層」として立てた。
`structured-latex/content/iterate-monoid-stable-fiber-depth.ts` に、層
$L_F(q,k)=\{y\in B_F(q)\mid\mu(y)=k\}$、各ファイバーの一意な層分割、$\mu(y)>0$ なら
$\mu(F(y))=\mu(y)-1$、$F(L_F(q,k))\subseteq L_F(\sigma_F(q),k-1)$、層別個数の有限決定を記述した。
有限集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は構造化記述まで。次 tick はレビュー後、SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバーの最小前周期層の SageMath 検算（2026-08-17 18:12）

前 tick の構造化記述（層 $L_F(q,k)$、一意な層分割、$\mu(y)>0\Rightarrow\mu(F(y))=\mu(y)-1$、
$F(L_F(q,k))\subseteq L_F(\sigma_F(q),k-1)$、層別個数の有限決定）を前章までの定義・主張と突き合わせ、
修正を要する誤りは見つからなかった。`sagemath/check/iterate-monoid-stable-fiber-depth/` に、層分割と非交差、
最小前周期の一段減少を人手証明の二段（両不等式）と各三等号に分けた検査、正の層の降下の三段と像の包含、
$\mu\le M$・走査との一致・一度だけの振り分けによる個数分解、の 4 本を追加し、769 写像で全て PASS。
各配位の $\mu(y),\pi(y)$ は前周期・周期の組の全称文を窓 $M$ で判定して定義から求めた。有限範囲の裏取りであり
一般証明ではない。$\mathbb R/\mathbb C$ 脱出なし。構造化テキスト check・PDF（49 ページ）、検算対応 28 件、
Lean build・sorry 検査を通した。**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の
Lean 具体版だけを作る。**

## 自動ループ tick: 安定ファイバーの最小前周期層の Lean 具体版（2026-08-17 19:12）

前 tick の SageMath 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidStableFiberDepth.lean` に、最小前周期層と有限走査表、一意な層分割、
異なる層の非交差、反復の一段移送、最小前周期の一段減少を与える二つの前周期・周期組、正の層の
次ファイバーへの降下、有限区間の走査による層別個数分解を人手証明と同じ順で形式化した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。構造化テキスト check・PDF、
検算対応、SageMath 4 本、Lean build・sorry 検査を通した。**現在地は Lean 具体版まで。次 tick は全層との
対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 安定ファイバーの最小前周期層の Lean 必要十分版と導出（2026-08-17 21:42）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、一意な層分割、最小前周期の
一段減少の二方向、正の層の降下、有限走査の対象・仮定・順序に修正を要する不一致は見つからなかった。
`lean/CellularAutomata/NecSuf/IterateMonoidStableFiberDepth.lean` を追加した。層と一意な分割には
写像 $E:X\to Q$ と深さ写像 $\mu:X\to\mathbb N$ だけが要る。最小前周期の一段減少には有限型 $X$ と
自己写像 $F:X\to X$ だけが要り、安定像・冪等性・衝突開始位置は不要である。正の層の降下は、前章の
安定ファイバー間の点ごとの移送と一段減少だけから従う。有限走査にだけ $X$ の等号判定を追加した。
具体版に必要十分版からの導出定理を追加し、構造化テキスト check・PDF、SageMath 4 本、検算対応、
Lean build・sorry 検査を全て通した。**対象は四層と導出が揃い `done`。次 tick は必要十分版と導出を
全層でレビューした後、次対象を立てる。**

## 自動ループ tick: 安定ファイバーの層別完全逆像の Lean 具体版（2026-08-18 00:12）

前 tick の構造化記述と SageMath 4 本を突き合わせて再実行し、零層保存、正の層の完全逆像、零層での
二層の合流、有限走査に修正を要する不一致がなく、769 写像で全て PASS することを確認した。
`lean/CellularAutomata/IterateMonoidStableFiberLayerPreimage.lean` に、零層保存を周期組の一段移送と
最小性から示す定理、正の層と零層の完全逆像、全配位を一度ずつ走査する有限表を、人手証明と同じ順序で
形式化した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。全検証を通した。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの導出だけを作る。**

## 自動ループ tick: 安定ファイバーの層別完全逆像の Lean 必要十分版と導出（2026-08-18 02:18）

前 tick の Lean 具体版を構造化記述・SageMath 4 本と突き合わせ、零層保存、正の層の完全逆像、
零層での二層の合流、有限走査に修正を要する不一致は見つからなかった。SageMath 4 本は 769 写像で
再び全て PASS した。必要十分版では、二つの完全逆像に要る性質を、ファイバー添字の点ごとの移送同値、
零層保存、正の深さの一段減少の三つへ限定した。有限性は最小前周期の零層保存を自己写像から導く段階、
等号判定は有限走査にだけ要る。具体版の零層保存と二つの完全逆像を必要十分版の特殊化として導出し、
有限走査は一般層で分離した。有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**対象は四層と導出が揃い `done`。次 tick は本 tick の必要十分版と導出を全層でレビューし、次の
CA 内在的対象を台帳へ立てる。**

## 自動ループ tick: 層別完全逆像の必要十分版レビューと次対象の起票（2026-08-18 03:12）

前 tick の Lean 必要十分版と具体版からの導出を、構造化記述・SageMath 4 本・Lean 具体版と
突き合わせた。二つの完全逆像がファイバー添字の点ごとの移送同値・零層保存・正の深さの一段減少の
三性質だけの一般層で証明され、有限性は零層保存の導出に、有限性と等号判定は有限走査にだけ分離され、
三性質がいずれも証明で実際に使われ、証明手順が具体版と同じ順序で、具体版の三主張が特殊化として
導出されていることを確認した。修正を要する不一致は見つからなかった。SageMath 4 本（769 写像）・
検算対応・構造化テキスト check・`lake build`・sorry 検査（新 11 定理を含む）を通した。
対象表の全対象が `done` になったので、次対象「安定ファイバーの層別分岐個数」を `todo` で起票した
（一段前像数を層別に足した値が、正の層では一つ上の層の個数、零層では零層と一層の個数の和に
一致するか、およびその有限決定。既証明の層別完全逆像と一段前像分解の合成で閉じる見込み）。
**次 tick はこの対象の構造化記述だけを進める。**

## 自動ループ tick: 安定ファイバーの層別分岐個数の構造化記述（2026-08-18 04:12）

前 tick の「安定ファイバーの層別完全逆像」の必要十分版と具体版からの導出を全層で再レビューし、
三つの必要性質への仮定の限定、有限性・等号判定の分離、具体版の特殊化に修正を要する不一致は
見つからなかった。`structured-latex/content/iterate-monoid-stable-fiber-layer-branching.ts` に、
正の層では一段前像数の総和が一つ上の層の個数に等しく、零層では零層と一層の個数の和に等しい
ことを構造化記述した。証明は、既証明の層別完全逆像、一段前像集合の非交差、層の非交差だけを
合成し、有限決定も既存の全層と全一段前像数の有限走査から導いた。有限集合・自然数・写像の等号
だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。**現在地は構造化記述まで。次 tick はレビュー後、
SageMath 検算を作る。**

## 自動ループ tick: 安定ファイバーの層別分岐個数の SageMath 検算（2026-08-18 05:12）

前 tick の構造化記述を、参照先の層別完全逆像・層分割・一段前像集合の非交差・一段前像数の
定義と突き合わせ、修正を要する不一致は見つからなかった。
`sagemath/check/iterate-monoid-stable-fiber-layer-branching/` に、有限部分集合の完全逆像の
一段前像集合への分解（全部分集合 70,658 個・所属同値 542,722 件）、正の層の保存式の 4 等号
（12,737 組）、零層の保存式の 5 等号（安定ファイバー 2,201 個）、数え上げ走査と有限加算による
有限決定を分けた検算 4 本を追加した。769 写像（セル数 0 と $1\le|V|\le3$ の全初等 CA 規則）で
全て PASS。有限範囲の検算であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。
**現在地は SageMath まで。次 tick はレビュー後、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

## 自動ループ tick: 安定ファイバーの層別分岐個数の Lean 具体版（2026-08-18 06:12）

前 tick の SageMath 4 本を再実行し、有限部分集合の完全逆像分解、正の層と零層の個数保存式、
有限走査の対応に修正を要する不一致がなく、769 写像で再び全て PASS することを確認した。
`lean/CellularAutomata/IterateMonoidStableFiberLayerBranching.lean` に、有限部分集合の
完全逆像表と一段前像表の合併の一致、相異な目標配位の前像表の非交差、正の層と零層の
保存式、全層の個数と層別分岐総和の有限走査表を人手証明と同じ順序で形式化した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と具体版からの
導出だけを作る。**

## 自動ループ tick: 数値プロファイル非完全性の Lean 具体版（2026-08-19 20:12）

前 tick の SageMath 5 本を再実行し、反例表・数値プロファイル・非共役性・有限共役判定が
構造化記述と一致して全て PASS することを確認した。修正を要する不一致は見つからなかった。
`lean/CellularAutomata/IterateMonoidConjugacyNumericalProfile.lean` に、全近傍の局所規則による
有限大域写像の実現、8 配位の反例二表、反復表、安定ファイバー、深さ別個数、分岐個数、
子孫集合個数、非共役性、有限型上の共役判定の Lean 具体版を追加した。非共役性は共役による
反復移送と、4 元の子孫集合から 3 元の子孫集合への単射が不可能であることから証明した。
有限集合・自然数・写像の等号だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。
**現在地は Lean 具体版まで。次 tick は全層との対応をレビュー後、Lean 必要十分版と
具体版からの導出だけを作る。**

## 自動ループ tick: 構造保存全配位全単射の一段発展可換性（2026-08-23 22:12）

前 tick の周期成分全単射の成分内可換性をレビューし、修正を要する不一致は見つからなかった。
Lean 具体版で、固定した各周期成分の構造保存全単射の可換性を導き、一段発展前後で一意な
周期成分添字が一致することを使って全配位への接着へ移した。写像符号の等号から構成した全単射は
全配位で一段発展と可換する。有限集合・自然数・写像・従属和の等号だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。対象は実態どおり `SageMath まで`。次 tick はレビュー後、
Lean 具体版に残る有限決定可能性を形式化する。

## 自動ループ tick: 前 tick のビルド不成立の修正と完全不変量の形式化（2026-08-23 23:12）

前 tick のコミットは `lake build` を通っていなかった（記録が実態と違っていた）。構造保存全単射の定義を
有限集合の等号に沿った移送で書き直し、全配位対応の値を成分添字の一意性で述べる補題を足して修正した。
そのうえで、写像符号が等しければ共役全単射が存在すること（完全性）と、写像符号の等号が共役全単射の存在と
同値であること（完全不変量）を Lean 具体版へ追加した。有限決定可能性は未形式化なので対象は
`SageMath まで` のまま。次 tick はレビュー後、有限決定可能性を形式化する。
**教訓: 完了報告の前に `lake build` の終了コードを必ず実測する。**

## 自動ループ tick: 再帰的前像木符号と共役の有限決定（2026-08-24 00:12）

前 tick のビルド修正・完全性・完全不変量を全層で再検証し、修正を要する不一致は
見つからなかった。Lean 具体版へ、写像符号の等号と共役全単射の存在の決定可能性、
符号一致時に再帰構成した共役全単射を返す `Option`、その値の存在と共役存在の同値を追加した。
対象は `Lean 具体版まで`。次 tick はレビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 自動ループ tick: 再帰的前像木符号の完全不変量と有限決定の必要十分版（2026-08-24 01:12）

前 tick の有限決定をレビューし、Lean 具体版の符号の等号の決定可能性がファイル冒頭の選択公理
インスタンスへ解決されて実質的に空だったことを検出し、符号型 `Multiset (Finset (List ℕ))` の
等号判定を明示的に渡す形へ修正した。そのうえで完全不変量と有限決定の段の Lean 必要十分版
（同値の構成には二つの含意だけ、存在の判定には符号型の等号判定だけ、有限性も全単射の走査も
不要）と、具体版がその特殊化であることの導出を追加した。必要十分版の定理は `Classical.choice`
にも依存しない。符号の定義・共役不変性・再帰構成の段の必要十分版は未形式化なので、対象は
`Lean 具体版まで` のまま。次 tick はレビュー後、共役不変性の段の必要十分版を作る。
**教訓: 決定可能性を `inferInstance` で置くと、選択公理の局所インスタンスへ解決されて
「有限比較で決まる」という内容が消える。判定に使うインスタンスは明示的に渡す。**

## 自動ループ tick: 基点語の共役保存の必要十分版と導出（2026-08-24 04:12）

前 tick の完成符号の必要十分版をレビューし、修正を要する不一致は見つからなかった。
Lean 必要十分版に、周期長、反復列、点符号だけから基点語を作る定義と、
これら三つを全単射が保存するなら基点語も保存する定理を追加した。
型の有限性・等号判定、自己写像則、周期性、前像木の再帰構成は使わない。
具体版の基点語がこの定義の特殊化であることを等式で明示し、既存の保存定理を
必要十分版からの導出へ置き換えた。全検証は成功し、$\mathbb R/\mathbb C$ 脱出はない。
対象は実態どおり `Lean 具体版まで` のまま。次 tick は周期成分符号の保存を
必要十分版へ接続する。

## 自動ループ tick: 周期成分符号の共役保存の必要十分版と導出（2026-08-24 05:12）

前 tick の基点語保存の必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。
Lean 必要十分版に周期成分符号の段を追加した。周期長と反復列だけから作る一周期分の
有限表と、その有限表の上の付値だけから作る成分符号を定義し、全単射が周期長と各反復を
保存するなら有限表を移すこと、有限表を移し付値を保存するなら成分符号も保存されることを
証明した。付値の値の型に要るのは等号判定だけで、有限列であることは使わない。
型全体の有限性、自己写像、周期性、付値が基点語であることも使わない。
具体版の一周期表・成分符号がその特殊化であることを等式で明示し、対応する二定理を
必要十分版からの導出へ置き換えた。全検証（Lean build、sorry 検査、構造化テキスト check と
PDF、検算対応 35 件、対象 SageMath 5 本）は成功し、$\mathbb R/\mathbb C$ 脱出はない。
対象は実態どおり `Lean 具体版まで` のまま。次 tick は写像全体の符号の保存を
必要十分版へ接続する。

## 自動ループ tick: 再帰的前像木対応から打ち切り節点全単射を構成する段の必要十分版（導出は未了）（2026-08-24 11:12）

前 tick の再帰的前像木対応の必要十分版と導出をレビューし、修正を要する不一致は見つからなかった。
本 tick は Lean 必要十分版に、対応から打ち切り節点型の全単射を構成する段を追加した。
有限子表一つだけから打ち切り節点型と根節点を定義し、対応から節点全単射を再帰的に構成して、
根が根へ移ること（深さ零を含む全深さ）と、子分岐が子出現対応を先に適用してから部分木の
全単射へ再帰する形であることを分離した。型全体の有限性、自己写像、周期性、最小前周期、
二値状態、セル、近傍、局所規則は使わない。落とせなかったのは、子表の台多重集合を型として
使うための元の等号判定と、対応の存在文から全単射を取り出すための選択である。
具体版をその特殊化へ置き換える書き換えはビルドが通らず戻したため、導出は未了である。
全検証（Lean build、sorry 検査、構造化テキスト check と PDF 67 ページ、検算対応 35 件、
対象 SageMath 5 本）は成功し、$\mathbb R/\mathbb C$ 脱出はない。
対象は実態どおり `Lean 具体版まで` のまま。次 tick はまず具体版を本 tick の必要十分版の
特殊化として導出し直し（今回はビルド不成立で戻した）、その後で節点全単射の辺保存を接続する。

## 自動ループ tick: 共役全単射の値と存在の同値を必要十分版から導出（2026-08-24 20:12）

前 tick の共役存在判定の導出をレビューし、修正を要する不一致は見つからなかった。
本 tick は、前 tick で未接続だった二段を必要十分版へ接続した。符号が等しいときに有限決定が
返す値の形（`conjugacyFromMapCodeDecision_eq_some_of_necSuf`）と、`Option` に値があることと
共役全単射の存在が同値であること（`conjugacyFromMapCodeDecision_isSome_iff_of_necSuf`）を、
必要十分版の `conjugacyFromDecision_eq_some`・`conjugacyFromDecision_isSome_iff` の特殊化として
導出した。渡すのは符号型 `Multiset (Finset (List ℕ))` の等号判定、二つの自己写像、二つの写像符号、
符号の等号から全単射を作る具体版の構成、および完全不変量の同値だけである。二値状態、セル、近傍、
局所規則、前像木、周期成分、配位型の有限性はこの二段では使わない。
全検証（Lean build、sorry 検査、構造化テキスト check と PDF 67 ページ、検算対応 35 件、
対象 SageMath 5 本）は成功し、$\mathbb R/\mathbb C$ 脱出はない。
対象は実態どおり `Lean 具体版まで` のまま。完全不変量と有限決定の段は接続し終えたが、本章前段
（子符号対応・周期成分符号対応・軌道出現対応・軌道木対応・共役移送）の導出の網羅性は未確認である。
次 tick は前段の具体版と必要十分版の対応を突き合わせ、欠けている導出を洗い出して一段接続する。

## 自動ループ tick: 可逆な大域写像の巡回型の起票と構造化記述の前半（2026-08-25 02:12）

前 tick の Lean 必要十分版（共役類の集合と写像符号の像の全単射）と導出をレビューし、
仮定が同値関係・符号写像・「同値 ⟺ 符号が等しい」の両方向だけであること、具体版が
その特殊化として導出され値も一致することを確認した。修正を要する不一致は見つからなかった。

対象表に未完了対象が無くなったため、新しい対象「可逆な大域写像の巡回型」を起票した。
本 tick は構造化記述の前半を `content/reversible-global-map-cycle-type.ts` に書いた:
単射な大域写像全体 $\mathcal M^\times(V)$ の定義、可逆なら全配位が周期点であること、
周期点の周期軌道の元数が最小周期に等しいこと（$\{r<\pi\}$ から周期軌道への写像を置き、
像の一致と単射性を分けて示す）、周期軌道の元数の有限多重集合としての巡回型の定義。
住処は有限集合と自然数だけで $\mathbb R/\mathbb C$ は現れない。

検証は構造化テキスト check、PDF（70 ページ、ブロック 294 件、相互参照 773 件すべて解決）、
検算対応検査 36 件、Lean 全体ビルド（8,728 ジョブ）と sorry 検査を通した。
対象は実態どおり `構造化記述の前半まで`。次 tick は残り（巡回型の総和が $2^{|V|}$、
共役不変性、巡回型一致からの共役全単射の構成、分割との全単射）を書く。

## 2026-08-25 06:12 tick（可逆な大域写像の巡回型: Lean 具体版の周期軌道分割段）

前 tick の Lean 具体版前半（有限置換への移送、固定点を補った巡回型、和、共役保存と逆）を
構造化記述と突き合わせ、修正を要する不一致は見つからなかった。

同じ具体版 `lean/CellularAutomata/ReversibleGlobalMapCycleType.lean` に、人手証明の
「周期点の周期軌道の元数は最小周期に等しい」と「周期軌道は配位集合を分割する」に対応する段を
追加した。周期軌道を最小周期ぶんの反復列として定義し、最小周期未満の反復回数の単射性、
元数と最小周期の一致、反復到達集合との一致、自分の周期軌道への所属、周期軌道の代表取り替え、
交わる二軌道の一致を、Mathlib の巡回置換分解を使わずに形式化した。

分割の任意実現と共役類との全単射は Lean 未形式化で、対象は `SageMath まで` のまま。
次 tick はその残りを同じ具体版へ追加する。

検証: lake build 全体、sorry 検査（新 6 定理を登録）、structured-latex check、PDF 72 ページ、
検算対応検査 37 件。SageMath は本 tick で変更なし。

## 2026-08-25 10:12 tick（局所性による巡回型の制限: 起票と構造化記述）

前 tick の「分割の実現」の明示構成（有限型の一列化 → 長さごとの連続部分列 → 各列の巡回置換の合成）を
Lean 必要十分版・具体版・構造化記述と突き合わせ、巡回型の存在定理への委任が残っていないこと、
具体版が同じ構成の配位型への特殊化であることを確認した。修正を要する不一致はなかった。

対象表に未完了対象が無くなったため、新対象「局所性による巡回型の制限」を起票した。
前章の「配位数の任意の分割が可逆な大域写像の巡回型として実現する」は近傍 $N(v)=V$ の舞台の主張であり、
近傍を制限すると実現される巡回型が減るかを問う。

構造化記述 `content/locality-restricts-cycle-type.ts` を書いた。有限舞台 $(V,N)$ 上の大域写像全体
$\mathcal M(V,N)$、局所規則の族から大域写像への対応の単射性と $|\mathcal M(V,N)|=\prod_v 2^{2^{|N(v)|}}$、
単射なもの全体、実現される巡回型の集合 $\mathrm{CT}(V,N)$ とその有限決定、そして反例として
3 セル・各セルが自分自身だけを近傍とする舞台で $\mathrm{CT}=\{\{\!\{1^8\}\!\},\{\!\{2^4\}\!\}\}$ を決定し、
分割 $\{\!\{8\}\!\}$ が実現しないこと（真部分集合性）を claim にした。局所規則の個数と分割の個数の比較
（64 対 22）では反例が出ないことは remark に残した。

検証: 構造化テキスト check、PDF 76 ページ（相互参照 844 件すべて解決）、検算対応検査 37 件、
Lean 全体ビルド（8,730 ジョブ）と sorry 検査。SageMath と Lean は本 tick で変更していない。
$\mathbb R/\mathbb C$ 脱出はない。対象は `構造化記述まで`。次 tick は本章の SageMath 検算を作る。

## 2026-08-25 12:12 tick（局所性による巡回型の制限: Lean 具体版の前半）

前 tick の SageMath 検算 4 本を再実行して全て PASS を確認し、構造化記述との対応に修正を要する
不一致がないことを確認した。

Lean 具体版 `lean/CellularAutomata/LocalityRestrictsCycleType.lean` を人手証明と同じ対象・仮定・
順序で書いた。局所規則の族から大域写像への対応の単射性（基準値延長で証人配位を作る人手証明の
論法をそのまま形式化）、その帰結の個数公式 $|\mathcal M(V,N)|=\prod_v 2^{2^{|N(v)|}}$、
$\mathcal M(V,N)$・$\mathcal M^\times(V,N)$・$\mathrm{CT}(V,N)$ の定義、3 セル自己近傍舞台での
$(F\,x)(v)=g_v(x(v))$、大域単射性と各値写像の単射性の同値、単射な $A$ 上の自己写像が恒等写像か
否定写像に限ること、$F^2=\mathrm{id}$ まで。

実現巡回型が二つだけであることと真部分集合性は未形式化（file 冒頭に明記）。必要十分版も未着手。
対象は `SageMath まで` のまま。次 tick は具体版の残りを追加する。

検証: SageMath 4 本再実行、structured-latex check、PDF、検算対応検査 38 件、lake build 全体
（8,731 ジョブ）、sorry 検査（新 6 定理を登録）。$\mathbb R/\mathbb C$ 脱出はない。

## 2026-08-25 15:12 tick（自己近傍舞台の可逆大域写像群: 起票と構造化記述）

前 tick の「局所性による巡回型の制限」の必要十分版と導出を全層で再検証し、修正を要する不一致は
見つからなかった。対象表に未完了対象が無くなったため、「自己近傍舞台の可逆大域写像群」を起票した。

構造化記述 `content/self-neighborhood-reversible-map-group.ts` に、任意の有限セル集合 $V$ について、
反転集合 $S\subseteq V$ と可逆大域写像 $F_S$ の全単射、合成と対称差の一致、元数 $2^{|V|}$ の有限可換群、
および空でない反転集合に対する固定点なしの対合と巡回型 $\{\!\{2^{\,2^{|V|-1}}\}\!\}$ を証明した。
有限集合・写像・自然数だけを使い、$\mathbb R/\mathbb C$ 脱出はない。対象は `構造化記述まで`。
次 tick は前 tick のレビュー後、本章の SageMath 検算だけを作る。

## 2026-08-25 16:12 tick: 自己近傍舞台の可逆大域写像群の引用範囲の修正と SageMath 検算

前 tick の構造化記述をレビューし、引用範囲の誤りを修正した。分類 claim は任意の有限自己近傍舞台
について述べているのに、証明が前章の 3 元舞台に固定された二つの claim を根拠に引いていた
（うち一つは claim 本体でなく「その証明」を引いていた）。一般の有限 $V$ について述べた三つの claim
（大域写像の点ごとの表示、2 元状態集合上の全単射が恒等写像か否定写像に限ること、単射な大域写像の
各セルの値写像が全単射であること）を本章へ追加し、引用先を差し替えた。前章は `done` なので触らず、
一般化した主張を本章で独立に持つ形にした。

SageMath 検算 5 本を `sagemath/check/self-neighborhood-reversible-map-group/` に追加した。
1 セルから 5 セルの全 1,364 局所規則族と全 62 反転集合を厳密に走査し全て PASS。有限範囲の
全数検査であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

対象は `SageMath まで`。次 tick は本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 2026-08-25 18:12 tick: 自己近傍舞台の可逆大域写像群の Lean 必要十分版と導出

前 tick の Lean 具体版を構造化記述・SageMath 5 本と段ごとに突き合わせ、修正を要する不一致は
見つからなかった（点ごとの表示、値写像の単射性、二分類、反転集合による分類、合成と対称差、
巡回型のいずれも一致）。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/SelfNeighborhoodReversibleMapGroup.lean` を追加した。
具体版が使っていた構造のうち、次を落とせることが分かった。

- 状態集合が 2 元であることは要らない。要るのは「各元 $a$ と異なる元がちょうど $\mathrm{neg}(a)$ で
  ある」という性質だけで、そこから $\mathrm{neg}$ の対合性と固定点のなさが導ける。状態型の有限性も
  等号判定も使わない。
- 反転集合が有限部分集合であることも要らない。決定可能な述語 $P:V\to\mathrm{Prop}$ で足り、
  合成は対称差ではなく述語の排他的論理和に一致する。
- 舞台の有限性は、分類・合成・固定点のなさには要らない。要るのは巡回型を有限多重集合として
  取る段だけである。

削れなかった仮定は、添字型の等号判定（証人配位の構成）、述語の決定可能性（値の分岐）、
状態型が空でないこと（述語の相違を配位の相違へ移す）、台の有限性と等号判定（巡回型）であり、
それぞれ理由をファイル冒頭に書いた。

具体版の主要主張（点ごとの表示、値写像の単射性、二分類、反転集合の写像、対合性、合成と対称差、
反転集合の一意性、固定点のなさ、空でない反転集合と空反転集合の巡回型）を必要十分版の特殊化として
導出する定理を具体版ファイル末尾の `Derivation` 名前空間に追加した。

検証は、構造化テキストの検査と PDF（78 ページ、未解決参照 0）、SageMath 5 本（全 PASS）、
検算と証明の対応検査、`lake build`、sorry 検査（新規 24 定理を登録、全て sorryAx 非依存）を通した。
有限集合・写像・自然数・有限多重集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象「自己近傍舞台の可逆大域写像群」は四層が揃い `done`。次 tick は本 tick の必要十分版と導出を
レビューし、対象表に未完了対象が無ければ新対象を起票する。

## 2026-08-26 06:12 tick: 有限近傍割り当ての合成モノイドの Lean 必要十分版と導出

前 tick の Lean 具体版を人手証明・SageMath 検算と段ごとに突き合わせ、修正を要する不一致は
見つからなかった。

Lean 必要十分版 `lean/CellularAutomata/NecSuf/FiniteNeighborhoodAssignmentMonoid.lean` を追加した。
削れた仮定は次のとおりである。

- 単位律・結合律に舞台の有限性は要らない。要るのは有限部分集合の族を一つの有限部分集合へ
  合併するための等号判定だけである。
- 合成の始域と終域が同じ型である必要も要らない。型をまたぐ割り当てでも単位律と結合律は
  そのまま成り立ち、同じ型へ制限して初めてモノイドになる。
- 等号判定すら本質ではない。部分集合を無制限の部分集合として表せば、合成・単位律・結合律は
  型にいかなる構造も要求しない。有限表現との対応は橋渡し定理で示した。
- 有限性が要るのは元数と全列挙（合成表）の段だけである。

さらに、具体版が使う三元舞台の非可換反例は必要でないことが分かった。二元舞台で既に非可換であり、
逆に舞台が高々一元なら合成は必ず可換になる。すなわち非可換性に必要十分な条件は舞台が二元以上を
持つことである。これは前 tick の SageMath が記録した可換でない組の個数（$|V|=1$ で 0 個、
$|V|=2$ で 156 個）と一致する。

具体版の主要主張（自己近傍割り当て、両側単位律、結合律、モノイドの積と単位元、元数、合成表への
所属、非可換反例）を必要十分版の特殊化として導出する定理を、具体版ファイル末尾の `Derivation`
名前空間に追加した。

検証は、構造化テキストの検査と PDF（82 ページ、未解決参照 0）、本章の SageMath 4 本（全 PASS）、
検算と証明の対応検査、`lake build`、sorry 検査（新規 25 定理を登録、全て sorryAx 非依存）を通した。
有限集合・写像・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象「有限近傍割り当ての合成モノイド」は四層が揃い `done`。次 tick は本 tick の必要十分版と
導出をレビューし、次の対象を起票する。

## 2026-08-26 07:12 tick: 合成写像の本質的依存台の起票と構造化記述

前 tick の「有限近傍割り当ての合成モノイド」の Lean 必要十分版と導出を、構造化記述・SageMath
検算と突き合わせた。有限性・等号判定を代数法則から分離する段、有限表現との橋渡し、二元舞台の
非可換反例と一元以下での可換性に修正を要する不一致は見つからなかった。

対象表に未完了対象が無かったため、新対象「合成写像の本質的依存台」を起票した。構造化記述
`structured-latex/content/composite-map-essential-dependency.ts` に、有限配位写像の各セルの値写像と
本質的依存台割り当て $D_F$ を定義し、任意の二写像について
$D_{F\circ G}(v)\subseteq(D_F\star D_G)(v)$ となることと、その有限決定を証明した。

二元舞台の反例では、一つの入力を二つの中間セルへ複製し、その二値の相違だけを読む写像と合成する。
合成は定値写像になるため依存台は空だが、合成近傍は一元集合であり、上界は一般に真の包含となる。
有限集合・写像・二値の等号だけを使い、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF（83 ページ、未解決参照 0）、検算対応、Lean 全体ビルド、sorry 検査、
前 tick の SageMath 4 本を全て通した。対象は `構造化記述まで`。次 tick は本章をレビュー後、上界・
有限決定・真の包含反例を分離した SageMath 検算だけを作る。

## 2026-08-26 08:12 tick: 合成写像の本質的依存台の SageMath 検算

前 tick の構造化記述を、引用先の本質的依存台・合成近傍・局所規則表現の定義へ戻ってレビューした。
上界の証明が三段（本質的依存台が最小の表現集合であること、合成が合成近傍上の局所規則族で
表せること、表せることから依存台の包含が従うこと）に分かれていること、二元舞台の反例の各値を
定義から再計算し、修正を要する不一致は見つからなかった。

`sagemath/check/composite-map-essential-dependency/` に検算 4 本を追加し、全て PASS した。
上界そのもの、証明の三段（合成局所規則族 $h_v$ を明示構成して $(F\circ G)_v$ と表として突き合わせる）、
有限決定（走査組数 $|V|\cdot2^{|V|}$、走査と存在量化の一致、包含判定の有限個の所属判定への分解）、
真の包含の明示反例を別々の検算に分けた。範囲は $|V|\leq2$ の全ての写像の組の全数検査で、
$|V|=3$ は写像が $8^8$ 個あるため 8 個の値写像から作る 512 写像の部分族に限ったことを
overview に明記した。有限集合・有限写像表・0/1 と自然数の等号だけで閉じ、
浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

検証は構造化テキストの検査と PDF（83 ページ、未解決参照 0）、本章の SageMath 4 本、検算と証明の
対応検査、Lean 全体ビルド、sorry 検査を全て通した。対象は `SageMath まで`。
次 tick は本章の Lean 具体版を、人手証明と同じ対象・仮定・順序で作る。

## 2026-08-26 09:12 tick: 合成写像の本質的依存台の Lean 具体版

前 tick の SageMath 検算 4 本を再実行し、構造化記述との対応に修正を要する不一致は見つからなかった。
Lean 具体版では、値写像、本質的依存台割り当て、依存台上の局所規則族を定義し、人手証明と同じ三段
（各写像の依存台上での表現、合成近傍上での合成表現、表現可能性からの依存台包含）で上界を証明した。
依存台所属の有限決定可能性も型として記録した。二元舞台の明示反例は有限真理値表を全展開し、
合成の依存台が空で合成近傍が一元集合となる真の包含を証明した。$\mathbb R/\mathbb C$ 脱出はない。
対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出を作る。

## 2026-08-26 10:12 tick: 合成写像の本質的依存台の Lean 必要十分版と導出

前 tick の Lean 具体版を構造化記述・SageMath と突き合わせ、三段の順序と反例の依存台の値が一致し、
修正を要する不一致は見つからなかった。

Lean 必要十分版を追加した。包含の主張に要るのは、合成近傍を作る舞台側の等号判定と、状態側の
「各元と異なる元が入れ替え写像の値に一意に定まること」だけである。舞台の有限性は依存台上での
基準値延長による表現と依存台を有限集合として集める段にだけ要り、状態の有限性・等号判定は依存台の
所属判定にしか要らないので、決定手続きを仮定として外へ出した。反例も一般化し、真の包含が起きるには
舞台が相異なる二セルを、状態型が相異なる二値を持てば足り、二セル舞台であることも二元状態で
あることも要らないことを示した。具体版の依存台割り当て、包含定理、反例の二写像、真の包含を
必要十分版の特殊化として導出した。

検証は Lean 全体ビルド、sorry 検査（新規 16 定理を追加）、構造化テキストの検査と PDF、
検算対応検査、本章の SageMath 4 本を通した。$\mathbb R/\mathbb C$ 脱出はない。対象は `done`。
次 tick は本 tick の必要十分版と導出をレビューし、新対象を起票する。

## 2026-08-26 11:12 tick: 必要十分版の検査入口修正と近傍割り当ての包含順序の構造化記述

「合成写像の本質的依存台」の Lean 必要十分版が検査入口 `lean/CellularAutomata.lean` から直接
import されておらず、前 tick の sorry 検査の記録が実態と一致していなかったため、import を追加して
全体ビルドと sorry 検査へ接続した。対象表に未完了対象が無くなったので、新対象
「近傍割り当ての包含順序と合成の単調性」を起票し、構造化記述
`content/ordered-neighborhood-assignment-monoid.ts` を書いた。

## 2026-08-26 12:12 tick: 近傍割り当ての包含順序の SageMath 検算

前 tick の構造化記述を、合成近傍・自己近傍割り当て・有限近傍割り当てモノイドの定義および主張と
突き合わせ、修正を要する不一致は見つからなかった。

`sagemath/check/ordered-neighborhood-assignment-monoid/` に検算 4 本を追加した。部分順序の三性質、
合成の単調性（証明の含意列を四段に分け、片側単調性も別に検査）、包含判定の有限走査と走査回数
$|V|^2$、有限モノイド公理・部分順序・積の単調性の同時成立をそれぞれ分離した。反射律は
$|V|\leq3$ の全 530 個、包含判定は $|V|\leq3$ の全 262,405 組、残りは $|V|\leq2$ の全数で検査し
全て PASS。有限範囲の全数検査であり一般証明ではない。浮動小数点と $\mathbb R/\mathbb C$ 脱出はない。

検証は本章の SageMath 4 本、検算と証明の対応検査、構造化テキストの検査と PDF（86 ページ、
未解決参照 0）、Lean 全体ビルドと sorry 検査を通した。対象は `SageMath まで`。
次 tick は本検算をレビュー後、本章の Lean 具体版を人手証明と同じ対象・仮定・順序で作る。

## 2026-08-26 13:12 tick: 近傍割り当ての包含順序の Lean 具体版

前 tick の SageMath 検算 4 本を構造化記述と突き合わせ、部分順序三性質、合成の単調性の四段、
有限走査と定義の同値、走査組数 $|V|^2$ に修正を要する不一致がないことを確認した。

Lean 具体版 `lean/CellularAutomata/OrderedNeighborhoodAssignmentMonoid.lean` に、点ごとの包含、
反射律・反対称律・推移律、合成近傍の両引数単調性、$V\times V$ の有限走査と包含判定の同値、
走査表の元数、決定可能性、既証明のモノイド積と合わせた有限順序モノイドの性質を、人手証明と
同じ順序で形式化した。検査入口と sorry 検査対象へ全定理を登録した。有限集合・有限写像・自然数だけで
閉じ、$\mathbb R/\mathbb C$ 脱出はない。対象は `Lean 具体版まで`。
次 tick は全層レビュー後、Lean 必要十分版と具体版からの導出だけを作る。

## 2026-08-26 14:12〜23:12 tick の記録の所在

この期間の 7 tick 分（近傍割り当ての包含順序の Lean 必要十分版と導出、点ごとの和と合成の
分配性の三層、点ごとの積と合成の非分配性の三層、非分配反例の最小舞台の起票と構造化記述）は
MEMORY.md への追記が漏れており、`docs/tasks/auto-loop-state.md` の該当 tick 節にのみ残っている。
内容を後から再構成して書き足すことはせず、所在だけを記録する。

## 2026-08-27 00:12 tick: 非分配反例の最小舞台の SageMath 検算

前 tick の「点ごとの積に対する合成の非分配反例の最小舞台」の構造化記述を、合成近傍・点ごとの積の
定義および前章の非分配性の主張と突き合わせ、修正を要する誤りは見つからなかった。

`sagemath/check/neighborhood-assignment-intersection-minimal-counterexample/` に検算 4 本を追加した。
一元以下での合成と点ごとの積の一致、そこから従う左右の分配律、二元舞台の左右の明示証人の式変形、
最小舞台元数の走査をそれぞれ別ファイルに分け、証明の各段を段ごとに検査した。一元以下は
$|V|=0,1$ の全数、最小性は $|V|=0,1,2$ の全三つ組（1、8、4,096）で検査し全て PASS。$|V|=2$ の
反例個数は左右とも 420 で、前章の走査記録と一致した。有限集合・有限写像・自然数の等号だけを使い、
$\mathbb R/\mathbb C$ 脱出はない。二元舞台の証人検査は個別例であり、反例全体の分類ではない。

検証は本章の SageMath 4 本、前章の SageMath 5 本の再実行、検算と証明の対応検査（47 件）、
構造化テキストの検査、PDF（92 ページ、未解決参照 0）、Lean 全体ビルドと sorry 検査を通した。
対象は `SageMath まで`。次 tick は本検算をレビュー後、本章の Lean 具体版を人手証明と同じ
対象・仮定・順序で作る。

## 2026-08-27 01:12 tick: 非分配反例の最小舞台の Lean 具体版

前 tick の SageMath 検算 4 本を再実行して構造化記述と突き合わせ、修正を要する不一致は
見つからなかった。

Lean 具体版では、舞台元数が一以下なら舞台の任意の二元が等しいことを使い、合成近傍の存在証人を
唯一の元へ同定する向きと、対象セルを証人に取る逆向きを分けて、合成近傍と点ごとの積の一致を
形式化した。この一致を各合成へ順に適用して左右の分配律を導いた。二元舞台では人手証明と同じ
左右の近傍割り当てを定義し、対象セルで空集合と一元集合が現れる各計算と非等号を形式化した。
一元以下での反例の非存在、二元舞台の元数、左右の反例の存在をまとめた最小性の定理も置いた。
検査入口へ接続し、新規 12 定理を sorry 検査対象へ登録した。有限集合・有限写像・自然数だけで閉じ、
$\mathbb R/\mathbb C$ 脱出はない。対象は `Lean 具体版まで`。次 tick は全層レビュー後、Lean
必要十分版と具体版からの導出だけを作る。

## 2026-08-27 11:12 tick: 自己転置な近傍割り当ての合成閉性の起票と構造化記述

前 tick の「自己転置な近傍割り当ての個数」の Lean 必要十分版と導出を全層・検査入口・sorry 検査
対象と突き合わせ、修正を要する不一致は見つからなかった。

新対象「自己転置な近傍割り当ての合成閉性」を起票し、構造化記述に、自己転置な二つの割り当ての
合成が自己転置であることと両者の可換性の同値を記述した。二元舞台では自己ループ一つの割り当てと
二点を結ぶ対称な割り当てがともに自己転置だが可換でなく、合成が自己転置でない明示反例になる。
有限決定も記述した。有限集合・有限部分集合・自然数だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

対象は `構造化記述まで`。次 tick は本章をレビュー後、SageMath 検算だけを作る。

## 2026-08-27 15:12 tick: 自己転置な近傍割り当て全体の合成閉性の特徴づけ

前 tick の Lean 必要十分版と導出を全層・検査入口と突き合わせ、修正を要する不一致は
見つからなかった。前 tick が明示的に保留した逆向きを新対象として起票し、構造化記述に、
自己転置な近傍割り当て全体が合成で閉じることと舞台元数が一以下であることの同値を証明した。
相異なる二元がある向きは任意の有限舞台上の明示証人、一元以下の向きは合成の所属条件の一致から
導いた。有限集合と有限部分集合だけで閉じ、$\mathbb R/\mathbb C$ 脱出はない。

構造化テキストの検査と PDF、前章の SageMath 4 本、検算対応検査、Lean 全体ビルド、sorry 検査を
通した。対象は `構造化記述まで`。次 tick は本章の SageMath 検算だけを作る。

## 2026-08-28 10:12 tick: 合成冪等な近傍割り当ての特徴づけの Lean 必要十分版と導出

前 tick の Lean 具体版を構造化記述・SageMath 検算と突き合わせ、修正を要する不一致は
見つからなかった。Lean 必要十分版と具体版への導出を追加し、対象は四層が揃い `done`。

確定したこと。合成冪等性と「推移性かつ二段分解可能性」の同値、および自己近傍を含む場合の
同値には、舞台の有限性も等号判定も要らない（近傍の値を `Set` で書けばインスタンスなしで通る）。
二条件が互いを含意しない反例に要るのは相異なる二元だけで、三元舞台は不要である
（$N(a)=\{a,b\}$、$N(b)=\{a\}$）。これは 08:12 tick の SageMath 全数走査の発見と一致する。
二段分解可能性の側は相異なることすら使わない。有限性と等号判定が要るのは、値を `Finset` で
表して合成を `Finset.biUnion` で書く段と、決定可能性を有限走査へ落とす段だけである。

構造化テキスト検査と PDF（103 ページ）、SageMath 5 本、検算対応検査 54 件、Lean 全体ビルド、
sorry 検査を通した。$\mathbb R/\mathbb C$ 脱出はない。

# Slack配送経路

- 論文公開通知は旧Workflow Builder triggerを使わず、`slack route-post math`が解決するHex-AIの明示routeだけへ送る。
## 公開通知の正規ルート応答検証（2026-08-27）

`scripts/publish-artifact.sh` が `slack route-post math` の成功応答を旧配送先
`local-pc-management` に固定していたため、SSOT が選んだ `math-ai-notification` への投稿成功後に
tick を exit 1 にしていた。チャンネル名の知識を呼出元から除去し、中央Slack経路がSSOTを照合した後の
応答について、リポジトリ名と非空のチャンネル名・チャンネルID・Slack時刻だけを検証する独立ヘルパーへ
置き換えた。配送先変更を受理し、別リポジトリや欠落フィールドを拒否する回帰テストを追加した。
