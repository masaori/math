# cellular-automata-statistical-mechanics

**連続物理が $\mathbb{R}$ の対象に帰している内容のうち、どこまでを 2 値セルオートマトンの
可算構造が担えるかを確定する。**

> 前提は [`docs/context/`](../docs/context/) の三レイヤー（着手前に全て読む）。
> このプロジェクトは**理論物理レイヤーの中心命題を、CA という具体的な舞台で実行する試み**である。
> 既存理論を目標仕様にせず、有限・可算な構造から数学を作り直す研究姿勢は
> [`docs/マニフェスト.md`](docs/マニフェスト.md) を正本とする。

## 定義

用語と定義の正本は
[`docs/2値セルオートマトンの定義と呼び名.md`](docs/2値セルオートマトンの定義と呼び名.md)。

**呼び名の規約（厳守）**: 修飾語を付けない「2 値セルオートマトン」は、
**舞台にも状態集合にも構造を仮定していない**ことを意味する。構造を入れたときは必ず名前に付ける
（「群上の」「ポート付きグラフ上の」「$\mathbb{F}_2$-線形な」など）。

## なぜ CA を舞台に選ぶのか

理論物理レイヤーは「物理の内容を担う決定可能な可算部分集合は何か」を問う。
その舞台として CA が特別なのは、**規則が有限データで尽きる**からである。

| 舞台 | 規則を書くのに要る数 | 非可算の混入 |
|---|---|---|
| **2 値 CA** | 有限の真理値表。重みは $\{0,1\}$ | **規則にはゼロ**（下記の注意） |
| 有理重みの格子模型 | $\mathbb{Q}$ | $e^{-\beta E}$ を書いた瞬間に出る |
| 代数的重みの模型 | $\overline{\mathbb{Q}}$ | 同上 |
| 連続場の理論 | $\mathbb{R}/\mathbb{C}$ | 最初から全面的に |

「自然は有限」という前提を最も素直に実装した物理模型のクラスであり、
非可算が本当に必要な場所を切り分ける実験場として理想的である。
さらに、格子模型が $\{0,1\}$ 重みに退化したものが CA なので、
**統計力学の道具（転送行列・分配関数・自由エネルギー）がそのまま使える。**
分配関数は純粋な数え上げ $Z_N=\operatorname{Tr}T^N\in\mathbb{N}$ になる。

> **注意（誤解しやすい）**: 「CA には非可算が入らない」は**不正確**である。
> 規則は常に有限データだが、**舞台のセル集合を無限にすると配位空間 $A^V$ は非可算**になる
> （$\{0,1\}^{\mathbb{N}}$ は Cantor 空間で濃度は連続体）。
> **舞台を無限にすることが CA における非可算への脱出点であり、
> これは統計力学の熱力学極限に対応する理想化である。**
> したがって代数構造を拾うときは、**有限舞台で定義できるか無限舞台を要するか**を必ず記録する。

## 問い

既存文献の大半は **「CA が連続物理を再現する／創発させる」** という向きで書かれている
（確率的 CA からフェルミオン場の理論へ、量子 CA から Dirac 方程式へ、CA から有向浸透の臨界指数へ）。

**このプロジェクトは矢印を逆に読む。**

> 連続理論の側が $\mathbb{R}$ の対象に帰している内容は、実は CA の可算構造が担っている。
> **連続極限は内容を生む操作ではなく、内容を落として決定可能性を失う操作である。**
> ならば、落ちる前の可算な担い手を一つずつ同定できるはずだ。

したがって具体的な問いは三つ。

1. **同定** — 連続物理の各概念（自由エネルギー、温度、可積分性、中心電荷、フェルミオン、臨界性）
   について、CA 側の可算な担い手は何か。それは決定可能か。
2. **境界** — 可算側で担えない部分はどこか。**$\mathbb{R}$ が本当に付け加えているものは何か。**
   これを正直に同定することが、この立場の誠実さを担保する。
3. **構成** — CA だけから、$\mathbb{R}$ を一度も経由せずに熱力学（$\Omega, S, \beta, \Phi$）を
   構成しきれるか。

## 探索の順序 — まず数学、あとで物理

**物理側の代数構造を起点にすると、探索が確証バイアスで狭くなる。**
物理が既に名前を付けた構造しか見つからず、CA 側に自然に存在するが物理に対応物がない構造は
原理的に視界に入らない。実際、最初の調査は経路をすべて物理側の終着点で定義していたため、
次元群・オートマトン群・Perron 数・ゼータ関数といった純粋に数学側の構造を丸ごと落としていた。

そこで探索を二相に分ける。

| 相 | やること | 物理を見るか |
|---|---|---|
| **第一相** | 2 値 CA に施せる操作を網羅し、出てくる代数構造をカタログ化する | **見ない** |
| **第二相** | カタログと物理側の構造を突き合わせる | 見る |

**第二相で対応物が見つからなかった構造こそが最も価値がある。**
枠組みは [`docs/survey/CAから出発する代数構造の探索.md`](docs/survey/CAから出発する代数構造の探索.md)。

## 成果物

1. **CA 側の代数構造カタログ**（第一相）— 2 値 CA に施せる操作ごとに、出てくる代数構造と
   その不変性・決定可能性・識別力を記録する。
   → [`docs/survey/CAから出発する代数構造の探索.md`](docs/survey/CAから出発する代数構造の探索.md)
2. **可算コアの台帳**（第二相）— 連続物理の概念ごとに「CA 側の担い手／決定可能か／$\mathbb{R}$ でしか
   言えない残り」を記録した表。→ [`docs/survey/連続物理の可算な担い手.md`](docs/survey/連続物理の可算な担い手.md)
3. **$\Lambda$ 値熱力学の構成** — CA と加法的保存量から $\Omega\in\mathbb{N}$, $S\in\Lambda$,
   $\beta\in\Lambda$, $\Phi_N\in\Lambda$ を構成し、機械検証する。**$\mathbb{R}$ 脱出ゼロ**を実証する。
4. **決定可能な述語の表** — 可積分性・可解性・可逆性・保存量の有無を有限検査に落とし、
   小さい規則空間を総当たりする。「解ける模型かどうか」が決定可能になることの実例。
5. **失われるものの同定** — $\mathbb{R}$ 側にしかない内容の特定。これも成果である。

**件数ではなく、「可算側で厳密に述べ直され、機械にかけられ、既存研究と差分がある」ことで評価する。**

## 研究の段取り（射程・現在地・完了条件）

舞台の広さと規則クラスの二軸で研究の射程を段階に分け、各段階の依存・現在地・完了条件を
`structured-latex/research-roadmap.ts` に正本として固定する。段取りは出版物（PDF / HTML）の
本文の前に載り、公開ページからも読める。**初等セルオートマトンは最初の有限校正の対象であって
射程の全体ではない。** 状態集合は 2 元集合に固定し、多値・環値への一般化は段取りに入れない。

段取りと公開物の一致は次で検査する（`--published` を付けると公開ページも取得して照合する）。

```sh
cellular-automata-statistical-mechanics/scripts/verify-roadmap-artifact.sh --published
```

30 分ごとの継続作業は
[`docs/tasks/auto-loop-runbook.md`](docs/tasks/auto-loop-runbook.md) と
[`docs/tasks/auto-loop-state.md`](docs/tasks/auto-loop-state.md) を正本として進める。

## 現段階での見取り図

既存研究は**互いにほとんど交流のない独立した経路**として存在する。
経路ごとの調査と、矢印を逆に読み直した結果は [`docs/survey/`](docs/survey/) にある。

最も重要な既存の事実は二つ。

- **可積分 CA（箱玉系）** — 頂点模型を $q\to0$（結晶化）で潰すと、
  楕円関数も Bethe 方程式も消え、**有限集合上の組合せ写像だけが残る**。
  それでもソリトン・散乱・保存量はすべて生き残る。
  **$\mathbb{C}$ の装置がほとんど内容を運んでいなかったことの、既存研究による証明**である。
  この立場にとって最強の実例。
- **量子 CA の指数** — 1 次元 QCA の完全不変量は**正の有理数**、すなわち $\Lambda$ の元である。
  連続極限で $\mathbb{R}$ 値の中心電荷になる量が、格子上では決定可能な可算不変量として存在する。
  **「$\mathbb{R}$ 値の連続量の可算な祖先を探す」という作業の雛形。**

## 構成

```text
cellular-automata-statistical-mechanics/
├── README.md              # このファイル（ゴール設定）
├── MEMORY.md              # 引き継ぎメモ
├── docs/
│   ├── 2値セルオートマトンの定義と呼び名.md   # 用語・定義の正本
│   ├── 舞台のカタログ_有限構造とその極限.md   # 舞台を有限から集め、極限の向きで整理
│   ├── 局所的極限の定式化.md                   # 有限→無限の極限をどう定義するか（決着済み）
│   ├── 有限代数系から無限代数系が現れる機構.md # 純粋数学レイヤー。群に限らない五つの機構
│   ├── survey/            # 既存研究の調査と、矢印を逆に読み直した台帳
│   └── ideas/             # アイディアの種（命題になる前の仮説）
└── sagemath/check/        # 検算
```

探索フェーズでは Markdown で速く回す。命題として固まったら構造化テキストへ移し、
[`docs/context/証明の書き方.md`](../docs/context/証明の書き方.md) の四層を適用する。

## やらないこと

- CA の計算万能性・複雑性クラス分類そのもの（Wolfram クラス、Rule 110 万能性）は、
  可算コアの同定に効く範囲でのみ触れる。
- 数値シミュレーションによる現象論的探索（パターン分類、機械学習）はしない。
  **厳密計算と有限検査だけを使う。**
- 「CA が現実の物理である」ことの主張・検証はしない。
  扱うのは**どの数学構造がどの内容を担うか**であって、自然科学的な当否ではない。
