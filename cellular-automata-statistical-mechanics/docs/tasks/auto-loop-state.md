# 自動ループ 状態台帳

手順の正本は [auto-loop-runbook.md](auto-loop-runbook.md)、研究姿勢の正本は
[マニフェスト](../マニフェスト.md) である。このファイルは進捗だけを持つ。

## 現在地

- 2026-08-14: 30 分ごとの自動ループを開始した。最初の対象は、物理的意味を入れずに局所真理値表だけから
  定まる「本質的依存台」である。
- 2026-08-14（同日 tick）: 構造化証明基盤を `structured-latex/` に初期化した
  （リポジトリ直下のシステムを使い、住処 `habitat` / `realEscape` のメタデータは
  `exact-solution-of-2d-ising-model-lambda` の流儀を流用。Lean 未着手なので lean 連携検査は未設置）。
  対象「本質的依存台の有限決定」の構造化記述を `content/essential-dependency.ts` に書いた:
  状態集合・局所真理値表・入れ替え写像・一点反転写像・本質的依存・本質的依存台の定義、
  「本質的依存 ⟺ 一点反転検査」の同値（証明つき）、「supp(f) は高々 |S|·2^{|S|} 回の
  A の元の等号検査で決定できる」（証明つき）。元の個数の記法だけ habitat "N"、有限集合・写像・
  述語を扱う残りのブロックは habitat "finite" とし、ℝ/ℂ は現れない。
  `pnpm run check` と `pnpm run build:pdf`（3 ページ、未解決参照 0）を通した。
  SageMath 層は次の同日 tick で完了した。
- 2026-08-14（同日 tick）: 前 tick の構造化記述をレビューし、入れ替え写像が 2 元集合の値の場合分け
  だけを使うこと、本質的依存の存在量化から一点反転へ移る論証、比較回数の上界に誤りがないことを
  確認した。`sagemath/check/essential-dependency-support/` に、2 元集合の異なる元の一意性、
  本質的依存と一点反転検査の同値、走査で得る依存台と比較回数上界の検算を分けて追加した。
  $|S|=0,1,2,3$ の全 278 真理値表（$|S|=3$ の 256 規則を含む）を有限集合上で厳密に全数検査し、
  全て PASS。浮動小数点と ℝ/ℂ 脱出はない。構造化記述と検算の対応検査も追加した。
  次 tick は Lean 具体版を、人手証明と同じ対象・仮定・順序で作る。
- 2026-08-15（00:12 tick）: 前 tick の SageMath 検算と構造化記述を突き合わせ、修正を要する誤りは
  見つからなかった。`lean/` を初期化し（lib 名 `CellularAutomata`、toolchain・mathlib は既存
  プロジェクトと同じ v4.32.1、依存は 2 次元側の `.lake` を clone copy で持ち込み）、Lean 具体版
  `lean/CellularAutomata/EssentialDependency.lean` を人手証明と同じ対象・仮定・順序で書いた。
  状態集合は演算を入れない 2 元帰納型、ν は値の場合分け、φ_w は u = w か否かの場合分け、
  同値の証明は人手証明の両方向（外延性による x = x' の矛盾、A∖{a}={ν(a)} からの x' = φ_w x）を
  そのまま写した。有限決定可能性は「存在文が有限個の論理和として決定できること」
  （`Decidable` インスタンスと `mem_supp_iff`）と「走査する組 (w,x) の総数が |S|·2^{|S|}」
  （`card_scan_pairs`）として形式化した。**比較回数のコストモデル自体は形式化していない**
  （ファイル冒頭に明記）。`lake build` と `scripts/check-no-sorry.sh`（4 定理とも sorryAx 非依存）、
  構造化テキストの check・build:pdf、SageMath 3 本、対応検査を全て通した。
  次 tick は Lean 必要十分版と、具体版がその特殊化であることの導出を作る。
- 2026-08-15（00:42 tick）: 前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・証明順序が
  一致し、修正を要する誤りがないことを確認した。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/EssentialDependency.lean` を追加した。一点反転との同値に必要なのは
  添字型の等号判定と、状態型上で「各元と異なる元が入れ替え写像の値に一意に定まる」性質だけであり、
  添字型・状態型の有限性は不要である。有限決定可能性には両型の有限性と状態の等号判定を追加し、
  走査組数は一般の有限状態型について $|S|\,|A|^{|S|}$ とした。具体版の決定可能性インスタンスを
  必要十分版の特殊化へ置き換え、同値と走査組数が 2 元状態型への特殊化で得られる導出定理を追加した。
  構造化テキスト、PDF、SageMath 3 本、対応検査、Lean build、sorry 検査を全て通した。
- 2026-08-15（01:12 tick）: 前 tick の Lean 必要十分版と導出をレビューし、必要十分版の仮定
  「各元と異なる元が ν の値に一意」が同値の証明で実際に使う性質そのものであること、導出定理が
  状態型と ν の特殊化になっていることを確認した。修正を要する誤りは見つからなかった。
  対象「冗長近傍からの独立性」の構造化記述を `content/redundant-neighbor.ts` に書いた:
  制限写像 ρ^T_S、冗長拡大 (T, f∘ρ^T_S)、基準値延長写像 ι^T_S（ρ∘ι が恒等になることを含む）の
  定義と、「冗長拡大は足した元 w∈T∖S に本質的に依存しない」「w∈S への本質的依存は冗長拡大で
  変わらない」「supp(f∘ρ^T_S)=supp(f)（T の部分集合としての等号）」の 3 つの claim（証明つき）。
  同一視を使わず、A^T と A^S の行き来は ρ と ι の 2 本の写像だけを通した。全ブロック habitat
  "finite"、ℝ/ℂ なし。構造化テキスト check・build:pdf（5 ページ、未解決参照 0）、既存の
  SageMath 3 本、対応検査、Lean build、sorry 検査を全て通した。この章の SageMath は未着手。
  次 tick は本章の SageMath 検算を作る。
- 2026-08-15（01:42 tick）: 前 tick の「冗長近傍からの独立性」の構造化記述をレビューした。
  3 つの claim の数学的内容に反例はなかったが、基準値延長・制限・冗長拡大の計算で複数の等号を
  一行へまとめた箇所が「一ステップ一定理」に違反していたため、各等号と根拠を分離した。
  `sagemath/check/redundant-neighbor-independence/` に、追加元の反転が制限で消えること、
  $\rho^T_S\circ\iota^T_S$ が恒等写像であること、追加元への非依存、元の添字への依存の移送、
  本質的依存台の集合としての一致を別々に検査する 5 本を追加した。$|T|\le3$ の全ての
  $S\subseteq T$・全局所真理値表・必要な全入力を有限集合上で厳密に検査し、全て PASS。
  浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。構造化テキスト check・build:pdf（5 ページ、
  未解決参照 0）、既存を含む SageMath 8 本、対応検査、Lean build、sorry 検査を全て通した。
  次 tick は本章の Lean 具体版を、人手証明と同じ対象・仮定・順序で作る。
- 2026-08-15（02:12 tick）: 前 tick の SageMath 5 本と構造化記述の対応をレビューし、修正を要する
  誤りは見つからなかった（依存台一致の検算が $S$ の位置番号を $T$ の元へ写してから比較している
  ことも確認）。「冗長近傍からの独立性」の Lean 具体版
  `lean/CellularAutomata/RedundantNeighbor.lean` を人手証明と同じ対象・仮定・順序で書いた。
  $T$ を有限型、$S$ を `Finset T` とし、$A^S$ は部分型上の写像で表す。$A^S$ と $A^T$ の行き来は
  人手証明どおり制限写像 `restrict`（ρ）と基準値延長写像 `baseExtend`（ι、既定値 0）の 2 本
  だけを通し、同一視をしない。ρ∘ι が恒等（`restrict_baseExtend`）、追加元への非依存
  （`no_essentialDep_on_added_element`。一点反転が制限で消えることから同値定理を適用）、
  依存の移送（`essentialDep_transfer`。証人を ι で延長・ρ で制限）、依存台の等号
  （`supp_extendRule`。右辺は supp f を包含写像で `Finset T` へ写した像）を形式化した。
  構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、sorry 検査（新 4 定理を
  検査対象へ追加、全て sorryAx 非依存）を全て通した。
  次 tick は本章の Lean 必要十分版と、具体版がその特殊化であることの導出を作る。
- 2026-08-15（02:42 tick）: 前 tick の Lean 具体版を人手証明と突き合わせ、制限写像・基準値延長写像の
  2 本だけを通ること、3 つの claim と証明順序が一致することを確認し、修正を要する不一致は
  見つからなかった。Lean 必要十分版 `lean/CellularAutomata/NecSuf/RedundantNeighbor.lean` を追加した。
  制限・冗長拡大・依存の移送には添字型の等号判定と延長用の基準値だけが要り、添字型・状態型の
  有限性は不要である。追加元への非依存は具体版と同じ一点反転検査で証明し、状態側に要る性質を
  「各元と異なる元が入れ替え写像の値に一意に定まること」へ限定した。依存台の等号より前にある
  点ごとの必要十分条件にも有限性は要らず、有限性が要るのは具体版で依存述語を `Finset` として
  集める段階だけである。具体版の 4 定理が必要十分版の特殊化から得られる導出定理も追加した。
  sorry 検査の一時ファイル作成を macOS の `mktemp` で確実に動く一時ディレクトリ方式へ修正した。
  構造化テキスト check・PDF、SageMath 8 本、対応検査、Lean build、sorry 検査を全て通した。
  次 tick は本 tick の必要十分版と導出をレビューし、「時間展開上の直接依存」の構造化記述を作る。
- 2026-08-15（03:12 tick）: 前 tick の Lean 必要十分版と導出をレビューし、仮定が証明で実際に使う
  性質（添字型の等号判定、延長用の基準値、状態側の「別値の一意性」）に限られていること、証明手順が
  具体版と同じであること、具体版の 4 定理が特殊化として導出されていることを確認した。修正を要する
  誤りは見つからなかった。対象「時間展開上の直接依存」の構造化記述を
  `content/time-expansion-dependency.ts` に書いた: 有限舞台 (V,N)、有限舞台上の 2 値 CA、
  大域写像 F（各セルの値写像が冗長拡大 f_v∘ρ^V_{N(v)} に等しいことを定義内で明示）、時間区間
  [0,τ]_ℕ、イベント集合 E_τ=[0,τ]_ℕ×V、一段依存関係 D_τ（t=s+1 かつ u∈supp(f_v)）の定義と、
  4 つの claim（証明つき）: 大域写像の u での一点反転による値の変化 ⟺ u∈supp(f_v)（前二章の
  一点反転同値と依存台不変性の合成。依存関係の定義に N(v) でなく supp(f_v) を採る根拠）、
  |E_τ|=(τ+1)·|V|、D_τ の有限性と所属の有限決定可能性（ℕ の等号 1 回＋高々 |N(v)|·2^{|N(v)|} 回の
  比較）、一段依存で時刻が厳密に増える（s<t。次対象「推移閉包の反対称性」の入力）。時間は ℕ の
  大小比較と後者だけを使い、ℝ/ℂ なし（時間・個数のブロックは habitat "N"、残りは "finite"）。
  構造化テキスト check・build:pdf（7 ページ、未解決参照 0）、SageMath 8 本、対応検査、Lean build、
  sorry 検査を全て通した。この章の SageMath・Lean は未着手。次 tick は本章の SageMath 検算を作る。
- 2026-08-15（03:42 tick）: 前 tick の「時間展開上の直接依存」の構造化記述を、前二章の定義・
  主張および用語の正本と突き合わせ、修正を要する不一致は見つからなかった。
  `sagemath/check/time-expansion-dependency/` に検算 6 本を追加した。大域写像の一点反転による変化と
  局所規則の本質的依存台への所属、時間区間の個数、イベント集合の個数、一段依存関係の所属条件、
  依存台走査の比較回数上界、一段依存での時刻増加をそれぞれ分け、$|V|\leq3$ の全ての近傍・
  局所真理値表を含む有限範囲で厳密に検査して全て PASS。これは一般の有限集合に対する証明ではない。
  浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。次 tick は本章の Lean 具体版を、人手証明と同じ
  対象・仮定・順序で作る。
- 2026-08-15（04:12 tick）: 前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する
  誤りは見つからなかった（一段依存の検算が全セル同一規則の範囲に限られる点は、所属条件が
  組 $(N(v),f_v)$ ごとに分解されるため検証範囲の記載どおりで、主張の反例探索には影響しない）。
  「時間展開上の直接依存」の Lean 具体版 `lean/CellularAutomata/TimeExpansionDependency.lean` を
  人手証明と同じ対象・仮定・順序で書いた。有限舞台は有限型 `V` と `N : V → Finset V`、局所規則の
  族は前二章と同じく部分型 `↥(N v)` 上の写像で表し、大域写像の各セルの値写像が冗長拡大
  `extendRule` に等しいことを定義の一致（`globalMap_eq_extendRule`）で明示した。
  大域一点反転の特徴づけ（`globalFlip_iff_mem_supp`）は人手証明と同じ順で前二章の
  `essentialDep_iff_flip`・`mem_supp_iff`・`supp_extendRule` を順に適用した。時間区間は
  `Finset.range (τ+1)`（所属 ⟺ $t\leq\tau$ を別定理で明示）、イベント集合の個数は直積の積の法則、
  一段依存関係は `E_τ ×ˢ E_τ` の filter とし、所属の言い換え・supp ⊆ N(v) の含意・部分集合性・
  走査組数 $|N(v)|\cdot2^{|N(v)|}$（前章 `card_scan_pairs` の適用）・時刻の厳密増加を形式化した。
  比較回数のコストモデル自体は前二章と同じく形式化していない（ファイル冒頭に明記）。不要な仮定は
  `omit` で明示した。構造化テキスト check・PDF、SageMath 14 本、対応検査、Lean build、sorry 検査
  （新 10 定理を検査対象へ追加）を全て通した。次 tick は本章の Lean 必要十分版と、具体版がその
  特殊化であることの導出を作る。
- 2026-08-15（04:42 tick）: 前 tick の Lean 具体版を人手証明と突き合わせ、対象・仮定・
  証明順序に修正を要する不一致は見つからなかった。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/TimeExpansionDependency.lean` を追加した。大域写像と一点反転の
  特徴づけには舞台・状態型の有限性は要らず、舞台の等号判定、状態側の別値の一意性、
  制限を逆向きに移送するための基準値一つだけが要る。イベント集合の個数には有限舞台と
  選んだ有限時間集合だけが要る。一段依存の時刻増加は、任意の時間型上の次時刻写像と
  関係に対する `lt s (next s)` だけから導いた。具体版の大域写像、一点反転の特徴づけ、
  イベント個数、時刻増加がそれぞれ必要十分版の特殊化で得られる導出定理も追加した。
  構造化テキスト check・PDF（7 ページ、未解決参照 0）、SageMath 14 本、対応検査、Lean build、
  sorry 検査を全て通した。「時間展開上の直接依存」は四層が揃い `done`。次 tick はこの必要十分版と
  導出をレビューし「依存の推移閉包の反対称性」の構造化記述を作る。
- 2026-08-15（05:12 tick）: 前 tick の Lean 必要十分版と導出をレビューした。証明手順・特殊化の
  明示は具体版と一致していたが、必要十分版のイベント集合（`eventSet`・`card_eventSet`）が
  `DecidableEq Time`・`DecidableEq V` を仮定していた。`Finset` の直積の構成と個数の積の法則に
  等号判定は使われないので、「実際に使う性質だけを残す」要件への違反として両仮定を削除し、
  要るのが選んだ時間集合の有限性と舞台の有限性だけであることをコメントに明記した（導出定理側にも
  `omit [DecidableEq V]` を追加）。修正後に Lean build・sorry 検査を通した。
  台帳先頭の未完了対象「依存の推移閉包の反対称性」の構造化記述を
  `content/transitive-closure-antisymmetry.ts` に書いた: 依存経路（長さ n≥1 の写像
  p:[0,n]_ℕ→E_τ で隣接対が D_τ に入るもの）、到達可能関係 C_τ（依存経路の存在。有限集合）、
  反射的到達可能関係 ⪯_τ（等号または C_τ）、部分順序（反射・反対称・推移の 3 条件）の定義と、
  5 つの claim（証明つき）: 依存経路に沿った時刻の厳密増加（経路長についての帰納法。前章の
  claim_time_strictly_increases と ℕ の推移性だけを使う）、D_τ ⊆ C_τ（長さ 1 の経路）、
  C_τ の推移性（経路の連結）、C_τ が D_τ を含む最小の推移的関係であること（推移閉包の名の根拠）、
  相互到達の不存在（s<t かつ t<s から ℕ の非反射性で矛盾）とその特例の非反射性、
  ⪯_τ が E_τ 上の部分順序であること。時間・個数のブロックは habitat "N"、残りは "finite"、
  ℝ/ℂ なし。構造化テキスト check・build:pdf（9 ページ、未解決参照 0）、SageMath 14 本、
  対応検査、Lean build、sorry 検査を全て通した。この章の SageMath・Lean は未着手。
  次 tick は本章の SageMath 検算を作る。
- 2026-08-15（05:42 tick）: 前 tick の「依存の推移閉包の反対称性」の構造化記述を、前章の
  一段依存関係・時刻増加の主張と突き合わせ、修正を要する不一致は見つからなかった。
  `sagemath/check/transitive-closure-antisymmetry/` に検算 6 本を追加した。CA 固有の規則より広く、
  隣接時刻間の任意の有限関係について、経路に沿う時刻増加、長さ 1 の経路、経路連結と推移性、
  推移的上位関係に対する最小性、相互到達・自己到達の不存在、反射閉包の部分順序性を別々に検査した。
  $|V|\leq2,\ 0\leq\tau\leq3$ と $|V|=3,\tau=1$ の全 4,900 関係で全て PASS。最小性は
  $|E_\tau|\leq3$ の全候補関係も列挙した。一般の有限集合に対する証明ではない。浮動小数点と
  $\mathbb{R}/\mathbb{C}$ 脱出はない。次 tick は検算と構造化記述の対応をレビューし、Lean 具体版を作る。
- 2026-08-15（06:12 tick）: 前 tick の SageMath 6 本と構造化記述の対応をレビューし、修正を要する
  不一致は見つからなかった（検算対象を隣接時刻間の任意の有限関係へ広げている点は、人手証明が
  一段依存から使う性質が時刻の厳密増加だけなので、主張の反例探索を狭めない。overview に明記済み）。
  「依存の推移閉包の反対称性」の Lean 具体版
  `lean/CellularAutomata/TransitiveClosureAntisymmetry.lean` を人手証明と同じ対象・仮定・順序で
  書いた。依存経路は「n ≥ 1、各 i ≤ n で p(i) ∈ E_τ、各 i < n で隣接対が D_τ に入る」の
  3 条件（`IsDepPath`）、到達可能関係 C_τ は依存経路の存在で定義し、E_τ × E_τ の部分集合ゆえ
  有限という定義中の注記も `reachable_subset_event_product`・`reachable_finite` として形式化した。
  経路に沿う時刻増加（経路長の帰納法。基底は前章 `time_strictly_increases`、帰納段は制限と
  ℕ の推移性）、長さ 1 の経路による D_τ ⊆ C_τ、経路連結による推移性（継ぎ目 i = m の
  well-defined 性は p(m) = b = q(0) の等式）、推移的上位関係に対する最小性（経路長の帰納法）、
  相互到達の不存在（時刻増加を両向き＋ℕ の推移性と非反射性）、非反射性（特例）、反射的到達可能
  関係 ⪯_τ の部分順序性（反射は a = a、反対称は相互到達の不存在、推移は 4 場合分け）の 8 定理を
  形式化し、sorry 検査の対象へ追加した（最小性の仮定 R ⊆ E_τ × E_τ は人手証明の主張どおり
  仮定に置き、証明では使わないことを明記）。構造化テキスト check・PDF（9 ページ・未解決参照 0）、
  SageMath 20 本、対応検査、Lean build、sorry 検査を全て通した。
  次 tick は本章の Lean 必要十分版と、具体版がその特殊化であることの導出を作る。
- 2026-08-15（06:42 tick）: 前 tick の「依存の推移閉包の反対称性」の Lean 具体版を人手証明と
  突き合わせ、経路長の帰納法、経路連結、時刻増加の両向き適用、反射閉包の 4 場合分けが同じ順で
  形式化されており、修正を要する不一致がないことを確認した。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/TransitiveClosureAntisymmetry.lean` を追加した。経路・到達可能性・
  推移閉包にはイベント集合と一段関係だけが要る。相互到達の不存在と反射閉包の部分順序性には、
  任意の時刻型への写像、推移的かつ非反射的な時刻関係、一段関係の各辺で時刻が増えることだけが
  要り、舞台・状態・近傍・局所規則・自然数のその他の構造は不要である。イベント集合の有限性は
  到達可能関係を有限集合として述べる注記だけに分離した。具体版の経路・到達可能性・有限性・
  一段包含・推移性・最小性・相互到達と自己到達の不存在・部分順序性が必要十分版の特殊化として
  得られる導出定理を追加した。構造化テキスト、PDF、SageMath 20 本、対応検査、Lean build、
  sorry 検査を全て通した。「依存の推移閉包の反対称性」は四層が揃い `done`。
  次 tick はこの必要十分版と導出をレビューし、「有限伝播境界」の構造化記述を作る。
- 2026-08-15（07:12 tick）: 前 tick の「依存の推移閉包の反対称性」の Lean 必要十分版と導出を
  レビューし、修正を要する不一致は見つからなかった（仮定は時刻写像・推移的かつ非反射的な
  時刻関係・一段ごとの時刻増加に限られ、イベント集合の有限性は到達可能関係の有限性の注記だけに
  使われ、8 定理すべてに具体版への導出定理がある）。台帳先頭の未完了対象「有限伝播境界」の
  構造化記述を `content/finite-propagation-boundary.ts` に書いた: 依存経路に沿って時刻が
  ちょうど 1 ずつ増えること（$t=s+n$。前章の $s<t$ の等式への精密化。一段依存の条件 $t=s+1$ と
  帰納法）、伝播球 $B_1(v)=\operatorname{supp}(f_v)$、
  $B_{n+1}(v)=\bigcup_{u\in\operatorname{supp}(f_v)}B_n(u)$ の再帰的定義、伝播球の有限性と
  個数の再帰的上界 $|B_{n+1}(v)|\leq\sum_{u\in\operatorname{supp}(f_v)}|B_n(u)|$、依存経路の
  始点セルが伝播球に属すること（帰納法）、依存元集合 $P_\tau(t,v)=\{a\mid(a,(t,v))\in C_\tau\}$ の
  定義と、有限伝播境界 $P_\tau(t,v)\subseteq\bigcup_{n\in[1,t]_{\mathbb{N}}}(\{t-n\}\times B_n(v))$、
  個数上界 $|P_\tau(t,v)|\leq\sum_{n\in[1,t]_{\mathbb{N}}}|B_n(v)|$（$|V|$ にも $\tau$ にも依存しない）、
  特例 $P_\tau(0,v)=\emptyset$。時間・個数のブロックは habitat "N"、残りは "finite"、ℝ/ℂ なし。
  構造化テキスト check・build:pdf（11 ページ、未解決参照 0）、SageMath 20 本、対応検査、
  Lean build、sorry 検査を全て通した。この章の SageMath・Lean は未着手。
  次 tick は本章の SageMath 検算を作る。
- 2026-08-15（07:42 tick）: 前 tick の「有限伝播境界」の構造化記述を、一段依存・依存経路・
  到達可能関係の定義と突き合わせ、伝播球の再帰の向き、経路長と時刻差、依存元集合の包含と個数上界に
  修正を要する不一致は見つからなかった。`sagemath/check/finite-propagation-boundary/` に検算 4 本を
  追加した。経路の正確な時刻差、伝播球の再帰式と有限合併の個数上界、経路始点セルの伝播球所属、
  依存元集合の包含・個数上界・時刻 0 での空性を分けて検査した。$|V|\leq3$ の全 531 個の本質的
  依存台の族と $0\leq\tau\leq4$ を組み合わせた 2,655 個の組で全て PASS。一般の有限舞台に対する
  証明ではない。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。次 tick は検算と構造化記述の
  対応をレビューし、Lean 具体版を作る。

- 2026-08-15（08:12 tick）: 前 tick の「有限伝播境界」の SageMath 4 本と構造化記述の対応を
  レビューし、修正を要する不一致は見つからなかった（検算の一段依存関係・伝播球・境界合併の
  定義が構造化記述と一致し、個数上界は包含と境界の個数を経由して連鎖で検査されている）。
  「有限伝播境界」の Lean 具体版 `lean/CellularAutomata/FinitePropagationBoundary.lean` を
  人手証明と同じ対象・仮定・順序で書いた。supp(f_v) を V の部分集合へ写した像（前章の
  一段依存の所属条件と同じ表し方）を `suppV` と置き、経路の正確な時刻差 t = s+n（経路長の
  帰納法。基底は一段依存の条件 t = s+1、帰納段は ℕ の加法の結合則）、伝播球の再帰
  （n = 0 は定義域外の番人値と明記。有限性は `Finset` の型が表す）と個数
  （|B_1(v)| = |supp(f_v)| と合併の個数の和の上界）、経路始点セルの伝播球所属（経路長の
  帰納法）、依存元集合（E_τ の部分集合ゆえ有限の注記つき）、有限伝播境界の包含・個数上界
  （部分集合の個数 → 合併の個数の和 → 一元集合との直積の順で人手証明と同じ）・時刻 0 での
  空性の 12 定理を形式化し、sorry 検査の対象へ追加した。伝播球の定義と個数に V の有限性が
  不要なことは `omit [Fintype V]` で明示した（次 tick の必要十分版の最初の検査項目になる）。
  構造化テキスト check・PDF、SageMath 24 本、対応検査、Lean build、sorry 検査を全て通した。
  次 tick は本章の Lean 必要十分版と、具体版がその特殊化であることの導出を作る。

- 2026-08-15（08:42 tick）: 前 tick の「有限伝播境界」の Lean 具体版を人手証明と突き合わせ、
  経路長の帰納法、伝播球の再帰、依存元集合の境界包含と個数上界が同じ順で形式化されており、
  修正を要する不一致は見つからなかった。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/FinitePropagationBoundary.lean` を追加した。経路の時刻差には
  自然数時刻と一段ごとの後者だけ、伝播球にはセル型の等号判定とセルごとの有限な直接依存元集合だけ、
  依存元集合の有限性には舞台全体の有限性でなく選んだイベント集合の有限性だけが要る。
  具体版の主要定義・定理が必要十分版の特殊化として得られる導出定理を追加した。全層の検証を通し、
  「有限伝播境界」は四層が揃い `done`。次 tick はこの必要十分版と導出をレビューし、
  「依存順序から得る部分構造」の構造化記述を作る。

- 2026-08-15（09:12 tick）: 前 tick の「有限伝播境界」の Lean 必要十分版と導出をレビューし、
  必要十分版の依存元個数上界（`card_dependencySourceSet_le`）が証明で使わない仮定
  `X.Finite`（イベント集合の有限性）を持っていたのを見つけて削除した（個数上界は境界が
  `Finset` であることだけで通る。イベント集合の有限性が要るのは依存元集合の有限性の注記
  だけである。具体版の導出定理側の適用も合わせて修正）。「仮定は実際に使う性質だけ」の
  要件への修正で、他に修正を要する不一致は見つからなかった。
  台帳先頭の未完了対象「依存順序から得る部分構造」の構造化記述を
  `content/dependency-order-substructures.ts` に書いた: イベント集合 E_τ と反射的到達可能関係
  ⪯_τ だけから内在的に定義できる部分構造として、順序凸部分集合、下方集合、上方集合、
  非比較関係 ⊥_τ、反鎖、時刻切片 E_τ^{(t)}、一段境界 ∂K を定義し、7 つの claim（証明つき）:
  下方集合は順序凸（使わない仮定も明記）、上方集合は順序凸、順序凸集合の共通部分は順序凸、
  反鎖は順序凸（前章の反対称性が効く場合分け）、時刻切片は反鎖（依存経路に沿う時刻の
  厳密増加と ℕ の非反射性）、下方集合には外から入る一段依存が存在しない、およびその帰結として
  下方集合の一段境界が外向きの一段依存だけで定まること。物理的な意味（因果・光円錐）は
  持ち込まず、既存因果構造との比較は別対象に残した。時刻切片の claim だけ habitat "N"、
  残りは "finite"、ℝ/ℂ なし。構造化テキスト check・build:pdf（13 ページ、未解決参照 0）、
  SageMath 24 本、対応検査、Lean build、sorry 検査を全て通した。この章の SageMath・Lean は
  未着手。次 tick は本章の SageMath 検算を作る。

- 2026-08-15（運用変更）: 構造化証明から自己完結 HTML を生成し、`hexagonal-computation/artifacts` へ
  公開する処理を追加した。以後は正常終了かつ clean な tick だけ、公開 URL の HTTP 200 を確認してから
  URL つき Slack 通知を一度送る。tick 内のエージェントによる旧通知は廃止し、同じ論文版の重複通知も
  外部マーカーで防ぐ。公開物は一時閲覧用であり、リポジトリ文書から恒久リンクしない。

- 2026-08-15（09:42 tick）: 前 tick の「依存順序から得る部分構造」の構造化記述を、既出の
  一段依存・到達可能関係・反射的到達可能関係の定義と突き合わせ、数学的な不一致は見つからなかった。
  記録上は claim が 7 つあるのに 6 つと数えていたため訂正した。
  `sagemath/check/dependency-order-substructures/` に検算 6 本を追加し、下方集合と上方集合の順序凸性、
  順序凸集合の共通部分、反鎖の順序凸性、時刻切片の反鎖性、下方集合への外向きからの一段依存の
  不存在、下方集合の境界の外向き表示を分けて検査した。$|V|\leq2$、$0\leq\tau\leq2$ の
  隣接時刻間の全 283 関係と、各イベント集合の全ての部分集合で全て PASS。一般の有限舞台に対する
  証明ではない。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。次 tick は検算と構造化記述の
  対応をレビューし、Lean 具体版を作る。

- 2026-08-15（10:12 tick）: 前 tick の「依存順序から得る部分構造」の SageMath 6 本と構造化記述の
  対応をレビューし、修正を要する不一致は見つからなかった（検算の補助定義——推移閉包・反射閉包・
  下方集合・上方集合・順序凸・非比較・反鎖・一段境界——が構造化記述の定義と一致し、検査対象を
  CA から得る関係より広い隣接時刻間の任意の関係としている点も overview に明記済み）。
  「依存順序から得る部分構造」の Lean 具体版
  `lean/CellularAutomata/DependencyOrderSubstructures.lean` を人手証明と同じ対象・仮定・順序で
  書いた。順序凸・下方集合・上方集合・反鎖は `Finset (ℕ × V)` 上の述語、非比較関係と一段境界は
  集合として定義し、⪯_τ・C_τ・D_τ は前二章の定義だけを通した。7 つの claim（下方集合と上方集合の
  順序凸性、順序凸集合の共通部分、反鎖の順序凸性［a = c か否かの場合分けで反対称性と推移性を適用］、
  時刻切片の反鎖性［経路の時刻増加と ℕ の非反射性］、下方集合へ外から入る一段依存の不存在
  ［D_τ ⊆ C_τ と下方性］、下方集合の一段境界の外向き表示［両包含］）と、定義中の 2 つの注記
  （非比較関係の入れ替え対称性、一段境界の有限性）を形式化し、sorry 検査の対象へ追加した。
  人手証明の主張が置く部分集合条件（J ⊆ E_τ 等）は、証明で使わない場合も主張どおり仮定に置き、
  使わないことを引数名の下線で明示した。構造化テキスト check・PDF（13 ページ・未解決参照 0）、
  SageMath 30 本、対応検査、Lean build、sorry 検査を全て通した。
  次 tick は本章の Lean 必要十分版と、具体版がその特殊化であることの導出を作る。

- 2026-08-15（10:42 tick）: 前 tick の Lean 具体版を構造化記述と突き合わせ、7 つの claim と
  定義中の 2 注記が同じ仮定・場合分け・証明順序で形式化されていることを確認した。修正を要する
  不一致は見つからなかった。Lean 必要十分版では、下方・上方・順序凸性には集合と関係だけ、
  反鎖の順序凸性には関係の反対称性と推移性だけ、時刻切片の反鎖性には時刻写像・非反射的な
  時刻関係・相異なる二点間での時刻増加だけ、一段境界には一段関係が順序関係に含まれることだけが
  要ると確定した。舞台・状態・近傍・局所規則・自然数・有限性は、有限境界の注記における対象集合の
  有限性を除いて不要である。具体版の 7 claim と 2 注記が必要十分版の特殊化であることを別定理で
  導出した。構造化テキスト、PDF、SageMath 30 本、対応検査、Lean build、sorry 検査を全て通した。
  「依存順序から得る部分構造」は四層が揃い `done`。次 tick は必要十分版と導出をレビューし、
  「既存因果構造との比較」の構造化記述に着手する。

- 2026-08-15（11:12 tick）: 前 tick の「依存順序から得る部分構造」の Lean 必要十分版と導出を
  レビューし、修正を要する不一致は見つからなかった（仮定は各 claim で実際に使う性質——集合と
  関係、反対称性と推移性、時刻写像と非反射的な時刻関係と相異なる二点間の時刻増加、一段関係の
  順序への包含——に限られ、7 claim と 2 注記のすべてに具体版への導出定理がある）。
  台帳先頭の未完了対象「既存因果構造との比較」の構造化記述を
  `content/causal-structure-comparison.ts` に書いた。内容は組 $(E_\tau,\preceq_\tau,D_\tau)$
  だけから述べられる順序側の性質に固定した: 区間 $I_\tau(a,b)$ の定義と有限性
  （$|I_\tau(a,b)|\leq(\tau+1)\cdot|V|$）、被覆関係 $\lessdot_\tau$（$E_\tau$ と $C_\tau$
  だけから定義。局所真理値表を参照しない）の定義、$D_\tau\subseteq\lessdot_\tau$
  （一段依存の時刻差 1 と ℕ の「$s<r\iff s+1\leq r$」による中間イベントの排除）、
  $\lessdot_\tau\subseteq D_\tau$（長さ 2 以上の経路から中間イベント $p(1)$ を構成して被覆に矛盾）、
  両包含による $D_\tau=\lessdot_\tau$（一段依存関係が $(E_\tau,C_\tau)$ から復元できる。保存される構造）、
  および反例 claim「順序を保つ全単射は時刻写像を保つとは限らない」（$V=\{v\}$、定値 $f_v$、
  $\tau=1$ で $\preceq_\tau$ が等号に退化し、時刻を入れ替える全単射が順序を保つ。
  時刻写像は $(E_\tau,\preceq_\tau)$ から定まらない。失われる情報）。文献が「causal set」等の
  名で定める公理系との同定は、一次文献の定義を確認した後の層で行うと章内の remark に明記し、
  二次情報に基づく同定は記していない。時間・個数の claim は habitat "N"、残りは "finite"、
  ℝ/ℂ なし。構造化テキスト check・build:pdf（15 ページ、未解決参照 0）、SageMath 30 本、
  対応検査、Lean build、sorry 検査を全て通した。この章の SageMath・Lean は未着手。
  次 tick は本章の SageMath 検算を作る。

- 2026-08-15（12:42 tick）: 前 tick の「既存因果構造との比較」の構造化記述を、依存経路・
  到達可能関係・反射的到達可能関係の既出定義と突き合わせ、修正を要する不一致は見つからなかった。
  「既存因果構造との比較」の SageMath 検算 5 本を追加し、区間の有限性、一段依存と被覆関係の
  両包含および集合としての一致、順序を保つ
  全単射が時刻を保たない具体的反例を別々に検査した。一般側は $|V|\leq2$、
  $0\leq\tau\leq2$ の隣接時刻間の全 283 関係を対象とし、9,521 区間、1,061 一段辺、
  一段辺でない到達可能対 449 組を厳密に検査して全て PASS。反例側は一元舞台・定値局所規則・
  $\tau=1$ を構造化記述どおり構成し、2 イベント上の 4 順序対で順序保存と時刻非保存を確認した。
  これは一般の場合の証明ではない。浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。
  次 tick は検算と構造化記述の対応をレビューし、本章の Lean 具体版を作る。

- 2026-08-15（13:12 tick、レビューのみ）: 前 tick の SageMath 5 本を再実行し（全て PASS、
  検証↔証明の対応検査も通過）、構造化記述と突き合わせた。反例側の検算が構造化記述の構成
  （一元舞台、自己近傍、定値局所規則、本質的依存台が空、$\tau=1$、2 イベントの入れ替え）と
  一致すること、一般側が「一段依存で時刻が一つ増える」だけを使う隣接時刻間の任意関係を
  対象にしていることを確認し、修正を要する不一致は見つからなかった。この tick は開始時刻が
  遅く（実測 13:12、強制終了 13:39）、Lean 具体版を sorry なしで完了できないため新規層に
  着手しなかった。次 tick は Lean 具体版を作る。再利用する既存 Lean の定義:
  `TransitiveClosureAntisymmetry.lean` の `Reachable`（$C_\tau$）・`ReflReachable`（$\preceq_\tau$）・
  `path_time_strictly_increases`・`reachable_transitive`・`reachable_irreflexive`、
  `DependencyOrderSubstructures.lean` の `Finset` 上の部分構造の書き方。

- 2026-08-15（13:42 tick）: 前 tick の「既存因果構造との比較」の SageMath 5 本を再実行し、
  全て PASS、検証と構造化ラベルの対応も通過した。一般側の検算が隣接時刻間の任意の有限関係を
  対象とし、反例側が一元舞台・自己近傍・定値局所規則・自然数時刻 0 と 1 の入れ替えを構成して
  いることを構造化記述と再度突き合わせ、修正を要する不一致は見つからなかった。
  Lean 具体版 `lean/CellularAutomata/CausalStructureComparison.lean` を人手証明と同じ対象・仮定・
  順序で追加した。区間をイベント集合内の反射的到達可能関係による二重条件として定義し、有限性と
  個数上界をイベント集合への包含から形式化した。被覆関係を到達可能対のうち到達可能な中間イベントを
  持たない対として定義し、一段依存から被覆への包含は時刻差 1 と経路時刻増加から、逆包含は長さ 2
  以上の経路の中間点 `p 1` から、人手証明どおり示した。両包含による関係の等号も形式化した。
  時刻非保存の反例は、一元舞台・自己近傍・定値 0 規則では一段依存と到達可能関係が空になることを
  証明し、イベント `(0,v)` と `(1,v)` を交換する全単射が反射的到達可能関係を保存する一方で時刻を
  変えることとして構成した。`lake build` と sorry 検査を通し、新しい 6 定理はいずれも `sorry` /
  `admit` を使わない。この章の次層である Lean 必要十分版と具体版からの導出は未着手。
  次 tick は Lean 具体版を構造化記述と突き合わせ、必要十分版と導出を作る。
- 2026-08-15（14:12 tick）: 前 tick の Lean 具体版 `CausalStructureComparison.lean` を構造化記述と
  突き合わせた。区間・被覆関係の定義、一段依存⊆被覆（時刻差 1 と経路時刻増加）、被覆⊆一段依存
  （長さ 2 以上の経路の中間点 p(1)）、等号、一元舞台の定値規則による時刻非保存の反例が、いずれも
  人手証明と同じ対象・仮定・順序であり、修正を要する不一致は見つからなかった。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/CausalStructureComparison.lean` を追加した。順序区間には集合と関係だけが
  要り、有限性・個数上界には集合の有限性だけが要る（上界は $|X|$ で述べ、$(\tau+1)|V|$ は特殊化で
  現れる）。一段関係⊆被覆には D⊆C、C に沿った時刻増加、D の対の時刻の間に時刻がないこと（隣接性）
  だけが要り、自然数時刻・後者関数は不要。被覆⊆一段関係には C が X 内の D 経路の到達可能関係で
  あることだけが要り、時刻は不要。等号には D の両端が X に属すること・時刻関係の推移性・一段の
  時刻増加・隣接性が要る。時刻非保存の反例には一段関係が空であること、X 内に時刻の異なる二点、
  イベントの等号判定（入れ替え写像の定義用）だけが要り、舞台・近傍・局所規則は不要。具体版の
  区間・被覆の定義が必要十分版の特殊化に定義として等しいこと（`rfl`）、到達可能関係の集合としての
  一致、および区間の有限性・個数上界、一段⊆被覆、被覆⊆一段、反例の 6 定理が必要十分版から
  導出できることを追加した。`lake build` と sorry 検査（新 13 定理を登録、全て sorryAx 非依存）を
  通した。構造化テキストと SageMath はこの tick で変更していない。この章の四層が揃ったので
  status を done にする。次 tick は本 tick の必要十分版と導出をレビューし、対象表の全対象が done
  になったことを受けて、runbook「最初の探索列」の末尾（カタログ化後の既存因果構造との比較の
  一次文献確認）を新対象として台帳へ起こすかを判断する。

- 2026-08-15（14:42 tick）: 前 tick の「既存因果構造との比較」の Lean 必要十分版と導出を、
  具体版・構造化記述と突き合わせた。区間、被覆関係の両包含、時刻非保存反例の証明順序と仮定の
  限定に修正を要する不一致は見つからなかった。Bombelli–Lee–Meyer–Sorkin (1987) の本文で、
  因果集合が局所有限な半順序集合として定義されること、連続時空への近似には順序を一致させる
  埋め込みだけでなく一様密度と尺度分離も要求されることを確認した。この定義との恒等写像による
  比較を `docs/survey/CA依存順序と因果集合の一次文献比較.md` に記録した。
  有限な $(E_\tau,\preceq_\tau)$ は因果集合の数学的定義を満たし、$D_\tau$ は被覆関係として
  復元できる。一方、比較先の多様体・埋め込み・密度・尺度・誤差または収束概念は未定義なので、
  連続時空の近似や物理的因果との同一視は主張できない。時刻写像が順序から復元できない反例も
  因果集合の定義とは矛盾しない。次 tick はこの一次文献比較をレビューし、比較結果を構造化記述へ
  反映するか、近似を扱わず調査結果のまま確定するかを実態に即して判断する。

- 2026-08-15（15:12 tick）: 前 tick の一次文献比較 `docs/survey/CA依存順序と因果集合の一次文献比較.md` を、
  既証明の部分順序性・区間有限性・被覆関係の等号・時刻非保存の反例と突き合わせ、比較の内容
  （恒等写像のもとで局所有限な部分順序集合の定義を満たす、時刻写像は復元できない、連続時空近似は
  未定義のため主張しない）に修正を要する不一致は見つからなかった。一次文献の本文そのものは
  この tick で再取得しておらず、定義の引用は前 tick の確認記録に依る。比較のうち数学的主張に
  なる部分だけを構造化記述へ移し、対象「因果集合の一次文献との照合」を数学的命題として扱う。
  `structured-latex/content/causal-set-primary-literature.ts` に、局所有限な部分順序集合の定義
  （順序の言葉だけ。有限集合上では区間有限性が自動で従うことを定義内に明記）、claim
  「$(E_\tau,\preceq_\tau)$ は局所有限な部分順序集合であり、その区間は前章の $I_\tau(a,b)$ と
  集合として等しい」（証明は既証明の部分順序性・イベント集合の有限性・区間有限性の合成）、
  一次文献の出典と範囲を述べる remark、主張しないこと（連続時空近似・物理的因果・光円錐・
  時刻写像の復元）を列挙する remark を書いた。全ブロック habitat "finite"、$\mathbb{R}/\mathbb{C}$
  脱出なし。構造化テキスト check・PDF（16 ページ・未解決参照 0）、対応検査、Lean build、sorry 検査を
  通した。**この章の現在地は構造化記述まで。次 tick はレビュー後にこの章の SageMath 検算を作る**
  （検査対象は隣接時刻間の任意の有限関係で、部分順序性・区間の有限性・二つの区間定義の一致を分ける）。

- 2026-08-15（15:42 tick）: 前 tick の「因果集合の一次文献との照合」の構造化記述を、一次文献比較の
  調査記録および既証明の部分順序性・区間有限性と突き合わせ、修正を要する不一致は見つからなかった。
  `sagemath/check/causal-set-primary-literature/` に検算 3 本を追加し、反射的到達可能関係の部分順序性、
  一次文献の区間の有限性、一次文献の区間と前章の区間の集合としての一致を分けて検査した。
  $|V|\leq2$、$0\leq\tau\leq2$ の隣接時刻間の全 283 関係について、部分順序性を全数検査し、
  9,521 区間の有限性と二つの定義による区間の一致を全て PASS。一般の有限舞台に対する証明ではない。
  浮動小数点と $\mathbb{R}/\mathbb{C}$ 脱出はない。**この章の現在地は SageMath まで。次 tick は
  検算と構造化記述の対応をレビューし、人手証明と同じ対象・仮定・順序の Lean 具体版を作る。**

- 2026-08-15（16:12 tick）: 前 tick の SageMath 3 本を再実行し（全て PASS）、構造化記述との対応を
  レビューして修正を要する不一致は見つからなかった。Lean 具体版
  `lean/CellularAutomata/CausalSetPrimaryLiterature.lean` に、一次文献の区間 $A_R(x,y)$
  （`literatureInterval`）、局所有限な部分順序集合（`IsLocallyFinitePartialOrderOn`：前章の
  `IsPartialOrderOn` と全区間の有限性の連言）、定義中の注記「有限集合上の区間は常に有限」
  （`literatureInterval_finite_of_finset`）、区間の一致 $A_{\preceq_\tau}(a,b)=I_\tau(a,b)$
  （`literatureInterval_eq_orderInterval`：両者の所属条件が同一なので `Iff.rfl`）、
  $(E_\tau,\preceq_\tau)$ が局所有限な部分順序集合であること（`eventOrder_locally_finite`：
  既証明の `reflReachable_partial_order` と、区間の一致を経由した `orderInterval_finite`）を
  人手証明と同じ順序で形式化した。近似・物理的因果・時刻写像の復元は形式化の対象にしていない
  （人手証明の remark と同じ範囲）。注記の補題にセル型の有限性・等号判定が不要なことが `omit` の
  位置から見えている（必要十分版の最初の検査項目）。入口 import と sorry 検査の対象へ追加し、
  `lake build`・sorry 検査・構造化テキスト check・PDF（16 ページ・未解決参照 0）・対応検査を通した。
  **この章の現在地は Lean 具体版まで。次 tick は具体版と人手証明の対応をレビューし、
  Lean 必要十分版と具体版の導出を作る**（区間・局所有限性には集合と関係だけが要り、
  区間の有限性には台集合の有限性だけが要る見込み）。

- 2026-08-15（16:42 tick）: 前 tick の Lean 具体版を構造化記述・既証明の部分順序性・区間有限性と
  突き合わせ、定義、主張、証明順序が一致し、修正を要する不一致は見つからなかった。Lean 必要十分版
  `lean/CellularAutomata/NecSuf/CausalSetPrimaryLiterature.lean` に、任意の集合 $X$ と関係 $R$ だけから
  一次文献の区間と局所有限な部分順序集合を定義した。区間は常に $X$ の部分集合であり、その有限性に
  要るのは $X$ の有限性だけで、部分順序性すら不要である。したがって、有限な $X$ 上の任意の部分順序が
  局所有限になることを、具体版と同じ順序で証明した。具体版の区間、有限集合上の区間有限性、局所有限性
  述語、$(E_\tau,\preceq_\tau)$ の局所有限部分順序性を必要十分版の特殊化として導出した。セル、二値状態、
  近傍、局所規則、自然数時刻は必要十分版に現れず、連続時空への近似・物理的因果・時刻写像の復元も
  形式化していない。**この章の四層と導出が揃ったため現在地は done。**

- 2026-08-15（17:12 tick）: 前 tick の「因果集合の一次文献との照合」の Lean 必要十分版と導出を、具体版・
  構造化記述と突き合わせた。区間・局所有限性の定義に集合と関係だけを使うこと、区間有限性が台集合の
  有限性だけから従うこと（部分順序性不要）、具体版の 4 定義・定理が特殊化として導出されていることを
  確認し、修正を要する不一致は見つからなかった（`lake build`・sorry 検査を再実行、全て通過）。
  対象表の第一の探索列が全て done になったので、マニフェスト「CA 側を先に分類する」と survey
  「時間（F の反復）を動かす」に従い、第二の探索列「大域写像の反復から内在的に生じる構造」を開いた。
  物理的な名前を使わず、有限集合上の写像の反復であることだけから定まる構造を扱う。最初の対象
  「軌道の最終周期性」の構造化記述を `structured-latex/content/global-map-iteration.ts` に書いた:
  反復 $F^n$ と軌道 $O(y)$ の定義、軌道の衝突（$0\le i<j\le 2^{|V|}$ で $F^iy=F^jy$。鳩の巣）、
  衝突の反復不変性（$k$ の帰納法）、最終周期性（$n\ge i$ で $F^{n+p}y=F^ny$、$p\ge1$、$i+p\le2^{|V|}$）、
  衝突組の有限決定可能性（等号検査高々 $\tfrac12 2^{|V|}(2^{|V|}+1)$ 回）、主張しないこと
  （無限舞台・極限集合・母関数、最小前周期・最小周期の一意性は次層以降）。使う $\mathbb{N}$ の構造は
  後者・加法・大小比較・等号のみ、$\mathbb{R}/\mathbb{C}$ 脱出なし。構造化テキスト check・PDF
  （18 ページ・未解決参照 0）・対応検査を通した。**この章の現在地は構造化記述まで。次 tick は
  レビュー後に SageMath 検算を作る**（$|V|\le3$ の全規則・全配位で衝突の上界、反復不変性、
  最終周期性、走査回数を分けて検査する）。

## 対象表

`status` は `todo` / `一次文献確認まで` / `構造化記述まで` / `SageMath まで` /
`Lean 具体版まで` / `done` のいずれか。
数学的命題の `done` は四層が揃った場合だけ付ける。

| 対象 | 問うこと | status |
| --- | --- | --- |
| 本質的依存台の有限決定 | 局所真理値表のある近傍元を反転して出力が変わるかという有限検査が、その元への本質的依存と同値か | done |
| 冗長近傍からの独立性 | 局所規則を使わない元を近傍へ追加しても本質的依存台が変わらないか | done |
| 時間展開上の直接依存 | 有限舞台と有限時間区間から、イベントの有限集合と一段の依存関係を定義できるか | done |
| 依存の推移閉包の反対称性 | 一段の依存で時間が増えることだけから、到達可能性が部分順序になるか | done |
| 有限伝播境界 | 有限近傍から、有限時刻後の依存元が有限個に限られることを示せるか | done |
| 依存順序から得る部分構造 | 順序凸部分集合、非比較関係、境界を CA 内在的な対象として定義し分類できるか | done |
| 既存因果構造との比較 | 上で得た構造と causal set / net 的構造の間の写像、保存量、欠落情報は何か | done |
| 因果集合の一次文献との照合 | 有限依存順序が一次文献のどの定義を満たし、連続時空への近似に何が不足するか | done |
| 軌道の最終周期性 | 有限舞台上の大域写像の反復が、有限集合上の写像であることだけから最終的に周期的になり、衝突が有限検査で見つかるか | 構造化記述まで |
| 最小前周期と最小周期 | 各配位の最小の前周期・周期を定義でき、一意性と有限決定を示せるか | todo |
| 周期点の個数 | 周期点集合と $Z_n=|\{y:F^ny=y\}|\in\mathbb{N}$ を定義し、その有限決定と周期による分解を示せるか | todo |
| 大域写像の可逆性の有限決定 | 有限舞台上で単射・全射・全配位が周期点であることの同値と、その有限検査を示せるか | todo |

## レビュー位置

- 2026-08-14 tick でレビュー済み: 台帳・README・マニフェスト・2 値 CA の定義の間に実質的な
  用語の食い違いはなかった。1 点だけ、台帳が「座標」と呼んでいた対象は、最小の定義では近傍
  $N(v)$ が順序のない集合なので「近傍の元」へ言い換えた（構造化記述もこの語を使う。
  「座標」は近傍に番号付けがある場合＝群上・ポート付きグラフ上に限る語とする）。
- 2026-08-14 の独立再検証で、有限集合・写像を habitat "N" とした分類は所属として不正確だと判明した。
  schema に "finite" と "countable" を追加し、この章は上記の分類へ直した。また負の型テストが作る
  `type-tests/.tmp/` を生成物として ignore し、検証後も専用 worktree が clean に戻るようにした。
- 2026-08-14 の SageMath 層で、入れ替え写像、存在量化、一点反転との同値、回数勘定をレビューし、
  修正を要する誤りは見つからなかった。全数検査は一般の場合の証明ではなく、$|S|\le3$ の有限検算である。
- 2026-08-15 の 00:12 tick で、SageMath の検算 3 本と構造化記述の各段（入れ替え写像の一意性、
  同値の両方向、走査と回数上界）の対応をレビューし、修正を要する誤りは見つからなかった。
- 2026-08-15 の 00:42 tick で、Lean 具体版と人手証明の対応をレビューし、修正を要する誤りは
  見つからなかった。必要十分版により、同値には添字型の有限性だけでなく状態型の有限性も不要で、
  状態側には「別値の一意性」だけが要ることを確認した。有限性が要るのは有限決定可能性と走査組数である。
- 2026-08-15 の 01:12 tick で、Lean 必要十分版と具体版からの導出をレビューし、修正を要する誤りは
  見つからなかった。台帳が「座標」と書いていた「冗長近傍からの独立性」の行は、既定の語の規約
  （順序のない近傍では「元」と呼ぶ）に合わせて「元」へ言い換えた。
- 2026-08-15 の 01:42 tick で、「冗長近傍からの独立性」の構造化記述をレビューした。主張は正しいが、
  複数等号を一行へまとめた記述規律違反を修正した。SageMath は $|T|\le3$ の全包含・全規則を検査し、
  追加元への非依存、依存の移送、依存台の一致を含む 5 本が全て PASS。これは一般の場合の証明ではない。
- 2026-08-15 の 02:12 tick で、前 tick の構造化記述修正と SageMath 5 本の対応をレビューし、
  修正を要する誤りは見つからなかった。Lean 具体版は $S\subseteq T$ を `Finset T` と部分型で
  表したが、行き来する写像は人手証明の ρ・ι の 2 本に固定した。
- 2026-08-15 の 02:42 tick で、Lean 具体版と人手証明の対応をレビューし、修正を要する不一致は
  見つからなかった。必要十分版により、添字型・状態型の有限性は点ごとの依存不変性には不要で、
  依存台を `Finset` として集める具体版の段階にだけ要ることを確認した。
- 2026-08-15 の 03:12 tick で、「冗長近傍からの独立性」の Lean 必要十分版と導出をレビューし、
  修正を要する誤りは見つからなかった。「時間展開上の直接依存」の構造化記述では、依存関係の条件に
  N(v) でなく supp(f_v) を採り、その根拠を大域写像の一点反転による特徴づけ（前二章の合成）として
  claim に昇格させた。時刻の厳密増加も claim にした（次対象「推移閉包の反対称性」がこれを使う）。
- 2026-08-15 の 03:42 tick で、「時間展開上の直接依存」の構造化記述をレビューし、修正を要する
  不一致は見つからなかった。SageMath 6 本は大域一点反転、二つの個数計算、一段依存の所属、
  走査上界、時刻増加を分離し、全て PASS。有限範囲の検算であり一般証明ではない。
- 2026-08-15 の 04:12 tick で、「時間展開上の直接依存」の SageMath 6 本と構造化記述の対応を
  レビューし、修正を要する誤りは見つからなかった。Lean 具体版は舞台・局所規則・大域写像を
  人手証明と同じ順で定義し、行き来は前二章の制限写像・冗長拡大だけを通した。大域一点反転の
  特徴づけの証明は、人手証明の 3 段（一点反転同値・依存台の定義・依存台不変性）をそのまま写した。
- 2026-08-15 の 04:42 tick で、Lean 具体版と人手証明の対応をレビューし、修正を要する
  不一致は見つからなかった。必要十分版により、大域依存に舞台・状態型の有限性は不要であり、
  時刻増加には自然数自体でなく次時刻が厳密に後である性質だけが要ることを確認した。
- 2026-08-15 の 05:12 tick で、「時間展開上の直接依存」の Lean 必要十分版と導出をレビューした。
  イベント集合の定義と個数定理に、証明で使われない `DecidableEq Time`・`DecidableEq V` が
  残っていたので削除した（必要十分版の要件「仮定は具体版の証明が実際に使う性質だけ」への修正。
  大域一点反転の特徴づけに要る舞台の等号判定は、一点反転写像の定義が使うのでそのまま残る）。
  「依存の推移閉包の反対称性」の構造化記述では、到達可能関係を依存経路の存在で定義し、
  推移閉包という名は「D_τ を含む最小の推移的関係」の claim を根拠にしてから使った。
  反対称性は相互到達の不存在（時刻の厳密増加と ℕ の非反射性）として述べ、部分順序は
  反射閉包 ⪯_τ に対して 3 条件を場合分けで示した。
- 2026-08-15 の 05:42 tick で、「依存の推移閉包の反対称性」の構造化記述をレビューし、修正を要する
  不一致は見つからなかった。SageMath 6 本は隣接時刻間の任意の有限関係まで対象を広げ、4,900 関係で
  経路・推移閉包・反対称性・部分順序性を全て PASS。これは一般の場合の証明ではない。
- 2026-08-15 の 06:12 tick で、「依存の推移閉包の反対称性」の SageMath 6 本と構造化記述の対応を
  レビューし、修正を要する不一致は見つからなかった。Lean 具体版は依存経路・到達可能関係・
  反射的到達可能関係・部分順序を人手証明と同じ順で定義し、5 つの claim と 2 つの注記
  （C_τ ⊆ E_τ × E_τ と有限性）を人手証明と同じ論法（経路長の帰納法・経路連結・時刻増加の
  両向き適用）で形式化した。
- 2026-08-15 の 06:42 tick で、Lean 具体版と人手証明の対応をレビューし、修正を要する不一致は
  見つからなかった。必要十分版により、経路・到達可能性・推移閉包には集合と関係だけが要り、
  反対称性には時刻写像、推移的かつ非反射的な時刻関係、一段ごとの時刻増加だけが要ることを
  確認した。イベント集合の有限性は到達可能関係の有限性の注記にだけ要る。
- 2026-08-15 の 07:12 tick で、「依存の推移閉包の反対称性」の Lean 必要十分版と導出をレビューし、
  修正を要する不一致は見つからなかった。「有限伝播境界」の構造化記述では、依存関係の向きが
  過去（始点）から未来（終点）なので、伝播球の再帰は終点セルの本質的依存台の元ごとに一段浅い
  伝播球を合併する形（$B_{n+1}(v)=\bigcup_{u\in\operatorname{supp}(f_v)}B_n(u)$）にした。
  依存元集合の個数上界は舞台全体の大きさ $|V|$ にも $\tau$ にも依存しない形で述べた
  （有限近傍だけが効くという対象表の問いへの直接の答え）。
- 2026-08-15 の 07:42 tick で、「有限伝播境界」の構造化記述をレビューし、修正を要する不一致は
  見つからなかった。SageMath 4 本は全ての本質的依存台の族を $|V|\leq3$ で列挙し、経路の正確な
  時刻差、伝播球の再帰と個数上界、始点セルの所属、依存元集合の有限伝播境界を全て PASS。
  これは一般の場合の証明ではない。
- 2026-08-15 の 08:12 tick で、「有限伝播境界」の SageMath 4 本と構造化記述の対応をレビューし、
  修正を要する不一致は見つからなかった。Lean 具体版は経路の時刻差・伝播球・始点所属・依存元集合・
  境界包含を人手証明と同じ順で形式化し、伝播球と個数上界に V の有限性が不要なことが `omit` の
  位置から見えている（依存元集合の有限性と個数の主張にはイベント集合の有限性が要る）。
- 2026-08-15 の 08:42 tick で、「有限伝播境界」の Lean 具体版を人手証明と突き合わせ、修正を要する
  不一致は見つからなかった。必要十分版により、セル型全体の有限性は伝播球には不要であり、
  依存元集合の有限性には有限舞台でなく有限イベント集合だけが要ることを確認した。具体版の主要な
  定義・定理について、必要十分版の特殊化であることを導出した。
- 2026-08-15 の 09:12 tick で、「有限伝播境界」の Lean 必要十分版と導出をレビューし、依存元
  個数上界の未使用仮定 `X.Finite` を削除した（上記）。「依存順序から得る部分構造」の構造化
  記述では、部分構造をすべて既出の (E_τ, ⪯_τ, D_τ) だけから定義し、前章の反対称性
  （反鎖の順序凸性）と時刻の厳密増加（時刻切片の反鎖性）が実際に効く claim を置いた。
- 2026-08-15 の 09:42 tick で、「依存順序から得る部分構造」の構造化記述をレビューし、数学的な
  不一致は見つからなかった。記録上の claim 数だけを 6 つから実在する 7 つへ訂正した。SageMath
  6 本は隣接時刻間の任意の有限関係まで対象を広げ、283 関係の全ての部分集合について 7 claim を
  全て PASS。これは一般の場合の証明ではない。
- 2026-08-15 の 10:12 tick で、「依存順序から得る部分構造」の SageMath 6 本と構造化記述の対応を
  レビューし、修正を要する不一致は見つからなかった。Lean 具体版は 7 claim と定義中の 2 注記を
  人手証明と同じ順で形式化し、⪯_τ の反対称性・推移性は前章の部分順序性定理の成分だけを使った。
  順序凸・反鎖の claim に有限性・部分集合条件が証明上不要なことが引数の下線から見えている
  （必要十分版の最初の検査項目になる）。
- 2026-08-15 の 10:42 tick で、Lean 具体版と人手証明の対応をレビューし、修正を要する不一致は
  見つからなかった。必要十分版により、反鎖の順序凸性に反射性・有限性は不要で、時刻切片には
  自然数自体でなく非反射的な時刻関係と相異なる二点間の時刻増加だけが要ること、一段境界には
  一段関係の順序関係への包含だけが要ることを確認した。具体版の全 claim・注記を特殊化として導出した。
- 2026-08-15 の 11:12 tick で、「依存順序から得る部分構造」の Lean 必要十分版と導出をレビューし、
  修正を要する不一致は見つからなかった。「既存因果構造との比較」の構造化記述では、外部の公理系を
  目標仕様として持ち込まず、順序側の性質（区間有限性・被覆関係）と可復元性・非可復元性だけを
  claim にした。被覆関係の一致により一段依存が順序から定まる一方、時刻写像は順序を保つ全単射で
  保たれない反例を構成した（反例も成果として保存する方針どおり）。文献名との同定は一次文献確認後の
  層に残した。次 tick は本章の SageMath 検算を作る。
- 2026-08-15 の 12:42 tick で、「既存因果構造との比較」の構造化記述をレビューし、修正を要する
  不一致は見つからなかった。SageMath 5 本は区間有限性、被覆関係との両包含・一致、時刻非保存の
  反例を分離して全て PASS。一般側は隣接時刻間の任意の有限関係まで広げた有限範囲の検算であり、
  反例側は構造化記述どおり定値局所規則から構成した。これは一般の場合の証明ではない。
- 2026-08-15 の 13:12 tick で、「既存因果構造との比較」の SageMath 5 本と構造化記述の対応を
  レビューし、修正を要する不一致は見つからなかった。時間の制約により新規層は作らず、
  次 tick の Lean 具体版で再利用する既存定義を現在地に記録した。
- 2026-08-15 の 13:42 tick で、同じ SageMath 5 本と対応検査を再実行し、全て PASS した。
  Lean 具体版は区間有限性、被覆関係と一段依存関係の両包含・等号、順序保存全単射が時刻を保存しない
  一元舞台上の定値規則の反例を、人手証明と同じ順序で形式化した。外部の因果構造の公理系は
  目標仕様として持ち込まず、有限集合・有限関係・自然数時刻だけを使い、R/C 脱出はない。

- 2026-08-15 の 14:12 tick で、「既存因果構造との比較」の Lean 具体版を人手証明と突き合わせ、
  修正を要する不一致は見つからなかった。必要十分版により、区間には集合と関係、被覆の両包含には
  それぞれ「隣接性つきの時刻増加」と「経路による到達可能性」だけが要り、反例には空な一段関係と
  時刻の異なる二点だけが要ることを確認した。具体版の定義・定理を特殊化として導出した。
- 2026-08-15 の 14:42 tick で、前 tick の Lean 必要十分版と導出をレビューし、修正を要する不一致は
  見つからなかった。Bombelli–Lee–Meyer–Sorkin (1987) の本文を確認し、有限依存順序が因果集合の
  数学的定義（局所有限な半順序集合）を満たすことと、連続時空近似には未定義の埋め込み・密度・
  尺度条件が別途必要であることを分離して記録した。

- 2026-08-15 の 15:12 tick で、前 tick の一次文献比較（survey）を既証明の claim と突き合わせ、修正を
  要する不一致は見つからなかった。数学的主張になる部分（局所有限な部分順序集合の定義と、
  $(E_\tau,\preceq_\tau)$ がそれを満たすこと）だけを構造化記述へ移し、近似・物理的因果・時刻写像の
  復元は主張しない remark に限定した。

- 2026-08-15 の 15:42 tick で、「因果集合の一次文献との照合」の構造化記述をレビューし、修正を
  要する不一致は見つからなかった。SageMath 3 本は隣接時刻間の任意の有限関係まで対象を広げ、
  283 関係について部分順序性、9,521 区間について有限性と二つの区間定義の一致を全て PASS。
  これは一般の場合の証明ではない。
- 2026-08-15 の 16:12 tick で、「因果集合の一次文献との照合」の SageMath 3 本と構造化記述の対応を
  レビューし、修正を要する不一致は見つからなかった。Lean 具体版は一次文献の区間、局所有限な部分順序
  集合、区間の一致、(E_τ, ⪯_τ) の局所有限部分順序性を人手証明と同じ順序で形式化し、既証明の
  部分順序性と区間有限性だけを使った。近似・物理的因果・時刻写像の復元は形式化していない。
- 2026-08-15 の 16:42 tick で、「因果集合の一次文献との照合」の Lean 具体版と人手証明の対応を
  レビューし、修正を要する不一致は見つからなかった。必要十分版により、区間と局所有限性の定義には
  集合と関係だけが要り、区間有限性には台集合の有限性だけが要ることを確認した。具体版の定義・定理を
  有限イベント集合と反射的到達可能関係への特殊化として導出した。

- 2026-08-15 の 17:12 tick で、「因果集合の一次文献との照合」の Lean 必要十分版と導出をレビューし、
  修正を要する不一致は見つからなかった。第二の探索列を開き、「軌道の最終周期性」の構造化記述を
  書いた。反復・軌道・衝突・最終周期性・有限決定を有限集合上の写像の性質だけから述べ、物理的名称と
  無限舞台を入れていない。

## 保留中の判断

なし。
