# 自動ループ 状態台帳

手順の正本は [auto-loop-runbook.md](auto-loop-runbook.md)。このファイルは**状態だけ**を持つ。
セクションには番号を振らない（内容の分かる名前で呼ぶ）。

## 現在地

- 2026-08-13（4 tick 目）: 辺の定義の書き直しを、実際の作業量（辺に触れる定義・証明が約 10 ブロックと
  SageMath 検算）から 1 tick で終わらない大きさと判断し、中身で 2 セクションへ割り直した
  （グラフ側の定義置換と追随／双対面側の追随と検算更新）。2 次元側の規約（`def_lattice` の
  「番号の集合＋端点写像」、多重辺を避ける理由の明記）を確認し、3 次元側の設計
  （始点と方向の組 $(a,i)$、$\partial_0/\partial_1$、頂点対との対応の一意性）を台帳へ確定した。
  レビューでは「有限等比和の上界」の証明が分配則と冪の法則を 1 行に混ぜていたのを 2 行へ分離した。
  本文の前進はない。
- 2026-08-13: プロジェクトを作成し、本文の最初の版（有限箱・分配多項式・分離集合・低温側の証拠・
  未解決問題）を構造化テキストで書いた。
- 2026-08-13（2 tick 目）: 旧セクション「極小分離集合の指数上界」は複数の論法を含み 1 tick で
  終わらないため、3 つに割り直した（下表の先頭 3 行）。その最初の 1 つ
  「極小分離集合は到達集合の辺境界」を証明付きで書いた。PDF は 7 ページ。
  **未証明の主張が 2 つある**（極小分離集合の指数上界、Peierls の反転写像による上界）。
  SageMath 検証はまだ 1 件も無い。
- 2026-08-13（3 tick 目）: 「辺境界に双対な面の連結性」を、双対面と面隣接の定義、連結性の証明に
  割り直した。前者を有限な有理座標の頂点集合として定義し、SageMath の厳密検算まで完了した。
  PDF は 8 ページ。
- 2026-08-13（ユーザーの指摘で追加）: 辺の集合を頂点の 2 元集合として定義していたのは、
  2 次元側の規約（辺は番号の集合、格子の形は端点写像だけが担う）に反している。
  数え上げの議論で頂点の構造が暗黙に混ざるため、**数え上げのセクションより先に書き直す**。
  台帳の先頭へセクションを立てた。箱を取ってトーラスを取らない理由は README に書いた（意図的な選択）。
- 次の tick は「辺を番号の集合と端点写像で定義し直し、頂点対で書いていた全箇所を追随させる」から
  始める。そのあと双対面側の追随と検算更新を経て、「辺境界に双対な面の連結性」の証明へ戻る。

## セクション台帳

`status` は四層のどこまで済んだかを表す（`todo` / `記述まで` / `記述と SageMath まで` /
`Lean 具体版まで` / `done`）。**`done` は四層すべて（記述・SageMath・Lean 具体版・
Lean 必要十分版）を満たしたときだけ書く**（runbook「セクション完了の条件」）。

| セクション | 内容 | status |
| --- | --- | --- |
| 辺を番号の集合と端点写像で定義し直し、頂点対で書いていた全箇所を追随させる | 内部辺・境界辺を「始点と方向の組」の集合 $E_L,B_L\subset\mathbb{Z}^3\times\{1,2,3\}$ として定義し（始点 $a$、方向 $i$ の辺の端点は $\partial_0(a,i)=a$、$\partial_1(a,i)=a+\varepsilon_i$。2 次元側 `def_lattice`/`def_boundary_maps` と同じ規約）、隣接する頂点対（少なくとも一方が箱の中）にただ一つの辺が対応する主張を置き（現行の「格子辺の向きと始点は一意に定まる」を吸収する）、破れ数・道・分離集合・到達集合・辺境界の定義と証明で辺を $\{u,v\}$ と書いていた箇所をすべて端点写像・対応写像の表記へ追随させる。**1 つの定義置換とその機械的追随で閉じる**。中間状態で止めない（止まったら次 tick が leftover を拾う） | todo |
| 双対面の定義を新しい辺の定義へ追随させ SageMath 検算を更新する | 辺が始点と方向の組そのものになるので、双対面 $P(e)$ を端点写像から直接定義し直し、「格子辺の向きと始点は一意に定まる」ブロックを前セクションの対応の主張へ置き換える。`sagemath/check/dual-face-vertex-set` を新しい定義に追随させて再実行する | todo |
| 極小分離集合は到達集合の辺境界 | 辺集合を避けた到達集合 $W_F$ と辺境界 $\partial W$ を定義し、両包含（辺境界は分離集合である／分離集合は辺境界を含む）で $F=\partial W_F$ を示す | 記述まで（SageMath は「低温側の証拠の数値的な裏取り」で小さい箱の全数検査を行う） |
| 格子辺に双対な面と面隣接の定義 | 格子辺の向きと始点を一意に定め、双対面を $\mathbb{Q}^3$ の四頂点の集合として定義する。二面の共通頂点が二つであることを面隣接の定義にする | 記述と SageMath まで（Lean 未着手） |
| 辺境界に双対な面の連結性 | 極小分離集合の辺に双対な面の集合が、定義済みの面隣接で連結であることを示す。3 次元 Peierls の技術的核心 | todo |
| 辿る対応による個数の上界 | 連結性を使い、指定した一面から順に辿る対応で極小分離集合の個数を $C^n$ で抑える。定数 $C$ を具体的な自然数として決める（`claim_minimal_separating_growth` の `todo` を消す） | todo |
| Peierls の反転写像 | 極小分離集合の内側を反転する写像が単射であり、破れ数をちょうど $\#F$ 減らすことを示して `claim_peierls_bound` を証明する | todo |
| 低温側の証拠の数値的な裏取り | 小さい箱で $R_L(q)$ と分離集合の数え上げを厳密に計算し、本文の不等式が成り立つことを確かめる（SageMath。`ZZ`/`QQ` のみ） | todo |
| 高温側の証拠の定義 | 有限集合 $S$ 上の $\Phi_S\in\mathbb{Q}(x)$ を、分子・分母が $\mathbb{Z}[x]$ に属する形で定義し、検査が有理数の不等式 1 本になることを示す | todo |
| 高温側の証拠の単調性 | $\Phi_S$ が $x$ について非増加であることを $\mathbb{Z}[x]$ の係数の議論で示す（$\mathbb{R}$ 側の相関不等式に頼らない） | todo |
| 高温側の有理点の集合が上向きに閉じること | 上の単調性から、高温側の集合が上向きに閉じることを示す | todo |
| 両側が重ならないこと | 低温側と高温側の証拠が同じ有理数に同時に付かないことを示す（付いたら本文の不等式が矛盾する） | todo |
| 覆うことの現状の記録 | どの有理数にどちらの証拠も付かないかを、具体的な有理数の範囲として記録する。未解決問題の輪郭を数値で示す | todo |

## 前進の記録（新しい 5 件まで。古いものは削らずここから落として MEMORY へ）

- 2026-08-13: 格子辺の向きと始点の一意性を示し、双対面を連続な正方形ではなく $\mathbb{Q}^3$ の
  四頂点の集合として定義した。共通頂点が二つであることを面隣接と定め、三方向の辺で SageMath 検算を通した。
- 2026-08-13: 「極小分離集合は到達集合の辺境界」を証明付きで書いた（到達集合・辺境界の定義 2 つと
  主張 3 つ。両包含）。旧「極小分離集合の指数上界」を 3 セクションに割り直した。
- 2026-08-13: プロジェクト作成。本文の最初の版と PDF、自動ループの手順書・台帳・起動スクリプト。

## レビュー記録（新しい 5 件まで）

- 2026-08-13（4 tick 目）: 「有限等比和の上界」の証明の最初の行が、分配則（$(1-r)$ を配る）と
  冪の法則（$r\cdot r^n=r^{n+1}$ を各項へ適用）の 2 つの操作を 1 行にまとめていた。
  一ステップ一定理に反するので 2 行へ分離した。他に修正はない。
- 2026-08-13: 分離集合と極小分離集合が一つの定義ブロックに同居し、極小性だけを参照できなかったため、
  「極小分離集合」を独立した定義ブロックへ分離し、極小性を使う二箇所の参照を専用ラベルへ直した。
- 2026-08-13: 低温側の証拠の定義に $C\ge1$ を追加（$C=0$ でも旧定義は満たされ、一様上界の証明の
  $r=Cq>0$ が正当化されなかった）。同証明の $r/(1-r)\le\frac{1/3}{1-1/3}$ の一手が有理関数の
  単調性を無根拠に使っていたので、分子・分母を別々に評価する初等的な 2 ステップへ書き換えた。
  「破れ辺集合が分離集合を含む」の証明末尾で、どの族の中で個数最小を取るのかを明示した。

## 引き継ぎ（未解決のものだけ）

- **SageMath 検証は双対面の頂点集合の 1 件だけである。** 共通定義、検算ディレクトリ、
  検証と本文ラベルの対応検査は用意済みである。
- **Lean の置き場所は用意済み（2026-08-13）。** `lean/`（`Ising3DCut` と `Ising3DCut/NecSuf`）、
  `lean/scripts/check-no-sorry.sh`、`lean/README.md` があり、mathlib は 2 次元側と同じ v4.32.1 に
  固定して `lake build` と検査が通る状態にしてある。**形式化した定理は 0 件。**
  最初の形式化を入れる tick は、入口 `Ising3DCut.lean` へ import を足し、
  検査スクリプトの `targets` 配列へ 3 本（具体版・必要十分版・導出）を登録する。
- **四層すべてがセクション完了の条件である**（2 次元側と同じ。2026-08-13 にユーザー指示で確定）。
  既に「記述まで」「記述と SageMath まで」で置いたセクションは、Lean を後から足して `done` へ上げる。
  **Lean は 1 tick に 1 主張（具体版・必要十分版・導出の 3 本）を上限とする。**
- **Lean の依存は clone copy で持ち込む。** tick スクリプトが 2 次元側の `.lake/packages`
  （同じ版に固定してある）から copy-on-write で持ち込む。ダウンロードし直さない。
