/**
 * 旧 `.mjs` 資産（`content/*.mjs` / `notes/*.mjs`）向けの互換入口。
 *
 * スキーマの正本は `schema.ts` に移した。ここは実体を持たない再エクスポートだけで、
 * 実行時の挙動は `schema.ts` と同一である（検証ロジックの二重管理をしない）。
 * Node 22.18+ は `.ts` を型ストリップしてそのまま読めるため、ビルド成果物は不要。
 *
 * `content/` と `notes/` を `.ts` へ移行し終えたら（第2段）、このファイルは削除する。
 */
export * from "./schema.ts";
