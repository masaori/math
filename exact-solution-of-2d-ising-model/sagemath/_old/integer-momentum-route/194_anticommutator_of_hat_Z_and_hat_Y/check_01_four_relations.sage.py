

# This file was *autogenerated* from the file check_01_four_relations.sage
from sage.all_cmdline import *   # import sage library

_sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_4 = Integer(4); _sage_const_5 = Integer(5); _sage_const_0 = Integer(0); _sage_const_1 = Integer(1); _sage_const_1j = ComplexNumber(0, '1')# <anticommutator_of_hat_Z_and_hat_Y>: hat の反交換関係 4 式
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rep = CheckReport("anticommutator_of_hat_Z_and_hat_Y")
for M in [_sage_const_2 ,_sage_const_3 ,_sage_const_4 ,_sage_const_5 ]:
    Id = eye_M(M)
    rng_mu = list(range(-M, _sage_const_0 )) + list(range(_sage_const_1 , M+_sage_const_1 ))
    for mu in rng_mu:
        for nu in rng_mu:
            d = delta_M(mu+nu, _sage_const_0 , M)
            for sgn in ['+','-']:
                # (1) 同符号
                rep.close(acomm(hatZ_op(mu,M,sgn), hatZ_op(nu,M,sgn)), _sage_const_2 *M*d*Id,
                          f"M={M} ({sgn},{sgn}) mu={mu} nu={nu}: [hatZ,hatZ]+")
                # (2) 異符号（補正項つき）
                other = '-' if sgn=='+' else '+'
                corr = -_sage_const_2 *np.exp(-_sage_const_1j *_sage_const_2 *np.pi*(mu+nu)/M) * _sage_const_2  * Id
                rep.close(acomm(hatZ_op(mu,M,sgn), hatZ_op(nu,M,other)), _sage_const_2 *M*d*Id + corr,
                          f"M={M} ({sgn},{other}) mu={mu} nu={nu}: [hatZ,hatZ]+ 補正項つき")
                # (3) hatZ と hatY
                rep.close(acomm(hatZ_op(mu,M,sgn), hatY_op(nu,M)), _sage_const_0 *Id,
                          f"M={M} sgn={sgn} mu={mu} nu={nu}: [hatZ,hatY]+ = 0")
            # (4) hatY どうし
            rep.close(acomm(hatY_op(mu,M), hatY_op(nu,M)), _sage_const_2 *M*d*Id,
                      f"M={M} mu={mu} nu={nu}: [hatY,hatY]+")
rep.finish()

