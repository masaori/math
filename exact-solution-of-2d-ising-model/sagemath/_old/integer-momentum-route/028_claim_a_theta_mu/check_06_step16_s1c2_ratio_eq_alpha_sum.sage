# ---------------------------------------------------------
# SageMath: Step 16 (2*s_1*c_2)/(c_1+1) = alpha_1 + alpha_2^(-1)
# 対象: parts/008_.../028_claim_a_theta_mu.typ Step 16
# 式ペア: (2*s_1*c_2)/(c_1+1) = alpha_1 + 1/alpha_2
# ---------------------------------------------------------
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if '__file__' in dir() else '.'
load(os.path.join(_dir, '../../_shared/defs.sage'))

# ---------------------------------------------------------
# 式1: (2*s_1*c_2) / (c_1 + 1)
# ---------------------------------------------------------
# 式1(typst):
# (2 s_1 c_2) / (c_1 + 1)
expr1 = (2*s_1*c_2) / (c_1 + 1)

# ---------------------------------------------------------
# 式2: alpha_1 + alpha_2^(-1)
# ---------------------------------------------------------
# 式2(typst):
# alpha_1 + alpha_2^(-1)
expr2 = alpha_1 + 1/alpha_2

# ---------------------------------------------------------
# 検証
# ---------------------------------------------------------
numerical_check(expr1, expr2, label="Step 16: (2*s_1*c_2)/(c_1+1) = alpha_1 + 1/alpha_2")
