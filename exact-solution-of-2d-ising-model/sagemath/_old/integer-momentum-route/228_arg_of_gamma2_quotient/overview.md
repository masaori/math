# SageMath Check: 228_arg_of_gamma2_quotient

## 対象

**対象ラベル**: `arg_of_gamma2_quotient` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part2.ts`

- 範囲: |γ₂(θ_μ)/γ₂(−θ_μ)| = 1 と arg^[0,2π)(商) = s_[0,2π)([2φ_μ + π])

原文がプレースホルダー `???` のままだったところに、別セッションが確定式を入れた主張。**mod 2π 還元の境界**（φ_μ が π/2 または 3π/2 の近く）を踏む事例が実際にあることをカウントして確認している。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_quotient.sage` | 絶対値と偏角、境界近傍の被覆 | 2505 | 3.331e-16 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 備考

比較は 2π を法とする円周上の距離で行う。

## 実行時に出力された観測値

```
  mod 2pi 還元の境界（phi が pi/2 または 3pi/2 の近く）を踏んだ事例: 196 件
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 228
```

実行ログは `sagemath/check/228_arg_of_gamma2_quotient/logs/` に保存してある（この表の数値はそのログから取った）。
