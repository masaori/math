# SageMath Check: 260_def_T_V

## 対象

**対象ラベル**: `def_T_V` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part1.ts`

- 範囲: `T_{(V)}(X) := T_{(V_1^{(±)})^{1/2}}( T_{V_2}( T_{(V_1^{(±)})^{1/2}}(X) ) )` が、行列 `V := (V_1^{(±)})^{1/2} V_2 (V_1^{(±)})^{1/2}` による 1 回の共役 `T_V` と一致すること

**左辺と右辺は別々の計算として独立に評価できる**。左辺は「3 段の共役を順に施す」、右辺は「1 個の行列 `V` を先に作ってから 1 回だけ共役する」であり、両者が一致することは `def_T_g` の合成則 `T_A ∘ T_B = T_{AB}`（`255_def_T_g` で検証済み）の帰結である。食わせる元は、本証明が実際に扱う `hatZ^{(-)}_μ`・`hatY_μ` と、Pauli 基底の元、および複素乱数行列。`(±)` の両セクターを回す。

あわせて `(V_1^{(±)})^{1/2}` の記号の意味（2 乗すると `V_1^{(±)}` に戻ること）も確認している。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_T_V_eq_T_of_V.sage` | `T_{(V)} = T_V`（3 段の合成 vs 行列 `V` による 1 回の共役）、`((V_1^{(±)})^{1/2})^2 = V_1^{(±)}` | 720 | 1.898e-13 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

`M = 2, 3, 4` × `_shared/operators.sage` の `OP_TEST_PARAMS`（6 組の `(K_1, K_2)`）× 両セクター `(+), (-)` で回している。

## 備考

- ディレクトリ番号について。`def_T_V` は `content/008_TV1_hatZ_hatY_part1.ts` の後方（`def_T_g` より後）にあるが、25x 帯は `259` まで埋まっているため `260` を使った。`def_T_g` は `255_def_T_g` に置いてあり、両者の相対順序は保っている。既存ディレクトリの番号は変更していない。
- `256_T_V_eq_T_Vprime_on_hatZ_hatY` は `T_{(V)}` と `T_{(V')}` が `hatZ, hatY` 上で一致するという別の主張であり、ここで検証しているのは `T_{(V)}` の定義そのものが 1 個の行列による共役へ潰れることである。

## 実行時に出力された観測値

```
  M=2 : 全 6 パラメータ × 両セクターで T_{(V)} = T_V を確認
  M=3 : 全 6 パラメータ × 両セクターで T_{(V)} = T_V を確認
  M=4 : 全 6 パラメータ × 両セクターで T_{(V)} = T_V を確認
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 260
```

実行ログは `sagemath/check/260_def_T_V/logs/` に保存してある（この表の数値はそのログから取った）。
