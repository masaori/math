

# This file was *autogenerated* from the file check_01_center.sage
from sage.all_cmdline import *   # import sage library

_sage_const_309 = Integer(309); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_1en8 = RealNumber('1e-8'); _sage_const_5 = Integer(5); _sage_const_1en6 = RealNumber('1e-6'); _sage_const_0 = Integer(0); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_1en9 = RealNumber('1e-9')# <center_of_multiplicative_group_is_scalar>: Z(R^x) = {c I | c in C \ {0}}
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
rng = np.random.default_rng(int(_sage_const_309 ))
rep = CheckReport("center_of_multiplicative_group_is_scalar")
for M in [_sage_const_1 ,_sage_const_2 ,_sage_const_3 ]:
    n = _sage_const_2 **M
    gens = [Zop(m,M) for m in range(_sage_const_1 ,M+_sage_const_1 )] + [Yop(m,M) for m in range(_sage_const_1 ,M+_sage_const_1 )]
    blocks = [np.kron(g, np.eye(n)) - np.kron(np.eye(n), g.T) for g in gens]
    sv = np.linalg.svd(np.vstack(blocks), compute_uv=False)
    nullity = int(np.sum(sv < _sage_const_1en8 )) + (n*n - len(sv))
    print(f"  M={M}: すべての生成元と可換な W の空間の次元 = {nullity}")
    rep.truth(nullity == _sage_const_1 , f"M={M}: 解空間はスカラーの 1 次元のみ")
    # スカラー行列は実際に中心に属する（正則性も込み）
    for _ in range(_sage_const_5 ):
        c = complex(rng.normal(), rng.normal())
        if abs(c) < _sage_const_1en6 : continue
        W = c*np.eye(n)
        rep.truth(abs(np.linalg.det(W)) > _sage_const_0 , f"M={M}: cI は正則（R^x の元）")
        for g in gens:
            rep.close(W @ g, g @ W, f"M={M}: cI は生成元と可換")
    # 非スカラーは中心に属さない
    for _ in range(_sage_const_5 ):
        W = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
        if np.max(np.abs(W - (np.trace(W)/n)*np.eye(n))) < _sage_const_1en6 : continue
        if n == _sage_const_1 : continue
        rep.truth(any(np.max(np.abs(comm(W,g))) > _sage_const_1en9  for g in gens),
                  f"M={M}: 非スカラーの W は中心に属さない")
rep.finish()

