

# This file was *autogenerated* from the file check_04_det_and_gamma1.sage
from sage.all_cmdline import *   # import sage library

_sage_const_0p0 = RealNumber('0.0'); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_1en8 = RealNumber('1e-8'); _sage_const_1en9 = RealNumber('1e-9')# ---------------------------------------------------------
# SageMath: det A(theta~_mu) = 1 と gamma_1(theta~_mu) > 1
# 対象: structured-latex det_A_theta_tilde, gamma1_gt_1_theta_tilde
#   (1) det A(theta~) = 1
#   (2) gamma_1(theta~)^2 + gamma_2(theta~) gamma_2(-theta~) = 1
#   (3) lambda_+ lambda_- = 1
#   (4) gamma_1(theta~) >= 1、さらに gamma_2 != 0 より gamma_1(theta~) > 1
#       （gamma_1^2 - 1 = |gamma_2|^2 > 0 を残差付きで確認する）
# ---------------------------------------------------------
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if '__file__' in dir() else '.'
load(os.path.join(_dir, '_prelude.sage'))

print("=== det A(theta~) = 1 と gamma_1(theta~) > 1 ===")
all_ok = True
for (K1, K2) in K_CASES:
    P = coeffs(K1, K2)
    w = {'det': _sage_const_0p0 , 'sum': _sage_const_0p0 , 'prod': _sage_const_0p0 , 'pyth': _sage_const_0p0 }
    g1_min_excess = None      # min (gamma_1 - 1)
    for M in M_CASES:
        for mu in range(_sage_const_1 , M + _sage_const_1 ):
            t = th_tilde(M, mu)
            a = g2(t, P)
            b = g2(-t, P)
            u = g1(t, P)
            r = RDF(abs(a))
            A = A_mat(t, P)
            w['det'] = max(w['det'], abs(A.det() - _sage_const_1 ))
            w['sum'] = max(w['sum'], abs(u ** _sage_const_2  + a * b - _sage_const_1 ))
            w['prod'] = max(w['prod'], abs((u + r) * (u - r) - _sage_const_1 ))
            w['pyth'] = max(w['pyth'], abs(u ** _sage_const_2  - _sage_const_1  - r ** _sage_const_2 ))
            e = RDF(u - _sage_const_1 )
            g1_min_excess = e if g1_min_excess is None else min(g1_min_excess, e)
    worst = max(w.values())
    ok = worst <= _sage_const_1en8  and g1_min_excess > _sage_const_1en9 
    all_ok = ok and all_ok
    print(f"  {case_label(K1, K2)}: det {w['det']:.1e}, gamma_1^2+gamma_2gamma_2(-) {w['sum']:.1e}, "
          f"lambda_+lambda_- {w['prod']:.1e}, gamma_1^2-1-|gamma_2|^2 {w['pyth']:.1e}, "
          f"min(gamma_1 - 1) = {float(g1_min_excess):.3e} -> {'PASS' if ok else 'FAIL'}")

print("RESULT: PASS" if all_ok else "RESULT: FAIL")
if not all_ok:
    import sys
    sys.exit(_sage_const_1 )

