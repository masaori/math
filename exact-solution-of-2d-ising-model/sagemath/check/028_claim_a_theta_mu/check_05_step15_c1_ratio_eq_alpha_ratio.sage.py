

# This file was *autogenerated* from the file check_05_step15_c1_ratio_eq_alpha_ratio.sage
from sage.all_cmdline import *   # import sage library

_sage_const_1 = Integer(1)# ---------------------------------------------------------
# SageMath: Step 15 (c_1-1)/(c_1+1) = alpha_1 * alpha_2^(-1)
# 対象: parts/008_.../028_claim_a_theta_mu.typ Step 15
# 式ペア: (c_1-1)/(c_1+1) = alpha_1 * alpha_2^(-1) = (tanh K_1)^2
# ---------------------------------------------------------
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if '__file__' in dir() else '.'
load(os.path.join(_dir, '../../_shared/defs.sage'))

# ---------------------------------------------------------
# 式1: (c_1 - 1) / (c_1 + 1)
# ---------------------------------------------------------
# 式1(typst):
# (c_1 - 1) / (c_1 + 1)
expr1 = (c_1 - _sage_const_1 ) / (c_1 + _sage_const_1 )

# ---------------------------------------------------------
# 式2: alpha_1 * alpha_2^(-1)
# ---------------------------------------------------------
# 式2(typst):
# alpha_1 alpha_2^(-1)
expr2 = alpha_1 / alpha_2

# ---------------------------------------------------------
# 検証
# ---------------------------------------------------------
numerical_check(expr1, expr2, label="Step 15: (c_1-1)/(c_1+1) = alpha_1/alpha_2")

