# SageMath Check: 129_def_matrix_norm

## 対象

**対象ラベル**: `def_matrix_norm` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/002_linear_space_general.ts`

- 範囲: ノルムの定義式そのもの（K^d と Mat(n,K) の両方）、および収束 `A_N → A :⟺ ‖A_N − A‖ → 0` の定義

定義の「書き写し」を検査しても同語反復にしかならないので、定義式とは独立なアルゴリズムを含む複数経路で突き合わせる。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_norm_definition_paths.sage` | ノルムの値を 4 経路（定義の二重ループ / numpy / √tr(A*A) / 特異値）で突き合わせ | 110 | 2.614e-16 | **PASS** |
| 02 | `check_02_convergence_definition.sage` | ノルム収束 ⟺ 成分収束（d_N ≤ ‖·‖ ≤ n·d_N）、級数 ΣB_m = lim S_N と閉形式の一致 | 35 | 6.494e-16 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 備考

- 01 の試験行列には乱数だけでなく退化した行列（零・単位・冪零 Jordan・ランク 1・非対角化不能）と、Ising 側の実際の作用素（`hatZ`, `hatY`, `H_1`, `H_2`, `V_1`。臨界点近傍のパラメータを含む）を混ぜてある。スケールが 1e-8〜1e+8 の場合も見ている。
- 02 の「ノルム収束 ⟺ 成分収束」は本文が暗に使っている事実で、定義そのものではない。逆向き（1 成分がずれ続ける列はノルムでも収束しない）も確認している。

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 129
```

実行ログは `sagemath/check/129_def_matrix_norm/logs/` に保存してある（この表の数値はそのログから取った）。
