

# This file was *autogenerated* from the file check_01_fixed.sage
from sage.all_cmdline import *   # import sage library

_sage_const_1p0 = RealNumber('1.0'); _sage_const_2 = Integer(2); _sage_const_2p0 = RealNumber('2.0'); _sage_const_1 = Integer(1); _sage_const_1en10 = RealNumber('1e-10'); _sage_const_0p5 = RealNumber('0.5'); _sage_const_0 = Integer(0); _sage_const_3 = Integer(3); _sage_const_4 = Integer(4); _sage_const_0p3 = RealNumber('0.3'); _sage_const_0p4407 = RealNumber('0.4407'); _sage_const_0p6 = RealNumber('0.6'); _sage_const_1p1 = RealNumber('1.1')# <T_Vprime_fixes_hatZ_hatY_when_gamma2_zero>: gamma_2(theta_mu) = 0 のとき T_(V') は hatZ^{(-)}_mu, hatY_mu を固定する
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
def crit_K1(K2):
    return float(np.arcsinh(_sage_const_1p0 /np.sinh(_sage_const_2 *K2))/_sage_const_2p0 )
rep = CheckReport("T_Vprime_fixes_hatZ_hatY_when_gamma2_zero")

def Vprime_op(K1, K2, M):
    n = _sage_const_2 **M
    S = np.zeros((n,n), dtype=complex)
    for mu in range(_sage_const_1 , M+_sage_const_1 ):
        th = theta_mu_of(mu, M)
        if abs(gamma2_of(th, K1, K2)) < _sage_const_1en10 :
            continue
        g1v = float(np.real(gamma1_of(th, K1, K2)))
        gam = float(np.arccosh(max(g1v, _sage_const_1p0 )))
        pd, _ = psi_ops(mu, M, K1, K2)
        _, pm = psi_ops(-mu, M, K1, K2)
        S = S + gam*(pd @ pm - _sage_const_0p5 *np.eye(n))
    return _expm(S)

found = _sage_const_0 
for M in [_sage_const_2 ,_sage_const_3 ,_sage_const_4 ]:
    for K2 in [_sage_const_0p3 , _sage_const_0p4407 , _sage_const_0p6 , _sage_const_1p1 ]:
        K1 = crit_K1(K2)              # 臨界点ちょうど（ここでのみ gamma_2 = 0 が起きる）
        Vp = Vprime_op(K1, K2, M)
        for mu in list(range(-M,_sage_const_0 )) + list(range(_sage_const_1 ,M+_sage_const_1 )):
            th = theta_mu_of(mu, M)
            if abs(gamma2_of(th,K1,K2)) >= _sage_const_1en10 :
                continue
            found += _sage_const_1 
            Zm = hatZ_op(mu,M,'-'); Ym = hatY_op(mu,M)
            rep.close(T_conj(Vp, Zm), Zm, f"M={M} mu={mu} K2={K2}: T_(V')(hatZ^(-)) = hatZ^(-)")
            rep.close(T_conj(Vp, Ym), Ym, f"M={M} mu={mu} K2={K2}: T_(V')(hatY) = hatY")
            rep.close(A_of(th,K1,K2), np.eye(_sage_const_2 ), f"M={M} mu={mu}: そこで A(theta_mu) = I")
print(f"  gamma_2 = 0 を実際に踏んだ (M,mu,K2) の事例: {found} 件")
rep.truth(found > _sage_const_0 , "gamma_2 = 0 の事例を実際に踏んでいる")
rep.finish()

