

# This file was *autogenerated* from the file check_01_four_relations_that_hold.sage
from sage.all_cmdline import *   # import sage library

_sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_4 = Integer(4); _sage_const_5 = Integer(5); _sage_const_0 = Integer(0); _sage_const_1 = Integer(1); _sage_const_1j = ComplexNumber(0, '1')# <commutator_of_H_and_Z_Y> のうち、数値的に成り立つ 4 式
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rep = CheckReport("commutator_of_H_and_Z_Y: 成り立つ 4 式")
for M in [_sage_const_2 ,_sage_const_3 ,_sage_const_4 ,_sage_const_5 ]:
    for mu in list(range(-M,_sage_const_0 )) + list(range(_sage_const_1 ,M+_sage_const_1 )):
        e_m = np.exp(-_sage_const_1j *_sage_const_2 *np.pi*mu/M); e_p = np.exp(+_sage_const_1j *_sage_const_2 *np.pi*mu/M)
        for sgn in ['+','-']:
            H1 = H1_op(M, sgn)
            rep.close(comm(H1, hatZ_op(mu,M,sgn)), _sage_const_2 *e_m*hatY_op(mu,M),
                      f"M={M} sgn={sgn} mu={mu}: [H_1^(±), hatZ^(±)] = 2 e^-ith hatY")
            rep.close(comm(H1, hatY_op(mu,M)), -_sage_const_2 *e_p*hatZ_op(mu,M,sgn),
                      f"M={M} sgn={sgn} mu={mu}: [H_1^(±), hatY] = -2 e^ith hatZ^(±)")
        H2 = H2_op(M)
        rep.close(comm(H2, hatZ_op(mu,M,'-')), -_sage_const_2 *hatY_op(mu,M),
                  f"M={M} mu={mu}: [H_2, hatZ^(-)] = -2 hatY")
        rep.close(comm(H2, hatY_op(mu,M)), _sage_const_2 *hatZ_op(mu,M,'-'),
                  f"M={M} mu={mu}: [H_2, hatY] = 2 hatZ^(-)")
rep.finish()

