

# This file was *autogenerated* from the file check_01_scalar.sage
from sage.all_cmdline import *   # import sage library

_sage_const_108 = Integer(108); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_8 = Integer(8); _sage_const_20 = Integer(20); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_0 = Integer(0); _sage_const_1en8 = RealNumber('1e-8')# <scalar_identity_commutes>: [cI, A] = 0
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rng = np.random.default_rng(int(_sage_const_108 ))
rep = CheckReport("scalar_identity_commutes")
for n in [_sage_const_1 ,_sage_const_2 ,_sage_const_3 ,_sage_const_5 ,_sage_const_8 ]:
    for _ in range(_sage_const_20 ):
        c = complex(rng.normal(), rng.normal())
        A = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
        rep.close(comm(c*np.eye(n), A), np.zeros((n,n)), f"n={n}: [cI, A] = 0")
    # 逆: スカラーでない行列は一般には可換でない
    if n >= _sage_const_2 :
        cnt = _sage_const_0 
        for _ in range(_sage_const_20 ):
            W = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
            A = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
            if np.max(np.abs(comm(W,A))) > _sage_const_1en8 :
                cnt += _sage_const_1 
        rep.truth(cnt > _sage_const_0 , f"n={n}: 一般の W では [W,A] != 0（主張が非自明）")
rep.finish()

