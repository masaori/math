

# This file was *autogenerated* from the file check_01_exp_zero.sage
from sage.all_cmdline import *   # import sage library

_sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_8 = Integer(8); _sage_const_16 = Integer(16); _sage_const_0 = Integer(0); _sage_const_20 = Integer(20)# <theorem_exp_zero>: exp(O) = I
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
rep = CheckReport("theorem_exp_zero")
for n in [_sage_const_1 ,_sage_const_2 ,_sage_const_3 ,_sage_const_5 ,_sage_const_8 ,_sage_const_16 ]:
    O = np.zeros((n,n), dtype=complex)
    rep.close(_expm(O), np.eye(n), f"n={n}: expm(O) = I")
    # 級数を直接足しても I
    S = np.zeros((n,n), dtype=complex); P = np.eye(n, dtype=complex)
    for m in range(_sage_const_0 , _sage_const_20 ):
        S = S + P/math.factorial(m); P = P @ O
    rep.close(S, np.eye(n), f"n={n}: 級数（O^0 = I の規約）でも I")
rep.finish()

