

# This file was *autogenerated* from the file check_01_continuity.sage
from sage.all_cmdline import *   # import sage library

_sage_const_105 = Integer(105); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_1 = Integer(1); _sage_const_16 = Integer(16); _sage_const_2p0 = RealNumber('2.0'); _sage_const_1en12 = RealNumber('1e-12'); _sage_const_1en4 = RealNumber('1e-4')# <matrix_multiplication_continuity>: ||A_N - A|| -> 0 ⟹ ||A_N B - A B|| -> 0
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rng = np.random.default_rng(int(_sage_const_105 ))
rep = CheckReport("matrix_multiplication_continuity")
def nrm(A):
    return float(np.sqrt(np.sum(np.abs(A)**_sage_const_2 )))
for n in [_sage_const_2 ,_sage_const_3 ,_sage_const_5 ]:
    A = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
    B = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
    E = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
    E = E/nrm(E)
    prev = None
    for k in range(_sage_const_1 , _sage_const_16 ):
        eps = _sage_const_2p0 **(-k)
        AN = A + eps*E
        d_in, d_out = nrm(AN-A), nrm(AN@B - A@B)
        rep.truth(d_out <= d_in*nrm(B) + _sage_const_1en12 , f"n={n} k={k}: ||A_N B - AB|| <= ||A_N-A|| ||B||")
        if prev is not None:
            rep.truth(d_out < prev, f"n={n} k={k}: 単調に 0 へ近づく")
        prev = d_out
    rep.truth(prev < _sage_const_1en4 , f"n={n}: 最終的に十分小さい ({prev:.3e})")
rep.finish()

