# SageMath Check: 173_def_kronecker

## 対象

**対象ラベル**: `def_kronecker` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/002_linear_space_general.mjs`

- 範囲: 添字写像 ν の全単射性と、クロネッカー積の成分の定義式

ν(I) = 1 + Σ(i_k−1)2^{M−k} が {1,…,2^M} への全単射であること（像を全部並べて {1,…,2^M} と一致することで確認）と、ベクトルのクロネッカー積の成分が因子の積になることを全 I で確認する。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_nu_bijection.sage` | #I_M = 2^M、ν の全単射性、成分の定義式 | 72 | 0.000e+00 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 備考

定義ブロックだが、ν の全単射性は計算で確かめられる主張なので check を立てている。

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 173
```

実行ログは `sagemath/check/173_def_kronecker/logs/` に保存してある（この表の数値はそのログから取った）。
