

# This file was *autogenerated* from the file check_01_eigenspaces.sage
from sage.all_cmdline import *   # import sage library

_sage_const_20260926 = Integer(20260926); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_4 = Integer(4); _sage_const_5 = Integer(5); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_1en10 = RealNumber('1e-10')# <def_eigenspaces_of_epsilon>: F^{(+)} = {f | eps f = f} は C^{2^M} の C-部分線型空間
#   零ベクトルを含み、和と複素スカラー倍で閉じることを乱数ベクトルで確かめ、
#   あわせて eps = sigma^x_1 ... sigma^x_M の積表示と dim F^{(+)} = 2^{M-1} を観測する。
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rep = CheckReport("def_eigenspaces_of_epsilon")
rng = np.random.default_rng(int(_sage_const_20260926 ))
for M in [_sage_const_1 ,_sage_const_2 ,_sage_const_3 ,_sage_const_4 ,_sage_const_5 ]:
    E = eps_op(M)
    d = _sage_const_2 **M
    # eps = sigma^x_1 ... sigma^x_M
    prod = eye_M(M)
    for m in range(_sage_const_1 ,M+_sage_const_1 ):
        prod = prod @ sx(m,M)
    rep.close(E, prod, f"M={M}: eps = sigma^x_1 ... sigma^x_M")
    # F^{(+)} の元を作る: f = x + eps x は eps f = x + eps^2 x = f を満たす
    def even_vector():
        x = rng.normal(size=int(d)) + _sage_const_1j *rng.normal(size=int(d))
        return x + E @ x
    zero = np.zeros(int(d), dtype=complex)
    rep.close(E @ zero, zero, f"M={M}: 零ベクトルは F^(+) に属する")
    for t in range(_sage_const_4 ):
        f = even_vector(); g = even_vector()
        a = complex(rng.normal(), rng.normal())
        rep.close(E @ f, f, f"M={M} #{t}: eps f = f")
        rep.close(E @ (f + g), f + g, f"M={M} #{t}: eps (f+g) = f+g")
        rep.close(E @ (a*f), a*f, f"M={M} #{t}: eps (a f) = a f")
    w = np.linalg.eigvalsh(E)
    npos = int(np.sum(np.abs(w - _sage_const_1 ) < _sage_const_1en10 ))
    print(f"  M={M}: dim F^(+)={npos}, 2^(M-1)={_sage_const_2 **(M-_sage_const_1 )}")
    rep.truth(npos == _sage_const_2 **(M-_sage_const_1 ), f"M={M}: dim F^(+) = 2^(M-1)（観測）")
rep.finish()

