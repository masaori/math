# SageMath Check: 251_def_transfer_matrix

## 対象

**対象ラベル**: `def_transfer_matrix` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/001_partition_function_2d_ising.ts`

- 範囲: V₁ が対角、V₂ が対称、成分が正、サイズが 2^{M_col}×2^{M_col}、および全成分の値（行・列番号は `def_row_configuration_numbering` の ord）

転送行列の全成分を定義式から独立に計算して突き合わせる。行列は `_prelude.sage` が「番号 k → μ = ord^{-1}(k)（2 進展開）」の向きで作り、照合側は「μ → ord(μ)」の式で番号を引く（ord の両向きを別経路で使う）。

記号は本文どおり: 列数 M_col、同じ行の結合定数 K₁（V₁）、隣り合う行の結合定数 K₂（V₂）。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_transfer_matrix_sanity.sage` | 構造の性質と全成分 | 1137 | 0.000e+00 | **PASS** |
| 02 | `check_02_V2_as_kronecker_power.sage` | V₂ が 2×2 転送行列 t のクロネッカー積 t^(⊠M_col) に一致すること、|𝔐| = 2^{M_col}、V₁ の対角成分が 1 次元 Ising 環のエネルギーから ord の順に組み上がること | 60 | 4.736e-16 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

check 02 は check 01 とは別経路の構成である。check 01 は (V₂)_{ord(μ),ord(μ′)} = exp(K₂ Σ_m μ(m)μ′(m)) を成分ごとに再計算して照合しているのに対し、check 02 は t := ((e^{K₂}, e^{-K₂}), (e^{-K₂}, e^{K₂})) の M_col 重クロネッカー積として V₂ を**構成レベルで**組み上げる。定義式の和 Σ_m が積 Π_m へ分解すること（サイトごとに独立であること）を、成分計算を経ずに確かめる経路になっている。添字の対応は ord のとおり「μ(m) = +1 → 桁 0、μ(m) = -1 → 桁 1、m が上位から下位へ」。V₁ は隣接サイトを結ぶため同じ形には分解しないので、対角成分の直接照合にとどめている。

## 備考

2026-09-26: 本文の定義が成分を ord で指す形になり、記号が M_col・K₁・K₂ に統一されたのに合わせて書き直した。以前は行・列番号を「μ(j) = -1 → ビット 0」の順（ord の逆順）で作っていたが、V₁・V₂ は全スピン反転で不変なので、判定数・残差は書き直しの前後で変わらない。旧記号との対応は J′ = K₁（V₁）、J = K₂（V₂）、N = M_col。

## check 02 の実行時に出力された観測値

```
  M_col=1 : |𝔐|=2, V_2 = t^(⊠M_col) を全パラメータで確認
  M_col=2 : |𝔐|=4, V_2 = t^(⊠M_col) を全パラメータで確認
  M_col=3 : |𝔐|=8, V_2 = t^(⊠M_col) を全パラメータで確認
  M_col=4 : |𝔐|=16, V_2 = t^(⊠M_col) を全パラメータで確認
  M_col=5 : |𝔐|=32, V_2 = t^(⊠M_col) を全パラメータで確認
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 251
```

実行ログは `sagemath/check/251_def_transfer_matrix/logs/` に保存してある（2026-09-26、SageMath 10.9。この表の数値はそのログから取った）。
