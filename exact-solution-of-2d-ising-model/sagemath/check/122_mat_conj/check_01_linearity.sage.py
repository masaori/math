

# This file was *autogenerated* from the file check_01_linearity.sage
from sage.all_cmdline import *   # import sage library

_sage_const_303 = Integer(303); _sage_const_1 = Integer(1); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_8 = Integer(8); _sage_const_20 = Integer(20); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_1en12 = RealNumber('1e-12')# <mat_conj>: T_B(A) := B A B^{-1} は線型写像
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
rng = np.random.default_rng(int(_sage_const_303 ))
rep = CheckReport("mat_conj")
for n in [_sage_const_1 ,_sage_const_2 ,_sage_const_3 ,_sage_const_5 ,_sage_const_8 ]:
    for _ in range(_sage_const_20 ):
        B = _expm(rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n)))
        rep.truth(abs(np.linalg.det(B)) > _sage_const_1en12 , f"n={n}: B は正則")
        A = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
        C = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
        la, lb = complex(rng.normal(),rng.normal()), complex(rng.normal(),rng.normal())
        rep.close(T_conj(B, la*A + lb*C), la*T_conj(B,A) + lb*T_conj(B,C), f"n={n}: 線型性")
        rep.close(T_conj(B, A + C), T_conj(B,A) + T_conj(B,C), f"n={n}: 加法性")
rep.finish()

