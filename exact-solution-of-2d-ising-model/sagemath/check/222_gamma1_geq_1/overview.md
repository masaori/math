# SageMath Check: 222_gamma1_geq_1

## 対象

**対象ラベル**: `gamma1_geq_1` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part2.mjs`

- 範囲: γ₁(θ_μ) ≥ 1

γ₁ が実数であること、≥ 1 であることを M = 2..32、臨界点を含むパラメータで確認し、**最小値がどこで取られるか**も記録する。併せて `c₁c₂*/(s₁s₂*) > 1` を直接確認しており、これは本文の arg(γ₁) の場合分けの**第 2 の場合（arg = π）が空である**ことの数値的裏付けになる。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_lower_bound.sage` | γ₁ ≥ 1、γ₁ > 0、c₁c₂*/(s₁s₂*) > 1 | 3910 | 0.000e+00 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 備考

本文の `TV1_hatZ_hatY_021_claim_arg_gamma1_gamma2`（γ₁ の偏角）にはラベルが付いていないため、`verify-check-linkage.mjs` で紐づけられる check を独立に立てられない。その内容はこの check の中で確認している。

## 実行時に出力された観測値

```
  gamma_1 の最小値: 1.000000000000000 at (M,mu,K1,K2)=(2, -2, 0.11125998429077752, 1.10000000000000)
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 222
```

実行ログは `sagemath/check/222_gamma1_geq_1/logs/` に保存してある（この表の数値はそのログから取った）。
