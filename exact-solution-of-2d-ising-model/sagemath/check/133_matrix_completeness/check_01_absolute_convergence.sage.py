

# This file was *autogenerated* from the file check_01_absolute_convergence.sage
from sage.all_cmdline import *   # import sage library

_sage_const_104 = Integer(104); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_0p3 = RealNumber('0.3'); _sage_const_0p5 = RealNumber('0.5'); _sage_const_120 = Integer(120); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_1 = Integer(1); _sage_const_1en8 = RealNumber('1e-8'); _sage_const_100 = Integer(100); _sage_const_119 = Integer(119); _sage_const_1en10 = RealNumber('1e-10')# <matrix_completeness> (2): sum ||B_m|| が収束 ⟹ sum B_m が収束し ||sum B_m|| <= sum ||B_m||
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rng = np.random.default_rng(int(_sage_const_104 ))
rep = CheckReport("matrix_completeness: 絶対収束判定")
def nrm(A):
    return float(np.sqrt(np.sum(np.abs(A)**_sage_const_2 )))
for n in [_sage_const_2 ,_sage_const_3 ,_sage_const_5 ]:
    for trial in range(_sage_const_5 ):
        # ||B_m|| ~ r^m（r<1）となる列
        r = _sage_const_0p3  + _sage_const_0p5 *rng.random()
        Bs = []
        for m in range(_sage_const_120 ):
            X = rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n))
            X = X / nrm(X) * (r**m)
            Bs.append(X)
        norms = [nrm(B) for B in Bs]
        rep.truth(abs(sum(norms) - (_sage_const_1 -r**_sage_const_120 )/(_sage_const_1 -r)) < _sage_const_1en8 , f"n={n}: ノルムの和は等比和")
        # 部分和の Cauchy 性
        S = np.zeros((n,n), dtype=complex)
        partials = []
        for B in Bs:
            S = S + B
            partials.append(S.copy())
        tail = max(nrm(partials[-_sage_const_1 ]-partials[k]) for k in range(_sage_const_100 ,_sage_const_119 ))
        rep.truth(tail < _sage_const_1en10 , f"n={n}: 部分和が Cauchy（末尾の振れ {tail:.3e}）")
        rep.truth(nrm(partials[-_sage_const_1 ]) <= sum(norms) + _sage_const_1en10 , f"n={n}: ||sum B_m|| <= sum ||B_m||")
rep.finish()

