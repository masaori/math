

# This file was *autogenerated* from the file check_01_matrix_series.sage
from sage.all_cmdline import *   # import sage library

_sage_const_106 = Integer(106); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_0p2 = RealNumber('0.2'); _sage_const_1p0 = RealNumber('1.0'); _sage_const_3p0 = RealNumber('3.0'); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_0 = Integer(0); _sage_const_90 = Integer(90); _sage_const_1en8 = RealNumber('1e-8')# <matrix_exp_series_converges>: S_N(A) の収束と ||S_N(A)|| <= ||I|| + E(||A||)
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
rng = np.random.default_rng(int(_sage_const_106 ))
rep = CheckReport("matrix_exp_series_converges")
def nrm(A):
    return float(np.sqrt(np.sum(np.abs(A)**_sage_const_2 )))
for n in [_sage_const_2 ,_sage_const_3 ,_sage_const_5 ]:
    for scale in [_sage_const_0p2 , _sage_const_1p0 , _sage_const_3p0 ]:
        A = scale*(rng.normal(size=(n,n)) + _sage_const_1j *rng.normal(size=(n,n)))
        bound = nrm(np.eye(n)) + math.exp(nrm(A))
        S = np.zeros((n,n), dtype=complex); P = np.eye(n, dtype=complex)
        for m in range(_sage_const_0 , _sage_const_90 ):
            S = S + P/math.factorial(m)
            P = P @ A
            rep.truth(nrm(S) <= bound + _sage_const_1en8 , f"n={n} scale={scale} m={m}: ||S_N|| <= ||I|| + E(||A||)")
        rep.close(S, _expm(A), f"n={n} scale={scale}: 級数 = scipy の expm（独立2経路）")
        print(f"  n={n} scale={scale}: ||S|| = {nrm(S):.6e}, 上界 = {bound:.6e}, 比 = {nrm(S)/bound:.4f}")
rep.finish()

