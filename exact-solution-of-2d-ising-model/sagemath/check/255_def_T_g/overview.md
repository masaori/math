# SageMath Check: 255_def_T_g

## 対象

**対象ラベル**: `def_T_g` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part1.ts`

- 範囲: `T_g : Mat(2^M,C) → Mat(2^M,C), h ↦ g h g^{-1}` （`g ∈ (Mat(2^M,C))^×`）の定義から従う性質

**行列表示を 2 経路で作って突き合わせている**。合成則 `T_A ∘ T_B = T_{AB}` を「作用素として両辺に同じ h を食わせる」だけで確認すると、結合律 `(AB)h(AB)^{-1} = A(BhB^{-1})A^{-1}` の言い換えにしかならない。そこで `T_g` を Pauli 基底 `{σ^{a_1} ⊠ … ⊠ σ^{a_M}}` に関する 4^M × 4^M の行列として、(i) 基底の像の展開係数 `tr(P_i^† g P_j g^{-1}) / 2^M` から読む経路と、(ii) ベクトル化の公式 `vec(A X B) = (A ⊗ B^T) vec(X)` から `g ⊗ (g^{-1})^T` を作り Pauli 基底へ基底変換する経路の 2 通りで構成し、両者の一致を見る。そのうえで合成則を行列の積 `[T_A][T_B] = [T_{AB}]` として確認する。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_composition_law.sage` | 合成則（作用素・行列表示の両方）、行列表示の 2 経路一致、`T_g(I) = I`、`T_{g^{-1}} ∘ T_g = id`、積の保存 `T_g(hh') = T_g(h)T_g(h')`、線型性 | 114 | 1.010e-14 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

`M = 1, 2, 3`（したがって行列サイズ `2^M`、`T_g` の行列表示は最大 64×64）について、可逆な複素乱数行列を 3 組ずつ取って回している。

## 備考

- ディレクトリ番号について。`def_T_g` は `content/008_TV1_hatZ_hatY_part1.ts` で `injectivity_of_T_up_to_scalar`（既存 `253_`）の直前に置かれているが、`250_`〜`254_` は既に埋まっているため、25x 帯の空き番号 `255` を使った。同じ理由で後続の `def_T_V` は `260_` に置いてある（両者の相対順序は保っている）。既存ディレクトリの番号は変更していない。
- `117_conjugation_is_ring_homomorphism` と `200_linearity_of_T` は同じ共役写像を扱うが、いずれも作用素レベルの合成則・線型性までで、行列表示（vec 公式）を経由する経路は持っていない。

## 実行時に出力された観測値

```
  M=1 : 合成則・行列表示の 2 経路一致・線型性を乱数 3 組で確認
  M=2 : 合成則・行列表示の 2 経路一致・線型性を乱数 3 組で確認
  M=3 : 合成則・行列表示の 2 経路一致・線型性を乱数 3 組で確認
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 255
```

実行ログは `sagemath/check/255_def_T_g/logs/` に保存してある（この表の数値はそのログから取った）。
