

# This file was *autogenerated* from the file check_01_arg_recip.sage
from sage.all_cmdline import *   # import sage library

_sage_const_208 = Integer(208); _sage_const_1en12 = RealNumber('1e-12'); _sage_const_0 = Integer(0); _sage_const_1 = Integer(1); _sage_const_0p0 = RealNumber('0.0'); _sage_const_2 = Integer(2); _sage_const_1en8 = RealNumber('1e-8')# <range_of_args_of_reciprocal_of_complex_numbers>: arg(1/z) の場合分け
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
load(os.path.join(_dir, "_prelude.sage"))
import numpy as np
import math
rng = np.random.default_rng(int(_sage_const_208 ))
rep = CheckReport("range_of_args_of_reciprocal_of_complex_numbers")
zs = [z for z in sample_complex(rng) if abs(z) > _sage_const_1en12 ]
c0 = cp = _sage_const_0 
for z in zs:
    a = arg_def(z)
    got = arg_def(_sage_const_1 /z)
    if a < _sage_const_1en12 :
        c0 += _sage_const_1 ; exp = _sage_const_0p0 
    else:
        cp += _sage_const_1 ; exp = _sage_const_2 *math.pi - a
    d = abs((got - exp + math.pi) % (_sage_const_2 *math.pi) - math.pi)
    rep.truth(d < _sage_const_1en8 , f"z={z}: arg(1/z) (arg z={a:.6f}, 差={d:.3e})")
print(f"  arg z = 0 の場合 {c0} 件、0 < arg z < 2pi の場合 {cp} 件")
rep.truth(c0 > _sage_const_0  and cp > _sage_const_0 , "両方の場合を踏んでいる")
rep.finish()

