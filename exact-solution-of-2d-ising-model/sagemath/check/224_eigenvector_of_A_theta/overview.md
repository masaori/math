# SageMath Check: 224_eigenvector_of_A_theta

## 対象

**対象ラベル**: `eigenvector_of_A_theta` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part2.ts`

- 範囲: 固有値 λ_± と固有ベクトル v_±（γ₂ = 0 の場合と ≠ 0 の場合）

A v = λ v を直接確認する。γ₂ = 0 のとき A = I になり任意の非零ベクトルが固有ベクトルになること、≠ 0 のとき本文の v_± が固有ベクトルであることを分けて見る。γ₂ = 0 の事例を実際に踏んだ件数も記録する。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_eigen.sage` | 両方の場合の固有方程式 | 1296 | 4.102e-14 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 備考

223 と同じ平方根の分枝の扱いをしている。

## 実行時に出力された観測値

```
  gamma_2 != 0 の事例 308 件、gamma_2 = 0 の事例 32 件
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 224
```

実行ログは `sagemath/check/224_eigenvector_of_A_theta/logs/` に保存してある（この表の数値はそのログから取った）。
