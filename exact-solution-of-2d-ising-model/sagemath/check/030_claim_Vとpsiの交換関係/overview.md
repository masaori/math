# 030 `T_(V)(ψ) = e^{±γ} ψ` — SageMath 検証

## 対象
`parts/008_T_V1_hatZとhatZ_hatYの関係/030_claim_Vとpsiの交換関係.typ`（+033 `λ_±=e^{±γ}`）:
`T_(V)(ψ_μ^†)=e^{γ(θ_μ)}ψ_μ^†`、`T_(V)(ψ_μ)=e^{-γ(θ_μ)}ψ_μ`。

## 位置づけ
030 は従来 SageMath 未検証だった。**038/039 の正しさはこの 030（V 側）＋ 037（V' 側）から従う**ため、
038/039 検証の一環として土台の 030 を全空間で確認する。

## 実行 / 結果: 全 PASS（M=2,3,4 × 5パラメータ）
`sage -python check_01_TV_psi.py` → `max rel |T_V(ψ^†)-e^γ ψ^†|`: 1.1e-15 〜 1.4e-13 → PASS

## 知見
- V の構築は **セクター '-'**（`V_1^(-)`, 境界 `+Y_M Z_1`）。`'+'` だと誤差 ~0.5 で FAIL。
- `hat(Z)_μ^(-)` は **uniform（全 j 係数 +1）**（def_hatZ_hatY 009 の `(minus)`）。
  j=1 を -1 にすると FAIL する（013 の旧バグの符号）。
