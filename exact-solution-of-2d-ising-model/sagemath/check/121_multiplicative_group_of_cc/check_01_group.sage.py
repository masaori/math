

# This file was *autogenerated* from the file check_01_group.sage
from sage.all_cmdline import *   # import sage library

_sage_const_302 = Integer(302); _sage_const_1en12 = RealNumber('1e-12'); _sage_const_1 = Integer(1); _sage_const_0 = Integer(0); _sage_const_30 = Integer(30); _sage_const_2 = Integer(2); _sage_const_20 = Integer(20); _sage_const_1en24 = RealNumber('1e-24'); _sage_const_8 = Integer(8)# <multiplicative_group_of_cc>: C^x = C \ {0} が乗法群、z^{-1} = 1/z
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
import math
load(os.path.join(_dir, "_prelude.sage"))
rng = np.random.default_rng(int(_sage_const_302 ))
rep = CheckReport("multiplicative_group_of_cc")
zs = [z for z in sample_complex(rng) if abs(z) > _sage_const_1en12 ]
one = complex(_sage_const_1 ,_sage_const_0 )
for a in zs[:_sage_const_30 ]:
    inv = complex(a.real/(a.real**_sage_const_2 +a.imag**_sage_const_2 ), -a.imag/(a.real**_sage_const_2 +a.imag**_sage_const_2 ))
    rep.close(a*inv, one, f"z={a}: 逆元 z^{{-1}} z = 1")
    rep.close(inv, _sage_const_1 /a, f"z={a}: z^{{-1}} = 1/z")
    rep.truth(abs(a*inv) > _sage_const_0 , f"z={a}: 積が 0 でない（閉性）")
    for b in zs[:_sage_const_20 ]:
        rep.truth(abs(a*b) > _sage_const_1en24 , f"閉性: 非零どうしの積は非零")
        for c in zs[:_sage_const_8 ]:
            rep.close((a*b)*c, a*(b*c), "結合律")
    rep.close(a*one, a, f"z={a}: 単位元")
rep.finish()

