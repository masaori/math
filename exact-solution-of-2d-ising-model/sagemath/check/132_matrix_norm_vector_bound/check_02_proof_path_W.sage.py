

# This file was *autogenerated* from the file check_02_proof_path_W.sage
from sage.all_cmdline import *   # import sage library

_sage_const_2 = Integer(2); _sage_const_1p0 = RealNumber('1.0'); _sage_const_1j = ComplexNumber(0, '1'); _sage_const_0 = Integer(0); _sage_const_1en12 = RealNumber('1e-12'); _sage_const_4 = Integer(4); _sage_const_1000 = Integer(1000); _sage_const_2p0 = RealNumber('2.0'); _sage_const_6 = Integer(6); _sage_const_3 = Integer(3); _sage_const_1 = Integer(1); _sage_const_0p0 = RealNumber('0.0'); _sage_const_11000 = Integer(11000); _sage_const_7 = Integer(7); _sage_const_1p5 = RealNumber('1.5')# <matrix_norm_vector_bound>: 本文の証明が使う行列 W の構成を再現して検査する
#
# 本文は ||Aw|| <= ||A|| ||w|| を、第 1 列が w で他の列が 0 の行列 W を作り
#   ||AW|| = ||Aw||、||W|| = ||w||
# を経由して <matrix_norm_submultiplicativity> へ帰着させている。
# check_01 は不等式そのもの（直接経路）を見ているので、ここでは
# 「証明のステップ（W の構成）が正しく書けているか」を独立に検査する。
# 書き写しミス（列の位置・共役・転置の取り違え）はここで落ちる。
import os
_dir = os.path.dirname(os.path.abspath(__file__)) if "__file__" in dir() else "."
load(os.path.join(_dir, "../../_shared/operators.sage"))
import numpy as np
rep = CheckReport("matrix_norm_vector_bound: 証明の経路（行列 W の構成）")

def fro(A):
    return float(np.linalg.norm(np.asarray(A, dtype=complex)))
def vec_norm(w):
    w = np.asarray(w, dtype=complex)
    return float(np.sqrt(float(np.sum(np.abs(w)**_sage_const_2 ))))
def rand_mat(n, seed, scale=_sage_const_1p0 ):
    g = np.random.default_rng(int(seed))
    return float(scale)*(g.standard_normal((int(n),int(n))) + _sage_const_1j *g.standard_normal((int(n),int(n))))
def rand_vec(n, seed, scale=_sage_const_1p0 ):
    g = np.random.default_rng(int(seed))
    return float(scale)*(g.standard_normal(int(n)) + _sage_const_1j *g.standard_normal(int(n)))
def W_of(w, n):
    """本文の証明で使う行列 W（第 1 列が w、第 2 列以降が 0）。"""
    W = np.zeros((int(n), int(n)), dtype=complex)
    W[:, _sage_const_0 ] = np.asarray(w, dtype=complex)
    return W
def le_ok(x, y, slack=_sage_const_1en12 ):
    x = float(x); y = float(y)
    return x <= y + float(slack)*max(_sage_const_1p0 , abs(x), abs(y))

# 乱数 + 退化行列 + Ising 側の実際の作用素
mats = [("乱数 4x4 #%d" % k, rand_mat(_sage_const_4 , _sage_const_1000 +k, _sage_const_2p0 )) for k in range(_sage_const_6 )]
mats += [
    ("零行列 4x4", np.zeros((_sage_const_4 ,_sage_const_4 ), dtype=complex)),
    ("冪零 Jordan 4x4", np.diag(np.ones(_sage_const_3 , dtype=complex), _sage_const_1 )),
    ("非対角化可能 2x2", np.array([[_sage_const_1p0 ,_sage_const_1p0 ],[_sage_const_0p0 ,_sage_const_1p0 ]], dtype=complex)),
]
for M in [_sage_const_2 ,_sage_const_3 ]:
    mats.append(("hatZ^(-)_1 (M=%d)" % M, hatZ_op(_sage_const_1 , M, '-')))
    mats.append(("H_1^(-) (M=%d)" % M, H1_op(M, '-')))
    mats.append(("H_2 (M=%d)" % M, H2_op(M)))

best_ratio = _sage_const_0p0 
best = None
count = _sage_const_0 
for nm, A in mats:
    A = np.asarray(A, dtype=complex)
    n = A.shape[_sage_const_1 ]
    vecs = [("乱数 #%d" % k, rand_vec(n, _sage_const_11000 +k*_sage_const_7 +n, _sage_const_1p5 )) for k in range(_sage_const_4 )]
    vecs.append(("零ベクトル", np.zeros(n, dtype=complex)))
    for j in range(min(int(n), _sage_const_3 )):
        e = np.zeros(n, dtype=complex); e[j] = _sage_const_1p0 
        vecs.append(("基底 e_%d" % (j+_sage_const_1 ), e))
    for vn, w in vecs:
        count += _sage_const_1 
        lhs = vec_norm(A @ w)
        W = W_of(w, n)
        rep.close(fro(A @ W), lhs, f"||AW|| = ||Aw||: {nm} / {vn}")
        rep.close(fro(W), vec_norm(w), f"||W|| = ||w||: {nm} / {vn}")
        # W 経由の評価（劣乗法性の適用先）が直接経路の右辺と一致すること
        rep.close(fro(A)*fro(W), fro(A)*vec_norm(w), f"||A||||W|| = ||A||||w||: {nm} / {vn}")
        rhs = fro(A)*vec_norm(w)
        if rhs > _sage_const_0 :
            r = lhs/rhs
            if r > best_ratio:
                best_ratio = r
                best = (nm, vn)

print("検査した (A,w) の組: %d" % count)
print("比 ||Aw||/(||A|| ||w||) の最大値: %.15f  （%s / %s）" % (best_ratio, best[_sage_const_0 ], best[_sage_const_1 ]))
rep.truth(le_ok(best_ratio, _sage_const_1p0 ), "比の最大値が 1 を超えない")

rep.finish()

