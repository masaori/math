# SageMath Check: 226_gamma_2_theta_is_0

## 対象

**対象ラベル**: `gamma_2_theta_is_0` （structured-latex 側の安定識別子）

- ファイル: `structured-latex/content/008_TV1_hatZ_hatY_part2.ts`

- 範囲: γ₂(θ_μ) = 0 ⟺ {sin θ_μ = 0 かつ c₂s₁ = c₁cos θ_μ} ⟺ μ = ±M かつ c₁ = s₁c₂

**本文が注意している点の数値的裏付けが主目的**。M が偶数のとき μ = ±M/2 では sin θ_μ = 0 だが γ₂ ≠ 0 であることを明示的に確認しており、「sin θ_μ = 0 ⟺ μ = ±M」が単独では偽で、もう一方の条件（cos θ_μ = −1 のとき c₂s₁ = −c₁ < 0 となって正値性に反する）と連立して初めて μ = ±M に絞られる、という論法が正しいことが分かる。

## チェック一覧

| # | ファイル | 検証内容 | 判定数 | 最大相対誤差 | ステータス |
|---|---------|---------|-------|------------|-----------|
| 01 | `check_01_zero_condition.sage` | 同値性、μ=±M/2 の反例、γ₂=0 ⟹ μ=±M かつ c₁=s₁c₂ | 780 | 0.000e+00 | **PASS** |

許容誤差は既定の相対誤差 `1.0e-09`（成分の最大絶対値で正規化）。既定から変更していない。

## 実行時に出力された観測値

```
  M 偶数・mu=±M/2（sin=0 だが gamma_2!=0）の事例: 80 件
```

## 実行方法

```bash
bash sagemath/tools/run-all-checks.sh 226
```

実行ログは `sagemath/check/226_gamma_2_theta_is_0/logs/` に保存してある（この表の数値はそのログから取った）。
