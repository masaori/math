# 代数的評価点における Fisher 零点差の逆三乗和と係数表示

## 概要

一般有限グラフの重複度込み Fisher 零点について、零点でない代数的評価点における零点差の
逆三乗和を、分配多項式の前三回の形式微分に対応する有限係数和で与える。

## 背景・前提

- リポジトリ直下の `CLAUDE.md`、`AGENTS.md`、`docs/context/` と、このプロジェクトの `README.md` を読む。
- 依存する本文ラベルは `theorem_fisher_zero_algebraic_shifted_reciprocal_sum_coefficient_ratio`、
  `theorem_fisher_zero_algebraic_shifted_reciprocal_square_sum_coefficient_ratio`、
  `claim_partition_polynomial_coefficient_expansion` である。
- 評価点での分配多項式の値が零でないことを仮定する。

## スコープ

構造化本文へ一つの定理だけを追加する。複素平面上の位置、距離、偏角、数値近似、熱力学極限は扱わない。

## 記号の帰属と R 脱出の見込み

- グラフ、スピン配位集合、零点添字集合は有限集合に属する。
- 次数、係数添字、多重度、およびそれらの積は `N` に属する。
- Ising 分配多項式は `Z[x]` に属する。
- 評価点、Fisher 零点、差、逆数、三乗、有限和、評価値、商は `Qbar` に属する。
- 複素平面への埋め込み、数値近似、実数、極限、積分を使わないため、`R/C` 脱出は起きない。

## 作業内容

### 構造化本文

- 係数表示の三回形式微分を第三係数和として書く。
- 重複度込み一次因子分解の三回形式微分を相異なる順序付き三重添字和として書く。
- 逆数和の三乗を添字の一致型ごとに分け、前三回の形式微分を照合する。
- 既存の逆数和・逆二乗和の係数表示を一つずつ代入し、逆三乗和を係数有限和の比で表す。

### SageMath 検算

- 三回形式微分、有限和の三乗分解、最終係数比を一行ずつ厳密演算で検算する。
- 次数が三未満の場合の空和も検算する。

## 対象ファイル

- `structured-latex/content/main-text.ts`
- `sagemath/check/fisher-zero-algebraic-shifted-reciprocal-cube-sum-coefficient-ratio/`

## 完了条件

- [x] 一つの定理として逆三乗和の係数表示が証明されている。
- [x] ステートメントと証明が整合している。
- [x] 各式変形を対応 SageMath ファイルで厳密検算した。
- [x] 参照が全て解決し、未解決参照がゼロである。
- [x] 記号の帰属を書き、`R/C` 脱出が無いことを明記した。
- [x] 構造化本文の生成・型検査・実行時検証・HTML/PDF 生成が通る。
- [x] 検算対応の検証が通る。
- [x] 記述と SageMath 検算までの完了であり、Lean は未着手だと明記した。
- [x] 逆数和と逆二乗和の積を、各定義の代入、有限分配、対角分割、非対角添字交換へ分離した。
- [x] `q_2` の非零性を、自然数から有理数、有理数から代数的数への各標準単射と局所定義の適用へ分離した。
- [x] 最高次係数像の非零性を、自然数での非零性と二つの標準単射の各適用へ分離した。
- [x] `q_3-q_1=q_2` を、有理数内の差、代数的数への標準単射、局所定義の各適用へ分離した。
- [x] `q_1` の乗法単位元性を、二つの標準単射の単位元保存と局所定義の各適用へ分離した。
- [x] 三回形式微分式への評価点代入と、評価後の係数有限和に局所定義 `C_G(a)` を適用する二行を別々に直接検算した。
- [x] 自然数 `2` の非零性と、自然数から有理数への標準単射による非零性の保存を別々の式へ分離した。
- [x] 係数表示を整数係数から有理数係数、有理数係数から代数的数係数へ移す二つの標準単射を別々の等式へ分離した。
- [x] 非対角二重和を局所記号で定義し、二つの移項、代入、分配、並べ替え、係数の括り出しを個別の等式へ分離してから、`q_1=1` と `q_3-q_1=q_2` の導出後に `q_2S_{G,3}(a)` へ移した。
- [x] 逆数和の三乗を、有限三重和への展開、添字一致型による五項への分割、非対角添字交換、三つの同一和の `q_3` による集約へ分離した。
- [x] 最終式の `q_2` の消去と、商の結合律による三次分母への結合を別々の等式へ分離した。
- [x] 三つの同一な非対角和の三重加法を `q_3` 倍として独立に示し、一致型分解への適用を別の代入へ分離した。
- [x] `q_3` による三項集約を、`q_1=1`、二段の標準単射の加法保存、分配律、`q_3=q_1+q_1+q_1` の代入へ分離し、依存順序を修正した。
- [x] 有理数内の `eta(3)-eta(1)=eta(2)` から代数的数内の等式へ移る操作を、標準単射の適用と差の保存へ分離した。
- [x] 有理数内の `eta(3)-eta(1)=eta(2)` を、自然数の加法、標準単射の適用、加法保存、有理数での移項へ分離した。
- [x] 自然数 `2` の正値性と非零性を別々の式へ分離した。
- [x] 最終係数式の共通分母化を、商の三乗、商の積、第一項の分母引き上げ、同分母三項の結合へ分離した。
- [x] 逆三乗和の孤立を、逆数和の三乗を引く操作、逆数和と逆二乗和の積を加える操作、等式の対称律へ分離した。
- [x] 相異なる順序付き三重和の孤立を、逆三乗和を引く操作、非対角二重和項を引く操作、等式の対称律へ分離した。
- [x] 非零な `q_2` を消去して逆三乗和を得る等式を、独立した SageMath ファイルで直接検査した。
- [x] 最終係数整理後の `q_2S_{G,3}(a)` と三次係数商の等式を、独立した SageMath ファイルで直接検査した。
- [x] 第一項の分母引き上げ前に、非零評価値の二乗も非零であることを本文と独立した SageMath 検算で示した。
