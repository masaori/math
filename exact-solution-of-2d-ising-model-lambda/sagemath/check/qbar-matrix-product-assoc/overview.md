# SageMath Check: 代数的数を成分とする行列の積は結合的である

## 対象

**対象ラベル**: `claim_qbar_matrix_product_assoc`（structured-latex 側の安定識別子）

- 本文: `structured-latex/content/main-text.ts` の章「固有値の代数性」の主張 1 件
- 併せて引く定義: `def_qbar_matrix`（$\mathrm{Mat}_{R_L}(\overline{\mathbb{Q}})$）・
  `def_qbar_matrix_product`（成分ごとの有限和で定める積）・`def_row_configuration`（添字集合 $R_L$）

### 何を確定させるための検証か

$U^{L}=I$（`theorem_shift_matrix_order`）を $\mathrm{Ev}_{\xi}$ で
$\mathrm{Mat}_{R_L}(\overline{\mathbb{Q}})$ へ運ぶには、$\mathrm{Ev}_{\xi}$ が行列の冪を保つことが要る。
ところが 2 つの冪は定め方が違う——$\mathbb{Z}[x]$ 側は $A^{1}:=A$ から始めて右から掛け
（`def_matrix_over_row_configs`）、$\overline{\mathbb{Q}}$ 側は
$A^{0}:=I^{\overline{\mathbb{Q}}}_L$ から始めて左から掛ける（`def_qbar_matrix_power`）——ので、
帰納法の一歩で向きを合わせる段（$A^{k+1}=A^{k}A$）が要る。その段が使うのが**この結合則**である。

既にある `claim_qbar_action_product` は行列 2 つと列ベクトル 1 つについての主張であり、
行列 3 つについての等式はそれとは別に要る（証明の中身は同じ有限和の書き換えである）。

確かめるのは次の 12 で、2 から 9 は人手証明の 8 段の鎖に 1 対 1 で対応する。

1. 添字集合。行配位の全体 $R_L$ が $2^{L}$ 個の元をもつこと。
2. 第 1 段。積の定義（外側の $\tau''$ についての和）。
3. 第 2 段。積の定義（内側の $\tau'$ についての和へ開く）。
4. 第 3 段。有限和と元の積についての分配則（右から掛ける側）。
5. 第 4 段。積の結合則（成分ごと、$\overline{\mathbb{Q}}$ の元 3 つについて）。
6. 第 5 段。有限和の順序の入れ替え。
7. 第 6 段。元と有限和の積についての分配則（左から掛ける側）。
8. 第 7 段。積の定義（$BC$ の成分へ畳む）。
9. 第 8 段。積の定義（$A(BC)$ の成分へ畳む）。
10. 主張そのもの。$(AB)C=A(BC)$ が全成分で成り立つこと。
11. 主張が空虚でないこと。両辺が零行列でないこと。
12. 積の可換性を使っていないこと。成分を非可換環（2 次上三角行列、$\mathbb{Z}$ 係数）に
    取り替えても結合則が成り立つこと。

### 計算をどこで行っているか（$\overline{\mathbb{Q}}$ の扱い）

代数的数の全体は `QQbar`（厳密な代数的数の体）で表した。成分に使う値には有理数のほか
$\sqrt2$・$-\sqrt3$・$\sqrt{-1}$・$\sqrt[3]{5}$ を混ぜてある。行列は乱数ではなく添字の順番から
決まる形で作っており、実行するたび同じものになる。
**浮動小数点は使っていない**（`ZZ` / `QQ` / `QQbar` の厳密計算だけ）。実数体にも複素数体にも入っていない。

## 走らせた範囲

- $L=1,2,3$（$R_L$ の元の個数はそれぞれ 2・4・8）。$3$ つの行列の全成分について 8 段すべてを確かめた。
- 非可換な成分での確認も $L=1,2,3$。

$L=4$ 以降を回していないのは、この主張が $L$ に依存せず、有限和の書き換えしか使っていないためである
（$L$ を増やしても新しい場合は現れず、`QQbar` での三重和の計算量だけが増える）。

## 実行

```sh
sage sagemath/check/qbar-matrix-product-assoc/check.sage
```

## 結果

**2026-08-11 実行。すべて通過。**

```
== 代数的数を成分とする行列の積は結合的である ==
L=1: R_L は 2 元。全成分で鎖の 8 段すべてと (AB)C = A(BC) が成り立ち、両辺は零行列でない
L=2: R_L は 4 元。全成分で鎖の 8 段すべてと (AB)C = A(BC) が成り立ち、両辺は零行列でない
L=3: R_L は 8 元。全成分で鎖の 8 段すべてと (AB)C = A(BC) が成り立ち、両辺は零行列でない
L=1: 成分を非可換環（2 次上三角行列）に取っても (AB)C = A(BC) が成り立つ（この段が積の可換性を使っていないこと）
L=2: 成分を非可換環（2 次上三角行列）に取っても (AB)C = A(BC) が成り立つ（この段が積の可換性を使っていないこと）
L=3: 成分を非可換環（2 次上三角行列）に取っても (AB)C = A(BC) が成り立つ（この段が積の可換性を使っていないこと）
すべて通過
```

## 四層のどこまで済んでいるか

**四層すべて**（記述・SageMath・Lean 具体版・Lean 必要十分版）。
Lean は `lean/Ising2DLambda/AlgebraicEigenvalue/QbarMatrixProductAssoc.lean`（具体版
`qbarMatrixProduct_assoc`。鎖の 8 段が calc の 8 段に 1 対 1 で対応する）と
`lean/Ising2DLambda/AlgebraicEigenvalue/QbarMatrixProductAssocFromNecSuf.lean`
（必要十分版の特殊化であることの導出）である（2026-08-11 に `lake build` と sorry 検査を通した）。

**必要十分版は新しく書いていない。** 要るものは既にある
`Ising2DLambda.NecSuf.AlgebraicEigenvalue.action_product_necSuf`
（`claim_qbar_action_product` の必要十分版。仮定は添字の型が有限であることと値の側が
非単位的半環であることの 2 つだけ）そのもので、列ベクトルを $C$ の第 $\tau'''$ 列と取れば
この結合則になる。一行で終わる別名を新しい必要十分版として立てない方針
（`docs/context/証明の書き方.md`）に従い、2 つの主張が同じ言明の 2 つの特殊化であることを
導出で見せている。

### 記録

- 2026-08-22 に本文の側を一般化した。この節の線型代数は、行配位の全体ではなく
  **空でない有限集合 $\mathcal{J}$** を添字集合として述べてある。この検証は $\mathcal{J}=R_L$
  （行配位の全体）に取った場合を小さい $L$ で総当たりに固定するものであり、**一般の
  $\mathcal{J}$ についての標本**である。行配位であることを使う性質は本文でも使っていない。
