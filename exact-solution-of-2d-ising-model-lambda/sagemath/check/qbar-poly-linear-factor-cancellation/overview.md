# SageMath Check: 一次因子は消去できる

**対象ラベル**: `claim_qbar_poly_linear_factor_cancellation`

本文の証明の各段を厳密計算（`QQbar`）で確かめる。浮動小数点は使わない。

- 一次式 $t-\widehat{w}$ の係数（$\mathrm{ac}_0=-w$、$\mathrm{ac}_1=1$、$i\ge2$ で $0$）。
- 積の係数 $\mathrm{ac}_{m+1}((t-\widehat{w})C)=\mathrm{ac}_m(C)+(-w)\,\mathrm{ac}_{m+1}(C)$。
- 帰納法の一歩に対応する取り戻しの鎖（係数を上の番号から順に決めると一意に戻ること）。
- 消去そのもの（$(t-\widehat{w})A=(t-\widehat{w})B$ ならば $A=B$）。

```sh
sage sagemath/check/qbar-poly-linear-factor-cancellation/check.sage
```

**2026-08-12 実行: すべて通過。**

2026-09-05 に再実行し、全段のプログラミングによる検証が通過した。
LLM による検証では、書式統一で最後の多項式の等号と根拠を行末へ移しても、
係数の帰納法・全称量化・参照が保存されていることを確認した。

2026-09-05 の前進前レビューで、SageMath のプログラミングによる検証を、
一次式の係数の三つの鎖・積の係数の五段・帰納法の出発点と一歩の六段へ展開した。
既存の最終等式の assertion は残した。変更後の全段は終了コード 0 で通過した。
LLM による検証: Lean 二版も同じ準備と帰納法へ揃え、必要十分版から積の可換性を除いた。
一次因子の消去は商の一意性と重複度上界で繰り返し使うため、空の四則主張ではない。
Lean 二版と後続を含む全体ビルド、登録 1,635 定理の未証明依存のプログラミングによる検証も通過した。
