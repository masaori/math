# SageMath Check: 正の実数での分配多項式の値の上からの評価

## 対象

**対象ラベル**: `claim_partition_value_upper_bound`

- 実行日: 2026-08-15
- 結果: 有限標本検査がすべて通過（合計 766 件）
- 帰属: すべて $\mathbb{Q}$ の厳密比較。実対数に触れる検査は無く、浮動小数点は使わない。

## 何を確かめるか

- 準備の第一（冪の正値性）: 標本 $w$ 7 点 × 指数 $k\in\{0,\dots,8\}$ で $0<w^{k}$（63 件）。
- 準備の第二（底の単調性）: $0<u\le v$ の標本対 × 指数で $u^{k}\le v^{k}$。
  対には本体で使う組 $(t,\,1+t)$ を含める（315 件）。
- 準備の第三（指数の単調性）: $w=1+t$（$1\le w$）× $m\le n$ で $w^{m}\le w^{n}$（196 件）。
- 準備の第四（定数の有限和）: $\sum_{\sigma\in\Sigma_L}c=|\Sigma_L|\cdot c$（21 件）。
- 本体の式変形の各行: $L\in\{1,2,3\}$ × 正の有理点 $t$ 7 点で、$|\Sigma_L|=2^{L^2}$、
  $t\le1+t$、$1\le1+t$、$b(\sigma)\le2L^2$、代入の環準同型性
  $Z_L(t)=\sum_\sigma t^{b(\sigma)}$、底を $1+t$ へ上げる比較、指数を $2L^2$ へ上げる比較、
  定数の有限和、主張の不等式 $Z_L(t)\le2^{L^2}(1+t)^{2L^2}$（171 件）。

## 範囲の注記（黙って狭めない）

- $t$ の標本は正の**有理数**である（$\iota_{\mathbb{Q}\to\mathbb{R}}$ を通した
  $\mathbb{R}$ の元のモデル）。普遍量化された主張そのものの証明は本文の人手証明が担う。
- $L\in\{1,2,3\}$ に限る（`free-energy-density` の検査と同じ範囲）。
- Lean 具体版・必要十分版・導出まで完了（2026-08-15）。

## 実行方法

```sh
sage check.sage
```
