# トーラスの向き付き辺・回転位相・四つのスピン構造

## 対象

**対象ラベル**: `claim_reversal_is_involution` / `claim_reversal_has_no_fixed_point` / `def_spin_structures`

本文の向き付き辺、反転、始点・終点、直ちに引き返さない接続、方向番号、回転位相、切断線の偶奇、四つのスピン構造を検査する。

## 検査内容

- `L=1,...,5` で向き付き辺が `4L²` 本であること。
- 反転が固定点を持たない対合であり、切断線の偶奇を保つこと。
- 直ちに引き返さない後続辺の方向差が直進・左回転・右回転の三つを尽くし、対応する位相が 8 乗して `1` になること。
- スピン構造がちょうど四つあり、各ねじれ符号が `{-1,1}` に属すること。

## 実行結果

2026-08-29: PASS。有限集合、整数、`QQbar` の厳密計算だけを用い、浮動小数点は使っていない。

```bash
sage sagemath/check/torus-kac-ward-data/check.sage
```
