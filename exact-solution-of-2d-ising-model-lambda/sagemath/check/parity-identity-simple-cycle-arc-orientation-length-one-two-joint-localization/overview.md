# 語長 1 と語長 2 の切断旗局在の拘束を同時に課した解の存在判定

**対象ラベル**: `claim_kac_ward_determinant_fiber_stratified_phase_sum`

`parity-identity-simple-cycle-arc-orientation-length-one-quadratic-solution` と
`parity-identity-simple-cycle-arc-orientation-length-two-wrap-localization` で、語長 $1$・
語長 $2$ のそれぞれについては「弧型の値が、定数項 $0$・一次項 $0$・切断旗を含む
二次単項式だけの和で書ける」解が orient_d 合同系の解空間に存在すると確定した。
ただし判定は語長ごとに別々で、同じ一つの解が両方の局在形を同時に満たすかは
未判定である。ここでは弧型ごとの値と両語長の式の係数を同時に未知数とする単一の
連立 $\mathbb F_2$ 線型系（orient_d 合同系＋語長 $1$ の各弧型での「値＝二次式」の等式＋
語長 $2$ の各弧型での同じ等式）に、両ブロックの定数項・一次係数・切断旗非関与の
二次係数を全て $0$ に固定する拘束行を積み、可解かどうかを判定する。

- 実行: `sage sagemath/check/parity-identity-simple-cycle-arc-orientation-length-one-two-joint-localization/check.sage`
- 状態: **PASS**（2026-09-05）。

  | 観測 | 値 |
  | --- | --- |
  | 切断旗非関与の積 | 語長 $1$: $946$ 個中 $496$ 個、語長 $2$: $1{,}540$ 個中 $780$ 個 |
  | 拘束を積んだ系 | 行 $11{,}554$（合同系 $7{,}085$＋語長 $1$ の等式 $1{,}249$＋語長 $2$ の等式 $1{,}842$＋拘束 $1{,}378$）・未知数 $12{,}686$・階数 $11{,}194$・**可解** |
  | 拘束後の核 | 次元 $1{,}492$・係数座標へ射影した次元 $784$ |
  | 強制された係数座標 | $1{,}378$ 個。$0$ に固定した拘束座標にちょうど一致（拘束の外に新たな強制は生じない） |
  | 拘束後の正準代表（語長 $1$） | 定数項 $0$・一次項 $0$ 個・二次項 $16$ 個。全て切断旗を含む積 |
  | 拘束後の正準代表（語長 $2$） | 定数項 $0$・一次項 $0$ 個・二次項 $184$ 個。全て切断旗を含む積。step0 を含む項も現れる |

  読み方: 語長 $1$ と語長 $2$ の両方で「定数項 $0$・一次項 $0$・切断旗を含む
  二次単項式だけの和」の形を同時に要求しても連立系は可解なので、両語長の
  弧型の値が同時にこの局在形で書ける**単一の解**が解空間に存在する。局在した
  二次式の形は語長ごとに別々の解でしか実現できないのではなく、一つの解が
  両方を同時に担える。語長 $2$ の正準代表の二次項数は単独判定の $52$ 個から
  $184$ 個へ増え、step0 を含む項も現れる（語長 $1$ の拘束を同時に課すと核が
  縮み、正準化で消去できる方向が減るため。局在そのものは保たれている）。
  観測は assert へ固定した。**固定後の再実行は一回 $13$ 分の実行時間が tick の
  締切に収まらないため未実施**（日次監査が `check.sage` を全数で回して確認する）。
- 実測時間: 約 $13$ 分（2026-09-05）。
- 方法: 有限集合、$\mathbb F_2$、整数、$\mathbb Q(\zeta_8)$ の厳密演算のみ。
  浮動小数点は使わない。
