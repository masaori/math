# SageMath Check: 頂点単純閉路の内側セル集合は辺連結である

**対象ラベル**: `claim_interior_cell_set_edge_connected`

原点から始まる長さ $10$ 以下の頂点単純な閉じた非後退単位格子歩をすべて列挙する。
右半直線と縦辺の交差数が奇数である内側セルを外接長方形内で直接作り、次を検査する。

- 内側セル集合が空でない。
- 各内側セルから同じ行を右へ進んだ最初の外側セルとの境界縦辺は奇数回通過され、その直前の内側セルは歩道沿いの内側帯に属する。
- 辺隣接による幅優先探索が内側セル全体に到達する。

- 実行: `sage sagemath/check/interior-cell-set-edge-connected/check.sage`
- 状態: PASS（2026-08-31。頂点単純な閉じた非後退歩道 704 本・内側セル 2,888 個・行方向の帯への到達 2,888 件）
- 計算: 単位格子歩の有限列挙と `ZZ` の比較・加法・奇偶だけ。浮動小数点は使わない。
