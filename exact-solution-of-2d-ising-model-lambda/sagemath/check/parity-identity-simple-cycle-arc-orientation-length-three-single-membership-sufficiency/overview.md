# 語長三の非切断項を単一辺所属の積だけに限る十分性判定

**対象ラベル**: `claim_kac_ward_determinant_fiber_stratified_phase_sum`

一辺二・三の既存 7,085 鍵における有限探索。語長三の弧型 2,044 種だけに
相異なる二特徴の積だけからなる純二次式を課す。切断旗を含む 1,150 項と、単一辺所属同士の 190 項を許し、
切断旗を含まない他の五種類 938 項を同時に零へ固定する。
定数項・一次項は零、他の語長と閉路型の値は自由未知数である。

| ファイル | プログラミングによる検証の内容 | 状態 |
| --- | --- | --- |
| `check.sage` | 特徴名とビット配置の二通りで許す列を照合し、系の行・階数・解または左核証拠を判定する | PASS（2026-09-05 初回・観測固定後の全再実行） |

LLM による検証: 前回の五種類の単独除去とは異なり、今回は五種類を同時に除く。
一つの解の係数を削るのではなく、残る全未知数について存在を判定する。
一般の辺長の命題と Lean 二版は未追加。整数・有限体・上流の円分体の厳密計算のみを使う。

```sh
sage sagemath/check/parity-identity-simple-cycle-arc-orientation-length-three-single-membership-sufficiency/check.sage
```

初回のファイル作成は作業ディレクトリと相対パスが一致せず失敗した。
対象パスを修正して同じ作成処理を実行した。数学的主張と判定条件は変更していない。

プログラミングによる検証: 9,129 行・11,438 未知数の係数行列と拡大行列は
ともに階数 8,825 で、解が存在した。各弧型の値を式から評価し、全 7,085 鍵を復元した。
得た一解は 104 項で、切断旗を含む積が 83 項、単一辺所属同士の積が 21 項である。
項数の最小性や個々の係数の必要性は主張しない。係数は実行時に特徴名の対として全て出力する。

LLM による検証: この有限集合では、単一辺所属同士の積を許すだけで非切断項としては十分である。
前回の全除去の矛盾と合わせ、この種類から少なくとも一項は必要だが、他の五種類は同時に不要である。
切断旗と二重辺・選択辺所属との積は今回も許しており、それらの所属ビット自体が不要だとは言えない。
次は、切断旗を含まない積を同じ内部頂点または同じ切断端点の単一辺所属同士だけへ制限しても可解かを判定する。
