# 語長 1 の弧型の支持台の規則

**対象ラベル**: `claim_kac_ward_determinant_fiber_stratified_phase_sum`

`parity-identity-simple-cycle-arc-orientation-support` で、orient_d 署名の
合同 $\mathbb F_2$ 線型系の解を核ベクトルの貪欲な縮約で疎にすると、
支持台は $2{,}519$ 弧型で語長 $1$ から $8$ まで分布する（語長 $1$ は
$222$ 種）と確定した。ここでは縮約した解（核基底の順序が決定的なので
一意に再現する代表）を語長 $1$ の弧型へ制限し、その値（支持台なら $1$、
外なら $0$）が、単一ステップの向き・切断旗・$D$ 所属と両端点の完全署名の
ビットから、定数項つきの $\mathbb F_2$ 一次式（および相異なる二成分の積を
加えた二次式）で書けるかを判定する。

- 実行: `sage sagemath/check/parity-identity-simple-cycle-arc-orientation-length-one-rule/check.sage`
- 状態: **PASS**（2026-09-05）。

  | 観測 | 値 |
  | --- | --- |
  | 語長 $1$ の弧型の総数 | $1{,}249$（閉路型は $0$） |
  | うち縮約代表の支持台 | $222$ |
  | 特徴ビット数 | $44$ |
  | 一次式 | 直接衝突 $0$・階数 $37$・**非可解** |
  | 二次式（二成分の積を追加。$990$ 特徴） | 直接衝突 $0$・階数 $439$・**非可解** |

  読み方: 語長 $1$ の弧型の特徴ビット列は互いに値を衝突させない
  （同じビット列に $0$ と $1$ が同時に割り当たることはない）が、
  縮約代表の値はその一次式でも二次式でも書けない。従って、この代表の
  語長 $1$ の値の規則は特徴ビットの低次の閉じた式にならない。値は
  解空間の代表の取り方（貪欲な縮約の順序）に依存するので、次は代表を
  固定せず、語長 $1$ の制限が低次式になる解が解空間に存在するかを、
  式の係数も未知数に加えた連立 $\mathbb F_2$ 線型系で判定する。
  観測は同じ実行の中で assert へ固定し、固定後の再実行でも一致
  （PASS）を確認した。
- 実測時間: 約 $7$ 分半（2026-09-05）。
- 方法: 有限集合、$\mathbb F_2$、整数、$\mathbb Q(\zeta_8)$ の厳密演算のみ。
  浮動小数点は使わない。
