import { defineNotes, paragraph } from "../../../schema.ts";

void paragraph;

export default defineNotes([
  {
    id: "note_neg_target",
    targets: ["claim_adjacent_cells_ray_crossing_difference"],
    body: [paragraph(["参照用ノート"])],
  },
]);
