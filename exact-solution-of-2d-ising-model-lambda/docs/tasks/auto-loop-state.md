# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-01 の 61 回目の tick で、巻き付きベクトルの平行階段の横断座標が両端の水準 $0$ と $Lw_{\mathrm h}w_{\mathrm v}$ の間に収まり、基点の下端水準を $K_{\max}$ より上へ置けば階段全体が周期持ち上げと交わらないことを本文と SageMath で固定した。** 任意の基点 $Q$ について $\min\{0,LA\}\le\kappa_\gamma(Q+G_s^\gamma)-\kappa_\gamma(Q)\le\max\{0,LA\}$（$A=w_{\mathrm h}w_{\mathrm v}$）を平行階段の二部分から示し、$\kappa_\gamma(Q)+\min\{0,LA\}>K_{\max}$ を帯外条件とした（`claim_parallel_staircase_transverse_width_bound`）。SageMath は $L=1,2,3$ の非零巻き付きの頂点単純閉路 3,464 本・平行階段の頂点 18,336 個・帯との比較 133,560 件を `ZZ` で全検査した。Lean は未着手（記述と SageMath まで）。次は横断階段と平行階段を組み合わせ、逆向き周期を含まない一側閉包を構成して単純性を示す。
- **2026-09-01 の 60 回目の tick で、巻き付きベクトルの正の平行階段（一周期の並進ベクトル $B_\gamma=(Lw_{\mathrm v},Lw_{\mathrm h})$ を実現する単純な整数格子路）を定義し、各歩が平行座標を真に増やす単位歩であることを本文と SageMath で固定した。** 前 tick で確定した二側閉包の相殺を受け、逆向きの周期持ち上げを使わない一側閉包の最初の部品である。縦方向へ $L|w_{\mathrm v}|$ 歩、横方向へ $L|w_{\mathrm h}|$ 歩進む有限列 $G^{\gamma}$ を定義し（`def_winding_parallel_staircase`）、各差が単位格子ベクトルで平行座標 $\pi_\gamma$ が各歩で $|w_{\mathrm v}|$ または $|w_{\mathrm h}|$ だけ真に増えるため頂点単純であることを証明した（`claim_winding_parallel_staircase_step_increase`）。逆向きに読めば一周期の並進を打ち消す接続路の素材になる。SageMath は $L=1,2,3$ の非零巻き付きの頂点単純閉路 3,464 本・平行階段 14,872 歩を `ZZ` で全検査した。Lean は未着手（記述と SageMath まで）。次は平行階段の横断座標の幅を評価し、帯外に留まる一側閉包の接続路を構成する。
- **2026-09-01 の 59 回目の tick で、二側閉包の周期数差から周期持ち上げの回転数を取り出す予定論法が恒等的な相殺で成立しないことを確定した。** 閉包の向きの巡回語は接続階段を $A$、周期持ち上げ一周期を $P$ とすると $A P^c \overline A \overline P^c$ であり、$c$ を一つ増やすと $T(P)$ と逆向きの $-T(P)$ が同時に加わるため差は元の $T(P)$ にかかわらず零になる。参照用ノートへ不採用理由を固定し、SageMath は $L=1,2,3$ の全対象について $c=1,2,3$ の閉包 226,080 本・頂点 7,752,240 個・三周期比較 75,360 件を整数で検査した。次は逆向きの周期持ち上げを同時に追加しない一側閉包を構成する。
- **2026-09-01 の 58 回目の tick で、接続階段と二つの周期持ち上げから作る閉路を任意の周期数 $c\ge1$ へ一般化し、$c$ 周期分の閉包が頂点単純な閉路であることを本文と SageMath で固定した。** 後続の回転数比較（周期数一、二、三の閉包の循環総回転数を比較して周期単純路の回転数零を出す）が複数周期の閉包を要するため、`def_periodic_lift_closure_cycle` に周期数 $c$ を加え、$N=2h+2cm$ の四部分（接続階段、移動後の持ち上げの $c$ 周期分、接続階段の $c$ 周期並進の逆向き、元の持ち上げの $c$ 周期分の逆向き）へ書き直した。頂点単純性の証明は、持ち上げが $B_{\gamma}$ の整数倍の並進で点集合として不変であることを明示し、接続階段の交点の限定を $c\,B_{\gamma}$ 並進へ移す形へ更新した（`claim_periodic_lift_closure_is_simple_cycle`）。SageMath は $c=1,2$ で閉包 150,720 本・頂点 4,075,040 個の閉性・単位歩・頂点の相異なりを `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の一周期閉包を本文・SageMath で照合し、修正対象は無かった。次は閉包の循環総回転数の周期差を求める。
- **2026-09-01 の 57 回目の tick で、接続階段とその一周期並進、元と移動後の周期持ち上げの各一周期分を合成し、頂点単純な閉路になることを本文と SageMath で固定した。** 接続階段を進み、移動後の持ち上げを一周期進み、接続階段の周期並進を逆向きに戻り、元の持ち上げを一周期だけ逆向きに戻る有限列を定義した（`def_periodic_lift_closure_cycle`）。閉性と単位格子歩は四部分の端点の一致から、頂点単純性は接続階段と二つの持ち上げの交点の限定・接続階段と非零周期並進の分離・各持ち上げの単射性・二つの持ち上げの分離から示した（`claim_periodic_lift_closure_is_simple_cycle`）。SageMath は閉路 3,464 本・閉包 75,360 本・閉包の頂点 1,490,960 個について閉性・単位歩・終点を除く頂点の相異なりを `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の最初の交点で打ち切る接続階段を本文・SageMath・今回の閉包で照合し、「何も言っていない主張」や不一致は無かった。次は二つの有限閉包の回転数比較により周期単純路の回転数零を示す。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 閉包の周期数の一般化（済。Lean 未着手）→ 二側閉包の周期差では回転数が相殺すること（反証済）→ 巻き付きベクトルの平行階段（済。Lean 未着手）→ 平行階段の横断座標の幅と帯外に留まる条件（済。Lean 未着手）→ 一側閉包の構成と単純性 → 一側閉包の周期差による周期単純路の回転数零 → 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-01（61 回目）: 平行階段の横断座標を二部分で計算し、$0$ と $Lw_{\mathrm h}w_{\mathrm v}$ の間に収まる幅評価と、基点の下端水準を $K_{\max}$ より上へ置けば階段全体が周期持ち上げを避ける帯外条件を証明した（`claim_parallel_staircase_transverse_width_bound`）。SageMath は閉路 3,464 本・階段頂点 18,336 個・帯比較 133,560 件を検査した。Lean は未着手。
- 2026-09-01（60 回目）: 「逆向き周期を同時に追加しない一側閉包の構成」を、巻き付きベクトルの平行階段・平行階段の横断座標の幅と帯外に留まる条件・一側閉包の構成と単純性の三部品へ割り直した（複数の論法を含むため）。最初の部品として一周期の並進ベクトル $B_\gamma$ を実現する正の平行階段を定義し、各歩が平行座標を真に増やす単位歩で頂点単純であることを証明した（`def_winding_parallel_staircase`, `claim_winding_parallel_staircase_step_increase`）。SageMath は閉路 3,464 本・平行階段 14,872 歩を検査した。Lean は未着手。
- 2026-09-01（59 回目）: 閉包の周期数を $c$ から $c+1$ へ増やすと、順向き一周期の回転数 $T(P)$ と逆向き一周期の回転数 $-T(P)$ が同時に加わって差が恒等的に零になるため、予定していた二側閉包の比較では $T(P)$ を決定できないと確定した。不採用理由を参照用ノートへ置き、次を一側閉包の構成へ差し替えた。SageMath は $c=1,2,3$ の閉包 226,080 本と回転数比較 75,360 件を検査した。
- 2026-09-01（58 回目）: 「二つの有限閉包の回転数比較による周期単純路の回転数零」を、閉包の周期数の一般化・閉包の循環総回転数の周期差・周期数一、二、三の閉包の回転数比較の三部品へ割り直した（回転数比較が複数周期の閉包を要するため）。最初の部品として `def_periodic_lift_closure_cycle` と `claim_periodic_lift_closure_is_simple_cycle` を任意の周期数 $c\ge1$ へ一般化した。SageMath は $c=1,2$ で閉包 150,720 本・頂点 4,075,040 個を検査した。Lean は未着手。
- 2026-09-01（57 回目）: 接続階段・移動後の周期持ち上げの一周期分・接続階段の一周期並進の逆向き・元の周期持ち上げの一周期分の逆向きを合成し、閉じた単位格子路で頂点単純であることを証明した（`def_periodic_lift_closure_cycle`, `claim_periodic_lift_closure_is_simple_cycle`）。SageMath は閉包 75,360 本・頂点 1,490,960 個を検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-09-01（61 回目）: 姉妹側の「$K_2$ と $K_2^*$ の双対関係」（`duality_c2_star_eq_s2_star_c2`）で、指数表示の準備を行末根拠つきの鎖へ移し、$s_2^*$ と $c_2^*$ の各鎖の先頭に定義の等号を独立した行として置き、全根拠を行末列へ揃えた。内容・参照は不変で、対象 SageMath 二件・姉妹側 check・PDF 350 ページを通した。
- 2026-09-01（60 回目）: 姉妹側の「共役写像の非零スカラー倍を除く単射性」の証明ブロック（`008_TV1_hatZ_hatY_part1.ts`）で、Step 4 の $uh=(cI)h=h(cI)=hu$（一行三等号・根拠は文中参照のみ）を 3 段の行末根拠つきの鎖へ開いた。内容・参照は不変で、姉妹側 check・PDF 350 ページを通した。
- 2026-09-01（59 回目）: 姉妹側の「フェルミオン数演算子どうしの可換性」（`number_operators_commute`）の証明ブロックで、反交換子の対称性にあった一行三等号を 3 段の行末根拠つきの鎖へ開いた。内容・参照は不変。
- 2026-09-01（58 回目）: 姉妹側の「ψ の反交換関係」（`anticommutator_of_psi`）の証明ブロックで、Step 0-2 の $\theta_\nu$ の書き換え（1 行に等号 3 つ・根拠なし）を 4 段の行末根拠つきの鎖へ開いた。内容・参照は不変で、姉妹側 check・PDF 350 ページを通した。
- 2026-09-01（57 回目）: 姉妹側の「偶セクターと奇セクターでの交換関係」（`why_008_applies_only_to_minus_sector`）の証明ブロックで、Step 1 と二つのセクターの計算にある行中の根拠 21 行を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・段数・参照は不変。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-09-01（61 回目）: 前 tick の `def_winding_parallel_staircase` と `claim_winding_parallel_staircase_step_increase` を本文・SageMath（閉路 3,464 本・平行階段 14,872 歩 PASS）・今回の横断幅評価で照合した。二場合の境界 $s=LV$、終点 $B_\gamma$、単位歩と平行座標の真の増加は一致し、後続が直接使う非自明な内容であるため、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。
- 2026-09-01（60 回目）: 前 tick の二側閉包の回転数相殺の不採用ノート（`note_kac_ward_claim_periodic_lift_closure_turning_difference_cancellation`）と SageMath（$c=1,2,3$ の閉包 226,080 本・三周期比較 75,360 件 PASS）を照合した。巡回語 $A P^c \overline A \overline P^c$ の表示と相殺 $T(P)+T(\overline P)=0$ の論証は正しく、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。
- 2026-09-01（59 回目）: 前 tick の任意周期数の閉包定義・単純性を本文と SageMath（$c=1,2$ の閉包 150,720 本 PASS）で照合した。周期延長・交差排除は後続構成が直接引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。次の予定論法だけは順向き・逆向き周期の回転数が相殺するため成立しないと判明した。
- 2026-09-01（58 回目）: 前 tick の `def_periodic_lift_closure_cycle` と `claim_periodic_lift_closure_is_simple_cycle`（一周期閉包）を本文・SageMath（閉包 75,360 本 PASS）で照合した。四部分の交差排除は組ごとに既証明の主張を引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。並進した接続階段の交点の議論が持ち上げの $B_{\gamma}$ 並進不変性に暗黙に依っていた点は、今回の一般化で本文へ明示した。
- 2026-09-01（57 回目）: 前 tick の `def_first_hit_connecting_staircase` と `claim_first_hit_connecting_staircase_meets_lifts_only_at_ends` を本文・SageMath（閉路 3,464 本・接続階段の頂点 236,600 個・所属判定 510,640 件 PASS）・今回の閉包構成で照合した。接続階段の単純性と二つの持ち上げとの交点の限定は今回が直接引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
