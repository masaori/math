# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-01 の 68 回目の tick で、歩道沿いの外側帯と内側セル集合の補集合の辺連結性の仮定を、$n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ緩和した。** `claim_walk_side_exterior_band_edge_connected` と `claim_exterior_cell_set_edge_connected` の主張・題名・証明内の根拠を実際に使う仮定へ揃えた（呼び出し側は前 tick までに相異性の導出つきの適用へ直っている）。SageMath は持ち上げ点相異の歩 704 本（射影後に $n_{\mathrm{ct}}>0$ となる例を含む）で外側帯の橋 6,672 箇所・外側帯セル 9,488 個・外側セル 864 個の行方向到達を検査し、Lambda 側 check・linkage 373 検算・Lean 9,616 ジョブ・`sorryAx` 非依存 1,635 件・PDF 369 ページを通した（この二主張の Lean は未着手）；次は境界頂点の対角接触排除の仮定を緩和する。
- **2026-09-01 の 67 回目の tick で、歩道沿いの内側帯と内側セル集合の辺連結性の仮定を、$n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ緩和した。** 二主張と呼び出し側を実際に使う仮定へ揃え、SageMath は持ち上げ点相異の歩 704 本（射影後に $n_{\mathrm{ct}}>0$ となる例を含む）を検査し、Lambda 側 check・linkage・Lean 9,616 ジョブ・`sorryAx` 非依存 1,635 件・PDF 369 ページを通した（この二主張の Lean は未着手）；次は外側帯と補集合連結性の仮定を緩和する。
- **2026-09-01 の 66 回目の tick で、離散 Whitney 系の仮定緩和の対象 7 主張を列挙して五部品へ割り直し、最初の部品として「頂点まわりの同奇偶セルの弧」の仮定を $n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ緩和した。** 零巻き付き側で $n_{\mathrm{ct}}=0$ を仮定する主張は、同奇偶セルの弧・内側帯・内側セル連結・外側帯・補集合連結・対角接触排除・回転数正負 4 の 7 件で、いずれも証明では持ち上げ一周期点の相異性しか使っていないことを確認した。弧の主張（`claim_vertex_surrounding_cells_form_parity_arcs`）は仮定を差し替えても証明本体は不変で、呼び出し側 3 箇所（内側帯・外側帯・対角接触排除）は $n_{\mathrm{ct}}=0$ から `claim_vertex_simple_plane_lift_points_distinct` で相異性を導いて適用する形へ直した。SageMath は持ち上げ点相異の歩 704 本・頂点 6,672 個の弧の検査に加え、$L=1,2,3$ への射影で $n_{\mathrm{ct}}>0$ となる歩（旧仮定では扱えなかった対象）が 704・696・592 本あることを確認した。Lean は未着手（記述と SageMath まで）。次は内側帯と内側セル連結性の仮定を緩和する。
- **2026-09-01 の 65 回目の tick で、射影の平面持ち上げが元の平面閉路の一定の平行移動に一致し、巻き付きが零になることを本文と SageMath で固定した。** 射影した各辺の平面変位が元の単位歩 $W_{j+1}-W_j$ に一致することを四場合で確かめ、持ち上げの漸化式から $P_k(\Pi(W))=P_0(\Pi(W))+W_k-W_0$ を帰納法で示した。閉性 $W_n=W_0$ と持ち上げ終点の巻き付き表示を合わせて $w_{\mathrm h}(\Pi(W))=w_{\mathrm v}(\Pi(W))=0$ を得た（`claim_projected_plane_cycle_lift_translation`）。SageMath は頂点単純な閉単位格子路 144 本、$L=1,2,3$ の射影の歩 3,216 件・持ち上げ点 3,648 件を `ZZ` で全数検査した。Lean は未着手（記述と SageMath まで）。次は離散 Whitney 系で使う $n_{\mathrm{ct}}=0$ の仮定を、持ち上げ一周期の点の相異性へ緩和する対象ブロックを列挙して論法単位へ割り直す。
- **2026-09-01 の 64 回目の tick で、平面の頂点単純な閉単位格子路を固定 $L$ トーラスの閉じた非後退辺列へ射影する橋を本文と SageMath で固定した。** 一側閉包の周期数差から回転数零を導くには、一側閉包（平面路）の循環総回転数へ平面単純閉多角形の回転数定理（正負 4）を適用する経路が要るが、既存の定理と離散 Whitney 系はトーラス頂点単純（$n_{\mathrm{ct}}=0$）な零巻き付き辺列を対象にしており、一側閉包はトーラス上では頂点を繰り返すため直接は適用できないと判明した。そこでセクション「一側閉包の周期差による周期単純路の回転数零」を五部品（平面単純閉路のトーラス射影／射影の平面持ち上げの一致と巻き付き零／離散 Whitney 系の仮定の緩和／一側閉包の循環総回転数は正負 4／周期数差の回転数等式と三周期比較）へ割り直し、最初の部品を進めた。頂点射影 $\operatorname{vt}$ と、単位格子の一歩を向き付き辺へ写す場合分けの射影 $\Pi$ を定義し（`def_plane_unit_path_torus_projection`）、頂点単純な閉単位格子路（$n\ge3$）の射影が閉じた非後退辺列で始点・終点写像が頂点射影と両立することを証明した（`claim_plane_simple_cycle_projection_closed_nonbacktracking`）。SageMath は頂点単純な閉単位格子路 144 本（長さ 10 以下）・$L=1,2,3$ の射影の歩 3,216 件を `ZZ` で全数検査した。Lean は未着手（記述と SageMath まで）。次は射影の平面持ち上げが元の平面路の平行移動に一致し巻き付きが零であることを示す。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 閉包の周期数の一般化（済。Lean 未着手）→ 二側閉包の周期差では回転数が相殺すること（反証済）→ 巻き付きベクトルの平行階段（済。Lean 未着手）→ 平行階段の横断座標の幅と帯外に留まる条件（済。Lean 未着手）→ 一側閉包の定義と閉性（済。Lean 未着手）→ 一側閉包の頂点単純性（済。Lean 未着手）→ 平面単純閉路のトーラス射影（済。Lean 未着手）→ 射影の平面持ち上げの一致と巻き付き零（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧の仮定の緩和（済。Lean 未着手。$n_{\mathrm{ct}}=0$ を持ち上げ一周期点の相異性へ差し替え、呼び出し側 3 箇所は $n_{\mathrm{ct}}=0$ から相異性を導いて適用する形に直した）→ 内側帯と内側セル連結性の仮定の緩和（済。Lean 未着手）→ 外側帯と補集合連結性の仮定の緩和（済。Lean 未着手）→ 境界頂点の対角接触排除の仮定の緩和 → 平面閉多角形の回転数定理の仮定の緩和（緩和後は持ち上げ点相異の零巻き付き辺列に正負 4 が立つ）→ 一側閉包の循環総回転数は正負 4（緩和した定理を射影へ適用）→ 周期数差の回転数等式と三周期比較による周期単純路の回転数零 → 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-01（68 回目）: `claim_walk_side_exterior_band_edge_connected` と `claim_exterior_cell_set_edge_connected` の仮定を $n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ差し替え、証明内の旧仮定依存（相異性の導出と一意性の根拠）を仮定の直接使用へ置き換えた。SageMath は二検算とも歩 704 本を通し、射影後に $n_{\mathrm{ct}}>0$ となる歩の実在も確認した。Lean は未着手。
- 2026-09-01（67 回目）: `claim_walk_side_interior_band_edge_connected` と `claim_interior_cell_set_edge_connected` の仮定を $n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ差し替え、証明内の旧仮定依存を除いた。SageMath は二検算とも歩 704 本を通し、射影後に $n_{\mathrm{ct}}>0$ となる歩の実在も確認した。Lean は未着手。
- 2026-09-01（66 回目）: 「離散 Whitney 系の仮定の緩和」を対象 7 主張の列挙にもとづき五部品（同奇偶セルの弧／内側帯と内側セル連結性／外側帯と補集合連結性／対角接触排除／回転数定理）へ割り直した（各主張の仮定差し替えと呼び出し側の書き直しがそれぞれ一論法のため）。最初の部品として `claim_vertex_surrounding_cells_form_parity_arcs` の仮定を $n_{\mathrm{ct}}=0$ から持ち上げ一周期点の相異性へ緩和し、呼び出し側 3 箇所を相異性の導出つきの適用へ直した。SageMath は歩 704 本・頂点 6,672 個に加えて射影後 $n_{\mathrm{ct}}>0$ の実在を検査した。Lean は未着手。
- 2026-09-01（65 回目）: 射影した各辺の平面変位が元の単位歩に一致する四場合と持ち上げの漸化式から、射影の平面持ち上げが元の平面閉路の一定平行移動であり、終点が始点へ戻って横・縦の巻き付きがともに零であることを証明した（`claim_projected_plane_cycle_lift_translation`）。SageMath は射影の歩 3,216 件・持ち上げ点 3,648 件を検査した。Lean は未着手。
- 2026-09-01（64 回目）: 一側閉包の回転数へ既存の正負 4 定理を直接適用できない（対象がトーラス頂点単純な辺列に限られている）ことが判明したため、「一側閉包の周期差による周期単純路の回転数零」を五部品へ割り直し、最初の部品として平面の単位格子路のトーラス射影を定義し、頂点単純な閉単位格子路の射影が閉じた非後退辺列であることを証明した（`def_plane_unit_path_torus_projection`, `claim_plane_simple_cycle_projection_closed_nonbacktracking`）。SageMath は閉単位格子路 144 本・射影の歩 3,216 件を検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-09-01（67 回目）: 姉妹側の「半整数運動量モード」（`def_half_integer_modes`）の反周期性と添字周期性の二本の式変形鎖で、行中の $\quad(\because\ \dots)$ の根拠 10 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・ラベル参照は不変で、姉妹側 check・PDF 353 ページを通した。
- 2026-09-01（66 回目）: 姉妹側の「$H_1^{(\pm)}, H_2$ を $\hat Z,\hat Y$ で表す」（`H1_H2_via_hatZ_hatY`）の二本の式変形鎖で、行中の $\quad(\because\ \dots)$ の根拠 22 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠は不変で、姉妹側 check・PDF 353 ページを通した。
- 2026-09-01（65 回目）: 姉妹側の「半整数運動量の指数和」（`antiperiodic_exp_sum`）の二本の式変形鎖で、行中の $\quad(\because\ \dots)$ の根拠 7 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠は不変で、姉妹側 check・PDF 353 ページを通した。
- 2026-09-01（64 回目）: 姉妹側の「行列単位の積公式」（`002_linear_space_general.ts` の Step 2 の鎖）で、行中の $\quad(\because\ \dots)$ の根拠 3 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠は不変で、姉妹側 check・PDF 353 ページを通した。
- 2026-09-01（63 回目）: 姉妹側の「行列の exp 級数はノルム収束する」（`matrix_exp_series_converges`）の証明ブロックで、四本の鎖に行中の $\quad(\because\ \dots)$ で置かれていた根拠 12 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠は不変で、姉妹側 check・PDF 353 ページを通した。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-09-01（68 回目）: 前 tick の内側帯・内側セル連結性の仮定緩和を本文・呼び出し側・SageMath（持ち上げ点相異の歩 704 本 PASS）で照合した。緩和後の二主張が引く補題（行方向到達・弧・隣接奇偶）は零巻き付きまたは相異性だけを仮定しており、仮定の連鎖に飛びは無かった。「何も言っていない主張」も無く、修正対象は無かった。
- 2026-09-01（67 回目）: 前 tick の「頂点まわりの同奇偶セルの弧」の仮定緩和を本文・呼び出し側 3 箇所・SageMath（持ち上げ点相異の歩 704 本・頂点 6,672 個 PASS）で照合した。主張と検算は一致し、「何も言っていない主張」も無かったが、題名だけが旧仮定の「頂点単純閉路」のままで実際の主張範囲より狭かったため、「持ち上げ点が相異なる閉路」へ直した。
- 2026-09-01（66 回目）: 前 tick の射影の持ち上げと巻き付き零（`claim_projected_plane_cycle_lift_translation`）を本文・射影の定義・SageMath（閉単位格子路 144 本・射影の歩 3,216 件・持ち上げ点 3,648 件 PASS）で照合した。四場合の変位・帰納法・巻き付き表示は一致し、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。台帳のセクション表で 65 回目に済んだ「射影の平面持ち上げの一致と巻き付き零」へ（済。Lean 未着手）の印が付いていなかったので付けた。
- 2026-09-01（65 回目）: 前 tick の平面単純閉路のトーラス射影（`def_plane_unit_path_torus_projection`, `claim_plane_simple_cycle_projection_closed_nonbacktracking`）を本文・SageMath（閉単位格子路 144 本・射影の歩 3,216 件 PASS）・今回の持ち上げの復元で照合した。四つの差と辺種・向き、端点、非後退性は一致し、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。本文末尾の予定とセクション表も一致していた。
- 2026-09-01（64 回目）: 前 tick の一側閉包の頂点単純性（`claim_one_sided_periodic_lift_closure_simple`）を本文・SageMath（一側閉包 30,144 本・頂点相異性 636,240 件 PASS）・軸順序を選び直した平行階段の幅評価と照合した。横断座標の挟み撃ちと平行座標の分離の引用は幅評価の区間 $[-L|A|,0]$ と一致し、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。本文末尾の「この先に書くこと」とセクション表の食い違いも無かった。

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
