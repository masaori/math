# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-01 の 48 回目の tick で、巻き付きベクトルに平行な整数座標が周期並進で正の定数だけ増えることを本文と SageMath で固定した。** 「整数帯の外側を通る単純な閉路への有限閉包」を論法単位へ割り直し、最初の部品として、平行座標 $\pi_{\gamma}(a,b):=w_{\mathrm v}(\gamma)a+w_{\mathrm h}(\gamma)b$ を定義し（`def_winding_parallel_coordinate`）、整数の除法で $k+m=(q+1)m+r$ と読み替えて $\pi_{\gamma}(\widetilde P_{k+m})=\pi_{\gamma}(\widetilde P_{k})+L(w_{\mathrm v}^2+w_{\mathrm h}^2)$ を示し、増分が $1$ 以上の正の整数であることを証明した（`claim_periodic_plane_lift_parallel_period_increase`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・一周期ずらしの比較 123,960 件を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の有限幅整数帯の主張に修正対象がなかった。
- **2026-09-01 の 47 回目の tick で、非零巻き付きの周期延長した平面持ち上げが有限幅の整数帯に入ることを本文と SageMath で固定した。** 巻き付きベクトルに直交する整数横断座標 $\kappa_{\gamma}(a,b):=w_{\mathrm h}(\gamma)a-w_{\mathrm v}(\gamma)b$ を定義し（`def_winding_transverse_coordinate`）、周期並進 $(Lw_{\mathrm v},Lw_{\mathrm h})$ が横断座標を変えないことから、周期延長した持ち上げの値全体が一周期の有限集合に等しいと示した（`claim_periodic_plane_lift_transverse_bounded`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・周期延長した持ち上げ点 123,960 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の周期延長した持ち上げの単射性に修正対象がなかった。
- **2026-08-31 の 46 回目の tick で、非零巻き付きの頂点単純閉路の周期延長した平面持ち上げ点が相異なることを本文と SageMath で固定した。** 台帳の「周期単純路の回転数零」を論法単位（周期延長した持ち上げの単射性／回転数零の本体）へ割り直し、最初の部品として、整数の除法で $k=qm+r$ と書いて $\widetilde P_k(\gamma):=P_r(\gamma)+q\cdot(L\,w_{\mathrm v},L\,w_{\mathrm h})$ を定義し（`def_periodic_plane_lift`）、$n_{\mathrm{ct}}(\gamma)=0$ かつ巻き付きベクトルが非零なら $\widetilde P$ が単射であることを、代表座標の剰余の議論（余りの一致）と巻き付きベクトルの非零性（商の一致）の二段で証明した（`claim_periodic_plane_lift_points_distinct`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・周期延長した持ち上げ点 123,960 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の方向番号の門の横断数と循環総回転数に修正対象がなかった。
- **2026-08-31 の 45 回目の tick で、循環総回転数が方向番号の門の正負横断数の差の 4 倍であることを本文と SageMath で固定した。** 方向番号の標準整数代表と門 $3\mid0$ の正負横断数を定義し、標準代表の差の循環和が望遠和で消えることから $t_{\circ}(\gamma)=4(g_+(\gamma)-g_-(\gamma))$ を得た（`claim_direction_gate_crossing_turning`）。SageMath は非後退な循環方向列 88,592 本・循環遷移 841,564 箇所を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の境界頂点での対角接触の排除に修正対象がなかった。
- **2026-08-31 の 44 回目の tick で、格子点まわりの内側セルが対角の二セルだけになることが無いことを本文と SageMath で固定した。** 訪問頂点では頂点まわりの弧の主張により内側セルが連続な弧をなし、対角対は添字差が $4$ を法として $2$ なので弧の形にならない。非訪問頂点では四接辺の通過回数の総和が $0$ になり、隣接セルの境界奇偶則から四セルの内外がすべて一致する。これで、平面単純閉多角形の回転数の証明に最後まで残っていた本文未証明の断り（境界頂点での対角接触の排除）を実在する参照へ置き換えた（`claim_boundary_vertex_diagonal_contact_excluded`）。SageMath `no-diagonal-only-contact` は長さ $10$ 以下の頂点単純閉歩道 704 本・格子点 20,096 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の辺連結セル集合の Euler 数の主張に修正対象がなかった。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 整数帯の外側を通る単純な閉路への有限閉包 → 二つの有限閉包の回転数比較による周期単純路の回転数零 → 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-01（48 回目）: 「整数帯の外側を通る単純な閉路への有限閉包」を論法単位（平行座標の一周期ごとの増加／閉包路の構成と単純性／回転数定理の適用）へ割り直し、最初の部品として、巻き付きベクトルに平行な整数座標 $\pi_{\gamma}(a,b)=w_{\mathrm v}(\gamma)a+w_{\mathrm h}(\gamma)b$ を定義し（`def_winding_parallel_coordinate`）、周期並進が平行座標を正の定数 $L(w_{\mathrm v}^2+w_{\mathrm h}^2)\ge1$ だけ増やすことを整数の除法と四則で証明した（`claim_periodic_plane_lift_parallel_period_increase`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・一周期ずらしの比較 123,960 件を検査した。Lean は未着手。
- 2026-09-01（47 回目）: 「周期単純路の回転数零」を論法単位（有限幅の整数帯／帯の外側での有限閉包／二つの閉包の回転数比較）へ割り直し、最初の部品として、巻き付きベクトルに直交する整数横断座標を定義し、周期延長した持ち上げの横断座標の値全体が一周期の有限集合に等しいことを証明した（`claim_periodic_plane_lift_transverse_bounded`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・周期延長した持ち上げ点 123,960 個を検査した。Lean は未着手。
- 2026-08-31（46 回目）: 「周期単純路の回転数零」を論法単位（周期延長した持ち上げの単射性／回転数零の本体）へ割り直し、最初の部品として、整数の除法 $k=qm+r$ による周期延長した平面持ち上げ $\widetilde P_k(\gamma)=P_r(\gamma)+q\cdot(L\,w_{\mathrm v},L\,w_{\mathrm h})$ を定義し（`def_periodic_plane_lift`）、頂点単純かつ非零巻き付きなら単射であることを、代表座標の剰余の議論と巻き付きベクトルの非零性の二段で証明した（`claim_periodic_plane_lift_points_distinct`）。SageMath は非零巻き付きの頂点単純閉路 3,464 本・周期延長した持ち上げ点 123,960 個を検査した。Lean は未着手。
- 2026-08-31（45 回目）: 方向番号の標準整数代表と門 $3\mid0$ の正負横断数を定義し、標準代表の差の望遠和から $t_{\circ}(\gamma)=4(g_+(\gamma)-g_-(\gamma))$ を証明した（`claim_direction_gate_crossing_turning`）。SageMath は非後退な循環方向列 88,592 本・循環遷移 841,564 箇所を検査した。Lean は未着手。
- 2026-08-31（44 回目）: 格子点まわりの内側セルが対角の二セルだけになることが無いことを、訪問頂点（弧の主張の添字差の議論）と非訪問頂点（四接辺の通過回数がすべて $0$ で四セルの内外が一致）の二場合で証明した（`claim_boundary_vertex_diagonal_contact_excluded`）。多角形回転数の証明の本文未証明の断りを参照へ置き換えた。SageMath は頂点単純閉歩道 704 本・格子点 20,096 個を検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-09-01（47 回目）: 姉妹側の「交換子と反交換子の関係」の証明で、五段の鎖に行中の `\quad(\because\dots)` で置かれていた根拠を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（46 回目）: 姉妹側の「Z と Y の反交換関係」の証明で、$Z_\mu$・$Y_\mu$ のクロネッカー積表示の二行の根拠が前置の日本語文だけに置かれていたのを、行末の根拠列（`aligned` の `&&`。定義の適用と直前の表示の読み）へ明記した。内容・式・参照は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（45 回目）: 姉妹側の「$\lambda_{\pm,\mu}=e^{\pm\gamma(\theta_\mu)}$」の証明で、三本の式変形にある根拠 9 行を他の証明と同じ行末の根拠列へ揃えた。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（44 回目）: 姉妹側の「トレースの基本性質」の証明で、巡回性と相似不変性の二本の鎖に行中の `\quad(\because\dots)` で置かれていた根拠 10 行を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-09-01（48 回目）: 前 tick の `def_winding_transverse_coordinate` と `claim_periodic_plane_lift_transverse_bounded` を本文・SageMath（非零巻き付きの頂点単純閉路 3,464 本・持ち上げ点 123,960 個 PASS）・次の閉包の論法で照合した。横断座標の周期不変と値集合の有限性は閉包路を帯の外へ置く議論が直接引く非自明な主張なので「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-09-01（47 回目）: 前 tick の `claim_periodic_plane_lift_points_distinct` を本文・SageMath（非零巻き付きの頂点単純閉路 3,464 本・周期延長した持ち上げ点 123,960 個 PASS）・次の横断座標の論法で照合した。余りの一致と非零巻き付きによる商の一致を担い、周期路解析が直接使う非自明な主張なので「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（46 回目）: 前 tick の `claim_direction_gate_crossing_turning` を本文・SageMath（非後退な循環方向列 88,592 本・循環遷移 841,564 箇所 PASS）・引く定義（一歩の回転数の三場合の標準整数代表による読み）で照合した。望遠和による等式は回転数零の本体が直接引く非自明な主張であり「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（45 回目）: 前 tick の `claim_boundary_vertex_diagonal_contact_excluded` を本文・SageMath（頂点単純閉歩道 704 本・格子点 20,096 個 PASS）・後続参照で照合した。訪問頂点と非訪問頂点の二場合が仮定を尽くし、後続が直接引く非自明な主張なので「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（44 回目）: 前 tick の `claim_hole_free_cell_set_euler_number_one` を本文・SageMath（頂点単純閉歩道 704 本・内側セル延べ 2,888 個 PASS）・台帳で照合した。面数を $|S|+1$ と数えて全域木の数え上げと合成する筋は仮定（辺連結・補集合の辺連結）と既証明の適用条件が一致し、回転数の証明が直接引くため「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
