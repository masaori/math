# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-15 の tick 260 は、「実対数の自然数冪」（$\log_{\mathbb{R}}(u^{n})=\iota_{\mathbb{Q}\to\mathbb{R}}(n)\cdot\log_{\mathbb{R}}(u)$）を記述と SageMath まで進めた。レビューでは前 tick が完成させた
  Lean 三本の名前が本文ブロックの lean 宣言に入っていない欠落を見つけ、補って前進前に main へ
  反映した。** 本体は指数についての帰納法一本で、基底は実対数の 1 における値、歩みは乗法を
  加法へ移す性質と体準同型の加法保存だけを使う（狭義単調性・完備性は使わない）。自然数倍は
  $\iota_{\mathbb{Q}\to\mathbb{R}}(n)$ を通して書き、記号の濫用を避けた。SageMath
  （`sagemath/check/real-log-natural-power/`）は可算側を厳密に、実対数に触れる行だけ ball の
  整合で検査した（合計 451 件）。Lean 未着手で、次 tick はこの一片の Lean
  （具体版・必要十分版・導出）を進める。式変形統一は姉妹側の転送行列章の後続から続ける。

- **2026-08-15 の tick 259 は、「分配多項式の値の上からの評価」の Lean 具体版・必要十分版・
  導出を完成させ、四層を揃えた。レビューでは前 tick の本文と SageMath に不整合は無く、修正は無い。**
  具体版は底と指数についての二つの単調性を本文と同じ帰納法で示し、各項を二度比較して
  定数の有限和へまとめた。必要十分版は格子・配位・実数を外し、有限和と二つの冪の比較だけを
  残し、導出で配位・破れボンド数・実数へ特殊化した。式変形統一では姉妹側
  「$T_{(V)}$ の $\hat Z,\hat Y$ への作用」の共役作用・行列積・四成分の計算へ行末根拠を補い、
  一行に複数の等号があった箇所を開いた。次の本文は「実対数の自然数冪」。次の式変形統一は
  同じ転送行列章の後続証明から進める。

- **2026-08-15 の tick 258 は、「自由エネルギー密度の上からの評価」を「分配多項式の値の上からの評価」
  「実対数の自然数冪」「自由エネルギー密度の上からの評価」の三片へ割り直し、最初の一片を記述と
  SageMath まで進めた（$Z_L(t)\le_{\mathbb{R}}\iota_{\mathbb{Q}\to\mathbb{R}}(2^{L^2})\cdot(1+t)^{2L^2}$。
  順序の議論のみで実対数・完備性は使わない。Lean 未着手）。レビューでは前 tick の Lean 三本に
  不整合は無く、修正は無い。** 割り直しの理由は、冪の比較（帰納法）・実対数の冪の等式（帰納法）・
  両者の合成と、論法が複数あるため。上界の底は $\max(1,t)$ ではなく $1+t$ に取り、
  $t\le_{\mathbb{R}}1$ と $1\le_{\mathbb{R}}t$ の場合分けを避けた。準備は冪の正値性・底の単調性・
  指数の単調性・定数の有限和の四つで、いずれも帰納法。SageMath
  （`sagemath/check/partition-value-upper-bound/`）はすべて $\mathbb{Q}$ の厳密比較で
  浮動小数点を使わない（合計 766 件）。式変形統一では姉妹側「$K_2$ と $K_2^*$ の双対関係」の
  二つの鎖に残っていた根拠なしの等号 4 行ずつへ、指数表示の代入・$\tanh$ の定義・通分・
  記号の定義の行末根拠を補った。次の本文はこの一片の Lean（具体版・必要十分版・導出）。
  次の式変形統一は同じ転送行列章の後続の根拠なし連鎖から続ける。

- **2026-08-14 の tick 257 は、「自由エネルギー密度の下からの評価」の Lean 具体版・必要十分版・
  導出を完成させ、四層を揃えた。レビューでは前 tick の本文と SageMath に不整合は無く、修正は無い。**
  具体版は全て正の配位の破れボンド数を零と示し、その一項による分配多項式の下界、実対数の
  単調性、非負係数との積を本文と同じ順に辿る。必要十分版は格子を外し、有限和の選んだ指数が零、
  正の冪、一で零になる狭義単調写像、非負倍率だけを残し、導出で具体版へ特殊化した。
  式変形統一では姉妹側「直積作用の計算」の二つの三段鎖へ、直積写像の定義・二つの共役作用の
  行列表示・二列を行列へ並べる操作の行末根拠を補った。次の本文は
  「自由エネルギー密度の上からの評価」。次の式変形統一は同じ転送行列章の後続の根拠なし連鎖から続ける。

- **2026-08-14 の tick 256 は、「自由エネルギー密度の極限の存在」から順序の議論だけで閉じる
  部分を「自由エネルギー密度の下からの評価」として切り出し、記述と SageMath まで進めた
  （全て正の定数配位の定義、その破れボンド数が零、自由エネルギー密度の非負性
  $0\le_{\mathbb{R}}\psi_L(t)$。Lean 未着手）。レビューでは、実自由エントロピーと
  自由エネルギー密度の定義に $L\in\mathbb{N}$ の帰属を補った。**
  非負性の証明は、定数配位の 1 項分離による $1\le_{\mathbb{R}}Z_L(t)$、実対数の 1 における値と
  狭義単調性による $0\le_{\mathbb{R}}\varphi_L(t)$、$0<1/L^2$ の実数埋め込みとの積、の三準備と
  本体二鎖で書き、使う性質を宣言済みの順序体の性質・実対数の二性質に限った（完備性は使わない）。
  SageMath（`sagemath/check/free-energy-density-lower-bound/`）は可算側を厳密に、実対数に
  触れる不等式だけ ball の分離で検査した（合計 165 件）。式変形統一では姉妹側
  「ホロノミック量子場 p142 下段」の二つの $V_2$ 共役鎖に残っていたスカラー相殺の圧縮行
  （逆元の分配・可換・相殺・級数展開を一行にまとめていた）を行末根拠付きの四行へ開き、
  鎖の後の段落にあったスカラーの可換と相殺の根拠を鎖の前の準備へ移した。
  次の本文はこの三ブロックの Lean（具体版・必要十分版・導出）。次の式変形統一は
  同じ転送行列章の後続の根拠なし連鎖から続ける。

（これより古い 215 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 5 セクション
- 形式検証の土台: 1 セクション
- 零点の詰め寄り: 5 セクション
- 熱力学極限: 5 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 熱力学極限 | 実対数の自然数冪 | 記述と SageMath まで | $\log_{\mathbb{R}}(u^{n})=\iota_{\mathbb{Q}\to\mathbb{R}}(n)\cdot\log_{\mathbb{R}}(u)$。Lean（具体版・必要十分版・導出）が残り |
| 熱力学極限 | 自由エネルギー密度の上からの評価 | todo | 上の 2 つを合成し $\psi_L(t)$ の $L$ に依らない上界を出す |
| 熱力学極限 | 自由エネルギー密度の極限の存在 | todo | 完備性（上限の存在）を使う。宣言は済んだ |
| 熱力学極限 | 零点密度 | todo | |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-15（tick 260）: 「実対数の自然数冪」を記述と SageMath まで進めた。一ブロック:
  `claim_real_log_natural_power`
  （$\log_{\mathbb{R}}(u^{n})=\iota_{\mathbb{Q}\to\mathbb{R}}(n)\cdot\log_{\mathbb{R}}(u)$。
  指数についての帰納法一本。基底は実対数の 1 における値、歩みは乗法を加法へ移す性質・分配則・
  体準同型の加法保存。自然数倍は $\iota_{\mathbb{Q}\to\mathbb{R}}$ を通して書く。準備は冪の
  正値性のみ。Lean 未着手）。SageMath（`sagemath/check/real-log-natural-power/`）は可算側を
  厳密に、実対数に触れる行だけ ball の整合で検査した（合計 451 件）。

- 2026-08-15（tick 259）: 「分配多項式の値の上からの評価」の Lean 具体版・必要十分版・導出を
  完成させ、四層を揃えた。具体版は底の単調性・指数の単調性を帰納法で示し、各項の二段比較、
  定数の有限和、配位数 $2^{L^2}$ の順を本文と一対一に辿る。必要十分版は有限和と二つの冪の比較だけへ
  仮定を削った。式変形統一では姉妹側「$T_{(V)}$ の $\hat Z,\hat Y$ への作用」の根拠なし等号を
  行末根拠付きの鎖へ整えた。

- 2026-08-15（tick 258）: 「自由エネルギー密度の上からの評価」を「分配多項式の値の上からの評価」
  「実対数の自然数冪」「自由エネルギー密度の上からの評価」の三片へ割り直し（冪の比較の帰納法・
  実対数の冪の帰納法・両者の合成と、論法が複数あるため）、最初の一片を記述と SageMath まで進めた。
  一ブロック: `claim_partition_value_upper_bound`
  （$Z_L(t)\le_{\mathbb{R}}\iota_{\mathbb{Q}\to\mathbb{R}}(2^{L^2})\cdot(1+t)^{2L^2}$。
  底を $1+t$ に取り場合分けを回避。準備は冪の正値性・底の単調性・指数の単調性・定数の有限和。
  Lean 未着手）。式変形統一では姉妹側「$K_2$ と $K_2^*$ の双対関係」の二つの鎖の根拠なし等号へ
  行末根拠を補った。

- 2026-08-14（tick 257）: 「自由エネルギー密度の下からの評価」の Lean 具体版・必要十分版・
  導出を完成させ、四層を揃えた。具体版は全て正の配位、破れボンド数零、一項分離、実対数の
  単調性、非負倍率の順を本文と一対一に辿り、必要十分版は有限和・指数零の選択項・狭義単調写像・
  非負倍率だけへ仮定を削った。式変形統一では姉妹側「直積作用の計算」の二つの三段鎖へ
  行末根拠を補った。

- 2026-08-14（tick 256）: 「自由エネルギー密度の極限の存在」から順序の議論だけで閉じる部分を
  「自由エネルギー密度の下からの評価」として切り出し（存在の証明は完備性という別の道具を要する
  ため）、記述と SageMath まで進めた。三ブロック: 全て正の定数配位の定義、その破れボンド数が零、
  自由エネルギー密度の非負性 $0\le_{\mathbb{R}}\psi_L(t)$（Lean 未着手）。式変形統一では
  姉妹側「ホロノミック量子場 p142 下段」の $V_2$ 共役鎖二つのスカラー相殺の圧縮行を
  行末根拠付きの四行へ開いた。

（これより古い 226 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-15（tick 260）: 前 tick の「分配多項式の値の上からの評価」の Lean 三本を本文と
  突き合わせた。具体版・必要十分版・導出・入口 import・sorry 非依存検査への登録（3 件）は
  本文と一対一で揃っているが、本文ブロック `claim_partition_value_upper_bound` の lean 宣言
  （Lean 名の対応付け）が欠けていたため補い、前進前に main へ反映した。

- 2026-08-15（tick 259）: 前 tick の「分配多項式の値の上からの評価」の本文と SageMath を
  突き合わせた。冪の正値性・底の単調性・指数の単調性・定数の有限和という四つの準備、
  本体の五段の鎖、一般命題と有理数有限標本の区別が一致しているため、修正は無い。

- 2026-08-15（tick 258）: 前 tick の「自由エネルギー密度の下からの評価」の Lean 三本を本文と
  突き合わせた。具体版は破れボンド数零・一項分離・実対数の単調性・非負倍率を本文と同じ順に持ち、
  必要十分版は有限和の選んだ指数零の項・正の冪・一で零になる狭義単調写像・非負倍率だけを要求し、
  導出・入口 import・sorry 非依存検査への登録（3 件）も揃っている。修正は無い。

- 2026-08-14（tick 257）: 前 tick の「自由エネルギー密度の下からの評価」の本文と SageMath を
  突き合わせた。全て正の配位、破れボンド数零、一項分離による $1\le Z_L(t)$、実対数の単調性、
  正の有理係数との積の各段と、可算側を厳密・実対数の不等式を ball の分離で検査する範囲が
  一致しているため、修正は無い。

- 2026-08-14（tick 256）: 前 tick の「自由エネルギー密度と極限の言明の定式化」の Lean 三本を
  本文と突き合わせた。具体版・必要十分版・導出・入口 import・sorry 非依存検査への登録（3 件）は
  本文と一対一で揃っている。一方、前 tick のレビュー記録は「自由エネルギー密度の定義に
  $L\in\mathbb{N}$ を補った」と書きながら、実際の編集は文書冒頭の格子の定義ブロックへ入っており、
  「有限系の実自由エントロピー」と「自由エネルギー密度」の定義は「各 $L\ge1$」のまま帰属が
  無かった。両ブロックへ $L\in\mathbb{N}$ を補い、前進前に main へ反映した。

（これより古い 247 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
