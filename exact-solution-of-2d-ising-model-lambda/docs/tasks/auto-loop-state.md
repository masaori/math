# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-02 の 91 回目の tick で、標準対が位相反転する置換の部分集合の上で、標準対平滑化が位相寄与の符号を反転する不動点の無いファイバー保存対合であることを示した。** 標準接触対が位相反転接触対である置換の集合 $\mathcal B_L\subseteq\mathcal A_L$ を定義し（`def_phase_reversing_standard_subset`）、標準対の平滑化不変性と回転数和の等式の対称性から $S(\mathcal B_L)\subseteq\mathcal B_L$ を導き、既証明の対合性・ファイバー保存・符号反転の必要十分条件を合成した（`claim_phase_reversing_standard_smoothing_involution`）。SageMath は $L=2$ の $\mathcal B_L$ の $4{,}748$ 個の全数で符号反転対合・不動点なし・ファイバー保存を検査した（符号反転 $18{,}992$ 件。Lean 未着手）；次は $\mathcal B_L$ の外に残る置換（$\mathcal A_L$ の回転差 $\pm4$ の $7{,}232$ 個と、標準対が切り替え可能でない $18{,}755$ 個）の相殺または寄与の扱いを決める。
- **2026-09-02 の 90 回目の tick で、接触平滑化が位相寄与の符号を反転するための必要十分条件を固定した。** 二接続の平滑化前後の回転数和が等しい位相反転接触対を定義し（`def_phase_reversing_contact_pair`）、置換符号の反転と局所回転位相の変化を合わせて、位相寄与が符号反転することとの同値を示した（`claim_contact_smoothing_phase_reversal_iff`）。SageMath は $L=2$ の切り替え可能接触対 $192{,}896$ 件×四スピン構造を検査し、符号反転する回転差零は $140{,}416$ 件、位相寄与が不変な回転差 $\pm4$ は各 $26{,}240$ 件だった（Lean 未着手）；次は平滑化で不変な規則により位相反転接触対を選べる部分集合を切り出す。
- **2026-09-02 の 89 回目の tick で、切り替え可能に限らない全接触対の辞書式最小を標準接触対と定め、標準対での平滑化が不動点の無いファイバー保存対合であることを示した。** 接触対の集合 $\operatorname{Ct}(\varphi)$ は動かす辺集合 $M(\varphi)$ と終点写像だけで書かれるため切り替え可能な平滑化で不変であり（`claim_contact_pair_set_smoothing_invariant`）、標準対 $\operatorname{ct}_{\min}(\varphi)$ が切り替え可能な置換の集合 $\mathcal A_L$ の上で $S(\varphi)=\operatorname{Sm}_{\operatorname{ct}_{\min}(\varphi)}(\varphi)$ は $\mathcal A_L$ に留まり、二回適用で元へ戻り、不動点を持たず、ファイバーを保つ（`claim_standard_contact_smoothing_involution`）。SageMath は $L=2$ の接触対を持つ置換 $30{,}735$ 個で不変性 $192{,}896$ 件と $\mathcal A_L$ の $11{,}980$ 個の対合を全数検査した（Lean 未着手）；次は平滑化が位相付き寄与の符号を反転することを示し、標準対が切り替え可能でない残り $18{,}755$ 個の扱いを決める。
- **2026-09-02 の 88 回目の tick で、辞書式最小の切り替え可能接触対は平滑化の前後で保存されないことを反例で固定した。** 同じ対自体は平滑化後も切り替え可能で対合を与えるが、その対と一辺を共有する別の接触対の切り替え可能性が変わり、選ばれる最小対が変わり得る。SageMath は $L=2$ の切り替え可能な接触対を持つ置換 $29{,}905$ 個を全数検査し、$7{,}935$ 個で最小対が変わることを確認した。この単純な最小化は採用せず、次は平滑化で不変な局所データから標準対を選ぶ規則を構成する。
- **2026-09-02 の 87 回目の tick で、同じ接触対が平滑化後も切り替え可能なまま残り、同じ対での平滑化が対合であることを示した。** 前 tick のファイバー保存から $\psi:=\operatorname{Sm}_{\vec e,\vec f}(\varphi)$ が非後退置換で $M(\psi)=M(\varphi)$ であることを使い、非後退性 $\varphi(\vec e)\in\operatorname{Next}(\vec e)$・$\varphi(\vec f)\in\operatorname{Next}(\vec f)$ から交換後の対の三条件を確かめ、場合分けの二回適用で $\operatorname{Sm}_{\vec e,\vec f}(\psi)=\varphi$ を得た（`claim_contact_smoothing_same_pair_involution`）。SageMath は $L=2$ の非後退置換 $30{,}784$ 個中、切り替え可能な接触対 $192{,}896$ 件で三条件の保存と対合の等式を全数検査した（Lean 未着手）；次は相殺対象の置換に標準的な切り替え可能接触対を選ぶ規則を定め、選んだ対が平滑化で保たれることを示して符号反転対合へ組み上げる。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 反転対を含む置換の項 | 位相反転部分集合 $\mathcal B_L$ 上の符号反転対合まで記述・SageMath 完了（Lean 未着手） | 標準対が位相反転する集合 $\mathcal B_L$（$L=2$ で $4{,}748$ 個）の上では、標準対平滑化が位相寄与の符号を反転する不動点の無いファイバー保存対合である。残るのは $\mathcal A_L$ の回転差 $\pm4$ の各 $3{,}616$ 個と、標準対が切り替え可能でない $18{,}755$ 個で、これらの相殺または寄与の扱いを決めて、各添字の $\mathcal K=\mathcal U$ と平方恒等式へ合成する。離散 Whitney 系の Lean 二版は未着手で、同じ列で一件ずつ配線する。 |
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 閉包の周期数の一般化（済。Lean 未着手）→ 二側閉包の周期差では回転数が相殺すること（反証済）→ 巻き付きベクトルの平行階段（済。Lean 未着手）→ 平行階段の横断座標の幅と帯外に留まる条件（済。Lean 未着手）→ 一側閉包の定義と閉性（済。Lean 未着手）→ 一側閉包の頂点単純性（済。Lean 未着手）→ 平面単純閉路のトーラス射影（済。Lean 未着手）→ 射影の平面持ち上げの一致と巻き付き零（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧の仮定の緩和（済。Lean 未着手。$n_{\mathrm{ct}}=0$ を持ち上げ一周期点の相異性へ差し替え、呼び出し側 3 箇所は $n_{\mathrm{ct}}=0$ から相異性を導いて適用する形に直した）→ 内側帯と内側セル連結性の仮定の緩和（済。Lean 未着手）→ 外側帯と補集合連結性の仮定の緩和（済。Lean 未着手）→ 境界頂点の対角接触排除の仮定の緩和（済。Lean 未着手）→ 平面閉多角形の回転数定理の仮定の緩和（済。Lean 未着手。持ち上げ点相異の零巻き付き辺列に正負 4 が立つ。Euler 等式の「とくに」の尾部も同じ仮定へ揃えた）→ 一側閉包の循環総回転数は正負 4（済。Lean 未着手。緩和した定理を射影へ適用）→ 三周期比較の整数補題（済。Lean 未着手。三つの等差項が正負 4 に収まるなら公差は零）→ 周期数差の回転数等式（済。Lean 未着手。一側閉包の循環総回転数は周期数 $c$ に対して公差 $t_{\circ}(\gamma)$ の等差数列）→ 周期単純路の回転数零への合成（済。Lean 未着手。基点 $k_0$・$t=1$ の一側閉包の $c=1,2,3$ の射影回転数と周期数差の等差数列を三周期比較の整数補題へ渡し $t_{\circ}(\gamma)=0$）→ 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-02（91 回目）: 標準対が位相反転する置換の集合 $\mathcal B_L$ を定義し（`def_phase_reversing_standard_subset`）、標準対平滑化 $S$ が $\mathcal B_L$ の上で位相寄与の符号を反転する不動点の無いファイバー保存対合であることを示した（`claim_phase_reversing_standard_smoothing_involution`）。証明は標準対の平滑化不変性、回転数和の等式の対称性による $\mathcal B_L$ の保存、既証明の対合性と符号反転の必要十分条件の合成である。SageMath は $L=2$ の $\mathcal B_L$ $4{,}748$ 個を全数検査した（符号反転 $18{,}992$ 件）。Lean は未着手。
- 2026-09-02（90 回目）: 非後退置換一つの位相寄与 $\mathcal W^{a,b}_L(\varphi)$ と位相反転接触対を定義し、接触平滑化が位相寄与の符号を反転することと二接続の回転数和が平滑化前後で等しいことが同値であると示した（`claim_contact_smoothing_phase_reversal_iff`）。SageMath は $L=2$ の切り替え可能接触対 $192{,}896$ 件×四スピン構造を検査し、回転差零 $140{,}416$ 件だけが符号反転し、回転差 $\pm4$ の各 $26{,}240$ 件では位相寄与が不変であることを確認した。Lean は未着手。
- 2026-09-02（89 回目）: 切り替え可能に限らない全接触対の辞書式最小を標準接触対 $\operatorname{ct}_{\min}(\varphi)$ と定義し（`def_standard_contact_pair`）、接触対の集合が $M(\varphi)$ だけから定まるため平滑化で不変であること（`claim_contact_pair_set_smoothing_invariant`）、標準対が切り替え可能な置換の集合 $\mathcal A_L$ の上で標準対での平滑化が不動点の無いファイバー保存対合であること（`claim_standard_contact_smoothing_involution`）を示した。SageMath は $L=2$ で不変性 $192{,}896$ 件と $\mathcal A_L$ の $11{,}980$ 個を全数検査した。Lean は未着手。
- 2026-09-02（88 回目）: 辞書式最小の切り替え可能接触対を選ぶ候補を検査し、平滑化後も同じ対自体は切り替え可能である一方、最小対は保存されないことを反例として記録した。SageMath は $L=2$ の対象 $29{,}905$ 個中 $7{,}935$ 個で選択結果の変化を検出したため、この規則は採用しない。
- 2026-09-02（87 回目）: 同じ接触対が平滑化後も切り替え可能なまま残り、同じ対での平滑化が対合であることを示した（`claim_contact_smoothing_same_pair_involution`）。非後退性から交換後の対の三条件を確かめ、場合分けの二回適用で $\operatorname{Sm}_{\vec e,\vec f}(\psi)=\varphi$ を得た。SageMath は $L=2$ の切り替え可能な接触対 $192{,}896$ 件で三条件と対合を全数検査した。Lean は未着手。
- 2026-09-02（91 回目）: 姉妹側の「$\gamma_2(\theta_\mu)$ と $\gamma_2(-\theta_\mu)$ の関係」（`relation_of_gamma_2`）で、鎖の直後に置いていた `def_A_theta` の参照を、$\gamma_2$ の定義を引く各行末の `\blkref` へ移した。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 358 ページを通した。
- 2026-09-02（90 回目）: 姉妹側の「フーリエ変換した $Z,Y$ の反交換関係」（`hatZ_hatY_anticommutation`）で、二本の式変形鎖が引く四つの既証明を証明末尾の参照一覧から各行末の `\blkref` へ移した。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 358 ページを通した。
- 2026-09-02（89 回目）: 姉妹側の「$V_1$ を $Z,Y,\varepsilon$ で表す」（`V1_in_Z_Y_epsilon`）の結びの二本の鎖（指数の中身の分解と $V_1$ の結論）で、行中にあった根拠 6 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 356 ページを通した。
- 2026-09-02（88 回目）: 姉妹側の「$V_1$ を $Z,Y,\varepsilon$ で表す」（`V1_in_Z_Y_epsilon`）の Step 3 の鎖で、行中にあった根拠 5 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変である。
- 2026-09-02（87 回目）: 姉妹側の「$V_1$ を $Z,Y,\varepsilon$ で表す」（`V1_in_Z_Y_epsilon`）の Step 2 の鎖で、行中にあった根拠 5 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 356 ページを通した。
- 2026-09-02（91 回目）: 前 tick の `def_nonbacktracking_permutation_phase_contribution`・`def_phase_reversing_contact_pair`・`claim_contact_smoothing_phase_reversal_iff` を本文・SageMath の全数検査・今回の直接利用と照合した。符号反転の必要十分条件という実質を持ち、「何も言っていない主張」や仮定・記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。証明が引く「接続の組み替えの回転差」への前方参照は、文書全体に 48 件ある前方参照の慣習の範囲であり問題としなかった。
- 2026-09-02（90 回目）: 前 tick の `def_standard_contact_pair`・`claim_contact_pair_set_smoothing_invariant`・`claim_standard_contact_smoothing_involution` を本文・SageMath の全数検査・今回の位相寄与の直接計算と照合した。有限集合上の標準対とファイバー保存対合という実質を持ち、「何も言っていない主張」や仮定・記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。
- 2026-09-02（89 回目）: 前 tick の反例ノート（辞書式最小の切り替え可能接触対は平滑化で保たれない）と検算 `lexicographic-switchable-contact-choice-counterexample` を、本文の対合性の主張・台帳のセクション表・本文末尾「この先に書くこと」と照合した。不採用経路の理由の記録という実質を持ち、「何も言っていない主張」や記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。
- 2026-09-02（88 回目）: 前 tick の `claim_contact_smoothing_same_pair_involution` を、本文の三条件・場合分けの二回適用、SageMath の切り替え可能な接触対 $192{,}896$ 件の全数検査、今回の標準対候補の検討と照合した。固定した対での対合という実質を持ち、「何も言っていない主張」や仮定・記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。
- 2026-09-02（87 回目）: 前 tick の `def_switchable_permutation_contact_pair`・`def_permutation_contact_smoothing`・`claim_switchable_contact_smoothing_preserves_fiber` を、本文の三条件と場合分けの定義、SageMath の接触対 $192{,}896$ 件の全数検査、今回の対合性の主張との接続で照合した。ファイバー保存という実質を持ち、「何も言っていない主張」や仮定・記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。
- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
