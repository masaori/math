# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-08-30 の 16 回目の tick で、横断の平滑化が循環総回転数を保つことを四層で証明した。** 平滑化後の出辺の族がすべて非後退後続に属すること（軸の相違による背理法）を確認して平滑化後の循環総回転数 $t_{\mathrm{sm}}(\gamma;k,l)\in\mathbb Z$ を定義し、接続の組み替えの回転差 $\{-4,0,4\}$ と一歩の回転数の値域 $\{0,1,-1\}$ の交わりから、新しく現れる二つの曲がりの和が 0 になることを示して $t_{\mathrm{sm}}(\gamma;k,l)=t_{\circ}(\gamma)$ を得た。SageMath は全横断対 3,584 対で前後の循環総回転数を独立に計算して一致を検査し、Lean 必要十分版には加法可換モノイド上の二点更新の有限和保存だけが残った。次は平滑化が切断線偶奇（巻き付き偶奇）を保つことを確認し、回転数と横断数の偶奇関係の帰納へ進む。
- **2026-08-30 の 15 回目の tick で、一つの横断を平滑化した前後の閉歩道全体の横断数の更新式を四層で証明した。** 平滑化後の横断数 $c_{\mathrm{sm}}(\gamma;k,l)\in\mathbb N$ を定め、頂点ごとの分解・横断の頂点での積表示・他頂点の不変性を結合して $c(\gamma)+1=c_{\mathrm{sm}}(\gamma;k,l)+n_{v,0}(\gamma)+n_{v,1}(\gamma)$ を得た。SageMath は非孤立横断 1,248 対を含む全横断対 3,584 対で前後の横断数を定義から独立に数え、Lean 必要十分版には有限和の一項更新だけが残った。次は平滑化による回転数・横断数・巻き付き偶奇の同時更新を結び、トーラス上の回転位相の符号を横断数と巻き付き偶奇で表す。
- **2026-08-30 の 14 回目の tick で、横断の平滑化が横断の頂点以外の頂点の横断数を変えないことを四層で証明した。** 平滑化後の頂点横断数の積表示・各軸の直進通過数の不変性（$w\ne v$ の場合）・平滑化前の積表示を等式の鎖一本で結び、横断の頂点 $v$ と異なる任意の格子頂点 $w$ で $c_w^{\mathrm{sm}}(\gamma;k,l)=c_w(\gamma)$ を得た。SageMath は全横断対 3,584 対について、他頂点の横断数を平滑化の前後で横断の定義から独立に数えて一致することを 11,904 件検査した。Lean は具体版と、既存の「二軸の直進通過の直積」「一元だけ帰属が変わる」の必要十分版の組合せを通した。次は横断の頂点での平滑化前後の横断数の差を求め、頂点分解と結合して閉歩道全体の横断数の更新式を得る。
- **2026-08-30 の 13 回目の tick で、横断の平滑化後の頂点横断数が、平滑化前の各軸の直進通過数から一つずつ引いた積になることを四層で証明した。** 平滑化後に頂点 $w$ で横断する添字対の個数 $c_w^{\mathrm{sm}}(\gamma;k,l)\in\mathbb N$ を定義し、二軸の直進通過の直積との全単射と前 tick の直進通過数の更新を結合して、横断の頂点 $v$ で $c_v^{\mathrm{sm}}=(n_{v,0}-1)(n_{v,1}-1)$ を得た。SageMath は閉歩道 24,628 件・横断対 3,584 対（非孤立 1,248 対）を横断の定義から独立に数え、Lean 必要十分版に残る仮定は二因子が各一つ減ることと更新後の積表示だけである。前進前レビューでは任意の横断の平滑化が孤立横断だけの定義を引用していた定義域の暗黙の拡張を除いた。次は平滑化前後の頂点横断数の差を求め、他頂点の不変性と頂点分解へ結合する。
- **2026-08-30 の 12 回目の tick で、横断の平滑化が横断の頂点で各軸の直進通過数を一つ減らし、他の頂点では変えないことを四層で証明した。** 平滑化後の出辺 $\vec f_r$ と平滑化後の直進通過数 $n^{\mathrm{sm}}_{w,a}(\gamma;k,l)\in\mathbb N$ を定義し（孤立性は仮定しない）、選んだ二添字が平滑化後に直進を失うこと（方向番号の一歩の更新による）と他の添字の帰属が不変なことから、$S^{\mathrm{sm}}_{w,a}=S_{w,a}\setminus\{k,l\}$ の一元除去で更新式を得た。SageMath は閉歩道 24,628 件・横断対 3,584 対（非孤立 1,248 対）を `ZZ` で厳密検査し、Lean 必要十分版に残る仮定は「述語がただ一つの元で真から偽へ変わる」ことだけである。前進前レビューでは前 tick の積表示の SageMath 検算が左辺を横断の定義から計算していなかった弱点を直し、先行して push した。次は頂点ごとの横断数の更新 $c_v^{\mathrm{sm}}=(n_{v,0}-1)(n_{v,1}-1)$ を積表示と結合して示す。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 循環総回転数の平滑化不変まで | 次は平滑化が切断線偶奇（巻き付き偶奇）を保つことを確認し、回転数・横断数・巻き付き偶奇の帰納的関係からトーラス上の回転位相の符号表示へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-08-30（16 回目）: 平滑化の同時更新のうち、循環総回転数の不変性（二点更新の有限和保存一本の論法）だけを進めた。回転数・横断数・巻き付き偶奇の結合は論法が別なので次 tick 以降へ残した。
- 2026-08-30（15 回目）: 横断の頂点での差と他頂点の不変性を頂点ファイバーの有限和へ戻し、閉歩道全体の更新式一本を四層で示した。
- 2026-08-30（14 回目）: 平滑化前後の頂点横断数の比較のうち、他頂点の不変性（三つの既存主張を結ぶ等式の鎖一本の論法）だけを進めた。横断の頂点での差と頂点分解への結合は別の論法なので次 tick へ残した。
- 2026-08-30（13 回目）: 平滑化後の頂点横断数を横断の定義から直接定め、二軸の直進通過の直積との全単射と各軸の一元減少を一つの論法に結合した。非孤立の横断を含む全標本で、平滑化後の横断数を横断述語から独立に数えて積表示と照合した。
- 2026-08-30（12 回目）: 頂点ごとの横断数の更新を、直進通過数の更新（一元除去の論法）と積表示との結合という二つの論法へ割り、前者だけを四層で示した。平滑化の定義は孤立性を使わないことを明示し、非孤立の横断 1,248 対を含む標本（$L=2,3$ は辺 8 本まで）で検査した。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-08-30（16 回目）: 姉妹側の偶数セクター終結章の「$\varepsilon\check Q_\epsilon=\eta_\epsilon\check Q_\epsilon$ と符号の反転則」の (1) で、一行二等号とその根拠内の一行三等号を、$\check Q_\epsilon q=q$ の四段の準備の鎖と二段の本体の鎖（いずれも一行一等号・行末根拠つき）へ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_02 PASS）・check・PDF 341 ページ通過。
- 2026-08-30（15 回目）: 姉妹側の偶数セクター終結章の「$\varepsilon$ は $\check Z,\check Y,\check\psi$ と反可換」で、$\check\psi_\mu^\dagger$ の反可換性の一行三等号を三段の一行一等号・行末根拠つきへ開き、$Z_j$ の鎖の先頭にも定義の根拠を付けた。内容と参照は不変。
- 2026-08-30（14 回目）: 姉妹側の偶数セクター固有値章の「$\check\Lambda_{\max}=\Lambda^{(1/2)}_M$ であり単純固有値」で、(2) の一行四関係（等号・不等号の混在）と一行三関係、(3) 末尾の一行二等号を、一行一関係・行末根拠つきの鎖へ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_06 PASS）・check・PDF 341 ページ通過。
- 2026-08-30（13 回目）: 姉妹側の偶数セクター固有値章の「$V^{(+)}$ の固有値」で、右固有値等式の一行三等号を三段の一行一等号・行末根拠つきへ開き、最大・最小固有値の積も二段の一続きの式変形へ直した。内容と参照は不変。姉妹側 SageMath 対象検算（check_06 PASS）・check・PDF を通した。
- 2026-08-30（12 回目）: 姉妹側の偶数セクター固有値章の「定数 $c$ の決定」の符号確定段で、散文中の一行二等号 $c^2=(2s_2)^M=((2s_2)^{M/2})^2$ を、Step 3 と正の実数の冪の指数法則の二段の一行一等号・行末根拠つきへ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_05 PASS）・check・PDF 341 ページ通過。








### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-08-30（17 回目）: 前 tick の循環総回転数の平滑化不変性（コミット `d4f69807`）を四層で照合した。本文末尾が、証明済みの「新しい二項の回転数の和は 0」より強い「左回転と右回転が一つずつ」という未証明の帰結を述べていたため削除し、主張を四層で一致させた。独立ブロックには「何も言っていない主張」は無かった。
- 2026-08-30（16 回目）: 前 tick の全体更新式（コミット `60569d64`）を四層で照合した。本文が、閉歩道自身の横断対だけについて述べた頂点分解の主張を「同じ論法」と一言で平滑化後の通過族へ流用していたため、平滑化後の横断対集合の被覆と互いに素の二段を本文へ書き下した（Lean は元から任意の通過族で証明済みで変更なし）。
- 2026-08-30（15 回目）: 前 tick の他頂点の横断数不変性（コミット `1cd10159`）を四層で照合した。本文と検算は一致したが、Lean 必要十分版の「二つの積表示と対応する因子の等式から結果の等式を得る」は等式の代入以外を言っていない。この独立定理と導出定理を削除し、具体版の鎖に代入を直接書き、既存の直積と一元変更の必要十分版に対応づけた。
- 2026-08-30（14 回目）: 前 tick の平滑化後の頂点横断数の積表示（コミット `59ef5f57`）を四層で照合した。本文・SageMath・Lean 二本・sorry 検査登録は一致したが、証明中で同じ等式が独立の表示式と鎖の先頭に二重に書かれ、間に日本語が挟まっていたため、一続きの式変形一本へ統合した（内容と参照は不変）。
- 2026-08-30（13 回目）: 前 tick の平滑化後の直進通過数（コミット `216db278`）を四層で照合した。本文が任意の横断を扱う一方、平滑化後の通過族を孤立横断だけを定義域とする既存定義から引用しており、定義域を暗黙に拡張していた。出辺交換の三場合をその場で直接定義し、孤立性を仮定しないことを式自体から確定させた。定義と更新式は今回の頂点ごとの横断数の更新が直接引くため、「何も言っていない主張」ではない。
## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
