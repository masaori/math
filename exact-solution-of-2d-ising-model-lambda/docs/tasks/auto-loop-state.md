# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-08-31 の 44 回目の tick で、格子点まわりの内側セルが対角の二セルだけになることが無いことを本文と SageMath で固定した。** 訪問頂点では頂点まわりの弧の主張により内側セルが連続な弧をなし、対角対は添字差が $4$ を法として $2$ なので弧の形にならない。非訪問頂点では四接辺の通過回数の総和が $0$ になり、隣接セルの境界奇偶則から四セルの内外がすべて一致する。これで、平面単純閉多角形の回転数の証明に最後まで残っていた本文未証明の断り（境界頂点での対角接触の排除）を実在する参照へ置き換えた（`claim_boundary_vertex_diagonal_contact_excluded`）。SageMath `no-diagonal-only-contact` は長さ $10$ 以下の頂点単純閉歩道 704 本・格子点 20,096 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の辺連結セル集合の Euler 数の主張に修正対象がなかった。
- **2026-08-31 の 43 回目の tick で、空でない有限な辺連結セル集合について、補集合も辺連結なら Euler 数が 1 であることを本文と SageMath で固定した。** セル集合の頂点・辺からなる有限連結平面グラフの面数を $|S|+1$ と数え、全域木から残りの辺を戻す Euler の数え上げと合成して $\chi_{\square}(S)=1$ を得た（`claim_hole_free_cell_set_euler_number_one`）。これを内側セル集合と補集合の既証明の辺連結性へ適用し、平面単純閉多角形の回転数の証明に残っていた未証明の Euler 等式を実在する参照へ置き換えた。SageMath `hole-free-cell-set-euler-number` は長さ $10$ 以下の頂点単純閉歩道 704 本・内側セル延べ 2,888 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の一セル追加の増分等式に修正対象がなかった。
- **2026-08-31 の 42 回目の tick で、セル集合の複体量（頂点集合・辺集合・Euler 数）を定義し、一セルを追加したときの Euler 数の増分等式を本文と SageMath で固定した。** 台帳の「セル複体の Euler の等式」を論法単位（複体量の定義と一セル追加の増分等式／内側セル複体の Euler 数が 1 に等しいこと）へ割り直し、最初の部品として、有限セル集合 $S$ の頂点集合 $\mathcal V_{\square}(S)$・辺集合 $\mathcal E_{\square}(S)$・Euler 数 $\chi_{\square}(S)=|\mathcal V_{\square}(S)|-|\mathcal E_{\square}(S)|+|S|$ を定義し（`def_cell_vertex_set`、`def_cell_edge_set`、`def_cell_euler_number`）、$S$ に属さないセル $x$ について $\chi_{\square}(S\cup\{x\})=\chi_{\square}(S)+1+e_x(S)-v_x(S)$（$e_x,v_x$ は共有辺・共有頂点の個数）を、単一セルの頂点数・辺数が $4$ であることと合併の分配、有限集合の包除で証明した（`claim_cell_complex_one_cell_increment`）。SageMath `cell-complex-one-cell-increment` は $3\times3$ と $2\times4$ の窓の全部分集合にわたる増分等式 3,328 件・単一セルの頂点数・辺数 17 件を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。既存出力のレビューでは前 tick の外側セル補集合の辺連結性に修正対象がなかった。
- **2026-08-31 の 41 回目の tick で、頂点単純閉路の内側セル集合の補集合が空でなく辺連結であることを本文と SageMath で固定した。** 外接長方形の外側のセル集合を共通の辺連結な到達先とし、各外側セルから同じ行を右へ進めた。右方に奇数回通過の縦辺が無ければ外側のまま長方形外へ、あれば最初のその辺の左隣まで外側のまま進み歩道沿いの外側帯へ到達する。外側帯は前 tick で辺連結であり、最大列の訪問頂点で長方形外とも交わるので、全外側セルが一つにつながる（`claim_exterior_cell_set_edge_connected`）。SageMath `exterior-cell-set-edge-connected` は頂点単純な閉じた非後退歩道 704 本・外側セル 864 個・外側帯への到達 432 件・外接長方形外への到達 432 件を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。既存出力のレビューでは前 tick の外側帯の辺連結性に修正対象がなく、並列の式変形統一では姉妹側の「転送行列による分配関数の表式」で後置されていた参照を対応する等号の行末根拠へ移した。
- **2026-08-31 の 40 回目の tick で、頂点単純閉路の歩道沿いの外側帯（訪問頂点を四隅に持つ外側セルの集合）が空でなく辺連結であることを本文と SageMath で固定した。** 「補集合の無限成分以外に成分が無いこと」を論法単位（歩道沿いの外側帯の辺連結性／外側セルの行方向の到達と合成）へ割り直し、最初の部品として、外側セルと歩道沿いの外側帯 $\mathcal O(\gamma)$ を定義し（`def_walk_side_exterior_band`）、内側帯と対称に、各訪問頂点の外側セルが非空な弧をなすこと（訪問頂点まわりの弧の主張の外側の側）と、歩の各辺の通過回数が $1$ になりその辺を挟む二セルのうちちょうど一方が外側で隣り合う訪問頂点の弧を橋渡しすることから、弧と橋の合成で辺連結性を得た（`claim_walk_side_exterior_band_edge_connected`）。SageMath `walk-side-exterior-band-connected` は頂点単純な閉じた非後退歩道 704 本・橋 6,672 箇所・外側帯セル延べ 9,488 個を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。並列の式変形統一は、姉妹側の「共役写像は環準同型」の証明の五本の鎖に行中の $\quad(\because\ \dots)$ で置かれていた根拠 20 行を、行末の根拠列へ揃えた。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除まで記述・SageMath 完了し、回転数の証明が引く部品はすべて本文の証明済み参照になった | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式 → 周期単純路の回転数零 → 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-08-31（44 回目）: 格子点まわりの内側セルが対角の二セルだけになることが無いことを、訪問頂点（弧の主張の添字差の議論）と非訪問頂点（四接辺の通過回数がすべて $0$ で四セルの内外が一致）の二場合で証明した（`claim_boundary_vertex_diagonal_contact_excluded`）。多角形回転数の証明の本文未証明の断りを参照へ置き換えた。SageMath は頂点単純閉歩道 704 本・格子点 20,096 個を検査した。Lean は未着手。
- 2026-08-31（43 回目）: 有限な辺連結セル集合と辺連結な補集合から有限平面グラフの面数を $|S|+1$ と数え、全域木からの Euler の数え上げと合成して $\chi_{\square}(S)=1$ を証明した（`claim_hole_free_cell_set_euler_number_one`）。内側セル集合へ適用して回転数の証明の未証明参照を塞いだ。SageMath は頂点単純閉歩道 704 本・内側セル延べ 2,888 個を検査した。Lean は未着手。
- 2026-08-31（42 回目）: 「セル複体の Euler の等式」を論法単位（複体量の定義と一セル追加の増分等式／内側セル複体の Euler 数が 1 に等しいこと）へ割り直し、最初の部品として、セル集合の頂点集合・辺集合・Euler 数を定義し（`def_cell_vertex_set`、`def_cell_edge_set`、`def_cell_euler_number`）、一セル追加の増分等式 $\chi_{\square}(S\cup\{x\})=\chi_{\square}(S)+1+e_x(S)-v_x(S)$ を有限集合の包除で証明した（`claim_cell_complex_one_cell_increment`）。SageMath は二つの窓の全部分集合にわたる増分等式 3,328 件・単一セルの頂点数・辺数 17 件を検査した。Lean は未着手。
- 2026-08-31（41 回目）: 任意の外側セルから同じ行を右へ進み、奇数回通過の縦辺があれば最初の辺の左隣で歩道沿いの外側帯へ、無ければ外接長方形の外側へ到達することを示した。外側帯と長方形外が最大列の訪問頂点で交わることを前 tick の帯の辺連結性と合成し、内側セル集合の補集合が空でなく辺連結であることを証明した（`claim_exterior_cell_set_edge_connected`）。SageMath は頂点単純な閉じた非後退歩道 704 本・外側セル 864 個・外側帯への到達 432 件・長方形外への到達 432 件を検査した。Lean は未着手。
- 2026-08-31（40 回目）: 「補集合の無限成分以外に成分が無いこと」を論法単位（歩道沿いの外側帯の辺連結性／外側セルの行方向の到達と合成）へ割り直し、最初の部品として、外側セルと歩道沿いの外側帯を定義し、帯が空でなく辺連結であることを内側帯と対称な弧と橋の合成で証明した（`def_walk_side_exterior_band`、`claim_walk_side_exterior_band_edge_connected`）。SageMath は頂点単純な閉じた非後退歩道 704 本・橋 6,672 箇所・外側帯セル延べ 9,488 個を検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-08-31（44 回目）: 姉妹側の「トレースの基本性質」の証明で、巡回性と相似不変性の二本の鎖に行中の `\quad(\because\dots)` で置かれていた根拠 10 行を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（43 回目）: 姉妹側の臨界点の章の「初等的な正弦の評価」の証明で、四本の鎖に行中の `\quad(\because\dots)` で置かれていた根拠 13 行を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・参照は不変。
- 2026-08-31（42 回目）: 姉妹側の臨界点の章の定義 $A:=s_1s_2^*$ の二段の鎖に行中の $\quad(\because\ \dots)$ で置かれていた根拠 2 行を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（41 回目）: 姉妹側の「転送行列による分配関数の表式」の証明で、指数の積公式と分配関数の定義への後置参照を、それぞれ対応する等号の行末根拠へ移した。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。
- 2026-08-31（40 回目）: 姉妹側の「共役写像は環準同型」の証明で、五本の鎖（乗法性・単位性・逆元の準備二本・合成則）に行中の $\quad(\because\ \dots)$ で置かれていた根拠 20 行を、他の証明と同じ行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・参照は不変。姉妹側 check・PDF 349 ページを通した。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-08-31（44 回目）: 前 tick の `claim_hole_free_cell_set_euler_number_one` を本文・SageMath（頂点単純閉歩道 704 本・内側セル延べ 2,888 個 PASS）・台帳で照合した。面数を $|S|+1$ と数えて全域木の数え上げと合成する筋は仮定（辺連結・補集合の辺連結）と既証明の適用条件が一致し、回転数の証明が直接引くため「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（43 回目）: 前 tick の `claim_cell_complex_one_cell_increment` を本文・SageMath（増分等式 3,328 件・単一セルの頂点数と辺数 17 件 PASS）・台帳で照合した。有限集合の包除による増分等式は今回の Euler 数計算を支える再利用可能な非自明な主張であり、「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（42 回目）: 前 tick の `claim_exterior_cell_set_edge_connected` を本文・SageMath（頂点単純歩道 704 本・外側セル 864 個・外側帯への到達 432 件・長方形外への到達 432 件 PASS）・台帳で照合した。外接長方形外の集合と外側帯を共通到達先に取り行方向の到達で合成する筋は仮定と既証明の適用条件が一致し、後続の Euler 等式が直接引くため「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（41 回目）: 前 tick の `claim_walk_side_exterior_band_edge_connected` を本文・SageMath（頂点単純歩道 704 本・橋 6,672 箇所・外側帯セル延べ 9,488 個 PASS）・台帳で照合した。訪問頂点ごとの外側弧を各歩辺に接する外側セルで橋渡しする合成は仮定と既証明の適用条件が一致し、今回の補集合の辺連結性が直接引くため「何も言っていない主張」ではない。記号・住処・参照にも不一致がなく、修正対象なしとした。
- 2026-08-31（40 回目）: 前 tick の `claim_interior_cell_set_edge_connected` を本文・SageMath（頂点単純歩道 704 本・内側セル 2,888 個・帯への到達 2,888 件 PASS）・台帳で照合した。行方向の到達と帯の辺連結性の合成は仮定（零巻き付き・頂点単純）と引く既証明の適用条件が一致し、後続の Euler 等式が直接引くため「何も言っていない主張」ではない。修正対象なしとした。
## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
