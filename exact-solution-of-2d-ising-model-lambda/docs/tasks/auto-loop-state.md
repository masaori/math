# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-08-30 の 12 回目の tick で、横断の平滑化が横断の頂点で各軸の直進通過数を一つ減らし、他の頂点では変えないことを四層で証明した。** 平滑化後の出辺 $\vec f_r$ と平滑化後の直進通過数 $n^{\mathrm{sm}}_{w,a}(\gamma;k,l)\in\mathbb N$ を定義し（孤立性は仮定しない）、選んだ二添字が平滑化後に直進を失うこと（方向番号の一歩の更新による）と他の添字の帰属が不変なことから、$S^{\mathrm{sm}}_{w,a}=S_{w,a}\setminus\{k,l\}$ の一元除去で更新式を得た。SageMath は閉歩道 24,628 件・横断対 3,584 対（非孤立 1,248 対）を `ZZ` で厳密検査し、Lean 必要十分版に残る仮定は「述語がただ一つの元で真から偽へ変わる」ことだけである。前進前レビューでは前 tick の積表示の SageMath 検算が左辺を横断の定義から計算していなかった弱点を直し、先行して push した。次は頂点ごとの横断数の更新 $c_v^{\mathrm{sm}}=(n_{v,0}-1)(n_{v,1}-1)$ を積表示と結合して示す。
- **2026-08-30 の 11 回目の tick で、頂点ごとの横断数が二軸の直進通過数の積になることを四層で証明した。** 頂点 $v$ を軸 $a\in\mathbb Z/2\mathbb Z$ に沿って直進する通過数 $n_{v,a}(\gamma)\in\mathbb N$ を定義し、軸 0 と軸 1 の通過の直積から二添字を小さい順に並べる全単射により $c_v(\gamma)=n_{v,0}(\gamma)n_{v,1}(\gamma)$ を得た。SageMath は閉歩道 1,064 件・頂点 5,480 件（多重直進通過 92 件）を `ZZ` で厳密検査し、Lean 必要十分版に残るのは有限全順序集合と二値写像だけである。前進前レビューでは前 tick の頂点ごとの分解に混入していた未形式化の偶奇帰結を削除し、先行して push した。次は選んだ一つの横断を平滑化したときの $n_{v,0},n_{v,1}$ と $c_v$ の更新式を示す。
- **2026-08-30 の 10 回目の tick で、閉歩道の横断数が通過頂点ごとの横断数の和に分解されることを四層で証明した。** 横断対の二つの通過の頂点が等しいことから横断対の頂点を定め、頂点ごとの横断数 $c_v(\gamma)\in\mathbb N$ を導入し、被覆と互いに素の二段（各対の頂点は $V_L$ の元・一つの対の頂点は一つに定まる）で $c(\gamma)=\sum_{v\in V_L}c_v(\gamma)$ を得た。平滑化が横断数の偶奇へ与える影響は頂点ごとの和へ切り分けられる。SageMath は閉歩道 1,064 件・横断対 96 対を `ZZ` で厳密検査し、Lean 必要十分版に残る仮定は二つの型の決定可能な等号と値の被覆だけである。前進前レビューでは前 tick の孤立横断の平滑化の四層を照合し、修正対象は無かった。次は同じ頂点を三回以上通る頂点の局所解析を、頂点ごとの横断数について行う。
- **2026-08-30 の 9 回目の tick で、孤立した横断の平滑化が横断数を一つ減らして偶奇を反転させることを四層で証明した。** 選んだ横断の頂点を通る通過がその二つだけであることを孤立性として明示し、二つの出辺を交換する平滑化を定義した。選択対は二つの曲がる通過へ変わって横断でなくなり、他の添字対の判定は変わらないため、平滑化後の横断対集合は元の集合から一元だけを除いたものになる。SageMath は有限局所データ 115,400 件を `ZZ` で厳密検査し、Lean 必要十分版に残る仮定は全順序と実在する順序なし対を一つ除くことだけである。前進前レビューでは前 tick の閉歩道の横断数と二倍等式の四層を照合し、「何も言っていない主張」を含む修正対象は無かった。次は同じ頂点を三回以上通る一般の場合を、孤立横断の平滑化へ分解する。
- **2026-08-30 の 8 回目の tick で、閉歩道の通過と横断数を定義し、順序つき横断対の個数が横断数の二倍になることを四層で証明した。** 閉じた非後退辺列の添字集合に巡回後続写像を置いて各添字の通過（非後退接続）と通過の頂点を定め、添字対の横断を頂点の一致と前 tick の横断関係で定義し、横断数を $k<l$ の横断対の個数 $\in\mathbb N$ とした。順序つき横断対の集合を三分律で二つの半分に分け、入れ替え写像が横断の対称性により全単射になることから二倍の等式を得た。SageMath は閉歩道 1,064 件（横断あり 56 件）を `ZZ` で厳密検査し、Lean 必要十分版に残る仮定は添字の全順序と関係の対称性だけである。前進前レビューでは前 tick の横断関係の四層の一致を確認し、修正対象は無かった。次は平滑化による横断数の偶奇更新を定義・証明する。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 平滑化の直進通過数の更新まで | $n^{\mathrm{sm}}_{w,a}$ の更新式は四層済み。次は頂点ごとの横断数の更新 $c_v^{\mathrm{sm}}=(n_{v,0}-1)(n_{v,1}-1)$ を積表示と結合して示す。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-08-30（12 回目）: 頂点ごとの横断数の更新を、直進通過数の更新（一元除去の論法）と積表示との結合という二つの論法へ割り、前者だけを四層で示した。平滑化の定義は孤立性を使わないことを明示し、非孤立の横断 1,248 対を含む標本（$L=2,3$ は辺 8 本まで）で検査した。
- 2026-08-30（11 回目）: 多重通過頂点の局所解析を、平滑化の更新とは別の論法である横断数の積表示へ割った。軸 0 と軸 1 の直進通過の直積と、頂点 $v$ の横断対との全単射一本で $c_v=n_{v,0}n_{v,1}$ を四層で示し、平滑化の更新式は次 tick へ残した。
- 2026-08-30（10 回目）: 一般の閉歩道の孤立横断への分解を、まず横断数の頂点ごとの分解（有限集合のファイバー分割一本の論法）から始めた。横断対の頂点の well-defined 性を定義に置き、被覆と互いに素の二段で和の等式を四層で示した。多重通過頂点の局所解析は別の論法なので次 tick へ残した。
- 2026-08-30（9 回目）: 「平滑化による偶奇更新」を、孤立横断の一元除去と多重通過頂点の分解という異なる論法へ割り、最初の孤立横断を四層で示した。同じ頂点を三回以上通る場合には無条件の偶奇反転が成り立たない有限反例があるため、孤立性を仮定として明示した。
- 2026-08-30（8 回目）: 閉歩道の添字集合と巡回後続写像から通過と通過の頂点を定義し、添字対の横断と横断数（$k<l$ の横断対の個数 $\in\mathbb N$）を定め、順序つき横断対の個数が横断数の二倍になることを、三分律による分割・入れ替え写像の全単射・個数の和の一つの論法で四層で示した。平滑化による偶奇更新は別の論法なので次 tick へ残した。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-08-30（12 回目）: 姉妹側の偶数セクター固有値章の「定数 $c$ の決定」の符号確定段で、散文中の一行二等号 $c^2=(2s_2)^M=((2s_2)^{M/2})^2$ を、Step 3 と正の実数の冪の指数法則の二段の一行一等号・行末根拠つきへ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_05 PASS）・check・PDF 341 ページ通過。
- 2026-08-30（11 回目）: 姉妹側の偶数セクター固有値章の「定数 $c$ の決定」の Step 3 で、トレース比の一行四等号と一行三等号を、それぞれ三段と二段の一行一等号・行末根拠つきへ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_05 PASS）・check・PDF 340 ページ通過。
- 2026-08-30（10 回目）: 姉妹側の偶数セクター固有値章の「V^{(+)} は正定値」の Step 1 と Step 3 で、散文中の一行二等号二つ（平方根因子と $V_2$ の表示）と表示式の一行二等号（$BAB=B^*AB$）を、一行一等号・行末根拠つきへ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_05 PASS）・check・PDF 340 ページ通過。
- 2026-08-30（9 回目）: 姉妹側の偶数セクター固有値章の「V̌′ のトレース」で、逆行列のトレースの一行三等号を三段の一行一等号・行末根拠つきへ開き、正値性の散文中の不等式を因子の二段と有限積の二段へ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_04 PASS）・check・PDF 340 ページ通過。
- 2026-08-30（8 回目）: 姉妹側の偶数セクター固有値章の「V̌′ の固有値」の Step 4 で、散文中の一行三等号（$\check Q_\epsilon y=\check Q_\epsilon^2x=\check Q_\epsilon x=y$）と表示式の一行三等号（$\check V'y=\check V'\check Q_\epsilon y=e^{\check g(\epsilon)}\check Q_\epsilon y=e^{\check g(\epsilon)}y$）を、四段・三段の一行一等号・行末根拠つきへ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check_03 PASS）・check・PDF 340 ページ通過。








### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-08-30（12 回目）: 前 tick の頂点ごとの横断数の積表示（コミット `92d370d1`）を四層で照合した。本文・Lean 具体版と必要十分版・sorry 検査登録は一致したが、SageMath 検算が左辺 $c_v(\gamma)$ を横断の定義から独立に計算せず、直積 $H\times V$ から作った対の非重複だけを見ていた（ほぼ恒真の検査）。左辺を横断述語 `def_index_pair_crossing` で独立に数えて積と比較する形へ強化した（PASS: 頂点 5,480 件）。
- 2026-08-30（11 回目）: 前 tick の横断数の頂点ごとの分解（コミット `c4a23a1b`）を四層で照合した。和の等式は一致したが、本文にだけ「横断数の偶奇は、頂点ごとの横断数が奇数である頂点数の偶奇に等しい」という未形式化の帰結が混入していたため削除し、SageMath の対応外検算も除いた。頂点ごとの横断数の定義と和の等式は後続の多重通過頂点の解析が直接引くため「何も言っていない主張」ではない。
- 2026-08-30（10 回目）: 前 tick の孤立横断の平滑化（コミット `ad144a04`）を本文・`ZZ` の検算 115,400 件・Lean 具体版と必要十分版・sorry 検査登録で照合し、一致を確認した。孤立性の仮定は本文・SageMath・Lean で同じ形（選択した順序なし対だけの一元除去）に固定されており、「何も言っていない主張」・記号の濫用・住処の不一致・未定義記号は無かった。
- 2026-08-30（9 回目）: 前 tick の閉歩道の通過・横断数・順序つき横断対の二倍等式（コミット `e8a15b68`）を本文、`ZZ` 検算、Lean 具体版・必要十分版、sorry 検査登録で照合し、一致を確認した。三定義と二倍等式は今回の平滑化が直接引くため「何も言っていない主張」ではなく、未証明の帰結・記号の濫用・住処の不一致も無かった。
- 2026-08-30（8 回目）: 前 tick の格子頂点の横断関係（コミット `8303d0e1`）を四層で照合した。軸の定義・横断の三条件・対称性の証明・`ZZ` の検算 144 組・Lean 具体版と必要十分版・sorry 検査登録は一致する。対称性の主張は今回の横断数の定義と二倍等式が直接引いたため「何も言っていない主張」ではなく、記号の帰属・住処・一ステップ一定理にも修正対象は無かった。本文末尾の「この先に書くこと」とセクション表の食い違いも無かった。
## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
