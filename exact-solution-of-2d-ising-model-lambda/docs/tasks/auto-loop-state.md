# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-01 の 57 回目の tick で、接続階段とその一周期並進、元と移動後の周期持ち上げの各一周期分を合成し、頂点単純な閉路になることを本文と SageMath で固定した。** 接続階段を進み、移動後の持ち上げを一周期進み、接続階段の周期並進を逆向きに戻り、元の持ち上げを一周期だけ逆向きに戻る有限列を定義した（`def_periodic_lift_closure_cycle`）。閉性と単位格子歩は四部分の端点の一致から、頂点単純性は接続階段と二つの持ち上げの交点の限定・接続階段と非零周期並進の分離・各持ち上げの単射性・二つの持ち上げの分離から示した（`claim_periodic_lift_closure_is_simple_cycle`）。SageMath は閉路 3,464 本・閉包 75,360 本・閉包の頂点 1,490,960 個について閉性・単位歩・終点を除く頂点の相異なりを `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の最初の交点で打ち切る接続階段を本文・SageMath・今回の閉包で照合し、「何も言っていない主張」や不一致は無かった。次は二つの有限閉包の回転数比較により周期単純路の回転数零を示す。
- **2026-09-01 の 56 回目の tick で、移動後の持ち上げに最初に当たる歩数で打ち切った接続階段を定義し、元の持ち上げと始点でのみ、移動後の持ち上げと終点でのみ交わる頂点単純な接続路であることを本文と SageMath で固定した。** 横断座標が上端 $K_{\max}$ の持ち上げ点 $\widetilde P_{k_0}$ を基点に、分離条件 $u\,W_{\perp}>K_{\max}-K_{\min}$ の移動回数 $u$ で反復横断階段を出し、移動後の持ち上げに属する最初の歩数 $s_{\mathrm{hit}}$（終端 $\widetilde P^{[u]}_{k_0}$ が当たるので空でない有限集合の最小元）で打ち切った（`def_first_hit_connecting_staircase`）。頂点の相異なりは階段の単純性、元の持ち上げの回避は帯上端からの分離、始点が移動後の持ち上げに属さないことは横断座標の鎖 $\kappa(\widetilde P^{[u]}_k)\ge K_{\min}+uW_{\perp}>K_{\max}$、途中の回避は最小元の定義から従う（`claim_first_hit_connecting_staircase_meets_lifts_only_at_ends`）。前 tick の反例（階段が反対側の持ち上げを途中で貫く）はこの打ち切りで排除される。SageMath は閉路 3,464 本・上端基点 37,680 個・接続階段の頂点 236,600 個・持ち上げ所属の厳密判定（周期並進ベクトルの整数割り算。有限窓に頼らない）510,640 件を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の直接閉包の反例ノートを再計算し（$\kappa=-b$、移動後の持ち上げ $(0,-2),(0,-1),(1,-1),(1,-2),(2,-2)$、階段の $(0,-1)$ での交差）、修正対象はなかった。次は帯外で接続階段と二つの周期持ち上げを合成する単純閉路を構成する。
- **2026-09-01 の 55 回目の tick で、二つの周期持ち上げを対応点どうしの反復横断階段で結ぶ直接閉包には自己交差が残ることを、整数格子上の反例で確定した。** 一辺 $L=2$ の頂点単純な非零巻き付き閉路の平面持ち上げ $(0,0),(0,1),(1,1),(1,0),(2,0)$ を二回横断平行移動して直接結ぶと、接続階段と平行移動後の持ち上げが頂点 $(0,-1)$ で交わる。二つの持ち上げ同士と二本の階段同士がそれぞれ分離していても、階段と反対側の持ち上げとの交差は排除できないため、この候補を採用せず参照用ノートへ反例と理由を残した。次は反対側の持ち上げも終点以外で避ける接続路を構成する。Lean は未着手。前進前レビューでは前 tick の平行座標幅と二階段分離に「何も言っていない主張」や不一致はなかった。
- **2026-09-01 の 54 回目の tick で、反復横断階段の平行座標の幅の上界を独立の主張へ抽出し、基点の平行座標が幅を超えて離れた二つの反復横断階段が交わらないことを本文と SageMath で固定した。** 幅の上界 $\min\{0,A\}\le\pi_{\gamma}(D^{\gamma,Q,t}_s)-\pi_{\gamma}(Q)\le\max\{0,A\}$（$A=w_{\mathrm h}w_{\mathrm v}$）を `claim_iterated_staircase_parallel_width_bound` として抽出し、前 tick の周期並進分離の証明をこれを引く形へ短縮した。そのうえで、基点の平行座標の差が $|A|$ を真に超える二つの反復横断階段（反復回数は別々でよい）が頂点を共有しないことを、仮定した一致点の平行座標から差を二つの区間内の値の差として評価する一本の鎖で証明した（`claim_parallel_separated_staircases_disjoint`）。SageMath は幅の上界を非零巻き付き対 280 組・平行座標のずれ 16,800 件、分離を基点対 1,120 組・二階段間の頂点比較 315,952 件で `ZZ` 検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の周期並進分離に「何も言っていない主張」や不一致はなかった。
- **2026-09-01 の 53 回目の tick で、反復横断階段がその任意の非零周期並進と交わらないことを本文と SageMath で固定した。** 平行座標 $\pi_{\gamma}$ における階段の幅が $|w_{\mathrm h}w_{\mathrm v}|$ 以下である一方、一周期並進の増分 $L(w_{\mathrm h}^2+w_{\mathrm v}^2)$ はこの幅を真に超えるため、非零周期並進との交点が無いことを証明した（`claim_period_translates_of_iterated_staircase_disjoint`）。SageMath は非零巻き付き対 280 組・階段頂点 16,800 個・非零周期並進との比較 908,544 件を `ZZ` で検査した。Lean は未着手（記述と SageMath まで）。前進前レビューでは前 tick の帯上端からの階段分離に「何も言っていない主張」や不一致はなかった。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 二つの有限閉包の回転数比較による周期単純路の回転数零 → 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-01（57 回目）: 接続階段・移動後の周期持ち上げの一周期分・接続階段の一周期並進の逆向き・元の周期持ち上げの一周期分の逆向きを合成し、閉じた単位格子路で頂点単純であることを証明した（`def_periodic_lift_closure_cycle`, `claim_periodic_lift_closure_is_simple_cycle`）。SageMath は閉包 75,360 本・頂点 1,490,960 個を検査した。Lean は未着手。
- 2026-09-01（56 回目）: 「反対側の持ち上げも終点以外で避ける接続路」として、移動後の持ち上げに属する最初の歩数 $s_{\mathrm{hit}}$ で反復横断階段を打ち切った接続階段を定義し（`def_first_hit_connecting_staircase`）、頂点の相異なり・元の持ち上げとの始点のみの交わり・移動後の持ち上げとの終点のみの交わりを証明した（`claim_first_hit_connecting_staircase_meets_lifts_only_at_ends`）。SageMath は上端基点 37,680 個・接続階段の頂点 236,600 個・持ち上げ所属の厳密判定 510,640 件を検査した。Lean は未着手。
- 2026-09-01（55 回目）: 二つの周期持ち上げを対応点どうしの反復横断階段で結ぶ直接閉包を有限列で検査し、$L=2$ の非零巻き付き閉路で接続階段と平行移動後の持ち上げが頂点 $(0,-1)$ で交わる反例を得た。誤った単純閉路の主張は本文へ入れず、反例と不採用理由を参照用ノートへ固定し、次を「反対側の持ち上げも避ける接続路」と「合成本体」へ割り直した。
- 2026-09-01（54 回目）: 「帯外で二つの反復横断階段と二つの周期持ち上げを合成する単純閉路」の部品として、前 tick の証明中に埋まっていた平行座標の幅の上界を `claim_iterated_staircase_parallel_width_bound` へ抽出し（周期並進分離の証明はこれを引く形へ短縮）、基点の平行座標の差が $|w_{\mathrm h}w_{\mathrm v}|$ を真に超える二つの反復横断階段が交わらないことを証明した（`claim_parallel_separated_staircases_disjoint`）。SageMath は幅 16,800 件・分離の頂点比較 315,952 件を検査した。Lean は未着手。
- 2026-09-01（53 回目）: 「帯外で二つの反復横断階段と二つの周期持ち上げを合成する単純閉路」を、階段どうしの分離と合成本体へ割り直した。最初の部品として、反復横断階段の平行座標の幅が $|w_{\mathrm h}w_{\mathrm v}|$ 以下で、一周期増分 $L(w_{\mathrm h}^2+w_{\mathrm v}^2)$ より真に小さいことから、階段とその非零周期並進が交わらないことを証明した（`claim_period_translates_of_iterated_staircase_disjoint`）。SageMath は非零巻き付き対 280 組・階段頂点 16,800 個・比較 908,544 件を検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-09-01（57 回目）: 姉妹側の「偶セクターと奇セクターでの交換関係」（`why_008_applies_only_to_minus_sector`）の証明ブロックで、Step 1 と二つのセクターの計算にある行中の根拠 21 行を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・段数・参照は不変。
- 2026-09-01（56 回目）: 姉妹側の「双曲線関数の加法定理・半角公式ほか」（`cosh_addition_and_half_angle`）の証明ブロックで、(1)(2)(4) の四本の鎖に行中の `\quad (\because …)` で置かれていた根拠 7 行を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・段数・参照は不変で、姉妹側 check・PDF 349 ページを通した。
- 2026-09-01（55 回目）: 姉妹側の「クロネッカー積の転置（因子ごとの転置になること）」の証明ブロックで、成分比較の四段の鎖に行中の `\quad (\because …)` で置かれていた根拠 4 行を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・段数・参照は不変。
- 2026-09-01（54 回目）: 姉妹側の「クロネッカー積の各因子についての線型性」の証明ブロックで、成分比較の五段の鎖に行中の `\quad (\because …)` で置かれていた根拠 5 行を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・段数・参照は不変で、姉妹側 check・PDF 349 ページを通した。
- 2026-09-01（53 回目）: 姉妹側の「負符号セクターの Rayleigh 上限は正符号セクター以下」の証明ブロックで、Step 2 の二段の不等式に行中の `\quad (\because …)` で置かれていた根拠を行末の根拠列（`aligned` の `&&`）へ揃えた。式変形・根拠・参照は不変。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-09-01（57 回目）: 前 tick の `def_first_hit_connecting_staircase` と `claim_first_hit_connecting_staircase_meets_lifts_only_at_ends` を本文・SageMath（閉路 3,464 本・接続階段の頂点 236,600 個・所属判定 510,640 件 PASS）・今回の閉包構成で照合した。接続階段の単純性と二つの持ち上げとの交点の限定は今回が直接引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。
- 2026-09-01（56 回目）: 前 tick の直接閉包候補の反例ノートを再計算で照合した（$L=2$ の持ち上げ、$\kappa=-b$、$u=2$ の移動後の持ち上げと基点 $(0,0)$ からの階段が $(0,-1)$ で交わること）。反例・不採用理由・「四部分の交差を組ごとに検査する必要」の記述はいずれも正しく、修正対象は無かった。
- 2026-09-01（55 回目）: 前 tick の `claim_iterated_staircase_parallel_width_bound` と `claim_parallel_separated_staircases_disjoint` を本文・SageMath（幅 16,800 件・分離 315,952 件）・直接閉包候補の反例で照合した。両主張自体は非自明で正しく、記号・住処・参照・検算の不一致や「何も言っていない主張」は無かった。一方、両者だけでは階段と反対側の持ち上げとの交差を排除しないことを反例で確定した。
- 2026-09-01（54 回目）: 前 tick の `claim_period_translates_of_iterated_staircase_disjoint` を本文・SageMath（巻き付き対 280 組・比較 908,544 件 PASS）・今回の抽出の論法で照合した。周期並進との分離は閉路合成が直接引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった（幅の導出の抽出は前進側で実施）。
- 2026-09-01（53 回目）: 前 tick の `claim_staircase_from_band_top_meets_lift_only_at_base` を本文・SageMath（閉路 3,464 本・階段頂点 155,040 個・比較 5,727,600 件 PASS）・今回の周期並進分離の論法で照合した。帯上端からの階段が元の周期持ち上げを避けることは閉包路の合成が直接引く非自明な内容で、「何も言っていない主張」・記号・住処・参照・検算の不一致は無かった。

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
