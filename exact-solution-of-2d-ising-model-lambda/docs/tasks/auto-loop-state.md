# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-13 の tick 196 は、前 tick の「二次体の表示の一意性」の Lean 三本と姉妹側
  「$iK_1H_1^{(\pm)}$ と $iK_2^*H_2$ は実対称」の境界項の根拠補いをレビューし、修正不要と
  確認した。そのあとセクション「二次体の正錐と三分律」を論法の数で二つ（二次体の正錐の
  定義・二次体の三分律）へ割り、先頭の「二次体の正錐の定義」を記述と SageMath まで進めた。**
  台集合 $Q_s:=\{a+b\cdot s\mid a,b\in\mathbb{Q}\}$、表示の写像
  $\mathrm{rep}_s:Q_s\to\mathbb{Q}\times\mathbb{Q}$（well-defined 性は表示の一意性から）、
  正錐 $P_s$（表示 $(a,b)$ の有理数の不等式三条件。$0\le a,0\le b,(a,b)\ne(0,0)$／
  $0<a,b<0,2b^2<a^2$／$a<0,0<b,a^2<2b^2$）を定義し、$\mathrm{rep}_s(s)=(0,1)$ が第一条件を
  満たすことでこの正錐が「$s$ を正と宣言する」選択であること、根の取り替え $s\mapsto-s$ で
  同じ構造が対で得られることを remark に書いた。SageMath は三条件と実代数的数 `AA` の
  厳密順序の一致 361 組・取り替えの恒等式 722 組・表示の一意性の対偶 1200 組を検査して
  通過した。**Lean は未着手**で、次 tick はこの定義の Lean を書くか、続けて
  「二次体の三分律」を進める。式変形統一では姉妹側「$V$ は正定値、とくに
  $\mathrm{tr}(V)>0$」を整えた。

- **2026-08-13 の tick 195 は、前 tick の「二次体の表示の一意性」の本文・SageMath と姉妹側
  「$iK_1H_1^{(\pm)}$ と $iK_2^*H_2$ は実対称」の書式変更をレビューし、参照を囲む括弧の
  境界だけを修正して前進前に push した。そのあと「二次体の表示の一意性」の Lean 具体版・
  必要十分版・導出を完成させ、四層すべてを満たした。** 具体版は本文と同じ十四段の等式列、
  一次独立性の適用、二本の六段の等式列を持つ。必要十分版は係数側と値側の加法可換群、
  加法を保つ係数写像、右から掛ける操作の加法・零元保存、一次独立性だけを要求し、乗法の
  結合則・単位元・可換性・体・代数閉性・順序・可算性を要求しない。次は「二次体の正錐と
  三分律」である。式変形統一では姉妹側の同じ証明ブロックに残っていた境界項
  $Y_MZ_1$ の始点と終点へ根拠を補い、ブロック全体を完了した。次 tick は同ファイルの
  「$V$ は正定値、とくに $\mathrm{tr}(V)>0$」から続ける。

- **2026-08-13 の tick 194 は、前 tick の「一と $s$ の一次独立性」の Lean 三本と姉妹側
  「エルミート行列の $\exp$ は正定値」(2)〜(4) の書式変更をレビューし、修正不要と確認した。
  そのあとセクション「二次体の正錐と三分律」から、表示の一意性を独立の論法として
  「二次体の表示の一意性」へ割り出し、その先頭を記述と SageMath まで進めた。** 主張は、
  $s\cdot s=2$ を満たす $s$ と任意の $a,b,a',b'\in\mathbb{Q}$ について
  $a+b\cdot s=a'+b'\cdot s$ ならば $(a,b)=(a',b')$（`claim_quadratic_representation_unique`）。
  証明は差 $\alpha:=a+(-a')$、$\beta:=b+(-b')$ を置き、十四段の鎖で $\alpha+\beta\cdot s=0$
  を導いて一次独立性を当て、六段の鎖二本で $a=a'$ と $b=b'$ を得る。SageMath は `QQbar` の
  2 根 × 有理数標本で対偶 4704 組と鎖の恒等変形 4802 組を厳密検査して通過した。
  **Lean は未着手**で、次 tick はこの主張の Lean（具体版・必要十分版・導出）を完成させる。
  式変形統一では姉妹側「$iK_1H_1^{(\pm)}$ と $iK_2^*H_2$ は実対称」の Step 0 の行列計算三本と
  Step 1・Step 2 の散文内の等号鎖を一行一等号と行末根拠へ整えた。次 tick は同ファイルの
  次の根拠なしの鎖から続ける。

- **2026-08-13 の tick 193 は、前 tick の「一と $s$ の一次独立性」の本文・SageMath と
  姉妹側「エルミート行列の $\exp$ は正定値」(1) をレビューした。SageMath に移項の五段の
  中間等式を直接検査する 4324 組を補い、前進前に別コミットで push した。そのあと
  「一と $s$ の一次独立性」の Lean 具体版・必要十分版・導出を完成させ、四層すべてを
  満たした。** 具体版は本文と同じ背理法と二本の等式列を実装した。必要十分版は体、単射な
  環準同型、平方が二になる元が有理数側に無いことだけを要求し、代数閉性・順序・可算性を
  要求しない。次は「二次体の正錐と三分律」である。式変形統一では姉妹側の同じ証明ブロックの
  (2)〜(4) を一行一等号と行末根拠へ整え、ブロック全体を完了した。次 tick は同ファイルの
  「$iH$ は実対称」から続ける。

- **2026-08-13 の tick 192 は、前 tick の「有理数の平方は二にならない」の Lean 三本と姉妹側
  「$\|A^*\|=\|A\|$ と極限の共役転置」(2)(3) の書式変更をレビューし、修正不要と確認した。
  そのあとセクション「一と $s$ の一次独立性」（`claim_one_s_linearly_independent`）を
  記述と SageMath まで進めた。** 証明は、$a+b\cdot s=0$ のもとで $b\ne0$ と仮定し、
  五段の鎖二本で $s=b^{-1}\cdot(-a)=:r\in\mathbb{Q}$ を導き、$r\cdot r=2$ が
  「有理数の平方は二にならない」と矛盾することから $b=0$、続いて四段の鎖で $a=0$ を得る
  背理法である。SageMath は `QQbar` の 2 根 × 有理数標本で対偶 4416 組と鎖の各段を
  厳密検査して通過した。**Lean は未着手**で、次 tick はこの主張の Lean
  （具体版・必要十分版・導出）を完成させる。式変形統一では姉妹側「エルミート行列の
  $\exp$ は正定値」の (1) を一行一等号と行末根拠へ整えた。次 tick は同ブロックの
  (2)〜(4) から続ける。

（これより古い 156 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 23 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 2 セクション
- 形式検証の土台: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Fisher 零点 | 二次体の正錐の定義 | 記述と SageMath まで | 台集合 $Q_s$・表示の写像 $\mathrm{rep}_s$・正錐 $P_s$（表示 $(a,b)$ の有理数の不等式三条件）と根の取り替えの remark を書いた（2026-08-13 tick 196）。Lean（定義の形式化）が未着手 |
| Fisher 零点 | 二次体の三分律 | todo | 各元が正・零・負（加法の逆元が正）のちょうど一つであることを、表示 $(a,b)$ の符号と $a\cdot a$、$2\cdot b\cdot b$ の比較の場合分けで示す。$a\cdot a=2\cdot b\cdot b$（$b\ne0$）の排除は `claim_no_rational_square_two` から |
| Fisher 零点 | 正錐と加法・乗法の両立 | todo | 正の元どうしの和と積が正であること |
| Fisher 零点 | 正の根の特定 $x_c=\sqrt2-1$ | todo | 上の順序で $-1+s>0$ かつ $-1-s<0$ を判定し、$x_c:=-1+s$（順序が指す唯一の正根）と定義する |
| 有限系の自由エントロピー | 一般の $q$ での $\Phi_L(q)$ の性質（双対な点どうしの関係） | todo | 本文末尾の「この先に書くこと」に「続き」として載っていたが、セクション表に無く実行の列から落ちていた（2026-08-09 に追加）。自己双対点のあとに置くのは、双対な点どうしの関係が Kramers–Wannier 双対を定義してからでないと書けないため |
| 零点の詰め寄り | 相転移を $\mathbb{Q}$ 上の量化言明として書く | todo | |
| 熱力学極限 | 自由エネルギー密度・零点密度（**ここが $\mathbb{R}$ 脱出**） | todo | `realEscape` を具体的に書く |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-13（tick 196）: セクション「二次体の正錐と三分律」を論法の数で二つ
  （二次体の正錐の定義・二次体の三分律。定義の組と、場合分けの論法は別）へ割り、先頭の
  「二次体の正錐の定義」を記述と SageMath まで進めた。台集合 $Q_s$
  （`def_quadratic_field_set`）、表示の写像 $\mathrm{rep}_s$
  （`def_quadratic_representation_map`。well-defined 性は
  `claim_quadratic_representation_unique` から）、正錐 $P_s$
  （`def_quadratic_positive_cone`。表示 $(a,b)$ の有理数の不等式三条件）、根の取り替えの
  remark（`remark_positive_cone_sign_choice`）を書いた。SageMath
  （`sagemath/check/quadratic-positive-cone/`）は三条件と実代数的数 `AA` の厳密順序の一致
  361 組・取り替えの恒等式 722 組・表示の一意性の対偶 1200 組を検査して通過した。
  Lean は未着手。

- 2026-08-13（tick 195）: 「二次体の表示の一意性」の Lean 具体版・必要十分版・導出を
  完成させた。具体版は本文の十四段の等式列、一次独立性の適用、二本の六段の等式列を同じ順に
  実装した。必要十分版は二つの加法可換群、加法を保つ写像、右から掛ける操作の加法・零元保存、
  一次独立性だけへ薄め、導出は具体版の各入力をそのまま渡した。三定理を本文と sorry 検査へ
  登録し、このセクションは四層すべてを満たした。

- 2026-08-13（tick 194）: セクション「二次体の正錐と三分律」から表示の一意性を独立の論法として
  「二次体の表示の一意性」へ割り出した（正錐の定義・三分律とは論法が別であるため。
  well-defined 性の根拠となる主張を先に単独で立てる）。その「二次体の表示の一意性」
  （`claim_quadratic_representation_unique`）を記述と SageMath まで進めた。証明は差
  $\alpha:=a+(-a')$、$\beta:=b+(-b')$ を置き、十四段の鎖で $\alpha+\beta\cdot s=0$ を導いて
  `claim_one_s_linearly_independent` を当て、六段の鎖二本で $a=a'$ と $b=b'$ を得る。
  SageMath（`sagemath/check/quadratic-representation-unique/`）は `QQbar` の 2 根 ×
  有理数標本で対偶 4704 組・鎖の恒等変形 4802 組・同一表示の場合 98 組を厳密検査して
  通過した。Lean は未着手。

- 2026-08-13（tick 193）: 「一と $s$ の一次独立性」の Lean 具体版・必要十分版・導出を
  完成させた。具体版は本文と同じく $b\ne0$ から埋め込み像として $s$ を表し、その平方を
  有理数側へ単射で戻して矛盾させ、最後に $a=0$ を得る。必要十分版は体、単射な環準同型、
  平方が二になる元の不在だけへ薄め、導出は具体版の仮定をそのまま渡した。三定理を本文と
  sorry 検査へ登録し、このセクションは四層すべてを満たした。

- 2026-08-13（tick 192）: セクション「一と $s$ の一次独立性」を記述と SageMath まで進めた。
  主張は、$s\cdot s=2$ を満たす $s\in\overline{\mathbb{Q}}$ と任意の $a,b\in\mathbb{Q}$ について
  $a+b\cdot s=0$ ならば $(a,b)=(0,0)$（`claim_one_s_linearly_independent`）。証明は $b\ne0$ の
  背理法（移項の五段と逆元の五段の鎖で $s=b^{-1}\cdot(-a)=:r\in\mathbb{Q}$、$r\cdot r=2$ が
  `claim_no_rational_square_two` と矛盾）と、$b=0$ のもとでの四段の鎖 $a=0$ である。
  SageMath（`sagemath/check/one-s-linearly-independent/`）は `QQbar` の 2 根 × 有理数標本で
  対偶 4416 組・$b\ne0$ の鎖 4324 組・$b=0$ の段 92 組を厳密検査して通過した。Lean は未着手。

（これより古い 162 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-13（tick 196）: 前 tick の「二次体の表示の一意性」の Lean 三本を本文と
  突き合わせた。具体版は十四段の等式列・一次独立性の適用・二本の六段の等式列を本文と
  同じ順の calc で持ち、必要十分版は加法可換群と加法を保つ写像・右から掛ける操作の
  加法・零元保存・一次独立性だけを要求して既製定理への丸投げになっていない。imports・
  sorry 検査への三定理の登録・本文の `lean` フィールドも揃っている。姉妹側
  「$iK_1H_1^{(\pm)}$ と $iK_2^*H_2$ は実対称」の境界項 $Y_MZ_1$ の根拠補いも等号と
  根拠を失っていない。修正は無い。

- 2026-08-13（tick 195）: 前 tick の「二次体の表示の一意性」の本文・SageMath と、姉妹側
  「$iK_1H_1^{(\pm)}$ と $iK_2^*H_2$ は実対称」の書式変更を突き合わせた。数学内容と検証の
  対応、姉妹側の等号と根拠に欠落は無い。本文の主張で「二の平方根の存在」への参照を囲む
  括弧と、その直後の $2:=1+1$ の定義との境界だけが崩れていたため、二文へ分けて修正した。

- 2026-08-13（tick 194）: 前 tick の「一と $s$ の一次独立性」の Lean 三本を本文と突き合わせた。
  具体版は移項の五段・逆元の五段・平方の書き換え・$b=0$ のもとでの四段を本文と同じ順の
  calc で持ち、必要十分版は体・単射な環準同型・平方が二になる元の不在だけを要求して
  既製定理への丸投げになっていない。sorry 検査への三定理の登録と本文の `lean` フィールドも
  揃っている。姉妹側「エルミート行列の $\exp$ は正定値」(2)〜(4) の書式変更も等号と根拠を
  失っていない。修正は無い。

- 2026-08-13（tick 193）: 前 tick の「一と $s$ の一次独立性」の本文と SageMath、
  姉妹側「エルミート行列の $\exp$ は正定値」 (1) の書式変更を突き合わせた。
  本文の背理法と対偶の厳密標本検査は一致するが、SageMath が仮定から
  $b\cdot s=-a$ を得る五段のうち恒等変形を直接検査していなかった。各中間式の
  `QQbar` における等号を 4324 組で検査するよう補った。姉妹側の等号と根拠に欠落は無い。

- 2026-08-13（tick 192）: 前 tick の「有理数の平方は二にならない」の Lean 三本を本文と
  突き合わせた。具体版は符号の三分・正の有理数への帰着・素数 $2$ の対数成分による $1=m+m$ を
  本文と同じ順に持ち、終端の整数の矛盾は初等整数算術（omega。既存の Lean の流儀と同じ）で
  閉じる。必要十分版は三分・帰着・二倍の矛盾だけを要求し、既製定理への丸投げになっていない。
  sorry 検査への三定理の登録と本文の `lean` フィールドも揃っている。姉妹側
  「$\|A^*\|=\|A\|$ と極限の共役転置」(2)(3) の書式変更も等号と根拠を失っていない。修正は無い。

（これより古い 184 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
