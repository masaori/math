# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-02 の 82 回目の tick で、符号付き偶部分グラフ多項式の平方を、共通辺・対称差・選択集合ごとの符号付き和で層別した。** 符号付き選択和 $\mathcal U^{a,b}_L(D,E)$ を定義し（`def_signed_selection_sum`）、偶部分グラフ順序対の互いに素なファイバー分割と選択集合との全単射を合成して $(Q^{a,b}_L)^2=\sum_{D,E}\mathcal U^{a,b}_L(D,E)x^{2|D|+|E|}$ を示した（`claim_signed_even_subgraph_square_stratified`）。SageMath は $L=2$ の偶部分グラフ順序対 $1{,}024$ 件を四つのスピン構造すべてで検査した（Lean 未着手）；次は $\mathcal K^{a,b}_L(D,E)=\mathcal U^{a,b}_L(D,E)$ を示す。
- **2026-09-02 の 81 回目の tick で、四つの Kac--Ward 行列式の位相和を反転対と単純通過のファイバーで層別する $\overline{\mathbb Q}[x]$ の等式を示した。** ファイバーの位相付き寄与 $\mathcal K^{a,b}_L(D,E)$ を遷移成分の軌道積で定義し（`def_fiber_phase_weight`）、非後退置換の位相和・軌道重みの位相分解・軌道長総和の層別・ファイバーによる互いに素な分割を合成して $D^{a,b}_L(x)=\sum_{(D,E)}\widehat{\mathcal K^{a,b}_L(D,E)}\,x^{2|D|+|E|}$ を得た（`claim_kac_ward_determinant_fiber_stratified_phase_sum`）。SageMath は $L=2$ の四つのスピン構造で、行列式の直接計算と層別和の一致・各軌道の遷移成分積と位相分解の一致を、非後退置換 $30{,}784$ 個・非空ファイバー $609$ 個の全数で検査した。Lambda 側 check（862 ブロック）・linkage 382 検算・Lean 9,616 ジョブ・`sorryAx` 非依存検査・PDF 380 ページを通した（この主張の Lean は未着手）；次は各ファイバーの位相付き寄与を偶部分グラフ対の選択集合 $\mathcal C_L(D,E)$ の符号付き数え上げと対応づけ、平方恒等式への合成へ進む。
- **2026-09-02 の 80 回目の tick で、非後退置換の母関数を反転対と単純通過の対ごとのファイバーで層別する $\mathbb Z[x]$ の等式を示した。** ファイバー $\mathcal N_L(D,E)$ を定義し、単純通過集合の偶数性による添字への帰属・互いに素な分割・軌道長総和の層別を合成した（`claim_nonbacktracking_permutation_stratified_count`）。SageMath は $L=2$ の非後退置換 $30{,}784$ 個を非空ファイバー $609$ 個へ分け、両辺の多項式一致を検査した。Lambda 側 check（859 ブロック）・linkage 381 検算・Lean 9,616 ジョブ・`sorryAx` 非依存検査・PDF 379 ページを通した（この主張の Lean は未着手）；次は各ファイバーの位相付き寄与を偶部分グラフ対の選択集合と対応づけ、平方恒等式への合成へ進む。
- **2026-09-02 の 79 回目の tick で、置換の軌道の長さの総和が反転対と単純通過で数えられる $\mathbb N$ の等式 $\sum_{C\in\mathcal C(\varphi)}|C|=|M(\varphi)|=2|D(\varphi)|+|E_1(\varphi)|$ を示した。** これは非後退置換の置換項の $x$ の冪の指数（軌道の長さの総和）を、偶部分グラフ順序対の層別の指数 $2|D|+|E|$ と同じ形に揃える橋渡しである（`claim_moved_orbit_length_sum_stratified`）。証明は、軌道族の互いに素な分割（`claim_moved_orbit_partition`）と、動く辺集合の台の辺ごとの互いに素な合併・ファイバーの二場合（$D$ の上で 2、$E_1$ の上で 1）による個数の加法性の合成である。前進前レビューでは前 tick の層別数え上げを本文・SageMath・台帳・「この先に書くこと」と照合し、「何も言っていない主張」を含む修正対象は無かった。SageMath は $L=1$ の任意置換 24 個（主張の全称の範囲）と $L=2$ の非後退置換 30,784 個で証明の各段と結論を全数検査した。Lambda 側 check（856 ブロック）・linkage 380 検算・Lean 9,616 ジョブ・`sorryAx` 非依存検査・PDF 378 ページを通した（この主張の Lean は未着手）；次は非後退置換をその反転対・単純通過の対 $(D(\varphi),E_1(\varphi))$ で層別する数え上げ、その後に平方恒等式への合成へ進む。
- **2026-09-01 の 78 回目の tick で、偶部分グラフ順序対の母関数を共通辺と対称差の添字ごとの選択集合の個数で層別する $\mathbb{Z}[x]$ の等式を示した。** 順序対 $(A,B)$ の像 $(A\cap B,\ A\mathbin\triangle B)$ が互いに素で第 2 成分が偶部分グラフの添字集合に入ること、添字ごとのファイバーが $\mathcal P_L(D,E)$ で順序対全体を互いに素に覆うこと、位数の和の等式 $|A|+|B|=2|D|+|E|$ を示し、前 tick のファイバー全単射で $|\mathcal P_L(D,E)|$ を $|\mathcal C_L(D,E)|$ へ置き換えて $\sum x^{|A|+|B|}=\sum|\mathcal C_L(D,E)|x^{2|D|+|E|}$ を得た（`claim_even_subgraph_pair_stratified_count`）。前進前レビューでは前 tick のファイバー全単射を本文・SageMath・仮定 $\operatorname{Even}_L(E)$ の使用箇所と照合し、「何も言っていない主張」を含む修正対象は無かった。SageMath は $L=2$ の順序対 1,024 件・添字 881 組で証明の各段と両辺の多項式一致を全数検査した。Lambda 側 check（854 ブロック）・linkage 379 検算・Lean 9,616 ジョブ・`sorryAx` 非依存検査・PDF 377 ページを通した（この主張の Lean は未着手）；次は非後退置換の軌道とこの層別を対応づける数え上げへ進む。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 反転対を含む置換の項 | 行列式と符号付き偶部分グラフ平方を同じ $(D,E)$ で層別するところまで記述・SageMath 完了（Lean 未着手） | 位相付き寄与 $\mathcal K^{a,b}_L(D,E)$ と符号付き選択和 $\mathcal U^{a,b}_L(D,E)$ を定義し、$D^{a,b}_L(x)$ と $(Q^{a,b}_L)^2$ を同じ次数 $2|D|+|E|$ で層別した。次は各添字で $\mathcal K^{a,b}_L(D,E)=\mathcal U^{a,b}_L(D,E)$ を示し、その後に平方恒等式へ合成する。離散 Whitney 系の Lean 二版は未着手で、同じ列で一件ずつ配線する。 |
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 閉包の周期数の一般化（済。Lean 未着手）→ 二側閉包の周期差では回転数が相殺すること（反証済）→ 巻き付きベクトルの平行階段（済。Lean 未着手）→ 平行階段の横断座標の幅と帯外に留まる条件（済。Lean 未着手）→ 一側閉包の定義と閉性（済。Lean 未着手）→ 一側閉包の頂点単純性（済。Lean 未着手）→ 平面単純閉路のトーラス射影（済。Lean 未着手）→ 射影の平面持ち上げの一致と巻き付き零（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧の仮定の緩和（済。Lean 未着手。$n_{\mathrm{ct}}=0$ を持ち上げ一周期点の相異性へ差し替え、呼び出し側 3 箇所は $n_{\mathrm{ct}}=0$ から相異性を導いて適用する形に直した）→ 内側帯と内側セル連結性の仮定の緩和（済。Lean 未着手）→ 外側帯と補集合連結性の仮定の緩和（済。Lean 未着手）→ 境界頂点の対角接触排除の仮定の緩和（済。Lean 未着手）→ 平面閉多角形の回転数定理の仮定の緩和（済。Lean 未着手。持ち上げ点相異の零巻き付き辺列に正負 4 が立つ。Euler 等式の「とくに」の尾部も同じ仮定へ揃えた）→ 一側閉包の循環総回転数は正負 4（済。Lean 未着手。緩和した定理を射影へ適用）→ 三周期比較の整数補題（済。Lean 未着手。三つの等差項が正負 4 に収まるなら公差は零）→ 周期数差の回転数等式（済。Lean 未着手。一側閉包の循環総回転数は周期数 $c$ に対して公差 $t_{\circ}(\gamma)$ の等差数列）→ 周期単純路の回転数零への合成（済。Lean 未着手。基点 $k_0$・$t=1$ の一側閉包の $c=1,2,3$ の射影回転数と周期数差の等差数列を三周期比較の整数補題へ渡し $t_{\circ}(\gamma)=0$）→ 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-02（82 回目）: 「ファイバー位相和と符号付き選択数え上げの対応」を、符号付き偶部分グラフ平方の選択集合による層別と、各ファイバーでの $\mathcal K=\mathcal U$ の二論法へ分割した。前者として $\mathcal U^{a,b}_L(D,E)$ を定義し、$(Q^{a,b}_L)^2$ の層別等式を示した。SageMath は $L=2$ の順序対 $1{,}024$ 件と四つのスピン構造を全数検査した。Lean は未着手。
- 2026-09-02（81 回目）: ファイバーの位相付き寄与 $\mathcal K^{a,b}_L(D,E):=\sum_{\varphi\in\mathcal N_L(D,E)}\prod_{C}\bigl(-\prod_{\vec e\in C}M^{a,b}_{\vec e,\varphi(\vec e)}\bigr)$ を定義し、非後退置換の位相和・軌道重みの位相分解・軌道長総和の層別・ファイバー分割を合成して、四つの Kac--Ward 行列式を $(D,E)$ ごとに層別する等式を示した（`claim_kac_ward_determinant_fiber_stratified_phase_sum`）。SageMath は $L=2$ の四つのスピン構造・非後退置換 $30{,}784$ 個・非空ファイバー $609$ 個で全数検査した。Lean は未着手。
- 2026-09-02（80 回目）: 非後退置換のファイバー $\mathcal N_L(D,E)$ を定義し、$D\cap E=\varnothing$・$\operatorname{Even}_L(E)$ への帰属、ファイバーによる互いに素な分割、`claim_moved_orbit_length_sum_stratified` の指数等式を合成して、非後退置換の母関数を $\sum |\mathcal N_L(D,E)|x^{2|D|+|E|}$ と層別した（`claim_nonbacktracking_permutation_stratified_count`）。SageMath は $L=2$ の非後退置換 $30{,}784$ 個、非空ファイバー $609$ 個で証明の各段と多項式一致を検査した。Lean は未着手。
- 2026-09-02（79 回目）: 任意の置換 $\varphi\in\operatorname{Perm}(\vec E_L)$ について、軌道の長さの総和・動く辺の個数・反転対と単純通過の層別の三者を結ぶ $\mathbb N$ の等式 $\sum_{C\in\mathcal C(\varphi)}|C|=|M(\varphi)|=2|D(\varphi)|+|E_1(\varphi)|$ を、軌道族の分割と台の辺ごとのファイバーの二場合の個数の加法性で示した（`claim_moved_orbit_length_sum_stratified`）。SageMath は $L=1$ の任意置換 24 個と $L=2$ の非後退置換 30,784 個で全数検査した。Lean は未着手。
- 2026-09-01（78 回目）: 偶部分グラフ順序対の母関数を、像 $(A\cap B,A\mathbin\triangle B)$ による層別・位数の和の等式・ファイバー全単射の合成で、添字 $(D,E)$ ごとの選択集合の個数による和へ書き換える $\mathbb{Z}[x]$ の等式を示した（`claim_even_subgraph_pair_stratified_count`）。SageMath は $L=2$ の順序対 1,024 件・添字 881 組で両辺の多項式一致と証明の各段を全数検査した。Lean は未着手。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-09-02（82 回目）: 姉妹側の「$\kappa(K)$ の基本性質と臨界点 $K_c$」（`kappa_of_K_basic`）の微分計算の鎖で、行中にあった根拠 3 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変である。
- 2026-09-02（81 回目）: 姉妹側の「クロネッカー積がつくる基底」（`tensor_basis`）の Step 1・Step 2 の二本の鎖で、行中にあった根拠 6 行（`\quad (\because …)`）を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 356 ページを通した。
- 2026-09-02（80 回目）: 姉妹側の「$\check Z,\check Y$ の反交換関係」（`anticommutator_of_check_Z_Y`）の三本の鎖で、行中にあった根拠 22 行を `aligned` の行末根拠列（`&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・linkage 118 検算・PDF 356 ページを通した。
- 2026-09-02（79 回目）: 姉妹側の「ノルムの乗法性」（`matrix_norm_submultiplicativity`）の四本の鎖で、行中にあった根拠 14 行（`\quad (\because …)`）を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・PDF 356 ページを通した。
- 2026-09-01（78 回目）: 姉妹側の「最大固有値はどちらのセクターから来るか」（`c_equals_c_plus`）の Step 1 の鎖で、行中にあった根拠 2 行を行末の根拠列（`aligned` の `&&`）へ揃えた。内容・式変形・根拠・参照は不変で、姉妹側 check・PDF 356 ページを通した。





### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-09-02（82 回目）: 前 tick の `def_fiber_phase_weight`・`claim_kac_ward_determinant_fiber_stratified_phase_sum` を、本文の有限和・有限積と式変形鎖、SageMath の非後退置換 $30{,}784$ 個・非空ファイバー $609$ 個の全数検査、台帳と後続の用途で照合した。位相付き係数による行列式の層別という実質を持ち、「何も言っていない主張」や記号・住処・参照・検算の不一致は無かったため、修正対象は無かった。
- 2026-09-02（81 回目）: 前 tick の `def_nonbacktracking_permutation_fiber`・`claim_nonbacktracking_permutation_stratified_count` を、本文の互いに素な分割と式変形鎖、SageMath の非後退置換 $30{,}784$ 個・非空ファイバー $609$ 個の全数検査、本文末尾「この先に書くこと」とセクション表の整合、今回の位相付き層別での直接利用と照合した。母関数の層別という実質を持つ主張であり、「何も言っていない主張」や仮定・住処・参照の不一致は無かったため、修正対象は無かった。
- 2026-09-02（80 回目）: 前 tick の `claim_moved_orbit_length_sum_stratified` を、本文の軌道分割・台の辺ごとのファイバーの二場合・一続きの式変形、SageMath の任意置換 24 個と非後退置換 30,784 個、今回の層別での直接利用と照合した。軌道長総和と指数 $2|D|+|E|$ を結ぶ実質を持ち、「何も言っていない主張」や仮定・住処・参照の不一致は無かったため、修正対象は無かった。
- 2026-09-02（79 回目）: 前 tick の `claim_even_subgraph_pair_stratified_count` を、本文の三段（像の帰属・互いに素なファイバー被覆・位数の和）と式変形鎖、SageMath の順序対 1,024 件・添字 881 組の全数検査、本文末尾「この先に書くこと」とセクション表の整合で照合した。個数の等式という実質を持つ主張であり、「何も言っていない主張」や仮定・住処・参照の不一致は無かったため、修正対象は無かった。
- 2026-09-01（78 回目）: 前 tick の `def_even_subgraph_pair_fiber`・`claim_even_subgraph_pair_fiber_bijection` を、本文の順逆両構成・仮定 $\operatorname{Even}_L(E)$ の使用箇所（逆写像の第 2 成分の偶数性）・SageMath の互いに素な $(D,E)$ 881 件の全数検査・今回の層別での直接利用と照合した。個数の等式という実質を持つ主張であり、「何も言っていない主張」や仮定・住処・参照の不一致は無かったため、修正対象は無かった。

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
