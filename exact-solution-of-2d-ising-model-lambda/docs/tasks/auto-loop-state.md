# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-08-29 の 10 回目の tick で、反転が方向番号を二だけ進めること（$\operatorname{dir}(\iota(\vec e))=\operatorname{dir}(\vec e)+2$ in $\mathbb Z/4\mathbb Z$）を四層で証明した。** 後続の「逆歩道の回転位相の積は元の積の逆数」がこの等式を繰り返し引く。人手証明は方向番号の表の四場合を一行一等号で計算し、SageMath は $L=1,\dots,5$ の全 220 向き付き辺を整数の合同で検査、Lean は四場合分けの具体版と「加法可換モノイドと $c+c=0$」だけを仮定する必要十分版（辺の種類は基底値に吸収され、本質は $2+2=0$）を通した。前進前レビューでは前 tick の辺列ねじれ符号積の最終行に同居していた三操作（分配則・法 2 への還元・偶奇の置換）を一行一等号へ開いた。次は反転辺対の回転位相の積が 1 になること（逆歩道対称性）へ進む。
- **2026-08-29 の 9 回目の tick で、辺列に沿うねじれ符号の積が二つの切断線偶奇だけで決まることを四層で証明した。** 有限な向き付き辺列の横・縦切断線偶奇を定義し、各辺のねじれ符号の積を $(-1)^{a h+b v}$ へまとめた。SageMath は長さ 0 から 8 の全偶奇列 349,524 件を整数で厳密検査し、Lean は正方格子の向き付き辺に固定した具体版と、任意の型・二つの Bool 値写像だけを使う必要十分版を通した。前進前レビューでは前 tick の形式的平方根の存在証明に添字集合を補い、定数項の一行二等号を二段へ開いた。並列ストリームでは姉妹側の「数演算子の同時固有空間分解」の Step 1 を一行一等号へ開いた。次は回転位相と自己交差符号を閉歩道について追跡する。
- **2026-08-29 の 8 回目の tick で、係数再帰による定数項 1 の形式的平方根の存在を四層で証明した。** 平方根係数列を再帰 `s_0=1, s_n=(d_n−Σ_{j=1}^{n-1}s_js_{n−j})/2` で定義し、Cauchy 積の n 次係数を j=0 と j=n の項の分離で `2s_n+中間和` へ直して d_n へ戻した。SageMath は `QQbar` の打ち切り次数 8 の厳密検算、Lean は `Qbar[[X]]` 具体版と「可換環と 2 の可逆性」だけの必要十分版を通した（存在は一意性と違い零因子があっても通る）。前進前レビューでは、前 tick の形式的冪級数の定義に和・差・加法逆元が未定義のまま使われていたのを補い、定数項を取る行を一行一等号の鎖へ開いた（コミット済み・push 済み）。次は歩道展開によるセクター符号。
- **2026-08-29 の 7 回目の tick で、定数項 1 の形式的平方根の一意性を四層で証明した。** 代数的数係数の形式的冪級数を係数列と Cauchy 積で定義し、二つの平方根の差と和の積を零へ因数分解して、零積性と定数項 `1+1=2\ne0` から分岐を一意に固定した。SageMath は `QQbar` の有限標本、Lean は `Qbar[[X]]` 具体版と「零因子なし・2 が非零」だけの必要十分版を通した。元の「構成と一意性」は論法が二つなので分割し、次は係数再帰による存在構成を行う。前進前レビューでは直前の行列式定数項の四層を照合し、「何も言っていない主張」を含む修正対象は無かった。並列ストリームでは姉妹側の「数演算子の積のトレース」の複数等号を一行一等号へ開いた。
- **2026-08-29 の 6 回目の tick で、前 tick の行列式の定数項の証明に残っていた未定義記号三つ（クロネッカーのデルタ・その場限りの評価写像・$\overline{\mathbb Q}$ 行列の行列式）を除き、証明を既存の評価写像 $\mathrm{aev}_0$ と置換展開経由の鎖へ書き直した。** 多項式行列の定義は成分ごとの式（$\delta$ 抜き）へ直し、Lean 具体版・必要十分版も同じ置換展開の経路（`Matrix.det_apply'`・`eval_finsetSum`・`Finset.sum_eq_single`）へ揃えた。SageMath は主張が不変なので変更なし（再実行 PASS）。全検証（check 527 ラベル・PDF 288 ページ・linkage 299 件・`lake build` 9,524 jobs・sorry 検査 1,480 件）を通した。レビューと Lean の書き直しで締切に達したため、todo 先頭の「定数項 1 の形式的平方根の構成と一意性」は未着手のまま（$\overline{\mathbb Q}[x]$ に零積性質の道具が無いことも判明。次 tick はそこから）。並列ストリームでは姉妹側の「数演算子どうしは可換」の鎖を開いた。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 反転の方向番号シフトまで | Arf--Fourier 符号投影、四遷移行列、行列式、定数項 `1` の形式的平方根、辺列のねじれ符号積、反転の方向番号シフトは四層済み。次は反転辺対の回転位相の積が 1 になること（逆歩道対称性）、その後に回転位相と自己交差符号を閉歩道について追跡する。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-08-29（10 回目）: 反転が方向番号を二だけ進めることを四層で示した。方向番号の表の四場合の計算が唯一の論法である。Lean 必要十分版の仮定は加法可換モノイドと $c+c=0$ だけで、辺の種類（横・縦）は基底値として吸収され本質でないことが分かった。
- 2026-08-29（9 回目）: 有限な向き付き辺列の二つの切断線偶奇を定義し、辺ごとのねじれ符号の積が $(-1)^{a h+b v}$ になることを四層で示した。閉性・接続・回転位相を使わない符号因子だけを分離したため、次は閉歩道の回転位相と自己交差符号を一つの論法として扱う。
- 2026-08-29（8 回目）: 平方根係数列の再帰を定義し、定数項 1 の形式的平方根の存在を四層で示した。Cauchy 積の係数比較（j=0 と j=n の項の分離→再帰で d_n へ）が唯一の論法である。Lean 必要十分版の仮定は可換環と 2 の可逆性だけで、一意性が要した零積性は存在には不要だった。
- 2026-08-29（7 回目）: 代数的数係数の形式的冪級数を係数列と Cauchy 積で定義し、定数項が 1 の平方根の一意性を因数分解・零積性・標数零から四層で示した。「構成と一意性」は論法が二つなので、一意性と係数再帰による存在構成へ分割した。
- 2026-08-29（5 回目）: $\overline{\mathbb Q}[x]$ 成分の有限行列と行列式を定め、四つの Kac--Ward 多項式行列 $K^{a,b}(x)=I-x\widehat M^{a,b}$ と行列式 $D_L^{a,b}(x)$ を定義した。$x=0$ での評価を行列式へ通す四段で定数項が 1 であることを示し、SageMath は $L=1,2$、Lean は具体版・必要十分版を通した。有限トーラス公式全体は継続扱いである。

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

- 2026-08-29（10 回目）: 姉妹側の偶数セクター固有値章の「数演算子の同時固有空間分解」（`check_joint_eigenspace_decomposition`）の Step 2 で、一行に三つの等号が同居していた「和が単位行列」の鎖を、主張の左辺から始まる一行一等号・行末根拠つきの四段（定義→分配法則→Step 0 の和→単位行列の有限積）へ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check\_02 PASS）・check・PDF 338 ページ通過。
- 2026-08-29（9 回目）: 姉妹側の偶数セクター固有値章の「数演算子の同時固有空間分解」（`check_joint_eigenspace_decomposition`）の Step 1 で、異なる射影子の積が零になる三等号を一行一等号・行末根拠つきへ開き、同じ添字の射影子の冪等性も積の定義・因子の可換性・各因子の冪等性・積の定義の四段へ開いた。内容と参照は不変。姉妹側 SageMath 対象検算（check\_02 PASS）・check・PDF 338 ページ通過。
- 2026-08-29（8 回目）: 姉妹側の偶数セクター固有値章の「数演算子の同時固有空間分解」（`check_joint_eigenspace_decomposition`）の Step 0 で、1 行に複数の等号が同居していた四つの関係（冪等性二つ・異添字積の零・和の単位行列）を、関係ごとの一行一等号・行末根拠つきの鎖へ開き、機械識別子の行末根拠（`check_number_operator_idempotent (3)` 等）を人間可読な名前へ直した。`= 0 = R^{(0)}R^{(1)}` と畳まれていた逆順の積も独立の鎖にした。内容と参照は不変。姉妹側 SageMath 対象検算（check\_02 PASS）・check・PDF 337 ページ通過。
- 2026-08-29（7 回目）: 姉妹側の偶数セクター固有値章の「数演算子の積のトレース」で、数演算子の冪等性への機械識別子を人間可読な名前とラベル参照へ直し、移項と 2 による除法、二つの分岐、帰納法の最終計算に同居していた複数の等号を一行一等号・行末根拠つきの鎖へ開いた。主張内容と参照は不変。

- 2026-08-29（6 回目）: 姉妹側の偶数セクター固有値章（`017_even_sector_eigenvalues.ts`）の「数演算子どうしは可換」（`check_number_operators_commute`）で、Step 3 の一行に六つの等号が並んでいた鎖を一行一等号・行末根拠つきの七段へ開き（数演算子の定義・結合法則・(1) の適用を明示）、Step 1・Step 2 の行末根拠の機械識別子（`anticommutator_of_check_psi`・`def_check_number_operator`）を人間可読な名前（「$\check\psi$ の反交換関係の第 1〜3 式」「数演算子の定義」）へ直し、Step 2・Step 3 の導入散文へ `def_check_number_operator` のラベル参照を補った。元の内容と参照は不変。姉妹側 SageMath 対象検算（check\_01\_number\_operator PASS）・check・PDF 336 ページ通過。







### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録
- 2026-08-29（10 回目）: 前 tick の辺列ねじれ符号積（コミット `05044e54`）を四層で照合した。主張・SageMath（全偶奇列 349,524 件）・Lean 具体版・必要十分版は一致し、「何も言っていない主張」の削除対象は無かった。一方、証明の最終行に分配則・法 2 への還元・偶奇の置換の三操作が同居していたため、一行一等号・行末根拠つきの鎖へ開いた（コミット済み・push 済み）。主張内容は不変。
- 2026-08-29（9 回目）: 前 tick の形式的平方根の存在（コミット `7cc86e17`）を四層で照合した。平方根係数列の再帰と Cauchy 積の係数比較は本文・SageMath・Lean 具体版・必要十分版で一致し、独立した「何も言っていない主張」は無かった。一方、statement の形式的冪級数の和で添字集合が省略され、定数項の確認が一行二等号になっていたため、添字を `n\in\mathbb N` と明記し、定数項を一行一等号・行末参照つきの二段へ開いた。主張内容は不変。
- 2026-08-29（8 回目）: 前 tick の形式的平方根の一意性（コミット `0876f71a`）で、形式的冪級数の定義が Cauchy 積と定数項しか定めておらず、証明が使う和・差・加法逆元が未定義のままだった。定義ブロックへ係数ごとの和と加法逆元・差の記法を補い、定数項を取る行（1 行 3 等号・根拠なし）を一行一等号・行末根拠つきの鎖へ開いた。主張内容と Lean・SageMath の対応は不変。
- 2026-08-29（7 回目）: 直前の行列式定数項の本文・SageMath・Lean 具体版・必要十分版を照合した。定数項の主張は後続の平方根分岐の基点を確定しており、体の四則だけを独立させた「何も言っていない主張」ではない。記号の住処と対応も一致し、修正対象は無かった。
- 2026-08-29（6 回目）: 前 tick の行列式の定数項の証明（コミット `1d8d699b`）に、未定義の記号が三つ残っていた。クロネッカーのデルタ $\delta_{\vec e,\vec f}$（双対辺写像 $\delta_L$ と記号衝突）、既存の評価写像 $\mathrm{aev}_w$ と重複するその場限りの $\operatorname{ev}_0$、未定義の「$\overline{\mathbb Q}$ 行列の行列式」である。多項式行列の定義を成分ごとの式（デルタ抜き）へ直し、証明を $\mathrm{aev}_0$ と置換展開経由の鎖（係数和→評価→置換展開→項ごとの評価→恒等置換だけが残る）へ書き直し、Lean 具体版・必要十分版も同じ置換展開の経路へ揃えた。SageMath は主張が不変なので変更なし（再実行 PASS）。
## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
