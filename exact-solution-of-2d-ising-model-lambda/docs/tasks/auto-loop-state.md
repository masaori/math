# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-13 の tick 169 は、レビューで前 tick の Lean ファイル冒頭が全行・全列の
  周期和の零性を実装済みとしていないずれを直して独立に push したあと、道和の定義と
  横向き辺の差を Lean 具体版で完了した。** 各座標の `ZMod.val` 代表に沿う二本の
  有限和として道和を定義し、横座標が末尾未満なら有限和の末尾の一項、末尾なら全行和の
  零性を使って、隣接頂点の道和の差が横辺の所属指示子に等しいことを示した。
  次 tick は縦向き辺の差を形式化する。式変形の書き方の統一では、姉妹側の
  「$T_{(V')}$ の $\psi$ への作用」の Step 2 の結論を一行一等号の三段へ開いた。

- **2026-08-13 の tick 168 は、レビューで前 tick の Lean ファイル冒頭の説明を実装済み範囲
  （二周期・行和列和まで）へ合わせて独立に push したあと、配位復元の「道和と辺差の等式」を
  論法の数で三つ（全行・全列の周期和の零性・道和の定義と横向き辺の差・縦向き辺の差）へ割り、
  先頭の「全行・全列の周期和の零性」を Lean 具体版で完了した。** 二周期の等式を出発点、
  行和・列和の不変性を一歩とする自然数の帰納法で、任意の行の横辺和と任意の列の縦辺和が
  零であることを示した。sorry 検査は 749 件で通過した。次 tick は道和の定義と横向き辺の差の
  等式を形式化する。式変形の書き方の統一では、姉妹側の「$T_{(V')}$ の $\psi$ への作用」の
  Step 2 の冒頭の鎖と $\gamma_2$ の周期性の二本を一行一等号へ開いた。

- **2026-08-13 の tick 167 は、レビューで前 tick の Lean ファイル冒頭を実装済み範囲へ
  合わせて独立に push したあと、配位復元の存在構成のうち「二周期と行和・列和の等式」を
  Lean 具体版で完了した。** 自明セクターの二つの巻き付き偶奇を双対辺写像の逆写像で
  復元辺集合の周期閉路和へ戻し、格子面の等式を一周期にわたって足して、有限和の添字ずらしと
  $2=0$（$\mathbb Z/2\mathbb Z$）から行和・列和が隣接行・列で不変であることを得た。
  次 tick は道和と辺差の等式を形式化する。式変形の書き方の統一では、姉妹側の
  $\gamma_2$ の周期性を二つの連続した等号列へ開いた。

- **2026-08-12 の tick 166 は、レビューで必要十分版コメントの「対合」の記述と台帳から
  落ちていた進行中セクション行を直して独立に push したあと、配位復元の存在構成の Lean を
  論法の数で四つ（格子面の等式・二周期と行和列和の等式・道和と辺差の等式・復元の組み立て）へ
  割り、先頭の「格子面の等式」を Lean 具体版で完了した。** 復元した辺集合
  `reconstructedEdgeSet`（人手証明の $B=\delta_L^{-1}(A)$）を定義し、双対像の局所端点数を
  任意の辺集合の四つの所属指示子で書く一般化を経て、偶部分グラフであることだけから
  $b_{\mathrm v}(i,j)+b_{\mathrm h}(i,j)+b_{\mathrm v}(i,j+1)+b_{\mathrm h}(i+1,j)=0$
  （$\mathbb Z/2\mathbb Z$）を得た。次 tick は二周期の等式と行和・列和の望遠鏡和を形式化する。
  式変形の書き方の統一では、姉妹側の同じ証明の場合 2（$\gamma_2(\theta_\mu)=0$）を
  一行一等号の三段の鎖へ開いた。

- **2026-08-12 の tick 165 は、前 tick の配位復元証明に欠けていた根拠をレビュー修正して
  独立に push したあと、同じセクションの Lean 個数計算を進めた。** 復元した配位が一つ存在すれば、
  全スピン反転が異なる第二の原像を与え、同じ双対破れ像を持つ配位はこの二つに限ることを
  具体版・必要十分版・導出で示した。基点からの道和によって最初の配位を構成する Lean は未了なので、
  セクションはまだ `done` ではない。次 tick はこの存在構成を本文と一対一に形式化する。
  式変形の書き方の統一では、姉妹側の同じ証明に残っていた $\hat Y_\mu$ の作用を五段の鎖へ整えた。

（これより古い 130 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 10 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 2 セクション
- 形式検証の土台: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Fisher 零点 | 自明セクターの偶部分グラフから配位を復元する | 記述と SageMath と Lean 個数部分・道和の定義と横向き辺の差まで | 全行・全列の周期和の零性（tick 168）、道和の定義と横向き辺の差（tick 169）まで済。格子面の等式（tick 166）・二周期と行和列和の等式（tick 167）も済。残りは縦向き辺の差・復元の組み立て。必要十分版・導出はセクション完了時にまとめて判定する |
| Fisher 零点 | 非自明な三セクターを捨てず、周期・反周期の四境界条件の混合として低温展開と高温展開を可算な双対恒等式へ結ぶ | todo | 周期格子は平面グラフではないため、周期分配関数一つをそのまま自己双対としない |
| Fisher 零点 | 自己双対点 $x_c=\sqrt2-1$ | todo | |
| 有限系の自由エントロピー | 一般の $q$ での $\Phi_L(q)$ の性質（双対な点どうしの関係） | todo | 本文末尾の「この先に書くこと」に「続き」として載っていたが、セクション表に無く実行の列から落ちていた（2026-08-09 に追加）。自己双対点のあとに置くのは、双対な点どうしの関係が Kramers–Wannier 双対を定義してからでないと書けないため |
| 零点の詰め寄り | 相転移を $\mathbb{Q}$ 上の量化言明として書く | todo | |
| 熱力学極限 | 自由エネルギー密度・零点密度（**ここが $\mathbb{R}$ 脱出**） | todo | `realEscape` を具体的に書く |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-13（tick 169）: `ZMod.val` による縦向きの有限和と横向きの有限和の和として
  基点付き道和を Lean で定義し、横向きの隣接頂点の道和の差が対応する辺の所属指示子に
  等しいことを示した。代表が末尾未満の場合は有限和の末尾の一項、末尾の場合は全行和の
  零性を使った。sorry 検査へ定理を登録し、750 件で通過した。

- 2026-08-13（tick 168）: 配位復元の存在構成の残り「道和と辺差の等式」を論法の数で三つ
  （全行・全列の周期和の零性・道和の定義と横向き辺の差・縦向き辺の差）へ割り、先頭の
  「全行・全列の周期和の零性」を Lean 具体版で完了した。二周期の等式（行 $-1$・列 $-1$）を
  帰納法の出発点に、行和・列和の不変性を一歩に取り、$-1+n$ の形の自然数帰納法で任意の
  行・列へ伝播させた。任意の元 $i$ は $-1+(i+1)$ と表し、$i+1$ を自然数の値へ戻して
  帰納法へ当てた。sorry 検査へ一定理を登録し、749 件で通過した。

- 2026-08-13（tick 167）: 自明セクターの巻き付き偶奇を、双対辺写像の逆写像と周期有限和の
  添字交換によって、復元した辺集合の縦向き・横向き周期閉路和が零である二式へ戻した。
  格子面の四指示子の等式を一列または一行にわたって足し、周期添字を一つずらした和を元の和へ
  戻したうえで、重複する二つの和を $2=0$ により消去し、横辺の行和と縦辺の列和がそれぞれ
  隣接する行・列で不変であることを示した。sorry 検査へ二定理を登録し、748 件で通過した。

- 2026-08-12（tick 166）: 配位復元の存在構成の Lean を論法の数で四つへ割り
  （格子面の等式・二周期と行和列和の等式・道和と辺差の等式・復元の組み立て）、先頭の
  「格子面の等式」を具体版で完了した。双対像の局所端点数の計算を任意の辺集合へ一般化する
  補題を置き、復元した辺集合の四指示子の $\mathbb Z/2\mathbb Z$ 和が零であることを
  偶部分グラフの定義から導いた。sorry 検査へ 5 件を登録し 746 件で通過。

- 2026-08-12（tick 165）: 前 tick の本文が構成した配位を Lean で得た後に必要となる個数計算を
  分離した。双対破れ像は全スピン反転で不変であり、二つの双対破れ像が等しければ双対辺写像の
  単射性で元の破れた辺集合も等しいので、既存の一意性から二配位は等しいか全反転で結ばれる。
  必要十分版は、有限写像の一つの原像について、値を保つ不動点のない対と、同じ値を持つ元が
  その対だけであるという仮定だけを残した。具体版と導出は、この仮定を全スピン反転と既存の
  破れた辺集合の一意性で埋める。基点からの道和による最初の原像の存在構成は次 tick へ残した。

（これより古い 135 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-13（tick 169）: 前 tick の全行・全列の周期和の零性を本文と突き合わせ、
  二周期の等式を出発点、行和・列和の不変性を帰納法の一歩に使う手順が一致することを確認した。
  一方、Lean ファイル冒頭が「道和と辺差の等式」全体を未実装扱いしていたため、実装済みの範囲と
  残る二つの辺差の論法が分かる説明へ直した。

- 2026-08-13（tick 168）: 前 tick の「二周期の等式」「行和・列和の等式」の Lean を本文と
  突き合わせた。縦向き `true`＝$b_{\mathrm v}$・横向き `false`＝$b_{\mathrm h}$ の対応、
  添字を一つずらした和の戻し、$2=0$ による重複和の消去は本文と一致していた。一方、
  ファイル冒頭の説明が「二周期と行和・列和の等式は続く tick で加える」のまま実装済みに
  なっていたため、実装済みの範囲と残る二論法を正しく示す説明へ直した。

- 2026-08-13（tick 167）: 前 tick の配位復元の格子面等式を本文・SageMath と突き合わせた。
  四指示子の順序と結論は一致していたが、Lean ファイル冒頭が「個数計算だけを置き、存在構成は
  後続 tick で加える」と書かれたまま、既に格子面等式まで実装済みだった。実装済みの範囲と、
  存在構成に残る三つの論法を正しく示す説明へ直した。

- 2026-08-12（tick 166）: 前 tick の配位復元の個数部分の Lean を本文と突き合わせた。
  定理・導出は本文の末尾と一致していたが、必要十分版のファイル冒頭コメントが「対合」と
  書いており、定理は対合性を仮定していない（値の保存・不動点なし・一意性だけ）ので
  コメントを実態へ直した。また 23:05 の台帳整理で、進行中セクション
  「自明セクターの偶部分グラフから配位を復元する」の行がセクション表から落ちていたため、
  表の先頭へ状態つきで復旧した。

- 2026-08-12（tick 165）: 前 tick の「自明セクターの偶部分グラフから配位を復元できる」を
  本文と SageMath で突き合わせた。結論と有限検算は一致していたが、格子面の二値和、二周期の
  二値和、横向き辺の両端での道和の差に根拠が無く、最後の双対像の等式が
  $\mathcal B_L(\sigma_A)=B$ の適用と双対辺写像の逆写像律を一段にまとめていた。
  各式へ根拠を補い、最後を一行一等号の二段へ分けた。

（これより古い 158 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
