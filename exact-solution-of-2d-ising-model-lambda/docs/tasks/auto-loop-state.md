# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-14 の tick 234 は、前 tick の「正の根の特定」の Lean 三本（具体版・必要十分版・
  導出）を本文の八ブロックと突き合わせ、二根の所属・表示・正錐判定・一意性・臨界点が
  1 対 1 に対応し、入口 import と sorry 非依存検査への登録も揃っているため修正不要と確認した。
  そのあとセクション「一般の $q$ での $\Phi_L(q)$ の性質」を二片に割り直し、最初の一片
  「双対変換は零と一の間の有理数を保つ」を記述と SageMath まで進めた。** 集合
  $\mathbb{Q}_{(0,1)}:=\{r\in\mathbb{Q}:0<r<1\}$ を定義し（`def_unit_interval_rationals`）、
  $q\in\mathbb{Q}_{(0,1)}$ ならば $\mathrm{KW}(q)$ が定義されて
  $\mathrm{KW}(q)\in\mathbb{Q}_{(0,1)}$ であること
  （`claim_kw_dual_preserves_unit_interval`。よって双対な点でも $\Phi_L$ が確定する）を、
  $\mathbb{Q}$ の四則と順序だけで示した（$\overline{\mathbb{Q}}$ は部分体としての逆元の一致
  の確認のみ）。SageMath（`sagemath/check/kw-dual-preserves-unit-interval/`）は分母 40 まで
  の全既約分数 489 点で全段を厳密検査した。**Lean は未着手**で、次 tick はこの二ブロックの
  Lean（具体版・必要十分版・導出）を完成させる。式変形統一では姉妹側
  「$W$ の成分はすべて正」の Step 3 にあった一行圧縮（行列積の定義と対角性の二段を
  まとめていた等式）を、$W$ の定義・行列の積の定義・対角性による二度の項の消去を
  行末根拠にした四段の一続きの鎖へ開いた。次の本文は残りの一片
  「双対な点どうしの $\Phi_L$ の値の関係」、次の式変形統一は `011_max_eigenvalue.ts` を
  先頭から機械走査した次の根拠なしの計算から続ける。

- **2026-08-14 の tick 233 は、前 tick の「正の根の特定」の八ブロックを本文と SageMath で
  突き合わせ、二根の所属と表示、正錐三条件、一意性が全段で一致するため修正不要と確認した。
  そのあと Lean 具体版・必要十分版・導出を完成させ、四層を揃えた。** 具体版は本文と同じ
  表示の構成・表示の一意性・有理係数の正錐判定・二根の場合分けを持つ。必要十分版は
  二係数表示、所属述語、二根の排他だけを要求し、体・順序・二次体の構造を仮定しない。
  臨界点も `criticalPoint` として形式化し、入口 import・本文対応・sorry 非依存検査へ登録した。
  式変形統一では姉妹側「$W$ は実対称正定値」の証明にある $S_2$ の表示、合同変換、転置、
  核の計算を、一行一等号と行末根拠を持つ鎖へ開いた。次の本文は
  「一般の $q$ での $\Phi_L(q)$ の性質（双対な点どうしの関係）」、次の式変形統一は
  `011_max_eigenvalue.ts` の次の根拠なしの計算から続ける。

- **2026-08-14 の tick 232 は、前 tick の「正錐と乗法の両立」の Lean 三本を本文と突き合わせ、
  具体版が本文と同じ九通りの場合分けを六補題と可換則の転送で閉じ、必要十分版が型・二項演算・
  所属述語・三条件・六補題・転送だけを仮定し、導出・入口 import・sorry 非依存検査への登録も
  揃っているため修正不要と確認した。そのあと「正の根の特定 $x_c=\sqrt2-1$」を記述と SageMath
  まで進めた。** 根 $-1+s$ と $-1-s$ の所属（$Q_s$）と表示（$(-1,1)$ と $(-1,-1)$）を立て、
  $-1+s$ が正錐の第三条件（$-1<0$、$0<1$、$1<2$）を満たすこと、$-1-s$ が三条件をすべて
  破ること、自己双対方程式の根で正錐に属するものが $-1+s$ に限ることを示し、臨界点
  $x_c:=-1+s$ を定義した（実数の順序は使わない。$\sqrt2-1$ は流儀の照合としてのみ言及）。
  SageMath は $s\cdot s=2$ の 2 つの $s$ の両方で全段を厳密検査した。**Lean は未着手**で、
  次 tick はこの八ブロックの Lean（具体版・必要十分版・導出）を完成させる。
  式変形統一では姉妹側「$Z=\mathrm{tr}(W^n)$」の証明にあった二つの根拠なし連鎖
  （$W^n$ の括り直しとトレースの巡回性の三等号）を、結合法則・冪の定義・$V_1=BB$・
  巡回性・$W=BV_2B$ を行末根拠にした七段の一続きの鎖へ開いた。次の本文は
  「正の根の特定」の Lean。次の式変形統一は `011_max_eigenvalue.ts` を先頭から
  機械走査した次の根拠なしの計算（散文内の等号を含む）から続ける。

- **2026-08-14 の tick 231 は、前 tick の「正錐と乗法の両立」の本文と SageMath を
  突き合わせ、積の表示、可換則による転送、九通りの符号場合の網羅が一致するため修正不要と
  確認した。そのあと同じ主張の Lean 具体版・必要十分版・導出を完成させ、四層を揃えた。**
  具体版は本文と同じ九通りの場合分けを六つの積の補題と転送で閉じ、転送は表示の二成分を
  有理数の乗法・加法の可換則で逆順の積へ一致させる。必要十分版は型・二項演算・所属述語・
  三条件・六補題・転送だけを要求し、順序も代数構造も仮定しない。導出は具体的な正錐と六補題を
  必要十分版へ渡した。入口 import と sorry 非依存検査へ三定理を登録した。
  式変形統一では姉妹側「レイリー上限のセクター分解」の末尾に散文で埋め込まれていた
  $\mathcal R_\pm\subseteq\mathcal R$ から $c_\pm\le c(M)$、さらに
  $\max(c_+,c_-)\le c(M)$ を得る上限比較を、定義・上限の単調性・直前の上界を行末根拠にした
  二つの鎖へ開いた。次の本文は「正の根の特定 $x_c=\sqrt2-1$」。次の式変形統一は同ファイルを
  先頭から機械走査した次の根拠なしの計算（散文内の等号を含む）から続ける。

- **2026-08-14 の tick 230 は、前 tick の「二つの混合符号条件の積」の Lean 三本を本文と
  突き合わせ、具体版が本文と同じ係数符号の確定・$D>0$ の中間比較・移項・交差項の付加・
  平方展開の鎖を有理係数二次体上で自前で持ち、必要十分版が線形順序可換環上で同じ鎖、導出が
  必要十分版の特殊化で、入口 import と sorry 非依存検査への三定理の登録も揃っているため
  修正不要と確認した。そのあと「正錐と乗法の両立」
  （`claim_quadratic_positive_cone_mul_closed`）を記述と SageMath まで進めた。**
  加法版と同じ組み立てで、$\mathrm{rep}_s(\xi\cdot\eta)=\mathrm{rep}_s(\eta\cdot\xi)$
  （$\mathbb{Q}$ の乗法・加法の可換則）による転送を準備し、九通りの符号場合を既存の
  六つの積の補題と転送で尽くした。SageMath は正錐の表示の全順序対 69696 組で、積の表示が
  三条件の少なくとも一つを満たすこと・転送の可換則・九通りの網羅を厳密検査した。
  **Lean は未着手**で、次 tick はこの主張の Lean（具体版・必要十分版・導出）を完成させる。
  式変形統一では姉妹側「レイリー上限のセクター分解」の末尾にあったレイリー商の上界の
  三段圧縮（$x^\top Wx\le c_+\|x_+\|^2+c_-\|x_-\|^2\le\max(c_+,c_-)(\dots)=\max(c_+,c_-)$）を、
  交叉項の消去・各セクターの上限の適用・積の単調性・分配則・ノルムの分解・$\|x\|=1$ を
  行末根拠にした七段の鎖へ開いた。次の式変形統一は同ファイルの次の根拠なしの計算
  （散文内の等号を含めて機械走査する）から続ける。

（これより古い 192 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 2 セクション
- 形式検証の土台: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 有限系の自由エントロピー | 双対な点どうしの $\Phi_L$ の値の関係 | todo | 旧「一般の $q$ での $\Phi_L(q)$ の性質」を二片に割り直した残り（2026-08-14 tick 234。最初の一片「双対変換は零と一の間の有理数を保つ」は着手済み）。四境界条件の混合の双対恒等式を $q$ で評価し、$\Phi_L(q)$ と $\Phi_L(\mathrm{KW}(q))$ を結ぶ。自己双対点のあとに置くのは、双対変換を定義してからでないと書けないため |
| 零点の詰め寄り | 相転移を $\mathbb{Q}$ 上の量化言明として書く | todo | |
| 熱力学極限 | 自由エネルギー密度・零点密度（**ここが $\mathbb{R}$ 脱出**） | todo | `realEscape` を具体的に書く |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-14（tick 234）: セクション「一般の $q$ での $\Phi_L(q)$ の性質」を、
  「双対変換は零と一の間の有理数を保つ」と「双対な点どうしの $\Phi_L$ の値の関係」の二片へ
  割り直した（前者は $\mathbb{Q}$ の順序の一論法、後者は双対恒等式の評価の一論法で、
  論法が別のため。1 セクション = 1 論法の規則による）。最初の一片を記述と SageMath まで
  進めた（Lean 未着手）。$\mathbb{Q}_{(0,1)}$ を定義し、$\mathrm{KW}(q)\in\mathbb{Q}_{(0,1)}$
  を示した。SageMath は分母 40 までの全既約分数 489 点で全段を厳密検査した。

- 2026-08-14（tick 233）: 「正の根の特定 $x_c=\sqrt2-1$」の Lean 具体版・必要十分版・導出を
  完成させ、四層すべてを満たした。具体版は本文の表示構成・正錐判定・二根の場合分けを持ち、
  必要十分版は二係数表示・所属述語・二根の排他だけまで薄めた。

- 2026-08-14（tick 232）: 「正の根の特定 $x_c=\sqrt2-1$」を記述と SageMath まで進めた
  （Lean 未着手）。根 $-1+s$、$-1-s$ の所属と表示を立て、$-1+s\in P_s$（第三条件）、
  $-1-s\notin P_s$（三条件すべて破れる）、正の根の一意性を示し、臨界点 $x_c:=-1+s$ を
  定義した。SageMath は 2 つの $s$ の両方で全段を厳密検査した。

- 2026-08-14（tick 231）: 「正錐と乗法の両立」の Lean 具体版・必要十分版・導出を完成させ、
  四層すべてを満たした。具体版は本文と同じ九通りの場合分けを六補題と可換則の転送で閉じ、
  必要十分版は型・二項演算・三条件・六補題・転送だけまで薄め、入口 import と sorry 非依存検査へ
  三定理を登録した。

- 2026-08-14（tick 230）: 「正錐と乗法の両立」を記述と SageMath まで進めた（Lean 未着手）。
  積の表示の可換則による転送を準備し、九通りの符号場合を既存の六つの積の補題と転送で尽くした。
  SageMath は正錐の表示の全順序対 69696 組で全段と九通りの網羅を厳密検査した。

（これより古い 201 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-14（tick 234）: 前 tick の「正の根の特定」の Lean 三本を本文と突き合わせた。
  具体版は本文の八ブロック（二根の所属・表示、正錐判定、非所属、一意性、臨界点）と
  1 対 1 に対応し、必要十分版は表示・所属述語・二根の排他だけを仮定し、導出・入口 import・
  sorry 非依存検査への登録も揃っている。修正は無い。

- 2026-08-14（tick 233）: 前 tick の「正の根の特定」の本文と SageMath を突き合わせた。
  二根の所属・表示、$-1+s$ の第三条件、$-1-s$ が三条件を破ること、二根からの一意性が
  一段ずつ一致し、住処も $\mathbb{Q}/\overline{\mathbb{Q}}$ で閉じている。修正は無い。

- 2026-08-14（tick 232）: 前 tick の「正錐と乗法の両立」の Lean 三本を本文と突き合わせた。
  具体版は本文と同じ九通りの場合分けを六補題と可換則の転送で閉じ、必要十分版は型・二項演算・
  所属述語・三条件・六補題・転送だけを仮定し、導出・入口 import・sorry 非依存検査への登録も
  揃っている。修正は無い。

- 2026-08-14（tick 231）: 前 tick の「正錐と乗法の両立」の本文と SageMath を突き合わせた。
  積の表示、逆順の積への可換則による転送、九通りの符号場合と六補題の対応、69696 組の検査が
  一致しており、主張・証明・検算とも修正は無い。

- 2026-08-14（tick 230）: 前 tick の「二つの混合符号条件の積」の Lean 三本を本文と
  突き合わせた。具体版は本文と同じ係数符号の確定・$D>0$ の中間比較・移項・交差項の付加・
  平方展開の鎖を有理係数二次体上で自前で持ち、必要十分版は線形順序可換環上で同じ鎖、導出は
  必要十分版の特殊化で、入口 import と sorry 非依存検査への三定理の登録も揃っている。修正は無い。

（これより古い 222 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
