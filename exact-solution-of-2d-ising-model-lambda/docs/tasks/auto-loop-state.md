# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地
- **2026-09-03 の 152 回目の tick で、奇数辺長の全辺鍵の対角固定置換には位相の数え上げの障害が無いことを確定した。** 不変なマッチングは固定置換を固定置換同士で対にするしかないので、固定置換の位相ベクトルの多重集合が符号反転で不変であることが必要条件になる。全辺鍵のファイバーを氷型配向と各頂点の入出辺の全単射の組として構成し（一辺二の $288$ 個の直接列挙で構成法を検証、一辺三のファイバーは $75{,}776$ 個）、対角固定置換は一辺三・五・七で $80,\ 1{,}088,\ 16{,}640$ 個。三つの辺長すべてで多重集合は符号反転不変で四つのねじれ位相和も零だった（`translation-diagonal-full-key-fixed-permutation-phase` の等号 assert で固定）。前進前レビューでは前 tick の辺長偶奇の検算・overview・ノート・台帳を照合し、修正は無かった。「何も言っていない主張」は無かった。次は固定置換の間の位相反転候補辺を構成し、固定置換だけの部分グラフに完全マッチングがあるかを判定する。
- **2026-09-03 の 151 回目の tick で、対角平行移動で固定され選択文字が非自明な鍵は、辺長二から八では奇数辺長の全辺鍵一つだけに現れることを確定した。** 対角辺軌道の和である偶部分グラフ $E$ ごとに、$E$ 内の閉路上で選択文字が非自明かを $\mathbb F_2$ の頂点ポテンシャルの不存在として判定した。該当鍵数は $L=2,4,6,8$ で零、$L=3,5,7$ で各一つであり、奇数辺長では $(D,E)=(\varnothing,E_L)$ に限られた（`translation-diagonal-fixed-nontrivial-character` の等号 assert で固定）。従って一辺二の対角部分群の自由作用は一辺三で既に破れる。前進前レビューでは前 tick の対角共変族の記述・検算・台帳を照合し、修正は無かった。「何も言っていない主張」は無かった。並列の式変形統一では姉妹側の「負数から平方根への変換」から反射律だけの空疎な等式を削除し、末尾の参照一覧を実際に使う式変形行へ移した。次は奇数辺長の全辺鍵で対角部分群不変な完全マッチングが存在する条件を調べる。
- **2026-09-03 の 150 回目の tick で、一辺二の一様被覆検査対象の全 $64$ 鍵に、対角平行移動部分群と可換な符号反転完全マッチング族を構成し、選択和の文字評価へ合成した。** 対角部分群 $\{(0,0),(1,1)\}$ の作用は $64$ 鍵上で自由であり、対角軌道 $32$ 個の各代表に全候補辺の完全マッチングを構成して $(1,1)$ で運ぶと、$64$ 鍵全てで候補辺だけの完全マッチング（対の総数 $2{,}624$）になり、族は対角部分群と可換である（`translation-diagonal-covariant-matching` の等号 assert で固定）。合成として、各鍵の四つのねじれ位相和が対ごとの相殺で全て零になるので、巻き付き文字が非自明な全ファイバーの選択和への寄与は四スピン構造すべてで消える。軸方向固定の $8$ 鍵も対角部分群では自由軌道に入り覆われる。候補関係は非対称なので正負両側から候補辺を集める必要があった。前進前レビューでは前 tick の孤立軌道・対角絞り込みの記述と検算の assert の一致を確認し、修正は無かった。「何も言っていない主張」は無かった。締切のため姉妹側の式変形統一は見送った。次は一般の辺長で対角部分群に対応する固定鍵が零になるかを小さい辺長で判定する。
- **2026-09-03 の 149 回目の tick で、一辺二で位相反転候補辺だけを使える非自明な平行移動部分群は対角部分群に絞られることを確定した。** 固定部分群が非自明で一様被覆検査の対象になる $8$ 鍵の商二部グラフでは、負位相側の孤立頂点軌道が点別固定軌道とちょうど一致し、大きさ $32$ の四鍵では正側 $4$・負側 $8$ 軌道、大きさ $288$ の四鍵では負側 $24$ 軌道が孤立する。巻き付き文字が非自明な全 $64$ 鍵では二つの軸平行移動が各 $4$ 鍵を固定する一方、対角平行移動 $(1,1)$ の固定鍵は零なので、対角部分群の作用だけが自由である（`translation-fixed-fiber-invariant-matching` の等号 assert で固定）。前進前レビューでは前 tick の結論の射程を検査対象の $8$ 鍵へ限定した。「何も言っていない主張」は無かった。時計がまとめ開始時刻に達したため姉妹側の式変形統一は見送った。次は対角共変族を実際に構成し、選択和の文字評価へ合成する。
- **2026-09-03 の 148 回目の tick で、固定部分群が非自明で一様被覆検査の対象になる鍵では、位相反転候補辺に固定部分群不変な完全マッチングが存在しないことを確定した。** 一辺二で固定部分群が非自明な $89$ 鍵のうち一様被覆検査の対象は軸方向固定の $8$ 鍵（位数 $2$、ファイバーの大きさ $32$ が $4$ 個・$288$ が $4$ 個）だけで、完全マッチング自体はあるのに不変なものが一つも無い（`translation-fixed-fiber-invariant-matching`。点別固定部分群が一致する候補辺だけが落ちる頂点軌道の商二部グラフの完全マッチングとの一対一対応で判定し、全数値を等号 assert で固定）。対角方向の固定鍵 $37$ 個は全て対象外だった。従って一辺二の時点で平行移動群全体と可換な符号反転マッチング族は候補辺からは作れず、共変性は固定部分群が自明な軌道に限るか、この $8$ 鍵で候補辺以外の構造を使う必要がある。前進前レビューでは、前 tick の結論文が検査対象外の $81$ 鍵まで含むように読めたため、射程を一様被覆検査の対象となる $8$ 鍵へ限定した。「何も言っていない主張」は無かった。締切のため姉妹側の式変形統一は見送った。次はこの $8$ 鍵の商グラフでどの頂点軌道が孤立するかを調べ、共変性を課す部分群を絞る。
- Onsager 閉形式への接続（トーラスの向き付き辺・回転位相・四つのスピン構造の定義）: 1 セクション
- 全章（何も言っていない主張の一掃）: 1 セクション
- 零点の詰め寄り・固有値の代数性（本文の lean: から引かれていない Lean の配線）: 1 セクション
- 検算（終結式による $d_1(3)$ の定義多項式の直接構成）: 1 セクション
- 検算（$L=4$ の区間観察）: 1 セクション
- 検算（$L=5$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=6$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=7$ の区間観察。跡定理経由の構成）: 1 セクション
- 検算（$L=8$ の区間観察。跡定理経由の構成）: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| Onsager 閉形式への接続 | 反転対を含む置換の項 | 一辺二の $\mathcal K=\mathcal U$、一般の $L$ の回転差二部分の相等、選択和の文字評価、置換ごとに偶部分グラフを選ぶ符号反転完全マッチングまで記述・SageMath 完了（Lean 未着手）。未被覆16ファイバーでは共変な全候補集合と自由な平行移動作用から、全 $2{,}816$ 置換を $1{,}408$ 対へ分ける平行移動共変な完全マッチングを構成した。運搬の条件は代表マッチングの固定部分群不変性である。固定ファイバーには平行移動の位数 $m$ が $|D|,|E|$ を割る必要があり、辺長二から八までの可能位数を検算した | 一様被覆対象の全 $64$ 鍵に対角部分群と可換な符号反転完全マッチング族を構成し、各鍵の四つのねじれ位相和が対ごとの相殺で零になることまで固定した（`translation-diagonal-covariant-matching`）。一般の辺長では、対角固定かつ非自明文字の鍵は $L=2,4,6,8$ で零、$L=3,5,7$ で全辺鍵 $(\varnothing,E_L)$ 一つだけだった（`translation-diagonal-fixed-nontrivial-character`）。全辺鍵の対角固定置換（一辺三・五・七で $80,\ 1{,}088,\ 16{,}640$ 個）は位相ベクトル多重集合が符号反転不変で四位相和も零であり、位相の数え上げに障害は無い（`translation-diagonal-full-key-fixed-permutation-phase`）。次は固定置換の間の位相反転候補辺を構成し、固定置換だけの部分グラフに完全マッチングがあるかを判定する。その後、$\mathcal K=\mathcal U$ と平方恒等式へ合成する。離散 Whitney 系の Lean 二版は未着手で、同じ列で一件ずつ配線する。 |
| Onsager 閉形式への接続 | 四つの Kac--Ward 行列式による有限トーラス公式 | 零巻き付きの平面単純閉多角形の回転数まで記述・SageMath 完了（離散 Whitney 系の Lean は未着手）。内側セル集合とその補集合の辺連結性、セル複体の Euler の等式 $V-E+F=1$、境界頂点での対角接触の排除と、方向番号の門の横断数・循環総回転数の等式まで記述・SageMath 完了した | 離散 Whitney を一論法ずつ進める: 整数列の水準横断数（済。Lean 未着手）→ 右半直線交差による内側セルの定義と有限性（済。Lean 未着手）→ 隣接セルの交差奇偶と共有辺の通過奇偶（済。Lean 未着手）→ 内側セルの行区間が奇数回通過の縦辺で終わること（済。Lean 未着手）→ 頂点に接する四辺の通過総和の偶数性（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧（済。Lean 未着手）→ 歩道沿いの内側帯の辺連結性（済。Lean 未着手）→ 内側セルの辺連結性の合成（済。Lean 未着手）→ 歩道沿いの外側帯の辺連結性（済。Lean 未着手）→ 外側セルの行方向の到達と補集合の辺連結性の合成（済。Lean 未着手）→ セル集合の複体量の定義と一セル追加の増分等式（済。Lean 未着手）→ 内側セル複体の Euler 数が 1 に等しいこと（済。Lean 未着手）→ 境界頂点での対角接触の排除（済。Lean 未着手）→ 方向番号の門の横断数と回転数の等式（済。Lean 未着手）→ 周期延長した平面持ち上げの単射性（済。Lean 未着手）→ 巻き付きベクトルの横断座標による有限幅の整数帯（済。Lean 未着手）→ 巻き付きベクトルに平行な整数座標と一周期ごとの増加（済。Lean 未着手）→ 巻き付きベクトルの横断階段（済。Lean 未着手）→ 横断階段を反復して整数帯の外へ運ぶ単純路（済。Lean 未着手）→ 横断平行移動した周期持ち上げ同士の分離（済。Lean 未着手）→ 整数帯の上端からの反復横断階段と周期持ち上げの分離（済。Lean 未着手）→ 反復横断階段とその非零周期並進の分離（済。Lean 未着手）→ 反復横断階段の平行座標の幅（済。Lean 未着手）→ 平行座標が離れた二つの反復横断階段の分離（済。Lean 未着手）→ 対応点を直接結ぶ閉包候補の反例（済）→ 反対側の持ち上げも終点以外で避ける接続路（済。Lean 未着手）→ 帯外で接続路と二つの周期持ち上げを合成する単純閉路（済。Lean 未着手）→ 閉包の周期数の一般化（済。Lean 未着手）→ 二側閉包の周期差では回転数が相殺すること（反証済）→ 巻き付きベクトルの平行階段（済。Lean 未着手）→ 平行階段の横断座標の幅と帯外に留まる条件（済。Lean 未着手）→ 一側閉包の定義と閉性（済。Lean 未着手）→ 一側閉包の頂点単純性（済。Lean 未着手）→ 平面単純閉路のトーラス射影（済。Lean 未着手）→ 射影の平面持ち上げの一致と巻き付き零（済。Lean 未着手）→ 頂点まわりの同奇偶セルの弧の仮定の緩和（済。Lean 未着手。$n_{\mathrm{ct}}=0$ を持ち上げ一周期点の相異性へ差し替え、呼び出し側 3 箇所は $n_{\mathrm{ct}}=0$ から相異性を導いて適用する形に直した）→ 内側帯と内側セル連結性の仮定の緩和（済。Lean 未着手）→ 外側帯と補集合連結性の仮定の緩和（済。Lean 未着手）→ 境界頂点の対角接触排除の仮定の緩和（済。Lean 未着手）→ 平面閉多角形の回転数定理の仮定の緩和（済。Lean 未着手。持ち上げ点相異の零巻き付き辺列に正負 4 が立つ。Euler 等式の「とくに」の尾部も同じ仮定へ揃えた）→ 一側閉包の循環総回転数は正負 4（済。Lean 未着手。緩和した定理を射影へ適用）→ 三周期比較の整数補題（済。Lean 未着手。三つの等差項が正負 4 に収まるなら公差は零）→ 周期数差の回転数等式（済。Lean 未着手。一側閉包の循環総回転数は周期数 $c$ に対して公差 $t_{\circ}(\gamma)$ の等差数列）→ 周期単純路の回転数零への合成（済。Lean 未着手。基点 $k_0$・$t=1$ の一側閉包の $c=1,2,3$ の射影回転数と周期数差の等差数列を三周期比較の整数補題へ渡し $t_{\circ}(\gamma)=0$）→ 候補への合成（未着手の Lean 二版もこの列で一件ずつ配線する）。その後、反転対を含む置換の項、軌道と偶部分グラフ対の数え上げ、平方恒等式への合成へ進む。 |
| Onsager 閉形式への接続 | ねじれた有限 Fourier 分解と正方格子の分散因子 | 未着手 | 円分体上の有限代数計算として閉じ、SageMath と二本の Lean 証明を付ける。 |
| Onsager 閉形式への接続 | 有限体積の積公式と四ねじれの共通極限 | 未着手 | 平方根分岐・前因子を固定し、境界ねじれの密度差が消えることを証明する。 |
| Onsager 閉形式への接続 | Riemann 和から Onsager 閉形式と臨界点へ接続する | 未着手 | ここで初めて実対数・極限・積分へ脱出し、既存の自由エントロピー密度と同定する。 |
| 全章 | 行き止まりの主張 18 件の切り分け | 未着手 | 引用漏れか不要かを 1 件ずつ読んで判断する。判定は `ref()` と `\blkref{}` の両方を数えること（数え違いの実績あり）。まず転送行列章の 4 件（トレース定理の証明が引いていない可能性）。 |
| 全章 | 道具の節を割って道具の章へ移す | 未着手 | 道具は 123 件あるが章に入れたのは 60 件。二次体と正錐・実閉部分体・代数的数係数の多項式・有理係数の対数順序群は、節の中に本論が混ざっていて節ごと動かせない。 |
| 臨界指数を零点列で書く | 先頭距離の列の可算な命題 | 保留 | $L=2,\dots,8$ の七点では定義多項式の次数も実対数へ脱出した読みも一般則・収束先を一意に定めない。命題候補が立つまで本文へ項目を追加しない。 |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録
- 2026-09-03（152 回目）: 奇数辺長の全辺鍵の対角固定置換を氷型配向と頂点対応の組から全構成し（一辺二の直接列挙 $288$ 個で構成法を検証）、一辺三・五・七の固定置換 $80,\ 1{,}088,\ 16{,}640$ 個の位相ベクトル多重集合が符号反転不変で四位相和が零であることを等号 assert で固定した（`translation-diagonal-full-key-fixed-permutation-phase`、約 1 分 24 秒）。位相の数え上げの水準では対角部分群の障害は現れない。
- 2026-09-03（152 回目・前進前レビュー）: 前 tick の辺長偶奇の検算（対角固定鍵数・非自明文字鍵の一意性の assert）と overview・ノート・台帳・MEMORY の記述を照合した。修正は無かった。「何も言っていない主張」は無かった。
- 2026-09-03（151 回目）: 対角平行移動の辺軌道の和である全 $E$ を一辺二から八まで列挙し、$E$ 内の閉路上の選択文字を $\mathbb F_2$ の頂点ポテンシャルで判定した。非自明文字を持つ対角固定鍵は偶数辺長で零、奇数辺長で全辺鍵 $(\varnothing,E_L)$ 一つだけだった（`translation-diagonal-fixed-nontrivial-character`）。並列の式変形統一は姉妹側の「負数から平方根への変換」で、何も言っていない反射律の等式を削除し、末尾の参照一覧を使用行末へ移した。
- 2026-09-03（151 回目・前進前レビュー）: 前 tick の対角共変族について、全置換の被覆、候補 witness、四位相反転、対角共変性、文字和零の assert とノート・overview・台帳の記述を照合した。修正は無かった。「何も言っていない主張」は無かった。
- 2026-09-03（150 回目）: 一様被覆検査対象の全 $64$ 鍵に対角平行移動部分群と可換な符号反転完全マッチング族を構成し、各鍵の四つのねじれ位相和が対ごとの相殺で零になることまで等号 assert で固定した（`translation-diagonal-covariant-matching`、約 7 分、全数値の assert 固定後に再実行）。候補関係は非対称なので正負両側の置換から候補辺を集める必要があった。締切のため姉妹側の式変形統一は見送った。
- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止・再開・頻度変更は、**自分で `launchctl` を叩かず** tmux セッション `local-pc-management` の
ウィンドウ `tick窓口` へ依頼する（2026-08-16 に経路が固定された。`launchd-tick-loop` skill）。
