# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-14 の tick 244 は、前 tick の「実閉部分体と虚数単位」の Lean 三本（具体版・
  必要十分版・導出）を本文と突き合わせ、四条件のデータ保持と $\omega^4=1$ の三段が一段ずつ
  対応し、入口 import と sorry 非依存検査への登録も揃っているため修正不要と確認した。
  そのあとセクション「零点と有理点の距離の二乗と実代数的数の順序」を「実代数的数の順序」
  「零点と有理点の距離の二乗」の二片へ割り直し（順序は平方の三分法、距離の二乗は一意表示と、
  使う条件と論法が別のため）、最初の一片を記述と SageMath まで進めた。** 三ブロック:
  有理数は実代数的数である（`claim_rationals_are_real_algebraic`。帰納法と部分体の閉性）、
  零元でない実代数的数の平方は $-1$ にならない（`claim_neg_one_not_square`。三分法の排他性
  からの背理法）、実代数的数の狭義順序（`def_real_algebraic_strict_order`。$a<_R b:\Leftrightarrow
  b-a$ が非零元の平方）、広義順序（`def_real_algebraic_nonstrict_order`）、三分法
  （`claim_real_algebraic_order_trichotomy`。平方の三分法から従う）。順序と加法・乗法の両立、
  $\mathbb{Q}$ の通常の順序との一致はまだ主張しない（必要になる箇所で示す）と本文に明記した。
  SageMath（`sagemath/check/real-algebraic-order/`）はモデル $R=\texttt{AA}$ で三ブロックを
  有限標本で厳密検査した。**Lean は未着手**で、次 tick はこの三ブロックの Lean を進める。
  次の本文は「零点と有理点の距離の二乗」。式変形統一では姉妹側「トレース冪の挟み撃ち」
  Step 3 冒頭の比の単調性（三段）と望遠鏡積からの下からの評価（四段）を、それぞれ一続きの
  根拠付きの鎖へ開いた。次の式変形統一は同じ証明の半正定値行列のトレース評価
  $x^\top Ax\le\|x\|^2\,\mathrm{tr}(A)$ の行末根拠化から続ける。

- **2026-08-14 の tick 243 は、前 tick の「実閉部分体と虚数単位」をレビューし、SageMath の
  有限標本検査を普遍的な四条件の検証と読める表現、および $\omega^4=1$ の圧縮された三段を
  修正して、前進前に独立して main へ反映した。そのあと同セクションの Lean 具体版・
  必要十分版・導出を完成させ、四層を揃えた。** 具体版は $R\subset\overline{\mathbb{Q}}$、
  平方の三分法、$\omega^2=-1$、一意表示をそのまま保持するデータを定義し、本文と同じ三段で
  $\omega^4=1$ を示す。必要十分版は可換環と $\omega^2=-1$ だけへ薄め、導出は具体的な
  データを特殊化した。式変形統一では姉妹側「トレース冪の挟み撃ち」Step 2 の奇数・偶数の
  場合分けを、それぞれ四段の根拠付き不等式鎖へ開いた。次の式変形統一は同じ証明の Step 3
  冒頭にある比の単調性と積表示から続ける。次の本文は
  「零点と有理点の距離の二乗と実代数的数の順序」。

- **2026-08-14 の tick 242 は、前 tick の「正の有理点は Fisher 零点でない」の Lean 三本
  （具体版・必要十分版・導出）を本文と突き合わせ、評価の三段と背理法が一段ずつ対応し、
  入口 import と sorry 非依存検査への登録も揃っているため修正不要と確認した。そのあと
  セクション「相転移を $\mathbb{Q}$ 上の量化言明として書く（詰め寄りの述語の定式化）」を
  「実閉部分体と虚数単位の固定」「零点と有理点の距離の二乗と実代数的数の順序」
  「詰め寄りの述語の定式化」の三片へ割り直し（順に、構造の固定・道具の定義・量化言明で
  論法が別のため）、最初の一片を記述と SageMath まで進めた。** 定義
  `def_real_closed_subfield` は、$\overline{\mathbb{Q}}$ の部分体 $R$ と元 $\omega$ の組を
  4 条件（部分体・平方の三分法・$\omega^2=-1$・一意表示 $\xi=a+b\omega$）で固定する
  （存在は Artin--Schreier の定理で既知）。SageMath
  （`sagemath/check/real-closed-subfield/`）はモデル $R=\texttt{AA}$、
  $\omega=\texttt{QQbar(I)}$ で 4 条件を厳密検査した（高次の Fisher 零点での分解検査は
  exactify の資源上限で外し、overview に範囲の注記として明記）。**Lean は未着手**で、
  次 tick はこのブロックの Lean を進める。締切（45 分枠）のため式変形統一はこの tick では
  実施できなかった（次 tick は姉妹側「トレース冪の挟み撃ち」Step 2 の奇数・偶数の
  場合分けの圧縮から続ける）。次の本文は「零点と有理点の距離の二乗と実代数的数の順序」。

- **2026-08-14 の tick 241 は、前 tick の「正の有理点は Fisher 零点でない」の本文と
  SageMath を突き合わせ、代数的数での係数表示、有理数での代入値との一致、正値性、零点定義に
  よる背理法が同じ順で対応しているため修正不要と確認した。そのあと同じ主張の Lean 具体版・
  必要十分版・導出を完成させ、四層を揃えた。** 具体版は本文の三段と背理法を一段ずつ持ち、
  必要十分版は内側の値の非零性、埋め込み後の値との一致、零元保存、単射性、零点集合の所属と
  評価値の零性の対応だけへ薄めた。入口 import、本文対応、sorry 非依存検査にも三定理を登録した。
  式変形統一では姉妹側「トレース冪の挟み撃ち」の Step 2 にある $P=I$ 版の圧縮を、
  モーメントの定義、冪の指数法則、実対称性、Cauchy--Schwarz、ノルムの定義を行末根拠にした
  八段の一続きの鎖へ開いた。次の式変形統一は同じ証明の直後にある奇数・偶数の場合分けの
  圧縮から続ける。次の本文は「相転移を $\mathbb{Q}$ 上の量化言明として書く
  （詰め寄りの述語の定式化）」である。

- **2026-08-14 の tick 240 は、前 tick の「双対な点どうしの $\Phi_L$ の値の関係」の Lean 三本
  （具体版・必要十分版・導出）を本文と突き合わせ、双対分解の六段と自由エントロピーの七段が
  一段ずつ対応し、入口 import と sorry 非依存検査への登録も揃っているため修正不要と確認した。
  そのあと「零点の詰め寄り」章を開き、台帳のセクション「相転移を $\mathbb{Q}$ 上の量化言明として
  書く」から最初の一片「正の有理点は Fisher 零点でない」を切り出して記述と SageMath まで
  進めた。** 主張は $q\in\mathbb{Q}_{>0}$ ならば $q\notin\mathcal{F}_L$
  （`claim_positive_rational_not_fisher_zero`）。証明は係数表示による
  $\mathrm{Ev}^F_q(Z_L)=\sum_m\Omega_L(m)q^m$、部分体 $\mathbb{Q}$ での代入値との一致、
  正値性 `claim_value_at_rational_is_positive` の三段の鎖と、零元の部分体一致からの背理法で
  閉じる。SageMath（`sagemath/check/positive-rational-not-fisher-zero/`）は $L=1,2,3$ ×
  正の有理点 11 個で全段と、`QQbar` の全根に正の有理数が無いことを厳密検査した。
  **Lean は未着手**で、次 tick はこのブロックの Lean（具体版・必要十分版・導出）を完成させる。
  式変形統一では姉妹側「トレース冪の挟み撃ち」で各標準基底の評価を $d$ 個足す散文内の圧縮
  $\mathrm{tr}(W^n)\le d\,c^n=2^Mc^n$ を、トレースの標準基底表示・項ごとの不等式の有限和・
  同じ項の $d$ 個の和・略記 $d=2^M$ を行末根拠にした四段の一続きの鎖へ開いた。
  次の式変形統一は同じ証明の Step 2 の $P=I$ 版の散文内等式
  $(m_{a+b})^2\le m_{2a}m_{2b}$ から続ける。
  次の本文は残りの一片「相転移を $\mathbb{Q}$ 上の量化言明として書く（詰め寄りの述語の定式化）」。

（これより古い 201 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 5 セクション
- 形式検証の土台: 1 セクション
- 零点の詰め寄り: 2 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 零点の詰め寄り | 実代数的数の順序 | 記述と SageMath まで | 次 tick は Lean（具体版・必要十分版・導出） |
| 零点の詰め寄り | 零点と有理点の距離の二乗 | todo | 一意表示 $\xi=a+b\omega$ から $(a-q)^2+b^2\in R$ を定める |
| 零点の詰め寄り | 相転移を $\mathbb{Q}$ 上の量化言明として書く（詰め寄りの述語の定式化） | todo | |
| 熱力学極限 | 自由エネルギー密度・零点密度（**ここが $\mathbb{R}$ 脱出**） | todo | `realEscape` を具体的に書く |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-14（tick 244）: セクション「零点と有理点の距離の二乗と実代数的数の順序」を
  「実代数的数の順序」「零点と有理点の距離の二乗」の二片へ割り直した（順序は平方の三分法、
  距離の二乗は一意表示と、使う条件と論法が別のため）。最初の一片を記述と SageMath まで進めた
  （五ブロック: 有理数の所属・$-1$ は非零元の平方でない・狭義順序・広義順序・三分法。
  Lean 未着手）。

- 2026-08-14（tick 243）: 「実閉部分体と虚数単位の固定」の Lean 具体版・必要十分版・導出を
  完成させ、四層すべてを満たした。具体版は本文の四条件を Qbar 上のデータとして保持し、
  $\omega^4=1$ の三段を持つ。必要十分版は可換環と $\omega^2=-1$ だけを要求する。

- 2026-08-14（tick 242）: セクション「相転移を $\mathbb{Q}$ 上の量化言明として書く」を
  「実閉部分体と虚数単位の固定」「零点と有理点の距離の二乗と実代数的数の順序」
  「詰め寄りの述語の定式化」の三片へ割り直した（構造の固定・道具の定義・量化言明で
  論法が別のため）。最初の一片を記述と SageMath まで進めた（Lean 未着手）。
  距離と順序の道具が本文に無いことがこの割り直しの理由である。

- 2026-08-14（tick 241）: 「正の有理点は Fisher 零点でない」の Lean 具体版・必要十分版・導出を
  完成させ、四層すべてを満たした。具体版は本文の評価の三段と背理法を持ち、必要十分版は
  非零値を単射な零元保存写像で埋め込むために必要な仮定だけへ薄めた。

- 2026-08-14（tick 240）: 「零点の詰め寄り」章を開き、セクション「相転移を $\mathbb{Q}$ 上の
  量化言明として書く」を「正の有理点は Fisher 零点でない」（章の出発点。評価の一致と正値性
  からの背理法の一論法）と「詰め寄りの述語の定式化」の二片へ割り直した（前者は既存の道具
  だけで閉じる独立の一論法のため）。最初の一片を記述と SageMath まで進めた（Lean 未着手）。

（これより古い 211 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-14（tick 245）: 前 tick の「実代数的数の順序」を本文と SageMath で突き合わせた。
  一つの定義ブロックに狭義順序・広義順序・三分法が同居して「1 ブロック 1 主張」に反して
  いたため、二つの定義と一つの主張へ分割し、SageMath の対象ラベルも同じ粒度へ揃えた。

- 2026-08-14（tick 244）: 前 tick の「実閉部分体と虚数単位」の Lean 三本を本文と突き合わせた。
  具体版は本文の四条件（部分体・平方の三分法・$\omega^2=-1$・一意表示）をデータとして保持し、
  $\omega^4=1$ を本文と同じ三段（冪の定義・$\omega\cdot\omega=-1$・乗法）の calc で示す。
  必要十分版は可換環と $\omega^2=-1$ だけへ薄まり、導出・入口 import・sorry 非依存検査への
  登録も揃っている。修正は無い。

- 2026-08-14（tick 243）: 前 tick の「実閉部分体と虚数単位」の本文と SageMath を突き合わせた。
  SageMath は有限標本しか検査していないのに overview と終了メッセージが普遍量化された 4 条件を
  検査済みと読める表現だったため、有限標本検査であることと定理への依存を明記した。また本文の
  $\omega^4=1$ の三段を、一行一操作・行末根拠を持つ式変形へ開いた。

- 2026-08-14（tick 242）: 前 tick の「正の有理点は Fisher 零点でない」の Lean 三本を本文と
  突き合わせた。具体版は本文の評価の三段（係数表示の有限和・$\mathbb{Q}$ での代入値との
  一致・正値性）と背理法を同じ順に持ち、必要十分版は零元保存・単射・零点集合の所属の
  対応だけへ薄まり、導出・入口 import・sorry 非依存検査への登録も揃っている。修正は無い。

- 2026-08-14（tick 241）: 前 tick の「正の有理点は Fisher 零点でない」の本文と SageMath を
  突き合わせた。代数的数での評価を多重度の有限和へ開く段、有理数での代入値との一致、
  正値性から非零性を得る段、零点定義との矛盾が対応し、住処も $\mathbb{Q}$ と
  $\overline{\mathbb{Q}}$ だけで閉じている。修正は無い。

（これより古い 233 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
