# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-14 の tick 238 は、前 tick の「セクター多項式の値の双対関係」の Lean 三本
  （具体版・必要十分版・導出）を本文と突き合わせ、双対の準備等式・積の冪・冪の指数法則・
  括り出しの各段が 1 対 1 に対応し、入口 import と sorry 非依存検査への登録も揃っているため
  修正不要と確認した。そのあとセクション「双対な点どうしの $\Phi_L$ の値の関係」を記述と
  SageMath まで進めた。** 二ブロック: $q\in\mathbb{Q}_{(0,1)}$ について
  $2^{L^2}Z_L(q)=(1+q)^{2L^2}\sum_{(a,b)}G^{a,b}_L(\mathrm{KW}(q))$
  （`claim_partition_value_dual_factorization`。低温展開の自明セクター表示・混合双対恒等式・
  セクター値双対の四つの適用・括り出しの五段の鎖）と、$\Lambda$ の等式
  $L^2\ell_2+\Phi_L(q)=2L^2\log(1+q)+\log\bigl(\sum_{(a,b)}G^{a,b}_L(\mathrm{KW}(q))\bigr)$
  （`claim_free_entropy_dual_relation`。正値性の準備と対数の加法性・冪の法則の七段の鎖。
  四セクター和の正値性は値の双対分解からの背理法で示した）。SageMath
  （`sagemath/check/free-entropy-dual-relation/`）は $L=1,2,3$ × 有理点 6 個で両主張の全段を
  厳密検査した（$\Lambda$ は素因数分解の指数ベクトルの辞書で実装）。**Lean は未着手**で、
  次 tick はこの二ブロックの Lean（具体版・必要十分版・導出）を完成させる。
  式変形統一は締切のため見送った（次 tick は姉妹側「トレース冪の挟み撃ち」の
  偶数冪の上からの評価から続ける）。次の本文は「零点の詰め寄り」章の最初のセクション。

- **2026-08-14 の tick 237 は、前 tick の「セクター多項式の値の双対関係」の本文と SageMath を
  突き合わせ、双対変換の準備の等式、項ごとの三段、有限和からの括り出しが一致するため
  修正不要と確認した。そのあと同じ主張の Lean 具体版・必要十分版・導出を完成させ、四層を
  揃えた。** 具体版は整数係数多項式を有理点で評価し、本文と同じく
  $(1+q)\mathrm{KW}(q)=1-q$、積の冪、冪の指数法則を各項へ順に適用して共通因子を括り出す。
  必要十分版は可換半環、有限和、指数の上界、二つの重みだけを要求し、導出は具体的な有限和を
  その必要十分版へ特殊化した。式変形統一では姉妹側「トレース冪の挟み撃ち」の冒頭にあった
  $\mathrm{tr}(W^n)=\sum_k e_k^\top W^n e_k$ を、トレースの定義と標準基底による対角成分表示の
  二段の鎖へ開いた。次の式変形統一は同じ証明の偶数冪の上からの評価から続ける。
  次の本文は「双対な点どうしの $\Phi_L$ の値の関係」である。

- **2026-08-14 の tick 236 は、前 tick の「双対変換は零と一の間の有理数を保つ」の Lean 三本
  （具体版・必要十分版・導出）を本文と突き合わせ、三つの鎖と代数的数側の値との一致、
  入口 import と sorry 非依存検査への登録が揃っているため修正不要と確認した。そのあと
  台帳先頭のセクション「双対な点どうしの $\Phi_L$ の値の関係」を二片へ割り直し、最初の一片
  「セクター多項式の値の双対関係」を記述と SageMath まで進めた。** 主張は
  $q\in\mathbb{Q}_{(0,1)}$、$a,b\in\{0,1\}$ について
  $H^{a,b}_L(q)=(1+q)^{2L^2}\cdot G^{a,b}_L(\mathrm{KW}(q))$
  （`claim_sector_value_duality`）。準備の等式 $(1+q)\cdot\mathrm{KW}(q)=1-q$ を
  $\overline{\mathbb{Q}}$ の可換則・逆元で確かめて部分体で $\mathbb{Q}$ へ落とし、
  各項の書き換えと分配則による括り出しの六段の鎖で閉じた。SageMath
  （`sagemath/check/sector-value-duality/`）は $L=1,2,3$ × 有理点 6 個 × 4 セクターで
  主張の等式と項単位の中間 3 行を厳密検査した。**Lean は未着手**で、次 tick はこのブロックの
  Lean（具体版・必要十分版・導出）を完成させる。式変形統一では姉妹側
  「作用素ノルムのレイリー上限評価 $\|Wx\|\le c(M)\|x\|$」の証明にあった三つの根拠なし連鎖
  （ノルムの二乗の展開、Cauchy--Schwarz の適用、二つの上限評価の同時適用）を、
  ノルムの定義・置き換え・冪の指数法則・Cauchy--Schwarz・二段に分けた上限評価
  （非負因子を明示）・可換則を行末根拠にした二つの一続きの鎖へ開いた。
  次の式変形統一は同ファイルの次の根拠なしの計算（散文内の等号を含む）から続ける。
  次の本文は残りの一片「双対な点どうしの $\Phi_L$ の値の関係」（四つのセクターの等式を
  混合双対恒等式で束ね、$\Phi_L(q)$ と双対点の量を結ぶ）。

- **2026-08-14 の tick 235 は、前 tick の「双対変換は零と一の間の有理数を保つ」の本文と
  SageMath を突き合わせ、準備と三つの鎖が一致するため修正不要と確認した。そのあと同じ
  二ブロックの Lean 具体版・必要十分版・導出を完成させ、四層を揃えた。** 具体版は有理数の
  逆元を取り、有理数性・正値性・一未満を本文と同じ三つの鎖で示し、代数的数側の双対変換との
  一致も示した。必要十分版は台集合への所属、積での閉性、正値性、正の右因子を掛けた比較、
  上端を与える積の等式だけを要求する。式変形統一では姉妹側「半正定値双線型形式の
  Cauchy--Schwarz の不等式」の二次式展開と二つの場合分けを、一行一演算・行末根拠の鎖へ
  開いた。次の本文は「双対な点どうしの $\Phi_L$ の値の関係」、次の式変形統一は同ファイルの
  次の証明ブロックから続ける。

- **2026-08-14 の tick 234 は、前 tick の「正の根の特定」の Lean 三本（具体版・必要十分版・
  導出）を本文の八ブロックと突き合わせ、二根の所属・表示・正錐判定・一意性・臨界点が
  1 対 1 に対応し、入口 import と sorry 非依存検査への登録も揃っているため修正不要と確認した。
  そのあとセクション「一般の $q$ での $\Phi_L(q)$ の性質」を二片に割り直し、最初の一片
  「双対変換は零と一の間の有理数を保つ」を記述と SageMath まで進めた。** 集合
  $\mathbb{Q}_{(0,1)}:=\{r\in\mathbb{Q}:0<r<1\}$ を定義し（`def_unit_interval_rationals`）、
  $q\in\mathbb{Q}_{(0,1)}$ ならば $\mathrm{KW}(q)$ が定義されて
  $\mathrm{KW}(q)\in\mathbb{Q}_{(0,1)}$ であること
  （`claim_kw_dual_preserves_unit_interval`。よって双対な点でも $\Phi_L$ が確定する）を、
  $\mathbb{Q}$ の四則と順序だけで示した（$\overline{\mathbb{Q}}$ は部分体としての逆元の一致
  の確認のみ）。SageMath（`sagemath/check/kw-dual-preserves-unit-interval/`）は分母 40 まで
  の全既約分数 489 点で全段を厳密検査した。**Lean は未着手**で、次 tick はこの二ブロックの
  Lean（具体版・必要十分版・導出）を完成させる。式変形統一では姉妹側
  「$W$ の成分はすべて正」の Step 3 にあった一行圧縮（行列積の定義と対角性の二段を
  まとめていた等式）を、$W$ の定義・行列の積の定義・対角性による二度の項の消去を
  行末根拠にした四段の一続きの鎖へ開いた。次の本文は残りの一片
  「双対な点どうしの $\Phi_L$ の値の関係」、次の式変形統一は `011_max_eigenvalue.ts` を
  先頭から機械走査した次の根拠なしの計算から続ける。

（これより古い 196 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 4 セクション
- 形式検証の土台: 1 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 有限系の自由エントロピー | 双対な点どうしの $\Phi_L$ の値の関係 | 記述と SageMath まで（2026-08-14 tick 238） | Lean（具体版・必要十分版・導出）を次 tick で完成させる。対象は `claim_partition_value_dual_factorization` と `claim_free_entropy_dual_relation` の二ブロック |
| 零点の詰め寄り | 相転移を $\mathbb{Q}$ 上の量化言明として書く | todo | |
| 熱力学極限 | 自由エネルギー密度・零点密度（**ここが $\mathbb{R}$ 脱出**） | todo | `realEscape` を具体的に書く |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-14（tick 238）: セクション「双対な点どうしの $\Phi_L$ の値の関係」を記述と SageMath
  まで進めた（Lean 未着手）。分配多項式の値の双対分解
  $2^{L^2}Z_L(q)=(1+q)^{2L^2}\sum_{(a,b)}G^{a,b}_L(\mathrm{KW}(q))$ と、$\Lambda$ の等式
  $L^2\ell_2+\Phi_L(q)=2L^2\log(1+q)+\log\bigl(\sum_{(a,b)}G^{a,b}_L(\mathrm{KW}(q))\bigr)$
  の二ブロック。SageMath は $L=1,2,3$ × 有理点 6 個で全段を厳密検査した。
  本文末尾「この先に書くこと」から済んだこの項目を消した。

- 2026-08-14（tick 237）: 「セクター多項式の値の双対関係」の Lean 具体版・必要十分版・導出を
  完成させ、四層すべてを満たした。具体版は本文の各項の書き換えと共通因子の括り出しを持ち、
  必要十分版は可換半環・有限和・指数の上界・二つの重みだけまで薄めた。

- 2026-08-14（tick 236）: セクション「双対な点どうしの $\Phi_L$ の値の関係」を、
  「セクター多項式の値の双対関係」と「双対な点どうしの $\Phi_L$ の値の関係」の二片へ
  割り直した（前者は各項の書き換えと括り出しの一論法、後者は四つの等式を混合双対恒等式で
  束ねる一論法で、論法が別のため。1 セクション = 1 論法の規則による）。最初の一片を記述と
  SageMath まで進めた（Lean 未着手）。$q\in\mathbb{Q}_{(0,1)}$、$a,b\in\{0,1\}$ について
  $H^{a,b}_L(q)=(1+q)^{2L^2}\cdot G^{a,b}_L(\mathrm{KW}(q))$ を示した。SageMath は
  $L=1,2,3$ × 有理点 6 個 × 4 セクターで全段を厳密検査した。

- 2026-08-14（tick 235）: 「双対変換は零と一の間の有理数を保つ」の Lean 具体版・必要十分版・
  導出を完成させ、四層すべてを満たした。具体版は本文の三鎖と代数的数側の値との一致を持ち、
  必要十分版は台集合・積・正値性・比較に必要な仮定だけへ薄めた。

- 2026-08-14（tick 234）: セクション「一般の $q$ での $\Phi_L(q)$ の性質」を、
  「双対変換は零と一の間の有理数を保つ」と「双対な点どうしの $\Phi_L$ の値の関係」の二片へ
  割り直した（前者は $\mathbb{Q}$ の順序の一論法、後者は双対恒等式の評価の一論法で、
  論法が別のため。1 セクション = 1 論法の規則による）。最初の一片を記述と SageMath まで
  進めた（Lean 未着手）。$\mathbb{Q}_{(0,1)}$ を定義し、$\mathrm{KW}(q)\in\mathbb{Q}_{(0,1)}$
  を示した。SageMath は分母 40 までの全既約分数 489 点で全段を厳密検査した。

（これより古い 205 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-14（tick 238）: 前 tick の「セクター多項式の値の双対関係」の Lean 三本を本文と
  突き合わせた。具体版は本文と同じ双対の準備等式・積の冪・冪の指数法則・括り出しの鎖を持ち、
  必要十分版は可換半環・有限和・指数の上界・二つの重みだけを仮定し、導出・入口 import・
  sorry 非依存検査への登録も揃っている。修正は無い。

- 2026-08-14（tick 237）: 前 tick の「セクター多項式の値の双対関係」の本文と SageMath を
  突き合わせた。準備の等式、項単位の三段、有限和の主張がすべて厳密有理数計算と一致し、
  記号の住処も有限集合・$\mathbb{Z}[x]$・$\mathbb{Q}$ で閉じている。修正は無い。

- 2026-08-14（tick 236）: 前 tick の「双対変換は零と一の間の有理数を保つ」の Lean 三本を
  本文と突き合わせた。具体版は本文と同じ三つの鎖（有理数性・正値性・一未満）と代数的数側の
  値との一致を持ち、必要十分版は台集合・積での閉性・正値性・正の右因子の比較・上端の等式
  だけを仮定し、導出・入口 import・sorry 非依存検査への登録も揃っている。修正は無い。

- 2026-08-14（tick 235）: 前 tick の「双対変換は零と一の間の有理数を保つ」の本文と
  SageMath を突き合わせた。逆元の一致と正値性、有理数性・正値性・一未満の三鎖が一段ずつ
  対応しており、修正は無い。

- 2026-08-14（tick 234）: 前 tick の「正の根の特定」の Lean 三本を本文と突き合わせた。
  具体版は本文の八ブロック（二根の所属・表示、正錐判定、非所属、一意性、臨界点）と
  1 対 1 に対応し、必要十分版は表示・所属述語・二根の排他だけを仮定し、導出・入口 import・
  sorry 非依存検査への登録も揃っている。修正は無い。

（これより古い 226 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
