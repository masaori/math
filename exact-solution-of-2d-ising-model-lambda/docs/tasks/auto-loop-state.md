# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-15 の tick 269 は、「自由エネルギー密度の極限の存在」を、開境界長方形の分配多項式・
  長方形の接合不等式・周期境界との差の境界評価・極限の導出へ割り直し、先頭の開境界長方形の
  分配多項式を記述と SageMath まで進めた。レビューでは前 tick の上限一意性の四層に修正は無い。**
  頂点・辺・配位・破れボンド数・分配多項式を五ブロックで定義し、すべて有限集合・$\mathbb{N}$・
  $\mathbb{Z}[x]$ で閉じた。SageMath（`sagemath/check/open-rectangle-partition-polynomial`）は
  $a,b\in\{1,2,3\}$ の全長方形で 108 件を厳密検査した。Lean は未着手で、次 tick はこの五定義の
  Lean 具体版・必要十分版・導出を進める。式変形統一では姉妹側「$V^{(+)}$ と
  $T_{(V^{(+)})}$ の定義」の平方根の鎖へ、同類項の加法という行末根拠を補った。

- **2026-08-15 の tick 268 は、「上限の一意性（記法 sup の正当化）」を記述・SageMath・
  Lean 具体版・必要十分版まで一気に完成させ、一 tick で四層を揃えた。レビューでは前 tick の
  Lean 三本が本文と一対一に揃っており、修正は無い。** 一ブロック
  `claim_real_set_supremum_unique`（$u_1,u_2$ がともに $S$ の上限なら $u_1=u_2$。両向きの
  $\le_{\mathbb{R}}$ を最小性の適用で取り、略記の展開と三分律の場合の除外で閉じる。
  完備性は使わない。これで記法 $\sup S$ が正当化される）。SageMath
  （`sagemath/check/real-set-supremum-uniqueness`）は $\mathbb{Q}$ の厳密比較のみで 74 件。
  Lean は具体版 `realSetSupremum_unique`・必要十分版 `leastUpperBound_unique_necSuf`
  （半順序で足りることが判明。具体版が使った三分律の全体＝線形順序は不要）・導出。
  式変形統一では姉妹側「$H_1^{(+)},H_2$ を $\check Z,\check Y$ で表す」の二つの鎖に欠けていた
  スカラー $1/M$ と $M$ の相殺の行末根拠を補った。次の本文は「自由エネルギー密度の極限の存在」
  （残るは $\varepsilon$ を固定したときある $N$ 以降のすべての $L$ で近づくことの証明。道具は未定）、
  次の統一は同じ偶セクター章の後続から続ける。

- **2026-08-15 の tick 267 は、「上界・最小上界の定義と上限への任意近接」の Lean 具体版・
  必要十分版・導出を完成させ、四層を揃えた。レビューでは前 tick の本文と SageMath が一致しており、
  修正は無い。** 具体版は上界と最小上界を本文どおりの述語として定義し、
  $u-\varepsilon<u$、$u-\varepsilon$ が上界でないこと、反例の取得、値集合の展開を同じ順で辿る。
  必要十分版は自由エネルギー密度・実数・減法を外し、線形順序上の値域・上限・それより小さい元だけを
  残し、導出で正の格子サイズと実数へ特殊化した。式変形統一では姉妹側「$\check Z,\check Y$ から
  $Z_j,Y_j$ を復元する」の復元式の最終行へ、スカラー $1/M$ と $M$ の相殺という行末根拠を補った。
  次の本文は「上限の一意性（記法 $\sup$ の正当化）」、次の式変形統一は偶セクターの後続証明から続ける。

- **2026-08-15 の tick 266 は、「自由エネルギー密度の極限の存在」から一論法で閉じる片を
  「上界・最小上界の定義と上限への任意近接」として切り出し、記述と SageMath まで進めた。
  レビューでは、「最小上界」が未定義のまま完備性の宣言と上限存在の主張に使われていたこと、
  および $\le_{\mathbb{R}}$ の略記の「初出はここ」が実際の初出より後に置かれていたことを
  見つけ、前進セクションと一体で直した。** 三ブロック: `def_real_set_upper_bound`・
  `def_real_set_supremum`（上界と最小上界を述語として定義。存在も一意性もまだ主張しない）と
  `claim_free_energy_density_supremum_approximation`（$u$ を $\Psi_t$ の上限とすると、任意の
  $\varepsilon>0$ に対しある $L\ge1$ が存在して $u-\varepsilon<_{\mathbb{R}}\psi_L(t)$。
  順序の三分律を realEscape で追加宣言し、最小性から背理法一本で閉じる）。略記
  $\le_{\mathbb{R}}$ の宣言は「実数体への脱出の宣言」へ移した。SageMath
  （`sagemath/check/free-energy-density-supremum-approximation/`）は全て $\mathbb{Q}$ の
  厳密比較で 154 件（真の上限は有限標本で検査できないと明記。実対数の比較は
  $Z_a(t)^{b^2}\le Z_b(t)^{a^2}$ の $\mathbb{Q}$ 比較へ置き換えた）。Lean 未着手で、次 tick は
  この三ブロックの Lean（具体版・必要十分版・導出）を進める。式変形統一では姉妹側
  「$H$ と $\check Z,\check Y$ の交換子」の三つの鎖の最終行に欠けていた行末根拠を補い、
  単位行列の消去と同類項の統合の圧縮を開いた。

- **2026-08-15 の tick 265 は、「自由エネルギー密度の値集合の上限の存在」の Lean 具体版・
  必要十分版・導出を完成させ、四層を揃えた。レビューでは前 tick の本文と SageMath が一致しており、
  修正は無い。** 具体版は値集合の非空性を格子サイズ $1$ で示し、一様上界を与え、実数の完備性を
  一度だけ適用する本文の三段を辿る。必要十分版は格子・自由エネルギー密度・実数を外し、添字付き
  値集合の証人、一様上界、空でない上に有界な集合へ上限を与える性質だけを残し、導出で正の格子
  サイズと実数へ特殊化した。式変形統一では姉妹側「$V'$ のトレース」のフェルミオン数演算子の
  積和を指数関数の有限積へ分解する鎖の先頭行に、各二値成分の独立な選択という行末根拠を補った。
  次の本文は「自由エネルギー密度の極限の存在」。次の式変形統一は同じ固有値章の後続から続ける。

（これより古い 224 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 5 セクション
- 形式検証の土台: 1 セクション
- 零点の詰め寄り: 5 セクション
- 熱力学極限: 10 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 熱力学極限 | 開境界長方形の分配多項式 | 記述と SageMath まで | 次 tick は五定義の Lean 具体版・必要十分版・導出 |
| 熱力学極限 | 開境界長方形の接合不等式 | todo | 横または縦に接ぐときの積との比較 |
| 熱力学極限 | 周期境界と開境界の境界評価 | todo | 境界を横切る辺は $2L$ 本 |
| 熱力学極限 | 自由エネルギー密度の極限の存在 | todo | 接合不等式と境界評価から導く |
| 熱力学極限 | 零点密度 | todo | |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-15（tick 269）: 「自由エネルギー密度の極限の存在」を、論法の異なる三つの基礎と
  最後の導出へ割り直した（上限への任意近接だけでは任意の数列の収束を導けないため）。先頭の
  「開境界長方形の分配多項式」を五つの定義ブロックで記述し、SageMath は小さい全長方形で
  頂点・辺・配位・破れボンド数・$\mathbb{Z}[x]$ の分配多項式を 108 件厳密検査した。Lean 未着手。
  式変形統一では姉妹側「$V^{(+)}$ と $T_{(V^{(+)})}$ の定義」の平方根の鎖へ行末根拠を補った。

- 2026-08-15（tick 268）: 「上限の一意性（記法 sup の正当化）」を一 tick で四層まで完成させた
  （反対称性一本の小セクションのため）。一ブロック `claim_real_set_supremum_unique`。
  SageMath（`sagemath/check/real-set-supremum-uniqueness`）は $\mathbb{Q}$ の厳密比較のみで
  74 件。Lean 具体版 `realSetSupremum_unique`、必要十分版 `leastUpperBound_unique_necSuf`
  （実数を外し半順序だけを残した。`lt_or_eq_of_le` が反対称性を要するため `PartialOrder` が
  下限で、三分律の全体は不要と判明）、導出 `realSetSupremum_unique_from_necSuf`。
  本文の「この先に書くこと」とセクション表から当該項目を消した。式変形統一では姉妹側
  「$H_1^{(+)},H_2$ を $\check Z,\check Y$ で表す」の二つの鎖へ $1/M$ と $M$ の相殺の
  行末根拠を補った。

- 2026-08-15（tick 267）: 「上界・最小上界の定義と上限への任意近接」の Lean 具体版
  `freeEnergyDensity_supremum_approximation`、必要十分版
  `rangeValue_supremum_approximation_necSuf`、導出
  `freeEnergyDensity_supremum_approximation_from_necSuf` を完成させ、四層を揃えた。必要十分版は
  線形順序上の値域・上限・上限より小さい元だけへ仮定を削った。式変形統一では姉妹側
  「$\check Z,\check Y$ から $Z_j,Y_j$ を復元する」の復元式の最終行へ行末根拠を補った。

- 2026-08-15（tick 266）: 「自由エネルギー密度の極限の存在」から一論法で閉じる片を
  「上界・最小上界の定義と上限への任意近接」として切り出し（割り直しの理由: 任意近接は
  上限の最小性からの背理法一本で閉じるが、ある N 以降のすべての L で近づくことの証明は
  別の道具を要するため）、記述と SageMath まで進めた。三ブロック:
  `def_real_set_upper_bound`・`def_real_set_supremum`・
  `claim_free_energy_density_supremum_approximation`。SageMath
  （`sagemath/check/free-energy-density-supremum-approximation/`）は $\mathbb{Q}$ の厳密比較のみで
  154 件。Lean 未着手。あわせて「上限の一意性（記法 sup の正当化）」をセクション表と本文の
  「この先に書くこと」へ追加した（定義ブロックが一意性を後続へ約束したため）。式変形統一では
  姉妹側「$H$ と $\check Z,\check Y$ の交換子」の三つの鎖の最終行へ行末根拠を補った。

- 2026-08-15（tick 265）: 「自由エネルギー密度の値集合の上限の存在」の Lean 具体版
  `freeEnergyDensityValueSet_has_supremum`、必要十分版
  `indexedValueSet_has_supremum_necSuf`、導出
  `freeEnergyDensityValueSet_has_supremum_from_necSuf` を完成させ、四層を揃えた。必要十分版は、
  添字付き値集合の証人、一様上界、空でない上に有界な集合へ上限を与える性質だけへ仮定を削った。
  式変形統一では姉妹側「$V'$ のトレース」の有限積への分解の先頭行へ行末根拠を補った。

（これより古い 235 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-15（tick 269）: 前 tick の「上限の一意性」の本文・SageMath・Lean 具体版・必要十分版・
  導出を突き合わせた。両向きの上界比較、$\le$ の展開、狭義順序と相等の場合の除外が同じ順で
  揃い、SageMath の有限モデルの限界も明記されているため、修正は無い。

- 2026-08-15（tick 268）: 前 tick の「上限への任意近接」の Lean 三本を本文と突き合わせた。
  具体版は上界・上限の定義を本文どおりの述語として持ち、$u-\varepsilon<u$・最小性からの
  背理法・反例の取得・値集合の展開を同じ順で辿る。必要十分版の仮定は線形順序上の値域・
  上限・上限より小さい元の三つだけ、導出・sorry 非依存検査への登録（3 件）・本文ブロックの
  lean 宣言も揃っており、修正は無い。

- 2026-08-15（tick 267）: 前 tick の上界・最小上界の二定義と「上限への任意近接」の本文を
  SageMath と突き合わせた。$u-\varepsilon<u$、最小性から非上界を導く背理法、非上界の否定量化の
  展開、値集合から格子サイズの証人を取る段と、154 件の厳密比較の検査範囲が一致している。
  真の上限は有限標本で検査できないことも明記されているため、修正は無い。

- 2026-08-15（tick 266）: 前 tick の「自由エネルギー密度の値集合の上限の存在」の Lean 三本を
  本文と突き合わせた。具体版は証人 $L=1$・一様上界・完備性の一度の適用を同じ順で辿り、
  必要十分版の仮定は証人・点ごとの上界・上限付与の三つだけ、導出・sorry 非依存検査への登録
  （3 件）・本文ブロックの lean 宣言も揃っており、この点の修正は無い。ただし本文側で、
  「最小上界」が未定義のまま「実数の完備性への脱出の宣言」と上限存在の主張に使われていたこと、
  および同宣言の「$\le_{\mathbb{R}}$ の略記の初出はここ」が実際の初出（それより前の行の
  不等式鎖）と食い違っていたことを見つけた。上界・最小上界の定義ブロックの新設と、略記宣言の
  「実数体への脱出の宣言」への移動で直した（前進セクションと一体で main へ反映）。

- 2026-08-15（tick 265）: 前 tick の「自由エネルギー密度の値集合の上限の存在」の本文と
  SageMath を突き合わせた。非空性の証人 $L=1$、$L$ に依らない上界、完備性の一度の適用と、
  SageMath の厳密検査・ball 検査・有限モデルの範囲が一致し、完備性そのものは有限標本で検査
  できないことも明記されているため、修正は無い。

（これより古い 256 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
