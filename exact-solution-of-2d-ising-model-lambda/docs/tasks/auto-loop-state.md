# 自動ループ 状態台帳

[auto-loop-runbook.md](auto-loop-runbook.md) が毎 tick 読み書きする。**この台帳が進捗の正本**である。

- 起動: launchd `com.masaori.ising-lambda-auto-loop`（毎時 5 分。見送られたときの再試行が 35 分。上限 45 分）
- 1 tick = 既存出力のレビューと修正 → セクションを 1 つだけ前進 → 検証 → push → 停止

## 現在地

- **2026-08-15 の tick 282 は、「開境界長方形を第二座標方向へ反復接合した値の評価」の
  Lean（具体版・導出版）を完成させ、本文へ `lean` 宣言を付けてセクションを閉じた。必要十分版は
  接ぐ向きに依らない `iterated_glue_pow_bounds_necSuf` をそのまま使った。これで claim の四層が
  揃った。レビューでは前 tick の本文・SageMath（40 組）と第二座標方向の接合不等式が一致し、
  修正は無い。**
  具体版 `OpenRectangleIteratedGluingSecond.lean` は第一座標方向と同じ帰納法を、接合面の長さを
  `b` から `a` へ、接ぐ辺を第二座標へ入れ替えて人手証明と同じ順で辿った（k=1 の底の等号鎖、
  ka=a+(k-1)a、冪の指数法則、帰納法の仮定と正数の乗法、第二座標の長さ kb と b への接合不等式、
  (k+1)b=kb+b）。導出は low/high へ `t^a` と `1` を代入して二場合を復元した。
  sorry 検査 1018 件、すべて非依存。式変形統一では姉妹側「半整数運動量のモード」の反周期性の
  証明末尾の二等号の行へ行末根拠を付けた。次は「開境界正方形のブロック敷き詰め評価」。


- **2026-08-15 の tick 281 は、「開境界長方形を第二座標方向へ反復接合した値の評価」を
  本文と SageMath まで進めた。Lean（具体版・必要十分版・導出）は未着手である。レビューでは
  前 tick の第一座標方向の本文・SageMath・Lean が一致し、修正は無い。**
  第二座標方向の接合面は長さ `a` なので、一回の接合不等式を `k` について帰納的に適用し、
  `0<t≤1` と `1≤t` の二場合で `Z^op_{a,kb}(t)` を
  `t^{(k-1)a}(Z^op_{a,b}(t))^k` と `(Z^op_{a,b}(t))^k` の間に挟んだ。
  SageMath は正の有理点 5 点と長方形・反復回数 8 組の計 40 組を厳密検査した。
  式変形統一では姉妹側「半整数運動量の指数和」の二つの等号へ行末根拠を付けた。


- **2026-08-15 の tick 280 は、「開境界長方形を第一座標方向へ反復接合した値の評価」の
  Lean（具体版・必要十分版・導出）を完成させ、本文へ `lean` 宣言を付けてセクションを
  閉じた。これで claim の四層が揃った。レビューでは前 tick の本文・SageMath（40 組）が
  一致し、修正は無い。**
  具体版は反復回数の帰納法を人手証明と同じ順（k=1 の底の等号鎖、kb=b+(k-1)b、
  冪の指数法則、帰納法の仮定と正数の乗法、一辺 ka と a への接合不等式）で辿った。
  必要十分版 `iterated_glue_pow_bounds_necSuf` は格子・配位・実数を外し、一段の上下評価と
  底の一致から冪の評価を出す帰納法だけを残した（積の可換性は不要と判明。
  `Semiring` + `PartialOrder` + `IsOrderedRing`）。導出は接合面因子 low/high へ
  `t^b` と `1` を代入して二場合を復元した。sorry 検査 1014 件、すべて非依存。
  式変形統一では姉妹側「なぜ (−) セクターだけを使うか」の証明中、根拠なしで終わる
  四つの鎖の末尾行へ行末根拠を付けた。次は「開境界長方形を第二座標方向へ反復接合した
  値の評価」。


- **2026-08-15 の tick 279 は、「自由エネルギー密度の極限の存在」を論法ごとに割り直し、
  先頭の「開境界長方形を第一座標方向へ反復接合した値の評価」を本文と SageMath まで進めた。**
  一回の接合不等式を反復回数について帰納的に適用し、接合面 `k-1` 本が与える因子まで含む
  上下評価を得た。レビューでは前 tick の SageMath 概要に残っていた Lean 未了の古い記述を、
  四層完成済みという実態へ直し、前進前に main へ反映した。Lean は未着手である。


- **2026-08-15 の tick 278 は、「周期境界と開境界の境界評価」の実数評価の上下評価の
  Lean（具体版・必要十分版・導出）を完成させ、本文へ `lean` 宣言を付けてセクションを
  閉じた。これで claim の四層が揃った。レビューでは前 tick の辺の直和分解・境界破れ
  本数の上界・破れボンド数の分解が本文と一致し、修正は無い。**
  具体版は、全単射 $r_L$ に沿う和の並べ替え・破れボンド数の分解・冪の指数法則で
  周期境界の値を開境界配位の和へ書き換え、$0<t\le1$ と $1\le t$ の二場合で境界因子を
  自然数冪の順序（指数の帰納法）で挟んだ。必要十分版は格子・配位・実数を外し、
  添字の全単射・指数の二項分解・境界因子の項ごとの評価・可換半環の順序だけを残した
  （接合不等式の必要十分版と違い、添字の対の構造も不要と判明）。sorry 検査 1009 件、
  すべて非依存。式変形統一では姉妹側「行列版 $e^{X}Ye^{-X}=e^{\mathrm{ad}_X}(Y)$」の
  証明中、冪のノルム評価の帰納法の底 $l=1$ の行内三等号鎖を一行一等号と行末根拠へ
  開いた。次は「自由エネルギー密度の極限の存在」。


（これより古い 237 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## セクション台帳

**済んだ範囲**（章ごとの件数。個々の内訳は [auto-loop-archive.md](auto-loop-archive.md) と
MEMORY.md にある。番号で呼ばないので、ここでは章と件数だけを持つ）。

- 固有値の代数性: 128 セクション
- Fisher 零点: 44 セクション
- 分配多項式: 4 セクション
- 転送行列: 4 セクション
- 有限系の自由エントロピー: 5 セクション
- 形式検証の土台: 1 セクション
- 零点の詰め寄り: 5 セクション
- 熱力学極限: 16 セクション

**残っているもの**（この順に進める。tick は先頭の 1 件だけを実行する）。

| 章 | セクション | 状態 | 備考 |
|---|---|---|---|
| 熱力学極限 | 開境界正方形のブロック敷き詰め評価 | todo | 二方向の反復接合を組み合わせる |
| 熱力学極限 | 開境界自由エネルギー密度の極限 | todo | ブロック評価と上限への任意近接から導く |
| 熱力学極限 | 周期境界自由エネルギー密度への移送 | todo | 周期境界と開境界の境界評価から導く |
| 熱力学極限 | 零点密度 | todo | |
| 臨界指数を零点列で書く | 先頭零点の列と有限サイズスケーリング | todo | |

**セクションを割り直したら、この表を書き換える。** 番号は振らない（内容の分かる名前で書く）。
割り直した理由は「前進の記録」へ 1 行で残す。

## 前進の記録

- 2026-08-15（tick 282）: `ThermodynamicLimit/OpenRectangleIteratedGluingSecond.lean` に、
  反復回数の帰納法の具体版 `openPartitionValue_iteratedGlueSecond_bounds_of_le_one/of_one_le`
  を人手証明と 1 対 1 に実装した（底の等号鎖・ka=a+(k-1)a・冪の指数法則・帰納法の仮定と
  正数の乗法・第二座標の長さ kb と b への接合不等式・(k+1)b=kb+b）。必要十分版は接ぐ向きに
  依らない `iterated_glue_pow_bounds_necSuf` をそのまま使い、導出二定理
  `OpenRectangleIteratedGluingSecondFromNecSuf.lean`（low/high へ t^a と 1 を代入）で
  特殊化を示した。本文の claim へ `lean` 宣言を付け、SageMath 概要の未着手の記述を実態へ直した。
  sorry 検査 1018 件、すべて非依存。式変形統一では姉妹側「半整数運動量のモード」の反周期性
  $e^{-iM\tilde\theta_\mu}=-1$ の証明末尾にあった二等号の行 $1\cdot(-1)=-1$ を、三角関数の値の代入・
  積の整理・積の単位元の三行の一行一等号と行末根拠へ開いた。


- 2026-08-15（tick 281）: `claim_open_rectangle_iterated_gluing_second` を記述し、一回の
  第二座標方向の接合不等式を `k` について帰納的に適用して、接合面 `k-1` 本が与える因子
  `t^{(k-1)a}` を含む二場合の上下評価を得た。SageMath は正の有理点 5 点と
  長方形・反復回数 8 組の計 40 組を厳密検査した。Lean は未着手。式変形統一では姉妹側
  「半整数運動量の指数和」の有限和からの因子のくくり出しと指数の分母の約分へ行末根拠を付けた。


- 2026-08-15（tick 280）: `ThermodynamicLimit/OpenRectangleIteratedGluingFirst.lean` に、
  反復回数の帰納法の具体版 `openPartitionValue_iteratedGlueFirst_bounds_of_le_one/of_one_le`
  を人手証明と 1 対 1 に実装した（底の等号鎖・kb=b+(k-1)b・冪の指数法則・帰納法の仮定と
  正数の乗法・一辺 ka と a への接合不等式・(k+1)a=ka+a）。必要十分版
  `NecSuf/ThermodynamicLimit/OpenRectangleIteratedGluingFirst.lean`
  （`iterated_glue_pow_bounds_necSuf`。一段の上下評価 low·(P k·z) ≤ P(k+1) ≤ high·(P k·z)
  と底 P 1 = z から low^{k-1} z^k ≤ P k ≤ high^{k-1} z^k を出す帰納法だけを残した。
  積の可換性は不要と判明し `Semiring` で通る）と導出二定理（low/high へ t^b と 1 を代入）
  を書き、本文の `claim_open_rectangle_iterated_gluing_first` へ `lean` 宣言を付けて
  セクションを閉じた（sorry 検査 1014 件、すべて非依存）。式変形統一では姉妹側
  「なぜ (−) セクターだけを使うか」（`013_even_sector_modes`）の証明中、根拠なしで
  終わる四つの鎖の末尾行（符号の消去と結合法則、単位行列と同じ行列の和、スカラー倍の
  くくり出し、スカラーの計算）へ行末根拠を付けた。


- 2026-08-15（tick 279）: 「自由エネルギー密度の極限の存在」を、第一座標方向の反復接合、
  第二座標方向の反復接合、正方形のブロック敷き詰め評価、開境界での極限、周期境界への移送へ
  割り直した（それぞれが帰納法・二方向の合成・極限・境界移送という別の論法だからである）。
  先頭の `claim_open_rectangle_iterated_gluing_first` を記述し、一回の接合不等式を `k` について
  帰納的に適用して、`0<t≤1` と `1≤t` の二場合で `Z^op_{ka,b}(t)` を
  `t^{(k-1)b}(Z^op_{a,b}(t))^k` と `(Z^op_{a,b}(t))^k` の間に挟んだ。
  SageMath は正の有理点 5 点と長方形・反復回数 8 組の計 40 組を厳密検査した。Lean は未着手。
  式変形統一では姉妹側「`e^{ad_X}(Y)` の級数展開」の反復交換子の等式へ、再帰的定義という
  行末根拠を付けた。


- 2026-08-15（tick 278）: `ThermodynamicLimit/PeriodicOpenComparisonInequality.lean` に、
  周期境界の値を開境界配位の和へ書き換える `partitionValue_eq_open_double_product` と、
  二場合の上下評価 `partitionValue_periodicOpen_bounds_of_le_one/of_one_le` を実装した。
  必要十分版 `NecSuf/ThermodynamicLimit/PeriodicOpenComparisonInequality.lean`
  （`sum_pow_reindex_bounds_necSuf`。添字の全単射・指数の二項分解・境界因子の項ごとの
  評価・可換半環の順序だけを残した。接合不等式と違い添字の対の構造は不要と判明）と
  導出二定理を書き、本文の `claim_periodic_open_boundary_comparison` へ `lean` 宣言を
  付けてセクションを閉じた（sorry 検査 1009 件、すべて非依存）。式変形統一では姉妹側
  「行列版 $e^{X}Ye^{-X}=e^{\mathrm{ad}_X}(Y)$」の証明中、冪のノルム評価の帰納法の底
  $l=1$ の行内三等号鎖 $\|X^{1}\|=\|X\|=a=a^{1}$ を一行一等号と行末根拠へ開いた。


（これより古い 248 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 式変形の書き方の統一（並列の作業ストリーム。毎 tick 1 件）

規則は両プロジェクトの README にある「式変形は一続きにする。根拠は行末に $(\because\ \dots)$ で書く」。
**毎 tick 1 件だけ**書き換え、検証を通し、ここへ記録する。中身は変えない（書き方だけ）。

### 本プロジェクト（`exact-solution-of-2d-ising-model-lambda`）

| 証明 | 状態 |
|---|---|
| 分配多項式の係数は多重度である | 済（2026-08-08） |
| 多重度の総和は配位の総数に等しい | 済（2026-08-08） |
| すべての配位を等しく数える点での自由エントロピー | 済（2026-08-08） |

（済んだ分の一覧は [auto-loop-archive.md](auto-loop-archive.md)。）

## レビュー記録

- 2026-08-15（tick 282）: 前 tick の「開境界長方形を第二座標方向へ反復接合した値の評価」の
  本文（帰納法の二場合の上下評価、接合面因子の指数 `(k-1)a`）と SageMath（40 組の厳密検査、
  `check.sage` の判定式）、および Lean の第二座標方向の一回の接合不等式の形を突き合わせた。
  主張・場合分け・指数が一致し、修正は無い。


- 2026-08-15（tick 281）: 前 tick の「開境界長方形を第一座標方向へ反復接合した値の評価」の
  本文・SageMath・Lean 具体版・必要十分版・導出版を突き合わせた。二場合の不等式、接合面因子
  の指数 `(k-1)b`、一回の接合不等式の特殊化が一致し、修正は無い。


- 2026-08-15（tick 280）: 前 tick の「開境界長方形を第一座標方向へ反復接合した値の評価」の
  本文（帰納法の二場合の上下評価）と SageMath（40 組の厳密検査、`check.sage` の判定式）を
  突き合わせた。主張・場合分け・接合面因子の指数 $(k-1)b$ が一致し、`overview.md` の
  検査範囲の記述も実態どおりのため、修正は無い。


- 2026-08-15（tick 279）: 前 tick の「周期境界と開境界の境界評価」の実数評価について、
  本文・SageMath・Lean 具体版・必要十分版・導出版を突き合わせた。数学内容と四層の対応は
  揃っていたが、SageMath の `overview.md` だけが「実数評価の上下評価、必要十分版・導出版は
  未了」と記したままだったため、完成済みという実態へ訂正し、前進前に main へ反映した。


- 2026-08-15（tick 278）: 前 tick の `PeriodicOpenComparison.lean` の辺の直和分解
  （周期辺の横・縦への分割、開境界辺と各向き `L` 本の境界横断辺への振り分け）、
  境界破れ本数の `2L` 上界、破れボンド数の分解を本文の証明と突き合わせた。
  辺の対応・破れる条件の同値・分解の並びが一致し、入口 import と sorry 非依存検査への
  登録も揃っているため、修正は無い。


（これより古い 269 件は [auto-loop-archive.md](auto-loop-archive.md) へ移した。）

## 判断待ち（人間に問うべき論点）

- **content のファイルを分けるときの文書順の決め方。** システムは `content/` のファイル名昇順を
  文書順とみなすが、リポジトリの規約はファイル名の連番を禁じている。
  2026-08-08（tick 5）に 2 つめの章を書くときこれに当たった。連番は振らず、章ごとにファイルを
  分けることもせず、**本文を 1 ファイル `content/main-text.ts` へまとめて章を見出しブロックで
  区切る**形にした（ファイルが 1 つなら配列順がそのまま文書順になり、論点に当たらないため。
  旧ファイル名 `partition-polynomial.ts` は 1 章分しか指さないので改名した）。
  これは論点の解決ではなく回避である。本文が育ってファイルを分けたくなった時点で決着が要る。
  → **決着の案（人間の判断を待つ）**: システム側（リポジトリ直下 `structured-latex/`）に
  文書順の明示的な宣言（例えば `content/order.ts` にファイル名を並べる）を入れ、
  ファイル名昇順という暗黙の規則をやめる。この変更はシステム側の入力言語に触るため、
  他プロジェクト（`exact-solution-of-2d-ising-model/` 等）にも影響する。

## cron（launchd）

- ラベル: `com.masaori.ising-lambda-auto-loop`
- 定義: `~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`
- 実体: `scripts/auto-loop-tick.sh`（毎時 5 分、見送られたときの再試行が 35 分。45 分で打ち切る）
- ログ: `logs/auto-loop.log`（git 管理外）
- 各 tick は**独立した新しいセッション**として走る（文脈を持ち越さない。持ち越すのは
  この台帳とリポジトリの中身だけ）。使うエージェントは **Claude と Codex の交互**
  （Claude は `claude-fable-5` の effort medium、Codex は `gpt-5.6-sol` の reasoning medium）。
  片方が使用量の上限に当たった間は、期限を `logs/claude-blocked-until` へ記録してもう片方だけで回す。
- 監査は別ジョブ（毎時 55 分の軽い監査 `scripts/audit-light.sh`、毎日 04:20 の重い監査
  `scripts/audit-loop.sh`）。PDF は `scripts/refresh-pdf.sh` が 5 分おきに最新へ保つ。

停止するには `launchctl bootout gui/$(id -u)/com.masaori.ising-lambda-auto-loop`。
再開するには `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.masaori.ising-lambda-auto-loop.plist`。
