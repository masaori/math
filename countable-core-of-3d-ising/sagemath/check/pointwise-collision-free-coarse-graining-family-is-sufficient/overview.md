# SageMath Check: 各箱で有限箱データを区別する粗視化の族は極限量に対して十分である

**対象ラベル**: `claim_pointwise_collision_free_coarse_graining_family_is_sufficient`

各箱 $L$ ごとの写像 $\tau_L$ が値の衝突を持たないとき、$\pi_L(q):=\tau_L(S_q(L))$ で定めた族が
極限量に対して十分であることを、証明と同じ順で追う。すなわち、すべての $L$ で
$\pi_L(q)=\pi_L(q')$ という仮定から、$\tau_L$ が衝突を持たないことにより
$S_q(L)=S_{q'}(L)$ を得て、既存の橋の可算側の段（素指数データからの一意な復元、
$Z_L(q)=Z_L(q')$、$\#V_L$ の一致）へ渡るところまでを確認する。

結論の $\alpha(q)=\alpha(q')$ は実数の等式であり、
`claim_limit_quantity_depends_only_on_finite_box_sequence` への帰着なので検査対象外とする。

| 確かめた段 | 方法 | ステータス |
| --- | --- | --- |
| $\tau_L$ が値の衝突を持たないこと（有理点から実際に現れるデータの範囲で） | 像の一致から元の一致を全対で厳密比較 | PASS |
| 仮定 $\pi_L(q)=\pi_L(q')$ が成り立つ有理点の対を用意できること | 同じ有理数の異なる表示 3 組 | PASS |
| 仮定から $S_q(L)=S_{q'}(L)$ が従うこと | 有限タプルの厳密比較 | PASS |
| 素指数データから $Z_L(q)$ が一意に復元されること | `QQ` で $\prod p^{e_p}$ を厳密計算 | PASS |
| 可算側の等式 $Z_L(q)=Z_L(q')$ と $\#V_L$ の一致 | `ZZ[X]` の評価を `QQ` で厳密比較 | PASS |
| 判定が空虚でないこと（列が異なる対ではある $L$ で粗視化の値も異なる） | $L=1$ は辺が無く $Z_1$ が定数なので差は $L\ge2$ で現れることを確認 | PASS |
| 逆向きを主張しない理由（衝突を与えるデータが実際の族 $\{S_q(L)\}$ に現れない例） | 値 $1$ と $2$ を潰す写像で、有理点の族に両者がともには現れないことを確認 | PASS |

確かめた写像 $\tau_L$ は恒等写像・素指数データからの復元・既約表示の分子と分母の対の三つ。
箱は $L=1,2$（$L=3$ は $2^{27}$ 配位の列挙になるため含めない）。

この検査は有限側だけで閉じており、箱の大きさの極限を使わない。
浮動小数点、実対数、指数関数、無限和、級数、積分、微分は使わない。

```sh
sage sagemath/check/pointwise-collision-free-coarse-graining-family-is-sufficient/check.sage
```

**2026-08-23 実行: すべて通過。**
