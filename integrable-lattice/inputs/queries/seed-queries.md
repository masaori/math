# Seed Queries

## Local relation

```text
"Yang-Baxter equation" "lattice model"
"star-triangle relation" "lattice model"
"reflection equation" "integrable boundary" "lattice model"
```

## Transfer matrix

```text
"commuting transfer matrices" "integrable lattice"
"fusion hierarchy" "lattice model"
"T-system" "Y-system" "lattice model"
```

## Closed formula

```text
"partition function" determinant "six-vertex"
"domain wall boundary" "determinant formula" "six-vertex"
"Pfaffian" "dimer model" "partition function"
```

## Algebraic

```text
"Temperley-Lieb" "loop model" "integrable"
"Hecke algebra" "lattice model" "Yang-Baxter"
```

## Character identity

```text
"finitized character" "RSOS"
"ABF model" "character identity"
"Rogers-Ramanujan" "lattice model"
```

## Open problem patterns (cross-cut with model/operation queries)

これらは単独では雑音が多い。上のモデル/操作クエリと AND で組み合わせる。

```text
"open problem" "integrable lattice"
"remains open" "Yang-Baxter"
"we conjecture" "lattice model"
"it would be interesting" "transfer matrix"
"future work" "RSOS"
"open question" "six-vertex"
"is not known" "Bethe ansatz"
```

## Review / survey targets

review / survey は open problem 密度が高いので明示的に拾う。

```text
review "integrable lattice models"
survey "exactly solvable lattice"
"lecture notes" "integrable systems" "open"
proceedings "integrable" "open problems"
```


