# cycle 1 step 5 — 形式検証(F-U1): 状況と仕様

## 環境状況（正直に）

- このセッション環境に **Lean は未インストール**（`lean` not found）。よって Lean の `decide`/reflection 証明の実行は**この周ではブロック**。
- ただし F-U1 の数学的中身は「命題が**決定手続き**に乗る」ことであり、その決定手続き自体は SageMath で既に exhibit 済み（下記）。Lean は手続きの機械反映に過ぎない。

## 決定手続きは実証済み（Lean なしでも中身は確認できる）

- **Λ の等号・順序 = 整数比較**: Massieu $\Phi_N=\log Z_N(q)$ の等号は $Z_N(q)\in\mathbb{Q}$ の一致、$\Phi_A=\Phi_B\iff Z_A=Z_B$。第〇法則 $\beta_A=\beta_B$ は $\Omega$ 比の整数比較。`D_phi_lambda` で $Z_N$ を厳密整数として計算し素因数分解 ⇒ 等号・順序は無条件決定可能。
- **ℚ̄ の等号・順序 = 根分離**: 固有値・零点の一致/大小は最小多項式＋実根分離。`C-U3_bethe_qqbar` / `apply_higher_spin_qqbar` で `QQbar` により実演（`all in QQbar = True`、最小多項式 witness）。
- いずれも **ℝ 不使用・丸めゼロ・witness 付き**。強度は RCA₀ 級（可算性の効用§2-4）。

## Lean 形式化のターゲット仕様（環境が整い次第そのまま実装）

```
-- 命題 P1 (第〇法則, decide 可能)
-- 与えられた有限・離散二系 A,B と全エネルギー U に対し、
-- 平衡配分 U_A* で β_A(U_A*) = β_B(U_B*)。
-- 表現: Ω は ℕ 値、β は ℤ 上の差分商の符号 ⇒ 命題は ℤ 上の不等式系の充足 ⇒ `decide`。
--
-- 命題 P2 (Massieu Φ の Λ 等式, decide 可能)
-- 有理点 q で Φ_N = log Z_N(q) を素因数分解の指数ベクトル(有限台の ℤ^(素数)) として、
-- 二つの構成 Φ, Φ' の一致は対応する整数ベクトルの一致 ⇒ `decide`。
--
-- 命題 P3 (零点/固有値の ℚ̄ 帰属と分離, decide 可能)
-- 整係数多項式 p の根は代数的、相異判定・実根数は Sturm/根分離 ⇒ 決定可能。
-- Lean では `Polynomial ℤ` + 既存の根分離/`decide` 補題で反映。
```

## ステータス

F-U1 = **環境ブロック（Lean 未導入）**。数学的中身（決定手続き）は SageMath で実証済み。Lean 実装は環境導入後の作業として仕様を確定。これは判断停止ではなく環境制約の記録。
