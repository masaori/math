/**
 * **本文側の主張ごとに、完了まで何段残っているかを数える**（cycle 39 step 5 で新設。検査 D）。
 *
 * ## なぜこれが要るか
 *
 * **本文側の 16 件が 4 サイクル動いていない**（cycle 36・37・38・39）。
 * ところが「16 件」という数は**完了したかどうかの 2 値でしか見ていない**ので、
 * 近づいているのか止まっているのかが分からない。段が動いても件数は動かないため、
 * **進んだ日と何もしなかった日が、この数の上では同じに見える。**
 *
 * cycle 39 の焦点は「16 件それぞれについて、完了までに何段残っているかを
 * 機械が数え直せる形にできるか」だった。**測った答えは「一部はできる。残りは今の台帳では数えられない」である。**
 *
 * ## 何が数えられて、何が数えられないか（実測で分かったこと）
 *
 * 完了でない 16 件の内訳は次のとおりである。
 *
 * - **`remainingItems` を持つ欄**は、その配列の長さがそのまま残りの段数になる。**数えられる。**
 * - **持たない `部分的` の欄**は、本文が部（(G1)(G2)… のような小見出し）で切られているものである。
 *   cycle 34 step 5 の検査が「台帳が部を 1 つ残らず扱っていること」を見ているが、
 *   **どの部が済んでいてどの部が残っているかは台帳が持っていない。** 今の形では数えられない。
 * - **`未着手` の欄**は、着手していないので段の切り方自体が無い。**原理的に数えられない。**
 *
 * ## この台帳が要求すること
 *
 * **数えられない欄には、なぜ数えられないかを書かせる。** 黙って数から外れる道を塞ぐためである。
 * 「16 件」という数だけを見ていると、**数えられない欄があること自体が見えない。**
 *
 * ## 限界（正直に書く）
 *
 * - **段の数は、段の切り方に依存する。** 同じ残りを 1 件と書くことも 3 件と書くこともできる。
 *   したがってこの数は**欄どうしで比べられない。** 比べられるのは同じ欄の時間変化だけである。
 * - **残りが尽きたら完了する、とは言えない。** cycle 35・36・38・39 で 4 回起きたとおり、
 *   書いてみると外側に段が現れる（検査 E がその一部を塞いでいる）。
 *   **この数は下界であって、完了までの距離ではない。**
 * - 数えられない理由の宣言は人の言葉であり、機械はそれが正しいかを見ない。
 */

export type UncountableReason = {
  /** 検査 F の台帳の `block`。 */
  readonly block: string;
  /** なぜ今の台帳では段数が数えられないか。 */
  readonly why: string;
};

/**
 * **段数が数えられない欄と、その理由**（cycle 39 step 5 の実測）。
 *
 * 実測では 完了でない 16 件のうち **10 件**がここに入る（`部分的` 8 件・`未着手` 2 件）。
 */
export const UNCOUNTABLE_REASONS: readonly UncountableReason[] = [
  {
    block: "paper_031_theorem_lsw",
    why: "未着手。多変数の Mahler 測度が mathlib に無く、段の切り方そのものが無い",
  },
  {
    block: "paper_053_theorem_lower_order",
    why:
      "未着手。本文の 4 部（低位項・退化点の計数・トーラス塔・消滅深度による一般の退化塔）は " +
      "いずれも塔の全域木数の漸近に依り、その漸近が外部定理（Cuoco–Monsky の岩澤型漸近。未着手）である。" +
      "**cycle 45 step 1 の数え直しで、cycle 39 以来ここに書いてあった「段の切り方そのものが無い」は" +
      "正しくないと分かった**——散文は残りを名指ししている。ただし名指しているのは外部定理そのもので、" +
      "それは外部定理の台帳の側で 1 件として数えている。二重に数えないためにここでは数えない",
  },
];
