/**
 * 論文本体 第 11 章（英語版）: 一般の塔の閉形式（5 係数すべて）と、係数の情報階層。
 *
 * **正本は日本語版 `../../../content/010_general_closed_form.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする
 * （本章で新しく要る語＝捻り段データ・飽和深度・過渡欠損・情報階層は同用語集 §4 にある）。
 *
 * **数式の中に日本語は 1 つも無い**ので、`../structure-exceptions.ts` への新規登録は行っていない。
 * 強調（`**`）も日本語版の時点でノードをまたがないように書いてあるため、
 * 英語版でも同じ位置に同じ強調を置ける（009 章のような構造の組み替えは要らなかった）。
 */

import {
  defineTranslatedBlocks as defineBlocks,
  displayMath,
  list,
  math,
  paragraph,
  refInTranslation as ref,
} from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_110_heading_general_closed_form",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 1 },
    title: {
      text:
        "A closed form for a general tower (all five coefficients), and the information " +
        "hierarchy of the coefficients",
    },
    labels: [],
  },
  {
    id: "paper_111_theorem_general_closed_form",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 2 },
    title: {
      text:
        "Proposition M — a closed form for a general tower: all five coefficients are " +
        "determined by a finite computation from the coefficients of the spectral polynomial",
    },
    labels: ["paper_prop_M"],
    habitat: "Qbar",
    verification: [
      "sagemath/check/cycle21_T3_general_closed_form",
      "sagemath/check/cycle21_T3_b_star",
    ],
    statement: [
      paragraph([
        "We keep the setting of ",
        ref("paper_prop_K"),
        ". Here ",
        math(String.raw`X`),
        " is a finite connected voltage multigraph of rank two, that is ",
        math(String.raw`\Gamma\cong\mathbb{Z}_\ell^2`),
        ", and we put ",
        math(String.raw`D=\det L(z,w)`),
        ", ",
        math(String.raw`\mu=v_\ell(\mathrm{content}\,D)`),
        ", ",
        math(String.raw`E=\ell^{-\mu}D`),
        ", ",
        math(String.raw`\tilde E=z^{r}w^{s}E\in\mathbb{Z}[z,w]`),
        " and ",
        math(String.raw`\kappa_n=\kappa(X_{\ell^n,\ell^n})`),
        ". The hypothesis **(H)** (every stage is connected) is in force throughout. We use ",
        ref("paper_prop_J"),
        " and its ",
        math(String.raw`\theta`),
        ", ",
        math(String.raw`\hat\theta_M`),
        ", ",
        math(String.raw`\Theta_M=\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}\hat\theta_M(P)`),
        ", together with ",
        ref("paper_prop_K"),
        " and its ",
        math(String.raw`S_\infty`),
        ", ",
        math(String.raw`j^{*}(P_0)`),
        ", ",
        math(String.raw`e_{j^*}`),
        ".",
      ]),
      paragraph([
        "**(M1 Twisted stage data)** Let ",
        math(String.raw`P_0\in S_\infty`),
        " correspond to the primitive integer vector ",
        math(String.raw`u=(u_1,u_2)`),
        ", let ",
        math(String.raw`\det(u,\mathbf{e})=1`),
        " determine an integer vector ",
        math(String.raw`\mathbf{e}`),
        " (the extended Euclidean algorithm produces one), let ",
        math(String.raw`\eta`),
        " be a primitive ",
        math(String.raw`\ell^{k}`),
        "-th root of unity, and put ",
        math(String.raw`\mathcal{O}_k=\mathbb{Z}[\eta]`),
        ". Set",
      ]),
      displayMath(
        String.raw`\Phi^{[k]}_{u}(x):=\tilde E\bigl((1+x)^{u_1}\eta^{\mathbf{e}_1},\,(1+x)^{u_2}\eta^{\mathbf{e}_2}\bigr)
        =\sum_{m}A^{[k]}_m\,x^{m}\ \in\mathcal{O}_k[x]`,
      ),
      paragraph([
        "(when negative powers of ",
        math(String.raw`(1+x)`),
        " occur, drop the monomial factor; it is a unit and does not change valuations), and",
      ]),
      displayMath(
        String.raw`\Lambda_k:=\min_m v_\ell\bigl(A^{[k]}_m\bigr),\qquad
        \theta^\sharp_k:=\min\bigl\{m:v_\ell(A^{[k]}_m)=\Lambda_k\bigr\},\qquad
        m^\sharp_k:=\min\bigl\{m<\theta^\sharp_k:A^{[k]}_m\neq0\bigr\}`,
      ),
      paragraph([
        "(if no such ",
        math(String.raw`m`),
        " exists, put ",
        math(String.raw`m^\sharp_k=\infty`),
        "). ",
        "**Convention (it must not be omitted)**: the ",
        math(String.raw`\Lambda_k`),
        " and ",
        math(String.raw`\theta^\sharp_k`),
        " above are ",
        math(String.raw`\min`),
        " taken only over those ",
        math(String.raw`A^{[k]}_m\neq0`),
        ", that is, over the indices ",
        math(String.raw`m`),
        " at which the coefficient does not vanish (with the convention ",
        math(String.raw`v_\ell(0)=+\infty`),
        "). Reading it instead with ",
        math(String.raw`v_\ell(0)=0`),
        ", as most computer algebra systems and formalisation libraries do by default, makes ",
        math(String.raw`p=1`),
        " — one of the two towers carrying (U4) below — give a value of ",
        math(String.raw`\Lambda_1`),
        " which should be ",
        math(String.raw`2`),
        " but comes out as ",
        math(String.raw`0`),
        ", which changes ",
        math(String.raw`c`),
        " and destroys the conclusion. We normalise ",
        math(String.raw`v_\ell`),
        " by ",
        math(String.raw`v_\ell(\ell)=1`),
        " and compute it by ",
        math(String.raw`v_\ell(\alpha)=v_\ell\bigl(N_{\mathbb{Q}(\eta)/\mathbb{Q}}(\alpha)\bigr)/\varphi(\ell^{k})`),
        ", that is, by a single valuation of an integer. Then:",
      ]),
      list([
        [
          math(String.raw`\Lambda_k,\theta^\sharp_k,m^\sharp_k`),
          " are the same at every point of the layer of depth ",
          math(String.raw`k`),
          " (the points are carried into one another by Galois conjugation, and there is a single place above ",
          math(String.raw`\ell`),
          ").",
        ],
        [
          "If ",
          math(String.raw`M\ge2k`),
          " and ",
          math(String.raw`\varphi(\ell^{M})>\bigl(\theta^\sharp_k-m^\sharp_k\bigr)\varphi(\ell^{k})`),
          ", then at **every** point of the layer of depth ",
          math(String.raw`k`),
          " one has ",
          math(String.raw`\hat\theta_M(P)=\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
          ". Here, when ",
          math(String.raw`m^\sharp_k=\infty`),
          ", that is when no coefficient with ",
          math(String.raw`m<\theta^\sharp_k`),
          " is non-zero, the difference ",
          math(String.raw`\theta^\sharp_k-m^\sharp_k`),
          " is to be read as ",
          math(String.raw`-\infty`),
          " and **the inequality is then satisfied automatically** (the difference is used in the proof only to rule out that a term with ",
          math(String.raw`m<\theta^\sharp_k`),
          " and ",
          math(String.raw`A^{[k]}_m\neq0`),
          " steals the minimum, so if no such ",
          math(String.raw`m`),
          " exists the condition is vacuous).",
        ],
        [
          math(String.raw`\varphi(\ell^{k})\Lambda_k\in\mathbb{Z}_{\ge1}`),
          ". In particular the right-hand side above is an integer.",
        ],
      ]),
      paragraph([
        "Here is the definition of the layers. The layer of depth ",
        math(String.raw`k`),
        " consists of those points of level ",
        math(String.raw`M`),
        " whose distance from ",
        math(String.raw`P_0`),
        " is exactly ",
        math(String.raw`\ell^{-(M-k)}`),
        ", that is, the points ",
        math(String.raw`P=\bigl(u+\ell^{M-k}t\,\mathbf{e}\bigr)\bmod\ell^{M}`),
        " with ",
        math(String.raw`t\in(\mathbb{Z}/\ell^{k})^{\times}`),
        " and ",
        math(String.raw`k\ge1`),
        ", together with the single point of depth ",
        math(String.raw`k=0`),
        ". As for the number of points: when ",
        math(String.raw`k\ge1`),
        " it is ",
        math(String.raw`\varphi(\ell^{k})`),
        ", and when ",
        math(String.raw`k=0`),
        " it is ",
        math(String.raw`1`),
        ". ",
        "**The content of (M1) is this: inspecting a neighbourhood of the base point amounts to " +
          "twisting a one-parameter subgroup by a root of unity, and once the ring of coefficients " +
          "is enlarged from the integers to the cyclotomic integers, the tie between minimising " +
          "indices is broken.**",
        " The root used for the twist is ",
        math(String.raw`\eta`),
        ", the enlarged ring is ",
        math(String.raw`\mathcal{O}_k`),
        ", and the tie is what occurs if the minimisation of ",
        ref("paper_prop_J"),
        " is carried out over ",
        math(String.raw`\mathbb{Z}`),
        " instead.",
      ]),
      paragraph([
        "**(M2 An explicit bound for the saturation depth)** Put ",
        math(String.raw`K(P_0):=\max\bigl\{k\ge0:\ j^{*}\ell\ge(\ell-1)\ell^{k}\bigr\}`),
        " (this is always defined, and ",
        math(String.raw`j^{*}\ge1`),
        " gives ",
        math(String.raw`K\ge0`),
        "). Let further ",
        math(String.raw`\lambda(P_0)`),
        " be the least natural number with ",
        math(String.raw`\ell^{\lambda}\ge e_{j^*}+1`),
        " (it is determined by comparing powers of ",
        math(String.raw`\ell`),
        " one after another, so no real logarithm is involved; its value equals ",
        math(String.raw`\lceil\log_\ell(e_{j^*}+1)\rceil`),
        "). On a layer of depth ",
        math(String.raw`k>K`),
        " one has, as soon as ",
        math(String.raw`M\ge\max\bigl(R'+k,\ k+\lambda\bigr)`),
        ",",
      ]),
      displayMath(String.raw`\hat\theta_M(P)=\theta(P)=e_{j^*}+j^{*}\ell^{\,M-k},`),
      paragraph(["that is,"]),
      displayMath(
        String.raw`\Lambda_k=\frac{j^{*}}{\varphi(\ell^{k})},\qquad\theta^\sharp_k=e_{j^*}`,
      ),
      paragraph([
        "(here ",
        math(String.raw`R'(P_0)`),
        " is the smallest level at or above the ",
        ref("paper_prop_K"),
        " (K5) quantity ",
        math(String.raw`r_0`),
        " at which the ",
        math(String.raw`\mathrm{argmin}`),
        " becomes the single index ",
        math(String.raw`j^{*}`),
        "). In particular ",
        math(String.raw`K=0`),
        " is equivalent to ",
        math(String.raw`j^{*}\le\ell-2`),
        ", and for ",
        math(String.raw`\ell=2`),
        " one has ",
        math(String.raw`j^{*}\ge1=\ell-1`),
        ", so necessarily ",
        math(String.raw`K\ge1`),
        ". ",
        "**Thus saturation is not a phenomenon peculiar to the 2-adic towers; this is what lies behind it.** ",
        "The saturation that ",
        ref("paper_prop_G_ell2"),
        " exhibited as peculiar to ",
        math(String.raw`\ell=2`),
        " is merely a consequence of ",
        math(String.raw`\ell-1`),
        " being as small as possible there. The same thing happens for odd ",
        math(String.raw`\ell`),
        " in towers with ",
        math(String.raw`j^{*}\ge\ell-1`),
        ". Note that ",
        math(String.raw`K`),
        " is an upper bound: it does not assert that the layers with ",
        math(String.raw`k\le K`),
        " are actually saturated (by (U1a) below, taking ",
        math(String.raw`K`),
        " larger than necessary leaves the five coefficients unchanged).",
      ]),
      paragraph([
        "**(M3 A closed form for the level sums)** Put ",
        math(String.raw`r^\sharp:=\max\bigl(r_0,\ \max_{P_0}R'(P_0)\bigr)`),
        " and ",
        math(String.raw`U:=\mathbb{P}^1(\mathbb{Z}_\ell)\setminus\bigcup_{P_0\in S_\infty}B(P_0,\ell^{-r^\sharp})`),
        ". Then ",
        math(String.raw`\theta`),
        " is bounded on ",
        math(String.raw`U`),
        ", and a bound for it can be computed from the coefficients of ",
        math(String.raw`D`),
        ". Write ",
        math(String.raw`\theta^{\max}_U`),
        " for the maximum, take a level ",
        math(String.raw`L\ge r^\sharp`),
        " with ",
        math(String.raw`\ell^{L}\ge\theta^{\max}_U`),
        ", and set",
      ]),
      displayMath(
        String.raw`A_{\mathrm{gen}}:=\frac{1}{\ell^{L}}\sum_{P\in U\cap\mathbb{P}^1(\mathbb{Z}/\ell^{L})}\theta(P)\ \in\mathbb{Q}`,
      ),
      paragraph([
        "(this does not depend on the choice of ",
        math(String.raw`L`),
        ", since passing from ",
        math(String.raw`L\to L+1`),
        " splits every fibre uniformly into ",
        math(String.raw`\ell`),
        " points). Then there is an explicit ",
        math(String.raw`M^{*}`),
        ", given by (M5) below, such that for ",
        math(String.raw`M\ge M^{*}`),
        " one has",
      ]),
      displayMath(String.raw`\Theta_M=\alpha\,M\ell^{M}+\beta\,\ell^{M}+\gamma`),
      paragraph([
        "where",
      ]),
      displayMath(String.raw`\alpha=\frac{\ell-1}{\ell}\sum_{P_0\in S_\infty}j^{*}(P_0),`),
      displayMath(
        String.raw`\beta=A_{\mathrm{gen}}+\sum_{P_0\in S_\infty}\Bigl[\frac{e_{j^*}}{\ell^{\,r^\sharp}}
        -\frac{(\ell-1)\,j^{*}\,(K+r^\sharp)}{\ell}
        +\frac{\ell-1}{\ell}\Bigl(\Lambda_0+\sum_{k=1}^{K}\varphi(\ell^{k})\Lambda_k\Bigr)\Bigr],`,
      ),
      displayMath(
        String.raw`\gamma=\sum_{P_0\in S_\infty}\Bigl[-e_{j^*}\,\ell^{K}
        +\theta^\sharp_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\theta^\sharp_k\Bigr].`,
      ),
      paragraph([
        "**(M4 The five coefficients)** Consequently, for ",
        math(String.raw`n\ge M^{*}-1`),
        " the five-coefficient form of ",
        ref("paper_prop_J"),
        " (J4), namely ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=a\,\ell^{2n}+b\,n\ell^{n}+c\,\ell^{n}+d\,n+e`),
        ", holds with",
      ]),
      displayMath(
        String.raw`a=\mu,\qquad b=\frac{\ell}{\ell-1}\,\alpha=\sum_{P_0\in S_\infty}j^{*}(P_0),\qquad
        c=\frac{\ell}{\ell-1}\,\beta-\frac{\ell}{(\ell-1)^2}\,\alpha,\qquad d=\gamma-2,`,
      ),
      displayMath(
        String.raw`e=v_\ell(\kappa(X))-\mu
        +\Bigl[\sum_{M=1}^{M^{*}-1}\Theta_M-\alpha\,\mathcal{S}_1(M^{*}\!-\!1)-\beta\,\mathcal{S}_0(M^{*}\!-\!1)-\gamma(M^{*}\!-\!1)\Bigr]
        +\frac{\ell}{(\ell-1)^2}\alpha-\frac{\ell}{\ell-1}\beta,`,
      ),
      paragraph([
        "where ",
        math(String.raw`\mathcal{S}_1(N)=\sum_{M=1}^{N}M\ell^{M}`),
        " and ",
        math(String.raw`\mathcal{S}_0(N)=\sum_{M=1}^{N}\ell^{M}`),
        ". ",
        "**Thus all five coefficients are determined by a finite computation from the coefficients " +
          "of the spectral polynomial.** ",
        "In particular ",
        math(String.raw`b=\sum j^{*}`),
        " follows from ",
        ref("paper_prop_J"),
        " (J4) **without** the hypothesis that the latter left open, namely the uniqueness of the index realising ",
        math(String.raw`\hat\theta`),
        ". The reason is that uniqueness is **proved**, rather than assumed, both on the saturated ",
        "layers (M1) and on the unsaturated ones (M2).",
      ]),
      paragraph([
        "**(M5 An explicit form for the level at which the closed form starts)** It suffices to take for ",
        math(String.raw`M`),
        " the smallest value ",
        math(String.raw`M^{*}`),
        " satisfying all of the following. Each of them is a finite computation from the coefficients of ",
        math(String.raw`D`),
        ".",
      ]),
      list([
        [
          math(String.raw`M\ge L`),
          " (",
          math(String.raw`A_{\mathrm{gen}}`),
          " factors through this level).",
        ],
        [
          math(String.raw`M\ge r^\sharp+\max_{P_0}K(P_0)`),
          ". **This condition says that the closed form for the unsaturated layers holds; it does " +
            "not say that those layers are non-empty.** For ",
          math(String.raw`M=r^\sharp+\max K`),
          " the unsaturated layers are empty, but the identity ",
          math(String.raw`\sum_{s=K+1}^{M-r^\sharp}\varphi(\ell^{s})=\ell^{M-r^\sharp}-\ell^{K}`),
          " still holds, both sides being ",
          math(String.raw`0`),
          ", so the closed form remains correct. It first fails for ",
          math(String.raw`M<r^\sharp+\max K`),
          ".",
        ],
        [
          math(String.raw`\varphi(\ell^{M})>\theta^{\max}_U-2`),
          " (the contribution of the generic region ",
          math(String.raw`U`),
          ").",
        ],
        [
          "For every ",
          math(String.raw`P_0`),
          " and every ",
          math(String.raw`k\le K`),
          ": ",
          math(String.raw`M\ge2k`),
          " and ",
          math(String.raw`\varphi(\ell^{M})>(\theta^\sharp_k-m^\sharp_k)\varphi(\ell^{k})`),
          " (see (M1); on a layer with ",
          math(String.raw`m^\sharp_k=\infty`),
          " this inequality is satisfied automatically).",
        ],
        [
          "For every ",
          math(String.raw`P_0`),
          ": ",
          math(String.raw`e_{j^*}+j^{*}\ell^{\,M-K-1}-2<\varphi(\ell^{M})`),
          " (the outermost layer ",
          math(String.raw`k=K+1`),
          " of (M2); for ",
          math(String.raw`M=r^\sharp+\max K`),
          " that layer is absent and the condition is vacuous).",
        ],
      ]),
      paragraph([
        "**(M6 The case with no point of infinite vanishing depth)** Whether ",
        math(String.raw`S_\infty=\emptyset`),
        " can be decided by ",
        ref("paper_prop_K"),
        " (K3). In that case ",
        math(String.raw`\alpha=\gamma=0`),
        " and ",
        math(String.raw`\beta=A_{\mathrm{gen}}`),
        ", so that",
      ]),
      displayMath(
        String.raw`a=\mu,\quad b=0,\quad c=\frac{\ell}{\ell-1}A_{\mathrm{gen}}=\frac{\Theta_L}{\varphi(\ell^{L})},\quad d=-2,`,
      ),
      displayMath(
        String.raw`e=v_\ell(\kappa(X))-\mu+\sum_{M=1}^{M^{*}-1}\Theta_M
        -A_{\mathrm{gen}}\Bigl(\mathcal{S}_0(M^{*}\!-\!1)+\frac{\ell}{\ell-1}\Bigr).`,
      ),
      paragraph([
        "**Explicit statement that the real field is not used**: all we use is (a) the valuation of ",
        math(String.raw`\mathcal{O}_k=\mathbb{Z}[\zeta_{\ell^{k}}]`),
        " — the ring of integers of a countable algebraic extension of ",
        math(String.raw`\mathbb{Q}`),
        " — at its unique prime above ",
        math(String.raw`\ell`),
        " (whose value is computed by taking ",
        math(String.raw`N_{\mathbb{Q}(\eta)/\mathbb{Q}}`),
        " and reducing to the valuation of a single integer in ",
        math(String.raw`\mathbb{Z}`),
        ", namely the one adic to ",
        math(String.raw`\ell`),
        "), (b) finite sums over ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        ", and (c) arithmetic of rational numbers in ",
        math(String.raw`\mathbb{Q}`),
        ". Moreover the ",
        math(String.raw`\lambda`),
        " of (M2) needs no real logarithm either: it is determined by comparing powers of ",
        math(String.raw`\ell`),
        " alone. **Hence this proposition never escapes to the real field.**",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "**The scope is limited to rank two, the hypothesis (H), and voltage graphs of Laplacian " +
            "determinant type.** In symbols this is ",
          math(String.raw`d=2`),
          " (",
          math(String.raw`\Gamma\cong\mathbb{Z}_\ell^2`),
          "), and the objects considered are only those of the form ",
          math(String.raw`\det L`),
          ". The twisting idea of (M1) still makes sense in rank three and higher, but the " +
            "stratification of ",
          math(String.raw`\mathbb{P}^{d-1}`),
          " is not as simple as that of ",
          math(String.raw`\mathbb{P}^1`),
          ", so the argument does not carry over as it stands.",
        ],
        [
          math(String.raw`M^{*}`),
          " is a sufficient condition and **no sharpness is claimed**. In experiments the closed form already holds for many towers from values of ",
          math(String.raw`M^{*}`),
          " smaller than the bound, that is from smaller ",
          math(String.raw`M`),
          ". Hence agreement or disagreement at ",
          math(String.raw`n<M^{*}-1`),
          " must not be used as a verification of this proposition.",
        ],
        [
          "No formula is given that reads ",
          math(String.raw`(\Lambda_k,\theta^\sharp_k)`),
          " off the combinatorial data of ",
          math(String.raw`\bar{\tilde E}\in\mathbb{F}_\ell[z,w]`),
          "; **none is given** because, as ",
          ref("paper_prop_U"),
          " shows, none can exist. What is given is a formula in terms of a finite computation from the coefficients of ",
          math(String.raw`D`),
          ".",
        ],
        [
          "The general statement ",
          math(String.raw`a\le\mu`),
          " remains borrowed from Cuoco–Monsky (within the scope of this proposition ",
          math(String.raw`a=\mu`),
          " comes directly from the expansion in ",
          ref("paper_prop_J"),
          ", so what is borrowed is only the general statement).",
        ],
        [
          "**No novelty is claimed.** The identities ",
          math(String.raw`a=\mu`),
          " and ",
          math(String.raw`b=\sum j^{*}`),
          " are Definitions 1.1 and 1.2 together with Theorem 1.7 of Cuoco–Monsky, *Class numbers in ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions*, Math. Ann. **255** (1981), 235–258, and the attribution is already recorded in ",
          ref("paper_prop_K"),
          " (K6). As for ",
          math(String.raw`c`),
          ", Monsky, *Fine estimates for the growth of ",
          math(String.raw`e_n`),
          " in ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions* (1989) writes of the constant ",
          math(String.raw`\alpha^{*}`),
          " occupying the same position that there is no easy description of ",
          math(String.raw`\alpha^{*}`),
          ", and proves only its existence and, for ",
          math(String.raw`d=2`),
          ", its rationality (Introduction, p.309; we obtained and read the original text). ",
          "The present proposition makes that quantity explicit in the restricted setting above, but " +
            "**the literature search is not exhaustive**. ",
          "In particular the operation in (M1) — twisting by a character of order a power of ",
          math(String.raw`\ell^{k}`),
          " to decompose, and taking the valuation of each component — is standard in Iwasawa theory, " +
            "so it is quite possible that the same computation has already been carried out for concrete objects such as those of ",
          math(String.raw`\det L`),
          " type.",
        ],
        [
          "The coefficient ",
          math(String.raw`d`),
          " of ",
          math(String.raw`n`),
          " and the constant term ",
          math(String.raw`e`),
          " lie outside Monsky's assertion to begin with: for ",
          math(String.raw`d=2`),
          " the error term of that paper is ",
          math(String.raw`O(n)`),
          ", so it does not reach them.",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "**We state, before using them, every fact that the proof takes from outside.** There are exactly five.",
      ]),
      list([
        [
          "**(a Valuations and the decomposition by level)** Parts (R4) and (R5) of ",
          ref("paper_prop_R"),
          ". For a primitive ",
          math(String.raw`\ell^{M}`),
          "-th root of unity ",
          math(String.raw`\zeta`),
          " and a point ",
          math(String.raw`P=(a{:}b)`),
          ", take the character ",
          math(String.raw`\omega_P:z\mapsto\zeta^{a},\ w\mapsto\zeta^{b}`),
          ". Then ",
          math(String.raw`\hat\theta_M(P)=\varphi(\ell^{M})\,v_\ell\bigl(\tilde E(\omega_P)\bigr)`),
          ", and both ",
          math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
          " and ",
          math(String.raw`\mathrm{ord}_\ell(\kappa_n)=v_\ell(\kappa(X))-2n+\mu(\ell^{2n}-1)+\Sigma_n`),
          " hold **with no hypothesis whatsoever**.",
        ],
        [
          "**(b The digit theorem)** Part (J1) of ",
          ref("paper_prop_J"),
          ". If ",
          math(String.raw`\theta(P)\le\ell^{L}`),
          " then ",
          math(String.raw`\theta(P)`),
          " is determined by ",
          math(String.raw`P\bmod\ell^{L}`),
          " alone. The step at exactly ",
          math(String.raw`m=\ell^{L}`),
          " requires ",
          math(String.raw`\bar A_1\equiv0`),
          ", which follows unconditionally from ",
          math(String.raw`D(z,w)=D(z^{-1},w^{-1})`),
          " (part (G6) of ",
          ref("paper_prop_G"),
          "). **This is the only non-trivial point that the present proposition borrows from outside.**",
        ],
        [
          "**(c An effective upper bound for the vanishing depth)** Part (R3) of ",
          ref("paper_prop_R"),
          ". At a point where ",
          math(String.raw`\theta(P)<\infty`),
          " one has ",
          math(String.raw`\theta(P)\le\ell^{\mathrm{sep}(P)}-1`),
          ", and the bound is computable, via the valuations of the difference vectors of ",
          math(String.raw`S=\mathrm{supp}(\bar{\tilde E})`),
          ", from the coefficients of ",
          math(String.raw`D`),
          " alone. **This is an effective bound, not a mere appeal to compactness.**",
        ],
        [
          "**(d A description of the set of points of infinite vanishing depth)** The set in question is ",
          math(String.raw`S_\infty`),
          ". The facts used are part (J6) of ",
          ref("paper_prop_J"),
          " together with parts (K2), (K4) and (K5) of ",
          ref("paper_prop_K"),
          ". The set ",
          math(String.raw`S_\infty`),
          " is finite and contained in ",
          math(String.raw`\mathbb{P}^1(\mathbb{Q})`),
          ", one has ",
          math(String.raw`P=\iota([u])\in S_\infty\iff(\chi^{u^\perp}-1)\mid\bar{\tilde E}`),
          " and ",
          math(String.raw`j^{*}(P)=m_u`),
          " (the multiplicity). Moreover, for ",
          math(String.raw`r\ge r_0`),
          " the balls around distinct base points ",
          math(String.raw`P_0`),
          ", of radius ",
          math(String.raw`\ell^{-r}`),
          ", are pairwise disjoint, and the minimum of ",
          math(String.raw`\Lambda(r)=\min_j\bigl(e_j+j\ell^{r}\bigr)`),
          " is attained at the single index ",
          math(String.raw`j=j^{*}`),
          ".",
        ],
        [
          "**(e The lowest degree)** Part (G6) of ",
          ref("paper_prop_G"),
          " gives that, for ",
          math(String.raw`\Phi_P\in\mathbb{Z}[x]`),
          ", the lowest degree ",
          math(String.raw`m_1`),
          " satisfies ",
          math(String.raw`m_1\ge2`),
          ".",
        ],
      ]),
      paragraph([
        "**We also record the principle that is used over and over: equality in a non-archimedean estimate.** In a field with a valuation ",
        math(String.raw`v`),
        ", if ",
        math(String.raw`\alpha=\sum_m\alpha_m`),
        " and the minimum of ",
        math(String.raw`v(\alpha_m)`),
        " is attained at exactly one index ",
        math(String.raw`m`),
        ", then ",
        math(String.raw`v(\alpha)=\min_m v(\alpha_m)`),
        " (when the minimum is attained more than once, only ",
        math(String.raw`\ge`),
        " can be asserted). **Every step below reduces to showing that the minimising index is unique.**",
      ]),
      paragraph([
        "**Proof of (M1); first, a description of the layers.** Since ",
        math(String.raw`\det(u,\mathbf{e})=1`),
        ", the pair ",
        math(String.raw`\{u,\mathbf{e}\}`),
        " is a basis of ",
        math(String.raw`\mathbb{Z}^2`),
        ". If ",
        math(String.raw`g`),
        " is a primitive ",
        math(String.raw`\ell^{M}`),
        "-th root of unity, then ",
        math(String.raw`\eta:=g^{\ell^{M-k}}`),
        " is a primitive ",
        math(String.raw`\ell^{k}`),
        "-th root of unity, and for ",
        math(String.raw`(a,b)=u+\ell^{M-k}t\,\mathbf{e}`),
        " one has",
      ]),
      displayMath(
        String.raw`\bigl(g^{a},g^{b}\bigr)=\bigl(g^{u_1}\eta^{t\mathbf{e}_1},\ g^{u_2}\eta^{t\mathbf{e}_2}\bigr)`,
      ),
      paragraph([
        " (and when ",
        math(String.raw`M\ge2k`),
        " distinct values of ",
        math(String.raw`t`),
        " give distinct projective points, so the number of points in a layer is as asserted). Writing ",
        math(String.raw`\pi:=g-1`),
        " and reading ",
        math(String.raw`g^{u_i}=(1+\pi)^{u_i}`),
        ", the value of ",
        math(String.raw`\tilde E`),
        " at this point is exactly ",
        math(String.raw`\Phi^{[k]}_{u}(\pi)`),
        ".",
      ]),
      paragraph([
        "**1 (the data are common to all points of a layer).** The map ",
        math(String.raw`\sigma_t:\eta\mapsto\eta^{t}`),
        " is an element of ",
        math(String.raw`\mathrm{Gal}\bigl(\mathbb{Q}(\eta)/\mathbb{Q}\bigr)`),
        " which fixes ",
        math(String.raw`\mathbb{Z}`),
        " and carries the coefficients of ",
        math(String.raw`\Phi^{[k]}_{u}`),
        " to those of the version indexed by ",
        math(String.raw`t`),
        ". There is a single place above ",
        math(String.raw`\ell`),
        ", so ",
        math(String.raw`v_\ell`),
        " is Galois invariant and the sequence ",
        math(String.raw`\bigl(v_\ell(A^{[k]}_m)\bigr)_m`),
        " does not depend on ",
        math(String.raw`t`),
        ". Hence neither do ",
        math(String.raw`\Lambda_k,\theta^\sharp_k,m^\sharp_k`),
        ".",
      ]),
      paragraph([
        "**2 (the value under the stated condition).** The prime ",
        math(String.raw`\ell`),
        " is totally ramified in ",
        math(String.raw`\mathbb{Q}(g)/\mathbb{Q}`),
        " and ",
        math(String.raw`\pi`),
        " is a uniformiser, so ",
        math(String.raw`v_\ell(\pi)=1/\varphi(\ell^{M})`),
        ". By (a),",
      ]),
      displayMath(
        String.raw`\hat\theta_M(P)=\varphi(\ell^{M})\,v_\ell\Bigl(\sum_m A^{[k]}_m\pi^{m}\Bigr),
        \qquad
        \varphi(\ell^{M})\,v_\ell\bigl(A^{[k]}_m\pi^{m}\bigr)=\varphi(\ell^{M})\,v_\ell\bigl(A^{[k]}_m\bigr)+m`,
      ),
      paragraph([
        ", whose right-hand side takes values in ",
        math(String.raw`\mathbb{Z}\cup\{\infty\}`),
        ". We show that the minimum is attained at the single index ",
        math(String.raw`m=\theta^\sharp_k`),
        ".",
      ]),
      list([
        [
          "At ",
          math(String.raw`m=\theta^\sharp_k`),
          " the value is ",
          math(String.raw`\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
          ".",
        ],
        [
          "For ",
          math(String.raw`m>\theta^\sharp_k`),
          " we have ",
          math(String.raw`v_\ell(A^{[k]}_m)\ge\Lambda_k`),
          ", so the value is ",
          math(String.raw`\ge\varphi(\ell^{M})\Lambda_k+m>\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
          ".",
        ],
        [
          "For ",
          math(String.raw`m<\theta^\sharp_k`),
          " with ",
          math(String.raw`A^{[k]}_m=0`),
          " the value is ",
          math(String.raw`\infty`),
          ", so such an index cannot take the minimum.",
        ],
        [
          "For ",
          math(String.raw`m<\theta^\sharp_k`),
          " with ",
          math(String.raw`A^{[k]}_m\neq0`),
          ", the values of ",
          math(String.raw`v_\ell`),
          " lie in ",
          math(String.raw`\tfrac{1}{\varphi(\ell^{k})}\mathbb{Z}`),
          " and are strictly larger than ",
          math(String.raw`\Lambda_k`),
          ", whence ",
          math(String.raw`v_\ell(A^{[k]}_m)\ge\Lambda_k+\tfrac{1}{\varphi(\ell^{k})}`),
          " and the value is ",
          math(String.raw`\ge\varphi(\ell^{M})\Lambda_k+\tfrac{\varphi(\ell^{M})}{\varphi(\ell^{k})}+m^\sharp_k`),
          ". That this exceeds ",
          math(String.raw`\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
          " strictly is equivalent to",
        ],
      ]),
      displayMath(
        String.raw`\frac{\varphi(\ell^{M})}{\varphi(\ell^{k})}+m^\sharp_k\ >\ \theta^\sharp_k`,
      ),
      paragraph([
        ". **This sum form is the form in which the stated condition is actually used.** If ",
        math(String.raw`m^\sharp_k`),
        " is finite, rearranging gives ",
        math(String.raw`\varphi(\ell^{M})>\bigl(\theta^\sharp_k-m^\sharp_k\bigr)\varphi(\ell^{k})`),
        "; if ",
        math(String.raw`m^\sharp_k=\infty`),
        " the left-hand side is ",
        math(String.raw`\infty`),
        " and the inequality holds automatically — indeed the fourth case above is then empty. The convention stated in the assertion, namely to read the difference as ",
        math(String.raw`-\infty`),
        ", says the same thing in the difference form.",
      ]),
      paragraph([
        "The minimising index being unique, the principle above gives, at every point of the layer of depth ",
        math(String.raw`k`),
        ", the equality ",
        math(String.raw`\hat\theta_M(P)=\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
        ".",
      ]),
      paragraph([
        "**3 (integrality).** Since the values of ",
        math(String.raw`v_\ell`),
        " lie in ",
        math(String.raw`\tfrac{1}{\varphi(\ell^{k})}\mathbb{Z}`),
        ", we get ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k\in\mathbb{Z}`),
        ". For the lower bound, when ",
        math(String.raw`k\ge1`),
        " the maximal ideal of ",
        math(String.raw`\mathcal{O}_k`),
        " is ",
        math(String.raw`(\eta-1)`),
        " with residue field ",
        math(String.raw`\mathbb{F}_\ell`),
        ", and the residue map induces ",
        math(String.raw`\eta\mapsto1`),
        ", so the residue of ",
        math(String.raw`A^{[k]}_m`),
        " equals the coefficient of ",
        math(String.raw`\overline{\Phi_u}`),
        " in degree ",
        math(String.raw`m`),
        ", namely ",
        math(String.raw`\bar A_m(u)`),
        ". By (d), the condition ",
        math(String.raw`P_0\in S_\infty`),
        " means ",
        math(String.raw`\overline{\Phi_u}\equiv0`),
        ", that is ",
        math(String.raw`m`),
        " ranges over all indices with ",
        math(String.raw`\bar A_m(u)=0`),
        ", whence ",
        math(String.raw`v_\ell(A^{[k]}_m)\ge\tfrac{1}{\varphi(\ell^{k})}`),
        " and therefore ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k\ge1`),
        ". For ",
        math(String.raw`k=0`),
        " we have ",
        math(String.raw`\mathcal{O}_0=\mathbb{Z}`),
        " and ",
        math(String.raw`\varphi(\ell^{0})=1`),
        ", and the same argument gives ",
        math(String.raw`v_\ell(A_m)\ge1`),
        ".",
      ]),
      paragraph([
        "**Proof of (M2).** The condition ",
        math(String.raw`k>K`),
        " says ",
        math(String.raw`j^{*}\ell<(\ell-1)\ell^{k}`),
        ", that is (both sides being integers) ",
        math(String.raw`(\ell-1)\ell^{k-1}-j^{*}\ge1`),
        ". Since ",
        math(String.raw`r=M-k\ge R'`),
        ", part (d) gives that the minimum of ",
        math(String.raw`\Lambda(r)`),
        " is attained at the single index ",
        math(String.raw`j=j^{*}`),
        ", so that ",
        math(String.raw`\theta(P)=e_{j^*}+j^{*}\ell^{\,M-k}`),
        ". The coefficients of ",
        math(String.raw`\Phi_P\in\mathbb{Z}[x]`),
        " satisfy, for ",
        math(String.raw`m<\theta(P)`),
        ", the bound ",
        math(String.raw`v_\ell(A_m)\ge1`),
        ", and at ",
        math(String.raw`m=\theta(P)`),
        " the equality ",
        math(String.raw`v_\ell(A_\theta)=0`),
        " (by the definition of ",
        math(String.raw`\theta`),
        "). A sufficient condition for ",
        math(String.raw`\min_m\bigl(\varphi(\ell^{M})v_\ell(A_m)+m\bigr)`),
        " to be attained at the single index ",
        math(String.raw`m=\theta(P)`),
        " is ",
        math(String.raw`\theta(P)-m_1<\varphi(\ell^{M})`),
        ", where we use ",
        math(String.raw`m_1\ge2`),
        " from (e), and",
      ]),
      displayMath(
        String.raw`\varphi(\ell^{M})-\theta(P)+m_1\ \ge\ (\ell-1)\ell^{M-1}-j^{*}\ell^{\,M-k}-e_{j^*}+2
        =\ell^{\,M-k}\bigl[(\ell-1)\ell^{k-1}-j^{*}\bigr]-e_{j^*}+2\ \ge\ \ell^{\,M-k}-e_{j^*}+2`,
      ),
      paragraph([
        ", so the right-hand side is positive as soon as ",
        math(String.raw`\ell^{\,M-k}>e_{j^*}`),
        ". Both ",
        math(String.raw`\ell^{\,M-k}`),
        " and ",
        math(String.raw`e_{j^*}`),
        " are integers, so ",
        math(String.raw`\ell^{\,M-k}>e_{j^*}\iff\ell^{\,M-k}\ge e_{j^*}+1`),
        ", and by the definition of ",
        math(String.raw`\lambda`),
        " this is equivalent to ",
        math(String.raw`M\ge k+\lambda`),
        ". **Deciding it is a comparison between an integer and a power of the prime; no real logarithm is involved.** The prime in question is ",
        math(String.raw`\ell`),
        ". Hence ",
        math(String.raw`\hat\theta_M(P)=\theta(P)`),
        ".",
      ]),
      paragraph([
        "Now compare this with the value from (M1) at two or more levels ",
        math(String.raw`M`),
        " at which both hold. The identity ",
        math(String.raw`\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k=e_{j^*}+j^{*}\ell^{\,M-k}`),
        " then holds identically in ",
        math(String.raw`M`),
        ", so comparing the coefficients of ",
        math(String.raw`\ell^{M}`),
        " gives ",
        math(String.raw`\tfrac{\ell-1}{\ell}\Lambda_k=j^{*}\ell^{-k}`),
        ", that is ",
        math(String.raw`\Lambda_k=\tfrac{j^{*}\ell^{1-k}}{\ell-1}=\tfrac{j^{*}}{\varphi(\ell^{k})}`),
        " (using ",
        math(String.raw`\varphi(\ell^{k})=(\ell-1)\ell^{k-1}`),
        "), and comparing constant terms gives ",
        math(String.raw`\theta^\sharp_k=e_{j^*}`),
        ". Finally ",
        math(String.raw`K=0\iff j^{*}\ell<(\ell-1)\ell\iff j^{*}\le\ell-2`),
        ", and for ",
        math(String.raw`\ell=2`),
        " one has ",
        math(String.raw`j^{*}\ge1=\ell-1`),
        ", hence ",
        math(String.raw`K\ge1`),
        ".",
      ]),
      paragraph([
        "**Proof of (M3); first, boundedness on the generic region.** The region is ",
        math(String.raw`U`),
        " and the quantity to be bounded there is ",
        math(String.raw`\theta`),
        ". Since ",
        math(String.raw`r^\sharp\ge r_0`),
        ", part (d) gives that the balls ",
        math(String.raw`B(P_0,\ell^{-r^\sharp})`),
        " are pairwise disjoint, and on ",
        math(String.raw`U`),
        " we have ",
        math(String.raw`\theta<\infty`),
        ". Applying the effective bound (c) on ",
        math(String.raw`U`),
        " gives ",
        math(String.raw`\theta\le\theta^{\max}_U<\infty`),
        " with a bound computable from the coefficients of ",
        math(String.raw`D`),
        ". If ",
        math(String.raw`L\ge r^\sharp`),
        " is a level with ",
        math(String.raw`\ell^{L}\ge\theta^{\max}_U`),
        ", then by (b) the restriction ",
        math(String.raw`\theta|_U`),
        " factors through ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        ".",
      ]),
      paragraph([
        "Split ",
        math(String.raw`M\ge M^{*}`),
        " — more precisely, split ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        " for such a level — into three parts. They are pairwise disjoint and cover everything: outside ",
        math(String.raw`U`),
        " one has the balls around the points of ",
        math(String.raw`S_\infty`),
        ", of radius ",
        math(String.raw`\ell^{-r^\sharp}`),
        ", and inside such a ball the points are stratified by their distance to the base point.",
      ]),
      paragraph([
        "**(a) The generic part.** On ",
        math(String.raw`U`),
        " we have ",
        math(String.raw`\theta\le\theta^{\max}_U`),
        ", so by (e) ",
        math(String.raw`\theta-m_1\le\theta^{\max}_U-2<\varphi(\ell^{M})`),
        " (the third condition of (M5)); the same estimate as in the proof of (M2) makes the minimising index unique and gives ",
        math(String.raw`\hat\theta_M=\theta`),
        ". The fibres of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})\to\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        " have uniform size ",
        math(String.raw`\ell^{\,M-L}`),
        ", so this part contributes ",
        math(String.raw`\ell^{\,M-L}\cdot\ell^{L}A_{\mathrm{gen}}=A_{\mathrm{gen}}\ell^{M}`),
        ". **Neither a term in the first shape nor a constant term arises here**, that is, no term ",
        math(String.raw`M\ell^{M}`),
        " and no constant. The same computation, applied to ",
        math(String.raw`L\to L+1`),
        ", shows that the fibres split uniformly into ",
        math(String.raw`\ell`),
        " pieces, whence ",
        math(String.raw`A_{\mathrm{gen}}`),
        " does not depend on ",
        math(String.raw`L`),
        ".",
      ]),
      paragraph([
        "**(b) For each base point, the layers of intermediate depth.** For a base point ",
        math(String.raw`P_0`),
        " and depths ",
        math(String.raw`K+1\le k\le M-r^\sharp`),
        ", part (M2) gives that the layer is unsaturated, so ",
        math(String.raw`\hat\theta_M=\theta=e_{j^*}+j^{*}\ell^{r}`),
        " with ",
        math(String.raw`r=M-k`),
        ", and the layer has ",
        math(String.raw`\varphi(\ell^{\,M-r})`),
        " points. Using that ",
        math(String.raw`\varphi(\ell^{\,M-r})\ell^{r}=\ell^{M}-\ell^{M-1}=\varphi(\ell^{M})`),
        " is independent of ",
        math(String.raw`r`),
        ", together with the telescoping identity ",
        math(String.raw`\sum_{s=K+1}^{M-r^\sharp}\varphi(\ell^{s})=\ell^{\,M-r^\sharp}-\ell^{K}`),
        ", one obtains",
      ]),
      displayMath(
        String.raw`\sum_{r=r^\sharp}^{M-K-1}\varphi(\ell^{\,M-r})\bigl(e_{j^*}+j^{*}\ell^{r}\bigr)
        =e_{j^*}\bigl(\ell^{\,M-r^\sharp}-\ell^{K}\bigr)+j^{*}\varphi(\ell^{M})\bigl(M-K-r^\sharp\bigr)`,
      ),
      paragraph([
        ". The coefficient of ",
        math(String.raw`M\ell^{M}`),
        " is ",
        math(String.raw`j^{*}\tfrac{\ell-1}{\ell}`),
        ", the coefficient of ",
        math(String.raw`\ell^{M}`),
        " is ",
        math(String.raw`e_{j^*}\ell^{-r^\sharp}-j^{*}(K+r^\sharp)\tfrac{\ell-1}{\ell}`),
        ", and the constant term is ",
        math(String.raw`-e_{j^*}\ell^{K}`),
        ". **This identity also holds when the range of layers is empty**, that is when ",
        math(String.raw`M=r^\sharp+K`),
        ", both sides then being ",
        math(String.raw`0`),
        ".",
      ]),
      paragraph([
        "**(c) For each base point, the layers of small depth.** For a base point ",
        math(String.raw`P_0`),
        " and depths ",
        math(String.raw`0\le k\le K`),
        ", part (M1) gives ",
        math(String.raw`\hat\theta_M=\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
        ", the number of points being ",
        math(String.raw`k=0`),
        " for a single point, namely ",
        math(String.raw`1`),
        ", and for ",
        math(String.raw`k\ge1`),
        " equal to ",
        math(String.raw`\varphi(\ell^{k})`),
        ". Hence this part contributes",
      ]),
      displayMath(
        String.raw`\varphi(\ell^{M})\Bigl(\Lambda_0+\sum_{k=1}^{K}\varphi(\ell^{k})\Lambda_k\Bigr)
        +\Bigl(\theta^\sharp_0+\sum_{k=1}^{K}\varphi(\ell^{k})\theta^\sharp_k\Bigr)`,
      ),
      paragraph([
        ", whose coefficient of ",
        math(String.raw`\ell^{M}`),
        " is the first bracket multiplied by ",
        math(String.raw`\tfrac{\ell-1}{\ell}`),
        " and whose constant term is the second bracket. **Here too no term of the first shape arises**, that is, no term ",
        math(String.raw`M\ell^{M}`),
        ".",
      ]),
      paragraph([
        "Adding (a), (b) and (c) gives ",
        math(String.raw`\Theta_M=\alpha M\ell^{M}+\beta\ell^{M}+\gamma`),
        " with the asserted ",
        math(String.raw`\alpha,\beta,\gamma`),
        ". No part produces a term linear in ",
        math(String.raw`M`),
        ". **This point cannot be omitted:** a term ",
        math(String.raw`\delta M`),
        " would make ",
        math(String.raw`\Sigma_n`),
        " contain ",
        math(String.raw`n^{2}`),
        ", destroying the five-coefficient shape of (M4) itself.",
      ]),
      paragraph([
        "**Proof of (M4).** The standard formulas for a geometric series and for an arithmetico-geometric series give",
      ]),
      displayMath(
        String.raw`\mathcal{S}_1(n)=\frac{\ell-(n+1)\ell^{n+1}+n\ell^{n+2}}{(\ell-1)^{2}},
        \qquad
        \mathcal{S}_0(n)=\frac{\ell^{n+1}-\ell}{\ell-1}`,
      ),
      paragraph([
        ". In the numerator of ",
        math(String.raw`\mathcal{S}_1`),
        " the terms involving ",
        math(String.raw`n`),
        " are ",
        math(String.raw`n\ell^{n+2}-n\ell^{n+1}=n\ell^{n+1}(\ell-1)`),
        ", so in ",
        math(String.raw`\mathcal{S}_1(n)`),
        " the coefficient of ",
        math(String.raw`n\ell^{n}`),
        " is ",
        math(String.raw`\tfrac{\ell}{\ell-1}`),
        ", the coefficient of ",
        math(String.raw`\ell^{n}`),
        " is ",
        math(String.raw`-\tfrac{\ell}{(\ell-1)^{2}}`),
        ", and the constant term is ",
        math(String.raw`\tfrac{\ell}{(\ell-1)^{2}}`),
        ". Similarly, in ",
        math(String.raw`\mathcal{S}_0(n)`),
        " the coefficient of ",
        math(String.raw`\ell^{n}`),
        " is ",
        math(String.raw`\tfrac{\ell}{\ell-1}`),
        " and the constant term is ",
        math(String.raw`-\tfrac{\ell}{\ell-1}`),
        ". By (a), namely ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        ", together with (M3), for ",
        math(String.raw`n\ge M^{*}-1`),
        " we get",
      ]),
      displayMath(
        String.raw`\Sigma_n=\sum_{M=1}^{M^{*}-1}\Theta_M
        +\alpha\bigl[\mathcal{S}_1(n)-\mathcal{S}_1(M^{*}\!-\!1)\bigr]
        +\beta\bigl[\mathcal{S}_0(n)-\mathcal{S}_0(M^{*}\!-\!1)\bigr]
        +\gamma\,\bigl(n-M^{*}\!+\!1\bigr)`,
      ),
      paragraph([
        ". Substituting this into the identity ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=v_\ell(\kappa(X))-2n+\mu(\ell^{2n}-1)+\Sigma_n`),
        " of (a) and reading off the coefficients of ",
        math(String.raw`\ell^{2n},\ n\ell^{n},\ \ell^{n},\ n,\ 1`),
        " yields the asserted ",
        math(String.raw`a,b,c,d,e`),
        ": the term ",
        math(String.raw`-2n`),
        " contributes the ",
        math(String.raw`d=\gamma-2`),
        " summand ",
        math(String.raw`-2`),
        ", and ",
        math(String.raw`v_\ell(\kappa(X))-\mu`),
        " contributes the first two summands of ",
        math(String.raw`e`),
        ".",
      ]),
      paragraph([
        "**No uniqueness hypothesis on the minimising index is needed.** In parts (a) and (b) of (M3) the uniqueness was **proved** rather than assumed, and in part (c) we did not pass through the minimisation over ",
        math(String.raw`\mathbb{Z}`),
        " at all but used (M1), that is, uniqueness over ",
        math(String.raw`\mathcal{O}_k`),
        ". Hence ",
        math(String.raw`b=\tfrac{\ell}{\ell-1}\alpha=\sum_{P_0\in S_\infty}j^{*}(P_0)`),
        " holds without the hypothesis imposed in part (J4) of ",
        ref("paper_prop_J"),
        ". **Enlarging the coefficient ring resolves a tie in the minimum that does occur otherwise:** the enlargement is from ",
        math(String.raw`\mathbb{Z}`),
        " to ",
        math(String.raw`\mathcal{O}_k`),
        ", and this is the technical point of (M1).",
      ]),
      paragraph([
        "**Proof of (M5).** It suffices to see where the proof of (M3) used each condition. Condition 1 is used in part (a), where the level through which ",
        math(String.raw`\theta|_U`),
        " factors is needed; condition 3 is the inequality ",
        math(String.raw`\theta-m_1<\varphi(\ell^{M})`),
        " of part (a). Condition 2 is what makes the telescoping identity ",
        math(String.raw`\sum_{s=K+1}^{M-r^\sharp}\varphi(\ell^{s})=\ell^{\,M-r^\sharp}-\ell^{K}`),
        " of part (b) valid, and it is equivalent to ",
        math(String.raw`K\le M-r^\sharp`),
        ", that is to ",
        math(String.raw`M\ge r^\sharp+K`),
        ". Condition 4 is exactly the hypothesis under which (M1) applies; on a layer with ",
        math(String.raw`m^\sharp_k=\infty`),
        " it is automatic, as seen above, so only ",
        math(String.raw`M\ge2k`),
        " remains. Condition 5 is what makes the estimate of (M2) valid on the outermost layer ",
        math(String.raw`k=K+1`),
        " in part (b). All of them are decided by a finite computation from the coefficients of ",
        math(String.raw`D`),
        ".",
      ]),
      paragraph([
        "**Proof of (M6).** If ",
        math(String.raw`S_\infty=\emptyset`),
        " then parts (b) and (c) of (M3) are empty and ",
        math(String.raw`U=\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", so ",
        math(String.raw`\alpha=\gamma=0`),
        " and ",
        math(String.raw`\beta=A_{\mathrm{gen}}`),
        ". Choosing ",
        math(String.raw`M^{*}\le L`),
        " — that is, choosing ",
        math(String.raw`L`),
        " large enough — the argument of part (a) gives ",
        math(String.raw`\hat\theta_L=\theta`),
        ", so ",
        math(String.raw`\ell^{L}A_{\mathrm{gen}}=\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{L})}\theta(P)=\Theta_L`),
        ", and from ",
        math(String.raw`\tfrac{\ell}{\ell-1}\ell^{-L}=\tfrac{1}{\varphi(\ell^{L})}`),
        " we get ",
        math(String.raw`c=\tfrac{\ell}{\ell-1}A_{\mathrm{gen}}=\tfrac{\Theta_L}{\varphi(\ell^{L})}`),
        ". Also ",
        math(String.raw`d=\gamma-2=-2`),
        ", and ",
        math(String.raw`e`),
        " is obtained by substituting ",
        math(String.raw`\alpha=\gamma=0`),
        " and ",
        math(String.raw`\beta=A_{\mathrm{gen}}`),
        " into the formula of (M4). That the condition ",
        math(String.raw`S_\infty=\emptyset`),
        " can be decided is part (K3) of ",
        ref("paper_prop_K"),
        ".",
      ]),
    ],
  },
  {
    id: "paper_112_theorem_coefficient_layers",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 3 },
    title: {
      text:
        "Proposition U — the information hierarchy of the coefficients (which extra data are " +
        "needed), and the failure of any fixed finite precision",
    },
    labels: ["paper_prop_U"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle22_T3_coefficients_d_e"],
    statement: [
      paragraph([
        "We keep the setting and the notation of ",
        ref("paper_prop_M"),
        ". For each ",
        math(String.raw`P_0\in S_\infty`),
        " put",
      ]),
      displayMath(
        String.raw`\mathcal{L}(P_0):=\Lambda_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\Lambda_k,
        \qquad
        \mathcal{T}(P_0):=\theta^\sharp_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\theta^\sharp_k`,
      ),
      paragraph([
        "By the third item of ",
        ref("paper_prop_M"),
        " (M1) we have ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k\in\mathbb{Z}_{\ge1}`),
        ", and also ",
        math(String.raw`\theta^\sharp_k\in\mathbb{Z}_{\ge0}`),
        ", so that both ",
        math(String.raw`\mathcal{L}(P_0)`),
        " and ",
        math(String.raw`\mathcal{T}(P_0)`),
        " are non-negative integers. **It is the individual summands that are asserted to be integers, "
          + "not the valuations themselves.** ",
        math(String.raw`\Lambda_k`),
        " is itself a rational number which in general is not an integer: indeed, on the unsaturated "
          + "layers of part (M2) of ",
        ref("paper_prop_M"),
        " one has ",
        math(String.raw`\Lambda_k=j^{*}/\varphi(\ell^{k})`),
        " (for ",
        math(String.raw`k\ge1`),
        " this is in general not an integer). What is an integer is ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k`),
        " and ",
        math(String.raw`\theta^\sharp_k`),
        ". In particular the condition ",
        math(String.raw`N>\max\Lambda_k`),
        " in (U6) below compares the integer ",
        math(String.raw`N`),
        " with **that rational number**, not with ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k`),
        ".",
      ]),
      paragraph([
        "**(U1 The valuation side and the position side do different jobs)**",
      ]),
      displayMath(
        String.raw`d=\sum_{P_0\in S_\infty}\Bigl(\mathcal{T}(P_0)-e_{j^*}(P_0)\,\ell^{K(P_0)}\Bigr)-2,`,
      ),
      displayMath(
        String.raw`c=\frac{\ell}{\ell-1}A_{\mathrm{gen}}
        +\sum_{P_0\in S_\infty}\Bigl[\frac{e_{j^*}\,\ell^{\,1-r^\sharp}}{\ell-1}
        -j^{*}\Bigl(K+r^\sharp+\frac{1}{\ell-1}\Bigr)+\mathcal{L}(P_0)\Bigr].`,
      ),
      paragraph([
        "Thus the **valuation side** ",
        math(String.raw`\Lambda_k`),
        " of the twisted stage data enters only ",
        math(String.raw`c`),
        ", while the **position side** ",
        math(String.raw`\theta^\sharp_k`),
        " enters only ",
        math(String.raw`d`),
        ". In particular ",
        math(String.raw`d`),
        " is determined by the local quantities of ",
        math(String.raw`S_\infty`),
        ", namely ",
        math(String.raw`(\mathcal{T},e_{j^*},K)`),
        ", alone: neither the global average ",
        math(String.raw`A_{\mathrm{gen}}`),
        ", nor the transient at low levels, nor ",
        math(String.raw`v_\ell(\kappa(X))`),
        " is used. Hence ",
        math(String.raw`d`),
        " is **always an integer**, whereas ",
        math(String.raw`c`),
        " and ",
        math(String.raw`e`),
        " may be non-integral rationals such as ",
        math(String.raw`13/3`),
        ". If ",
        math(String.raw`S_\infty=\emptyset`),
        " the sum is empty and ",
        math(String.raw`d=-2`),
        ", in agreement with ",
        ref("paper_prop_M"),
        " (M6). ",
        "**Thus the naive expectation that the lower the degree the harder the coefficient does not hold.** The order of difficulty is",
        math(String.raw`a,b\ \prec\ d\ \prec\ c\ \prec\ e`),
        ".",
      ]),
      paragraph([
        "**(U1a One may take the saturation depth larger than necessary)** Let ",
        math(String.raw`K'\ge K(P_0)`),
        " be arbitrary, extend the sums above to ",
        math(String.raw`k\le K'`),
        " and replace ",
        math(String.raw`K`),
        " by ",
        math(String.raw`K'`),
        "; the values of ",
        math(String.raw`c,d`),
        " do not change. Indeed, for ",
        math(String.raw`K\to K+1`),
        " the layer of depth ",
        math(String.raw`K+1`),
        " is unsaturated by ",
        ref("paper_prop_M"),
        " (M2), so ",
        math(String.raw`\Lambda_{K+1}=j^{*}/\varphi(\ell^{K+1})`),
        " and ",
        math(String.raw`\theta^\sharp_{K+1}=e_{j^*}`),
        "; the increment ",
        math(String.raw`\mathcal{T}`),
        " receives, namely ",
        math(String.raw`\varphi(\ell^{K+1})e_{j^*}`),
        ", and the change ",
        math(String.raw`-e_{j^*}\ell^{K}\to-e_{j^*}\ell^{K+1}`),
        " cancel exactly (",
        math(String.raw`\varphi(\ell^{K+1})-\ell^{K+1}+\ell^{K}=0`),
        "), and likewise the increment ",
        math(String.raw`\mathcal{L}`),
        " receives, namely ",
        math(String.raw`j^{*}`),
        ", cancels against the change in ",
        math(String.raw`-j^{*}(K+r^\sharp+\frac{1}{\ell-1})`),
        ". The rest is induction.",
      ]),
      paragraph([
        "**(U2 A decomposition of the constant term)** Put ",
        math(String.raw`\Theta^{\mathrm{as}}_M:=\alpha M\ell^{M}+\beta\ell^{M}+\gamma`),
        " and define the **transient defect** by",
      ]),
      displayMath(String.raw`T_{\mathrm{def}}:=\sum_{M\ge1}\bigl(\Theta_M-\Theta^{\mathrm{as}}_M\bigr)`),
      paragraph([
        "By ",
        ref("paper_prop_M"),
        " (M3) the terms with ",
        math(String.raw`M\ge M^{*}`),
        " are ",
        math(String.raw`0`),
        ", so this is a **finite sum**, and it does not depend on the choice of ",
        math(String.raw`M^{*}`),
        " (truncating anywhere at or above ",
        math(String.raw`M^{*}`),
        " gives the same value). Then",
      ]),
      displayMath(String.raw`e=v_\ell(\kappa(X))-a-c+T_{\mathrm{def}},`),
      paragraph(["and therefore"]),
      displayMath(String.raw`c+e=v_\ell(\kappa(X))-a+T_{\mathrm{def}}.`),
      paragraph([
        "Moreover, putting ",
        math(String.raw`\delta_M:=\Theta_M-\Theta^{\mathrm{as}}_M`),
        " and ",
        math(String.raw`S(n):=\sum_{M=1}^{n}\delta_M`),
        " (so that ",
        math(String.raw`S(0)=0`),
        "), the discrepancy from the closed form at level ",
        math(String.raw`n`),
        " equals ",
        math(String.raw`S(n)-T_{\mathrm{def}}`),
        ". Hence the following three statements hold.",
      ]),
      list([
        [
          "The closed form holds at level ",
          math(String.raw`n`),
          " if and only if ",
          math(String.raw`S(n)=T_{\mathrm{def}}`),
          ".",
        ],
        [
          math(String.raw`T_{\mathrm{def}}=0`),
          " is equivalent to **the closed form holding at the first level**, that is at ",
          math(String.raw`n=0`),
          ", because ",
          math(String.raw`S(0)=0`),
          ".",
        ],
        [
          "**The closed form holding at every level**, that is for all ",
          math(String.raw`n\ge0`),
          ", is equivalent to ",
          math(String.raw`\Theta_M=\Theta^{\mathrm{as}}_M`),
          " holding for every ",
          math(String.raw`M\ge1`),
          ", that is to the absence of any transient, ",
          math(String.raw`\delta_M=0`),
          ", and this is **strictly stronger** than ",
          math(String.raw`T_{\mathrm{def}}=0`),
          ".",
        ],
      ]),
      paragraph([
        "The converse of the third statement, from ",
        math(String.raw`T_{\mathrm{def}}=0`),
        " to all ",
        math(String.raw`n\ge0`),
        ", **fails**. A total sum may vanish while a partial sum does not: ",
        math(String.raw`0`),
        " is the value of the whole series, but not necessarily of ",
        math(String.raw`S(n)`),
        ", which need not be ",
        math(String.raw`0`),
        ". For a counterexample take ",
        math(String.raw`\Theta^{\mathrm{as}}\equiv0`),
        ", ",
        math(String.raw`\Theta_1=1`),
        ", ",
        math(String.raw`\Theta_2=-1`),
        " and ",
        math(String.raw`\Theta_M=0\ (M\ge3)`),
        ": then ",
        math(String.raw`T_{\mathrm{def}}=0`),
        " while ",
        math(String.raw`S(1)=1`),
        ", so that at ",
        math(String.raw`n=1`),
        " the closed form is off by ",
        math(String.raw`1`),
        ". **The scope of this counterexample is limited**: it takes ",
        math(String.raw`\Theta`),
        " arbitrarily, and no genuine tower in which the signs of ",
        math(String.raw`\delta_M`),
        " are mixed has been exhibited. The bracket in the formula for ",
        ref("paper_prop_M"),
        " (M4), that is for ",
        math(String.raw`e`),
        ", is exactly this ",
        math(String.raw`T_{\mathrm{def}}`),
        ". Note that the **sum** of ",
        math(String.raw`c`),
        " and ",
        math(String.raw`e`),
        " lies at a shallower informational level than either of them separately.",
      ]),
      paragraph([
        "**(U3 The information hierarchy has three layers)** The five coefficients split into three layers. The first layer, that of ",
        math(String.raw`\bmod\ \ell`),
        ", consists of the quantities readable from ",
        math(String.raw`\bar{\tilde E}`),
        ": the valuation of the content, the multiplicities of the binomial factors, ",
        math(String.raw`S_\infty`),
        ", ",
        math(String.raw`j^{*}`),
        ", ",
        math(String.raw`e_j`),
        ", ",
        math(String.raw`K`),
        ", ",
        math(String.raw`r^\sharp`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}`),
        "; from these, ",
        math(String.raw`a`),
        " and ",
        math(String.raw`b`),
        " are determined. The second layer adds the twisted stage data ",
        math(String.raw`(\Lambda_k,\theta^\sharp_k)`),
        " to the first, and from these ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        " are determined. The third layer adds ",
        math(String.raw`v_\ell(\kappa(X))`),
        " and ",
        math(String.raw`T_{\mathrm{def}}`),
        " to the second, and only ",
        math(String.raw`e`),
        " requires it. ",
        "**This is the answer to the question of which extra data are needed.** The items (U4)–(U6) below show that the hierarchy really does not collapse: the second layer does not reduce to the first, and it does not reduce to any fixed number of ",
        math(String.raw`\ell`),
        "-adic digits either.",
      ]),
      paragraph([
        "**(U4 The residue field alone does not determine the second layer)** The break lies between ",
        math(String.raw`b`),
        " and ",
        math(String.raw`(c,d,e)`),
        ". Let ",
        math(String.raw`\ell=2`),
        ", let ",
        math(String.raw`p`),
        " be odd, and let ",
        math(String.raw`X_p`),
        " be the one-vertex bouquet whose loops carry the voltages ",
        math(String.raw`(1,0)`),
        " with multiplicity ",
        math(String.raw`p`),
        " and ",
        math(String.raw`(0,1)`),
        " once. Then ",
        math(String.raw`\mu=0`),
        " and ",
        math(String.raw`\bar{\tilde E}=z^2w+zw^2+z+w\in\mathbb{F}_2[z,w]`),
        " is independent of the odd number ",
        math(String.raw`p`),
        ". Hence so are ",
        math(String.raw`S_\infty=\{(1{:}1),(1{:}-1)\}`),
        " and, at each of its points, ",
        math(String.raw`j^{*}=1`),
        ", ",
        math(String.raw`e_{j^*}=2`),
        ", ",
        math(String.raw`K=1`),
        ", ",
        math(String.raw`r^\sharp=2`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}=2`),
        ". Nevertheless, at each ",
        math(String.raw`P_0\in S_\infty`),
        ",",
      ]),
      displayMath(
        String.raw`\Lambda_0=v_2(p+1),\quad\theta^\sharp_0=2,\qquad
        \Lambda_1=\min\bigl(2,\,v_2(p-1)\bigr),\qquad
        \theta^\sharp_1=\begin{cases}0&(p\equiv1\bmod4)\\ 2&(p\equiv3\bmod4)\end{cases}`,
      ),
      paragraph([
        "Here the ",
        math(String.raw`\min`),
        " is taken, as in the convention of (M1), only over those ",
        math(String.raw`A_m\neq0`),
        ", that is over the indices ",
        math(String.raw`m`),
        " with non-vanishing coefficient (",
        math(String.raw`v_2(0)=+\infty`),
        "). At depth ",
        math(String.raw`1`),
        " the coefficients are ",
        math(String.raw`A_0=A_1=4`),
        " and ",
        math(String.raw`A_2=1-p`),
        ", so **for one of the two towers the last coefficient vanishes**: for ",
        math(String.raw`p=1`),
        " we get ",
        math(String.raw`A_2=0`),
        ", hence ",
        math(String.raw`\Lambda_1=v_2(4)=2`),
        " and ",
        math(String.raw`\theta^\sharp_1=0`),
        " (which is the case ",
        math(String.raw`p\equiv1\bmod4`),
        "). Dropping the convention and reading ",
        math(String.raw`v_2(0)=0`),
        " would give ",
        math(String.raw`\Lambda_1=0`),
        " and the values below would not come out. By (U1) we have ",
        math(String.raw`d=2\theta^\sharp_1-6`),
        " and ",
        math(String.raw`c=2\mathcal{L}-2`),
        " with ",
        math(String.raw`\mathcal{L}=v_2(p+1)+\min(2,v_2(p-1))`),
        ". In particular ",
        math(String.raw`p=1`),
        " and ",
        math(String.raw`p=3`),
        " have the same ",
        math(String.raw`\bar{\tilde E}`),
        " and the same ",
        math(String.raw`\mu`),
        ", and yet",
      ]),
      displayMath(String.raw`(a,b,c,d,e)=(0,2,4,-6,-1)`),
      paragraph(["and"]),
      displayMath(String.raw`(a,b,c,d,e)=(0,2,4,-2,-4)`),
      paragraph([
        "respectively, so that ",
        math(String.raw`d`),
        " and ",
        math(String.raw`e`),
        " differ. **Hence none of the three coefficients of the second and third layers is a function of the reduction over the residue field.** In symbols, none of ",
        math(String.raw`c,d,e`),
        " is a function of ",
        math(String.raw`\bar{\tilde E}`),
        ". The counterexample uses **two genuine voltage graphs**, not an artificial perturbation of ",
        math(String.raw`\tilde E`),
        ", so the escape route “perhaps for graphs there is a formula readable from ",
        math(String.raw`\bar{\tilde E}`),
        "” is closed off as well. ",
        "Note that in this pair ",
        math(String.raw`\mathcal{L}`),
        " happens to coincide, so that ",
        math(String.raw`c`),
        " is the same for both. That is, the counterexample for ",
        math(String.raw`c`),
        " and the counterexample for ",
        math(String.raw`d`),
        " are needed separately.",
      ]),
      paragraph([
        "**(U5 No fixed precision suffices)** Let ",
        math(String.raw`\ell=2`),
        ".",
      ]),
      list([
        [
          "(the side of ",
          math(String.raw`c`),
          ") Let ",
          math(String.raw`X_t`),
          " be the one-vertex bouquet whose loops carry the voltages ",
          math(String.raw`(1,0)`),
          " and ",
          math(String.raw`(0,1)`),
          " once each and ",
          math(String.raw`(1,-1)`),
          " with multiplicity ",
          math(String.raw`t`),
          ". For every ",
          math(String.raw`N\ge2`),
          ", the values ",
          math(String.raw`t=2^{N+1}-1`),
          " and ",
          math(String.raw`t'=3\cdot2^{N}-1`),
          " give ",
          math(String.raw`\tilde E_{X_t}\equiv\tilde E_{X_{t'}}\pmod{2^{N}}`),
          " (hence the same ",
          math(String.raw`\bar{\tilde E}`),
          " and the same ",
          math(String.raw`\mu`),
          "), and yet ",
          math(String.raw`c`),
          " differs by ",
          math(String.raw`3`),
          ". Here ",
          math(String.raw`\theta^\sharp`),
          " is unchanged at all four places, so ",
          math(String.raw`d`),
          " does not change.",
        ],
        [
          "(the side of ",
          math(String.raw`d`),
          ") Let ",
          math(String.raw`Y_{p,q,t}`),
          " be the one-vertex bouquet whose loops carry the voltages ",
          math(String.raw`(1,0)`),
          " with multiplicity ",
          math(String.raw`p`),
          ", ",
          math(String.raw`(0,1)`),
          " with multiplicity ",
          math(String.raw`q`),
          " and ",
          math(String.raw`(1,-1)`),
          " with multiplicity ",
          math(String.raw`t`),
          ". At ",
          math(String.raw`P_0=(0{:}1)`),
          " the coefficients at depth ",
          math(String.raw`1`),
          " are ",
          math(String.raw`A_0=A_1=4(p+t)`),
          " and ",
          math(String.raw`A_2=t-q`),
          ", and here too the ",
          math(String.raw`\min`),
          " is taken only over those ",
          math(String.raw`A_m\neq0`),
          ", that is over the indices ",
          math(String.raw`m`),
          " with non-vanishing coefficient (for ",
          math(String.raw`t=q`),
          " one has ",
          math(String.raw`A_2=0`),
          ", hence ",
          math(String.raw`\Lambda_1=v_2(4(p+t))`),
          " and ",
          math(String.raw`\theta^\sharp_1=0`),
          "). For every ",
          math(String.raw`N\ge1`),
          ", the values ",
          math(String.raw`t=1`),
          ", ",
          math(String.raw`q=1+2^{N+2}`),
          ", ",
          math(String.raw`p=2^{N+1}-1`),
          " and ",
          math(String.raw`p'=p+2^{N}`),
          " (for which ",
          math(String.raw`t\neq q`),
          ", so that the boundary case above is not touched) give ",
          math(String.raw`\tilde E_{Y_{p,q,t}}\equiv\tilde E_{Y_{p',q,t}}\pmod{2^{N}}`),
          ", while ",
          math(String.raw`\Lambda_1`),
          " stays **unchanged** at ",
          math(String.raw`N+2`),
          " and ",
          math(String.raw`\theta^\sharp_1`),
          " passes from ",
          math(String.raw`2`),
          " to ",
          math(String.raw`0`),
          ", so that ",
          math(String.raw`d`),
          " differs by ",
          math(String.raw`2`),
          ".",
        ],
      ]),
      paragraph([
        "**Hence, however many digits one fixes when reducing to a residue ring, no formula reading the coefficients of the second layer off that reduction exists.** ",
        "Writing the reduction as ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        ", this says that no formula for ",
        math(String.raw`c`),
        " and no formula for ",
        math(String.raw`d`),
        " exists for any fixed ",
        math(String.raw`N`),
        ". Moreover the obstruction on the position side, that is for ",
        math(String.raw`d`),
        ", is not a consequence of the obstruction on the valuation side, that is for ",
        math(String.raw`c`),
        ": in the second family ",
        math(String.raw`\Lambda_1`),
        " stays fixed while only ",
        math(String.raw`\theta^\sharp_1`),
        " moves.",
      ]),
      paragraph([
        "**(U6 The converse — when the precision does suffice)** Conversely, if ",
        math(String.raw`N\in\mathbb{Z}_{\ge1}`),
        " satisfies",
      ]),
      displayMath(
        String.raw`N>\max_{P_0\in S_\infty}\ \max_{0\le k\le K(P_0)}\Lambda_k(P_0)`,
      ),
      paragraph([
        "then ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        " determines ",
        math(String.raw`(\Lambda_k,\theta^\sharp_k)_{k\le K}`),
        " and hence ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        ". Indeed, writing ",
        math(String.raw`\tilde E'=\tilde E+\ell^{N}h`),
        ", the polynomial ",
        math(String.raw`\Phi^{[k]}`),
        " is an ",
        math(String.raw`\mathcal{O}_k`),
        "-linear form in the coefficients, so ",
        math(String.raw`A'^{[k]}_m=A^{[k]}_m+\ell^{N}\beta_m`),
        ", and therefore the **truncated valuation sequence** ",
        math(String.raw`\bigl(\min(v_\ell(A^{[k]}_m),N)\bigr)_m`),
        " is determined by ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        ". The condition gives ",
        math(String.raw`\Lambda_k<N`),
        ", so both ",
        math(String.raw`\Lambda_k`),
        " and the first index realising that minimum, namely ",
        math(String.raw`\theta^\sharp_k`),
        ", can be read off that sequence. ",
        "The condition looks self-referential, but in practice it is not circular: compute the truncated valuation sequence for a given ",
        math(String.raw`N`),
        ", and if the minimum is smaller than ",
        math(String.raw`N`),
        ", that very computation certifies that this ",
        math(String.raw`N`),
        " was large enough. ",
        "**The required precision is finite and computable for each tower, but it is not bounded above as the tower varies** (that is exactly (U5)).",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "**That the third layer is not superfluous**, that is, that ",
          math(String.raw`v_\ell(\kappa(X))`),
          " together with the whole second layer still fails to determine ",
          math(String.raw`e`),
          ", is an **existence statement**, supported by explicit towers found by scanning a population (an example suffices to establish existence). It is not a general proof.",
        ],
        [
          "(U6) concerns ",
          math(String.raw`c,d`),
          ". The constant term ",
          math(String.raw`e`),
          " involves ",
          math(String.raw`T_{\mathrm{def}}`),
          ", where the low-level sums ",
          math(String.raw`\Theta_M`),
          " occur and hence arbitrarily deep valuations, so that ",
          "**no explicit form for the precision required by the constant term is given.**",
        ],
        [
          "**The scope is limited to rank two, the hypothesis (H), and voltage graphs of Laplacian determinant type.** ",
          "This is the same restriction as in ",
          ref("paper_prop_M"),
          "; in symbols, ",
          math(String.raw`d=2`),
          " and the objects considered are only those of the form ",
          math(String.raw`\det L`),
          ".",
        ],
        [
          "**No novelty is claimed.** That the coefficients at and below ",
          math(String.raw`\ell^{n}`),
          " are not determined ",
          math(String.raw`\bmod\ \ell`),
          ", that is, that the terms below ",
          math(String.raw`\mu,\lambda`),
          " require deeper information, is a standard understanding in Iwasawa theory. ",
          "What this proposition adds is the finiteness statement that this deeper information is **exhausted** by the stage data over ",
          math(String.raw`\mathbb{Z}[\zeta_{\ell^{k}}]`),
          ", together with counterexamples establishing that no fixed number of ",
          math(String.raw`\ell`),
          "-adic digits suffices.",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "**Only two results are used from outside, and the form in which each is used is made explicit at the point of use.** They are ",
        ref("paper_prop_M"),
        " and ",
        ref("paper_prop_K"),
        ".",
      ]),
      paragraph([
        "**Proof of (U1).** The formula for ",
        math(String.raw`d`),
        " is nothing but the ",
        ref("paper_prop_M"),
        " (M3) expression for ",
        math(String.raw`\gamma`),
        ", combined with the identity ",
        math(String.raw`d=\gamma-2`),
        " of (M4), rewritten in terms of ",
        math(String.raw`\mathcal{T}`),
        ". The formula for ",
        math(String.raw`c`),
        " is obtained by substituting the (M3) expressions for ",
        math(String.raw`c=\tfrac{\ell}{\ell-1}\beta-\tfrac{\ell}{(\ell-1)^{2}}\alpha`),
        " — namely those for ",
        math(String.raw`\beta`),
        " and ",
        math(String.raw`\alpha`),
        " — into the (M4) identity. Indeed",
      ]),
      displayMath(
        String.raw`\frac{\ell}{\ell-1}\beta=\frac{\ell}{\ell-1}A_{\mathrm{gen}}
        +\sum_{P_0\in S_\infty}\Bigl[\frac{e_{j^*}\,\ell^{\,1-r^\sharp}}{\ell-1}
        -j^{*}\bigl(K+r^\sharp\bigr)+\mathcal{L}(P_0)\Bigr],`,
      ),
      displayMath(
        String.raw`\frac{\ell}{(\ell-1)^{2}}\alpha
        =\frac{\ell}{(\ell-1)^{2}}\cdot\frac{\ell-1}{\ell}\sum_{P_0\in S_\infty}j^{*}
        =\frac{1}{\ell-1}\sum_{P_0\in S_\infty}j^{*}`,
      ),
      paragraph([
        ", and taking the difference gives the asserted formula for ",
        math(String.raw`c`),
        ". The division of labour and the integrality can be read off from the right-hand sides. The valuation side ",
        math(String.raw`\Lambda_k`),
        " enters only through ",
        math(String.raw`\mathcal{L}`),
        ", hence only into ",
        math(String.raw`c`),
        "; the position side ",
        math(String.raw`\theta^\sharp_k`),
        " enters only through ",
        math(String.raw`\mathcal{T}`),
        ", hence only into ",
        math(String.raw`d`),
        ". That ",
        math(String.raw`d`),
        " is an integer follows from ",
        math(String.raw`\mathcal{T}\in\mathbb{Z}`),
        " (seen at the start of this proposition), from ",
        math(String.raw`e_{j^*}\in\mathbb{Z}`),
        " (the quantity ",
        ref("paper_prop_J"),
        " (J2) denotes by ",
        math(String.raw`e_j`),
        " is an ",
        math(String.raw`\mathrm{ord}_{x=0}`),
        ", hence an integer), and from ",
        math(String.raw`K\in\mathbb{Z}_{\ge0}`),
        ". By contrast the formula for ",
        math(String.raw`c`),
        " contains three terms with denominator ",
        math(String.raw`\ell-1`),
        ", namely ",
        math(String.raw`\tfrac{\ell}{\ell-1}A_{\mathrm{gen}}`),
        ", ",
        math(String.raw`\tfrac{e_{j^*}\ell^{\,1-r^\sharp}}{\ell-1}`),
        " and ",
        math(String.raw`\tfrac{j^{*}}{\ell-1}`),
        ", so that ",
        math(String.raw`c`),
        " may well be a non-integral rational number. If ",
        math(String.raw`S_\infty=\emptyset`),
        " the sum is empty and ",
        math(String.raw`d=-2`),
        ", in accordance with ",
        ref("paper_prop_M"),
        " (M6). The ordering of difficulty is exactly the inclusion of the input data of the layers listed in (U3).",
      ]),
      paragraph([
        "**Proof of (U1a).** It suffices to treat ",
        math(String.raw`K'=K+1`),
        " (the rest is induction). The layer of depth ",
        math(String.raw`K+1`),
        " is unsaturated by ",
        ref("paper_prop_M"),
        " (M2), so ",
        math(String.raw`\Lambda_{K+1}=j^{*}/\varphi(\ell^{K+1})`),
        " and ",
        math(String.raw`\theta^\sharp_{K+1}=e_{j^*}`),
        ". In the formula for ",
        math(String.raw`d`),
        " the quantity ",
        math(String.raw`\mathcal{T}`),
        " increases by ",
        math(String.raw`\varphi(\ell^{K+1})e_{j^*}`),
        " while ",
        math(String.raw`-e_{j^*}\ell^{K}`),
        " becomes ",
        math(String.raw`-e_{j^*}\ell^{K+1}`),
        ", so that the net change is",
      ]),
      displayMath(
        String.raw`e_{j^*}\bigl(\varphi(\ell^{K+1})-\ell^{K+1}+\ell^{K}\bigr)
        =e_{j^*}\bigl((\ell^{K+1}-\ell^{K})-\ell^{K+1}+\ell^{K}\bigr)=0`,
      ),
      paragraph([
        ". In the formula for ",
        math(String.raw`c`),
        " the quantity ",
        math(String.raw`\mathcal{L}`),
        " increases by ",
        math(String.raw`\varphi(\ell^{K+1})\cdot\tfrac{j^{*}}{\varphi(\ell^{K+1})}=j^{*}`),
        " while ",
        math(String.raw`-j^{*}\bigl(K+r^\sharp+\tfrac{1}{\ell-1}\bigr)`),
        " becomes ",
        math(String.raw`-j^{*}\bigl(K+1+r^\sharp+\tfrac{1}{\ell-1}\bigr)`),
        ", so the net change is ",
        math(String.raw`j^{*}-j^{*}=0`),
        ".",
      ]),
      paragraph([
        "**Proof of (U2); first, independence of the truncation level.** The level in question is ",
        math(String.raw`M^{*}`),
        ". By ",
        ref("paper_prop_M"),
        " (M3) the terms with ",
        math(String.raw`M\ge M^{*}`),
        " vanish, that is they are ",
        math(String.raw`0`),
        ", so truncating anywhere at or beyond ",
        math(String.raw`M^{*}`),
        " gives the same sum. **Next, the formula for the constant term.** The term in question is ",
        math(String.raw`e`),
        ", and the bracket in the ",
        ref("paper_prop_M"),
        " (M4) expression for ",
        math(String.raw`e`),
        " equals",
      ]),
      displayMath(
        String.raw`\sum_{M=1}^{M^{*}-1}\Theta_M-\alpha\,\mathcal{S}_1(M^{*}\!-\!1)
        -\beta\,\mathcal{S}_0(M^{*}\!-\!1)-\gamma(M^{*}\!-\!1)
        =\sum_{M=1}^{M^{*}-1}\bigl(\Theta_M-\Theta^{\mathrm{as}}_M\bigr)=T_{\mathrm{def}}`,
      ),
      paragraph([
        " (this is nothing but expanding the definitions of ",
        math(String.raw`\mathcal{S}_1`),
        " and ",
        math(String.raw`\mathcal{S}_0`),
        "). Moreover ",
        ref("paper_prop_M"),
        " (M4) gives ",
        math(String.raw`\tfrac{\ell}{(\ell-1)^{2}}\alpha-\tfrac{\ell}{\ell-1}\beta=-c`),
        " and ",
        math(String.raw`\mu=a`),
        ", so substituting yields ",
        math(String.raw`e=v_\ell(\kappa(X))-a-c+T_{\mathrm{def}}`),
        ", and rearranging gives the formula for ",
        math(String.raw`c+e`),
        ".",
      ]),
      paragraph([
        "**The three equivalences.** Define ",
        math(String.raw`\delta_M`),
        " and ",
        math(String.raw`S(n)`),
        " as in the assertion. The expression for ",
        ref("paper_prop_R"),
        " — namely part (R5), which writes ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)`),
        " in terms of ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        " — combined with applying the computation of ",
        math(String.raw`\Theta^{\mathrm{as}}`),
        " from ",
        ref("paper_prop_M"),
        " (M4) verbatim, shows that at level ",
        math(String.raw`n`),
        " the difference between the actual value and the value of the five-coefficient form, whose constant term ",
        math(String.raw`e`),
        " is given by the formula above, equals exactly ",
        math(String.raw`S(n)-T_{\mathrm{def}}`),
        " for all ",
        math(String.raw`n\ge0`),
        ". Hence the closed form holds at level ",
        math(String.raw`n`),
        " if and only if ",
        math(String.raw`S(n)=T_{\mathrm{def}}`),
        ". At ",
        math(String.raw`n=0`),
        " we have ",
        math(String.raw`S(0)=0`),
        ", which gives the second item. That it holds for every ",
        math(String.raw`n\ge0`),
        " is equivalent to ",
        math(String.raw`S`),
        " being constant equal to ",
        math(String.raw`T_{\mathrm{def}}`),
        ", which is equivalent to ",
        math(String.raw`\delta_M=0`),
        " for all ",
        math(String.raw`M\ge1`),
        ", and then ",
        math(String.raw`T_{\mathrm{def}}=0`),
        " follows as well, so this is strictly stronger than the second item. That the converse fails is shown by the counterexample stated in the assertion. **The point is that a total sum may vanish while a partial sum does not.** The total sum in question is ",
        math(String.raw`0`),
        ", while the partial sum need not equal ",
        math(String.raw`0`),
        ".",
      ]),
      paragraph([
        "**Proof of (U3).** By (U1) the coefficients ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        " are expressible in terms of the first-layer data together with ",
        math(String.raw`(\Lambda_k,\theta^\sharp_k)_{k\le K}`),
        " alone, while by (U2) the constant term ",
        math(String.raw`e`),
        " requires in addition ",
        math(String.raw`v_\ell(\kappa(X))`),
        " and ",
        math(String.raw`T_{\mathrm{def}}`),
        ". That ",
        math(String.raw`a=\mu`),
        " and ",
        math(String.raw`b=\sum j^{*}`),
        " are determined by the first layer is part (K6) of ",
        ref("paper_prop_K"),
        " together with ",
        ref("paper_prop_M"),
        " (M4). It remains for (U4) and (U5) to show that the second layer reduces neither to the first layer nor to any fixed number of ",
        math(String.raw`\ell`),
        "-adic digits.",
      ]),
      paragraph([
        "**Proof of (U4); first, that the first layer does not depend on the parameter.** Here the parameter is ",
        math(String.raw`p`),
        ". The coefficients of ",
        math(String.raw`D=p(2-z-z^{-1})+(2-w-w^{-1})`),
        " are ",
        math(String.raw`2p+2,-p,-p,-1,-1`),
        ", whose content is ",
        math(String.raw`1`),
        ", so ",
        math(String.raw`\mu=0`),
        ", ",
        math(String.raw`E=D`),
        " and ",
        math(String.raw`\tilde E=zwD`),
        ". For odd ",
        math(String.raw`p`),
        " one has ",
        math(String.raw`\bmod\ 2`),
        " the congruence ",
        math(String.raw`p\equiv1`),
        ", hence ",
        math(String.raw`\bar{\tilde E}=z^{2}w+w+zw^{2}+z`),
        ", which does not depend on ",
        math(String.raw`p`),
        ". Consequently the quantities that parts (K2), (K3) and (K4) of ",
        ref("paper_prop_K"),
        " read off from ",
        math(String.raw`\bar{\tilde E}`),
        " alone — namely ",
        math(String.raw`S_\infty`),
        ", ",
        math(String.raw`j^{*}`),
        " and ",
        math(String.raw`e_j`),
        " — are all independent of ",
        math(String.raw`p`),
        ". As for ",
        math(String.raw`r^\sharp`),
        ", from ",
        math(String.raw`\det\bigl((1,1),(1,-1)\bigr)=-2`),
        " part (K5) gives ",
        math(String.raw`r_0=2`),
        ", and since ",
        math(String.raw`j^{*}=1`),
        " is the unique minimising index we have ",
        math(String.raw`R'=r_0`),
        ", hence ",
        math(String.raw`r^\sharp=2`),
        ".",
      ]),
      paragraph([
        "**Next, the stage data.** Take ",
        math(String.raw`P_0=(1{:}1)`),
        ", so that for ",
        math(String.raw`u=(1,1)`),
        " a vector with ",
        math(String.raw`\det(u,\mathbf{e})=1`),
        " is ",
        math(String.raw`\mathbf{e}=(0,1)`),
        ". Write ",
        math(String.raw`y:=1+x`),
        ". At ",
        math(String.raw`k=0`),
        ", where ",
        math(String.raw`\eta=1`),
        ", we get ",
        math(String.raw`\Phi_u(x)=D(y,y)=(p+1)(2-y-y^{-1})=-(p+1)x^{2}y^{-1}`),
        ", and since ",
        math(String.raw`y^{-1}`),
        " is a unit this gives ",
        math(String.raw`\Lambda_0=v_2(p+1)`),
        " and ",
        math(String.raw`\theta^\sharp_0=2`),
        ". At ",
        math(String.raw`k=1`),
        ", where ",
        math(String.raw`\eta=-1`),
        ", we get ",
        math(String.raw`\Phi^{[1]}_u(x)=D(y,-y)=p(2-y-y^{-1})+(2+y+y^{-1})`),
        "; multiplying by the unit ",
        math(String.raw`y`),
        " gives ",
        math(String.raw`(1-p)y^{2}+(2p+2)y+(1-p)`),
        ", and substituting ",
        math(String.raw`y=1+x`),
        " yields",
      ]),
      displayMath(
        String.raw`A_0=(1-p)+(2p+2)+(1-p)=4,\qquad A_1=2(1-p)+(2p+2)=4,\qquad A_2=1-p`,
      ),
      paragraph([
        ". Hence ",
        math(String.raw`\Lambda_1=\min\bigl(v_2(4),v_2(1-p)\bigr)=\min\bigl(2,v_2(p-1)\bigr)`),
        ", and the first index attaining the minimum is ",
        math(String.raw`v_2(p-1)\ge2`),
        " — in which case it is ",
        math(String.raw`m=0`),
        " — while if ",
        math(String.raw`v_2(p-1)=1`),
        " it is ",
        math(String.raw`m=2`),
        ". Since ",
        math(String.raw`p`),
        " is odd we have ",
        math(String.raw`v_2(p-1)\ge1`),
        ", and ",
        math(String.raw`v_2(p-1)\ge2\iff p\equiv1\bmod4`),
        ". For ",
        math(String.raw`p=1`),
        " one has ",
        math(String.raw`A_2=0`),
        ", so by the convention of ",
        ref("paper_prop_M"),
        " (M1) the minimum ",
        math(String.raw`\min`),
        " is taken over ",
        math(String.raw`A_0,A_1`),
        " only, giving ",
        math(String.raw`\Lambda_1=v_2(4)=2`),
        " and ",
        math(String.raw`\theta^\sharp_1=0`),
        " (which falls under the case ",
        math(String.raw`p\equiv1\bmod4`),
        "). The point ",
        math(String.raw`P_0=(1{:}-1)`),
        " gives the same values because ",
        math(String.raw`D`),
        " is invariant under ",
        math(String.raw`w\mapsto w^{-1}`),
        ".",
      ]),
      paragraph([
        "**Finally, the values of the coefficients.** Substitute into (U1). We have ",
        math(String.raw`|S_\infty|=2`),
        ", at each point ",
        math(String.raw`\mathcal{T}=\theta^\sharp_0+\varphi(2)\theta^\sharp_1=2+\theta^\sharp_1`),
        " and ",
        math(String.raw`e_{j^*}\ell^{K}=2\cdot2=4`),
        ", so ",
        math(String.raw`d=2\bigl((2+\theta^\sharp_1)-4\bigr)-2=2\theta^\sharp_1-6`),
        ". As for ",
        math(String.raw`c`),
        ", from ",
        math(String.raw`\ell=2`),
        ", ",
        math(String.raw`r^\sharp=2`),
        ", ",
        math(String.raw`A_{\mathrm{gen}}=2`),
        ", ",
        math(String.raw`j^{*}=1`),
        " and ",
        math(String.raw`K=1`),
        " the bracket at each point equals ",
        math(String.raw`\tfrac{2\cdot2^{-1}}{1}-1\cdot(1+2+1)+\mathcal{L}=\mathcal{L}-3`),
        ", whence ",
        math(String.raw`c=2\cdot2+2(\mathcal{L}-3)=2\mathcal{L}-2`),
        ". For ",
        math(String.raw`p=1`),
        " we get ",
        math(String.raw`\theta^\sharp_1=0`),
        " and ",
        math(String.raw`\mathcal{L}=1+2=3`),
        ", hence ",
        math(String.raw`(c,d)=(4,-6)`),
        "; for ",
        math(String.raw`p=3`),
        " we get ",
        math(String.raw`\theta^\sharp_1=2`),
        " and ",
        math(String.raw`\mathcal{L}=2+1=3`),
        ", hence ",
        math(String.raw`(c,d)=(4,-2)`),
        ". The constant term is ",
        math(String.raw`e`),
        ", given by (U2) as ",
        math(String.raw`e=0-0-c+T_{\mathrm{def}}=-4+T_{\mathrm{def}}`),
        ", and the values of ",
        math(String.raw`T_{\mathrm{def}}`),
        ", namely ",
        math(String.raw`3`),
        " for ",
        math(String.raw`p=1`),
        " and ",
        math(String.raw`0`),
        " for ",
        math(String.raw`p=3`),
        ", are obtained by a finite computation since the defining sum is finite (they were computed in the numerical verification attached to this proposition). Hence ",
        math(String.raw`e=-1`),
        " and ",
        math(String.raw`e=-4`),
        " respectively. **For this pair the valuation-side sum happens to agree, so the third coefficient is the same and only the fourth and fifth differ.** In symbols, ",
        math(String.raw`\mathcal{L}`),
        " agrees so that ",
        math(String.raw`c`),
        " is the same, while ",
        math(String.raw`d`),
        " and ",
        math(String.raw`e`),
        " differ.",
      ]),
      paragraph([
        "**Proof of (U5); the valuation side.** Here the coefficient concerned is ",
        math(String.raw`c`),
        ". The coefficients of ",
        math(String.raw`D=(2-z-z^{-1})+(2-w-w^{-1})+t(2-zw^{-1}-z^{-1}w)`),
        " are ",
        math(String.raw`4+2t,-1,-1,-1,-1,-t,-t`),
        ", whose content is ",
        math(String.raw`1`),
        ", so ",
        math(String.raw`\mu=0`),
        ". For odd ",
        math(String.raw`t`),
        " one has ",
        math(String.raw`\bmod\ 2`),
        " the identity ",
        math(String.raw`\bar{\tilde E}=z^{2}w+zw^{2}+z+w+z^{2}+w^{2}=(z+w)(z+1)(w+1)`),
        ", and every binomial factor has multiplicity ",
        math(String.raw`1`),
        ", so part (K4) of ",
        ref("paper_prop_K"),
        " gives ",
        math(String.raw`j^{*}=1`),
        " and ",
        math(String.raw`S_\infty=\{(1{:}0),(0{:}1),(1{:}1)\}`),
        ". The stage data are obtained by computing the four restrictions, with ",
        math(String.raw`y=1+x`),
        " and ",
        math(String.raw`\eta=-1`),
        ".",
      ]),
      list([
        [
          "For ",
          math(String.raw`P_0=(1{:}1)`),
          " with ",
          math(String.raw`\mathbf{e}=(0,1)`),
          " and ",
          math(String.raw`k=0`),
          ": here ",
          math(String.raw`(z,w)=(y,y)`),
          " and ",
          math(String.raw`zw^{-1}=z^{-1}w=1`),
          ", so ",
          math(String.raw`\Phi_u=2(2-y-y^{-1})=-2x^{2}y^{-1}`),
          ". Hence ",
          math(String.raw`\Lambda_0=1`),
          " and ",
          math(String.raw`\theta^\sharp_0=2`),
          ", neither of which depends on ",
          math(String.raw`t`),
          ".",
        ],
        [
          "For ",
          math(String.raw`P_0=(1{:}1)`),
          " with ",
          math(String.raw`k=1`),
          ": here ",
          math(String.raw`(z,w)=(y,-y)`),
          " and ",
          math(String.raw`\Phi^{[1]}_u=(2-y-y^{-1})+(2+y+y^{-1})+t(2+1+1)=4(t+1)`),
          ", a constant. Hence ",
          math(String.raw`\Lambda_1=2+v_2(t+1)`),
          " and ",
          math(String.raw`\theta^\sharp_1=0`),
          ", the latter again independent of ",
          math(String.raw`t`),
          ".",
        ],
        [
          "For ",
          math(String.raw`P_0=(0{:}1)`),
          " with ",
          math(String.raw`\mathbf{e}=(-1,0)`),
          " and ",
          math(String.raw`k=0`),
          ": here ",
          math(String.raw`(z,w)=(1,y)`),
          " and ",
          math(String.raw`\Phi_u=(1+t)(2-y-y^{-1})`),
          ". Hence ",
          math(String.raw`\Lambda_0=v_2(t+1)`),
          " and ",
          math(String.raw`\theta^\sharp_0=2`),
          ".",
        ],
        [
          "For ",
          math(String.raw`P_0=(0{:}1)`),
          " with ",
          math(String.raw`k=1`),
          ": here ",
          math(String.raw`(z,w)=(-1,y)`),
          " and ",
          math(String.raw`\Phi^{[1]}_u=4+(2-y-y^{-1})+t(2+y+y^{-1})=(6+2t)+(t-1)(y+y^{-1})`),
          ". Multiplying by the unit ",
          math(String.raw`y`),
          " and substituting ",
          math(String.raw`y=1+x`),
          " gives ",
          math(String.raw`A_0=A_1=4(t+1)`),
          " and ",
          math(String.raw`A_2=t-1`),
          ", so ",
          math(String.raw`\Lambda_1=\min\bigl(2+v_2(t+1),\,v_2(t-1)\bigr)`),
          ", and ",
          math(String.raw`\theta^\sharp_1`),
          " equals, when ",
          math(String.raw`v_2(t-1)<2+v_2(t+1)`),
          ", the value ",
          math(String.raw`2`),
          ", and otherwise ",
          math(String.raw`0`),
          ".",
        ],
      ]),
      paragraph([
        "The point ",
        math(String.raw`P_0=(1{:}0)`),
        " behaves like ",
        math(String.raw`D`),
        " under ",
        math(String.raw`z\leftrightarrow w`),
        ", which leaves it invariant, so it gives the same data as ",
        math(String.raw`P_0=(0{:}1)`),
        ". Now put ",
        math(String.raw`t=2^{N+1}-1`),
        " and ",
        math(String.raw`t'=3\cdot2^{N}-1`),
        ", so that ",
        math(String.raw`t'-t=2^{N}`),
        " and the difference is ",
        math(String.raw`2^{N}(2-zw^{-1}-z^{-1}w)`),
        ", all of whose coefficients are divisible by ",
        math(String.raw`2^{N}`),
        ", that is ",
        math(String.raw`\tilde E_{X_t}\equiv\tilde E_{X_{t'}}\pmod{2^{N}}`),
        ". Both have ",
        math(String.raw`\mu`),
        " equal to ",
        math(String.raw`0`),
        ", and since ",
        math(String.raw`t,t'`),
        " are both odd the reductions ",
        math(String.raw`\bar{\tilde E}`),
        " agree as well. We have ",
        math(String.raw`v_2(t+1)=N+1`),
        " and ",
        math(String.raw`v_2(t'+1)=N`),
        ", and for ",
        math(String.raw`N\ge2`),
        " also ",
        math(String.raw`v_2(t-1)=v_2(2^{N+1}-2)=1`),
        " and ",
        math(String.raw`v_2(t'-1)=v_2(3\cdot2^{N}-2)=1`),
        ". Hence by the table above the stage data change in exactly three places: ",
        math(String.raw`\Lambda_1\bigl((1{:}1)\bigr)`),
        " goes ",
        math(String.raw`N+3\to N+2`),
        ", while ",
        math(String.raw`\Lambda_0\bigl((0{:}1)\bigr)`),
        " and ",
        math(String.raw`\Lambda_0\bigl((1{:}0)\bigr)`),
        " go ",
        math(String.raw`N+1\to N`),
        ". At ",
        math(String.raw`(0{:}1),(1{:}0)`),
        " the quantity ",
        math(String.raw`\Lambda_1`),
        " stays ",
        math(String.raw`\min(\cdot,1)=1`),
        " and is unchanged, and ",
        math(String.raw`\theta^\sharp`),
        " is unchanged in all four places. The quantities ",
        math(String.raw`K`),
        ", ",
        math(String.raw`e_{j^*}`),
        ", ",
        math(String.raw`r^\sharp`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}`),
        " are determined by ",
        math(String.raw`\bar{\tilde E}`),
        " and hence unchanged. In the (U1) formula for ",
        math(String.raw`c`),
        " the coefficient of ",
        math(String.raw`\mathcal{L}`),
        " is ",
        math(String.raw`1`),
        ", so ",
        math(String.raw`c`),
        " decreases by the decrease of ",
        math(String.raw`\sum_{P_0\in S_\infty}\mathcal{L}(P_0)`),
        ", that is by ",
        math(String.raw`3`),
        ". Since ",
        math(String.raw`\theta^\sharp`),
        " is unchanged in all four places, ",
        math(String.raw`d`),
        " is unchanged.",
      ]),
      paragraph([
        "**The position side.** Here the coefficient concerned is ",
        math(String.raw`d`),
        ". Take ",
        math(String.raw`D=p(2-z-z^{-1})+q(2-w-w^{-1})+t(2-zw^{-1}-z^{-1}w)`),
        " and ",
        math(String.raw`P_0=(0{:}1)`),
        ", ",
        math(String.raw`u=(0,1)`),
        ", ",
        math(String.raw`\mathbf{e}=(-1,0)`),
        ", ",
        math(String.raw`\eta=-1`),
        ", ",
        math(String.raw`y=1+x`),
        ". Substituting ",
        math(String.raw`(z,w)=(-1,y)`),
        " gives ",
        math(String.raw`z+z^{-1}\mapsto-2`),
        " and ",
        math(String.raw`zw^{-1}+z^{-1}w\mapsto-(y+y^{-1})`),
        ", so that",
      ]),
      displayMath(
        String.raw`\Phi^{[1]}_u(x)=4p+q\bigl(2-y-y^{-1}\bigr)+t\bigl(2+y+y^{-1}\bigr)
        =4p+2q+2t+(t-q)\bigl(y+y^{-1}\bigr)`,
      ),
      paragraph([
        ". Multiplying by the unit ",
        math(String.raw`y`),
        " and substituting ",
        math(String.raw`y=1+x`),
        " gives ",
        math(String.raw`A_0=A_1=4(p+t)`),
        " and ",
        math(String.raw`A_2=t-q`),
        ". Now specialise. From ",
        math(String.raw`t=1`),
        " and ",
        math(String.raw`p=2^{N+1}-1`),
        " we get ",
        math(String.raw`p+t=2^{N+1}`),
        " and ",
        math(String.raw`v_2\bigl(4(p+t)\bigr)=N+3`),
        ", while ",
        math(String.raw`q=1+2^{N+2}`),
        " gives ",
        math(String.raw`t-q=-2^{N+2}`),
        " and ",
        math(String.raw`v_2(t-q)=N+2`),
        ". Hence ",
        math(String.raw`\Lambda_1=N+2`),
        ", and the minimum is attained only at ",
        math(String.raw`A_2`),
        ", so ",
        math(String.raw`\theta^\sharp_1=2`),
        ". Replacing ",
        math(String.raw`p'=p+2^{N}`),
        " gives ",
        math(String.raw`p'+t=3\cdot2^{N}`),
        " and ",
        math(String.raw`v_2\bigl(4(p'+t)\bigr)=N+2`),
        ", so ",
        math(String.raw`\Lambda_1=\min(N+2,N+2)=N+2`),
        " is unchanged, but the **first** index attaining the minimum is now ",
        math(String.raw`A_0`),
        ", so ",
        math(String.raw`\theta^\sharp_1=0`),
        ": the position has moved.",
      ]),
      paragraph([
        "The difference of ",
        math(String.raw`D`),
        " is ",
        math(String.raw`2^{N}(2-z-z^{-1})`),
        ", all of whose coefficients are divisible by ",
        math(String.raw`2^{N}`),
        "; both ",
        math(String.raw`p,p'`),
        " are odd, so is ",
        math(String.raw`q`),
        ", and ",
        math(String.raw`t=1`),
        ", so ",
        math(String.raw`\bar{\tilde E}`),
        " and ",
        math(String.raw`\mu`),
        " are unchanged, and hence so are ",
        math(String.raw`K`),
        ", ",
        math(String.raw`e_{j^*}`),
        ", ",
        math(String.raw`r^\sharp`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}`),
        ". That the remaining four values of ",
        math(String.raw`\theta^\sharp`),
        " are unchanged is checked by the same computation. At ",
        math(String.raw`(0{:}1)`),
        " and ",
        math(String.raw`(1{:}0)`),
        " with ",
        math(String.raw`k=0`),
        " one has respectively ",
        math(String.raw`\Phi_u=(q+t)(2-y-y^{-1})`),
        " and ",
        math(String.raw`\Phi_u=(p+t)(2-y-y^{-1})`),
        ", hence ",
        math(String.raw`\theta^\sharp_0=2`),
        ". At ",
        math(String.raw`(1{:}0)`),
        " with ",
        math(String.raw`k=1`),
        " one has ",
        math(String.raw`A_0=A_1=4(q+1)`),
        " with ",
        math(String.raw`v_2=3`),
        " and ",
        math(String.raw`A_2=1-p`),
        ", and at ",
        math(String.raw`(1{:}1)`),
        " with ",
        math(String.raw`k=1`),
        " one has ",
        math(String.raw`A_0=A_1=4(q+1)`),
        " and ",
        math(String.raw`A_2=q-p`),
        "; both ",
        math(String.raw`v_2(1-p)`),
        " and ",
        math(String.raw`v_2(q-p)`),
        " equal, for ",
        math(String.raw`N=1`),
        ", the value ",
        math(String.raw`1`),
        ", and after replacing by ",
        math(String.raw`p'`),
        " the value ",
        math(String.raw`2`),
        ", but all of these are smaller than ",
        math(String.raw`3`),
        ", so the minimum is at ",
        math(String.raw`A_2`),
        " and ",
        math(String.raw`\theta^\sharp_1=2`),
        " remains unchanged (for ",
        math(String.raw`N\ge2`),
        " both equal ",
        math(String.raw`1`),
        "). Hence by (U1) the change in ",
        math(String.raw`d`),
        " comes only from the change of ",
        math(String.raw`(0{:}1)`),
        " — more precisely of its ",
        math(String.raw`\theta^\sharp_1`),
        " — and equals ",
        math(String.raw`\varphi(2)\cdot(0-2)=-2`),
        ". **Since the valuation stays put while only the position moves, this obstruction is not a consequence of the one on the valuation side.** The valuation that stays put is ",
        math(String.raw`\Lambda_1`),
        ".",
      ]),
      paragraph([
        "**Proof of (U6).** Put ",
        math(String.raw`\tilde E'=\tilde E+\ell^{N}h`),
        " with ",
        math(String.raw`h\in\mathbb{Z}[z,w]`),
        ". The quantity ",
        ref("paper_prop_M"),
        " (M1) denotes by ",
        math(String.raw`\Phi^{[k]}`),
        " is a linear form, with coefficients in ",
        math(String.raw`\tilde E`),
        " — more precisely a form over ",
        math(String.raw`\mathcal{O}_k`),
        " in the coefficients — so ",
        math(String.raw`A'^{[k]}_m=A^{[k]}_m+\ell^{N}\beta_m`),
        " with ",
        math(String.raw`\beta_m\in\mathcal{O}_k`),
        ". Since ",
        math(String.raw`v_\ell(\ell^{N}\beta_m)\ge N`),
        ", for those ",
        math(String.raw`v_\ell(A^{[k]}_m)<N`),
        " — that is, for those indices ",
        math(String.raw`m`),
        " with that property — equality in the non-archimedean estimate gives ",
        math(String.raw`v_\ell(A'^{[k]}_m)=v_\ell(A^{[k]}_m)`),
        ", whereas for those with ",
        math(String.raw`v_\ell(A^{[k]}_m)\ge N`),
        " — again ranging over such ",
        math(String.raw`m`),
        " — one gets ",
        math(String.raw`v_\ell(A'^{[k]}_m)\ge N`),
        ". In other words the truncated valuation sequence ",
        math(String.raw`\bigl(\min(v_\ell(A^{[k]}_m),N)\bigr)_m`),
        " is determined by ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        ". By hypothesis ",
        math(String.raw`\Lambda_k<N`),
        ", so ",
        math(String.raw`\Lambda_k`),
        " equals the minimum of that sequence, and ",
        math(String.raw`\theta^\sharp_k`),
        " is the first index attaining it, so both can be read off from it. The quantities ",
        math(String.raw`K`),
        ", ",
        math(String.raw`j^{*}`),
        ", ",
        math(String.raw`e_{j^*}`),
        ", ",
        math(String.raw`r^\sharp`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}`),
        " are determined by the first layer, so by (U1) the coefficients ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        " are determined as well. Raising ",
        math(String.raw`N`),
        " by ",
        math(String.raw`1`),
        " at a time terminates after finitely many steps, since the third item of ",
        ref("paper_prop_M"),
        " (M1) gives ",
        math(String.raw`\Lambda_k<\infty`),
        ".",
      ]),
    ],
  },
]);
