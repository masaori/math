/**
 * 論文本体 第 11 章（英語版）: 一般の塔の閉形式（5 係数すべて）と、係数の情報階層。
 *
 * **正本は日本語版 `../../../content/010_general_closed_form.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする
 * （本章で新しく要る語＝捻り段データ・飽和深度・過渡欠損・情報階層は同用語集 §4 にある）。
 *
 * **数式の中に日本語は 1 つも無い**ので、`../structure-exceptions.ts` への新規登録は行っていない。
 * 強調（`**`）も日本語版の時点でノードをまたがないように書いてあるため、
 * 英語版でも同じ位置に同じ強調を置ける（009 章のような構造の組み替えは要らなかった）。
 */

import {
  defineTranslatedBlocks as defineBlocks,
  displayMath,
  list,
  math,
  paragraph,
  refInTranslation as ref,
} from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_110_heading_general_closed_form",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 1 },
    title: {
      text:
        "A closed form for a general tower (all five coefficients), and the information " +
        "hierarchy of the coefficients",
    },
    labels: [],
  },
  {
    id: "paper_111_theorem_general_closed_form",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 2 },
    title: {
      text:
        "Proposition M — a closed form for a general tower: all five coefficients are " +
        "determined by a finite computation from the coefficients of the spectral polynomial",
    },
    labels: ["paper_prop_M"],
    habitat: "Qbar",
    verification: [
      "sagemath/check/cycle21_T3_general_closed_form",
      "sagemath/check/cycle21_T3_b_star",
    ],
    statement: [
      paragraph([
        "We keep the setting of ",
        ref("paper_prop_K"),
        ". Here ",
        math(String.raw`X`),
        " is a finite connected voltage multigraph of rank two, that is ",
        math(String.raw`\Gamma\cong\mathbb{Z}_\ell^2`),
        ", and we put ",
        math(String.raw`D=\det L(z,w)`),
        ", ",
        math(String.raw`\mu=v_\ell(\mathrm{content}\,D)`),
        ", ",
        math(String.raw`E=\ell^{-\mu}D`),
        ", ",
        math(String.raw`\tilde E=z^{r}w^{s}E\in\mathbb{Z}[z,w]`),
        " and ",
        math(String.raw`\kappa_n=\kappa(X_{\ell^n,\ell^n})`),
        ". The hypothesis **(H)** (every stage is connected) is in force throughout. We use ",
        ref("paper_prop_J"),
        " and its ",
        math(String.raw`\theta`),
        ", ",
        math(String.raw`\hat\theta_M`),
        ", ",
        math(String.raw`\Theta_M=\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}\hat\theta_M(P)`),
        ", together with ",
        ref("paper_prop_K"),
        " and its ",
        math(String.raw`S_\infty`),
        ", ",
        math(String.raw`j^{*}(P_0)`),
        ", ",
        math(String.raw`e_{j^*}`),
        ".",
      ]),
      paragraph([
        "**(M1 Twisted stage data)** Let ",
        math(String.raw`P_0\in S_\infty`),
        " correspond to the primitive integer vector ",
        math(String.raw`u=(u_1,u_2)`),
        ", let ",
        math(String.raw`\det(u,\mathbf{e})=1`),
        " determine an integer vector ",
        math(String.raw`\mathbf{e}`),
        " (the extended Euclidean algorithm produces one), let ",
        math(String.raw`\eta`),
        " be a primitive ",
        math(String.raw`\ell^{k}`),
        "-th root of unity, and put ",
        math(String.raw`\mathcal{O}_k=\mathbb{Z}[\eta]`),
        ". Set",
      ]),
      displayMath(
        String.raw`\Phi^{[k]}_{u}(x):=\tilde E\bigl((1+x)^{u_1}\eta^{\mathbf{e}_1},\,(1+x)^{u_2}\eta^{\mathbf{e}_2}\bigr)
        =\sum_{m}A^{[k]}_m\,x^{m}\ \in\mathcal{O}_k[x]`,
      ),
      paragraph([
        "(when negative powers of ",
        math(String.raw`(1+x)`),
        " occur, drop the monomial factor; it is a unit and does not change valuations), and",
      ]),
      displayMath(
        String.raw`\Lambda_k:=\min_m v_\ell\bigl(A^{[k]}_m\bigr),\qquad
        \theta^\sharp_k:=\min\bigl\{m:v_\ell(A^{[k]}_m)=\Lambda_k\bigr\},\qquad
        m^\sharp_k:=\min\bigl\{m<\theta^\sharp_k:A^{[k]}_m\neq0\bigr\}`,
      ),
      paragraph([
        "(if no such ",
        math(String.raw`m`),
        " exists, put ",
        math(String.raw`m^\sharp_k=\infty`),
        "). ",
        "**Convention (it must not be omitted)**: the ",
        math(String.raw`\Lambda_k`),
        " and ",
        math(String.raw`\theta^\sharp_k`),
        " above are ",
        math(String.raw`\min`),
        " taken only over those ",
        math(String.raw`A^{[k]}_m\neq0`),
        ", that is, over the indices ",
        math(String.raw`m`),
        " at which the coefficient does not vanish (with the convention ",
        math(String.raw`v_\ell(0)=+\infty`),
        "). Reading it instead with ",
        math(String.raw`v_\ell(0)=0`),
        ", as most computer algebra systems and formalisation libraries do by default, makes ",
        math(String.raw`p=1`),
        " — one of the two towers carrying (U4) below — give a value of ",
        math(String.raw`\Lambda_1`),
        " which should be ",
        math(String.raw`2`),
        " but comes out as ",
        math(String.raw`0`),
        ", which changes ",
        math(String.raw`c`),
        " and destroys the conclusion. We normalise ",
        math(String.raw`v_\ell`),
        " by ",
        math(String.raw`v_\ell(\ell)=1`),
        " and compute it by ",
        math(String.raw`v_\ell(\alpha)=v_\ell\bigl(N_{\mathbb{Q}(\eta)/\mathbb{Q}}(\alpha)\bigr)/\varphi(\ell^{k})`),
        ", that is, by a single valuation of an integer. Then:",
      ]),
      list([
        [
          math(String.raw`\Lambda_k,\theta^\sharp_k,m^\sharp_k`),
          " are the same at every point of the layer of depth ",
          math(String.raw`k`),
          " (the points are carried into one another by Galois conjugation, and there is a single place above ",
          math(String.raw`\ell`),
          ").",
        ],
        [
          "If ",
          math(String.raw`M\ge2k`),
          " and ",
          math(String.raw`\varphi(\ell^{M})>\bigl(\theta^\sharp_k-m^\sharp_k\bigr)\varphi(\ell^{k})`),
          ", then at **every** point of the layer of depth ",
          math(String.raw`k`),
          " one has ",
          math(String.raw`\hat\theta_M(P)=\varphi(\ell^{M})\Lambda_k+\theta^\sharp_k`),
          ". Here, when ",
          math(String.raw`m^\sharp_k=\infty`),
          ", that is when no coefficient with ",
          math(String.raw`m<\theta^\sharp_k`),
          " is non-zero, the difference ",
          math(String.raw`\theta^\sharp_k-m^\sharp_k`),
          " is to be read as ",
          math(String.raw`-\infty`),
          " and **the inequality is then satisfied automatically** (the difference is used in the proof only to rule out that a term with ",
          math(String.raw`m<\theta^\sharp_k`),
          " and ",
          math(String.raw`A^{[k]}_m\neq0`),
          " steals the minimum, so if no such ",
          math(String.raw`m`),
          " exists the condition is vacuous).",
        ],
        [
          math(String.raw`\varphi(\ell^{k})\Lambda_k\in\mathbb{Z}_{\ge1}`),
          ". In particular the right-hand side above is an integer.",
        ],
      ]),
      paragraph([
        "Here is the definition of the layers. The layer of depth ",
        math(String.raw`k`),
        " consists of those points of level ",
        math(String.raw`M`),
        " whose distance from ",
        math(String.raw`P_0`),
        " is exactly ",
        math(String.raw`\ell^{-(M-k)}`),
        ", that is, the points ",
        math(String.raw`P=\bigl(u+\ell^{M-k}t\,\mathbf{e}\bigr)\bmod\ell^{M}`),
        " with ",
        math(String.raw`t\in(\mathbb{Z}/\ell^{k})^{\times}`),
        " and ",
        math(String.raw`k\ge1`),
        ", together with the single point of depth ",
        math(String.raw`k=0`),
        ". As for the number of points: when ",
        math(String.raw`k\ge1`),
        " it is ",
        math(String.raw`\varphi(\ell^{k})`),
        ", and when ",
        math(String.raw`k=0`),
        " it is ",
        math(String.raw`1`),
        ". ",
        "**The content of (M1) is this: inspecting a neighbourhood of the base point amounts to " +
          "twisting a one-parameter subgroup by a root of unity, and once the ring of coefficients " +
          "is enlarged from the integers to the cyclotomic integers, the tie between minimising " +
          "indices is broken.**",
        " The root used for the twist is ",
        math(String.raw`\eta`),
        ", the enlarged ring is ",
        math(String.raw`\mathcal{O}_k`),
        ", and the tie is what occurs if the minimisation of ",
        ref("paper_prop_J"),
        " is carried out over ",
        math(String.raw`\mathbb{Z}`),
        " instead.",
      ]),
      paragraph([
        "**(M2 An explicit bound for the saturation depth)** Put ",
        math(String.raw`K(P_0):=\max\bigl\{k\ge0:\ j^{*}\ell\ge(\ell-1)\ell^{k}\bigr\}`),
        " (this is always defined, and ",
        math(String.raw`j^{*}\ge1`),
        " gives ",
        math(String.raw`K\ge0`),
        "). Let further ",
        math(String.raw`\lambda(P_0)`),
        " be the least natural number with ",
        math(String.raw`\ell^{\lambda}\ge e_{j^*}+1`),
        " (it is determined by comparing powers of ",
        math(String.raw`\ell`),
        " one after another, so no real logarithm is involved; its value equals ",
        math(String.raw`\lceil\log_\ell(e_{j^*}+1)\rceil`),
        "). On a layer of depth ",
        math(String.raw`k>K`),
        " one has, as soon as ",
        math(String.raw`M\ge\max\bigl(R'+k,\ k+\lambda\bigr)`),
        ",",
      ]),
      displayMath(String.raw`\hat\theta_M(P)=\theta(P)=e_{j^*}+j^{*}\ell^{\,M-k},`),
      paragraph(["that is,"]),
      displayMath(
        String.raw`\Lambda_k=\frac{j^{*}}{\varphi(\ell^{k})},\qquad\theta^\sharp_k=e_{j^*}`,
      ),
      paragraph([
        "(here ",
        math(String.raw`R'(P_0)`),
        " is the smallest level at or above the ",
        ref("paper_prop_K"),
        " (K5) quantity ",
        math(String.raw`r_0`),
        " at which the ",
        math(String.raw`\mathrm{argmin}`),
        " becomes the single index ",
        math(String.raw`j^{*}`),
        "). In particular ",
        math(String.raw`K=0`),
        " is equivalent to ",
        math(String.raw`j^{*}\le\ell-2`),
        ", and for ",
        math(String.raw`\ell=2`),
        " one has ",
        math(String.raw`j^{*}\ge1=\ell-1`),
        ", so necessarily ",
        math(String.raw`K\ge1`),
        ". ",
        "**Thus saturation is not a phenomenon peculiar to the 2-adic towers; this is what lies behind it.** ",
        "The saturation that ",
        ref("paper_prop_G_ell2"),
        " exhibited as peculiar to ",
        math(String.raw`\ell=2`),
        " is merely a consequence of ",
        math(String.raw`\ell-1`),
        " being as small as possible there. The same thing happens for odd ",
        math(String.raw`\ell`),
        " in towers with ",
        math(String.raw`j^{*}\ge\ell-1`),
        ". Note that ",
        math(String.raw`K`),
        " is an upper bound: it does not assert that the layers with ",
        math(String.raw`k\le K`),
        " are actually saturated (by (U1a) below, taking ",
        math(String.raw`K`),
        " larger than necessary leaves the five coefficients unchanged).",
      ]),
      paragraph([
        "**(M3 A closed form for the level sums)** Put ",
        math(String.raw`r^\sharp:=\max\bigl(r_0,\ \max_{P_0}R'(P_0)\bigr)`),
        " and ",
        math(String.raw`U:=\mathbb{P}^1(\mathbb{Z}_\ell)\setminus\bigcup_{P_0\in S_\infty}B(P_0,\ell^{-r^\sharp})`),
        ". Then ",
        math(String.raw`\theta`),
        " is bounded on ",
        math(String.raw`U`),
        ", and a bound for it can be computed from the coefficients of ",
        math(String.raw`D`),
        ". Write ",
        math(String.raw`\theta^{\max}_U`),
        " for the maximum, take a level ",
        math(String.raw`L\ge r^\sharp`),
        " with ",
        math(String.raw`\ell^{L}\ge\theta^{\max}_U`),
        ", and set",
      ]),
      displayMath(
        String.raw`A_{\mathrm{gen}}:=\frac{1}{\ell^{L}}\sum_{P\in U\cap\mathbb{P}^1(\mathbb{Z}/\ell^{L})}\theta(P)\ \in\mathbb{Q}`,
      ),
      paragraph([
        "(this does not depend on the choice of ",
        math(String.raw`L`),
        ", since passing from ",
        math(String.raw`L\to L+1`),
        " splits every fibre uniformly into ",
        math(String.raw`\ell`),
        " points). Then there is an explicit ",
        math(String.raw`M^{*}`),
        ", given by (M5) below, such that for ",
        math(String.raw`M\ge M^{*}`),
        " one has",
      ]),
      displayMath(String.raw`\Theta_M=\alpha\,M\ell^{M}+\beta\,\ell^{M}+\gamma`),
      paragraph([
        "where",
      ]),
      displayMath(String.raw`\alpha=\frac{\ell-1}{\ell}\sum_{P_0\in S_\infty}j^{*}(P_0),`),
      displayMath(
        String.raw`\beta=A_{\mathrm{gen}}+\sum_{P_0\in S_\infty}\Bigl[\frac{e_{j^*}}{\ell^{\,r^\sharp}}
        -\frac{(\ell-1)\,j^{*}\,(K+r^\sharp)}{\ell}
        +\frac{\ell-1}{\ell}\Bigl(\Lambda_0+\sum_{k=1}^{K}\varphi(\ell^{k})\Lambda_k\Bigr)\Bigr],`,
      ),
      displayMath(
        String.raw`\gamma=\sum_{P_0\in S_\infty}\Bigl[-e_{j^*}\,\ell^{K}
        +\theta^\sharp_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\theta^\sharp_k\Bigr].`,
      ),
      paragraph([
        "**(M4 The five coefficients)** Consequently, for ",
        math(String.raw`n\ge M^{*}-1`),
        " the five-coefficient form of ",
        ref("paper_prop_J"),
        " (J4), namely ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=a\,\ell^{2n}+b\,n\ell^{n}+c\,\ell^{n}+d\,n+e`),
        ", holds with",
      ]),
      displayMath(
        String.raw`a=\mu,\qquad b=\frac{\ell}{\ell-1}\,\alpha=\sum_{P_0\in S_\infty}j^{*}(P_0),\qquad
        c=\frac{\ell}{\ell-1}\,\beta-\frac{\ell}{(\ell-1)^2}\,\alpha,\qquad d=\gamma-2,`,
      ),
      displayMath(
        String.raw`e=v_\ell(\kappa(X))-\mu
        +\Bigl[\sum_{M=1}^{M^{*}-1}\Theta_M-\alpha\,\mathcal{S}_1(M^{*}\!-\!1)-\beta\,\mathcal{S}_0(M^{*}\!-\!1)-\gamma(M^{*}\!-\!1)\Bigr]
        +\frac{\ell}{(\ell-1)^2}\alpha-\frac{\ell}{\ell-1}\beta,`,
      ),
      paragraph([
        "where ",
        math(String.raw`\mathcal{S}_1(N)=\sum_{M=1}^{N}M\ell^{M}`),
        " and ",
        math(String.raw`\mathcal{S}_0(N)=\sum_{M=1}^{N}\ell^{M}`),
        ". ",
        "**Thus all five coefficients are determined by a finite computation from the coefficients " +
          "of the spectral polynomial.** ",
        "In particular ",
        math(String.raw`b=\sum j^{*}`),
        " follows from ",
        ref("paper_prop_J"),
        " (J4) **without** the hypothesis that the latter left open, namely the uniqueness of the index realising ",
        math(String.raw`\hat\theta`),
        ". The reason is that uniqueness is **proved**, rather than assumed, both on the saturated ",
        "layers (M1) and on the unsaturated ones (M2).",
      ]),
      paragraph([
        "**(M5 An explicit form for the level at which the closed form starts)** It suffices to take for ",
        math(String.raw`M`),
        " the smallest value ",
        math(String.raw`M^{*}`),
        " satisfying all of the following. Each of them is a finite computation from the coefficients of ",
        math(String.raw`D`),
        ".",
      ]),
      list([
        [
          math(String.raw`M\ge L`),
          " (",
          math(String.raw`A_{\mathrm{gen}}`),
          " factors through this level).",
        ],
        [
          math(String.raw`M\ge r^\sharp+\max_{P_0}K(P_0)`),
          ". **This condition says that the closed form for the unsaturated layers holds; it does " +
            "not say that those layers are non-empty.** For ",
          math(String.raw`M=r^\sharp+\max K`),
          " the unsaturated layers are empty, but the identity ",
          math(String.raw`\sum_{s=K+1}^{M-r^\sharp}\varphi(\ell^{s})=\ell^{M-r^\sharp}-\ell^{K}`),
          " still holds, both sides being ",
          math(String.raw`0`),
          ", so the closed form remains correct. It first fails for ",
          math(String.raw`M<r^\sharp+\max K`),
          ".",
        ],
        [
          math(String.raw`\varphi(\ell^{M})>\theta^{\max}_U-2`),
          " (the contribution of the generic region ",
          math(String.raw`U`),
          ").",
        ],
        [
          "For every ",
          math(String.raw`P_0`),
          " and every ",
          math(String.raw`k\le K`),
          ": ",
          math(String.raw`M\ge2k`),
          " and ",
          math(String.raw`\varphi(\ell^{M})>(\theta^\sharp_k-m^\sharp_k)\varphi(\ell^{k})`),
          " (see (M1); on a layer with ",
          math(String.raw`m^\sharp_k=\infty`),
          " this inequality is satisfied automatically).",
        ],
        [
          "For every ",
          math(String.raw`P_0`),
          ": ",
          math(String.raw`e_{j^*}+j^{*}\ell^{\,M-K-1}-2<\varphi(\ell^{M})`),
          " (the outermost layer ",
          math(String.raw`k=K+1`),
          " of (M2); for ",
          math(String.raw`M=r^\sharp+\max K`),
          " that layer is absent and the condition is vacuous).",
        ],
      ]),
      paragraph([
        "**(M6 The case with no point of infinite vanishing depth)** Whether ",
        math(String.raw`S_\infty=\emptyset`),
        " can be decided by ",
        ref("paper_prop_K"),
        " (K3). In that case ",
        math(String.raw`\alpha=\gamma=0`),
        " and ",
        math(String.raw`\beta=A_{\mathrm{gen}}`),
        ", so that",
      ]),
      displayMath(
        String.raw`a=\mu,\quad b=0,\quad c=\frac{\ell}{\ell-1}A_{\mathrm{gen}}=\frac{\Theta_L}{\varphi(\ell^{L})},\quad d=-2,`,
      ),
      displayMath(
        String.raw`e=v_\ell(\kappa(X))-\mu+\sum_{M=1}^{M^{*}-1}\Theta_M
        -A_{\mathrm{gen}}\Bigl(\mathcal{S}_0(M^{*}\!-\!1)+\frac{\ell}{\ell-1}\Bigr).`,
      ),
      paragraph([
        "**Explicit statement that the real field is not used**: all we use is (a) the valuation of ",
        math(String.raw`\mathcal{O}_k=\mathbb{Z}[\zeta_{\ell^{k}}]`),
        " — the ring of integers of a countable algebraic extension of ",
        math(String.raw`\mathbb{Q}`),
        " — at its unique prime above ",
        math(String.raw`\ell`),
        " (whose value is computed by taking ",
        math(String.raw`N_{\mathbb{Q}(\eta)/\mathbb{Q}}`),
        " and reducing to the valuation of a single integer in ",
        math(String.raw`\mathbb{Z}`),
        ", namely the one adic to ",
        math(String.raw`\ell`),
        "), (b) finite sums over ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        ", and (c) arithmetic of rational numbers in ",
        math(String.raw`\mathbb{Q}`),
        ". Moreover the ",
        math(String.raw`\lambda`),
        " of (M2) needs no real logarithm either: it is determined by comparing powers of ",
        math(String.raw`\ell`),
        " alone. **Hence this proposition never escapes to the real field.**",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "**The scope is limited to rank two, the hypothesis (H), and voltage graphs of Laplacian " +
            "determinant type.** In symbols this is ",
          math(String.raw`d=2`),
          " (",
          math(String.raw`\Gamma\cong\mathbb{Z}_\ell^2`),
          "), and the objects considered are only those of the form ",
          math(String.raw`\det L`),
          ". The twisting idea of (M1) still makes sense in rank three and higher, but the " +
            "stratification of ",
          math(String.raw`\mathbb{P}^{d-1}`),
          " is not as simple as that of ",
          math(String.raw`\mathbb{P}^1`),
          ", so the argument does not carry over as it stands.",
        ],
        [
          math(String.raw`M^{*}`),
          " is a sufficient condition and **no sharpness is claimed**. In experiments the closed form already holds for many towers from values of ",
          math(String.raw`M^{*}`),
          " smaller than the bound, that is from smaller ",
          math(String.raw`M`),
          ". Hence agreement or disagreement at ",
          math(String.raw`n<M^{*}-1`),
          " must not be used as a verification of this proposition.",
        ],
        [
          "No formula is given that reads ",
          math(String.raw`(\Lambda_k,\theta^\sharp_k)`),
          " off the combinatorial data of ",
          math(String.raw`\bar{\tilde E}\in\mathbb{F}_\ell[z,w]`),
          "; **none is given** because, as ",
          ref("paper_prop_U"),
          " shows, none can exist. What is given is a formula in terms of a finite computation from the coefficients of ",
          math(String.raw`D`),
          ".",
        ],
        [
          "The general statement ",
          math(String.raw`a\le\mu`),
          " remains borrowed from Cuoco–Monsky (within the scope of this proposition ",
          math(String.raw`a=\mu`),
          " comes directly from the expansion in ",
          ref("paper_prop_J"),
          ", so what is borrowed is only the general statement).",
        ],
        [
          "**No novelty is claimed.** The identities ",
          math(String.raw`a=\mu`),
          " and ",
          math(String.raw`b=\sum j^{*}`),
          " are Definitions 1.1 and 1.2 together with Theorem 1.7 of Cuoco–Monsky, *Class numbers in ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions*, Math. Ann. **255** (1981), 235–258, and the attribution is already recorded in ",
          ref("paper_prop_K"),
          " (K6). As for ",
          math(String.raw`c`),
          ", Monsky, *Fine estimates for the growth of ",
          math(String.raw`e_n`),
          " in ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions* (1989) writes of the constant ",
          math(String.raw`\alpha^{*}`),
          " occupying the same position that there is no easy description of ",
          math(String.raw`\alpha^{*}`),
          ", and proves only its existence and, for ",
          math(String.raw`d=2`),
          ", its rationality (Introduction, p.309; we obtained and read the original text). ",
          "The present proposition makes that quantity explicit in the restricted setting above, but " +
            "**the literature search is not exhaustive**. ",
          "In particular the operation in (M1) — twisting by a character of order a power of ",
          math(String.raw`\ell^{k}`),
          " to decompose, and taking the valuation of each component — is standard in Iwasawa theory, " +
            "so it is quite possible that the same computation has already been carried out for concrete objects such as those of ",
          math(String.raw`\det L`),
          " type.",
        ],
        [
          "The coefficient ",
          math(String.raw`d`),
          " of ",
          math(String.raw`n`),
          " and the constant term ",
          math(String.raw`e`),
          " lie outside Monsky's assertion to begin with: for ",
          math(String.raw`d=2`),
          " the error term of that paper is ",
          math(String.raw`O(n)`),
          ", so it does not reach them.",
        ],
      ]),
    ],
  },
  {
    id: "paper_112_theorem_coefficient_layers",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/010_general_closed_form.ts", ordinal: 3 },
    title: {
      text:
        "Proposition U — the information hierarchy of the coefficients (which extra data are " +
        "needed), and the failure of any fixed finite precision",
    },
    labels: ["paper_prop_U"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle22_T3_coefficients_d_e"],
    statement: [
      paragraph([
        "We keep the setting and the notation of ",
        ref("paper_prop_M"),
        ". For each ",
        math(String.raw`P_0\in S_\infty`),
        " put",
      ]),
      displayMath(
        String.raw`\mathcal{L}(P_0):=\Lambda_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\Lambda_k,
        \qquad
        \mathcal{T}(P_0):=\theta^\sharp_0+\sum_{k=1}^{K}\varphi(\ell^{k})\,\theta^\sharp_k`,
      ),
      paragraph([
        "By the third item of ",
        ref("paper_prop_M"),
        " (M1) we have ",
        math(String.raw`\varphi(\ell^{k})\Lambda_k\in\mathbb{Z}_{\ge1}`),
        ", and also ",
        math(String.raw`\theta^\sharp_k\in\mathbb{Z}_{\ge0}`),
        ", so that both ",
        math(String.raw`\mathcal{L}(P_0)`),
        " and ",
        math(String.raw`\mathcal{T}(P_0)`),
        " are non-negative integers.",
      ]),
      paragraph([
        "**(U1 The valuation side and the position side do different jobs)**",
      ]),
      displayMath(
        String.raw`d=\sum_{P_0\in S_\infty}\Bigl(\mathcal{T}(P_0)-e_{j^*}(P_0)\,\ell^{K(P_0)}\Bigr)-2,`,
      ),
      displayMath(
        String.raw`c=\frac{\ell}{\ell-1}A_{\mathrm{gen}}
        +\sum_{P_0\in S_\infty}\Bigl[\frac{e_{j^*}\,\ell^{\,1-r^\sharp}}{\ell-1}
        -j^{*}\Bigl(K+r^\sharp+\frac{1}{\ell-1}\Bigr)+\mathcal{L}(P_0)\Bigr].`,
      ),
      paragraph([
        "Thus the **valuation side** ",
        math(String.raw`\Lambda_k`),
        " of the twisted stage data enters only ",
        math(String.raw`c`),
        ", while the **position side** ",
        math(String.raw`\theta^\sharp_k`),
        " enters only ",
        math(String.raw`d`),
        ". In particular ",
        math(String.raw`d`),
        " is determined by the local quantities of ",
        math(String.raw`S_\infty`),
        ", namely ",
        math(String.raw`(\mathcal{T},e_{j^*},K)`),
        ", alone: neither the global average ",
        math(String.raw`A_{\mathrm{gen}}`),
        ", nor the transient at low levels, nor ",
        math(String.raw`v_\ell(\kappa(X))`),
        " is used. Hence ",
        math(String.raw`d`),
        " is **always an integer**, whereas ",
        math(String.raw`c`),
        " and ",
        math(String.raw`e`),
        " may be non-integral rationals such as ",
        math(String.raw`13/3`),
        ". If ",
        math(String.raw`S_\infty=\emptyset`),
        " the sum is empty and ",
        math(String.raw`d=-2`),
        ", in agreement with ",
        ref("paper_prop_M"),
        " (M6). ",
        "**Thus the naive expectation that the lower the degree the harder the coefficient does not hold.** The order of difficulty is",
        math(String.raw`a,b\ \prec\ d\ \prec\ c\ \prec\ e`),
        ".",
      ]),
      paragraph([
        "**(U1a One may take the saturation depth larger than necessary)** Let ",
        math(String.raw`K'\ge K(P_0)`),
        " be arbitrary, extend the sums above to ",
        math(String.raw`k\le K'`),
        " and replace ",
        math(String.raw`K`),
        " by ",
        math(String.raw`K'`),
        "; the values of ",
        math(String.raw`c,d`),
        " do not change. Indeed, for ",
        math(String.raw`K\to K+1`),
        " the layer of depth ",
        math(String.raw`K+1`),
        " is unsaturated by ",
        ref("paper_prop_M"),
        " (M2), so ",
        math(String.raw`\Lambda_{K+1}=j^{*}/\varphi(\ell^{K+1})`),
        " and ",
        math(String.raw`\theta^\sharp_{K+1}=e_{j^*}`),
        "; the increment ",
        math(String.raw`\mathcal{T}`),
        " receives, namely ",
        math(String.raw`\varphi(\ell^{K+1})e_{j^*}`),
        ", and the change ",
        math(String.raw`-e_{j^*}\ell^{K}\to-e_{j^*}\ell^{K+1}`),
        " cancel exactly (",
        math(String.raw`\varphi(\ell^{K+1})-\ell^{K+1}+\ell^{K}=0`),
        "), and likewise the increment ",
        math(String.raw`\mathcal{L}`),
        " receives, namely ",
        math(String.raw`j^{*}`),
        ", cancels against the change in ",
        math(String.raw`-j^{*}(K+r^\sharp+\frac{1}{\ell-1})`),
        ". The rest is induction.",
      ]),
      paragraph([
        "**(U2 A decomposition of the constant term)** Put ",
        math(String.raw`\Theta^{\mathrm{as}}_M:=\alpha M\ell^{M}+\beta\ell^{M}+\gamma`),
        " and define the **transient defect** by",
      ]),
      displayMath(String.raw`T_{\mathrm{def}}:=\sum_{M\ge1}\bigl(\Theta_M-\Theta^{\mathrm{as}}_M\bigr)`),
      paragraph([
        "By ",
        ref("paper_prop_M"),
        " (M3) the terms with ",
        math(String.raw`M\ge M^{*}`),
        " are ",
        math(String.raw`0`),
        ", so this is a **finite sum**, and it does not depend on the choice of ",
        math(String.raw`M^{*}`),
        " (truncating anywhere at or above ",
        math(String.raw`M^{*}`),
        " gives the same value). Then",
      ]),
      displayMath(String.raw`e=v_\ell(\kappa(X))-a-c+T_{\mathrm{def}},`),
      paragraph(["and therefore"]),
      displayMath(String.raw`c+e=v_\ell(\kappa(X))-a+T_{\mathrm{def}}.`),
      paragraph([
        "Moreover, putting ",
        math(String.raw`\delta_M:=\Theta_M-\Theta^{\mathrm{as}}_M`),
        " and ",
        math(String.raw`S(n):=\sum_{M=1}^{n}\delta_M`),
        " (so that ",
        math(String.raw`S(0)=0`),
        "), the discrepancy from the closed form at level ",
        math(String.raw`n`),
        " equals ",
        math(String.raw`S(n)-T_{\mathrm{def}}`),
        ". Hence the following three statements hold.",
      ]),
      list([
        [
          "The closed form holds at level ",
          math(String.raw`n`),
          " if and only if ",
          math(String.raw`S(n)=T_{\mathrm{def}}`),
          ".",
        ],
        [
          math(String.raw`T_{\mathrm{def}}=0`),
          " is equivalent to **the closed form holding at the first level**, that is at ",
          math(String.raw`n=0`),
          ", because ",
          math(String.raw`S(0)=0`),
          ".",
        ],
        [
          "**The closed form holding at every level**, that is for all ",
          math(String.raw`n\ge0`),
          ", is equivalent to ",
          math(String.raw`\Theta_M=\Theta^{\mathrm{as}}_M`),
          " holding for every ",
          math(String.raw`M\ge1`),
          ", that is to the absence of any transient, ",
          math(String.raw`\delta_M=0`),
          ", and this is **strictly stronger** than ",
          math(String.raw`T_{\mathrm{def}}=0`),
          ".",
        ],
      ]),
      paragraph([
        "The converse of the third statement, from ",
        math(String.raw`T_{\mathrm{def}}=0`),
        " to all ",
        math(String.raw`n\ge0`),
        ", **fails**. A total sum may vanish while a partial sum does not: ",
        math(String.raw`0`),
        " is the value of the whole series, but not necessarily of ",
        math(String.raw`S(n)`),
        ", which need not be ",
        math(String.raw`0`),
        ". For a counterexample take ",
        math(String.raw`\Theta^{\mathrm{as}}\equiv0`),
        ", ",
        math(String.raw`\Theta_1=1`),
        ", ",
        math(String.raw`\Theta_2=-1`),
        " and ",
        math(String.raw`\Theta_M=0\ (M\ge3)`),
        ": then ",
        math(String.raw`T_{\mathrm{def}}=0`),
        " while ",
        math(String.raw`S(1)=1`),
        ", so that at ",
        math(String.raw`n=1`),
        " the closed form is off by ",
        math(String.raw`1`),
        ". **The scope of this counterexample is limited**: it takes ",
        math(String.raw`\Theta`),
        " arbitrarily, and no genuine tower in which the signs of ",
        math(String.raw`\delta_M`),
        " are mixed has been exhibited. The bracket in the formula for ",
        ref("paper_prop_M"),
        " (M4), that is for ",
        math(String.raw`e`),
        ", is exactly this ",
        math(String.raw`T_{\mathrm{def}}`),
        ". Note that the **sum** of ",
        math(String.raw`c`),
        " and ",
        math(String.raw`e`),
        " lies at a shallower informational level than either of them separately.",
      ]),
      paragraph([
        "**(U3 The information hierarchy has three layers)** The five coefficients split into three layers. The first layer, that of ",
        math(String.raw`\bmod\ \ell`),
        ", consists of the quantities readable from ",
        math(String.raw`\bar{\tilde E}`),
        ": the valuation of the content, the multiplicities of the binomial factors, ",
        math(String.raw`S_\infty`),
        ", ",
        math(String.raw`j^{*}`),
        ", ",
        math(String.raw`e_j`),
        ", ",
        math(String.raw`K`),
        ", ",
        math(String.raw`r^\sharp`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}`),
        "; from these, ",
        math(String.raw`a`),
        " and ",
        math(String.raw`b`),
        " are determined. The second layer adds the twisted stage data ",
        math(String.raw`(\Lambda_k,\theta^\sharp_k)`),
        " to the first, and from these ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        " are determined. The third layer adds ",
        math(String.raw`v_\ell(\kappa(X))`),
        " and ",
        math(String.raw`T_{\mathrm{def}}`),
        " to the second, and only ",
        math(String.raw`e`),
        " requires it. ",
        "**This is the answer to the question of which extra data are needed.** The items (U4)–(U6) below show that the hierarchy really does not collapse: the second layer does not reduce to the first, and it does not reduce to any fixed number of ",
        math(String.raw`\ell`),
        "-adic digits either.",
      ]),
      paragraph([
        "**(U4 The residue field alone does not determine the second layer)** The break lies between ",
        math(String.raw`b`),
        " and ",
        math(String.raw`(c,d,e)`),
        ". Let ",
        math(String.raw`\ell=2`),
        ", let ",
        math(String.raw`p`),
        " be odd, and let ",
        math(String.raw`X_p`),
        " be the one-vertex bouquet whose loops carry the voltages ",
        math(String.raw`(1,0)`),
        " with multiplicity ",
        math(String.raw`p`),
        " and ",
        math(String.raw`(0,1)`),
        " once. Then ",
        math(String.raw`\mu=0`),
        " and ",
        math(String.raw`\bar{\tilde E}=z^2w+zw^2+z+w\in\mathbb{F}_2[z,w]`),
        " is independent of the odd number ",
        math(String.raw`p`),
        ". Hence so are ",
        math(String.raw`S_\infty=\{(1{:}1),(1{:}-1)\}`),
        " and, at each of its points, ",
        math(String.raw`j^{*}=1`),
        ", ",
        math(String.raw`e_{j^*}=2`),
        ", ",
        math(String.raw`K=1`),
        ", ",
        math(String.raw`r^\sharp=2`),
        " and ",
        math(String.raw`A_{\mathrm{gen}}=2`),
        ". Nevertheless, at each ",
        math(String.raw`P_0\in S_\infty`),
        ",",
      ]),
      displayMath(
        String.raw`\Lambda_0=v_2(p+1),\quad\theta^\sharp_0=2,\qquad
        \Lambda_1=\min\bigl(2,\,v_2(p-1)\bigr),\qquad
        \theta^\sharp_1=\begin{cases}0&(p\equiv1\bmod4)\\ 2&(p\equiv3\bmod4)\end{cases}`,
      ),
      paragraph([
        "Here the ",
        math(String.raw`\min`),
        " is taken, as in the convention of (M1), only over those ",
        math(String.raw`A_m\neq0`),
        ", that is over the indices ",
        math(String.raw`m`),
        " with non-vanishing coefficient (",
        math(String.raw`v_2(0)=+\infty`),
        "). At depth ",
        math(String.raw`1`),
        " the coefficients are ",
        math(String.raw`A_0=A_1=4`),
        " and ",
        math(String.raw`A_2=1-p`),
        ", so **for one of the two towers the last coefficient vanishes**: for ",
        math(String.raw`p=1`),
        " we get ",
        math(String.raw`A_2=0`),
        ", hence ",
        math(String.raw`\Lambda_1=v_2(4)=2`),
        " and ",
        math(String.raw`\theta^\sharp_1=0`),
        " (which is the case ",
        math(String.raw`p\equiv1\bmod4`),
        "). Dropping the convention and reading ",
        math(String.raw`v_2(0)=0`),
        " would give ",
        math(String.raw`\Lambda_1=0`),
        " and the values below would not come out. By (U1) we have ",
        math(String.raw`d=2\theta^\sharp_1-6`),
        " and ",
        math(String.raw`c=2\mathcal{L}-2`),
        " with ",
        math(String.raw`\mathcal{L}=v_2(p+1)+\min(2,v_2(p-1))`),
        ". In particular ",
        math(String.raw`p=1`),
        " and ",
        math(String.raw`p=3`),
        " have the same ",
        math(String.raw`\bar{\tilde E}`),
        " and the same ",
        math(String.raw`\mu`),
        ", and yet",
      ]),
      displayMath(String.raw`(a,b,c,d,e)=(0,2,4,-6,-1)`),
      paragraph(["and"]),
      displayMath(String.raw`(a,b,c,d,e)=(0,2,4,-2,-4)`),
      paragraph([
        "respectively, so that ",
        math(String.raw`d`),
        " and ",
        math(String.raw`e`),
        " differ. **Hence none of the three coefficients of the second and third layers is a function of the reduction over the residue field.** In symbols, none of ",
        math(String.raw`c,d,e`),
        " is a function of ",
        math(String.raw`\bar{\tilde E}`),
        ". The counterexample uses **two genuine voltage graphs**, not an artificial perturbation of ",
        math(String.raw`\tilde E`),
        ", so the escape route “perhaps for graphs there is a formula readable from ",
        math(String.raw`\bar{\tilde E}`),
        "” is closed off as well. ",
        "Note that in this pair ",
        math(String.raw`\mathcal{L}`),
        " happens to coincide, so that ",
        math(String.raw`c`),
        " is the same for both. That is, the counterexample for ",
        math(String.raw`c`),
        " and the counterexample for ",
        math(String.raw`d`),
        " are needed separately.",
      ]),
      paragraph([
        "**(U5 No fixed precision suffices)** Let ",
        math(String.raw`\ell=2`),
        ".",
      ]),
      list([
        [
          "(the side of ",
          math(String.raw`c`),
          ") Let ",
          math(String.raw`X_t`),
          " be the one-vertex bouquet whose loops carry the voltages ",
          math(String.raw`(1,0)`),
          " and ",
          math(String.raw`(0,1)`),
          " once each and ",
          math(String.raw`(1,-1)`),
          " with multiplicity ",
          math(String.raw`t`),
          ". For every ",
          math(String.raw`N\ge2`),
          ", the values ",
          math(String.raw`t=2^{N+1}-1`),
          " and ",
          math(String.raw`t'=3\cdot2^{N}-1`),
          " give ",
          math(String.raw`\tilde E_{X_t}\equiv\tilde E_{X_{t'}}\pmod{2^{N}}`),
          " (hence the same ",
          math(String.raw`\bar{\tilde E}`),
          " and the same ",
          math(String.raw`\mu`),
          "), and yet ",
          math(String.raw`c`),
          " differs by ",
          math(String.raw`3`),
          ". Here ",
          math(String.raw`\theta^\sharp`),
          " is unchanged at all four places, so ",
          math(String.raw`d`),
          " does not change.",
        ],
        [
          "(the side of ",
          math(String.raw`d`),
          ") Let ",
          math(String.raw`Y_{p,q,t}`),
          " be the one-vertex bouquet whose loops carry the voltages ",
          math(String.raw`(1,0)`),
          " with multiplicity ",
          math(String.raw`p`),
          ", ",
          math(String.raw`(0,1)`),
          " with multiplicity ",
          math(String.raw`q`),
          " and ",
          math(String.raw`(1,-1)`),
          " with multiplicity ",
          math(String.raw`t`),
          ". At ",
          math(String.raw`P_0=(0{:}1)`),
          " the coefficients at depth ",
          math(String.raw`1`),
          " are ",
          math(String.raw`A_0=A_1=4(p+t)`),
          " and ",
          math(String.raw`A_2=t-q`),
          ", and here too the ",
          math(String.raw`\min`),
          " is taken only over those ",
          math(String.raw`A_m\neq0`),
          ", that is over the indices ",
          math(String.raw`m`),
          " with non-vanishing coefficient (for ",
          math(String.raw`t=q`),
          " one has ",
          math(String.raw`A_2=0`),
          ", hence ",
          math(String.raw`\Lambda_1=v_2(4(p+t))`),
          " and ",
          math(String.raw`\theta^\sharp_1=0`),
          "). For every ",
          math(String.raw`N\ge1`),
          ", the values ",
          math(String.raw`t=1`),
          ", ",
          math(String.raw`q=1+2^{N+2}`),
          ", ",
          math(String.raw`p=2^{N+1}-1`),
          " and ",
          math(String.raw`p'=p+2^{N}`),
          " (for which ",
          math(String.raw`t\neq q`),
          ", so that the boundary case above is not touched) give ",
          math(String.raw`\tilde E_{Y_{p,q,t}}\equiv\tilde E_{Y_{p',q,t}}\pmod{2^{N}}`),
          ", while ",
          math(String.raw`\Lambda_1`),
          " stays **unchanged** at ",
          math(String.raw`N+2`),
          " and ",
          math(String.raw`\theta^\sharp_1`),
          " passes from ",
          math(String.raw`2`),
          " to ",
          math(String.raw`0`),
          ", so that ",
          math(String.raw`d`),
          " differs by ",
          math(String.raw`2`),
          ".",
        ],
      ]),
      paragraph([
        "**Hence, however many digits one fixes when reducing to a residue ring, no formula reading the coefficients of the second layer off that reduction exists.** ",
        "Writing the reduction as ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        ", this says that no formula for ",
        math(String.raw`c`),
        " and no formula for ",
        math(String.raw`d`),
        " exists for any fixed ",
        math(String.raw`N`),
        ". Moreover the obstruction on the position side, that is for ",
        math(String.raw`d`),
        ", is not a consequence of the obstruction on the valuation side, that is for ",
        math(String.raw`c`),
        ": in the second family ",
        math(String.raw`\Lambda_1`),
        " stays fixed while only ",
        math(String.raw`\theta^\sharp_1`),
        " moves.",
      ]),
      paragraph([
        "**(U6 The converse — when the precision does suffice)** Conversely, if ",
        math(String.raw`N\in\mathbb{Z}_{\ge1}`),
        " satisfies",
      ]),
      displayMath(
        String.raw`N>\max_{P_0\in S_\infty}\ \max_{0\le k\le K(P_0)}\Lambda_k(P_0)`,
      ),
      paragraph([
        "then ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        " determines ",
        math(String.raw`(\Lambda_k,\theta^\sharp_k)_{k\le K}`),
        " and hence ",
        math(String.raw`c`),
        " and ",
        math(String.raw`d`),
        ". Indeed, writing ",
        math(String.raw`\tilde E'=\tilde E+\ell^{N}h`),
        ", the polynomial ",
        math(String.raw`\Phi^{[k]}`),
        " is an ",
        math(String.raw`\mathcal{O}_k`),
        "-linear form in the coefficients, so ",
        math(String.raw`A'^{[k]}_m=A^{[k]}_m+\ell^{N}\beta_m`),
        ", and therefore the **truncated valuation sequence** ",
        math(String.raw`\bigl(\min(v_\ell(A^{[k]}_m),N)\bigr)_m`),
        " is determined by ",
        math(String.raw`\tilde E\bmod\ell^{N}`),
        ". The condition gives ",
        math(String.raw`\Lambda_k<N`),
        ", so both ",
        math(String.raw`\Lambda_k`),
        " and the first index realising that minimum, namely ",
        math(String.raw`\theta^\sharp_k`),
        ", can be read off that sequence. ",
        "The condition looks self-referential, but in practice it is not circular: compute the truncated valuation sequence for a given ",
        math(String.raw`N`),
        ", and if the minimum is smaller than ",
        math(String.raw`N`),
        ", that very computation certifies that this ",
        math(String.raw`N`),
        " was large enough. ",
        "**The required precision is finite and computable for each tower, but it is not bounded above as the tower varies** (that is exactly (U5)).",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "**That the third layer is not superfluous**, that is, that ",
          math(String.raw`v_\ell(\kappa(X))`),
          " together with the whole second layer still fails to determine ",
          math(String.raw`e`),
          ", is an **existence statement**, supported by explicit towers found by scanning a population (an example suffices to establish existence). It is not a general proof.",
        ],
        [
          "(U6) concerns ",
          math(String.raw`c,d`),
          ". The constant term ",
          math(String.raw`e`),
          " involves ",
          math(String.raw`T_{\mathrm{def}}`),
          ", where the low-level sums ",
          math(String.raw`\Theta_M`),
          " occur and hence arbitrarily deep valuations, so that ",
          "**no explicit form for the precision required by the constant term is given.**",
        ],
        [
          "**The scope is limited to rank two, the hypothesis (H), and voltage graphs of Laplacian determinant type.** ",
          "This is the same restriction as in ",
          ref("paper_prop_M"),
          "; in symbols, ",
          math(String.raw`d=2`),
          " and the objects considered are only those of the form ",
          math(String.raw`\det L`),
          ".",
        ],
        [
          "**No novelty is claimed.** That the coefficients at and below ",
          math(String.raw`\ell^{n}`),
          " are not determined ",
          math(String.raw`\bmod\ \ell`),
          ", that is, that the terms below ",
          math(String.raw`\mu,\lambda`),
          " require deeper information, is a standard understanding in Iwasawa theory. ",
          "What this proposition adds is the finiteness statement that this deeper information is **exhausted** by the stage data over ",
          math(String.raw`\mathbb{Z}[\zeta_{\ell^{k}}]`),
          ", together with counterexamples establishing that no fixed number of ",
          math(String.raw`\ell`),
          "-adic digits suffices.",
        ],
      ]),
    ],
  },
]);
