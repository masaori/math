/**
 * 論文本体 第 11 章（英語版）: 消滅深度の桁枝再帰（命題 R）。
 *
 * **正本は日本語版 `../../../content/009_theta_recursion.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする。
 *
 * ## 訳出で構造を変えた箇所（内容は変えていない）
 *
 * 日本語版は `**強調**` の中に数式ノード・`ref` を含めて書いている箇所が 3 つある（(R2) の
 * 「s* は必ず存在する」と「打ち消しは構造的に起こらない」、(R4) の「(J3) の仮定を使わない」）。
 * 英語版の生成器は**ノードをまたぐ `**` を拒否する**ので、強調文を数式なしで言い切り、
 * その直後に記号を対応づける文を置いた（用語集 §1.2d の前例と同じ扱い）。
 * **数式ノードの多重集合は日本語版と一致させてある。**
 *
 * ## 数式の中の日本語（`structure-exceptions.ts` へ登録した）
 *
 * (R3) の `\mathrm{sep}` の定義と、その下の displayMath の但し書きが `\text{}` の中に日本語を含む。
 * 英語版は和文フォントを読み込まないので、そのままだと PDF から無言で消える。
 * `\text{}` の中身を英訳した。日本語が後置修飾なので、英語として意味の通る語順にするために
 * `\text{}` の前後の記号の順を入れ替えた箇所がある。**記号そのものは 1 つも足しても消してもいない。**
 */

import { defineTranslatedBlocks as defineBlocks, displayMath, list, math, paragraph, refInTranslation as ref } from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_100_heading_theta_recursion",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/009_theta_recursion.ts", ordinal: 1 },
    title: {
      text:
        "No cancellation occurs — the digit-branch recursion for the vanishing depth, and the " +
        "valuation as a resultant",
    },
    labels: [],
  },
  {
    id: "paper_101_theorem_digit_branch",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/009_theta_recursion.ts", ordinal: 2 },
    title: {
      text:
        "Proposition R — the vanishing depth determined by the digit-branch recursion, and the " +
        "valuation as a resultant",
    },
    labels: ["paper_prop_R"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle20_T3_cancellation"],
    lean: [
      "IntegrableLattice.sigma_eq_of_max",
      "IntegrableLattice.sigma_ne_zero_of_lambda_ne_zero",
      "IntegrableLattice.PropRResultantValuation.associated_resultant_psi",
      "IntegrableLattice.PropRResultantValuation.padicValInt_eq_of_associated_pow",
      "IntegrableLattice.PropRResultantValuation.ordEll_kappa_of_level_decomposition",
      "IntegrableLattice.PropRResultantValuation.associated_resultant_psi_of_powerBasis",
      "IntegrableLattice.PropRResultantValuation.exists_ringHom_sub_one",
      "IntegrableLattice.PropRZellExponent.coeff_one_add_X_pow_congr",
      "IntegrableLattice.PropRZellExponent.coeff_zellPow_eq",
      "IntegrableLattice.PropRZellExponent.exists_coeff_ne_zero_of_sepAt",
      "IntegrableLattice.PropRZellExponent.exists_coeff_ne_zero_lt_pow_sep",
    ],
    statement: [
      paragraph([
        "We continue in the setting of ",
        ref("paper_prop_J"),
        ". For ",
        math(String.raw`\bar{\tilde E}=\sum\bar c_{pq}z^pw^q\in\mathbb{F}_\ell[z,w]`),
        " and primitive ",
        math(String.raw`(a,b)\in\mathbb{Z}_\ell^2`),
        ", collect the terms according to the value of the exponent ",
        math(String.raw`\gamma=pa+qb\in\mathbb{Z}_\ell`),
        " and write",
      ]),
      displayMath(
        String.raw`\overline{\Phi_{(a,b)}}(x)=\sum_{\gamma}\mu_\gamma\,(1+x)^{\gamma},
        \qquad \mu_\gamma=\sum_{pa+qb=\gamma}\bar c_{pq},`,
      ),
      paragraph([
        "Let ",
        math(String.raw`\mathcal{G}(a,b)`),
        " denote the set of all ",
        math(String.raw`\gamma`),
        " with ",
        math(String.raw`\mu_\gamma\neq0`),
        " (by part (J6) of ",
        ref("paper_prop_J"),
        " we have ",
        math(String.raw`\theta(a,b)=\infty\iff\mathcal{G}(a,b)=\emptyset`),
        ").",
      ]),
      paragraph([
        "(R1 The digit-branch decomposition) For ",
        math(String.raw`c\in\{0,\dots,\ell-1\}`),
        ", collect those elements of ",
        math(String.raw`\mathcal{G}`),
        " whose zeroth digit equals ",
        math(String.raw`c`),
        ", push them down by ",
        math(String.raw`\gamma\mapsto(\gamma-c)/\ell`),
        ", and write ",
        math(String.raw`\mathcal{G}_c`),
        " for the resulting family. Then, in ",
        math(String.raw`\mathbb{F}_\ell[[x]]`),
        ",",
      ]),
      displayMath(
        String.raw`\overline{\Phi_{(a,b)}}(x)=\sum_{c=0}^{\ell-1}(1+x)^{c}\,g_c(x^{\ell}),
        \qquad g_c(y)=\sum_{\gamma\in\mathcal{G}_c}\mu_{c+\ell\gamma}\,(1+y)^{\gamma}`,
      ),
      paragraph(["holds, by ", math(String.raw`(1+x)^{\ell}=1+x^{\ell}`), "."]),
      paragraph([
        "(R2 No cancellation occurs) Put ",
        math(String.raw`d:=\min_c\mathrm{ord}_y\,g_c`),
        " and ",
        math(String.raw`C:=\{c:\mathrm{ord}_y\,g_c=d\}`),
        ", and let ",
        math(String.raw`\lambda_c:=`),
        " denote the lowest-degree coefficient of ",
        math(String.raw`g_c`),
        ". Then",
      ]),
      displayMath(
        String.raw`\theta(a,b)=\ell\,d+s^{*},\qquad
        s^{*}=\min\Bigl\{s\ge0:\ \sigma_s:=\sum_{c\in C}\lambda_c\binom{c}{s}\neq0\Bigr\},`,
      ),
      paragraph([
        "and the index occurring on the right always exists, and it is at most one less than the prime. ",
        "The index in question is ",
        math(String.raw`s^{*}`),
        ", and the bound is ",
        math(String.raw`s^{*}\le\ell-1`),
        ". Indeed, for ",
        math(String.raw`0\le c,s\le\ell-1`),
        " the matrix ",
        math(String.raw`\bigl(\binom cs\bigr)_{c,s}`),
        " is lower triangular over ",
        math(String.raw`\mathbb{F}_\ell`),
        " with all diagonal entries equal to ",
        math(String.raw`1`),
        ", hence invertible; so ",
        math(String.raw`(\lambda_c)_{c\in C}\neq0`),
        " forces ",
        math(String.raw`(\sigma_s)_{0\le s\le\ell-1}\neq0`),
        ". ",
        "Consequently the cancellation that prevented part (J2) from determining the vanishing depth — " +
          "the case in which the minimum is not attained at a single index — cannot occur in this " +
          "decomposition, for structural reasons. The part in question is (J2) of ",
        ref("paper_prop_J"),
        ", and the quantity it left undetermined is ",
        math(String.raw`\theta`),
        ".",
      ]),
      paragraph([
        "(R3 Finiteness and an effective upper bound) Put",
      ]),
      displayMath(
        String.raw`\mathrm{sep}(a,b):=\min\{t\ge0:\ \text{the elements of}\ \mathcal{G}\ \text{are pairwise distinct}\ \bmod\ \ell^{t}\}`,
      ),
      paragraph([
        "Then the recursion of (R2) terminates at depth ",
        math(String.raw`\mathrm{sep}`),
        ", and",
      ]),
      displayMath(
        String.raw`\mathcal{G}\neq\emptyset\ \Longrightarrow\ \theta(a,b)\le\ell^{\mathrm{sep}(a,b)}-1<\infty.`,
      ),
      paragraph([
        "Furthermore, a difference of two elements of ",
        math(String.raw`\mathcal{G}`),
        " is a value ",
        math(String.raw`\delta_1a+\delta_2b`),
        " of a difference vector ",
        math(String.raw`\delta`),
        " of ",
        math(String.raw`S=\mathrm{supp}(\bar{\tilde E})`),
        ", so that",
      ]),
      displayMath(
        String.raw`\theta(a,b)\ \le\ \ell^{\,1+\max_{\delta}v_\ell(\delta_1a+\delta_2b)}-1
        \qquad(\text{the max is over those }\delta\text{ with }\delta_1a+\delta_2b\neq0),`,
      ),
      paragraph([
        "(If there is no such ",
        math(String.raw`\delta`),
        " at all, then ",
        math(String.raw`|\mathcal{G}|\le1`),
        " and hence ",
        math(String.raw`\theta(a,b)=0`),
        ", so no maximum has to be taken.)",
      ]),
      paragraph([
        "In other words, ",
        math(String.raw`\theta`),
        " grows only when ",
        math(String.raw`P=(a{:}b)`),
        " approaches, ",
        math(String.raw`\ell`),
        "-adically, one of the candidate points of ",
        math(String.raw`S_\infty`),
        " furnished by part (J6) of ",
        ref("paper_prop_J"),
        ", and that growth is bounded by the reciprocal of the distance to the candidate point. This is ",
        "stronger than the statement of part (J1) of ",
        ref("paper_prop_J"),
        ", that the depth is locally constant wherever it is finite, and it is effective.",
      ]),
      paragraph([
        "(R4 The valuation as a resultant) Put ",
        math(String.raw`\Psi_M(x):=\Phi_{\ell^{M}}(1+x)\in\mathbb{Z}[x]`),
        " (the minimal polynomial of ",
        math(String.raw`\pi=\zeta_{\ell^{M}}-1`),
        ", monic, of degree ",
        math(String.raw`\varphi(\ell^{M})`),
        "). If ",
        math(String.raw`E(\zeta^a,\zeta^b)\neq0`),
        ", then",
      ]),
      displayMath(
        String.raw`\hat\theta_{M}(a,b)=\varphi(\ell^{M})\,v_\ell\bigl(E(\zeta^a,\zeta^b)\bigr)
        =v_\ell\Bigl(\mathrm{Res}_x\bigl(\Psi_M,\ \Phi_{(a,b)}\bigr)\Bigr)`,
      ),
      paragraph([
        "holds. This follows from the fact that ",
        math(String.raw`\ell`),
        " is totally ramified in ",
        math(String.raw`\mathbb{Q}(\zeta_{\ell^{M}})`),
        " (ramification index ",
        math(String.raw`\varphi(\ell^{M})`),
        ", residue degree ",
        math(String.raw`1`),
        "), together with the fact that the resultant equals the norm ",
        math(String.raw`N_{\mathbb{Q}(\zeta_{\ell^{M}})/\mathbb{Q}}\bigl(\Phi_{(a,b)}(\pi)\bigr)`),
        ". ",
        "The right-hand side is the valuation of a single integer, and it does not use the uniqueness " +
          "hypothesis that part (J3) imposed on the minimising index. The valuation is the ",
        math(String.raw`\ell`),
        "-adic one, the part in question is (J3) of ",
        ref("paper_prop_J"),
        ", and the hypothesis was that the minimum in ",
        math(String.raw`\min_m(\varphi(\ell^{M})v_\ell(A_m)+m)`),
        " is attained at a single index.",
      ]),
      paragraph([
        "(R5 Removing the hypotheses from the predictive algorithm) Part (J3) of ",
        ref("paper_prop_J"),
        " decomposes by level as ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        ", and this holds with no hypotheses; combining it with (R4), one obtains, provided ",
        math(String.raw`E`),
        " does not vanish at any root of unity of level ",
        math(String.raw`\le n`),
        ",",
      ]),
      displayMath(
        String.raw`\mathrm{ord}_\ell(\kappa_n)=\mu(\ell^{2n}-1)-2n+v_\ell(\kappa(X))
        +\sum_{M=1}^{n}\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}
        v_\ell\bigl(\mathrm{Res}_x(\Psi_M,\Phi_P)\bigr)`,
      ),
      paragraph([
        "which holds with no hypotheses at all. The right-hand side is settled by integer computation from the ",
        "coefficients of ",
        math(String.raw`D`),
        " alone (neither the values of the tower nor any computation in a cyclotomic field enters).",
      ]),
      paragraph(["The limitations that remain in this proposition are the following."]),
      list([
        [
          "What (R2) kills is the cancellation in part (J2) of ",
          ref("paper_prop_J"),
          ", not the tie between minimising indices in (J3). These are two distinct obstructions. For the ",
          "torus with ",
          math(String.raw`\ell=2`),
          ", the level-three quantity ",
          math(String.raw`\Theta_3`),
          " comes out as ",
          math(String.raw`40`),
          " from the sum in (J3) while the true value is ",
          math(String.raw`44`),
          ", and the latter obstruction is the cause. Removing it is the role of (R4).",
        ],
        [
          "(R5) determines ",
          math(String.raw`\mathrm{ord}_\ell(\kappa_n)`),
          " for each ",
          math(String.raw`n`),
          ", but it is not a closed form in ",
          math(String.raw`n`),
          " (the number of terms at level ",
          math(String.raw`M`),
          " grows as ",
          math(String.raw`(\ell+1)\ell^{M-1}`),
          "). A closed form remains a problem for parts (J4) and (J5) of ",
          ref("paper_prop_J"),
          ", and on the side of ",
          ref("paper_prop_G_infty"),
          ".",
        ],
        ["The present proposition is confined to ", math(String.raw`d=2`), "."],
        [
          "We claim no novelty. In the Iwasawa algebra ",
          math(String.raw`\mathbb{F}_\ell[[\mathbb{Z}_\ell]]\cong\mathbb{F}_\ell[[x]]`),
          ", parts (R1) and (R2) amount to a procedure that computes the ",
          math(String.raw`\lambda`),
          "-invariant of a measure with finite support along the ",
          math(String.raw`\ell`),
          "-adic tree of that support, and this is quite likely a standard tool. (R4) too is an ",
          "application of the standard fact that a resultant is a norm. We could not identify the ",
          "corresponding proposition in the literature.",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "In order not to depend tacitly on any lemma absent from the text, every fact used along the " +
        "way is stated here before it is used. The only tools are the Frobenius over ",
        math(String.raw`\mathbb{F}_\ell`),
        ", linear algebra over ",
        math(String.raw`\mathbb{F}_\ell`),
        ", and the norm and ramification of the cyclotomic field ",
        math(String.raw`\mathbb{Q}(\zeta_{\ell^{M}})`),
        "; the field of real numbers is never entered.",
      ]),
      paragraph([
        "Proof of (R1). Write ",
        math(String.raw`\gamma=c+\ell\gamma'`),
        " for those ",
        math(String.raw`\gamma`),
        " with ",
        math(String.raw`\gamma\equiv c\ (\ell)`),
        ". Over ",
        math(String.raw`\mathbb{F}_\ell`),
        " the Frobenius gives ",
        math(String.raw`(1+x)^{\ell}=1+x^{\ell}`),
        ", so",
      ]),
      displayMath(
        String.raw`(1+x)^{\gamma}=(1+x)^{c}\bigl((1+x)^{\ell}\bigr)^{\gamma'}
        =(1+x)^{c}\bigl(1+x^{\ell}\bigr)^{\gamma'}`,
      ),
      paragraph([
        " (the series ",
        math(String.raw`(1+y)^{\gamma'}=\sum_m\binom{\gamma'}{m}y^{m}`),
        " is well defined for ",
        math(String.raw`\gamma'\in\mathbb{Z}_\ell`),
        " by part (J1) of ",
        ref("paper_prop_J"),
        "). Grouping the ",
        math(String.raw`\gamma`),
        " according to their lowest digit ",
        math(String.raw`c`),
        " and substituting ",
        math(String.raw`y=x^{\ell}`),
        " gives the asserted decomposition.",
      ]),
      paragraph([
        "Proof of (R2). Existence of ",
        math(String.raw`s^{*}`),
        " first (this part uses no induction). For ",
        math(String.raw`0\le c,s\le\ell-1`),
        " the number ",
        math(String.raw`\binom cs`),
        " is the ordinary integer binomial coefficient reduced ",
        math(String.raw`\bmod\ \ell`),
        ", which is ",
        math(String.raw`0`),
        " for ",
        math(String.raw`s>c`),
        " and ",
        math(String.raw`1`),
        " for ",
        math(String.raw`s=c`),
        ". Hence the matrix ",
        math(String.raw`B:=\bigl(\binom cs\bigr)_{0\le c,s\le\ell-1}`),
        " is lower triangular with diagonal entries ",
        math(String.raw`1`),
        " and therefore invertible. Since ",
        math(String.raw`C\neq\emptyset`),
        " and ",
        math(String.raw`\lambda_c\neq0\ (c\in C)`),
        ", the vector ",
        math(String.raw`\lambda=(\lambda_c)_c`),
        " (put equal to ",
        math(String.raw`0`),
        " for ",
        math(String.raw`c\notin C`),
        ") is nonzero, so ",
        math(String.raw`\sigma=\lambda B\neq0`),
        ", that is there exists ",
        math(String.raw`\sigma_s\neq0`),
        " with ",
        math(String.raw`s\le\ell-1`),
        ".",
      ]),
      paragraph([
        "This is a restatement of the following direct sum decomposition. The ring ",
        math(String.raw`\mathbb{F}_\ell[[x]]`),
        " is free of rank ",
        math(String.raw`\ell`),
        " over ",
        math(String.raw`\mathbb{F}_\ell[[x^{\ell}]]`),
        " on the basis ",
        math(String.raw`\{(1+x)^{c}\}_{0\le c\le\ell-1}`),
        " (that ",
        math(String.raw`\{1,x,\dots,x^{\ell-1}\}`),
        " is a basis amounts to splitting the ",
        math(String.raw`x`),
        "-adic expansion into ",
        math(String.raw`\ell`),
        " residue classes, and ",
        math(String.raw`(1+x)^{c}=\sum_{s=0}^{c}\binom csx^{s}`),
        " is a change of basis that is lower triangular with diagonal entries ",
        math(String.raw`1`),
        "). Part (R1) says that the components of ",
        math(String.raw`\overline{\Phi_{(a,b)}}`),
        " in this basis are exactly the ",
        math(String.raw`g_c`),
        ". Components of a direct sum decomposition do not interfere with one another, which is why no " +
        "cancellation can occur.",
      ]),
      paragraph([
        "Now the identity itself. We argue by induction on ",
        math(String.raw`\mathrm{sep}(a,b)=:t`),
        ". For ",
        math(String.raw`t=0`),
        " we have ",
        math(String.raw`|\mathcal{G}|=1`),
        ", so ",
        math(String.raw`\overline{\Phi_{(a,b)}}`),
        " is a unit and ",
        math(String.raw`\theta=0=\ell^{0}-1`),
        ". For ",
        math(String.raw`t\ge1`),
        ", inside a branch ",
        math(String.raw`c`),
        " we have ",
        math(String.raw`\gamma_i\equiv\gamma_j\ (\ell^{t'})\iff\gamma_i'\equiv\gamma_j'\ (\ell^{t'-1})`),
        ", so the pushed-down family has ",
        math(String.raw`\mathrm{sep}`),
        " at most ",
        math(String.raw`\le t-1`),
        ", and the induction hypothesis gives ",
        math(String.raw`\mathrm{ord}_y\,g_c<\infty`),
        " together with ",
        math(String.raw`\mathrm{ord}_y\,g_c\le\ell^{t-1}-1`),
        ".",
      ]),
      paragraph([
        "By (R1) the terms with ",
        math(String.raw`c\in C`),
        " are ",
        math(String.raw`(1+x)^{c}\bigl(\lambda_cx^{\ell d}+O(x^{\ell(d+1)})\bigr)`),
        " and those with ",
        math(String.raw`c\notin C`),
        " are ",
        math(String.raw`O(x^{\ell(d+1)})`),
        ". For ",
        math(String.raw`0\le s\le\ell-1`),
        " we have ",
        math(String.raw`\ell d+s<\ell(d+1)`),
        ", so the only contributions to the coefficient of ",
        math(String.raw`x^{\ell d+s}`),
        " come from the terms ",
        math(String.raw`\lambda_cx^{\ell d}\cdot\binom csx^{s}`),
        " with ",
        math(String.raw`c\in C`),
        ", whence",
      ]),
      displayMath(
        String.raw`\bigl[x^{\ell d+s}\bigr]\overline{\Phi_{(a,b)}}=\sum_{c\in C}\lambda_c\binom cs=\sigma_s
        \qquad(0\le s\le\ell-1)`,
      ),
      paragraph([
        ". As shown above ",
        math(String.raw`\sigma_{s^{*}}\neq0`),
        " with ",
        math(String.raw`s^{*}\le\ell-1`),
        ", while ",
        math(String.raw`\sigma_s=0`),
        " for ",
        math(String.raw`s<s^{*}`),
        ", so ",
        math(String.raw`\theta(a,b)=\ell d+s^{*}`),
        " and the lowest coefficient is ",
        math(String.raw`\sigma_{s^{*}}`),
        ".",
      ]),
      paragraph([
        "Proof of (R3). The last step of the induction above yields ",
        math(String.raw`\theta(a,b)=\ell d+s^{*}\le\ell(\ell^{t-1}-1)+(\ell-1)=\ell^{t}-1`),
        ", so ",
        math(String.raw`\mathcal{G}\neq\emptyset`),
        " implies ",
        math(String.raw`\theta(a,b)\le\ell^{\mathrm{sep}(a,b)}-1<\infty`),
        ". This bound is attained: for ",
        math(String.raw`\mathcal{G}=\{0,1,\dots,\ell^{t}-1\}`),
        " with all coefficients equal to ",
        math(String.raw`1`),
        " one has ",
        math(String.raw`\mathrm{sep}=t`),
        ", and over ",
        math(String.raw`\mathbb{F}_\ell`),
        " one computes ",
        math(String.raw`\sum_{j=0}^{\ell^{t}-1}(1+x)^{j}=\frac{(1+x)^{\ell^{t}}-1}{x}=x^{\ell^{t}-1}`),
        ", so ",
        math(String.raw`\theta=\ell^{t}-1`),
        ".",
      ]),
      paragraph([
        "For the second half, the difference of two distinct elements of ",
        math(String.raw`\mathcal{G}`),
        " equals ",
        math(String.raw`\delta_1a+\delta_2b\ (\neq0)`),
        " for some ",
        math(String.raw`\delta\in S-S`),
        ". Hence ",
        math(String.raw`\mathrm{sep}(a,b)\le1+\max_{\delta}v_\ell(\delta_1a+\delta_2b)`),
        ", and substituting into the bound of the first half gives the asserted inequality. If there is no ",
        "such ",
        math(String.raw`\delta`),
        " at all, then ",
        math(String.raw`|\mathcal{G}|\le1`),
        " and ",
        math(String.raw`\theta(a,b)=0`),
        ".",
      ]),
      paragraph([
        "Which hypotheses (R3) actually uses (stated explicitly). All that the argument above borrows ",
        "from part (J1) of ",
        ref("paper_prop_J"),
        " is the statement that ",
        math(String.raw`\binom{\gamma}{m}\bmod\ell`),
        " is determined by finitely many digits of ",
        math(String.raw`\gamma`),
        " for ",
        math(String.raw`\gamma\in\mathbb{Z}_\ell`),
        ", which is the range ",
        math(String.raw`m<\ell^{L}`),
        " and follows from Lucas' theorem alone; the borderline case ",
        math(String.raw`m=\ell^{L}`),
        ", which needs ",
        math(String.raw`A_1\equiv0`),
        ", is never used. Hence (R1)–(R3) do not depend on ",
        math(String.raw`A_1\equiv0`),
        ".",
      ]),
      paragraph([
        "Proof of (R4). Put ",
        math(String.raw`K=\mathbb{Q}(\zeta_{\ell^{M}})`),
        " and ",
        math(String.raw`\varphi=\varphi(\ell^{M})=[K:\mathbb{Q}]`),
        ". The prime ",
        math(String.raw`\ell`),
        " is totally ramified in ",
        math(String.raw`K`),
        ", with ",
        math(String.raw`\ell\mathcal{O}_K=(\pi)^{\varphi}`),
        " and residue degree ",
        math(String.raw`1`),
        ". The polynomial ",
        math(String.raw`\Psi_M`),
        " is monic and its roots are all the conjugates of ",
        math(String.raw`\pi`),
        ", so for ",
        math(String.raw`F\in\mathbb{Z}[x]`),
        " we get",
      ]),
      displayMath(
        String.raw`\mathrm{Res}_x(\Psi_M,F)=\prod_{\sigma}F(\sigma\pi)=N_{K/\mathbb{Q}}\bigl(F(\pi)\bigr)\in\mathbb{Z}`,
      ),
      paragraph([
        ". In general, for ",
        math(String.raw`\alpha\in K^\times`),
        " one has ",
        math(String.raw`v_\ell\bigl(N_{K/\mathbb{Q}}(\alpha)\bigr)=f\cdot v_{\mathfrak{l}}(\alpha)`),
        ", where ",
        math(String.raw`\mathfrak{l}`),
        " is the unique place above ",
        math(String.raw`\ell`),
        ", with ",
        math(String.raw`f=1`),
        " and ",
        math(String.raw`v_{\mathfrak{l}}=\varphi\cdot v_\ell`),
        ", so ",
        math(String.raw`v_\ell(N(\alpha))=\varphi\,v_\ell(\alpha)`),
        ". Taking ",
        math(String.raw`F=\Phi_{(a,b)}`),
        " gives ",
        math(String.raw`F(\pi)=\tilde E(\zeta^a,\zeta^b)`),
        ", and ",
        math(String.raw`\tilde E`),
        " differs from ",
        math(String.raw`E`),
        " only by a monomial, that is by a root of unity, which is a unit, so the valuations agree. Because " +
        "the norm is taken directly, the identity holds whether or not a minimiser is attained twice.",
      ]),
      paragraph([
        "Proof of (R5). One only has to combine the level decomposition of part (J3) of ",
        ref("paper_prop_J"),
        ", which holds with no hypothesis, with (R4). Substituting ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        " and ",
        math(String.raw`\hat\theta_M(P)=v_\ell(\mathrm{Res}_x(\Psi_M,\Phi_P))`),
        " into the identity ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=v_\ell(\kappa(X))-2n+\mu(\ell^{2n}-1)+\Sigma_n`),
        ", which comes from the product formula in the proof of ",
        ref("paper_prop_W"),
        ", gives the asserted formula. The only remaining premises are hypothesis (H) and the requirement " +
        "that ",
        math(String.raw`E`),
        " not vanish at any tuple of roots of unity of level ",
        math(String.raw`\le n`),
        ", both of which are demanded by the summation formula itself.",
      ]),
    ],
  },
]);
