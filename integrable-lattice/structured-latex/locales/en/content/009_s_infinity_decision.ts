/**
 * 論文本体 第 10 章（英語版）: θ = ∞ の点集合 S_∞ の判定手続きと、n ℓ^n の係数の閉じた式（命題 K）。
 *
 * **正本は日本語版 `../../../content/009_s_infinity_decision.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする。
 *
 * ## 訳出で構造を変えた箇所（内容は変えていない）
 *
 * 日本語版は `**強調**` の中に数式ノードを含めて書いている箇所が 3 つある（(K1) の「同一の有限集合を
 * 指している」、(K4) の表題、(K6) の「b は D の係数から決まる」）。英語版の生成器は**ノードをまたぐ
 * `**` を拒否する**ので、強調文を数式なしで言い切り、その直後に記号を対応づける文を置いた
 * （用語集 §1.2d の前例と同じ扱い）。**数式ノードの多重集合は日本語版と一致させてある。**
 *
 * ## 数式の中の日本語（`ja-en-exceptions.ts` へ登録した）
 *
 * 日本語版の displayMath 3 つが `\text{原始}` / `\text{したがって}` /
 * `\text{の原始二項式部分の次数}` を含む。英語版は和文フォントを読み込まないので、
 * これらは PDF から無言で消える。`\text{}` の中身を英訳した。
 * 最後の 1 つは日本語が後置修飾（「〜の次数」）なので、英語として意味の通る語順にするために
 * `\bar{\tilde E}` を `\text{}` の後ろへ回した。**記号そのものは 1 つも足しても消してもいない。**
 */

import { cite, defineTranslatedBlocks as defineBlocks, displayMath, list, math, paragraph, refInTranslation as ref } from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_100_heading_s_infinity_decision",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/009_s_infinity_decision.ts", ordinal: 1 },
    title: {
      text:
        "A decision procedure for infinite vanishing depth, and the coefficient of the type III " +
        "term as a multiplicity of binomial factors",
    },
    labels: [],
  },
  {
    id: "paper_101_theorem_s_infinity_decision",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/009_s_infinity_decision.ts", ordinal: 2 },
    title: {
      text:
        "Proposition K — a decision procedure for the set of points of infinite vanishing depth, " +
        "and a closed form for the coefficient of the type III term",
    },
    labels: ["paper_prop_K"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle20_T3_s_infinity"],
    statement: [
      paragraph([
        "We work in the setting of ",
        ref("paper_prop_J"),
        ": here ",
        math(String.raw`\tilde E=\sum c_{pq}z^pw^q`),
        ", the polynomial ",
        math(String.raw`\bar{\tilde E}`),
        " is its reduction ",
        math(String.raw`\bmod\ \ell`),
        ", ",
        math(String.raw`S=\mathrm{supp}(\bar{\tilde E})`),
        ", ",
        math(String.raw`\chi^{(p,q)}=z^pw^q`),
        ", and ",
        math(String.raw`S_\infty=\{P\in\mathbb{P}^1(\mathbb{Z}_\ell):\theta(P)=\infty\}`),
        ". Let ",
        math(String.raw`u=(a,b)\in\mathbb{Z}^2`),
        " be a primitive vector and put ",
        math(String.raw`u^\perp=(b,-a)`),
        ".",
      ]),
      paragraph([
        "**(K1 The correspondence)** The map ",
        math(String.raw`\iota([u])=(a{:}b)`),
        ", defined on the classes of primitive integer vectors modulo ",
        math(String.raw`\pm`),
        " and taking values in ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", is injective, and its image is ",
        math(String.raw`\mathbb{P}^1(\mathbb{Q})`),
        ". Moreover ",
        math(String.raw`\iota`),
        " carries the set of **directions of the exceptional lines** of ",
        ref("paper_prop_G_infty"),
        " bijectively onto ",
        math(String.raw`S_\infty`),
        ". In particular ",
        math(String.raw`|S_\infty|`),
        " equals the number of exceptional lines. ",
        "**Thus the point of view that reads these objects as lines in a rank-two lattice and the point " +
          "of view that reads them as points of a projective line single out one and the same finite set.** " +
          "The lattice in question is ",
        math(String.raw`\mathbb{Z}^2`),
        " and the projective line in question is ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ".",
      ]),
      paragraph([
        "**(K2 The two criteria agree)** For primitive ",
        math(String.raw`u`),
        " the following are equivalent: (i) ",
        math(String.raw`\theta(u)=\infty`),
        "; (ii) ",
        math(String.raw`\lambda(u)\ge1`),
        "; (iii) ",
        math(String.raw`(\chi^{u^\perp}-1)\mid\bar{\tilde E}`),
        "; (iv) for every ",
        math(String.raw`\gamma\in\mathbb{Z}`),
        ",",
      ]),
      displayMath(String.raw`\sum_{\substack{(p,q)\in S\\ pa+qb=\gamma}}\bar c_{pq}=0 .`),
      paragraph([
        "That is, the criterion (iii) used by ",
        ref("paper_prop_G_infty"),
        " and the criterion (iv) used by ",
        ref("paper_prop_J"),
        " are one and the same criterion.",
      ]),
      paragraph([
        "**(K3 The decision procedure)** Consequently the following is a **finite procedure** that uses ",
        "nothing but the coefficients of ",
        math(String.raw`D`),
        ", and it determines ",
        math(String.raw`S_\infty`),
        " completely (neither the values of the tower nor any cyclotomic field enters). (1) Form ",
        math(String.raw`\bar{\tilde E}`),
        " and ",
        math(String.raw`S`),
        ". (2) From the differences ",
        math(String.raw`(\Delta p,\Delta q)`),
        " of two distinct points of ",
        math(String.raw`S`),
        ", enumerate the candidates ",
        math(String.raw`u=\mathrm{prim}(\Delta q,-\Delta p)`),
        " (there are at most ",
        math(String.raw`\binom{|S|}{2}`),
        " of them). (3) Test criterion (iv) of (K2) on each candidate. The cost is ",
        math(String.raw`O(|S|^3)`),
        " operations over ",
        math(String.raw`\mathbb{F}_\ell`),
        ".",
      ]),
      paragraph([
        "**(K4 The exponent of part (J4) is a multiplicity)** The ring ",
        math(String.raw`\mathbb{F}_\ell[z^{\pm1},w^{\pm1}]`),
        " is a UFD; for primitive ",
        math(String.raw`v`),
        " the element ",
        math(String.raw`\chi^{v}-1`),
        " is prime, and the elements belonging to distinct directions are non-associate. Hence the ",
        "factorisation",
      ]),
      displayMath(
        String.raw`\bar{\tilde E}=c\,\chi^{w_0}\prod_{i=1}^{r}\bigl(\chi^{v_i}-1\bigr)^{m_i}\cdot G_0,
        \qquad (\chi^{v}-1)\nmid G_0\ \ (\forall v\ \text{primitive})`,
      ),
      paragraph([
        "is uniquely determined. Put ",
        math(String.raw`P=\iota([u])\in S_\infty`),
        " and let ",
        math(String.raw`m_u`),
        " be the multiplicity of ",
        math(String.raw`\chi^{u^\perp}-1`),
        ". Then",
      ]),
      displayMath(
        String.raw`e_j=\infty\ \ (j<m_u),\qquad e_{m_u}<\infty,\qquad\text{hence}\quad j^*(P)=m_u`,
      ),
      paragraph([
        "holds. In other words, the quantity ",
        math(String.raw`j^*`),
        " of part (J4) of ",
        ref("paper_prop_J"),
        " — a quantity read off from a deformation in an ",
        math(String.raw`\ell`),
        "-adic neighbourhood — is equal to an algebraic quantity: ",
        math(String.raw`j^*`),
        " is **the multiplicity in the factorisation** of ",
        math(String.raw`\bar{\tilde E}`),
        ".",
      ]),
      paragraph([
        "**(K5 The hypothesis (N) is discharged)** If ",
        math(String.raw`\ell^{r}>e_{m_u}`),
        ", then the minimum of ",
        math(String.raw`\Lambda(r)`),
        " is attained at the single index ",
        math(String.raw`j=m_u`),
        ". Moreover two distinct points ",
        math(String.raw`P=\iota([u]),P'=\iota([u'])`),
        " become the same point of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{k})`),
        " precisely when ",
        math(String.raw`\ell^{k}\mid\det(u,u')`),
        ". Hence, if we set",
      ]),
      displayMath(
        String.raw`r_0:=1+\max\Bigl(\max_{P\neq P'}v_\ell\bigl(\det(u,u')\bigr),\ \max_{P}\bigl\lfloor\log_\ell e_{m_u}\bigr\rfloor\Bigr)`,
      ),
      paragraph([
        "then those hypotheses of part (J4) of ",
        ref("paper_prop_J"),
        " that concern the separation of the balls and the uniqueness of the ",
        math(String.raw`\mathrm{argmin}`),
        " are **satisfied automatically**. The only hypothesis that remains is that, at sufficiently deep ",
        "levels, the minimum defining ",
        math(String.raw`\hat\theta`),
        " is attained at a single index.",
      ]),
      paragraph([
        "**(K6 The coefficient of the type III term)** Under that one remaining hypothesis, the ",
        "coefficient of the term ",
        math(String.raw`n\ell^n`),
        " is",
      ]),
      displayMath(
        String.raw`b=\sum_{P\in S_\infty}j^*(P)=\sum_{i=1}^{r}m_i
        \quad(=\ \text{the degree of the primitive binomial part of}\ \bar{\tilde E}),`,
      ),
      paragraph([
        "In particular, the vanishing ",
        math(String.raw`b=0`),
        ", the statement that ",
        math(String.raw`\bar{\tilde E}`),
        " has no factor of the shape ",
        math(String.raw`\chi^{v}-1`),
        ", and type II (no term ",
        math(String.raw`n\ell^n`),
        " occurs) are equivalent. ",
        "**The coefficient is settled by a finite computation over a finite field, from the coefficients " +
          "of the defining polynomial alone.** The coefficient in question is ",
        math(String.raw`b`),
        ", the polynomial is ",
        math(String.raw`D`),
        ", and the field is ",
        math(String.raw`\mathbb{F}_\ell`),
        ". The value ",
        math(String.raw`b=2`),
        " in the closed form that ",
        ref("paper_prop_G_infty"),
        " gives for its family corresponds to the primitive binomial part having degree two there ",
        "(two factors of multiplicity one, or one factor of multiplicity two).",
      ]),
      paragraph([
        "**Part (K6) is not a new assertion.** The quantity ",
        math(String.raw`b`),
        " here agrees, **by definition**, with the invariant ",
        math(String.raw`l_0`),
        " of Cuoco–Monsky, and the identity in (K6) is precisely the coefficient of ",
        math(String.raw`n\ell^n`),
        " supplied by their Theorem 1.7. The attribution is spelled out at the end of the limitations below.",
      ]),
      paragraph([
        "**(K7 An a priori upper bound)** Write ",
        math(String.raw`\mathrm{per}`),
        " for the lattice perimeter of a lattice polygon (the sum of the lattice lengths of its edges). ",
        "Then",
      ]),
      displayMath(String.raw`b\ \le\ \tfrac12\,\mathrm{per}\bigl(\mathrm{Newt}(\bar{\tilde E})\bigr).`),
      paragraph([
        "That is, the Newton polygon of ",
        math(String.raw`D`),
        " alone already bounds the coefficient of ",
        math(String.raw`n\ell^n`),
        " from above. Equality holds exactly when ",
        math(String.raw`G_0`),
        " is a monomial, that is, exactly when ",
        math(String.raw`\bar{\tilde E}`),
        " factors completely into binomials.",
      ]),
      paragraph(["**Limitations (stated as part of the assertion)**"]),
      list([
        [
          "(K6) inherits the hypothesis that remains in part (J4) of ",
          ref("paper_prop_J"),
          " (the uniqueness of the index at which the minimum defining ",
          math(String.raw`\hat\theta`),
          " is attained). That hypothesis does in fact fail for the torus with ",
          math(String.raw`\ell=2`),
          ", and there are towers with ",
          math(String.raw`\ell=3`),
          " for which it fails as well. For those towers the value of ",
          math(String.raw`b`),
          " is still correct, but **this is numerical support only and not a proof**.",
        ],
        [
          "(K1)–(K5) and (K7) carry no hypotheses. In particular the decision procedure of (K3) determines ",
          math(String.raw`S_\infty`),
          " completely, independently of the behaviour of ",
          math(String.raw`\hat\theta`),
          ".",
        ],
        [
          "The coefficients of ",
          math(String.raw`\ell^n`),
          " and of ",
          math(String.raw`n`),
          ", and the constant term — that is, ",
          math(String.raw`c,d,e`),
          " — are not determined by the present proposition. It is ",
          math(String.raw`G_0`),
          " that governs them.",
        ],
        ["The present proposition is confined to ", math(String.raw`d=2`), "."],
        [
          "**We claim no novelty; part (K6) in particular is not new.** Definition 1.2 of Cuoco–Monsky ",
          cite(["CuocoMonsky1981"], "Definition 1.2"),
          ", for a ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extension, defines ",
          math(String.raw`l_0(F)`),
          " to be the number, counted with multiplicity, of the irreducible factors of ",
          math(String.raw`\bar F_0`),
          " of the shape ",
          math(String.raw`\bar\sigma-1`),
          " with ",
          math(String.raw`\sigma`),
          " a group element that is not a ",
          math(String.raw`p`),
          "-th power (that is, primitive), where ",
          math(String.raw`F=p^{m_0}F_0`),
          ". Under ",
          math(String.raw`\Gamma\cong\mathbb{Z}_\ell^2`),
          " the element ",
          math(String.raw`\bar\sigma-1`),
          " corresponds to ",
          math(String.raw`\chi^{v}-1`),
          " for a primitive ",
          math(String.raw`v`),
          ", so this is the same quantity as ",
          math(String.raw`\sum_{i=1}^{r}m_i`),
          " in (K4). The identity in (K6) is therefore exactly the coefficient of ",
          math(String.raw`n\ell^n`),
          " in their Theorem 1.7, namely ",
          math(String.raw`\Sigma_n(F)=(m_0p^n+l_0n+O(1))p^{(d-1)n}`),
          ", and has been known since 1981. **We claim no novelty for this formula.** That theorem is ",
          "already cited in ",
          ref("paper_prop_D"),
          ".",
        ],
        [
          "**The equivalence in (K2) is likewise not new.** Lemma 2.3 of Monsky, *Some invariants of ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions* ",
          cite(["MonskySomeInvariants1981"], "Lemma 2.3"),
          ", states, for ",
          math(String.raw`d=2`),
          ", that ",
          math(String.raw`\lambda(G_a)`),
          " is unbounded on a neighbourhood of ",
          math(String.raw`(0,1)`),
          " if and only if ",
          math(String.raw`X_1\mid\bar G`),
          ". Under the specialisation ",
          math(String.raw`z\mapsto(1+T)^{a},\ w\mapsto(1+T)^{b}`),
          " the reduction ",
          math(String.raw`\bar{\tilde E}`),
          " maps to ",
          math(String.raw`\sum_m\bar A_m(a,b)\,T^m`),
          "; hence ",
          math(String.raw`\theta(a,b)=\infty`),
          " corresponds to ",
          math(String.raw`\bar G_a=0`),
          ", a finite ",
          math(String.raw`\theta(a,b)`),
          " agrees with ",
          math(String.raw`\lambda(G_a)`),
          ", and ",
          math(String.raw`X_1\mid\bar G`),
          " is condition (iii) of (K2) in the direction ",
          math(String.raw`(0{:}1)`),
          ". By part (J1) of ",
          ref("paper_prop_J"),
          " the function ",
          math(String.raw`\theta`),
          " is locally constant wherever it is finite, so ``unbounded on a neighbourhood'' and ``equal to ",
          math(String.raw`\infty`),
          " at the point'' are equivalent. **We have obtained and checked the original text.**",
        ],
        [
          "**Parts (K3), (K4), (K5) and (K7) were not found in what we read.** What we read is: the ",
          "Introduction, Sections 1 through 7 and the References of Cuoco–Monsky (1981) — every page of ",
          "pp. 235–258; the whole of Monsky, *Some invariants* (1981), pp. 229–233; and the Introduction ",
          "and the material around Theorem 1.1 of Cuoco ",
          cite(["Cuoco1980"]),
          " (the source to which Cuoco–Monsky refer on p. 248 for details on ",
          math(String.raw`m_0`),
          " and ",
          math(String.raw`l_0`),
          "). All of these **define** ",
          math(String.raw`l_0`),
          " through the prime factorisation of ",
          math(String.raw`\bar F_0`),
          " and nothing more: they give neither a finite procedure that settles it from the coefficients ",
          "(part (K3)) nor the pointwise identification of ",
          math(String.raw`j^*`),
          " (part (K4)). **What we have not read is Cuoco's thesis (Brandeis, 1979), so we cannot say " +
            "that it is absent there.** Part (K3), moreover, is the specialisation of ",
          ref("paper_prop_F"),
          " to ",
          math(String.raw`d=2`),
          ", and no novelty is claimed for that proposition either.",
        ],
        [
          "Factorisation of the group ring ",
          math(String.raw`\mathbb{F}_\ell[\mathbb{Z}^2]`),
          " by the augmentation-type ideals ",
          math(String.raw`(\chi^{v}-1)`),
          " is in itself a standard tool.",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "In order not to depend tacitly on any lemma absent from the text, every fact used along the " +
        "way is stated here before it is used. All that is used are gcd's and ",
        math(String.raw`\mathbb{Z}`),
        "-valuations at ",
        math(String.raw`\ell`),
        ", division in ",
        math(String.raw`\mathbb{F}_\ell[z^{\pm1},w^{\pm1}]`),
        ", the ",
        math(String.raw`\mathbb{Z}[x]`),
        "-coefficient valuations at ",
        math(String.raw`\ell`),
        ", and integral linear algebra over ",
        math(String.raw`\mathbb{Z}^2`),
        "; the field of real numbers is never entered.",
      ]),
      paragraph([
        "**Proof of (K1).** Suppose ",
        math(String.raw`\iota([u])=\iota([u'])`),
        ". By the definition of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        " there is a ",
        math(String.raw`c\in\mathbb{Z}_\ell^\times`),
        " with ",
        math(String.raw`u'=cu`),
        ", and since at least one coordinate of ",
        math(String.raw`u`),
        " is different from ",
        math(String.raw`0`),
        ", the ratio of that coordinate shows ",
        math(String.raw`c\in\mathbb{Q}^\times`),
        ". As ",
        math(String.raw`u,u'`),
        " are both primitive integer vectors, both ",
        math(String.raw`c`),
        " and its inverse are integers, so ",
        math(String.raw`c=\pm1`),
        " and the map is injective. As for the image, given ",
        math(String.raw`(a{:}b)`),
        " with ",
        math(String.raw`a,b\in\mathbb{Q}`),
        " not both ",
        math(String.raw`0`),
        ", clearing denominators and dividing by the gcd produces a primitive integer vector, so the image ",
        "is exactly ",
        math(String.raw`\mathbb{P}^1(\mathbb{Q})`),
        ".",
      ]),
      paragraph([
        "The extension of ",
        ref("paper_prop_J"),
        " (J1) of ",
        math(String.raw`\theta`),
        " to ",
        math(String.raw`\mathbb{Z}_\ell^2`),
        " agrees at the points of ",
        math(String.raw`\mathbb{Z}^2`),
        " with the defining expression ",
        math(String.raw`\mathrm{ord}_{x=0}\overline{\Phi_{(a,b)}}`),
        " itself, so for primitive ",
        math(String.raw`u\in\mathbb{Z}^2`),
        " we have ",
        math(String.raw`\iota([u])\in S_\infty\iff\theta(u)=\infty`),
        ". On the other hand ",
        ref("paper_prop_J"),
        " (J6) gives ",
        math(String.raw`S_\infty\subseteq\mathbb{P}^1(\mathbb{Q})`),
        ", so each point of ",
        math(String.raw`S_\infty`),
        " can be written uniquely as ",
        math(String.raw`\iota([u])`),
        " for a primitive integer vector, and that ",
        math(String.raw`u`),
        " is a direction of an exceptional line of ",
        ref("paper_prop_G_infty"),
        ". Without this rationality, examining all of ",
        math(String.raw`\mathbb{Z}^2`),
        " would not rule out omissions in the directions of ",
        math(String.raw`\mathbb{Z}_\ell\setminus\mathbb{Q}`),
        "; the completeness of the decision procedure rests on it.",
      ]),
      paragraph([
        "**Proof of (K2).** The equivalences (i) ",
        math(String.raw`\iff`),
        " (ii) and (ii) ",
        math(String.raw`\iff`),
        " (iii) are Lemmas B and C in the proof of ",
        ref("paper_prop_G_infty"),
        ". We prove (iii) ",
        math(String.raw`\iff`),
        " (iv). For the map ",
        math(String.raw`\bar\psi_u`),
        " of that proof, given by ",
        math(String.raw`z\mapsto y^a,\ w\mapsto y^b`),
        ", we have",
      ]),
      displayMath(
        String.raw`\bar\psi_u\bigl(\bar{\tilde E}\bigr)=\sum_{(p,q)\in S}\bar c_{pq}\,y^{\,pa+qb}
        =\sum_{\gamma\in\mathbb{Z}}\Bigl(\sum_{\substack{(p,q)\in S\\ pa+qb=\gamma}}\bar c_{pq}\Bigr)y^{\gamma}`,
      ),
      paragraph([
        ", and since the monomials ",
        math(String.raw`\{y^{\gamma}\}`),
        " form a basis of ",
        math(String.raw`\mathbb{F}_\ell[y^{\pm1}]`),
        ", the condition ",
        math(String.raw`\bar\psi_u(\bar{\tilde E})=0`),
        " is equivalent to (iv). On the other hand ",
        math(String.raw`\ker\bar\psi_u=(\chi^{u^\perp}-1)`),
        ", so ",
        math(String.raw`\bar\psi_u(\bar{\tilde E})=0`),
        " is equivalent to (iii) as well. Note that if ",
        math(String.raw`u`),
        " is an integer vector then ",
        math(String.raw`pa+qb\in\mathbb{Z}`),
        ", so where ",
        ref("paper_prop_J"),
        " (J6) lets ",
        math(String.raw`\gamma\in\mathbb{Z}_\ell`),
        " run, one may restrict to ",
        math(String.raw`\gamma\in\mathbb{Z}`),
        ".",
      ]),
      paragraph([
        "**Proof of (K3). Completeness** follows from the containment of the candidate set in ",
        ref("paper_prop_J"),
        " (J6) together with the correspondence (K1), which rules out omissions in the directions of ",
        math(String.raw`\mathbb{Z}_\ell\setminus\mathbb{Q}`),
        ". **Soundness** is (K2). **Termination** holds because there are finitely many candidates and each ",
        "test is a finite sum over ",
        math(String.raw`S`),
        ". **The complexity** is ",
        math(String.raw`O(|S|^2)`),
        " candidates with ",
        math(String.raw`O(|S|)`),
        " operations each, hence ",
        math(String.raw`O(|S|^3)`),
        " in all.",
      ]),
      paragraph([
        "**Proof of (K4). Uniqueness of the factorisation first.** The ring ",
        math(String.raw`\mathbb{F}_\ell[z^{\pm1},w^{\pm1}]`),
        " is a UFD, and for primitive ",
        math(String.raw`v`),
        " the quotient is ",
        math(String.raw`\mathbb{F}_\ell[y^{\pm1}]`),
        ", an integral domain, so ",
        math(String.raw`\chi^{v}-1`),
        " is prime. Moreover ",
        math(String.raw`\mathrm{Newt}(\chi^{v}-1)=[0,v]`),
        ", so different directions give different Newton polytopes and hence non-associate primes. The ",
        "asserted factorisation is therefore unique, the vectors ",
        math(String.raw`v_i`),
        " being taken up to ",
        math(String.raw`\pm`),
        ".",
      ]),
      paragraph([
        "Now we prove ",
        math(String.raw`j^*=m_u`),
        ". Choose ",
        math(String.raw`n\in\mathbb{Z}^2`),
        " with ",
        math(String.raw`\det(u,n)=1`),
        " and put ",
        math(String.raw`t:=(-n_2,n_1)`),
        "; then ",
        math(String.raw`\langle v,n\rangle=-1`),
        ", ",
        math(String.raw`\langle t,n\rangle=0`),
        " and ",
        math(String.raw`\det(v,t)=1`),
        ", so ",
        math(String.raw`(v,t)`),
        " is a basis of ",
        math(String.raw`\mathbb{Z}^2`),
        ", where ",
        math(String.raw`v=u^\perp`),
        ". Setting ",
        math(String.raw`s:=\chi^{v}`),
        " and ",
        math(String.raw`\tau:=\chi^{t}`),
        ", the deformation ",
        math(String.raw`\chi^{(p,q)}\mapsto\chi^{(p,q)}(1+T)^{\langle(p,q),n\rangle}`),
        " defining the ",
        math(String.raw`e_j`),
        " becomes, in these coordinates, a rescaling of ",
        math(String.raw`s`),
        " alone, namely ",
        math(String.raw`s\mapsto s(1+T)^{-1}`),
        " and ",
        math(String.raw`\tau\mapsto\tau`),
        ", while ",
        math(String.raw`\bar\psi_u`),
        " becomes ",
        math(String.raw`s\mapsto1`),
        ", ",
        math(String.raw`\tau\mapsto(1+x)^{\pm1}`),
        ".",
      ]),
      paragraph([
        "Writing ",
        math(String.raw`\bar{\tilde E}=(s-1)^{m}G`),
        " with ",
        math(String.raw`m=m_u`),
        " and ",
        math(String.raw`(s-1)\nmid G`),
        " and substituting, we obtain",
      ]),
      displayMath(
        String.raw`\sum_{j}\psi_j(x)T^j
        =\bigl(-T\bigr)^{m}(1+T)^{-m}\,\bar\psi_u\Bigl(G\bigl(s(1+T)^{-1},\tau\bigr)\Bigr)`,
      ),
      paragraph([
        ". The order in ",
        math(String.raw`T`),
        " of the right-hand side is exactly ",
        math(String.raw`m`),
        ", and the coefficient of ",
        math(String.raw`T^{m}`),
        " is ",
        math(String.raw`(-1)^{m}\bar\psi_u(G)`),
        ". From ",
        math(String.raw`(s-1)\nmid G`),
        " and ",
        math(String.raw`\ker\bar\psi_u=(s-1)`),
        " we get ",
        math(String.raw`\bar\psi_u(G)\neq0`),
        ", hence ",
        math(String.raw`\psi_j=0\ (j<m)`),
        " and ",
        math(String.raw`\psi_m\neq0`),
        ", that is ",
        math(String.raw`e_j=\infty\ (j<m)`),
        " and ",
        math(String.raw`e_{m}=\mathrm{ord}_{x=0}\bar\psi_u(G)<\infty`),
        ". Since ",
        math(String.raw`P\in S_\infty`),
        " forces ",
        math(String.raw`m\ge1`),
        ", we conclude ",
        math(String.raw`j^*(P)=m`),
        ".",
      ]),
      paragraph([
        "**Proof of (K5). Uniqueness of the minimiser**: by (K4) there is no term for ",
        math(String.raw`j<m_u`),
        ", while for ",
        math(String.raw`j>m_u`),
        " we have ",
        math(String.raw`e_j+j\ell^{r}\ge(m_u+1)\ell^{r}=m_u\ell^{r}+\ell^{r}>m_u\ell^{r}+e_{m_u}`),
        ", the last step using ",
        math(String.raw`\ell^{r}>e_{m_u}`),
        "; so the minimum is attained only at ",
        math(String.raw`j=m_u`),
        ". **Separation of balls**: for primitive integer vectors with ",
        math(String.raw`u\neq\pm u'`),
        ", the points ",
        math(String.raw`\iota([u])`),
        " and ",
        math(String.raw`\iota([u'])`),
        " coincide in ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{k})`),
        " only when ",
        math(String.raw`\ell^{k}\mid\det(u,u')`),
        " (if ",
        math(String.raw`u'\equiv cu`),
        " the determinant vanishes, and the converse follows because ",
        math(String.raw`u`),
        " is nonzero ",
        math(String.raw`\bmod\ \ell`),
        ", hence different from ",
        math(String.raw`0`),
        "). Since ",
        math(String.raw`\det(u,u')\neq0`),
        ", separation occurs as soon as ",
        math(String.raw`k>v_\ell(\det(u,u'))`),
        ". The asserted ",
        math(String.raw`r_0`),
        " satisfies both requirements simultaneously.",
      ]),
      paragraph([
        "On the floor function appearing in ",
        math(String.raw`r_0`),
        ". The quantity ",
        math(String.raw`\bigl\lfloor\log_\ell e_{m_u}\bigr\rfloor`),
        " is the largest natural number ",
        math(String.raw`t`),
        " with ",
        math(String.raw`\ell^{t}\le e_{m_u}`),
        ", and is therefore determined by comparisons of powers of ",
        math(String.raw`\ell`),
        " against the natural number ",
        math(String.raw`e_{m_u}\in\mathbb{N}`),
        ". **No real logarithm is involved**, so the expression stays on the countable side.",
      ]),
      paragraph([
        "**Proof of (K6).** Insert into the formula of the second half of ",
        ref("paper_prop_J"),
        " (J4) the finiteness of ",
        math(String.raw`S_\infty`),
        " from (J6), the removal of hypothesis (N) from (K5), the correspondence with exceptional lines ",
        "from (K1), and the identity ",
        math(String.raw`j^*=m_u`),
        " from (K4); this gives ",
        math(String.raw`b=\sum_{P\in S_\infty}j^*(P)=\sum_{i=1}^{r}m_i`),
        ". The case ",
        math(String.raw`b=0`),
        " means ",
        math(String.raw`S_\infty=\emptyset`),
        " and is the first half of (J4), that is type II. The only hypothesis that remains is the one of " +
        "(J4) requiring that, at sufficiently deep levels, the minimiser producing ",
        math(String.raw`\hat\theta`),
        " be unique; it is stated explicitly among the limitations.",
      ]),
      paragraph([
        "**Proof of (K7).** Applying the additivity of Newton polytopes (Ostrowski's theorem) to the ",
        "factorisation gives ",
        math(String.raw`\mathrm{Newt}(\bar{\tilde E})=\sum_i m_i\,[0,v_i]+\mathrm{Newt}(G_0)+w_0`),
        ". The lattice perimeter is additive for Minkowski sums, since the multiset of edge vectors of a sum ",
        "is the union of the multisets of edge vectors of the factors; as the ",
        math(String.raw`v_i`),
        " are primitive we have ",
        math(String.raw`\mathrm{per}(m_i[0,v_i])=2m_i`),
        ", and ",
        math(String.raw`\mathrm{per}\ge0`),
        " always, so ",
        math(String.raw`\mathrm{per}(\mathrm{Newt}(\bar{\tilde E}))\ge2\sum_im_i=2b`),
        ". Equality holds exactly when ",
        math(String.raw`\mathrm{per}(\mathrm{Newt}(G_0))=0`),
        ", that is exactly when ",
        math(String.raw`G_0`),
        " is a monomial.",
      ]),
    ],
  },
]);
