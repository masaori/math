/**
 * 論文本体 第 5 章の続き（2）（英語版）: 命題 G″（ℓ = 2 の bouquet 族の閉形式）。
 *
 * **正本は日本語版 `../../../content/005c_ell2_family.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする。
 *
 * ## 強調の置き方
 *
 * 英語版の生成器は**ノードをまたぐ `**` を拒否する**（PDF に素のアスタリスクが出るため）。
 * 日本語版が数式をまたいで強調している箇所は、`009_s_infinity_decision.ts` と同じ扱いにした
 * ——強調文を数式なしで言い切り、その直後に記号を対応づける文を置く。
 * **数式ノードの多重集合は日本語版と一致させてある。**
 *
 * ## 数式の中の日本語（`structure-exceptions.ts` へ登録した）
 *
 * 日本語版の displayMath 2 つが `\text{}` の中に日本語（「すなわち」「例外直線上」「かつ」
 * 「飽和」「および全ての場合の」）を持つ。英語版は和文フォントを読み込まないため、
 * そのままでは PDF に組めない。**`\text{}` の中身だけ**を英訳し、その外側の記号は 1 文字も
 * 変えていない。
 */

import { defineTranslatedBlocks as defineBlocks, displayMath, list, math, paragraph, refInTranslation as ref } from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_056_theorem_ell2_family",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/005c_ell2_family.ts", ordinal: 1 },
    title: {
      text:
        "Proposition G'' — the bouquet family at ℓ = 2: saturation, cancellation, " +
        "and four closed forms",
    },
    labels: ["paper_prop_G_ell2"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle20_T3_ell2"],
    lean: [
      "IntegrableLattice.gpp1_both_even",
      "IntegrableLattice.gpp1_not_both_four",
      "IntegrableLattice.gpp1_lambda_ne",
      "IntegrableLattice.gpp1_st_not_both_ge",
    ],
    statement: [
      paragraph([
        "Let ",
        math(String.raw`\ell=2`),
        " and let ",
        math(String.raw`X(p,q)`),
        " be the one-vertex bouquet of part (G'3) of ",
        ref("paper_prop_G_infty"),
        ": it carries ",
        math(String.raw`p`),
        " loops of voltage ",
        math(String.raw`(1,0)`),
        " and ",
        math(String.raw`q`),
        " loops of voltage ",
        math(String.raw`(0,1)`),
        ", with ",
        math(String.raw`p,q\ge1`),
        ". The base graph has a single vertex, so ",
        math(String.raw`\kappa(X)=1`),
        ", and its cycle voltage lattice is ",
        math(String.raw`\mathbb{Z}^2`),
        "; consequently, at every level ",
        math(String.raw`n`),
        ", **the hypothesis that every stage of the tower be connected is satisfied automatically.** Writing ",
        math(String.raw`f_z:=z+z^{-1}-2`),
        " and ",
        math(String.raw`f_w:=w+w^{-1}-2`),
        " we have ",
        math(String.raw`D=\det L=-(p\,f_z+q\,f_w)`),
        ", and we put ",
        math(String.raw`\mu=v_2(\gcd(p,q))`),
        ", ",
        math(String.raw`p':=p/2^{\mu}`),
        ", ",
        math(String.raw`q':=q/2^{\mu}`),
        ", ",
        math(String.raw`E=2^{-\mu}D=-(p'f_z+q'f_w)`),
        ". In particular ",
        math(String.raw`p'`),
        " and ",
        math(String.raw`q'`),
        " are never both even. We distinguish two cases.",
      ]),
      list([
        [
          "**case A**: ",
          math(String.raw`p',q'`),
          " both odd. Put ",
          math(String.raw`\lambda_0:=v_2(p'+q')\ \ (\ge1)`),
          " and ",
          math(String.raw`\lambda_-:=v_2(p'-q')`),
          " (which is ",
          math(String.raw`+\infty`),
          " when ",
          math(String.raw`p'=q'`),
          ").",
        ],
        [
          "**case B**: one of them even. Write ",
          math(String.raw`c_e`),
          " for the even coefficient and ",
          math(String.raw`c_o`),
          " for the odd one, and put ",
          math(String.raw`\lambda_1:=v_2(c_e)\ \ (\ge1)`),
          " and ",
          math(String.raw`w:=v_2\bigl(c_e/2+c_o\bigr)`),
          ". (If ",
          math(String.raw`\lambda_1=1`),
          " then ",
          math(String.raw`c_e/2`),
          " is odd and so is ",
          math(String.raw`c_o`),
          ", whence ",
          math(String.raw`w\ge1`),
          ". If ",
          math(String.raw`\lambda_1\ge2`),
          " then ",
          math(String.raw`w=0`),
          " and it is not used below.)",
        ],
      ]),
      paragraph([
        "**(G''1 The case distinction is exclusive and exhaustive)** If ",
        math(String.raw`p',q'`),
        " are both odd then ",
        math(String.raw`\lambda_0\neq\lambda_-`),
        " and ",
        math(String.raw`\min(\lambda_0,\lambda_-)=1`),
        ". Hence case A splits, exclusively, into ",
        math(String.raw`\lambda_0=1`),
        " (case A",
        math(String.raw`\alpha`),
        "; the torus ",
        math(String.raw`(p,q)=(1,1)`),
        " lies here) and ",
        math(String.raw`\lambda_0\ge2`),
        " (case A",
        math(String.raw`\beta`),
        "). Likewise, if ",
        math(String.raw`m\ge2`),
        " and ",
        math(String.raw`a',b'`),
        " are both odd, then ",
        math(String.raw`s:=v_2(a'-b')`),
        " and ",
        math(String.raw`t:=v_2(a'+b')`),
        " satisfy ",
        math(String.raw`s,t\ge1`),
        " and ",
        math(String.raw`\min(s,t)=1`),
        ", and ",
        math(String.raw`s\ge m`),
        " and ",
        math(String.raw`t\ge m`),
        " never hold simultaneously. **Without these two statements one cannot assert that the case distinctions below are exclusive and exhaustive.**",
      ]),
      paragraph([
        "**(G''2 This family is of type III without exception)** For every ",
        math(String.raw`p,q\ge1`),
        " the tower over ",
        math(String.raw`X(p,q)`),
        " is degenerate and moreover has a point of infinite vanishing depth ",
        math(String.raw`\theta=\infty`),
        "; that is, it carries a term ",
        math(String.raw`n2^{n}`),
        " (type III). Of the trichotomy that part (G'3) of ",
        ref("paper_prop_G_infty"),
        " provides for odd ",
        math(String.raw`\ell`),
        " (non-degenerate / no exceptional line / an exceptional line), the first two classes are empty for this family at ",
        math(String.raw`\ell=2`),
        ". **This, however, is a property of the family and not of general towers** at ",
        math(String.raw`\ell=2`),
        " (see the limitations below).",
      ]),
      paragraph([
        "**(G''3 The valuation point by point.)** Two branches occur here that are absent for odd ",
        math(String.raw`\ell`),
        ". Let ",
        math(String.raw`n\ge1`),
        ", let ",
        math(String.raw`g`),
        " be a primitive ",
        math(String.raw`2^{n}`),
        "th root of unity and let ",
        math(String.raw`(a,b)\in(\mathbb{Z}/2^{n})^{2}\setminus\{0\}`),
        ". Put ",
        math(String.raw`\varepsilon:=\min(v_2(a),v_2(b))`),
        ", ",
        math(String.raw`m:=n-\varepsilon\ (\ge1)`),
        ", ",
        math(String.raw`h:=g^{2^{\varepsilon}}`),
        " (a primitive ",
        math(String.raw`2^{m}`),
        "th root of unity), ",
        math(String.raw`a':=a/2^{\varepsilon}`),
        ", ",
        math(String.raw`b':=b/2^{\varepsilon}`),
        " and ",
        math(String.raw`\varphi_m:=\varphi(2^{m})=2^{m-1}`),
        ", so that ",
        math(String.raw`E(g^a,g^b)=-(p'f_h(a')+q'f_h(b'))`),
        ".",
      ]),
      paragraph([
        "**Case [A]: both coefficients odd.** Assume ",
        math(String.raw`p',q'`),
        " both odd, ",
        math(String.raw`m\ge2`),
        " and ",
        math(String.raw`a',b'`),
        " both odd. Then, with ",
        math(String.raw`r:=\max(s,t)`),
        ",",
      ]),
      displayMath(
        String.raw`\varphi_m\,v_2\bigl(E(h^{a'},h^{b'})\bigr)=
        \begin{cases}
        \lambda_0\varphi_m+2 & (r\ge m,\ \text{i.e. } a'\equiv\pm b'\ \text{; on an exceptional line})\\
        2^{m} & (\lambda_0=1\ \text{and}\ r=m-1\ \text{; }\textbf{saturation})\\
        2+2^{\,r} & (2\le r\le m-1).
        \end{cases}`,
      ),
      paragraph([
        "If exactly one of ",
        math(String.raw`a',b'`),
        " is odd then ",
        math(String.raw`\varphi_m v_2(E)=2`),
        ". For ",
        math(String.raw`m=1`),
        " one has ",
        math(String.raw`v_2(E)=2+\lambda_0`),
        " (when ",
        math(String.raw`a'=b'=1`),
        ") and ",
        math(String.raw`2`),
        " (when one of them is ",
        math(String.raw`0`),
        ").",
      ]),
      paragraph([
        "**Case [B]: one coefficient even.** Here one may take ",
        math(String.raw`m=n`),
        ". Write ",
        math(String.raw`a_e,a_o`),
        " for the exponents attached to ",
        math(String.raw`c_e,c_o`),
        " and put",
      ]),
      displayMath(
        String.raw`U_e:=\lambda_1\varphi_n+2^{\,v_2(a_e)+1},\qquad U_o:=2^{\,v_2(a_o)+1}`,
      ),
      paragraph([
        " (with ",
        math(String.raw`U_e=+\infty`),
        " when ",
        math(String.raw`a_e\equiv0`),
        ", and ",
        math(String.raw`U_o=+\infty`),
        " when ",
        math(String.raw`a_o\equiv0`),
        "). If ",
        math(String.raw`U_e\neq U_o`),
        " then ",
        math(String.raw`\varphi_n v_2(E)=\min(U_e,U_o)`),
        ". One has ",
        math(String.raw`U_e=U_o<\infty`),
        " **if and only if** ",
        math(String.raw`\lambda_1=1`),
        " and ",
        math(String.raw`\bigl(v_2(a_e),v_2(a_o)\bigr)=(n-2,\,n-1)`),
        " (so that ",
        math(String.raw`n\ge2`),
        " is necessary), and at such a point the true value is",
      ]),
      displayMath(String.raw`v_2\bigl(E\bigr)=2+w,\qquad w=v_2\bigl(c_e/2+c_o\bigr)\ge1,`),
      paragraph([
        "which is **strictly deeper** than the naive value ",
        math(String.raw`\min(U_e,U_o)/\varphi_n=2`),
        ". This is the branch we call **cancellation**.",
      ]),
      paragraph([
        "**Saturation and cancellation are branches that do not occur** for odd ",
        math(String.raw`\ell`),
        ". Saturation comes from ",
        math(String.raw`1+h^{N}=0`),
        ", that is, from ",
        math(String.raw`h^{N}=-1`),
        ", which is a consequence of ",
        math(String.raw`a'\mp b'`),
        " being both even and hence cannot happen for odd ",
        math(String.raw`\ell`),
        ". In the cancellation branch one descends **not to the next digit of the exponent, but to the next digit of the coefficient**; the digit in question is the next ",
        math(String.raw`2`),
        "-adic digit ",
        math(String.raw`w`),
        " of ",
        math(String.raw`c_e`),
        ", and **a single step suffices.**",
      ]),
      paragraph([
        "**(G''4 The closed form)** For every ",
        math(String.raw`n\ge1`),
        ",",
      ]),
      displayMath(
        String.raw`\mathrm{ord}_2(\kappa_n)=\mu\,(4^{n}-1)+
        \begin{cases}
        2n\,2^{n}+4\cdot2^{n}-6n-1 & (\text{A}\alpha:\ \lambda_0=1)\\
        2n\,2^{n}+2\lambda_0\,2^{n}-2n-3\lambda_0+2 & (\text{A}\beta:\ \lambda_0\ge2)\\
        2n\,2^{n}+\lambda_1\,(2^{n}-1) & (\text{B}:\ \lambda_1\ge2,\ \text{and } n=1 \text{ when B and }\lambda_1=1)\\
        2n\,2^{n}+2n-1+2w & (\text{B}:\ \lambda_1=1,\ n\ge2).
        \end{cases}`,
      ),
      paragraph([
        "**The fourth line is restricted to** ",
        math(String.raw`n\ge2`),
        ", because the condition ",
        math(String.raw`v_2(a_e)=n-2`),
        " for cancellation cannot be met when ",
        math(String.raw`n=1`),
        "; the value at ",
        math(String.raw`n=1`),
        " **is given by the third line.** In terms of the five coefficients of ",
        ref("paper_prop_W"),
        " we have ",
        math(String.raw`a=\mu`),
        " and ",
        math(String.raw`b=2`),
        " in all four cases, while ",
        math(String.raw`(c,d)`),
        " equals ",
        math(String.raw`(4,-6)`),
        ", ",
        math(String.raw`(2\lambda_0,-2)`),
        ", ",
        math(String.raw`(\lambda_1,0)`),
        " and ",
        math(String.raw`(0,+2)`),
        " respectively. In particular the coefficient of ",
        math(String.raw`n\,2^{n}`),
        " equals ",
        math(String.raw`2`),
        " in all four cases, **the same value** as for odd ",
        math(String.raw`\ell`),
        " in part (G'3) of ",
        ref("paper_prop_G_infty"),
        ". What differs is the coefficient of ",
        math(String.raw`2^{n}`),
        ", the coefficient of ",
        math(String.raw`n`),
        ", and the fact that an invariant ",
        math(String.raw`w`),
        " absent for odd ",
        math(String.raw`\ell`),
        " is required.",
      ]),
      paragraph([
        "**(G''5 The range in which the closed form for odd primes remains correct.)** The prime here is ",
        math(String.raw`\ell=2`),
        ". The closed form ",
        math(String.raw`\mu(\ell^{2n}-1)+2n\ell^{n}+\Lambda(\ell^{n}-1)`),
        " of part (G'3) of ",
        ref("paper_prop_G_infty"),
        ", read at ",
        math(String.raw`\ell=2`),
        ", is correct for all ",
        math(String.raw`n\ge1`),
        " **precisely in case B with the even coefficient of valuation at least two**, that is, for ",
        math(String.raw`\lambda_1\ge2`),
        " (and then ",
        math(String.raw`\Lambda=\lambda_1`),
        "). The other three cases carry a linear term in ",
        math(String.raw`n`),
        " (namely ",
        math(String.raw`-6n,\ -2n,\ +2n`),
        ") which the formula of (G'3) does not have, so they cannot agree.",
      ]),
      paragraph([
        "**Explicitly, no use is made of the real numbers.** What is used is (a) the valuation at the unique prime above ",
        math(String.raw`2`),
        " of ",
        math(String.raw`\mathbb{Q}(\zeta_{2^{m}})`),
        " (a countable algebraic extension of ",
        math(String.raw`\mathbb{Q}`),
        "), (b) the ",
        math(String.raw`2`),
        "-adic valuation on ",
        math(String.raw`\mathbb{Z}`),
        ", and (c) finite computations in ",
        math(String.raw`\mathbb{Z}/2^{m}`),
        ".",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "**This proposition is confined to the family described above; no closed form has been obtained for general towers.** The family in question is ",
          math(String.raw`p(1,0)+q(0,1)`),
          " and the prime is ",
          math(String.raw`\ell=2`),
          " throughout, here and in the limitations that follow. What makes the proof work is that ",
          math(String.raw`\bar{\tilde E}`),
          " factors into binomials, so that the vanishing depth on each layer has the explicit shape ",
          math(String.raw`2+2^{r}`),
          ". For a general tower the ",
          math(String.raw`2`),
          "-adic valuation of the quantity playing the role of the constant term at which saturation occurs varies from case to case, and the layer structure is no longer as simple as it is for the family.",
        ],
        [
          "**Outside the family, the identification of the coefficient of the type III term has numerical support only and is not proved.** The assertion in question is that ",
          math(String.raw`n2^{n}`),
          " has coefficient ",
          math(String.raw`b=\sum_{P\in S_\infty}j^{*}(P)`),
          " (part (K6) of ",
          ref("paper_prop_K"),
          ") for general towers at ",
          math(String.raw`\ell=2`),
          ". In a full sweep of the declared population — the 430 towers whose edge number is ",
          math(String.raw`\le3`),
          " — the 174 towers that fell within its scope all agreed, with no disagreement; but this is a decision about that finite population and **asserts nothing outside it**. Regarded as a sample, it only excludes failure rates above ",
          math(String.raw`1.7\%`),
          ".",
        ],
        [
          "**The statement in (G''2) that the tower is always of type III is specific to the family.** The same population contains 39 towers at ",
          math(String.raw`\ell=2`),
          " with ",
          math(String.raw`b=0`),
          " (type II). Likewise, that no tower of the population has coefficient ",
          math(String.raw`0`),
          " in front of ",
          math(String.raw`n`),
          " is a consequence of how the population was designed (the bound on the number of edges) and not a property of ",
          math(String.raw`\ell=2`),
          " in general. **An observation of zero occurrences is not to be read as a property.**",
        ],
        [
          "The hypothesis of part (J4) of ",
          ref("paper_prop_J"),
          " (uniqueness of the minimising point at sufficiently deep levels) fails, for this family at ",
          math(String.raw`\ell=2`),
          ", in every pair of case A and in case B with ",
          math(String.raw`\lambda_1=1`),
          ", and holds only in case B with ",
          math(String.raw`\lambda_1\ge2`),
          " — exactly the case distinction of (G''5). **We do not claim that this failure is specific to** ",
          math(String.raw`\ell=2`),
          ": as recorded in the limitations of ",
          ref("paper_prop_K"),
          ", there are towers at odd primes for which it fails too. Even so the coefficient of ",
          math(String.raw`n2^{n}`),
          " equals ",
          math(String.raw`2`),
          " in all four cases, and ",
          math(String.raw`\sum_{P\in S_\infty}j^{*}(P)`),
          " equals ",
          math(String.raw`2`),
          " for every pair as well (in case A because ",
          math(String.raw`S_\infty`),
          " consists of two points with ",
          math(String.raw`j^{*}=1`),
          " each, in case B because it consists of one point with ",
          math(String.raw`j^{*}=2`),
          ").",
        ],
        [
          "**Even where the minimising point is not unique, the value itself is furnished unconditionally by the resultant formula (R4)** of ",
          ref("paper_prop_R"),
          ". Accordingly ",
          math(String.raw`w`),
          " is not a quantity needed in order to compute the value; **it is the quantity needed in order to express that value in closed form, as an invariant of the pair of loop counts**, that is, of ",
          math(String.raw`(p,q)`),
          ".",
        ],
        [
          "This proposition is confined to ",
          math(String.raw`d=2`),
          ".",
        ],
        [
          "**We claim no novelty.** The value in case A",
          math(String.raw`\alpha`),
          " itself — for the torus ",
          math(String.raw`(p,q)=(1,1)`),
          " it reads ",
          math(String.raw`\mathrm{ord}_2(\kappa_n)=2n2^{n}+4\cdot2^{n}-6n-1`),
          " — already occurs as a numerical example in DuBose–Vallières (Alg. Comb. 6 (2023), alco.304), §7, Example (1). That the Iwasawa invariants of a ",
          math(String.raw`\mathbb{Z}_\ell`),
          "-extension behave specially at ",
          math(String.raw`\ell=2`),
          " is in itself a standard phenomenon in Iwasawa theory, and it is quite possible that the content of this proposition is already covered by the framework of Monsky or of Cuoco–Monsky. **We have not been able to check this.**",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "In order not to depend tacitly on any lemma absent from the text, every fact used along the " +
        "way is stated here before it is used. The only tools are the valuation at the unique place above ",
        math(String.raw`2`),
        " of ",
        math(String.raw`\mathbb{Q}(\zeta_{2^{m}})`),
        ", the ",
        math(String.raw`2`),
        "-adic valuation of ",
        math(String.raw`\mathbb{Z}`),
        ", and finite computations in ",
        math(String.raw`\mathbb{Z}/2^{m}`),
        "; the field of real numbers is never entered.",
      ]),
      paragraph([
        "**(0) Starting point.** We use the same summation formula as in step (0) of the proof of ",
        ref("paper_prop_G_infty"),
        ", namely",
      ]),
      displayMath(
        String.raw`\mathrm{ord}_2(\kappa_n)=v_2(\kappa_X)-2n+\mu\bigl(4^{n}-1\bigr)+\Sigma_n,
        \qquad
        \Sigma_n=\sum_{\substack{\zeta^{2^n}=\xi^{2^n}=1\\ (\zeta,\xi)\neq(1,1)}}v_2\bigl(E(\zeta,\xi)\bigr)`,
      ),
      paragraph([
        "(here ",
        math(String.raw`\kappa(X)=1`),
        ", so ",
        math(String.raw`v_2(\kappa_X)=0`),
        "). Let ",
        math(String.raw`m\ge1`),
        ", let ",
        math(String.raw`h`),
        " be a primitive ",
        math(String.raw`2^{m}`),
        "-th root of unity, and put ",
        math(String.raw`\alpha_m:=1/\varphi_m=2^{1-m}`),
        " and ",
        math(String.raw`\nu(\cdot):=v_2(\cdot)`),
        ". Since ",
        math(String.raw`2`),
        " is totally ramified,",
      ]),
      displayMath(
        String.raw`v_2\bigl(h^{N}-1\bigr)=2^{\,\nu(N)}\alpha_m\ \ (\nu(N)<m),\qquad
        h^{N}-1=0\ \ (2^{m}\mid N),\qquad
        f_h(c)=h^{-c}\bigl(h^{c}-1\bigr)^2`,
      ),
      paragraph([
        "and consequently ",
        math(String.raw`v_2(f_h(c))=2\cdot2^{\,\nu(c)}\alpha_m`),
        " for ",
        math(String.raw`\nu(c)<m`),
        ". Furthermore ",
        math(String.raw`-1=h^{2^{m-1}}`),
        " gives ",
        math(String.raw`1+h^{N}=1-h^{\,N+2^{m-1}}`),
        ", so that",
      ]),
      displayMath(
        String.raw`\mathrm{ph}(e):=v_2\bigl(1+h^{N}\bigr)\Big|_{\nu(N)=e}=
        \begin{cases}
        2^{\,e}\alpha_m & (e\le m-2)\\
        +\infty & (e=m-1)\\
        1 & (e\ge m).
        \end{cases}`,
      ),
      paragraph([
        "The case ",
        math(String.raw`\mathrm{ph}(m-1)=+\infty`),
        " expresses the phenomenon ",
        math(String.raw`h^{N}=-1`),
        ", which does not occur at an odd prime. This is the source of the saturation below.",
      ]),
      paragraph([
        "**Proof of (G''1).** If ",
        math(String.raw`p',q'`),
        " are odd then ",
        math(String.raw`p'\pm q'`),
        " are even, so ",
        math(String.raw`\lambda_0,\lambda_-\ge1`),
        ". If we had ",
        math(String.raw`\lambda_0=\lambda_-=k`),
        ", then, since the sum of two elements of valuation exactly ",
        math(String.raw`k`),
        " has valuation ",
        math(String.raw`\ge k+1`),
        ", we would get ",
        math(String.raw`v_2\bigl((p'+q')+(p'-q')\bigr)=v_2(2p')=1\ge k+1`),
        ", hence ",
        math(String.raw`k\le0`),
        ", a contradiction. Therefore ",
        math(String.raw`\lambda_0\neq\lambda_-`),
        ", and the equality case of the non-archimedean estimate gives ",
        math(String.raw`\min(\lambda_0,\lambda_-)=v_2(2p')=1`),
        ".",
      ]),
      paragraph([
        "The same argument applies to ",
        math(String.raw`a',b'`),
        ". The numbers ",
        math(String.raw`a'\mp b'`),
        " are even, so ",
        math(String.raw`s,t\ge1`),
        "; from ",
        math(String.raw`(a'-b')+(a'+b')=2a'`),
        " with ",
        math(String.raw`a'`),
        " odd we get ",
        math(String.raw`\min(s,t)\le1`),
        ", hence ",
        math(String.raw`\min(s,t)=1`),
        ". And ",
        math(String.raw`s,t\ge m`),
        " would give ",
        math(String.raw`2^{m}\mid2a'`),
        ", hence ",
        math(String.raw`m\le1`),
        ", contradicting ",
        math(String.raw`m\ge2`),
        ".",
      ]),
      paragraph([
        "**Proof of (G''2).** The lowest homogeneous part of ",
        math(String.raw`g(T,S)=E(1+T,1+S)`),
        " is ",
        math(String.raw`H=-(p'T^2+q'S^2)`),
        ". Over ",
        math(String.raw`\mathbb{F}_2`),
        ", if ",
        math(String.raw`p',q'`),
        " are both odd then ",
        math(String.raw`H\equiv T^2+S^2=(T+S)^2`),
        " has the zero ",
        math(String.raw`(1{:}1)`),
        ", while if one of them is even (say ",
        math(String.raw`p'`),
        " is even) then ",
        math(String.raw`H\equiv S^2`),
        " has the zero ",
        math(String.raw`(1{:}0)`),
        "; in either case the tower is degenerate. By the criterion of ",
        ref("paper_prop_G_infty"),
        " (G'1), whose proof never uses the parity of ",
        math(String.raw`\ell`),
        " and hence applies verbatim, the case ",
        math(String.raw`\theta=\infty`),
        " occurs exactly when ",
        math(String.raw`2\mid p'q'(p'+q')`),
        "; but ",
        math(String.raw`p',q'`),
        " are never both even, so either both are odd and ",
        math(String.raw`2\mid p'+q'`),
        ", or one is even and ",
        math(String.raw`2\mid p'q'`),
        ". **The condition therefore always holds.**",
      ]),
      paragraph([
        "**Proof of (G''3).** Put ",
        math(String.raw`A=(p'+q')/2`),
        " and ",
        math(String.raw`B=(p'-q')/2`),
        " (exactly one of which is odd, by (G''1)). We use the following three identities.",
      ]),
      displayMath(
        String.raw`p'f_h(a')+q'f_h(b')=A\bigl(f_h(a')+f_h(b')\bigr)+B\bigl(f_h(a')-f_h(b')\bigr),`,
      ),
      displayMath(
        String.raw`f_h(a')+f_h(b')=h^{-b'}\bigl(1+h^{\,b'-a'}\bigr)\bigl(1+h^{\,a'+b'}\bigr)-4,
        \qquad
        f_h(a')-f_h(b')=-h^{-a'}\bigl(h^{\,a'-b'}-1\bigr)\bigl(h^{\,a'+b'}-1\bigr)`,
      ),
      paragraph([
        "(the first follows from ",
        math(String.raw`A+B=p'`),
        " and ",
        math(String.raw`A-B=q'`),
        ", the second and third by expanding the right-hand sides). Write the valuations of the three terms as",
      ]),
      displayMath(
        String.raw`T_1:=(\lambda_0-1)+\mathrm{ph}(s)+\mathrm{ph}(t),\qquad
        T_2:=(\lambda_0-1)+2,\qquad
        T_3:=(\lambda_--1)+\bigl(2^{s}+2^{t}\bigr)\alpha_m`,
      ),
      paragraph([
        "(so ",
        math(String.raw`T_1`),
        " is the product term of the second identity, ",
        math(String.raw`T_2`),
        " its term ",
        math(String.raw`-4`),
        ", and ",
        math(String.raw`T_3`),
        " the third identity; here ",
        math(String.raw`v_2(A)=\lambda_0-1`),
        " and ",
        math(String.raw`v_2(B)=\lambda_--1`),
        "). By (G''1) the smaller of the two is ",
        math(String.raw`1`),
        ", so for ",
        math(String.raw`r<m`),
        " we have ",
        math(String.raw`T_3=(\lambda_--1)+(2+2^{r})\alpha_m`),
        ".",
      ]),
      paragraph([
        "**If exactly one is odd** (say ",
        math(String.raw`a'`),
        " odd and ",
        math(String.raw`\nu(b')=\beta\ge1`),
        "): ",
        math(String.raw`v_2(p'f_h(a'))=2\alpha_m`),
        " is strictly smaller than ",
        math(String.raw`v_2(q'f_h(b'))=2\cdot2^{\beta}\alpha_m`),
        ", so ",
        math(String.raw`\varphi_mv_2(E)=2`),
        ". If ",
        math(String.raw`m=1`),
        ": here ",
        math(String.raw`h=-1`),
        " and ",
        math(String.raw`f_h(1)=-4`),
        ", so for ",
        math(String.raw`a'=b'=1`),
        " we get ",
        math(String.raw`E=(p'+q')\cdot4`),
        " and ",
        math(String.raw`v_2=2+\lambda_0`),
        ", while if one of them is ",
        math(String.raw`0`),
        " then ",
        math(String.raw`v_2=2`),
        ". If ",
        math(String.raw`r\ge m`),
        ": one factor of the third identity equals ",
        math(String.raw`0`),
        ", so ",
        math(String.raw`f_h(a')=f_h(b')`),
        ", that is ",
        math(String.raw`E=-(p'+q')f_h(b')`),
        " and ",
        math(String.raw`v_2(E)=\lambda_0+2\alpha_m`),
        " (an exact identity).",
      ]),
      paragraph([
        "From now on ",
        math(String.raw`2\le r\le m-1`),
        ". Case A",
        math(String.raw`\alpha`),
        " (",
        math(String.raw`\lambda_0=1,\ \lambda_-\ge2`),
        ", so ",
        math(String.raw`T_2=2`),
        "). If ",
        math(String.raw`r\le m-2`),
        " then ",
        math(String.raw`T_1=(2+2^{r})\alpha_m`),
        " and ",
        math(String.raw`T_3=(\lambda_--1)+T_1>T_1`),
        ", while ",
        math(String.raw`T_1\le(2+2^{m-2})2^{1-m}=2^{2-m}+2^{-1}<2=T_2`),
        ", so the minimum is attained only at ",
        math(String.raw`T_1`),
        " and ",
        math(String.raw`v_2(E)=(2+2^{r})\alpha_m`),
        ". If ",
        math(String.raw`r=m-1`),
        " then ",
        math(String.raw`\mathrm{ph}(m-1)=+\infty`),
        " gives ",
        math(String.raw`T_1=+\infty`),
        ", and ",
        math(String.raw`T_3\ge1+2^{2-m}+1>2=T_2`),
        ", so the minimum is attained only at ",
        math(String.raw`T_2=2`),
        " and ",
        math(String.raw`\varphi_mv_2(E)=2^{m}`),
        ". **This is the saturation.**",
      ]),
      paragraph([
        "Case A",
        math(String.raw`\beta`),
        " (",
        math(String.raw`\lambda_0\ge2,\ \lambda_-=1`),
        ", so ",
        math(String.raw`T_2=\lambda_0+1\ge3`),
        "). If ",
        math(String.raw`r\le m-2`),
        " then ",
        math(String.raw`T_3=(2+2^{r})\alpha_m`),
        ", ",
        math(String.raw`T_1=(\lambda_0-1)+T_3>T_3`),
        " and ",
        math(String.raw`T_3<2<T_2`),
        ", so the minimum is attained only at ",
        math(String.raw`T_3`),
        ". If ",
        math(String.raw`r=m-1`),
        " then ",
        math(String.raw`T_1=+\infty`),
        " and ",
        math(String.raw`T_3=2^{2-m}+1\le2<3\le T_2`),
        " (using ",
        math(String.raw`m\ge2`),
        "), so again ",
        math(String.raw`T_3`),
        " is the unique minimum. In both subcases ",
        math(String.raw`\varphi_mv_2(E)=2+2^{r}`),
        ", which gives the three lines of [A].",
      ]),
      paragraph([
        "**Proof of [B].** Suppose ",
        math(String.raw`U_e=U_o<\infty`),
        ". Then ",
        math(String.raw`U_o=2^{\,\nu(a_o)+1}=2^{k}`),
        " is a power of ",
        math(String.raw`2`),
        ", and since the first summand of ",
        math(String.raw`U_e=\lambda_1 2^{n-1}+2^{\,\nu(a_e)+1}=2^{k}\le2^{n}`),
        " is positive we get ",
        math(String.raw`\lambda_1 2^{n-1}<2^{n}`),
        ", that is ",
        math(String.raw`\lambda_1=1`),
        ". Then ",
        math(String.raw`2^{n-1}+2^{\,\nu(a_e)+1}=2^{k}`),
        ", and a sum of two powers of ",
        math(String.raw`2`),
        " is a power of ",
        math(String.raw`2`),
        " only when the two are equal, so ",
        math(String.raw`\nu(a_e)=n-2`),
        " and ",
        math(String.raw`k=n`),
        " (whence ",
        math(String.raw`n\ge2`),
        "). Conversely, under this condition ",
        math(String.raw`U_e=U_o=2^{n}`),
        ".",
      ]),
      paragraph([
        "At that tie, ",
        math(String.raw`\nu(a_o)=n-1`),
        " gives ",
        math(String.raw`h^{a_o}=-1`),
        " and ",
        math(String.raw`f_h(a_o)=-4`),
        ", while ",
        math(String.raw`\nu(a_e)=n-2`),
        " makes ",
        math(String.raw`h^{a_e}`),
        " of order ",
        math(String.raw`4`),
        " with ",
        math(String.raw`f_h(a_e)=-2`),
        "; hence ",
        math(String.raw`-E=-2c_e-4c_o=-4(c_e/2+c_o)`),
        ", that is ",
        math(String.raw`v_2(E)=2+w`),
        ". Since ",
        math(String.raw`\lambda_1=1`),
        ", the number ",
        math(String.raw`c_e/2`),
        " is odd, and ",
        math(String.raw`c_o`),
        " is odd too, so ",
        math(String.raw`w\ge1`),
        " and the value is strictly deeper than the naive ",
        math(String.raw`\min(U_e,U_o)/\varphi_n=2`),
        ". What one descends to is not the next digit of the exponent but the second ",
        math(String.raw`2`),
        "-adic digit of the coefficient, and the descent closes after one step.",
      ]),
      paragraph([
        "**Proof of (G''4).** The points of level exactly ",
        math(String.raw`m`),
        " correspond bijectively to the pairs ",
        math(String.raw`(a',b')\in(\mathbb{Z}/2^{m})^2`),
        " with ",
        math(String.raw`\min(\nu(a'),\nu(b'))=0`),
        ". **In case A**, writing ",
        math(String.raw`S_m`),
        " for the subtotal, we have",
      ]),
      displayMath(
        String.raw`S_1=2+2+(2+\lambda_0)=6+\lambda_0,\qquad
        S_m^{\mathrm{off}}=2^{2m-1}\cdot2^{2-m}=2^{m+1}\ \ (m\ge2)`,
      ),
      paragraph([
        "(for ",
        math(String.raw`m=1`),
        " the points are the three pairs ",
        math(String.raw`(1,0),(0,1),(1,1)`),
        "; the points with exactly one odd coordinate number ",
        math(String.raw`2^{2m-1}`),
        " and each has ",
        math(String.raw`v_2=2^{2-m}`),
        "). For the points with both coordinates odd, fix ",
        math(String.raw`a'`),
        " odd (which can be done in ",
        math(String.raw`2^{m-1}`),
        " ways); then the number of ",
        math(String.raw`b'`),
        " with ",
        math(String.raw`r=j\ (2\le j\le m-1)`),
        " is ",
        math(String.raw`2^{m-j}`),
        ", and the number with ",
        math(String.raw`r\ge m`),
        " is ",
        math(String.raw`2`),
        " (check: ",
        math(String.raw`\sum_{j=2}^{m-1}2^{m-j}+2=2^{m-1}`),
        "); multiplying by the values divided by ",
        math(String.raw`\varphi_m`),
        " gives the layer contributions ",
        math(String.raw`2^{2-j}+2`),
        " and ",
        math(String.raw`2\lambda_0+2^{3-m}`),
        ".",
      ]),
      paragraph([
        "Hence the subtotal ",
        math(String.raw`S_m^{\mathrm{diag}}`),
        " over the points with both coordinates odd is as follows.",
      ]),
      displayMath(
        String.raw`S_m^{\mathrm{diag}}=\begin{cases}
        (m+1)2^{m}-4 & (\lambda_0=1)\\
        2^{m}\,(m-1+\lambda_0) & (\lambda_0\ge2).
        \end{cases}`,
      ),
      paragraph([
        "Indeed, for ",
        math(String.raw`\lambda_0\ge2`),
        " (no saturation) the bracket equals ",
        math(String.raw`\sum_{j=2}^{m-1}(2^{2-j}+2)+2\lambda_0+2^{3-m}=2m-2+2\lambda_0`),
        ", and multiplying by ",
        math(String.raw`2^{m-1}`),
        " gives the lower line (for ",
        math(String.raw`m=2`),
        " the sum is empty and the two agree). For ",
        math(String.raw`\lambda_0=1`),
        " the layer ",
        math(String.raw`r=m-1`),
        " saturates and contributes ",
        math(String.raw`4`),
        ", so for ",
        math(String.raw`m\ge3`),
        " the bracket equals ",
        math(String.raw`2m+2-2^{3-m}`),
        " and multiplying by ",
        math(String.raw`2^{m-1}`),
        " gives the upper line (for ",
        math(String.raw`m=2`),
        " all points have ",
        math(String.raw`r\ge m`),
        ", and the direct value ",
        math(String.raw`4(\lambda_0+1)=8`),
        " agrees).",
      ]),
      paragraph([
        "**Summation.** For ",
        math(String.raw`\lambda_0=1`),
        " we have ",
        math(String.raw`S_m=2^{m+1}+(m+1)2^{m}-4`),
        " for ",
        math(String.raw`m\ge2`),
        " and ",
        math(String.raw`S_1=7`),
        ", while the same formula evaluated at ",
        math(String.raw`m=1`),
        " gives ",
        math(String.raw`4`),
        ", a discrepancy of ",
        math(String.raw`3`),
        "; hence",
      ]),
      displayMath(
        String.raw`\Sigma_n=3+\sum_{m=1}^{n}\bigl[2^{m+1}+(m+1)2^{m}-4\bigr]=2n\,2^{n}+4\cdot2^{n}-4n-1`,
      ),
      paragraph([
        "(using ",
        math(String.raw`\sum_{m=1}^{n}(m+1)2^{m}=n2^{n+1}`),
        "). Adding ",
        math(String.raw`-2n`),
        " gives the first line. For ",
        math(String.raw`\lambda_0\ge2`),
        " we have ",
        math(String.raw`S_m=2^{m+1}+2^{m}(m-1+\lambda_0)`),
        " and ",
        math(String.raw`S_1=6+\lambda_0`),
        ", a discrepancy of ",
        math(String.raw`2-\lambda_0`),
        ", so ",
        math(String.raw`\Sigma_n=2^{n+1}(n+\lambda_0)+2-3\lambda_0`),
        ", and adding ",
        math(String.raw`-2n`),
        " gives the second line.",
      ]),
      paragraph([
        "Case B with ",
        math(String.raw`\lambda_1\ge2`),
        ". If ",
        math(String.raw`a_o\not\equiv0`),
        " then ",
        math(String.raw`U_e\ge2\cdot2^{n-1}+2>2^{n}\ge U_o`),
        ", and if ",
        math(String.raw`a_o\equiv0`),
        " then ",
        math(String.raw`U_o=+\infty`),
        ", so no tie occurs. The number of ",
        math(String.raw`x`),
        " with ",
        math(String.raw`\nu(x)=\gamma<n`),
        " is ",
        math(String.raw`2^{n-\gamma-1}`),
        ", so",
      ]),
      displayMath(
        String.raw`\varphi_n\Sigma_n=\sum_{\gamma=0}^{n-1}2^{n-\gamma-1}\cdot2^{n}\cdot2^{\gamma+1}
        +\sum_{\gamma=0}^{n-1}2^{n-\gamma-1}\bigl(\lambda_1 2^{n-1}+2^{\gamma+1}\bigr)
        =n2^{2n}+\lambda_1 2^{n-1}\bigl(2^{n}-1\bigr)+n2^{n},`,
      ),
      paragraph([
        "that is ",
        math(String.raw`\Sigma_n=2n\,2^{n}+\lambda_1(2^{n}-1)+2n`),
        ", and adding ",
        math(String.raw`-2n`),
        " gives the third line. In case B with ",
        math(String.raw`\lambda_1=1`),
        " and ",
        math(String.raw`n\ge2`),
        " we correct the computation above. The only place where one departs from the baseline is ",
        math(String.raw`a_o=2^{n-1}`),
        " (a single element); there the discrepancy is ",
        math(String.raw`2^{\,\nu(a_e)+1}-2^{n-1}`),
        " when ",
        math(String.raw`\nu(a_e)\le n-3`),
        ", it is ",
        math(String.raw`2^{n-1}w`),
        " when ",
        math(String.raw`\nu(a_e)=n-2`),
        " (two elements), and it is ",
        math(String.raw`0`),
        " otherwise. The total discrepancy is",
      ]),
      displayMath(
        String.raw`\sum_{\gamma=0}^{n-3}2^{n-\gamma-1}\bigl(2^{\gamma+1}-2^{n-1}\bigr)+2\cdot2^{n-1}w
        =(n-2)2^{n}-2^{2n-1}+2^{n+1}+2^{n}w`,
      ),
      paragraph([
        ", and dividing by ",
        math(String.raw`\varphi_n=2^{n-1}`),
        " gives ",
        math(String.raw`2n-2^{n}+2w`),
        ". Hence ",
        math(String.raw`\Sigma_n=2n2^{n}+4n-1+2w`),
        ", and adding ",
        math(String.raw`-2n`),
        " gives the fourth line. For ",
        math(String.raw`n=1`),
        " the condition ",
        math(String.raw`\nu(a_e)=n-2=-1`),
        " is impossible, so no tie occurs, the computation for ",
        math(String.raw`\lambda_1\ge2`),
        " applies verbatim, and the third line gives the value.",
      ]),
      paragraph([
        "**Proof of (G''5).** In case B with ",
        math(String.raw`\lambda_1\ge2`),
        " the third line above agrees with the formula of ",
        ref("paper_prop_G_infty"),
        " (G'3) (with ",
        math(String.raw`\Lambda=\lambda_1`),
        "). Conversely, the formula of (G'3) has no term linear in ",
        math(String.raw`n`),
        ", whereas case A",
        math(String.raw`\alpha`),
        " has ",
        math(String.raw`-6n`),
        ", case A",
        math(String.raw`\beta`),
        " has ",
        math(String.raw`-2n`),
        " and case B with ",
        math(String.raw`\lambda_1=1`),
        " has ",
        math(String.raw`+2n`),
        ", so they do not agree (already the two values ",
        math(String.raw`n=1,2`),
        " differ).",
      ]),
      paragraph([
        "**On the limits.** What made the counting above possible is that ",
        math(String.raw`\bar{\tilde E}`),
        " factors into binomials and that the valuation of each layer has the explicit shape ",
        math(String.raw`2+2^{r}`),
        ". For a general tower at ",
        math(String.raw`\ell=2`),
        " the ",
        math(String.raw`2`),
        "-adic valuation of the quantity playing the role of the constant term at which saturation occurs ",
        "varies from case to case, so the counting does not go through as it stands. The statements about the ",
        "declared population listed among the limits (agreement for ",
        math(String.raw`174`),
        " towers, and the existence of ",
        math(String.raw`39`),
        " towers of type II) are decisions about that finite population obtained by exhaustive search, and ",
        "assert nothing about anything outside it.",
      ]),
    ],
  },
]);
