/**
 * 論文本体 第 9 章（英語版）: 消滅深度の p 進化（命題 J）。
 *
 * **正本は日本語版 `../../../content/008_theta_padic.ts` である。**
 * ブロック id・`labels`・`habitat`・`verification`・`kind`・`proof` の有無、および**数式**は
 * 日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `integrable-lattice/docs/paper001-en-glossary.md` を正本とする。
 *
 * ## 訳出で構造を変えた箇所（内容は変えていない）
 *
 * 日本語版は `**強調**` の中に数式ノードを含めて書いている箇所が 5 つある（(J2) の判定条件、
 * (J4) の「n ℓ^n 項は現れない」、(J5) の「すべての n≥0 で」、(J6) の表題、(J6) の
 * 「(J4) の仮定は不要」）。英語版の生成器は**ノードをまたぐ `**` を拒否する**ので、
 * 強調文を数式なしで言い切り、その直後に記号を対応づける文を置いた（用語集 §1.2d の前例と同じ扱い）。
 * **数式ノードの多重集合は日本語版と完全に一致させてある。**
 *
 * ## 記号 Λ の三度目の衝突（読者への注意として本文に足した）
 *
 * (J2) は `\Lambda(r):=\min_j(e_j+j\ell^r)` と置く。これは序章の対数順序群 Λ とも、
 * 命題 G′ が例外直線の和として置く Λ とも**無関係な別の量**である。数式は変更できないので、
 * 英語版では地の文で「この記号はこの式に限った局所的なものである」と断った
 * （命題 G′ の英訳が同じ処置をしている）。
 *
 * ## 相互参照
 *
 * 日本語版の `ref("paper_prop_G")`（2 回）と `ref("paper_prop_G_infty")`（3 回）は、
 * 両ラベルとも英語版に実在するのでそのまま `ref()` で張ってある（地の文へ落としていない）。
 *
 * ## 日本語版に残っている宛先の無い参照（2026-08-01 の校閲で英語版だけ直した）
 *
 * (J4) の段落で、日本語版は「命題 G の **(G1″)** はこの L=1 の場合である」と書いている。
 * **命題 G に (G1″) という項目は存在しない**（あるのは (G1)・(G1′)・(G2)〜(G6)）。
 * L=1 で一致するのは **(G6)**（`Θ=Σ_{P∈P¹(F_ℓ)}θ(P)` を用いる閉形式。φ(ℓ)=ℓ-1 なので
 * (J4) の `Θ_L/φ(ℓ^L)` が L=1 でちょうど (G6) の `Θ/(ℓ-1)` になる）である。
 * 英語版は宛先の無い参照を投稿稿に残せないので **(G6)** へ直した。
 * **日本語版は正本なのでこの担当は触っていない。同じ誤りが残っているので、直す必要がある**
 * （呼び出し元への申し送り）。
 */

import { cite, defineTranslatedBlocks as defineBlocks, displayMath, list, math, paragraph, refInTranslation as ref } from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_090_heading_theta_padic",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/008_theta_padic.ts", ordinal: 1 },
    title: {
      text:
        "The p-adic refinement of the vanishing depth — the type of a tower is decided by " +
        "its p-adic points of infinite depth",
    },
    labels: [],
  },
  {
    id: "paper_091_theorem_theta_padic",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/008_theta_padic.ts", ordinal: 2 },
    title: {
      text:
        "Proposition J — the digit theorem, the Newton formula along a fibre, and the criterion " +
        "distinguishing type II from type III",
    },
    labels: ["paper_prop_J"],
    habitat: "Qbar",
    verification: ["sagemath/check/cycle19_T3_theta_ge_ell"],
    lean: [
      "IntegrableLattice.choose_cast_of_lt",
      "IntegrableLattice.choose_cast_pow",
      "IntegrableLattice.choose_cast_pow_succ",
      "IntegrableLattice.Abar_shift",
      "IntegrableLattice.Abar_congr",
      "IntegrableLattice.Abar_shift_pow_succ",
      "IntegrableLattice.Bbar_diag",
      "IntegrableLattice.cexDigit_fails",
      "IntegrableLattice.sum_level_stab",
      "IntegrableLattice.level_ratio_indep",
      "IntegrableLattice.layer_sum",
      "IntegrableLattice.sum_mul_pow",
      "IntegrableLattice.sum_Theta_J8",
      "IntegrableLattice.ordKappa_J8",
    ],
    statement: [
      paragraph([
        "We work in the setting of ",
        ref("paper_prop_G"),
        ": a ",
        math(String.raw`\mathbb{Z}_\ell^2`),
        "-tower of graphs, in which ",
        math(String.raw`\kappa_n`),
        " denotes the number of spanning trees at level ",
        math(String.raw`n`),
        ", with ",
        math(String.raw`\tilde E=\sum c_{pq}z^pw^q`),
        " and ",
        math(String.raw`A_m(a,b)=\sum c_{pq}\binom{pa+qb}{m}`),
        ". The vanishing depth is defined by ",
        math(String.raw`\theta(a,b)=\min\{m:\ell\nmid A_m(a,b)\}`),
        ". **Convention (not omissible)**: if there is no such ",
        math(String.raw`m`),
        " at all, read ",
        math(String.raw`\theta(a,b):=\infty`),
        ". **The set** ",
        math(String.raw`S_\infty`),
        "**, the protagonist of this proposition, is defined only under this reading**: the set ",
        math(String.raw`S_\infty=\{P:\theta(P)=\infty\}`),
        " of (J4) below would be empty under the reading ",
        math(String.raw`\min\emptyset=0`),
        ", forcing the coefficient ",
        math(String.raw`b`),
        " of ",
        math(String.raw`n\ell^n`),
        " to be ",
        math(String.raw`0`),
        " always.",
      ]),
      paragraph([
        "**(J1 The digit theorem)** Let ",
        math(String.raw`L\ge1`),
        ". For every integer ",
        math(String.raw`m`),
        " in the range ",
        math(String.raw`0\le m\le\ell^{L}`),
        ", the residue ",
        math(String.raw`A_m(a,b)\bmod\ell`),
        " is a function of ",
        math(String.raw`(a,b)\bmod\ell^{L}`),
        " alone. For the borderline value ",
        math(String.raw`m=\ell^{L}`),
        " the argument uses the identity ",
        math(String.raw`A_1\equiv0`),
        ", which holds as an identity of integers because ",
        math(String.raw`D(z,w)=D(z^{-1},w^{-1})`),
        " (see part (G6) of ",
        ref("paper_prop_G"),
        "). This hypothesis cannot be dropped: without it the case ",
        math(String.raw`m=\ell^{L}`),
        " is false, and ",
        math(String.raw`\tilde E=z`),
        " with ",
        math(String.raw`\ell=3`),
        " and ",
        math(String.raw`L=1`),
        " is a counterexample, since ",
        math(String.raw`\bar A_3(3,0)=1\neq0=\bar A_3(0,0)`),
        ". For ",
        math(String.raw`m<\ell^{L}`),
        " the statement follows from Lucas' theorem alone and does not use the hypothesis. Consequently, if ",
        math(String.raw`\theta(a,b)\le\ell^{L}`),
        ", then ",
        math(String.raw`\theta`),
        " is determined by ",
        math(String.raw`(a,b)\bmod\ell^{L}`),
        ". In particular ",
        math(String.raw`\theta`),
        " extends to a function on ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", and it is locally constant at every point at which it is finite.",
      ]),
      paragraph([
        "**(J1' The threshold is sharp)** The failure at ",
        math(String.raw`m=\ell^{L}+1`),
        " is governed by the polar form ",
        math(String.raw`\bar B(x,y)=\sum\bar c_{pq}\overline{(px_1+qx_2)}\,\overline{(py_1+qy_2)}`),
        " of ",
        math(String.raw`\bar A_2`),
        ", and does not depend on ",
        math(String.raw`L`),
        ". Hence, if ",
        math(String.raw`\ell`),
        " is an odd prime, the criterion reads “failure ",
        math(String.raw`\iff k=2`),
        "” (here ",
        math(String.raw`k`),
        " is, as in ",
        ref("paper_prop_W"),
        ", the degree of the lowest-degree homogeneous part of ",
        math(String.raw`\bar E`),
        "), whereas for ",
        math(String.raw`\ell=2`),
        " it reads “failure ",
        math(String.raw`\iff\bar A_2`),
        " is not a square over ",
        math(String.raw`\mathbb{F}_2`),
        "”.",
      ]),
      paragraph([
        "**(J2 The Newton formula along a fibre)** Fix a direction ",
        math(String.raw`P_0=(1{:}c)\in\mathbb{P}^1(\mathbb{F}_\ell)`),
        ", write ",
        math(String.raw`\bar{\tilde E}(z,w(1+u))=\sum_j D_j(z,w)u^j`),
        " (so that ",
        math(String.raw`D_j=\sum\bar c_{pq}\binom qj z^pw^q`),
        "), and put ",
        math(String.raw`e_j:=\mathrm{ord}_{x=0}D_j\bigl(1+x,(1+x)^c\bigr)`),
        ". Then, for ",
        math(String.raw`\beta\in\mathbb{Z}_\ell`),
        " and ",
        math(String.raw`r=1+v_\ell(\beta)`),
        ",",
      ]),
      displayMath(
        String.raw`\theta(1,c+\ell\beta)\ \ge\ \Lambda(r):=\min_{j\ge0}\bigl(e_j+j\,\ell^{r}\bigr),`,
      ),
      paragraph([
        "and equality holds as soon as the minimum is attained at a single index. (The symbol Λ in this ",
        "formula is local to the present proposition; it is unrelated both to the logarithmic ordered ",
        "group of the introduction and to the sum over exceptional lines of the preceding proposition.) ",
        "**In particular, if the term of index zero is finite and every term of positive index already " +
          "exceeds it at the first level, then the vanishing depth is constant on the whole fibre above " +
          "the given direction.** In symbols: if ",
        math(String.raw`e_0<\infty`),
        " and ",
        math(String.raw`e_j+j\ell>e_0`),
        " for every ",
        math(String.raw`j\ge1`),
        ", then ",
        math(String.raw`\theta`),
        " takes the constant value ",
        math(String.raw`e_0`),
        " on the entire fibre above ",
        math(String.raw`P_0`),
        ". This criterion is a finite computation, and it contains strictly more cases than the condition ",
        math(String.raw`\theta(P_0)\le\ell`),
        " demanded by ",
        ref("paper_prop_G"),
        ".",
      ]),
      paragraph([
        "**(J3 Decomposition by level, and the prediction in general)** Put ",
        math(String.raw`\hat\theta_{M}(\zeta,\xi):=\varphi(\ell^{M})\,v_\ell(E(\zeta,\xi))`),
        ". Then, with no hypothesis whatever, ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}\hat\theta_{M}(P)`),
        " holds (the factor ",
        math(String.raw`\varphi`),
        " cancels completely); and if the minimum in ",
        math(String.raw`\min_m\bigl(\varphi(\ell^{M})v_\ell(A_m)+m\bigr)`),
        " is attained at a single index, then its value equals ",
        math(String.raw`\hat\theta_{M}`),
        ". Consequently ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)`),
        " is determined by a finite computation from the coefficients of ",
        math(String.raw`D`),
        " alone: neither the values of the tower nor any computation in a cyclotomic field is needed.",
      ]),
      paragraph([
        "**(J4 Distinguishing type II from type III)** If ",
        math(String.raw`\theta`),
        " is finite everywhere on ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", then ",
        math(String.raw`\theta`),
        " is bounded, factors through ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        " for some level, and, for ",
        math(String.raw`n`),
        " large enough,",
      ]),
      displayMath(
        String.raw`\mathrm{ord}_\ell(\kappa_n)=\mu(\ell^{2n}-1)+\frac{\Theta_L}{\varphi(\ell^{L})}\,\ell^{n}-2n+\nu,
        \qquad \Theta_L=\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{L})}\theta(P),`,
      ),
      paragraph([
        "that is, **the tower is then of type II**: no term of the shape ",
        math(String.raw`n\ell^n`),
        " occurs. Part (G6) of ",
        ref("paper_prop_G"),
        " is the case ",
        math(String.raw`L=1`),
        " of this. Conversely, if there is a direction with ",
        math(String.raw`\theta(P_0)=\infty`),
        " — that is, one on which ",
        math(String.raw`\bar E`),
        " vanishes identically along the one-parameter subgroup — then an ",
        math(String.raw`n\ell^n`),
        " term does occur, and, under the hypothesis that no cancellation takes place, its coefficient is",
      ]),
      displayMath(
        String.raw`b=\sum_{P\in S_\infty} j^*(P),\qquad
        S_\infty=\{P\in\mathbb{P}^1(\mathbb{Z}_\ell):\theta(P)=\infty\},\qquad
        j^*(P)=\min\{j\ge1: e_j<\infty\}`,
      ),
      paragraph([
        "where ",
        math(String.raw`e_j`),
        " is taken with ",
        math(String.raw`P`),
        " itself as base point. That the set defining ",
        math(String.raw`j^*(P)`),
        " is nonempty, that is, that some ",
        math(String.raw`j\ge1`),
        " has ",
        math(String.raw`e_j<\infty`),
        ", is not proved in the present proposition. Part (K4) of ",
        ref("paper_prop_K"),
        " fills this in by showing ",
        math(String.raw`e_{m_u}<\infty`),
        ", where ",
        math(String.raw`m_u`),
        " is the multiplicity of ",
        math(String.raw`\chi^{u^\perp}-1`),
        ". **This sum is taken over points, not over directions**: it runs over the ",
        "points of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        " and not over the directions of ",
        math(String.raw`\mathbb{P}^1(\mathbb{F}_\ell)`),
        ". The distinction is a real one: the integer representative ",
        math(String.raw`(1,\ell-1)`),
        " of the direction ",
        math(String.raw`(1{:}\ell-1)`),
        " and the point ",
        math(String.raw`(1,-1)`),
        " of ",
        math(String.raw`\mathbb{Z}_\ell`),
        " are different points, and it does happen that ",
        math(String.raw`\theta`),
        " is finite at the former. For ",
        math(String.raw`\ell=2`),
        ", the two points of ",
        math(String.raw`S_\infty`),
        " are not separated ",
        math(String.raw`\bmod\ 2`),
        ", so that cancellation occurs; apart from the coefficient ",
        math(String.raw`b`),
        ", the argument of the present proposition yields nothing in that case.",
      ]),
      paragraph([
        "**(J5 A family of type III)** Let ",
        math(String.raw`\ell`),
        " be an odd prime, and take the bouquet on a single vertex carrying ",
        math(String.raw`\ell-1`),
        " loops of voltage ",
        math(String.raw`(1,0)`),
        " and one loop of voltage ",
        math(String.raw`(0,1)`),
        ". Then, **for all** ",
        math(String.raw`n\ge0`),
        ",",
      ]),
      displayMath(String.raw`\mathrm{ord}_\ell(\kappa_n)=2n\,\ell^{n}+2\,\ell^{n}-2.`),
      paragraph([
        "This is a tower of type III with ",
        math(String.raw`b=2`),
        ", and one such exists for every odd prime. It is the case ",
        math(String.raw`(p,q)=(\ell-1,1)`),
        " of the family ",
        math(String.raw`p(1,0)+q(0,1)`),
        " of ",
        ref("paper_prop_G_infty"),
        ", and the closed form agrees with the one given there. **The general form of the family is not " +
          "supplied by the present proposition**: it is given by ",
        ref("paper_prop_G_infty"),
        ". The reason for recording (J5) here is that it is obtained **by an independent route**, namely ",
        "from the formula ",
        math(String.raw`b=\sum j^*`),
        " of (J4), so that the agreement of the two values is a mutual check.",
      ]),
      paragraph([
        "**(J6 The set of points of infinite depth is finite, and all of its points are rational)** ",
        "The set ",
        math(String.raw`S_\infty`),
        " introduced in (J4) is finite, and every one of its points is rational. Indeed, for distinct ",
        math(String.raw`\gamma_1,\dots,\gamma_k\in\mathbb{Z}_\ell`),
        ", the series ",
        math(String.raw`(1+x)^{\gamma_1},\dots,(1+x)^{\gamma_k}\in\mathbb{F}_\ell[[x]]`),
        " are linearly independent over ",
        math(String.raw`\mathbb{F}_\ell`),
        " (this follows from the stability of the digits established in (J1)). Hence, writing ",
        math(String.raw`S:=\mathrm{supp}(\bar{\tilde E})`),
        ", one has, for primitive ",
        math(String.raw`(a,b)\in\mathbb{Z}_\ell^2`),
        ",",
      ]),
      displayMath(
        String.raw`\theta(a,b)=\infty\iff
        \forall\gamma\in\mathbb{Z}_\ell:\ \sum_{\substack{(p,q)\in S\\ pa+qb=\gamma}}\bar c_{pq}=0,`,
      ),
      paragraph([
        "and in particular, if ",
        math(String.raw`\theta(a,b)=\infty`),
        ", then there are distinct ",
        math(String.raw`(p,q),(p',q')\in S`),
        " with ",
        math(String.raw`pa+qb=p'a+q'b`),
        ". This condition is **a linear equation with integer coefficients**, so that",
      ]),
      displayMath(
        String.raw`S_\infty\subseteq\bigl\{(q-q'\,{:}\,p'-p)\ :\ (p,q)\neq(p',q')\in S\bigr\},`,
      ),
      paragraph([
        "that is, ",
        math(String.raw`S_\infty`),
        " is a **finite set** of at most ",
        math(String.raw`\binom{|S|}{2}`),
        " points, all of which lie in ",
        math(String.raw`\mathbb{P}^1(\mathbb{Q})`),
        ", and the candidates are exhausted by a finite computation from the coefficients of ",
        math(String.raw`D`),
        ". **The second half of (J4) tacitly requires this set to be finite, because the coefficient it " +
          "defines there is a sum over that set; the condition is therefore satisfied automatically and " +
          "need not be assumed.** The set in question is ",
        math(String.raw`S_\infty`),
        ", and the coefficient is ",
        math(String.raw`b=\sum_{P\in S_\infty}j^*(P)`),
        ". This set of candidates coincides with the one obtained in ",
        ref("paper_prop_G_infty"),
        " from the Minkowski decomposition of Newton polytopes, namely the directions occurring in ",
        math(String.raw`\mathrm{Newt}(\bar{\tilde E})-\mathrm{Newt}(\bar{\tilde E})`),
        ". The difference is that the proposition there speaks of vectors in ",
        math(String.raw`\mathbb{Z}^2`),
        ", whereas (J6) speaks of vectors in ",
        math(String.raw`\mathbb{Z}_\ell^2`),
        ", and it is the latter that (J4) requires.",
      ]),
      paragraph([
        "**Limitations (stated as part of the assertion)**",
      ]),
      list([
        [
          "If the minimum in (J2) is not attained at a single index — that is, if cancellation occurs — then ",
          math(String.raw`\theta`),
          " is not determined. A concrete instance is the torus at ",
          math(String.raw`\ell=2`),
          " with ",
          math(String.raw`r=1`),
          ", where ",
          math(String.raw`\Lambda(1)=4`),
          " while the true value is ",
          math(String.raw`\theta=6`),
          ".",
        ],
        [
          "Part (J6) narrows the **candidates** for ",
          math(String.raw`S_\infty`),
          " down to finitely many rational points, but we have neither implemented nor verified, for a ",
          "general tower, the **decision** whether ",
          math(String.raw`\theta=\infty`),
          " holds at a given candidate (that is, whether the sum of the coefficients over each class of ",
          math(String.raw`\gamma`),
          " vanishes). ",
          "For the family, this is settled completely by ",
          ref("paper_prop_G_infty"),
          ".",
        ],
        [
          "The hypothesis that remains in (J4) is that, at sufficiently deep levels, the minima in (J2) and ",
          "(J3) are attained at single indices, that is, that no cancellation occurs. For the torus at ",
          math(String.raw`\ell=2`),
          " this fails, and there the fact that the coefficient ",
          math(String.raw`b`),
          " gives the correct value is a collation and not a proof.",
        ],
        [
          "The present proposition is confined to ",
          math(String.raw`d=2`),
          ".",
        ],
        [
          "**We claim no novelty.** Part (J2) is the Newton polygon of the pull-back, to a one-parameter ",
          "subgroup, of the formal group ",
          math(String.raw`\hat{\mathbb{G}}_m`),
          ", and it is quite likely a standard tool in Iwasawa theory. ",
          "We have not been able to identify the corresponding statement in the literature. ",
          "**The formula for the coefficient in (J4) is, however, not new.** The coefficient in question is ",
          math(String.raw`b`),
          ", and its source is Cuoco–Monsky ",
          cite(["CuocoMonsky1981"], "Theorem 1.7 and Definition 1.2"),
          ". The attribution is spelled out among the limitations of ",
          ref("paper_prop_K"),
          ".",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "In order not to depend tacitly on any lemma absent from the text, every fact used along the " +
        "way is stated here before it is used. The only tools are Lucas' theorem, the Frobenius over ",
        math(String.raw`\mathbb{F}_\ell`),
        ", the valuation at the unique place above ",
        math(String.raw`\ell`),
        " of the cyclotomic field ",
        math(String.raw`\mathbb{Q}(\zeta_{\ell^{M}})`),
        ", and the compactness of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ". The space ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        " is uncountable, but all that is used below is reduction to a finite level ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        " and the existence of a finite subcover; the field of real numbers is never entered.",
      ]),
      paragraph([
        "**Proof of (J1).** We follow the change of ",
        math(String.raw`\bar A_m`),
        " under ",
        math(String.raw`(a,b)\mapsto(a+\ell^{L}u,\ b+\ell^{L}v)`),
        ". Since ",
        math(String.raw`N=pa+qb\mapsto N+\ell^{L}(pu+qv)`),
        ", the digits of ",
        math(String.raw`N`),
        " in positions ",
        math(String.raw`0,\dots,L-1`),
        " are unchanged. For ",
        math(String.raw`m<\ell^{L}`),
        ", Lucas' theorem ",
        math(String.raw`\binom{N}{m}\equiv\prod_i\binom{N_i}{m_i}`),
        " has ",
        math(String.raw`m_i=0`),
        " for ",
        math(String.raw`i\ge L`),
        " and ",
        math(String.raw`\binom{N_i}{0}=1`),
        ", so the right-hand side depends only on the lowest ",
        math(String.raw`L`),
        " digits and is unchanged.",
      ]),
      paragraph([
        "For ",
        math(String.raw`m=\ell^{L}`),
        " exactly, we have ",
        math(String.raw`m_L=1`),
        " and all other digits ",
        math(String.raw`0`),
        ", so ",
        math(String.raw`\binom{N}{\ell^{L}}\equiv\binom{N_L}{1}=N_L`),
        "; as the lower digits do not change there is no carry into position ",
        math(String.raw`L`),
        ", and ",
        math(String.raw`N_L\mapsto N_L+(pu+qv)`),
        ". Hence",
      ]),
      displayMath(
        String.raw`\bar A_{\ell^{L}}\bigl(a+\ell^{L}u,\ b+\ell^{L}v\bigr)-\bar A_{\ell^{L}}(a,b)
        =\sum_{(p,q)}\bar c_{pq}\,\overline{(pu+qv)}=\overline{A_1(u,v)}=0,`,
      ),
      paragraph([
        "the last equality being the identity ",
        ref("paper_prop_G"),
        " (G6), namely ",
        math(String.raw`A_1\equiv0`),
        ", which follows over the integers from ",
        math(String.raw`D(z,w)=D(z^{-1},w^{-1})`),
        ". **Without that hypothesis this step is false, as the counterexample in the statement shows.** The ",
        "indices used to decide ",
        math(String.raw`\theta`),
        " are only those with ",
        math(String.raw`m\le\theta\le\ell^{L}`),
        ", so ",
        math(String.raw`\theta(a,b)\le\ell^{L}`),
        " implies that ",
        math(String.raw`\theta`),
        " is determined by ",
        math(String.raw`(a,b)\bmod\ell^{L}`),
        ". Choosing ",
        math(String.raw`L`),
        " separately for each ",
        math(String.raw`m`),
        " extends every ",
        math(String.raw`\bar A_m`),
        " to a locally constant function on ",
        math(String.raw`\mathbb{Z}_\ell^2`),
        "; and since ",
        math(String.raw`\rho(x)=(1+x)^{c}-1`),
        " has order exactly ",
        math(String.raw`1`),
        ", we get ",
        math(String.raw`\theta(ca,cb)=\theta(a,b)`),
        " for ",
        math(String.raw`c\in\mathbb{Z}_\ell^\times`),
        ", so that ",
        math(String.raw`\theta`),
        " defines a function on ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ".",
      ]),
      paragraph([
        "**Proof of (J1').** For ",
        math(String.raw`m=\ell^{L}+1`),
        " we have ",
        math(String.raw`m_0=m_L=1`),
        " and all other digits ",
        math(String.raw`0`),
        ", so ",
        math(String.raw`\binom{N}{\ell^{L}+1}\equiv N_0N_L`),
        ". Here ",
        math(String.raw`N_0`),
        " is unchanged and ",
        math(String.raw`N_L`),
        " changes by ",
        math(String.raw`(pu+qv)`),
        ", so the difference equals ",
        math(String.raw`\bar B\bigl((a,b),(u,v)\bigr)`),
        ", in which ",
        math(String.raw`L`),
        " does not appear. From ",
        math(String.raw`N^2=2\binom{N}{2}+N`),
        " and ",
        math(String.raw`A_1=0`),
        " we get ",
        math(String.raw`\bar B(x,x)=\overline{2A_2(x)}`),
        ", so ",
        math(String.raw`\bar B`),
        " is the polar form of ",
        math(String.raw`\bar A_2`),
        ". If ",
        math(String.raw`\ell`),
        " is odd then ",
        math(String.raw`2`),
        " is a unit, and in characteristic ",
        math(String.raw`\neq2`),
        " a quadratic form is recovered from its polar form, so ",
        math(String.raw`\bar B=0\iff\bar A_2=0\iff k\ge3`),
        ", that is, failure occurs ",
        math(String.raw`\iff k=2`),
        ". If ",
        math(String.raw`\ell=2`),
        " then ",
        math(String.raw`\bar B(x,x)=0`),
        ", so ",
        math(String.raw`\bar B`),
        " is alternating, and over ",
        math(String.raw`\mathbb{F}_2`),
        " the vanishing of the polar form of a quadratic form is equivalent to that form being a square, ",
        "that is, in the image of the Frobenius.",
      ]),
      paragraph([
        "**Proof of (J2).** First, for ",
        math(String.raw`\beta\in\mathbb{Z}_\ell`),
        " with ",
        math(String.raw`v:=v_\ell(\beta)`),
        " and ",
        math(String.raw`r=v+1`),
        ", we have in ",
        math(String.raw`\mathbb{F}_\ell[[x]]`),
        " the identity ",
        math(String.raw`(1+x)^{c+\ell\beta}=(1+x)^{c}(1+x^{\ell})^{\beta}`),
        " (Frobenius), and ",
        math(String.raw`u:=(1+x^{\ell})^{\beta}-1`),
        " has ",
        math(String.raw`\mathrm{ord}_xu=\ell^{r}`),
        " with lowest coefficient the digit ",
        math(String.raw`\beta_v\neq0`),
        " of ",
        math(String.raw`\beta`),
        " in position ",
        math(String.raw`v`),
        ". Indeed, the continuous extension of Lucas' theorem to ",
        math(String.raw`\mathbb{Z}_\ell`),
        ", namely ",
        math(String.raw`\binom{\beta}{m}\equiv\prod_i\binom{\beta_i}{m_i}`),
        ", is well defined by (J1), and the smallest ",
        math(String.raw`m\ge1`),
        " for which it is nonzero ",
        math(String.raw`0`),
        " requires ",
        math(String.raw`m_i\le\beta_i`),
        " for all ",
        math(String.raw`i`),
        ", so that such an ",
        math(String.raw`m`),
        " is given by ",
        math(String.raw`m=\ell^{v}`),
        ", where the value is ",
        math(String.raw`\beta_v`),
        ".",
      ]),
      paragraph([
        "Substituting ",
        math(String.raw`z=1+x`),
        ", ",
        math(String.raw`w=(1+x)^{c}`),
        " and ",
        math(String.raw`1+u=(1+x^{\ell})^{\beta}`),
        " into the expansion ",
        math(String.raw`\bar{\tilde E}(z,w(1+u))=\sum_jD_j(z,w)u^j`),
        ", which itself comes from ",
        math(String.raw`z^pw^q(1+u)^q=\sum_j\binom qjz^pw^qu^j`),
        ", the lowest term of ",
        math(String.raw`\psi_j(x):=D_j\bigl(1+x,(1+x)^{c}\bigr)`),
        " is ",
        math(String.raw`x^{e_j}`),
        " and that of ",
        math(String.raw`u^j`),
        " is ",
        math(String.raw`\beta_v^{\,j}x^{\,j\ell^{r}}`),
        ", so ",
        math(String.raw`\mathrm{ord}_x(\psi_ju^j)=e_j+j\ell^{r}`),
        ". Hence ",
        math(String.raw`\theta(1,c+\ell\beta)\ge\Lambda(r)`),
        ", and the coefficient of ",
        math(String.raw`x^{\Lambda(r)}`),
        " equals ",
        math(String.raw`\sum_{j\in J(r)}\lambda_j\beta_v^{\,j}`),
        ", where ",
        math(String.raw`J(r)`),
        " is the set of minimising indices and ",
        math(String.raw`\lambda_j`),
        " the lowest coefficient of ",
        math(String.raw`\psi_j`),
        "; so if the minimiser is unique the coefficient is nonzero ",
        math(String.raw`0`),
        " and equality holds.",
      ]),
      paragraph([
        "In particular, if ",
        math(String.raw`e_0<\infty`),
        " and ",
        math(String.raw`e_j+j\ell>e_0`),
        " for all ",
        math(String.raw`j\ge1`),
        ", then ",
        math(String.raw`r\ge1`),
        " gives ",
        math(String.raw`e_j+j\ell^{r}\ge e_j+j\ell>e_0`),
        ", so ",
        math(String.raw`J(r)=\{0\}`),
        " for every ",
        math(String.raw`r`),
        " and ",
        math(String.raw`\theta\equiv e_0`),
        " on the whole fibre (the point ",
        math(String.raw`\beta=0`),
        " being ",
        math(String.raw`P_0`),
        " itself). That this condition strictly contains ",
        math(String.raw`\theta(P_0)\le\ell`),
        " is seen as follows: ",
        math(String.raw`\psi_1(0)=D_1(1,1)=\sum c_{pq}q=0`),
        " (because ",
        math(String.raw`E(1,1)=0`),
        " and ",
        math(String.raw`(\partial_wE)(1,1)=0`),
        "), hence ",
        math(String.raw`e_1\ge1`),
        " and ",
        math(String.raw`e_1+\ell\ge1+\ell>\ell\ge e_0`),
        ", while for ",
        math(String.raw`j\ge2`),
        " we get ",
        math(String.raw`e_j+j\ell\ge2\ell>\ell\ge e_0`),
        ".",
      ]),
      paragraph([
        "**Proof of (J3).** Stratify the sum ",
        math(String.raw`\Sigma_n`),
        " by the points of exact level ",
        math(String.raw`M`),
        ". Fixing a primitive ",
        math(String.raw`\ell^{M}`),
        "-th root of unity ",
        math(String.raw`g`),
        ", the points of exact level ",
        math(String.raw`M`),
        " correspond bijectively to the pairs ",
        math(String.raw`(\zeta,\xi)=(g^a,g^b)`),
        " with ",
        math(String.raw`(a,b)`),
        " primitive. The map ",
        math(String.raw`(a,b)\mapsto(ca,cb)`),
        " for ",
        math(String.raw`c\in(\mathbb{Z}/\ell^{M})^\times`),
        " corresponds to the Galois action ",
        math(String.raw`\zeta\mapsto\zeta^{c}`),
        ", which preserves the valuation because there is only one place above ",
        math(String.raw`\ell`),
        ". This action is free, its orbits are the points of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        " and each orbit has size ",
        math(String.raw`\varphi(\ell^{M})`),
        ", so the contribution of an orbit is ",
        math(String.raw`\varphi(\ell^{M})\cdot\hat\theta_{M}(P)/\varphi(\ell^{M})=\hat\theta_{M}(P)`),
        ": the factor ",
        math(String.raw`\varphi`),
        " cancels exactly, which gives the asserted decomposition (**no hypothesis has been used**).",
      ]),
      paragraph([
        "For the second half, ",
        math(String.raw`\tilde E(\zeta,\xi)=\Phi_{(a,b)}(\pi)=\sum_mA_m\pi^m`),
        " with ",
        math(String.raw`\pi=g-1`),
        ", and ",
        math(String.raw`v_\ell(A_m\pi^m)=v_\ell(A_m)+m/\varphi(\ell^{M})`),
        ", so it suffices to multiply by ",
        math(String.raw`\varphi(\ell^{M})`),
        " and apply the non-archimedean estimate: if the minimum is attained at a unique ",
        math(String.raw`m`),
        ", the valuation of the sum equals that minimum.",
      ]),
      paragraph([
        "**Proof of the first half of (J4) (type II).** Assume ",
        math(String.raw`\theta`),
        " is everywhere finite. By (J1) the function ",
        math(String.raw`\theta`),
        " is locally constant at every finite point, so each ",
        math(String.raw`\{\theta=t\}`),
        " is open; these sets cover ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", so by compactness there is a finite subcover, the set of values is finite and ",
        math(String.raw`\theta`),
        " is bounded. Let ",
        math(String.raw`\theta^{\max}`),
        " be the maximum and let ",
        math(String.raw`\ell^{L}\ge\theta^{\max}`),
        "; then by (J1) the function ",
        math(String.raw`\theta`),
        " factors through ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        ".",
      ]),
      paragraph([
        "Choose ",
        math(String.raw`n_1\ge L`),
        " with ",
        math(String.raw`\theta^{\max}-2<\varphi(\ell^{n_1})`),
        ". At the points of level ",
        math(String.raw`M\ge n_1`),
        ", together with ",
        math(String.raw`m_1\ge2`),
        " (which follows from ",
        math(String.raw`A_1\equiv0`),
        ") we get ",
        math(String.raw`\theta-m_1<\varphi(\ell^{M})`),
        ", so the minimiser in (J3) is uniquely ",
        math(String.raw`m=\theta`),
        ", that is ",
        math(String.raw`\hat\theta_{M}=\theta`),
        ". The fibres of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})\to\mathbb{P}^1(\mathbb{Z}/\ell^{L})`),
        " all have ",
        math(String.raw`\ell^{M-L}`),
        " elements, so ",
        math(String.raw`\Theta_{M}=\ell^{M-L}\Theta_L`),
        ", and the decomposition of (J3) gives",
      ]),
      displayMath(
        String.raw`\Sigma_n=\Sigma_{n_1-1}+\Theta_L\sum_{M=n_1}^{n}\ell^{M-L}
        =\Sigma_{n_1-1}+\Theta_L\,\frac{\ell^{\,n-L+1}-\ell^{\,n_1-L}}{\ell-1}`,
      ),
      paragraph([
        ". The coefficient of ",
        math(String.raw`\ell^{n}`),
        " is ",
        math(String.raw`\Theta_L\ell^{1-L}/(\ell-1)=\Theta_L/\varphi(\ell^{L})`),
        " and the rest is constant, which is the asserted formula; in particular no ",
        math(String.raw`n\ell^{n}`),
        " term occurs. Replacing ",
        math(String.raw`L`),
        " by ",
        math(String.raw`L'>L`),
        " gives ",
        math(String.raw`\Theta_{L'}=\ell^{L'-L}\Theta_L`),
        " and ",
        math(String.raw`\varphi(\ell^{L'})=\ell^{L'-L}\varphi(\ell^{L})`),
        ", so the ratio is unchanged.",
      ]),
      paragraph([
        "Proof of the second half of (J4) (the coefficient of ",
        math(String.raw`n\ell^{n}`),
        "). Here we assume that no cancellation occurs, namely: (N) there is an ",
        math(String.raw`r_0\ge1`),
        " such that the balls ",
        math(String.raw`B(P,\ell^{-r_0})`),
        " of radius ",
        math(String.raw`\ell^{-r_0}`),
        " for ",
        math(String.raw`P\in S_\infty`),
        " are pairwise disjoint and, taking each ",
        math(String.raw`P`),
        " as base point, the minimiser of ",
        math(String.raw`\Lambda(r)`),
        " is unique for all ",
        math(String.raw`r\ge r_0`),
        "; and (B) at all sufficiently deep levels the minimiser in (J3) is unique. These two are exactly the " +
        "remaining hypotheses listed among the limitations; the finiteness of ",
        math(String.raw`S_\infty`),
        ", which (J6) establishes, need not be assumed.",
      ]),
      paragraph([
        "Put ",
        math(String.raw`U:=\mathbb{P}^1(\mathbb{Z}_\ell)\setminus\bigcup_{P\in S_\infty}B(P,\ell^{-r_0})`),
        ". The set ",
        math(String.raw`U`),
        " is compact and ",
        math(String.raw`\theta`),
        " is everywhere finite on ",
        math(String.raw`U`),
        ", so by the argument of the first half it is bounded on ",
        math(String.raw`U`),
        " and factors through some ",
        math(String.raw`L_U`),
        ". Taking ",
        math(String.raw`M\ge\max(r_0,L_U,n_1)`),
        ", the contribution of the balls contained in ",
        math(String.raw`U`),
        " is a constant multiple of ",
        math(String.raw`\ell^{M}`),
        " and carries no ",
        math(String.raw`M\ell^{M}`),
        " term.",
      ]),
      paragraph([
        "Around a point ",
        math(String.raw`P\in S_\infty`),
        ", the balls of level ",
        math(String.raw`M`),
        " inside ",
        math(String.raw`B(P,\ell^{-r_0})`),
        " are indexed by ",
        math(String.raw`\beta`),
        " with ",
        math(String.raw`v=v_\ell(\beta)\ge r_0-1`),
        ", and the layer ",
        math(String.raw`v\le M-2`),
        " contains ",
        math(String.raw`\varphi(\ell^{M-1-v})`),
        " of them. Choose ",
        math(String.raw`R'\ge r_0`),
        " so that ",
        math(String.raw`\Lambda(r)=e_{j^*}+j^*\ell^{r}`),
        " for ",
        math(String.raw`r\ge R'`),
        " (the terms with ",
        math(String.raw`j>j^*`),
        " have larger slope, so for large ",
        math(String.raw`r`),
        " they always lose). Since ",
        math(String.raw`\varphi(\ell^{M-1-v})\cdot\ell^{v+1}=\varphi(\ell^{M})`),
        " does not depend on ",
        math(String.raw`v`),
        ", the contribution of the layers with ",
        math(String.raw`v`),
        " running from ",
        math(String.raw`R'-1`),
        " to ",
        math(String.raw`M-2`),
        " is",
      ]),
      displayMath(
        String.raw`\sum_{v=R'-1}^{M-2}\varphi(\ell^{M-1-v})\bigl(e_{j^*}+j^*\ell^{\,v+1}\bigr)
        =e_{j^*}\bigl(\ell^{\,M-R'}-1\bigr)+j^*\,\varphi(\ell^{M})\,(M-R')`,
      ),
      paragraph([
        ", whose second summand produces the ",
        math(String.raw`M\ell^{M}`),
        " term with coefficient ",
        math(String.raw`j^*(P)\,(1-\ell^{-1})`),
        " (the finitely many layers with ",
        math(String.raw`v<R'-1`),
        " and the innermost ball give only a constant multiple of ",
        math(String.raw`\ell^{M}`),
        " and a constant). Adding up, the coefficient of ",
        math(String.raw`M\ell^{M}`),
        " in ",
        math(String.raw`\Theta_{M}`),
        " is ",
        math(String.raw`\frac{\ell-1}{\ell}\sum_{P\in S_\infty}j^*(P)`),
        ", and since the coefficient of ",
        math(String.raw`n\ell^{n}`),
        " in ",
        math(String.raw`\sum_{M=1}^{n}M\ell^{M}`),
        " is ",
        math(String.raw`\frac{\ell}{\ell-1}`),
        ", multiplying the two gives ",
        math(String.raw`b=\sum_{P\in S_\infty}j^*(P)`),
        ". If ",
        math(String.raw`S_\infty=\emptyset`),
        " then ",
        math(String.raw`b=0`),
        ", in agreement with the first half.",
      ]),
      paragraph([
        "**Proof of (J5).** This family is the case ",
        ref("paper_prop_G_infty"),
        " (G'3) with ",
        math(String.raw`(p,q)=(\ell-1,1)`),
        ": here ",
        math(String.raw`\mu=0`),
        ", ",
        math(String.raw`p'=\ell-1`),
        ", ",
        math(String.raw`q'=1`),
        ", ",
        math(String.raw`\ell\mid p'+q'`),
        " and ",
        math(String.raw`v_\ell(p'+q')=1`),
        ", so ",
        math(String.raw`\Lambda=2`),
        " and the closed form of that proposition gives the asserted formula. What follows is an " +
        "independent route to the same formula through the other tool ",
        math(String.raw`b=\sum j^*`),
        " of (J4); the agreement of the two is a mutual check.",
      ]),
      paragraph([
        "The coefficients of ",
        math(String.raw`D=(\ell-1)(2-z-z^{-1})+(2-w-w^{-1})`),
        " have gcd ",
        math(String.raw`1`),
        ", so ",
        math(String.raw`\mu=0`),
        " and ",
        math(String.raw`E=D`),
        ", and the graph has one vertex, so ",
        math(String.raw`v_\ell(\kappa(X))=0`),
        ". Working ",
        math(String.raw`\bmod\ \ell`),
        ", from ",
        math(String.raw`-(\ell-1)\equiv1`),
        " we get ",
        math(String.raw`\bar{\tilde E}=w(z-1)^2-z(w-1)^2`),
        ", hence ",
        math(String.raw`H=T^2-S^2`),
        ", ",
        math(String.raw`k=2`),
        " and ",
        math(String.raw`z_H=2`),
        ". Putting ",
        math(String.raw`s:=(1+x)^{a}-1`),
        " and ",
        math(String.raw`t:=(1+x)^{b}-1`),
        ",",
      ]),
      displayMath(
        String.raw`\bar E\bigl((1+x)^{a},(1+x)^{b}\bigr)=\frac{s^2}{1+s}-\frac{t^2}{1+t}
        =\frac{(s-t)\bigl(s+t+st\bigr)}{(1+s)(1+t)}`,
      ),
      paragraph([
        ", which vanishes ",
        math(String.raw`0`),
        " only when ",
        math(String.raw`(1+x)^{a}=(1+x)^{b}`),
        " or ",
        math(String.raw`(1+x)^{a+b}=1`),
        ". In ",
        math(String.raw`\mathbb{F}_\ell[[x]]`),
        " one has ",
        math(String.raw`(1+x)^{c}=1\iff c=0`),
        " for ",
        math(String.raw`c\in\mathbb{Z}_\ell`),
        ", so ",
        math(String.raw`S_\infty=\{(1{:}1),(1{:}-1)\}`),
        ". If ",
        math(String.raw`\ell`),
        " is odd then ",
        math(String.raw`1\not\equiv-1`),
        ", so these two points lie in different directions ",
        math(String.raw`\bmod\ \ell`),
        " and the separation of balls required in (N) holds with ",
        math(String.raw`r_0=1`),
        ".",
      ]),
      paragraph([
        "Since ",
        math(String.raw`D_0=\bar{\tilde E}`),
        ", ",
        math(String.raw`D_1=w(z-1)^2-2zw(w-1)`),
        ", ",
        math(String.raw`D_2=-zw^2`),
        " and ",
        math(String.raw`D_j=0\ (j\ge3)`),
        ", taking ",
        math(String.raw`P=(1{:}1)`),
        " as base point gives ",
        math(String.raw`\psi_0=0`),
        ", ",
        math(String.raw`e_1=1`),
        " (because ",
        math(String.raw`\ell`),
        " odd makes ",
        math(String.raw`-2\neq0`),
        ") and ",
        math(String.raw`e_2=0`),
        ", so ",
        math(String.raw`j^*=1`),
        " and ",
        math(String.raw`\Lambda(r)=\min(1+\ell^{r},2\ell^{r})=1+\ell^{r}`),
        " has a unique minimiser. From ",
        math(String.raw`E(z,w)=E(z,w^{-1})`),
        " the same values are obtained at ",
        math(String.raw`P=(1{:}-1)`),
        ". In the non-degenerate directions, of which there are ",
        math(String.raw`\ell-1`),
        ", we have ",
        math(String.raw`e_0=2\le\ell`),
        ", so by the criterion in (J2) the depth is constant ",
        math(String.raw`\theta\equiv2`),
        " on each fibre.",
      ]),
      paragraph([
        "Moreover ",
        math(String.raw`\Phi_{(1,1)}=-\ell(1+x)x^2`),
        ", so ",
        math(String.raw`A_2=A_3=-\ell`),
        " and all others are ",
        math(String.raw`0`),
        "; the minimum in (J3) is attained uniquely at ",
        math(String.raw`m=2`),
        " and yields ",
        math(String.raw`\hat\theta_{M}=\varphi(\ell^{M})+2`),
        ". At a point of a fibre over a degenerate direction with ",
        math(String.raw`v\le M-2`),
        " we have ",
        math(String.raw`\theta=1+\ell^{\,v+1}\le1+\ell^{M-1}`),
        " and ",
        math(String.raw`\theta-2<\varphi(\ell^{M})`),
        " because ",
        math(String.raw`\ell\ge3`),
        ", so ",
        math(String.raw`\hat\theta=\theta`),
        ". Consequently",
      ]),
      displayMath(
        String.raw`\Theta_{M}=2(\ell-1)\ell^{M-1}+2\Bigl[\ell^{M-1}+1+M\,\varphi(\ell^{M})\Bigr]
        =2\ell^{M}+2+2M\,\varphi(\ell^{M})`,
      ),
      paragraph([
        " (the ",
        math(String.raw`\ell-1`),
        " non-degenerate directions contribute the value ",
        math(String.raw`2`),
        " at each of the ",
        math(String.raw`\ell^{M-1}`),
        " points of their fibres, and each of the two degenerate directions contributes ",
        math(String.raw`\ell^{M-1}+1+M\varphi(\ell^{M})`),
        "), and inserting ",
        math(String.raw`\sum_{M=1}^{n}\ell^{M}`),
        " and ",
        math(String.raw`\sum_{M=1}^{n}M\ell^{M}`),
        " gives ",
        math(String.raw`\Sigma_n=2n\ell^{n}+2\ell^{n}-2+2n`),
        "; adding ",
        math(String.raw`-2n`),
        " yields the asserted formula. For ",
        math(String.raw`n=0`),
        " the value is ",
        math(String.raw`0`),
        ", in agreement with the base graph.",
      ]),
      paragraph([
        "**Proof of (J6).** We first prove the **linear independence of the exponentials**. Let ",
        math(String.raw`\gamma_1,\dots,\gamma_k\in\mathbb{Z}_\ell`),
        " be distinct and suppose ",
        math(String.raw`\sum_ia_i(1+x)^{\gamma_i}=0`),
        " with ",
        math(String.raw`a_i\in\mathbb{F}_\ell`),
        ". Taking ",
        math(String.raw`L`),
        " large enough, the residues ",
        math(String.raw`n_i:=\gamma_i\bmod\ell^{L}\in\{0,\dots,\ell^{L}-1\}`),
        " are still distinct, and by (J1) we have ",
        math(String.raw`(1+x)^{\gamma_i}\equiv(1+x)^{n_i}\pmod{x^{\ell^{L}}}`),
        ". The left-hand side is a polynomial of degree ",
        math(String.raw`\max_in_i<\ell^{L}`),
        ", hence identically ",
        math(String.raw`0`),
        "; since the degrees of the ",
        math(String.raw`(1+x)^{n_i}`),
        " are distinct, the coefficient ",
        math(String.raw`a_i`),
        " of the term of largest degree is ",
        math(String.raw`0`),
        ", and repeating the argument all the coefficients are ",
        math(String.raw`0`),
        ".",
      ]),
      paragraph([
        "Now group the terms of ",
        math(String.raw`\overline{\Phi_{(a,b)}}=\sum_{(p,q)\in S}\bar c_{pq}(1+x)^{pa+qb}`),
        " according to the value of the exponent ",
        math(String.raw`\gamma=pa+qb`),
        ". By linear independence the sum vanishes ",
        math(String.raw`0`),
        " if and only if the coefficient sum of each class vanishes ",
        math(String.raw`0`),
        ", which is the asserted criterion. Since ",
        math(String.raw`S\neq\emptyset`),
        ", at least one class is non-empty, and a class with a single element has coefficient sum ",
        math(String.raw`\bar c_{pq}\neq0`),
        ", violating the criterion. Hence **every class has at least two elements**, and in particular there is an ",
        "integer vector ",
        math(String.raw`\Delta p\,a+\Delta q\,b=0`),
        " with ",
        math(String.raw`(\Delta p,\Delta q)\neq(0,0)`),
        ". As ",
        math(String.raw`(a,b)`),
        " is primitive, this linear equation has in ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        " the single solution ",
        math(String.raw`(a{:}b)=(\Delta q{:}-\Delta p)`),
        ", a rational point defined by an integer vector. Therefore ",
        math(String.raw`S_\infty`),
        " is a finite set with at most ",
        math(String.raw`\binom{|S|}{2}`),
        " points, and the candidates are exhausted by a finite computation from the coefficients of ",
        math(String.raw`D`),
        ".",
      ]),
    ],
  },
]);
