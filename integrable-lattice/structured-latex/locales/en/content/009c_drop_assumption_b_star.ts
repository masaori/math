/**
 * 論文本体 第 11 章（英語版）: 最小点の一意性の仮定を落とす（命題 Q）。
 *
 * **正本は日本語版 `../../../content/009c_drop_assumption_b_star.ts` である。**
 * ブロック id・`labels`・`habitat`・`realEscape`・`verification`・`kind`・`proof` の有無、
 * および**数式**は日本語版と完全に同じ文字列を使う（`tools/verify-localization.ts` の構造照合が検査する）。
 * 訳語は `../../../../docs/paper001-en-glossary.md` を正本とする。
 *
 * **数式の中に日本語は 1 つも無い**ので、`../structure-exceptions.ts` への新規登録は行っていない。
 * 日本語版が `**強調**` の中に数式ノードを含めて書いている箇所は、英語版では強調文を数式なしで
 * 言い切り、その直後に記号を対応づける文を置いた（009 章と同じ扱い）。
 * **数式ノードの多重集合と並びは日本語版と一致させてある。**
 */

import {
  defineTranslatedBlocks as defineBlocks,
  displayMath,
  list,
  math,
  paragraph,
  refInTranslation as ref,
} from "../../../schema.ts";

export default defineBlocks([
  {
    id: "paper_105_heading_drop_assumption",
    kind: "heading",
    level: 1,
    origin: { path: "structured-latex/locales/en/content/009c_drop_assumption_b_star.ts", ordinal: 1 },
    title: {
      text:
        "Discharging the uniqueness hypothesis on the minimising index — the coefficient of the " +
        "type III term determined unconditionally",
    },
    labels: [],
  },
  {
    id: "paper_106_theorem_drop_assumption",
    kind: "theorem",
    origin: { path: "structured-latex/locales/en/content/009c_drop_assumption_b_star.ts", ordinal: 2 },
    title: {
      text:
        "Proposition Q — the coefficient of the type III term is determined without any uniqueness " +
        "hypothesis on the minimising index, with an explicit error constant",
    },
    labels: ["paper_prop_Q"],
    habitat: "mixed",
    realEscape:
      "Only the proof of (Q4) — the crude bound hat theta_M(P) ≤ φ(ℓ^M) log_ℓ C_0 — uses the complex " +
      "absolute value, that is, the archimedean place. Its sole role is to bound, by O(ℓ^M), the " +
      "contribution of the bad points, of which there are O(1) at each level; it enters neither the " +
      "value of b nor the procedure that determines b. The term log_ℓ C_0 that remains in the constant C " +
      "of (Q7) comes from this single place and occurs only as a coefficient of the error term. " +
      "The remaining parts (Q1)(Q2)(Q3)(Q5)(Q6)(Q8) stay inside ℤ, F_ℓ and ℚ(ζ_{ℓ^M}).",
    verification: ["sagemath/check/cycle21_T3_b_star"],
    lean: [
      "IntegrableLattice.unique_min_of_val_seq",
      "IntegrableLattice.BG_dominates",
      "IntegrableLattice.sum_totient_pow",
      "IntegrableLattice.crudeBound_padicValInt_le_of_prod_countable",
      "IntegrableLattice.crudeBound_padicValInt_le_mul_logb",
    ],
    statement: [
      paragraph([
        "We keep the setting of ",
        ref("paper_prop_K"),
        ". Here ",
        math(String.raw`X`),
        " is a finite connected voltage multigraph, ",
        math(String.raw`D=\det L(z,w)`),
        ", ",
        math(String.raw`\mu=v_\ell(\mathrm{content}\,D)`),
        ", ",
        math(String.raw`E=\ell^{-\mu}D`),
        ", ",
        math(String.raw`\tilde E=\sum_{(p,q)}c_{pq}z^{p}w^{q}\in\mathbb{Z}[z,w]`),
        " (obtained from ",
        math(String.raw`E`),
        " by multiplying by a monomial to make it a polynomial), and ",
        math(String.raw`\kappa_n=\kappa(X_{\ell^n,\ell^n})`),
        "; the hypothesis (H) (connectedness at every stage) is in force throughout. Inside ",
        math(String.raw`K_M=\mathbb{Q}(\zeta_{\ell^{M}})`),
        " we normalise the valuation at ",
        math(String.raw`\ell`),
        " — at the unique place above it — by ",
        math(String.raw`v_{\mathfrak{l}}`),
        ", that is by ",
        math(String.raw`v_{\mathfrak{l}}(\ell)=\varphi(\ell^{M})`),
        ", and for a primitive ",
        math(String.raw`\ell^{M}`),
        "-th root of unity ",
        math(String.raw`\zeta`),
        " and ",
        math(String.raw`P=(a{:}b)\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        " we put the character ",
        math(String.raw`\omega_P:z\mapsto\zeta^{a},\ w\mapsto\zeta^{b}`),
        ", ",
        math(String.raw`\hat\theta_M(P)=v_{\mathfrak{l}}\bigl(\tilde E(\omega_P)\bigr)`),
        " and ",
        math(String.raw`\Theta_M=\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}\hat\theta_M(P)`),
        ".",
      ]),
      paragraph([
        "Take the unique factorisation of part (K4) of ",
        ref("paper_prop_K"),
        ",",
      ]),
      displayMath(
        String.raw`\bar{\tilde E}=c\,\chi^{w_0}\prod_{i=1}^{r}\bigl(\chi^{v_i}-1\bigr)^{m_i}\cdot\bar G_0,
        \qquad c\in\mathbb{F}_\ell^\times,\quad(\chi^{v}-1)\nmid\bar G_0`,
      ),
      paragraph([
        ", where the ",
        math(String.raw`v_i`),
        " are primitive vectors taken up to ",
        math(String.raw`\pm`),
        ", and put ",
        math(String.raw`b:=\sum_{i=1}^{r}m_i`),
        ". Parts (K4) and (K6) of ",
        ref("paper_prop_K"),
        " give ",
        math(String.raw`b=\sum_{P\in S_\infty}j^{*}(P)`),
        ".",
      ]),
      paragraph([
        "(Q1 A decomposition that stays over the integers) Put ",
        math(String.raw`B:=\prod_{i=1}^{r}\bigl(\chi^{v_i}-1\bigr)^{m_i}\in\mathbb{Z}[z^{\pm1},w^{\pm1}]`),
        " (the ",
        math(String.raw`v_i`),
        " are integer vectors, so ",
        math(String.raw`B`),
        " has integer coefficients). Choosing any lift ",
        math(String.raw`\bar{\tilde E}/\bar B\in\mathbb{F}_\ell[z^{\pm1},w^{\pm1}]`),
        " — that is, any ",
        math(String.raw`G\in\mathbb{Z}[z^{\pm1},w^{\pm1}]`),
        " lifting it — one has",
      ]),
      displayMath(
        String.raw`H:=\frac{\tilde E-B\,G}{\ell}\ \in\ \mathbb{Z}[z^{\pm1},w^{\pm1}]`,
      ),
      paragraph([
        ", and ",
        math(String.raw`\bar G`),
        " has no primitive binomial factor ",
        math(String.raw`\chi^{v}-1`),
        " at all. ",
        "Lifting the factorisation over the residue field to the integers is the idea of the present proposition, "
          + "and it moves all the depth into the first factor. The first factor is ",
        math(String.raw`B`),
        " and what remains is ",
        math(String.raw`G`),
        ".",
      ]),
      paragraph([
        "(Q2 The second factor is uniformly shallow) Let ",
        math(String.raw`\tilde G`),
        " be obtained from ",
        math(String.raw`G`),
        " by multiplying by a monomial to make it a polynomial, put ",
        math(String.raw`\Phi^{G}_{(a,b)}(x)=\tilde G\bigl((1+x)^{a},(1+x)^{b}\bigr)`),
        " and ",
        math(String.raw`\theta_G(P):=\mathrm{ord}_{x=0}\overline{\Phi^{G}_{(a,b)}}`),
        ". Then (1) ",
        math(String.raw`\theta_G(P)<\infty`),
        " for every ",
        math(String.raw`P\in\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ", so that ",
        math(String.raw`\theta_G`),
        " is bounded (write ",
        math(String.raw`\theta_G^{\max}`),
        " for its maximum); and (2) at every point ",
        math(String.raw`\varphi(\ell^{M})>\theta_G^{\max}`),
        " — that is, at every point of a level ",
        math(String.raw`M`),
        " satisfying that inequality — the point ",
        math(String.raw`P`),
        " satisfies ",
        math(String.raw`v_{\mathfrak{l}}\bigl(G(\omega_P)\bigr)=\theta_G(P)<\infty`),
        ".",
      ]),
      paragraph([
        "(Q3 A pointwise equality) Writing ",
        math(String.raw`\langle\cdot,\cdot\rangle`),
        " for the standard inner product, put",
      ]),
      displayMath(
        String.raw`\rho_i(P):=\min\Bigl(v_\ell\bigl(\langle v_i,(a,b)\rangle\bigr),\ M\Bigr)\in\{0,1,\dots,M\},
        \qquad \beta_P:=\sum_{i=1}^{r}m_i\,\ell^{\rho_i(P)}`,
      ),
      paragraph([
        ". Assume ",
        math(String.raw`\varphi(\ell^{M})>\theta_G^{\max}`),
        ". If a point ",
        math(String.raw`P`),
        " satisfies",
      ]),
      displayMath(String.raw`\beta_P+\theta_G^{\max}<\varphi(\ell^{M})`),
      paragraph([
        " (so that in particular, for every ",
        math(String.raw`i`),
        ", one has ",
        math(String.raw`\rho_i(P)<M`),
        "), then ",
        math(String.raw`\hat\theta_M(P)=\beta_P+\theta_G(P)`),
        " holds with equality. Points satisfying this condition will be called good, the others bad.",
      ]),
      paragraph([
        "(Q4 A crude bound; the one place where the real numbers are used) Put ",
        math(String.raw`C_0:=\sum_{(p,q)}|c_{pq}|`),
        ". If ",
        math(String.raw`\tilde E(\omega_P)\neq0`),
        " then",
      ]),
      displayMath(String.raw`\hat\theta_M(P)\ \le\ \varphi(\ell^{M})\,\log_\ell C_0 .`),
      paragraph([
        "This is the only place in the present proposition where we leave the countable world. What is used ",
        "is the complex absolute value, that is the archimedean place; the way the escape is isolated is made ",
        "explicit under the heading on countable versus uncountable below.",
      ]),
      paragraph([
        "(Q5 The bad points are finite in number, uniformly in the level) Put ",
        math(String.raw`\mathcal{B}_M:=\bigl\{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M}):\beta_P+\theta_G^{\max}\ge\varphi(\ell^{M})\bigr\}`),
        " and define",
      ]),
      displayMath(
        String.raw`c_1:=\min\bigl\{\,c\in\mathbb{Z}_{\ge0}\ :\ 2b<(\ell-1)\,\ell^{c}\,\bigr\}`,
      ),
      paragraph([
        ". If ",
        math(String.raw`\varphi(\ell^{M})\ge2\theta_G^{\max}`),
        " and ",
        math(String.raw`M>c_1`),
        ", then ",
        math(String.raw`\lvert\mathcal{B}_M\rvert\le r\,\ell^{c_1}`),
        "; in particular this number does not depend on ",
        math(String.raw`M`),
        ". ",
        "Neither a real logarithm nor a ceiling occurs in the definition of this constant. Indeed ",
        math(String.raw`\ell\ge2`),
        ", so ",
        math(String.raw`(\ell-1)\ell^{c}`),
        " is strictly increasing and unbounded in ",
        math(String.raw`c`),
        ", and the condition ",
        math(String.raw`2b<(\ell-1)\ell^{c}`),
        " is a single comparison of integers, hence decidable; so the set of ",
        math(String.raw`c`),
        " satisfying it is a non-empty subset of ",
        math(String.raw`\mathbb{Z}_{\ge0}`),
        " and has a least element. When ",
        math(String.raw`b=0`),
        ", the value ",
        math(String.raw`c=0`),
        " satisfies ",
        math(String.raw`0<\ell-1`),
        ", so ",
        math(String.raw`c_1=0`),
        ".",
      ]),
      paragraph([
        "(Q6 A counting identity) For a primitive integer vector ",
        math(String.raw`v`),
        " and ",
        math(String.raw`M\ge1`),
        ",",
      ]),
      displayMath(
        String.raw`\sum_{P\in\mathbb{P}^1(\mathbb{Z}/\ell^{M})}\ell^{\rho_v(P)}
        =(M-1)\,\varphi(\ell^{M})+2\,\ell^{M}.`,
      ),
      paragraph([
        "(Q7 The coefficient of the type III term is determined without any uniqueness hypothesis) The term in question is the one in ",
        math(String.raw`n\ell^{n}`),
        ". Under the hypothesis (H), for every level ",
        math(String.raw`\varphi(\ell^{M})\ge2\theta_G^{\max}`),
        " with ",
        math(String.raw`M>c_1`),
        " — that is, for every ",
        math(String.raw`M`),
        " satisfying both conditions —",
      ]),
      displayMath(
        String.raw`\bigl|\,\Theta_M-b\,M\,\varphi(\ell^{M})\,\bigr|\ \le\ C\,\ell^{M},
        \qquad
        C:=b\bigl(3+r\,\ell^{c_1}\bigr)+\theta_G^{\max}\frac{\ell+1}{\ell}+r\,\ell^{c_1}\log_\ell C_0`,
      ),
      paragraph([
        " holds, where ",
        math(String.raw`C`),
        " is an explicit constant not depending on ",
        math(String.raw`M`),
        ". Consequently",
      ]),
      displayMath(
        String.raw`\mathrm{ord}_\ell(\kappa_n)=\mu\bigl(\ell^{2n}-1\bigr)+b\,n\,\ell^{n}+O\bigl(\ell^{n}\bigr)`,
      ),
      paragraph([
        ". ",
        "Thus the hypothesis that remained in part (K6), namely that at sufficiently deep levels the minimising index is unique, is discharged. The part in question is (K6) of ",
        ref("paper_prop_K"),
        ". The reason it can be discharged is that, by (Q5), the points where that hypothesis may fail are finite in number uniformly in ",
        math(String.raw`M`),
        ", and finitely many points can move the sum by at most ",
        math(String.raw`O(\ell^{M})`),
        ", which does not affect the coefficient of ",
        math(String.raw`M\ell^{M}`),
        ".",
      ]),
      paragraph([
        "(Q8 The degenerate case) The case in question is ",
        math(String.raw`b=0`),
        ". The conditions ",
        math(String.raw`b=0`),
        " and ",
        math(String.raw`r=0`),
        " are equivalent (the ",
        math(String.raw`m_i`),
        " of (K4) are ",
        math(String.raw`\ge1`),
        "), and then ",
        math(String.raw`B`),
        " is the empty product ",
        math(String.raw`1`),
        ", every point ",
        math(String.raw`P`),
        " has ",
        math(String.raw`\beta_P=0`),
        ", and ",
        math(String.raw`c_1=0`),
        ", ",
        math(String.raw`\mathcal{B}_M=\emptyset`),
        ". The hypothesis ",
        math(String.raw`M>c_1`),
        " becomes ",
        math(String.raw`M\ge1`),
        ", that is every level, and the constant degenerates to ",
        math(String.raw`C=\theta_G^{\max}\frac{\ell+1}{\ell}`),
        ", so that (Q7) gives",
      ]),
      displayMath(String.raw`\lvert\Theta_M\rvert\ \le\ \theta_G^{\max}(\ell+1)\ell^{M-1}`),
      paragraph([
        ". In other words the term ",
        math(String.raw`n\ell^{n}`),
        " disappears and ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=\mu(\ell^{2n}-1)+O(\ell^{n})`),
        ". This case is not hypothetical. Part (M6) of ",
        ref("paper_prop_M"),
        " — the case ",
        math(String.raw`S_\infty=\emptyset`),
        " — is exactly this one, and there the error term is opened up to determine ",
        math(String.raw`c`),
        " as well (the value is the one given in (M6)). The two are consistent.",
      ]),
      paragraph([
        "The quantities appearing in this proposition separate into countable and uncountable ones as follows.",
      ]),
      list([
        [
          "The determination of ",
          math(String.raw`b`),
          " is the sum of the multiplicities of the primitive binomial factors of ",
          math(String.raw`\bar{\tilde E}`),
          ", and needs nothing but a finite computation over a finite field (part (K3) of ",
          ref("paper_prop_K"),
          "). Countable and decidable.",
        ],
        [
          "The computation of ",
          math(String.raw`\hat\theta_M`),
          " is the ",
          math(String.raw`\ell`),
          "-adic valuation of an integer resultant (part (R4) of ",
          ref("paper_prop_R"),
          "). It is a computation with integers only.",
        ],
        [
          "The quantity ",
          math(String.raw`\theta_G^{\max}`),
          " is bounded by a finite computation over ",
          math(String.raw`\mathbb{F}_\ell`),
          " together with an effective bound of the type of part (R3) of ",
          ref("paper_prop_R"),
          ". Countable.",
        ],
        [
          "The constant ",
          math(String.raw`c_1`),
          " is the least element of a decidable predicate on ",
          math(String.raw`\mathbb{Z}_{\ge0}`),
          ". Countable and decidable.",
        ],
        [
          "The space ",
          math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
          " is uncountable, but every assertion of this proposition factors through the finite level ",
          math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
          ", a finite set. No element of an uncountable set is handled individually.",
        ],
        [
          "The only escape to the real numbers, that is to the archimedean place, is in (Q4). Its role is solely to bound the contribution of the finitely many points of ",
          math(String.raw`\mathcal{B}_M`),
          " by ",
          math(String.raw`O(\ell^{M})`),
          ", and it enters neither the value of ",
          math(String.raw`b`),
          " nor the procedure that determines ",
          math(String.raw`b`),
          ". The term ",
          math(String.raw`C`),
          " of (Q7) retains ",
          math(String.raw`\log_\ell C_0`),
          ", which comes from (Q4) and occurs only as a coefficient of the error term.",
        ],
      ]),
      paragraph([
        "The limitations that remain in this proposition are the following.",
      ]),
      list([
        [
          "What the present proposition gives reaches only as far as the coefficient of the type III term. That term is the one in ",
          math(String.raw`n\ell^{n}`),
          ". The coefficients at and below ",
          math(String.raw`\ell^{n}`),
          " are hidden inside the error term ",
          math(String.raw`O(\ell^{n})`),
          ", whose contents this proposition does not open up. Opening them up is the business of ",
          ref("paper_prop_M"),
          ".",
        ],
        [
          "What the discharged hypothesis guaranteed was the exactness of the valuation at the finitely many innermost points, and that bears only on the coefficients at and below ",
          math(String.raw`\ell^{n}`),
          ". The pointwise values at the bad points do not follow from the present proposition. The bad points form ",
          math(String.raw`\mathcal{B}_M`),
          ", and the values in question are those of ",
          math(String.raw`\hat\theta_M(P)`),
          ".",
        ],
        [
          "No novelty is claimed. Part (Q7) is exactly Definition 1.2 together with Theorem 1.7 of Cuoco–Monsky, *Class numbers in ",
          math(String.raw`\mathbb{Z}_p^d`),
          "-extensions*, Math. Ann. 255 (1981), 235–258, and ",
          math(String.raw`b`),
          " coincides with their ",
          math(String.raw`l_0`),
          ". The attribution is already recorded in part (K6) of ",
          ref("paper_prop_K"),
          ".",
        ],
        [
          "The scope is limited to rank two, the hypothesis (H), and voltage graphs of Laplacian determinant type. This is the same restriction as in ",
          ref("paper_prop_M"),
          ".",
        ],
      ]),
    ],
    proof: [
      paragraph([
        "We state, before using them, all the facts that the proof takes from outside. They are part (K2) of ",
        ref("paper_prop_K"),
        " — the equivalence ",
        math(String.raw`\theta_F(u)=\infty\iff(\chi^{u^\perp}-1)\mid\bar F`),
        ", whose proof uses nothing but ",
        math(String.raw`\ker\bar\psi_u=(\chi^{u^\perp}-1)`),
        ", so that it holds not only for ",
        math(String.raw`\bar F=\bar{\tilde E}`),
        " but for a general ",
        math(String.raw`0\neq\bar F\in\mathbb{F}_\ell[z^{\pm1},w^{\pm1}]`),
        " — together with (K3), (K4) and (K6); parts (J1) (local constancy at points of finite depth) and (J6) (linear independence of the exponentials) of ",
        ref("paper_prop_J"),
        "; and, from ",
        ref("paper_prop_R"),
        ", parts (R3) (an effective bound), (R4) (valuations via resultants) and (R5), the last of which states that ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        " and ",
        math(String.raw`\mathrm{ord}_\ell(\kappa_n)=v_\ell(\kappa(X))-2n+\mu(\ell^{2n}-1)+\Sigma_n`),
        " hold with no hypothesis. We also use repeatedly the equality in a non-archimedean estimate: if exactly one term attains the minimum, the valuation of the sum equals that minimum.",
      ]),
      paragraph([
        "Proof of (Q1). Since ",
        math(String.raw`\bar B\bar G=\bar{\tilde E}`),
        ", every coefficient of ",
        math(String.raw`\tilde E-BG`),
        " is divisible by ",
        math(String.raw`\ell`),
        ", so ",
        math(String.raw`H\in\mathbb{Z}[z^{\pm1},w^{\pm1}]`),
        ". Moreover ",
        math(String.raw`\bar G=c\,\chi^{w_0}\bar G_0`),
        ", and by the uniqueness of the factorisation of (K4) the factor ",
        math(String.raw`\bar G_0`),
        " has no factor of the form ",
        math(String.raw`\chi^{v}-1`),
        ". The monomial ",
        math(String.raw`\chi^{w_0}`),
        " and the unit ",
        math(String.raw`c`),
        " are not divisible by the prime element ",
        math(String.raw`\chi^{v}-1`),
        ", so neither is ",
        math(String.raw`\bar G`),
        ".",
      ]),
      paragraph([
        "Proof of (Q2)(1). Apply the general form of (K2) stated above to ",
        math(String.raw`\bar F=\bar G`),
        ". By (Q1) the polynomial ",
        math(String.raw`\bar G`),
        " has no factor of the form ",
        math(String.raw`\chi^{u^\perp}-1`),
        ", so at rational points ",
        math(String.raw`\theta_G<\infty`),
        ". At irrational points, that is on ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)\setminus\mathbb{P}^1(\mathbb{Q})`),
        ", the proof of part (J6) of ",
        ref("paper_prop_J"),
        " applies verbatim to ",
        math(String.raw`\bar G`),
        " (it uses only the linear independence of the exponentials), so if ",
        math(String.raw`\theta_G=\infty`),
        " then ",
        math(String.raw`P`),
        " is rational. Hence the depth is finite everywhere. Boundedness follows from the local constancy of part (J1) of ",
        ref("paper_prop_J"),
        " together with the compactness of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}_\ell)`),
        ".",
      ]),
      paragraph([
        "Proof of (Q2)(2). Putting ",
        math(String.raw`\Phi^{G}=\sum_m A^{G}_m x^{m}`),
        " and ",
        math(String.raw`\pi=\zeta-1`),
        " we get ",
        math(String.raw`v_{\mathfrak{l}}\bigl(A^{G}_m\pi^{m}\bigr)=\varphi(\ell^{M})\,v_\ell(A^{G}_m)+m`),
        ". For ",
        math(String.raw`m<\theta_G(P)`),
        " we have ",
        math(String.raw`\ell\mid A^{G}_m`),
        ", so the value is ",
        math(String.raw`\ge\varphi(\ell^{M})>\theta_G^{\max}\ge\theta_G(P)`),
        "; at ",
        math(String.raw`m=\theta_G(P)`),
        " the value is exactly ",
        math(String.raw`\theta_G(P)`),
        "; and for ",
        math(String.raw`m>\theta_G(P)`),
        " the value is ",
        math(String.raw`>\theta_G(P)`),
        ". Hence the minimising index is the single index ",
        math(String.raw`m=\theta_G(P)`),
        ", and equality in the non-archimedean estimate gives the assertion. ",
        "The reason this is a theorem rather than a hypothesis can be said in one line. All the depth has been moved into the first factor of (Q1); what remains has depth bounded independently of the level, whereas the quantity it is compared against, namely ",
        math(String.raw`\varphi(\ell^{M})`),
        ", grows without bound with the level.",
      ]),
      paragraph([
        "Proof of (Q3). First we compute the valuation of the first factor. We have ",
        math(String.raw`\chi^{v_i}(\omega_P)=\zeta^{\langle v_i,(a,b)\rangle}`),
        ", and if ",
        math(String.raw`\rho:=\rho_i(P)<M`),
        " this is a primitive ",
        math(String.raw`\ell^{\,M-\rho}`),
        "-th root of unity, so ",
        math(String.raw`v_{\mathfrak{l}}\bigl(\chi^{v_i}(\omega_P)-1\bigr)=\varphi(\ell^{M})/\varphi(\ell^{\,M-\rho})=\ell^{\rho}`),
        " (whereas if ",
        math(String.raw`\rho_i(P)=M`),
        " then ",
        math(String.raw`\chi^{v_i}(\omega_P)=1`),
        " and the factor is ",
        math(String.raw`0`),
        "). Under the hypothesis ",
        math(String.raw`\beta_P+\theta_G^{\max}<\varphi(\ell^{M})`),
        " we have, for every ",
        math(String.raw`i`),
        ", the inequality ",
        math(String.raw`\rho_i(P)<M`),
        ". Indeed, if some ",
        math(String.raw`i`),
        " had ",
        math(String.raw`\rho_i(P)=M`),
        " then ",
        math(String.raw`\beta_P\ge m_i\ell^{M}\ge\ell^{M}>\varphi(\ell^{M})`),
        ", contradicting the hypothesis. Hence ",
        math(String.raw`B(\omega_P)\neq0`),
        " and ",
        math(String.raw`v_{\mathfrak{l}}\bigl(B(\omega_P)\bigr)=\beta_P`),
        ".",
      ]),
      paragraph([
        "Evaluating the decomposition of (Q1) at ",
        math(String.raw`\omega_P`),
        " gives ",
        math(String.raw`\tilde E(\omega_P)=B(\omega_P)G(\omega_P)+\ell\,H(\omega_P)`),
        ". By the computation above together with (Q2)(2), the valuation of the first term is ",
        math(String.raw`\beta_P+\theta_G(P)`),
        " (finite), while that of the second is ",
        math(String.raw`\varphi(\ell^{M})+v_{\mathfrak{l}}\bigl(H(\omega_P)\bigr)\ge\varphi(\ell^{M})`),
        " (or, if ",
        math(String.raw`H(\omega_P)=0`),
        ", it is ",
        math(String.raw`\infty`),
        "). By hypothesis ",
        math(String.raw`\beta_P+\theta_G(P)\le\beta_P+\theta_G^{\max}<\varphi(\ell^{M})`),
        ", strictly smaller, so by the non-archimedean estimate the valuation of the sum equals the smaller one. ",
        "Not a single uniqueness hypothesis on the minimising index has entered here. All that was used is the decomposition of (Q1) and part (Q2).",
      ]),
      paragraph([
        "Proof of (Q4); this is the one place in the present proposition where the real numbers are used. By part (R4) of ",
        ref("paper_prop_R"),
        " we have ",
        math(String.raw`\hat\theta_M(P)=v_\ell\bigl(N_{K_M/\mathbb{Q}}(\tilde E(\omega_P))\bigr)`),
        ", and the right-hand side is the ",
        math(String.raw`0`),
        " — more precisely, the valuation of a nonzero integer ",
        math(String.raw`N`),
        " — namely the ",
        math(String.raw`\ell`),
        "-adic valuation of that integer. Since ",
        math(String.raw`N=\prod_\sigma\sigma\bigl(\tilde E(\omega_P)\bigr)`),
        " and each conjugate has the form ",
        math(String.raw`\sum_{(p,q)}c_{pq}\xi^{k_{pq}}`),
        " with ",
        math(String.raw`\xi`),
        " a root of unity, its complex absolute value is ",
        math(String.raw`\le C_0`),
        ". Hence ",
        math(String.raw`|N|\le C_0^{\varphi(\ell^{M})}`),
        ", and since ",
        math(String.raw`|N|\ge1`),
        " we obtain ",
        math(String.raw`v_\ell(N)\le\log_\ell|N|\le\varphi(\ell^{M})\log_\ell C_0`),
        ". The complex absolute value was used in this one step only, and no purely non-archimedean route to this bound has been found.",
      ]),
      paragraph([
        "Proof of (Q5); first we dispose of the degenerate case. The case is ",
        math(String.raw`b=0`),
        ". Since the ",
        math(String.raw`m_i`),
        " of (K4) are ",
        math(String.raw`\ge1`),
        ", the conditions ",
        math(String.raw`b=\sum_i m_i=0`),
        " and ",
        math(String.raw`r=0`),
        " are equivalent, and then ",
        math(String.raw`B`),
        " is the empty product ",
        math(String.raw`1`),
        ", so every point ",
        math(String.raw`P`),
        " has ",
        math(String.raw`\beta_P=0`),
        ". Membership ",
        math(String.raw`P\in\mathcal{B}_M`),
        " would require ",
        math(String.raw`\theta_G^{\max}\ge\varphi(\ell^{M})`),
        ", which together with the hypothesis ",
        math(String.raw`\varphi(\ell^{M})\ge2\theta_G^{\max}`),
        " gives ",
        math(String.raw`\theta_G^{\max}\ge2\theta_G^{\max}`),
        ", that is ",
        math(String.raw`\theta_G^{\max}=0`),
        ", whence ",
        math(String.raw`0\ge\varphi(\ell^{M})\ge1`),
        ", a contradiction. Therefore ",
        math(String.raw`\mathcal{B}_M=\emptyset`),
        " and the assertion holds in the form ",
        math(String.raw`0\le r\ell^{c_1}=0`),
        ".",
      ]),
      paragraph([
        "Assume from now on that the coefficient is positive, that is ",
        math(String.raw`b\ge1`),
        ". If ",
        math(String.raw`P\in\mathcal{B}_M`),
        " then ",
        math(String.raw`\beta_P\ge\varphi(\ell^{M})-\theta_G^{\max}\ge\varphi(\ell^{M})/2`),
        ". Put ",
        math(String.raw`\rho_{\max}:=\max_i\rho_i(P)`),
        ". If some ",
        math(String.raw`i`),
        " has ",
        math(String.raw`\rho_i(P)=M`),
        " then ",
        math(String.raw`\rho_{\max}=M\ge M-c_1`),
        " and what follows is trivial, so assume that every ",
        math(String.raw`i`),
        " has ",
        math(String.raw`\rho_i(P)<M`),
        ". Then ",
        math(String.raw`\beta_P\le b\,\ell^{\rho_{\max}}`),
        ", whence ",
        math(String.raw`\ell^{\rho_{\max}}\ge\varphi(\ell^{M})/(2b)=\ell^{M-1}(\ell-1)/(2b)`),
        ". The strict inequality ",
        math(String.raw`c_1`),
        " in the definition — namely ",
        math(String.raw`2b<(\ell-1)\ell^{c_1}`),
        " — gives ",
        math(String.raw`(\ell-1)/(2b)>\ell^{-c_1}`),
        ", so that",
      ]),
      displayMath(
        String.raw`\ell^{\rho_{\max}}\ >\ \ell^{M-1}\cdot\ell^{-c_1}=\ell^{\,M-c_1-1},`,
      ),
      paragraph([
        " that is ",
        math(String.raw`\rho_{\max}>M-c_1-1`),
        ", and since ",
        math(String.raw`\rho_{\max}`),
        " is an integer we get ",
        math(String.raw`\rho_{\max}\ge M-c_1`),
        ". Only the strict inequality was used here; no logarithm and no ceiling. Consequently ",
        math(String.raw`\mathcal{B}_M\subseteq\bigcup_{i=1}^{r}\{P:\rho_i(P)\ge M-c_1\}`),
        ". For ",
        math(String.raw`\rho\ge1`),
        " the set ",
        math(String.raw`\{P:\rho_i(P)\ge\rho\}`),
        " is the fibre over a single point of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})\to\mathbb{P}^1(\mathbb{Z}/\ell^{\rho})`),
        ", of size ",
        math(String.raw`\ell^{\,M-\rho}`),
        ". Taking ",
        math(String.raw`\rho=M-c_1`),
        " (which is legitimate since ",
        math(String.raw`M>c_1`),
        " makes it ",
        math(String.raw`\ge1`),
        ") the size is ",
        math(String.raw`\ell^{c_1}`),
        ", and summing gives ",
        math(String.raw`\lvert\mathcal{B}_M\rvert\le r\ell^{c_1}`),
        ".",
      ]),
      paragraph([
        "Proof of (Q6). We have ",
        math(String.raw`\#\{P:\rho_v(P)\ge\rho\}=\ell^{\,M-\rho}`),
        " for ",
        math(String.raw`1\le\rho\le M`),
        ", and the number of points of ",
        math(String.raw`\mathbb{P}^1(\mathbb{Z}/\ell^{M})`),
        " is ",
        math(String.raw`(\ell+1)\ell^{M-1}`),
        ". Hence ",
        math(String.raw`\#\{\rho_v=0\}=(\ell+1)\ell^{M-1}-\ell^{M-1}=\ell^{M}`),
        ", ",
        math(String.raw`\#\{\rho_v=\rho\}=\varphi(\ell^{\,M-\rho})`),
        " for ",
        math(String.raw`1\le\rho\le M-1`),
        ", and ",
        math(String.raw`\#\{\rho_v=M\}=1`),
        ". Since ",
        math(String.raw`\varphi(\ell^{\,M-\rho})\ell^{\rho}=\varphi(\ell^{M})`),
        " holds for all ",
        math(String.raw`1\le\rho\le M-1`),
        ", that is in all ",
        math(String.raw`M-1`),
        " cases, we obtain",
      ]),
      displayMath(
        String.raw`\sum_{P}\ell^{\rho_v(P)}=\ell^{M}\cdot1+(M-1)\varphi(\ell^{M})+1\cdot\ell^{M}
        =(M-1)\varphi(\ell^{M})+2\ell^{M}`,
      ),
      paragraph([
        ". Counting the points incorrectly is a real trap here. The wrong count would be ",
        math(String.raw`\ell^{M}`),
        "; the number of points of a projective line is ",
        math(String.raw`(\ell+1)\ell^{M-1}`),
        ".",
      ]),
      paragraph([
        "Proof of (Q7). Split the points into ",
        math(String.raw`\mathcal{B}_M`),
        " and its complement. By (Q3),",
      ]),
      displayMath(
        String.raw`\Theta_M=\sum_{P\notin\mathcal{B}_M}\bigl(\beta_P+\theta_G(P)\bigr)
        +\sum_{P\in\mathcal{B}_M}\hat\theta_M(P)`,
      ),
      paragraph([
        ". Applying (Q6) for each ",
        math(String.raw`i`),
        " gives ",
        math(String.raw`\sum_{P}\beta_P=\sum_i m_i\bigl((M-1)\varphi(\ell^{M})+2\ell^{M}\bigr)=b(M-1)\varphi(\ell^{M})+2b\ell^{M}`),
        ", and since ",
        math(String.raw`0\le\sum_{P\in\mathcal{B}_M}\beta_P\le\lvert\mathcal{B}_M\rvert\,b\,\ell^{M}`),
        " we obtain",
      ]),
      displayMath(
        String.raw`\Bigl|\sum_{P\notin\mathcal{B}_M}\beta_P-b\,M\,\varphi(\ell^{M})\Bigr|
        \le b\,\varphi(\ell^{M})+2b\ell^{M}+\lvert\mathcal{B}_M\rvert\,b\,\ell^{M}
        \le\bigl(3b+\lvert\mathcal{B}_M\rvert\,b\bigr)\ell^{M}`,
      ),
      paragraph([
        ". The remaining two terms satisfy ",
        math(String.raw`0\le\sum_{P\notin\mathcal{B}_M}\theta_G(P)\le\theta_G^{\max}(\ell+1)\ell^{M-1}`),
        " and, by (Q4), ",
        math(String.raw`0\le\sum_{P\in\mathcal{B}_M}\hat\theta_M(P)\le\lvert\mathcal{B}_M\rvert\varphi(\ell^{M})\log_\ell C_0\le\lvert\mathcal{B}_M\rvert\ell^{M}\log_\ell C_0`),
        ". To apply (Q4) we need, at every point of ",
        math(String.raw`\mathcal{B}_M`),
        ", that ",
        math(String.raw`\tilde E(\omega_P)\neq0`),
        ". This follows from the hypothesis (H): it says that ",
        math(String.raw`X_{\ell^n,\ell^n}`),
        " is connected, that is ",
        math(String.raw`\kappa_n\neq0`),
        ", which by part (R5) of ",
        ref("paper_prop_R"),
        " is equivalent to the finiteness of the right-hand side of that expression, so at the points of ",
        math(String.raw`\mathcal{B}_M`),
        " one indeed has ",
        math(String.raw`\tilde E(\omega_P)\neq0`),
        ". The triangle inequality now gives the error estimate. Under the hypotheses, (Q5) gives ",
        math(String.raw`\lvert\mathcal{B}_M\rvert\le r\ell^{c_1}`),
        ", which may therefore be substituted (note that ",
        math(String.raw`\log_\ell C_0\ge0`),
        " because ",
        math(String.raw`C_0=\sum|c_{pq}|\ge1`),
        "), and then ",
        math(String.raw`C`),
        " is a constant independent of ",
        math(String.raw`M`),
        ".",
      ]),
      paragraph([
        "The asymptotic form. By part (R5) of ",
        ref("paper_prop_R"),
        " the identity ",
        math(String.raw`\Sigma_n=\sum_{M=1}^{n}\Theta_M`),
        " holds with no hypothesis. Sum the above error estimate over the levels at which the hypotheses hold (the finitely many smaller levels are absorbed into a constant). The coefficient of ",
        math(String.raw`\sum_{M=1}^{n}M\varphi(\ell^{M})=\sum_{M=1}^{n}M\bigl(\ell^{M}-\ell^{M-1}\bigr)`),
        " at ",
        math(String.raw`n\ell^{n}`),
        " is ",
        math(String.raw`\frac{\ell}{\ell-1}\cdot\frac{\ell-1}{\ell}=1`),
        ", and ",
        math(String.raw`\sum_{M=1}^{n}\ell^{M}=O(\ell^{n})`),
        ". Hence ",
        math(String.raw`\Sigma_n=b\,n\ell^{n}+O(\ell^{n})`),
        ", and substituting into the expression of part (R5) of ",
        ref("paper_prop_R"),
        " and absorbing ",
        math(String.raw`-2n+v_\ell(\kappa(X))=O(\ell^{n})`),
        " into the error gives the assertion.",
      ]),
      paragraph([
        "Proof of (Q8). The values of the various quantities when ",
        math(String.raw`b=0`),
        " were determined in the first step of the proof of (Q5). Substituting ",
        math(String.raw`C`),
        " — that is, substituting ",
        math(String.raw`b=0`),
        " and ",
        math(String.raw`r=0`),
        " into its definition — gives ",
        math(String.raw`C=\theta_G^{\max}\frac{\ell+1}{\ell}`),
        ". Indeed, when ",
        math(String.raw`b=0`),
        " we have ",
        math(String.raw`\mathcal{B}_M=\emptyset`),
        " and ",
        math(String.raw`\beta_P=0`),
        " at every point, so ",
        math(String.raw`\Theta_M=\sum_{P}\theta_G(P)\le\theta_G^{\max}(\ell+1)\ell^{M-1}`),
        ", and the bound of (Q7) agrees exactly with this trivial count (the second summand of ",
        math(String.raw`C`),
        " was this term to begin with). The asymptotic form is obtained by putting ",
        math(String.raw`b=0`),
        " in (Q7).",
      ]),
    ],
  },
]);
