

# This file was *autogenerated* from the file cancellation.sage
from sage.all_cmdline import *   # import sage library

_sage_const_0 = Integer(0); _sage_const_1 = Integer(1); _sage_const_78 = Integer(78); _sage_const_2 = Integer(2); _sage_const_3 = Integer(3); _sage_const_6 = Integer(6); _sage_const_5 = Integer(5); _sage_const_7 = Integer(7); _sage_const_150p0 = RealNumber('150.0'); _sage_const_4 = Integer(4); _sage_const_100p0 = RealNumber('100.0'); _sage_const_60 = Integer(60); _sage_const_44 = Integer(44); _sage_const_11 = Integer(11); _sage_const_40 = Integer(40); _sage_const_120p0 = RealNumber('120.0')# cycle 20 / T3 Pure: 打ち消し（cycle 19 定理 J4 の |J(r)| >= 2）を潰す。
#
# 対応する証明本体: outputs/reports/cycle20_T3_cancellation_recursion.md
#
#   Step A  定理 L1（桁枝再帰）の基礎照合。cycle 19 §7.1 の反例
#           （ell=2 トーラス、theta(1,3) = 6 に対し J4 の下界は 4）を含む。
#           母集団 x P^1(Z/ell^M) の全列挙で L1 = 直接計算（ord of bar Phi）を確認する。
#   Step B  cycle 19 §7.1 の奇素数の表を再計算し、J4 が tie だった層で L1 が
#           theta を決めることを内訳つきで示す。
#   Step C  系 L2（theta <= ell^sep - 1）と 系 L3（距離による上界）の照合。
#   Step D  定理 L4（終結式公式）を円分体での直接付値計算と照合し、
#           定理 B' が tie で外す件数の**内訳**を出す。
#           （Step E（定理 K' の母集団照合）は theorem_k_prime.sage に分けた。）
#   Step F  ell=2 トーラスの Theta_3 = 44（cycle 19 の定理 B' の和は 40）。
#   Step G  敵対的レビュー（自分の結論を反証しにいく）。
#
# 実行: sage cancellation.sage > cancellation.out 2>&1

import sys, time
from itertools import combinations_with_replacement as cwr
sys.stdout.reconfigure(line_buffering=True)

load('_defs20.sage')

FAIL = _sage_const_0 
TRUNC = []
def check(cond, msg):
    global FAIL
    if not cond:
        FAIL += _sage_const_1 
        print("   *** FAIL:", msg)
    return cond

def bq(vs):
    return (_sage_const_1 , [(_sage_const_0 , _sage_const_0 , tuple(v)) for v in vs])

def co_of(m, edges, ell):
    D = detL(m, edges)
    mu = mu_content(D, ell)
    return cleared_coeffs(E_of(D, ell, mu))

T_START = time.time()
print("=" * _sage_const_78 )
print("cycle 20 / T3: 打ち消し（定理 J4 の |J(r)| >= 2）— 桁枝再帰で閉じる")
print("=" * _sage_const_78 )

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step A: 定理 L1（桁枝再帰）の基礎照合", el())
print("=" * _sage_const_78 )
print("""
  主張: bar Phi = sum_c (1+x)^c g_c(x^ell) と枝分けすると
        theta = ell * min_c ord(g_c) + s*,  s* = min{s : sum_{c in C} lc_c binom(c,s) != 0}
        で、s* は必ず存在する（binom(c,s) の ell x ell 行列が可逆）。打ち消しは起きない。
  照合: (i) cycle 19 §7.1 の反例で L1 が真値を出すこと、
        (ii) 母集団 x レベルの全列挙で L1 = 直接計算（bar Phi の位数）。
""")

# --- (i) cycle 19 §7.1 の反例 ---------------------------------------------
(m2, e2) = bq([(_sage_const_1 , _sage_const_0 ), (_sage_const_0 , _sage_const_1 )])
co2 = co_of(m2, e2, _sage_const_2 )
print("  ell=2 トーラス: bar tilde E の台 =", sorted(co2.keys()))
meas = collapsed_measure(co2, _sage_const_2 , _sage_const_1 , _sage_const_3 )
print("  (a,b)=(1,3) の指数と係数:", sorted((ZZ(g), int(v)) for (g, v) in meas.items()))
(th_L1, lc) = theta_lc_L1(meas, _sage_const_2 )
th_dir = theta_signed(co2, _sage_const_2 , _sage_const_1 , _sage_const_3 )
(Lam, J) = J4_tie_data(co2, _sage_const_2 , _sage_const_1 , _sage_const_1 , _sage_const_1 )
print("  cycle 19 定理 J4: Lambda(1) = %s, J(1) = %s（|J| = %d なので theta を決めない）"
      % (Lam, J, len(J)))
print("  定理 L1: theta = %s（先頭係数 %s） / 直接計算 theta = %s" % (th_L1, lc, th_dir))
check(len(J) >= _sage_const_2 , "ell=2 トーラス (1,3): J4 が tie でない（前提が崩れている）")
check(th_L1 == _sage_const_6  and th_dir == _sage_const_6 , "ell=2 トーラス theta(1,3) が 6 でない: L1=%s 直接=%s" % (th_L1, th_dir))
print("  → cycle 19 §7.1 が「閉形式化を妨げている 1 つ目」とした反例で、L1 は真値 6 を出す。")

# --- (ii) 母集団 x レベルの全列挙 -------------------------------------------
def gen_pop():
    out = []
    V = [(_sage_const_1 , _sage_const_0 ), (_sage_const_0 , _sage_const_1 ), (_sage_const_1 , _sage_const_1 ), (_sage_const_1 , -_sage_const_1 ), (_sage_const_2 , _sage_const_1 ), (_sage_const_1 , _sage_const_2 ), (_sage_const_2 , _sage_const_3 ), (_sage_const_0 , _sage_const_2 ), (_sage_const_2 , _sage_const_0 )]
    for r in [_sage_const_2 , _sage_const_3 ]:
        for combo in cwr(range(len(V)), r):
            out.append(('bq ' + ' '.join(str(V[i]) for i in combo), _sage_const_1 ,
                        [(_sage_const_0 , _sage_const_0 , V[i]) for i in combo]))
    for r in [_sage_const_3 ]:
        for combo in cwr(range(len(V) + _sage_const_1 ), r):
            edges = [(_sage_const_0 , _sage_const_1 , ((_sage_const_0 , _sage_const_0 ) if i == len(V) else V[i])) for i in combo]
            out.append(('2v ' + ' '.join(('(0,0)' if i == len(V) else str(V[i]))
                                         for i in combo), _sage_const_2 , edges))
    return out

POP = gen_pop()
print()
print("  母集団 %d 個で L1 = 直接計算を全列挙照合する。" % len(POP))
LEVELS = {_sage_const_2 : _sage_const_5 , _sage_const_3 : _sage_const_3 , _sage_const_5 : _sage_const_2 , _sage_const_7 : _sage_const_2 }
A_BUDGET = _sage_const_150p0             # ell ごとの壁時計上限（秒）
A_STATS = {}
for ell in [_sage_const_2 , _sage_const_3 , _sage_const_5 , _sage_const_7 ]:
    t0 = time.time()
    npt = nagree = ninf = _sage_const_0 
    nleft = _sage_const_0 
    for (name, m, edges) in POP:
        if time.time() - t0 > A_BUDGET:
            nleft += _sage_const_1 
            continue
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        co = cleared_coeffs(E_of(D, ell, mu_content(D, ell)))
        for M in range(_sage_const_1 , LEVELS[ell] + _sage_const_1 ):
            for (a, b) in P1_reps(ell, M):
                t1 = theta_L1(co, ell, a, b)
                t2 = theta_signed(co, ell, a, b)
                npt += _sage_const_1 
                if t1 == t2:
                    nagree += _sage_const_1 
                else:
                    check(False, "ell=%d %s (a,b)=(%s,%s): L1=%s != 直接=%s"
                          % (ell, name, a, b, t1, t2))
                if t1 is oo:
                    ninf += _sage_const_1 
    if nleft:
        TRUNC.append("Step A ell=%d: 壁時計 %.0fs 超過のため %d 個を未実施" % (ell, A_BUDGET, nleft))
    A_STATS[ell] = (npt, nagree, ninf, nleft)
    print("    ell=%-2d : 照合 %d 点（一致 %d 点 / うち theta=oo %d 点）/ 未実施の塔 %d 個"
          % (ell, npt, nagree, ninf, nleft))
print("  ※ theta=oo の点でも L1 は（collapsed measure が空という形で）正しく oo を返す。")

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step B: cycle 19 §7.1 の奇素数の表を、L1 で潰す", el())
print("=" * _sage_const_78 )
print("""
  cycle 19 §7.1 は「Lambda が一意でない層」の件数を挙げて、そこで theta が決まらないと書いた。
  同じ層を L1 で計算し、直接計算と一致することを層ごとに示す（内訳を出す）。
""")

B_EX = [
    (_sage_const_3 , 'bq (1,0)x2,(0,1)', bq([(_sage_const_1 , _sage_const_0 ), (_sage_const_1 , _sage_const_0 ), (_sage_const_0 , _sage_const_1 )]), (_sage_const_1 , _sage_const_2 ), 'w'),
    (_sage_const_3 , 'bq (1,0),(2,3),(1,1)', bq([(_sage_const_1 , _sage_const_0 ), (_sage_const_2 , _sage_const_3 ), (_sage_const_1 , _sage_const_1 )]), (_sage_const_1 , _sage_const_0 ), 'w'),
    (_sage_const_5 , 'bq (1,0)x4,(0,1)', bq([(_sage_const_1 , _sage_const_0 )] * _sage_const_4  + [(_sage_const_0 , _sage_const_1 )]), (_sage_const_1 , _sage_const_4 ), 'w'),
]
for (ell, name, (m, edges), (a0, b0), pert) in B_EX:
    co = co_of(m, edges, ell)
    es = psi_data(co, ell, a0, b0, pert)
    print("  ell=%d %s 基点 (%d:%d): e_j = %s"
          % (ell, name, a0, b0, [('oo' if e is oo else str(e)) for (e, lc) in es]))
    nlayer = ntie = nres = _sage_const_0 
    Mmax = _sage_const_4  if ell == _sage_const_3  else _sage_const_3 
    for M in range(_sage_const_1 , Mmax + _sage_const_1 ):
        for beta in range(ell**(M - _sage_const_1 )):
            b = b0 + ell * beta
            v = oo if beta == _sage_const_0  else ZZ(beta).valuation(ell)
            r = None if v is oo else v + _sage_const_1 
            nlayer += _sage_const_1 
            if r is None:
                continue
            (Lam, J) = Lambda_at(es, ell, r)
            t1 = theta_L1(co, ell, a0, b)
            t2 = theta_signed(co, ell, a0, b)
            if len(J) >= _sage_const_2 :
                ntie += _sage_const_1 
                if t1 == t2 and t1 is not oo:
                    nres += _sage_const_1 
            check(t1 == t2, "ell=%d %s (a,b)=(%s,%s): L1=%s != 直接=%s" % (ell, name, a0, b, t1, t2))
    print("      走査した beta: %d 個 / J4 が tie（|J(r)|>=2）: %d 個 / L1 が有限の theta を決めた: %d 個"
          % (nlayer, ntie, nres))
    check(ntie > _sage_const_0 , "ell=%d %s: tie が 1 つも無い（cycle 19 の表と食い違う）" % (ell, name))
    check(ntie == nres, "ell=%d %s: tie のうち L1 で決まらなかったものがある" % (ell, name))

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step C: 系 L2（theta <= ell^sep - 1）と系 L3（距離による上界）", el())
print("=" * _sage_const_78 )
print("""
  系 L2: theta(nu) <= ell^{sep(nu)} - 1（sep = 指数が mod ell^sep で分離する深さ）。
  系 L3: theta(P) <= ell^{1 + max_delta v_ell(delta.(a,b))} - 1
         （delta は supp(bar tilde E) の差ベクトル）。
         右辺は P が S_oo の候補点へ近づくときだけ大きくなる。
""")
C_BUDGET = _sage_const_100p0             # ell ごとの壁時計上限（秒）
for ell in [_sage_const_2 , _sage_const_3 , _sage_const_5 ]:
    t0 = time.time()
    npt = nL2 = nL3 = _sage_const_0 
    worst = (_sage_const_0 , None)
    nleft = _sage_const_0 
    for (name, m, edges) in POP:
        if time.time() - t0 > C_BUDGET:
            nleft += _sage_const_1 
            continue
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        co = cleared_coeffs(E_of(D, ell, mu_content(D, ell)))
        for M in range(_sage_const_1 , (_sage_const_4  if ell == _sage_const_2  else _sage_const_3 ) + _sage_const_1 ):
            for (a, b) in P1_reps(ell, M):
                meas = collapsed_measure(co, ell, a, b)
                th = theta_lc_L1(meas, ell)[_sage_const_0 ]
                if th is oo:
                    continue
                npt += _sage_const_1 
                s = sep_of(meas, ell)
                if th <= ell**s - _sage_const_1 :
                    nL2 += _sage_const_1 
                else:
                    check(False, "系 L2 破れ ell=%d %s (%s,%s): theta=%s > ell^%s-1" % (ell, name, a, b, th, s))
                bd = L3_bound(co, ell, a, b)
                if bd is oo or th <= bd:
                    nL3 += _sage_const_1 
                else:
                    check(False, "系 L3 破れ ell=%d %s (%s,%s): theta=%s > %s" % (ell, name, a, b, th, bd))
                if th > worst[_sage_const_0 ]:
                    worst = (th, (name, a, b, s))
    if nleft:
        TRUNC.append("Step C ell=%d: 壁時計 %.0fs 超過のため %d 個を未実施" % (ell, C_BUDGET, nleft))
    print("  ell=%-2d : theta 有限な点 %d 個 / 系 L2 成立 %d / 系 L3 成立 %d / 未実施の塔 %d 個"
          % (ell, npt, nL2, nL3, nleft))
    print("           最大の theta: %s（%s）" % (worst[_sage_const_0 ], worst[_sage_const_1 ]))

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step D: 定理 L4（終結式公式）と、定理 B' の tie の内訳", el())
print("=" * _sage_const_78 )
print("""
  主張: hat theta_M(a,b) = v_ell( Res_x( Psi_M, Phi_{(a,b)} ) )、Psi_M(x) = Phi_{ell^M}(1+x)。
  照合: 円分体 Q(zeta_{ell^M}) での直接付値計算（cycle 16 point_val）と一致するか。
  同時に、cycle 19 定理 B' の min_m(phi v_ell(A_m) + m) が
    (a) 最小点が一意（B' が使える）
    (b) tie だが値は当たる
    (c) tie で値が外れる  ← ここが cycle 19 で予言を出せなかった原因
  の**内訳**を出す。
""")
D_BUDGET = _sage_const_150p0             # ell ごとの壁時計上限（秒）
for ell in [_sage_const_2 , _sage_const_3 , _sage_const_5 ]:
    t0 = time.time()
    Mmax = _sage_const_3  if ell == _sage_const_2  else _sage_const_2 
    npt = nagree = nuniq = ntie_ok = ntie_bad = _sage_const_0 
    nleft = _sage_const_0 
    bad_ex = []
    for (name, m, edges) in POP[:_sage_const_60 ]:
        if time.time() - t0 > D_BUDGET:
            nleft += _sage_const_1 
            continue
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        mu = mu_content(D, ell)
        co = cleared_coeffs(E_of(D, ell, mu))
        for M in range(_sage_const_1 , Mmax + _sage_const_1 ):
            for (a, b) in P1_reps(ell, M):
                (vres, _) = hat_theta_resultant(co, ell, M, a, b)
                vdir = delta_measured(D, ell, mu, M, a, b)
                if vres is None or vdir is None:
                    continue
                npt += _sage_const_1 
                if ZZ(vres) == ZZ(vdir):
                    nagree += _sage_const_1 
                else:
                    check(False, "定理 L4 破れ ell=%d %s M=%d (%s,%s): Res=%s != 円分体=%s"
                          % (ell, name, M, a, b, vres, vdir))
                (vB, uniq, arg) = hat_theta_predicted(co, ell, M, a, b)
                if uniq:
                    nuniq += _sage_const_1 
                    check(ZZ(vB) == ZZ(vres), "定理 B'（一意）が L4 と食い違う ell=%d %s M=%d (%s,%s)"
                          % (ell, name, M, a, b))
                elif ZZ(vB) == ZZ(vres):
                    ntie_ok += _sage_const_1 
                else:
                    ntie_bad += _sage_const_1 
                    if len(bad_ex) < _sage_const_3 :
                        bad_ex.append("%s M=%d (%s,%s): B'=%s 真値=%s" % (name, M, a, b, vB, vres))
    if nleft:
        TRUNC.append("Step D ell=%d: 壁時計 %.0fs 超過のため %d 個を未実施" % (ell, D_BUDGET, nleft))
    print("  ell=%-2d : 照合 %d 点（L4 = 円分体 %d 点）/ 未実施の塔 %d 個" % (ell, npt, nagree, nleft))
    print("           定理 B' の内訳: 一意 %d / tie だが当たる %d / **tie で外す %d**"
          % (nuniq, ntie_ok, ntie_bad))
    if bad_ex:
        print("           tie で外した例: %s" % ' | '.join(bad_ex))

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step F: ell=2 トーラスの Theta_M（cycle 19 §5.4 の 40 vs 真値 44）", el())
print("=" * _sage_const_78 )
co2 = co_of(m2, e2, _sage_const_2 )
for M in range(_sage_const_1 , _sage_const_5 ):
    (tB, okB) = Theta_level(co2, _sage_const_2 , M)
    (tX, okX) = Theta_level_exact(co2, _sage_const_2 , M)
    tD = sum(ZZ(delta_measured(detL(m2, e2), _sage_const_2 , mu_content(detL(m2, e2), _sage_const_2 ), M, a, b))
             for (a, b) in P1_reps(_sage_const_2 , M))
    print("  M=%d : 定理 B' の和 = %-4s（一意 %s） / 定理 L4 の和 = %-4s / 円分体の直接和 = %-4s"
          % (M, tB, okB, tX, tD))
    check(ZZ(tX) == ZZ(tD), "ell=2 トーラス M=%d: L4 の和が円分体の和と違う" % M)
check(ZZ(Theta_level_exact(co2, _sage_const_2 , _sage_const_3 )[_sage_const_0 ]) == _sage_const_44 , "ell=2 トーラス Theta_3 が 44 でない")
print("  → cycle 19 §5.4 が報告した「真値 Theta_3 = 44 に対し定理 B' の和は 40」を再現し、")
print("     定理 L4 は 44 を出す（B' の tie が原因であって、定理 J4 の打ち消しが原因ではない）。")

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step G: 敵対的レビュー", el())
print("=" * _sage_const_78 )

print("  G1: binom(c,s) の ell x ell 行列（0<=c,s<ell）は F_ell 上可逆か（定理 L1 の要）")
for ell in [_sage_const_2 , _sage_const_3 , _sage_const_5 , _sage_const_7 , _sage_const_11 ]:
    Mx = matrix(GF(ell), ell, ell, lambda c, s: GF(ell)(binomial(c, s)))
    check(Mx.is_invertible(), "ell=%d: binom 行列が可逆でない" % ell)
    print("      ell=%-2d det = %s" % (ell, Mx.det()))

print("  G2: 「L1 は打ち消しが起きないだけで、単に J4 と同じ下界を返しているのでは？」")
print("      → theta > Lambda(r) となる点が実在することを数える（L1 は真値、J4 は下界）。")
for (ell, name, (m, edges), (a0, b0), pert) in B_EX + [(_sage_const_2 , 'torus', bq([(_sage_const_1 , _sage_const_0 ), (_sage_const_0 , _sage_const_1 )]), (_sage_const_1 , _sage_const_1 ), 'w')]:
    co = co_of(m, edges, ell)
    es = psi_data(co, ell, a0, b0, pert)
    ngt = ntot = _sage_const_0 
    for M in range(_sage_const_1 , _sage_const_4 ):
        for beta in range(_sage_const_1 , ell**(M - _sage_const_1 ) + _sage_const_1 ):
            b = b0 + ell * beta
            r = ZZ(beta).valuation(ell) + _sage_const_1 
            (Lam, J) = Lambda_at(es, ell, r)
            t1 = theta_L1(co, ell, a0, b)
            if t1 is oo:
                continue
            ntot += _sage_const_1 
            if t1 > Lam:
                ngt += _sage_const_1 
    print("      ell=%d %s 基点(%d:%d): 走査 %d 点中 theta > Lambda(r) が %d 点"
          % (ell, name, a0, b0, ntot, ngt))

print("  G3: 「L1 の再帰は本当に止まるか（深さは sep で抑えられるか）」")
maxdepth = _sage_const_0 
for ell in [_sage_const_2 , _sage_const_3 ]:
    for (name, m, edges) in POP[:_sage_const_40 ]:
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        co = cleared_coeffs(E_of(D, ell, mu_content(D, ell)))
        for M in range(_sage_const_1 , _sage_const_4 ):
            for (a, b) in P1_reps(ell, M):
                meas = collapsed_measure(co, ell, a, b)
                if not meas:
                    continue
                maxdepth = max(maxdepth, ZZ(sep_of(meas, ell)))
print("      走査範囲での sep の最大値 =", maxdepth, "（再帰深さの上界）")
check(maxdepth < _sage_const_40 , "sep が異常に大きい（再帰が止まらない疑い）")

print("  G4: 「定理 L4 は E が消える点を黙って飛ばしていないか」")
nz = _sage_const_0 
for ell in [_sage_const_2 , _sage_const_3 ]:
    for (name, m, edges) in POP[:_sage_const_40 ]:
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        mu = mu_content(D, ell)
        co = cleared_coeffs(E_of(D, ell, mu))
        for M in range(_sage_const_1 , _sage_const_3 ):
            for (a, b) in P1_reps(ell, M):
                (v, _) = hat_theta_resultant(co, ell, M, a, b)
                d = delta_measured(D, ell, mu, M, a, b)
                if v is None:
                    nz += _sage_const_1 
                    check(d is None, "L4 が None なのに円分体では有限 ell=%d %s M=%d (%s,%s)" % (ell, name, M, a, b))
                else:
                    check(d is not None, "L4 が有限なのに円分体では 0 ell=%d %s M=%d (%s,%s)" % (ell, name, M, a, b))
print("      E(zeta^a,zeta^b) = 0 の点は %d 個（両者で一致）" % nz)

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("Step H: 系 L3′（S_oo = 空のときの一様な上界。系 J3 の有効版）", el())
print("=" * _sage_const_78 )
print("""
  系 J3 は「theta が至る所有限なら有界」を P^1(Z_ell) のコンパクト性で示していた（非構成的）。
  系 L3′ はそれを D の係数から計算できる明示的な上界に置き換える:
    候補点 u in U（系 J10）すべてで theta(u) < oo なら
      theta^max <= max( max_u theta(u),  ell^{g^max + L0} - 1 ),  ell^{L0} >= max_u theta(u)。
  照合: 母集団のうち S_oo = 空の塔について、レベルを尽くした theta の最大値が
        この上界以下であることを確認する（内訳つき）。
""")
H_BUDGET = _sage_const_120p0 
for ell in [_sage_const_2 , _sage_const_3 , _sage_const_5 ]:
    t0 = time.time()
    ntower = nok = _sage_const_0 
    nsinf = _sage_const_0 
    nleft = _sage_const_0 
    slack = []
    for (name, m, edges) in POP:
        if time.time() - t0 > H_BUDGET:
            nleft += _sage_const_1 
            continue
        D = detL(m, edges)
        if D == _sage_const_0 :
            continue
        co = cleared_coeffs(E_of(D, ell, mu_content(D, ell)))
        bd = L3prime_bound(co, ell)
        if bd is None:
            nsinf += _sage_const_1 
            continue
        (bound, tmax, L0, gm) = bd
        ntower += _sage_const_1 
        obs = _sage_const_0 
        for M in range(_sage_const_1 , (_sage_const_4  if ell == _sage_const_2  else _sage_const_3 ) + _sage_const_1 ):
            for (a, b) in P1_reps(ell, M):
                th = theta_L1(co, ell, a, b)
                if th is oo:
                    check(False, "系 L3′ の前提が破れている ell=%d %s (%s,%s)" % (ell, name, a, b))
                    continue
                obs = max(obs, ZZ(th))
        if obs <= bound:
            nok += _sage_const_1 
        else:
            check(False, "系 L3′ 破れ ell=%d %s: 観測 %s > 上界 %s" % (ell, name, obs, bound))
        slack.append((ZZ(bound), obs))
    if nleft:
        TRUNC.append("Step H ell=%d: 壁時計 %.0fs 超過のため %d 個を未実施" % (ell, H_BUDGET, nleft))
    print("  ell=%-2d : S_oo = 空の塔 %d 個（うち上界成立 %d）/ S_oo が空でない塔 %d 個 / 未実施 %d 個"
          % (ell, ntower, nok, nsinf, nleft))
    if slack:
        print("           上界の緩さ（上界 / 観測最大）の中央値: %s / %s"
              % (sorted(b for (b, o) in slack)[len(slack)//_sage_const_2 ],
                 sorted(o for (b, o) in slack)[len(slack)//_sage_const_2 ]))

# ==========================================================================
print()
print("=" * _sage_const_78 )
print("まとめ", el())
print("=" * _sage_const_78 )
print("  FAIL 件数 =", FAIL)
print("  打ち切った計算 =", len(TRUNC), "件")
for t in TRUNC:
    print("    -", t)
print("  総所要 = %.1f 秒" % (time.time() - T_START))

