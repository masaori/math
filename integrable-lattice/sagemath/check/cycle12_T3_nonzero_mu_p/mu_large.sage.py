

# This file was *autogenerated* from the file mu_large.sage
from sage.all_cmdline import *   # import sage library

_sage_const_2 = Integer(2); _sage_const_1 = Integer(1); _sage_const_0 = Integer(0); _sage_const_4 = Integer(4); _sage_const_6 = Integer(6); _sage_const_3 = Integer(3); _sage_const_5 = Integer(5); _sage_const_78 = Integer(78)# cycle 12 / T3 Pure: μ_2 をどこまで大きくできるかの探索（2 頂点底グラフ, 広めの範囲）。
# 判定基準 (☆) μ_ℓ = v_ℓ(content_z(det L(z)))。辺重複度 gcd = 1 のもの（＝ℓ 重多重グラフ
# ではない非自明例）だけを「非自明」として集計する。

R = LaurentPolynomialRing(ZZ, 'z')
z = R.gen()
from itertools import combinations_with_replacement
from collections import Counter

def det_and_content(edges):
    L = matrix(R, _sage_const_2 , _sage_const_2 )
    for (u, v, a) in edges:
        if u == v:
            L[u, u] += _sage_const_2  - z**a - z**(-a)
        else:
            L[u, u] += _sage_const_1 ; L[v, v] += _sage_const_1 
            L[u, v] -= z**a; L[v, u] -= z**(-a)
    D = det(L)
    if D == _sage_const_0 :
        return D, _sage_const_0 
    return D, gcd([ZZ(c) for c in D.coefficients()])

def mult_gcd(edges):
    c = Counter(tuple(sorted((u, v))) + (a,) for (u, v, a) in edges)
    return gcd(list(c.values()))

VMAX = _sage_const_4           # 平行辺 voltage は 0..VMAX
KMAX = _sage_const_6           # 平行辺の本数 ≤ KMAX
LOOPV = [_sage_const_1 , _sage_const_2 , _sage_const_3 ] # ループ voltage
LMAX = _sage_const_3           # 各頂点のループ本数 ≤ LMAX

loopsets = []
for k in range(_sage_const_0 , LMAX + _sage_const_1 ):
    for B in combinations_with_replacement(LOOPV, k):
        loopsets.append(B)

best = {}
total = _sage_const_0 
for ka in range(_sage_const_1 , KMAX + _sage_const_1 ):
    for A in combinations_with_replacement(range(_sage_const_0 , VMAX + _sage_const_1 ), ka):
        if A[_sage_const_0 ] != _sage_const_0 :      # 平行辺 voltage の一斉平行移動で det は不変 → min A = 0 に正規化
            continue
        for B in loopsets:
            for C in loopsets:
                edges = [(_sage_const_0 , _sage_const_1 , a) for a in A] + [(_sage_const_0 , _sage_const_0 , b) for b in B] + [(_sage_const_1 , _sage_const_1 , b) for b in C]
                D, cont = det_and_content(edges)
                if cont == _sage_const_0 :
                    continue
                total += _sage_const_1 
                mg = mult_gcd(edges)
                for p in [_sage_const_2 , _sage_const_3 , _sage_const_5 ]:
                    v = ZZ(cont).valuation(p)
                    if v > _sage_const_0 :
                        key = (p, mg == _sage_const_1 )
                        if key not in best or v > best[key][_sage_const_0 ]:
                            best[key] = (v, A, B, C, cont, D)

print("=" * _sage_const_78 )
print("μ_ℓ の最大値探索（2 頂点底グラフ）")
print(f"探索範囲: 平行辺 voltage ⊂ {{0..{VMAX}}} 本数 ≤ {KMAX}（min=0 正規化）, ")
print(f"          各頂点のループ voltage ⊂ {LOOPV} 本数 ≤ {LMAX}")
print(f"検査した底グラフ数: {total}")
print("=" * _sage_const_78 )
for p in [_sage_const_2 , _sage_const_3 , _sage_const_5 ]:
    for nontrivial in [True, False]:
        key = (p, nontrivial)
        tag = "非自明(辺重複度gcd=1)" if nontrivial else "自明含む(辺重複度gcd>1)"
        if key not in best:
            print(f"\nℓ={p} / {tag}: 該当なし")
            continue
        v, A, B, C, cont, D = best[key]
        print(f"\nℓ={p} / {tag}: 最大 μ_{p} = {v}")
        print(f"  A(平行辺 voltage)={A}, loop@0={B}, loop@1={C}")
        print(f"  det L(z) = {D}")
        print(f"  content = {factor(cont)}")

print("\n" + "=" * _sage_const_78 )
print("正直な注記: これは上記の有限探索範囲内での最大値であり、μ_2 が有界であることの")
print("根拠ではない。範囲を広げれば大きな μ が出うる（探索範囲は上に明示）。")
print("=" * _sage_const_78 )

