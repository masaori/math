# cycle 26 / T3 Pure: 持ち越し未検証 2 件 — 実行結果

対応する証明本体: [`outputs/reports/cycle26_T3_carryover_verifications.md`](../../../outputs/reports/cycle26_T3_carryover_verifications.md)。
対象ラベルは [overview.md](overview.md)、手順と限界は [README.md](README.md)。

実行日: 2026-08-02。生ログは `carryover.out`。

## 総括

| スクリプト | Step | FAIL | 打ち切り | 実測所要 |
|---|---|---|---|---|
| `carryover.sage` | A–D | **0** | **0** | 419.0 s |

母集団 133 塔 $\times\ \ell\in\{2,3\}$、**有効 191 組**。
$T_\mathrm{def}=0$ が **108**、$\ne0$ が **83**——**cycle 22 Step L の実測と一致**（母集団が同一であることの確認）。

## Step A — $\delta_M$ の実測

| 区分 | 件数 |
|---|---|
| $T_\mathrm{def}=0$ | **108** |
| うち $\delta_M$ が全て $0$（$(1.1)$ が全 $n\ge0$ で成り立つ） | **108** |
| うち総和だけ $0$（$n=0$ では成り立つが $n\ge1$ は不明） | **0** |
| $\delta_M$ の符号が混ざる塔 | **47** |
| そのうち $T_\mathrm{def}=0$ のもの | **0** |

符号が混ざる塔の例（$\ell=2$、$M^*=4$）: `BQ3 (1,0),(1,0),(0,1)` で $\delta=(-1,2,0)$、$T_\mathrm{def}=1$。

$\sum_M\delta_M=T_\mathrm{def}$ が全 191 組で成り立つことも同時に確認（定義の一致。破れ 0 件）。

## Step B — レベルごとの判定 vs 塔の値（Matrix–Tree）

| 内訳 | 件数 |
|---|---|
| 照合 | **860** |
| 判定と実測の食い違い | **0** |
| 判定「一致する」かつ実測も一致 | 721 |
| 判定「一致しない」かつ実測も不一致 | **139** |
| 除外（$\kappa_n=0$ 等） | 0 |

**139 件は「判定が空振りでないこと」の証拠である。** 全 860 件の 16% で $(1.1)$ は成り立っておらず、
判定はそれを 1 件残らず当てている。

## Step C — 注 3.1 の帰結（$n=0$ の同値）

| 内訳 | 件数 |
|---|---|
| 同値を確認した塔 | **191** |
| 破れ | **0** |
| うち $T_\mathrm{def}=0$ で実際に $(1.1)$ が $n=0$ で成り立った塔 | **108** |

## Step D — $\ell=2$ トーラス（cycle 21 §6.3）

$(a,b,c,d,e)=(0,2,4,-6,-1)$、$\delta_M=(3,0,0)$、$T_\mathrm{def}=3$、$M^*=4$。

| $n$ | $S(n)$ | 判定 | $\mathrm{ord}_2(\kappa_n)$ | $(1.1)$ | 実測 | DuBose–Vallières |
|---|---|---|---|---|---|---|
| 0 | 0 | 一致しない | 0 | 3 | 一致しない | — |
| 1 | 3 | 一致する | 5 | 5 | 一致する | 5 |
| 2 | 3 | 一致する | 19 | 19 | 一致する | 19 |
| 3 | 3 | 一致する | 61 | 61 | 一致する | 61 |
| 4 | 3 | 一致する | 167 | 167 | 一致する | 167 |

**成立開始レベル $n=1$**（機械が実測から決めた値。$1$ でなければ FAIL になる）。

## 検証中に自分の期待が誤っていた箇所（初回実行で FAIL 2 件）

初回実行は FAIL 2 件で終わった。**どちらも検証コードに書いた私の期待が誤っており、
理論のほうは正しかった。** 是正は期待を原典に合わせる方向で行った（検証を緩めていない）。

1. $(1.1)$ の $a$ を $1$ と書いた（正しくは $0$）。cycle 21 §6.3 の $\alpha=1$ との取り違え。
   §6.3 が印字する閉形式 $2n2^n+4\cdot2^n-6n-1$ に $2^{2n}$ の項が無いので $a=0$ である。
2. $n=0$ で $(1.1)$ が成り立つことを無条件に期待した。この塔は $T_\mathrm{def}=3\ne0$ なので、
   **定理 D2 は $n=0$ で成り立たないことを予言している**。

詳細は証明本体 §「私が犯した誤り」。
