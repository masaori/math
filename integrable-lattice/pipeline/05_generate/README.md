# 05_generate

## 目的

空白セルから未解決ステートメント候補を生成する。

## テンプレート

- `X`模型の`Y`境界条件において、transfer matrix family は可換か。
- `X`模型の分配関数は determinant/Pfaffian 公式を持つか。
- `X`模型の有限サイズ character は `Y` q-series と一致するか。
- `X`の fusion hierarchy は `Y`条件下で閉じるか。

